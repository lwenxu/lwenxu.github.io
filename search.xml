<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>AE 技巧</title>
    <url>/2019/10/27/Photo/AE%20%E6%8A%80%E5%B7%A7%E5%AD%A6%E4%B9%A0/</url>
    <content><![CDATA[<h2 id="TypeMonkey"><a href="#TypeMonkey" class="headerlink" title="TypeMonkey"></a>TypeMonkey</h2><p>这个脚本是用来生成一个文字的动画的，就是文字的闪出，我们经常看到的一些黑底白字的一些打电话的视频</p>
]]></content>
      <categories>
        <category>视频后期</category>
      </categories>
      <tags>
        <tag>视频后期</tag>
      </tags>
  </entry>
  <entry>
    <title>PR 技巧</title>
    <url>/2019/10/27/Photo/PR%20%E6%8A%80%E5%B7%A7/</url>
    <content><![CDATA[<h2 id="基本的工具"><a href="#基本的工具" class="headerlink" title="基本的工具"></a>基本的工具</h2><ol>
<li>波纹剪辑工具 B，在剪辑前一段素材的时候能够看到后一段素材第一帧的样子</li>
<li>滚动编辑工具和波纹剪辑工具类似 N ，但是影响的是两段素材，也就是增加第一段长度后面第二端也在减少，相当于把第二段删掉了一些 </li>
<li>比率拉伸工具就是快慢镜头，快慢放一个东西</li>
<li>内滑：保证时间线长度不变 ，被鼠标选中的素材长度不变但是这个素材两侧的素材的入点和出点被改变</li>
<li>外滑：保证鼠标选中的这个素材的出点和入点不变素材在上边滑动 也就是说截取的素材的总长度是固定的就是素材需要展示的片段不一样而已</li>
</ol>
<h2 id="视频导入导出格式"><a href="#视频导入导出格式" class="headerlink" title="视频导入导出格式"></a>视频导入导出格式</h2><img src="http://images.heniankj.com/20191027212942.png">



<h2 id="PR-批处理技巧"><a href="#PR-批处理技巧" class="headerlink" title="PR 批处理技巧"></a>PR 批处理技巧</h2><ol>
<li><p>对于效果的重复应用可以 ctrl+c 赋值效果，然后到 master 上粘贴效果  那么对于同一个素材的所有的片段都会被加上统一效果<img src="http://images.heniankj.com/20191027224020.png"></p>
</li>
<li></li>
<li><p>对于一些效果我们希望给到另外一些素材，注意跨素材的方式上面的技巧就不适用了，我们使用 ctrl+c 复制，然后使用 ctrl+alt+v 粘贴效果</p>
</li>
<li><p>对于音频则是可以多个轨道批处理，首先选择音轨混合器<img src="http://images.heniankj.com/20191027224527.png"><img src="http://images.heniankj.com/20191027224558.png"><img src="/Users/lwen/Blog/source/images/image-20191027224627607.png" alt="image-20191027224627607">立体声子轨道，然后需要的一起调整的轨道放到一个子混合里面直接调整子混合就 ok 了</p>
<p><img src="/Users/lwen/Blog/source/images/image-20191027224715219.png" alt="image-20191027224715219"></p>
</li>
<li></li>
</ol>
<h2 id="快捷键"><a href="#快捷键" class="headerlink" title="快捷键"></a>快捷键</h2><p>添加视频&amp;音频转场 shift+D<br>单独添加视频转场 Ctrl+D<br>单独添加音频转 Ctrl+shift+D<br>切分素材 Ctrl+K<br>波纹剪辑 Q&amp;W<br>延长剪辑至时间轴 Shift+Q&amp;W<br>匹配帧 F<br>Shift+R 反向匹配帧<br>(反斜杠) 快速缩放<br>ctrl+(反斜杠） 隐藏项目栏<br>lt+方向键 移动素材（需要激活轨道）<br>Alt+Shift+方向键 快速移动素材<br>I/O 设置工作区 ；删除选区<br>’ 抽取片段<br>ctrl+加减号 缩放轨道宽度<br>shift+加减号 扩展/缩小所有轨道宽度<br>shift+123 快速切换窗口</p>
<h2 id="Luts"><a href="#Luts" class="headerlink" title="Luts"></a>Luts</h2><p>这个东西的全名是 LookUpTable 也就是将画面中的颜色批量的替换成另外的颜色，Luts 大致分为两类，矫正 Luts 和风格 Luts ，一般我们会在添加 Luts 之前对画面的颜色进行调整，这样的效果是会被 Luts 再进行二次的映射，所以总的效果是在 Luts 的风格之下的。那么另外的就是如果在 Luts 之后调整就很容易突破 Luts 的限定，所以根据需求在添加 Luts 前后进行颜色风格的调整。</p>
<p>一般的我们可以建立一个控制调整图层来应用 Luts 然后整个的格调是统一的，然后我们分着对每一部分进行调整。</p>
<p>还有我们需要将所有的 Luts 导入到 PR 中避免每次都需要一个个选择。我们直接在 PR 中就可以预览我们的 Luts</p>
<p>最后我们调整好的 Luts 还可以直接导出 cube 然后给其他人用。</p>
<h2 id="脱机文件"><a href="#脱机文件" class="headerlink" title="脱机文件"></a>脱机文件</h2><p>在我们的文件名在系统中修改了以后 pr 找不到对应的文件，然后就显示脱机，我们可以使用查找然后手动关联这些更改后的文件。</p>
<h2 id="多个素材顺序自动插入"><a href="#多个素材顺序自动插入" class="headerlink" title="多个素材顺序自动插入"></a>多个素材顺序自动插入</h2><p>首先选择多个素材，然后点击自动序列匹配</p>
<img src="http://images.heniankj.com/20191028234148.png">



<h2 id="无间隔删除"><a href="#无间隔删除" class="headerlink" title="无间隔删除"></a>无间隔删除</h2><p>shift+delete 删除素材以后后面的素材会跟上来</p>
<h2 id="提升和提取"><a href="#提升和提取" class="headerlink" title="提升和提取"></a>提升和提取</h2><p>提升就是把这段选中的出入点之间的素材删除掉，后面的素材不跟上来</p>
<p>提取则是删除并且后面的素材跟上</p>
<p><strong>注意一点就是我们所有的操作在只会被应用到指定的轨道上面，如果那个轨道没有被激活则不会对他生效。</strong></p>
<p>在我们选择提升的时候提升操作只会被作用域我们激活的视频或者音频轨道，例如这里只激活了音频关闭了视频。</p>
<img src="http://images.heniankj.com/20191028235606.png">

<p>假如我们使用提取的话就不一样了，视频和音频都会被干掉抽出，因为这个时候视频音频同步锁定的。我们取消视频和音频的同步锁定就好了。可以看到后面有因为去掉了音频的时间差。</p>
<img src="http://images.heniankj.com/20191028235845.png">

<h2 id="插入素材"><a href="#插入素材" class="headerlink" title="插入素材"></a>插入素材</h2><p>我们按住 ctrl 键+ 拖入素材素材会被插入到以前的素材中，而不会覆盖以前的素材</p>
<h2 id="滚动条放大"><a href="#滚动条放大" class="headerlink" title="滚动条放大"></a>滚动条放大</h2><p>选择一个面板使用 +/- 可以放大滚动条和缩小</p>
<p>使用 ctrl+ +/- 可以放大视频轨道</p>
<p>使用连续预览视频就可以看到更多的帧照片了。</p>
<img src="http://images.heniankj.com/20191029000554.png">

<img src="http://images.heniankj.com/20191029000637.png">

<h2 id="默认插入素材的轨道"><a href="#默认插入素材的轨道" class="headerlink" title="默认插入素材的轨道"></a>默认插入素材的轨道</h2><img src="http://images.heniankj.com/20191029000735.png">

<p>在最左边有两个 a 和 v 只能激活一个视频一个音频所以这个就是插入的时候默认的一个轨道</p>
<h2 id="子剪辑"><a href="#子剪辑" class="headerlink" title="子剪辑"></a>子剪辑</h2><p>可以理解为素材中截取一段作为一个新的素材，我们选择入点和出点然后直接拖动到素材箱中就生成了一个子剪辑，但是这个的子剪辑名字所有的信息和原素材一样，而且在时间线上可以看到他有一个小三角也就是他的长度被限定了，我们希望生成子剪辑的时候限定一下名称和是否裁剪可以按 ctrl+拖动素材</p>
<img src="http://images.heniankj.com/20191029001602.png">

<p>取消修建的话在时间线上我们可以修改出入点的位置然后拖动而选中后就不行了，被裁减后的视频会有一个小三脚</p>
<h2 id="标记"><a href="#标记" class="headerlink" title="标记"></a>标记</h2><p>在时间线上打的标记就在时间线上然后在素材监视器中的标记就是素材上的。都可以用 M 来做标记</p>
<p>可以切换到标记面板直接查看我们标记点详细信息</p>
<img src="http://images.heniankj.com/20191029002124.png">]]></content>
      <categories>
        <category>视频后期</category>
      </categories>
      <tags>
        <tag>视频后期</tag>
      </tags>
  </entry>
  <entry>
    <title>视频拍摄技巧杂记</title>
    <url>/2019/10/27/Photo/%E6%9A%82%E6%97%B6%E6%95%B4%E7%90%86%E7%9A%84%E6%9D%82%E8%AE%B0/</url>
    <content><![CDATA[<h2 id="切镜"><a href="#切镜" class="headerlink" title="切镜"></a>切镜</h2><h4 id="1-跳切"><a href="#1-跳切" class="headerlink" title="1.跳切"></a>1.跳切</h4><p>人的动作不断的跳动，然后中间有一结是没有了 这就是被跳切了</p>
<h4 id="2-贴合切"><a href="#2-贴合切" class="headerlink" title="2.贴合切"></a>2.贴合切</h4><p>将人物的一个动作和另外一个细节动作相贴合  比如一开始人走过来  然后细节到 脚上  最后又到人走到镜头跟前。中间的细节部分就是我们的表现主体的一个时间和空间的位移 。</p>
<h4 id="3-离切"><a href="#3-离切" class="headerlink" title="3.离切"></a>3.离切</h4><p>和贴合切一样，但是不一样的是中间的细节转换成另外一个表现主体的一个画面，比如移动到一个鸟上。</p>
<h2 id="相机的基本参数调整"><a href="#相机的基本参数调整" class="headerlink" title="相机的基本参数调整"></a>相机的基本参数调整</h2><ol>
<li>对于曝光，我们可以使用曝光直方图来进行曝光检测，直方图的横向就是 0-255 纵向就是像素个数。我们最完美的曝光是能够让直方图均匀分布，并且两遍的位置都处于差点过爆差点欠爆的状态。除此之外外我们还可以使用斑马纹，对于过爆的地方就会显示出斑马条纹，提醒我们进行曝光减了。我们的曝光规则并不是宁欠勿过，而是宁右勿左。</li>
<li>对于光圈就是我们镜头里面的几个小扇叶，我们看到的 f1.4 f2 这类的参数就是光圈了，这个数值越大表示光圈越小，那么越小的话就意味更少的进量光整个画面也会更加的暗。因此我们可以通过调节光圈拍摄暗场景。同时光圈还决定了景深，光圈越大景深越浅，那么我们对焦点前后清晰区间就更小。会产生一个背景虚化的效果。但是注意光圈太大太小都不好。另外为什么是 f1.4 的下一档 f 2 因为光圈的计算公式是 <code>光圈 F 值=焦距/光圈直径</code> 而透光面积是 pi x r2 所以是和平方的倒数一致 ，那么 1.4x1.4 =1.9  2*2=4 所以是两档的区别</li>
<li>视频和拍照的快门速度，这个东西一般都是  1/50 这个东西因为在交流电的光下如果你的快门速度超过或者小于他就会导致频闪，可以调高快门获得额外的曝光，避免频闪就可以使用自然光或者打光</li>
<li>ISO 他就是放大电路的值，所以会将所有的值放大，那么噪点自然就有了，而我们需要注意的就是这个东西并不是越低越好，也不是越高越好，因为有一个默认的 ISO 值，在默认的情况下他的画面是最纯净的，通常拍摄夜景的时候会把 ISO 放在光圈快门之后来获得最纯净的画面。</li>
</ol>
]]></content>
      <categories>
        <category>视频后期</category>
      </categories>
      <tags>
        <tag>视频后期</tag>
      </tags>
  </entry>
  <entry>
    <title>九月份 Review：「遇见一群有意思的人」</title>
    <url>/2019/09/28/Life/%E4%B9%9D%E6%9C%88%E4%BB%BD%20review/</url>
    <content><![CDATA[<p>​    在车上看着路边飘扬着的红旗，才意识到九月就要结束了。这一个月发生了很多难过、开心和有趣的事情，想通过这篇文章总结一下，或者说记录这些美好，以后的某一天再看这篇文章依旧能够嘴角上扬。</p>
<h2 id="应接不暇的需求"><a href="#应接不暇的需求" class="headerlink" title="应接不暇的需求"></a>应接不暇的需求</h2><p>​    从入职到九月将近一个月的时间，这一个月基本在熟悉流程用了一两个需求熟悉开发流程和集团中间件。接下来就是 <code>99 大促</code> 和 <code>双11</code> 需求的支持和稳定性保障。</p>
<p>​    这次 <code>99大促章鱼狂欢城</code>，是我第一次作为开发参与 S 级大促系统开发和稳定性保障，整个过程充满了挑战和艰辛，同时暴漏出自身大量的问题和不足，在技术、业务、思考方式等方面都有不同程度的纰漏和待改进的点。</p>
<h3 id="技术"><a href="#技术" class="headerlink" title="技术"></a>技术</h3><p>​    每次大促都是排期短需求多，所以对于开发速度上有很高的要求，并且对于频繁变化的需求也要有相应的心里准备和代码框架设计，对可能变化的节点预测，留出足够的扩展点。</p>
<h4 id="关于轮子"><a href="#关于轮子" class="headerlink" title="关于轮子"></a>关于轮子</h4><p>​    在这次开发过程中有很明显的感触到如果很多代码不做抽象，下次需要用到相似的功能只能拷贝以前的代码做修改，代码中会出现大量的冗余，而且必须要一行行的理解别人的代码，有时候会觉得还不如自己写来的省事。但是注意一旦意识到这部分本来是可复用、可抽象、可被封装，那么这部分代码一定要有充分的设计，让这部分轮子具有较好的扩展性、健壮性以及易用性，否则你只是在逼迫下一个人必须造轮子，下一个看到你代码的人就是你现在的处境。既然花了时间投入到轮子的开发就应该产出优质的通用的代码。</p>
<p>​    举个简单的例子，对于常常使用的 Excel 导出，我们通常会将 MapList 或者 BeanList 作为数据源，那么对于 BeanList 这种规整的结构使用注解驱动最合适不过了，后续的使用只需在 Bean 上写对应的注解。而 MapList 应该也有对应的工具将数据和列名绑定，最后我们只需要关心列信息。但事实上很多人都有使用到导出 Excel 功能，但是这些代码都分散在各个业务代码当中，一方面破坏了业务流程的完整性，另外没有可复用性。</p>
<h4 id="代码规范"><a href="#代码规范" class="headerlink" title="代码规范"></a>代码规范</h4><p>​    看了项目中的很多代码，很少有人能写出自表达的代码，一方面是因为历史包袱，前面没有设计，而业务快速迭代导致后人只能加判断条件在原有代码上堆叠。另一方面需求多任务重时间紧，所以没有抽象只想着尽快实现功能尽快上线。而后者常常就是导致前者出现的原因。</p>
<p>​    在互联网公司，快速迭代是最基本的要求，毕竟国内互联网公司不像外企对代码质量进行严格把控，项目时间也拖的很长。行业现状很难会有变化，而开发人员在短期内很难把自身水准提升到能写出自表达代码，所以目前最行至有效的解法是 <code>注释</code> + <code>规范</code> 对于复杂的业务逻辑尽量多用一些注释进行说明业务框架和思路，另外严格遵守 《阿里巴巴编码规范》中提到的点，保持代码风格统一。</p>
<h4 id="效率"><a href="#效率" class="headerlink" title="效率"></a>效率</h4><p>​    在编码过程中，经常为了快速响应需求解决问题，看完 PRD 就开始 coding ，往往后来会发现这样一味的堆代码会导致很多问题变得难解，或者写出来的代码质量很差，有种拆东墙补西墙的感觉。</p>
<p>​    所以理解和思考完需求后的第一件事应该是打草稿，画出业务流图，设计业务框架，明晰如何组织包和类，对于流程中的核心点算法如何实现，可以采用哪些设计模式明晰业务结构，可以使用哪些轮子简化开发等等……</p>
<p>​    然后进行快速编码阶段，编码需要保证精力集中，避免因为被打断出现思维断线造成的 bug ，编码结束后使用静态检查工具，扫描可能存在的问题。尽量一次编码跑通，不要反复修改，减少部署系统所花费的时间。</p>
<h4 id="后期规划"><a href="#后期规划" class="headerlink" title="后期规划"></a>后期规划</h4><p>​    看的代码太少，只有看的多了才会知道好代码是什么样子，才会明白原来这些地方有更好的设计。后面保持阅读优秀的二方包和集团中间件。目前只看了 <code>Pandora</code> 源码，总结成了 ATA ，后面计划整理 <code>Diamond</code> 和 <code>MateQ</code> 源码，整理笔记。另外再看一遍 <code>Pandora</code> 主要看框架的设计，而非流程。</p>
<p><img src="http://images.heniankj.com/20190928231808.png" alt></p>
<p>​    以前的好习惯不能丢，对于出现的错误要勤于记录，错误原因 -&gt; 排查思路 -&gt; 耗时 -&gt; 排查经验 -&gt; 避免方案 给思维打补丁，让自己逻辑更缜密，对于一些常见报错，快速 troubleshooting 。 可以把这写问题记录都放到 csdn 上，形成自己的问题引擎。</p>
<p><img src="http://images.heniankj.com/20190928232238.png" alt></p>
<p>​    要保持对开源项目的接触和学习，来集团以后感觉和外部的技术脱节了，99 % 技术都是集团内部中间件，很少使用到新技术，保持对新技术的学习保持对技术的敏感度。</p>
<p><img src="http://images.heniankj.com/20190928232959.png" alt></p>
<p>​    <a href="http://lwenxu.coding.me/archives/">个人博客 </a>好久没更新了 ，最新的一篇是在入职前写的，最近忙了也变懒了。保持输出，技术，思考，感想。保持每个月两篇博客。</p>
<p><img src="http://images.heniankj.com/20190928231531.png" alt></p>
<h3 id="业务"><a href="#业务" class="headerlink" title="业务"></a>业务</h3><h4 id="平台业务"><a href="#平台业务" class="headerlink" title="平台业务"></a>平台业务</h4><p>​    对于招商部分的业务不够了解，本来的预期是熟悉营销工作台中常见的功能，对于其中的一些专有名词也要熟知含义，理解系统中的业务层次，画出大致的结构图。重要的是每部分业务应该自己创建测试然后尝试跑通一个流程，并且要和代码对应上，这是一个长期的工作内容，因为把这个系统搞清楚是一件比较困难的事情，系统的复杂度和人员参与度都太高，以及额外的 DTS Job ，只能先一点点的摸索着部分业务和代码，归纳总结系统架构，熟悉大家的代码风格，和已经存在的轮子，至少要有些印象避免重复的轮子。</p>
<h4 id="数据中心"><a href="#数据中心" class="headerlink" title="数据中心"></a>数据中心</h4><p>​    最近将近两个月的时间一直在做数据中心的建设工作，主要对阿里表格活动现场重写，还有双十一数据看板。为了以后更多的数据产品的出现应该有更详细的框架设计，沉淀系统快速响应能力，配置化能力，自动化能力。另外需要与前端打通，产出更多的业务组件与约定。</p>
<h4 id="只见树木，不见森林"><a href="#只见树木，不见森林" class="headerlink" title="只见树木，不见森林"></a>只见树木，不见森林</h4><p>​    大部分时间都被埋没在需求和修复问题中，很难看到自己所负责模块的全貌，对于部分熟悉但却不明白这部分的所处的位置和意义，这也是因为对于整体业务不够熟悉对平台的现状不能很好的把握导致的。这部分欠缺很难在短时间内得到填补，需要长时间沉淀和对业务的持续思考，才会有质的变化。</p>
<p>​    看书对形成电商思维和框架是一种简单而行之有效的方式，多看看马老师写的书，多关注电商领域的行业动态。</p>
<h2 id="百年阿里"><a href="#百年阿里" class="headerlink" title="百年阿里"></a>百年阿里</h2><h3 id="紧张-amp-期待"><a href="#紧张-amp-期待" class="headerlink" title="紧张&amp;期待"></a>紧张&amp;期待</h3><p>​    很明白自己不太善于沟通，所以对这次百阿之旅带着紧张，对于快融入团队并不是很有信心。但是也很期待，期待够能突破自己，发现自己新的可能，简单来说变得更 Open ，认识一些小伙伴。令人欣喜的是，这份期待没有被辜负～</p>
<h3 id="缘分"><a href="#缘分" class="headerlink" title="缘分"></a>缘分</h3><p>​    缘分真的是一个很神奇的东西，一群来自不同城市、不同部门、不同年龄的人组成了这么温暖、团结、活力的 1227 百阿班。很幸运来到 1227 ，很幸运来到 <code>三生万物</code> ，很幸运遇到你们，遇见新的自己 ～</p>
<p>​    感谢百阿让我遇到这么一群，有趣的、热情的、可爱的、灵动的、温情脉脉的人。</p>
<h3 id="破冰"><a href="#破冰" class="headerlink" title="破冰"></a>破冰</h3><p>​    一圈自我介绍后，其实我一个人名都没记住，虽然当时每个人自我介绍完后我都会重复好几遍，重复着重复着就忘了～原谅我的烂记性不是不想记住你们！！！不过最后认人名 PK 环节，我可是能叫出全班人的名字，得瑟一下。</p>
<p>​    印象比较深刻的是五个小游戏任务，大家在互相还不熟悉的情况下能够做到相互信任、团结一致、互相帮助，用智慧和合作共同创造价值，取得成果。很多环节很辛苦也充满了困难，但我们在短时间内形成的超强凝聚力和执行力，让团队势如破竹，朝着目标冲刺。在这个过程中出现了很多小彩蛋，在 “达芬奇密码” 环节大家看完规则你一句我一句着的说着游戏规则，我还没听懂规则是啥，天相突然跳出来说了一个听起来很牛逼的调度算法，目瞪狗呆。又是一个算法怪兽，规则看完调度算法就出来了，感觉智商受到了降维打击….. </p>
<p>​    由于算法对于调度者依赖性太高，对调度内存是一个很大的考验，为了保险起见我们采用了两趟遍历，虽然中间出了小插曲，导致牌序混乱。大家都一脸懵逼的时候，天相挺身而出力挽狂澜，终于体会到什么叫做：<strong>“此时此刻，非我莫属！”</strong>。</p>
<div style="display:flex">
    <div style="text-align: center"><img src="http://images.heniankj.com/1569155252878_IMG_3464.jpg">游戏任务</div>
  <div style="text-align: center"><img src="http://images.heniankj.com/1569155253214_IMG_7015.jpg">十只脚落地</div>
  <div style="text-align: center"><img src="http://images.heniankj.com/1569155252889_IMG_3473.jpg">安全部门合影</div>
</div>

<img src="http://images.heniankj.com/AF3301AB-955D-41A1-B605-8A5B726DBB2A.jpg">

<h3 id="凝聚"><a href="#凝聚" class="headerlink" title="凝聚"></a>凝聚</h3><p>经过第一天的破冰，第二天大家慢慢开始变得熟悉起来，尤其是在这次的土话的探寻中，团队更加凝聚。第一次见到阿里土话是在公司的大屏上：“此时此刻，非我莫属” 这是我记得最清楚的一句，也是最感动我的一句。</p>
<p>因为信任，所以简单。整个探寻任务从分工到执行只在很短的时间，没有过多的担心某个环节出问题了怎么办？某些子任务完不成怎么办？我们相信大家都能够把自己的那一部分做的很精彩，都很自觉的专注于自己的部分，相信伙伴，一切变得简单起来。</p>
<p>一点多才开始剪视频，又感受了一把速度与激情，有一种五点前赶发布的感觉。既要冷静又要快速响应，在大家的共同努力下，开讲前视频渲染结束。</p>
<p>晚上一起去庆功呀～ 5号食堂！</p>
<img src="http://images.heniankj.com/1569155296161_IMG_3511.jpg" width="700px" height="500px">

<p>结束后，弦柱在群里说了一句，“今天聊的很开心，好像又回到了学生时代！”对的！就是这种感觉，好放松～</p>
<h3 id="变化"><a href="#变化" class="headerlink" title="变化"></a>变化</h3><p>昨天还在一块团队任务，马上就要因为另外的任务分开。<strong>“拥抱变化”</strong> ，变化来的如此之快。在弦柱的带领下我们完成了对飞猪业务的探寻，让我重新认识了飞猪，她的历史，现状，困难与机遇。对于从来没接触过的 OTA 也有初步的了解。</p>
<p>在这个团队最大的感受就是有活力，因为我们的队长 — 弦柱（未来的杭州市旅游局局长） 就是一个很有活力的人！对团队也非常认真负责，带着我们去飞猪部门探索，采访！准备演讲到凌晨两点多，辛苦啦～</p>
<img src="http://images.heniankj.com/20190930120542.png">

<h3 id="夜谈"><a href="#夜谈" class="headerlink" title="夜谈"></a>夜谈</h3><p>对这个环节印象也非常深刻，每一个人的分享都好像是一本十分值的回味的书，透过他们的讲述读到不同的精彩故事。通过他们的精彩看到自身的不足，通过他们的失落看到逆境中的坚韧，每个人的经历都很有意思，可惜时间不够不然肯定整整一晚都在谈心交流。</p>
<p>不得不说十分佩服 泰伯，一个敢闯、敢拼、敢探索。知道自己想要什么，追求的是什么，并且付诸于行动，很少人能有这种想法和魄力。放一张大佬的照片，像不像香港扛把子 , haha~</p>
<img src="http://images.heniankj.com/lADPDgQ9rDNbRELND6DNF3A_6000_4000.jpg_620x10000q90g.jpg">

<h3 id="公益"><a href="#公益" class="headerlink" title="公益"></a>公益</h3><p>公益真的是一件帮助他人幸福自己的事情，在这个过程中能认识到自己的价值，也能为他人创造价值。公益能够感染身边的人，让更多的人以一颗柔软的心来对待这个世界。有很多同学反映在做公益的过程看到很多许多人的善举，就像马老师所说的要时刻保持爱，对社会、对自然、对身边的人，爱的力量小而强大。做一个内心温暖而强大的人。</p>
<p>公益后的 “总裁局” 也是让人十分放松和开心，好久没和这么多人一起开开心心吃吃喝喝玩玩了。<br><img src="http://images.heniankj.com/lADPDgQ9rCFOjyLNC9DND8A_4032_3024.jpg_620x10000q90.jpg"></p>
<h3 id="阿里"><a href="#阿里" class="headerlink" title="阿里"></a>阿里</h3><p>这就是阿里，以前我们会说：“这是阿里”，但是现在我们会告诉别人：“这就是阿里！”。因为现在的我们是一个不追求大，不追求强，我们追求成为一家活102年，截止到2036年，服务20亿消费者，创造1亿就业机会，帮助1000万家中小企业盈利的好公司。</p>
<p>对于新六脉神剑价值观，并不是为了改变我们的想法，或是束缚我们，而是在这个共识下选择同路人。价值观并非口号而是我们看到不论是少年阿里还是成年后的他一直都在用实际</p>
<p>另外对于年轻人，保持自己的好恶和棱角很重要，每个人都拥有对机会的选择权！</p>
<p>在阿里必须要遵守规则，在自身利益和公司里冲突的时候以公司利益优先，保证公司数据安全，不要做触碰公司红线的事情。</p>
<h3 id="“活着”"><a href="#“活着”" class="headerlink" title="“活着”"></a>“活着”</h3><p>来到阿里的第一年最重要的是 “活下来” ，因为作为一个新人很难融入到节奏走么快，业务难度这么高，新人培训少的团体。我们最重要的不是说想着如何实现集团的大目标，保证自己能够活下来，landing — 技术落地，业务落地。要明白自己生存在一个什么样的环境，去了解这个环境，适应这个环境。</p>
<p>而三到五年的沉淀，才能真正成为一个真正的阿里人，这个时候就需要面对更过的变化和更快的节奏，多从自身去思考问题。</p>
<h3 id="伙伴"><a href="#伙伴" class="headerlink" title="伙伴"></a>伙伴</h3><p>感谢在这次遇到的优秀的小伙伴们，因为我相信 无论我遇见谁，他/她都是我生命该出现的人，绝非偶然，他/她一定会教会你一些什么。</p>
<p>属七：感谢我的小天使，看得出来你是一个温暖的、热爱生活的人。从礼物的包装上，看得出你花了不少心思，真的非常感动。尤其是你的赠言充满了鼓励与期待，谢谢！你的字写的真的很棒～</p>
<p>弦柱(局长)：阳光、帅气、自信、具有少年感。跟他待在一块感觉自己又回到了大学校园，轻松愉悦！</p>
<p>余征：认真、严肃、细致。短短几个月就减掉 1/3 的体重的毅力，令人钦佩。</p>
<p>泰伯：敢闯、敢拼、有想法。看起来是一个很严肃的大佬，其实是很好相处的大佬！感谢鼓励陪伴！</p>
<p>拾月：博学、美丽、有气质。第一眼就被气场震撼到了，除了气质还有博学多识！</p>
<p>星吟：自律、美丽、脑洞大。每次不鸣则已，一鸣惊人，一句吐槽能让我笑一两分钟，苹果肌要坏掉了～</p>
<p>珺兮：聪明、文静、可爱。都是应届生相似点还蛮多的，也能说到一块去。可爱！有趣！</p>
<p>天相：自信、认真、有责任、有主见。令我印象深刻的不用一次性杯子盛可乐，因为不环保！！！</p>
<p>迪奇：聪明、好奇心、冷幽默。我们的数据中台，反应快，技术强，感觉内心也很萌哈哈，我们技术人的楷模。</p>
<p>久谙：幽默、有趣、情商高。思维太活跃了，想象力也超级棒，一句话带动全场气氛！</p>
<p>山魏：认真、戏精、沉稳、可靠。因为角色投入一炮而红，红遍大江南北！但是也非常的可靠，做事认真、结果导向～</p>
<p>顺序不分先后 ，按照 “总裁饭局” 次序，希望下次再相见，我们还能按照这个顺序坐在一起，聊天喝酒！</p>
<p>优秀的人大多会玩儿，幽默，会来事儿，正是因为这些特质，能够吸引并凝聚一波人，作为一个不太说话的程序员，应该培养锻炼自己让自己变的活跃，open！这次百阿是一个良好的开端。感谢认识你们，真的很开心～</p>
]]></content>
      <categories>
        <category>生活</category>
        <category>阿里巴巴</category>
        <category>开心</category>
      </categories>
      <tags>
        <tag>生活</tag>
        <tag>百阿</tag>
        <tag>开心</tag>
      </tags>
  </entry>
  <entry>
    <title>百年阿里</title>
    <url>/2019/09/28/Life/%E7%99%BE%E9%98%BF%E4%B9%8B%E6%97%85/</url>
    <content><![CDATA[<h2 id="百年阿里"><a href="#百年阿里" class="headerlink" title="百年阿里"></a>百年阿里</h2><hr>
<h3 id="紧张-amp-期待"><a href="#紧张-amp-期待" class="headerlink" title="紧张&amp;期待"></a>紧张&amp;期待</h3><p> 很明白自己不太善于沟通，所以对这次百阿之旅带着紧张，对于较快融入团队并不是很有信心。但是也很期待，期待够能突破自己，发现自己新的可能，简单来说变得更 Open ，认识一些小伙伴。令人欣喜的是，这份期待没有被辜负～</p>
<h3 id="缘分"><a href="#缘分" class="headerlink" title="缘分"></a>缘分</h3><p> 缘分真的是一个很神奇的东西，一群来自不同城市、不同部门、不同年龄的人组成了这么温暖、团结、活力的 1227 百阿班。很幸运来到 1227 ，很幸运来到 <code>三生万物</code> ，很幸运遇到你们，遇见新的自己 ～</p>
<p> 感谢百阿让我遇到这么一群，有趣的、热情的、可爱的、灵动的、温情脉脉的人。</p>
<h3 id="破冰"><a href="#破冰" class="headerlink" title="破冰"></a>破冰</h3><p> 一圈自我介绍后，其实我一个人名都没记住，虽然当时每个人自我介绍完后我都会重复好几遍，重复着重复着就忘了～原谅我的烂记性不是不想记住你们！！！不过最后认人名 PK 环节，我可是能叫出全班人的名字的人，得瑟一下。</p>
<p> 印象比较深刻的是五个小游戏任务，大家在互相还不熟悉的情况下能够做到相互信任、团结一致、互相帮助，用智慧和合作共同创造价值，取得成果。很多环节很辛苦也充满了困难，但我们在短时间内形成的超强凝聚力和执行力，让团队势如破竹，朝着目标冲刺。在这个过程中出现了很多小彩蛋，在 “达芬奇密码” 环节大家看完规则后，你一句我一句着的说着游戏规则，我还没听懂规则是啥，天相突然跳出来说了一个听起来很牛逼的调度算法，目瞪狗呆。又是一个算法怪兽，规则看完调度算法就出来了，感觉智商受到了降维打击….. </p>
<p> 由于算法对于调度者依赖性太高，对调度内存是一个很大的考验，为了保险起见我们采用了两趟遍历，虽然中间出了小插曲，导致牌序混乱。大家都一脸懵逼的时候，天相挺身而出力挽狂澜，终于体会到什么叫做 <strong>“此时此刻，非我莫属！”</strong>。</p>
<div style="display:flex">
  <div style="text-align: center"><img src="http://images.heniankj.com/1569155252878_IMG_3464.jpg">游戏任务</div>
  <div style="text-align: center"><img src="http://images.heniankj.com/1569155253214_IMG_7015.jpg">十只脚落地</div>
  <div style="text-align: center"><img src="http://images.heniankj.com/1569155252889_IMG_3473.jpg">安全部门合影</div>
</div>

<img src="http://images.heniankj.com/AF3301AB-955D-41A1-B605-8A5B726DBB2A.jpg">

<h3 id="凝聚"><a href="#凝聚" class="headerlink" title="凝聚"></a>凝聚</h3><p>经过第一天的破冰，第二天大家慢慢开始变得熟悉起来，尤其是在这次的土话的探寻中，团队更加凝聚。第一次见到阿里土话是在公司的大屏上：“此时此刻，非我莫属” 这是我记得最清楚的一句，也是最感动我的一句。</p>
<p>因为信任，所以简单。整个探寻任务从分工到执行只在很短的时间，没有过多的担心某个环节出问题了怎么办？某些子任务完不成怎么办？我们相信大家都能够把自己的那一部分做的很精彩，都很自觉的专注于自己的部分，相信伙伴！</p>
<p>一点多才开始剪视频，又感受了一把速度与激情，有一种五点前赶发布的感觉。既要冷静又要快速响应，在大家的共同努力下，开讲前视频渲染结束。</p>
<p>晚上一起去庆功呀～ 5号食堂！</p>
<img src="http://images.heniankj.com/1569155296161_IMG_3511.jpg" width="700px" height="500px">

<p>结束后，弦柱在群里说了一句，“今天聊的很开心，好像又回到了学生时代！”对的！就是这种感觉，好放松～</p>
<h3 id="变化"><a href="#变化" class="headerlink" title="变化"></a>变化</h3><p>昨天还在一起团队任务，马上就要因为另外的任务分开。<strong>“拥抱变化”</strong> ，变化来的如此之快。在弦柱的带领下我们完成了对飞猪业务的探寻，让我重新认识了飞猪，她的历史，现状，困难与机遇。对于从来没接触过的 OTA 也有了初步的了解。</p>
<p>在这个团队最大的感受就是有活力，因为我们的队长 — 弦柱（未来的杭州市旅游局局长） 就是一个很有活力的人！对团队也非常认真负责，带着我们去飞猪部门探索，采访！准备演讲到凌晨两点多，辛苦啦～</p>
<img src="http://images.heniankj.com/20190930120542.png">

<h3 id="夜谈"><a href="#夜谈" class="headerlink" title="夜谈"></a>夜谈</h3><p>对这个环节印象也非常深刻，每一个人的分享都好像是一本十分值的回味的书，透过他们的讲述读到不同的精彩故事。通过他们的精彩看到自身的不足，通过他们的失落看到逆境中的坚韧，每个人的经历都很有意思，可惜时间很短不然肯定整整一晚都在谈心交流。</p>
<p>不得不说，十分佩服泰伯，一个敢闯、敢拼、敢探索。知道自己想要什么，追求的是什么，并且付诸于行动，很少人能有这种想法和魄力。放一张大佬的照片，像不像香港扛把子 , haha~</p>
<img src="http://images.heniankj.com/lADPDgQ9rDNbRELND6DNF3A_6000_4000.jpg_620x10000q90g.jpg">

<h3 id="公益"><a href="#公益" class="headerlink" title="公益"></a>公益</h3><p>公益真的是一件帮助他人幸福自己的事情，在这个过程中能认识到自己的价值，也能为他人创造价值。公益能够感染身边的人，让更多的人以一颗柔软的心来对待这个世界。有很多同学反映在做公益的过程受到许多人的帮助，就像马老师所说的要时刻保持爱，对社会、对自然、对身边的人，爱的力量小而强大。做一个内心温暖而强大的人。</p>
<p>公益后的 “总裁局” 也是让人十分放松和开心，好久没和这么多人一起开开心心吃吃喝喝玩玩了。</p>
<img src="http://images.heniankj.com/lADPDgQ9rCFOjyLNC9DND8A_4032_3024.jpg_620x10000q90.jpg">


<h3 id="阿里"><a href="#阿里" class="headerlink" title="阿里"></a>阿里</h3><p>这就是阿里，以前我们会说：“这是阿里”，但是现在我们会告诉别人：“这就是阿里！”。因为现在的我们是一个不追求大，不追求强，我们追求成为一家活102年，截止到2036年，服务20亿消费者，创造1亿就业机会，帮助1000万家中小企业盈利的好公司。</p>
<p>对于新六脉神剑价值观，并不是为了改变我们的想法，或是束缚我们，而是在这个共识下选择同路人。价值观并非口号。可以看到不论是少年阿里还是成年后的他一直都在用实际行动诠释着价值观里的每一条。</p>
<p>另外对于年轻人，保持自己的好恶和棱角很重要，每个人都拥有对机会的选择权！</p>
<p>做一个守则的阿里人，在自身利益和公司里冲突的时候以公司利益优先，保证公司数据安全，不要触碰公司红线。</p>
<h3 id="活着"><a href="#活着" class="headerlink" title="活着"></a>活着</h3><p>来到阿里的第一年最重要的是 “活下来” ，因为作为一个新人很难融入到节奏走么快，业务难度这么高，新人培训少的团体。我们最重要的不是说想着如何实现集团的大目标，保证自己能够活下来，landing — 技术落地，业务落地。要明白自己生存在一个什么样的环境，去了解这个环境，适应这个环境。</p>
<p>而三到五年的沉淀，才能真正成为一个真正的阿里人，这个时候就需要面对更过的变化和更快的节奏，多从自身去思考问题。</p>
<h3 id="伙伴"><a href="#伙伴" class="headerlink" title="伙伴"></a>伙伴</h3><p>感谢在这次遇到的优秀的小伙伴们，因为我相信 无论我遇见谁，他/她都是我生命该出现的人，绝非偶然，他/她一定会教会你一些什么。</p>
<p>属七：感谢我的小天使，看得出来你是一个温暖的、热爱生活的人。从礼物的包装上，看得出你花了不少心思，真的非常感动。尤其是你的赠言充满了鼓励与期待，谢谢！你的字写的真的很棒～</p>
<p>弦柱(局长)：阳光、帅气、自信、具有少年感。跟他待在一块感觉自己又回到了大学校园，轻松愉悦！</p>
<p>余征：认真、严肃、细致。短短几个月就减掉 1/3 的体重的毅力，令人钦佩。</p>
<p>泰伯：敢闯、敢拼、有想法。看起来是一个很严肃的大佬，其实是很好相处的大佬！感谢鼓励陪伴！</p>
<p>拾月：博学、美丽、有气质。第一眼就被气场震撼到了，除了气质还有博学多识！</p>
<p>星吟：自律、美丽、脑洞大。每次不鸣则已，一鸣惊人，一句吐槽能让我笑一两分钟，苹果肌要坏掉了～</p>
<p>珺兮：聪明、文静、可爱。都是应届生相似点还蛮多的，也能说到一块去。可爱！有趣！</p>
<p>天相：自信、认真、有责任、有主见。令我印象深刻的就是 不使用一次性杯子盛可乐，因为不环保！！！</p>
<p>迪奇：聪明、好奇心、冷幽默。我们的数据中台，反应快，技术强，感觉内心也很萌哈哈，我们技术人的楷模。</p>
<p>久谙：幽默、有趣、情商高。思维太活跃了，想象力也超级棒，一句话带动全场气氛！</p>
<p>山魏：认真、戏精、沉稳、可靠。因为角色投入一炮而红，红遍大江南北！但是也非常的可靠，做事认真。</p>
<p>顺序不分先后 ，按照 “总裁饭局” 席位，希望下次再相见，我们还能按照这个顺序坐在一起，聊天喝酒！</p>
<p>优秀的人大多会玩儿，幽默，会来事儿，正是因为这些特质，能够吸引并凝聚一波人，作为一个不太说话的程序员，应该培养锻炼自己让自己变的活跃，open！这次百阿是一个良好的开端。感谢认识你们，真的很开心～</p>
]]></content>
      <categories>
        <category>生活</category>
        <category>阿里巴巴</category>
        <category>开心</category>
      </categories>
      <tags>
        <tag>生活</tag>
        <tag>百阿</tag>
        <tag>开心</tag>
      </tags>
  </entry>
  <entry>
    <title>oh-my-zsh主题体验</title>
    <url>/2019/07/24/Tools/OhMyZsh%E4%B8%BB%E9%A2%98%E4%BD%93%E9%AA%8C/</url>
    <content><![CDATA[<p>以前一直使用的 <code>oh-my-zsh</code> 的默认主题，这是因为一直找不到一款钟爱的主题，powerline 一开始看起来比较有科技感其实也不是很耐看，而且配置很麻烦啊。</p>
<p>后来想着干脆直接用 <code>随机主题</code> 好了，你永远不知道下次他会给你什么惊喜。</p>
<p>下面是在使用随机主题的时候看到的一些比较漂亮的一些主题，整理出来最后直接保留这些主题，那么再怎么 random 都是我喜欢的主题啦~</p>
<h2 id="pygmalion-virtualenv-zsh-theme"><a href="#pygmalion-virtualenv-zsh-theme" class="headerlink" title="pygmalion-virtualenv.zsh-theme"></a>pygmalion-virtualenv.zsh-theme</h2><p><img src="http://images.heniankj.com/20190724103433.png" alt></p>
]]></content>
      <categories>
        <category>工具集</category>
      </categories>
      <tags>
        <tag>工具</tag>
        <tag>终端</tag>
        <tag>oh-my-zsh</tag>
        <tag>主题</tag>
      </tags>
  </entry>
  <entry>
    <title>RemoveNthNodeEndOfList-19</title>
    <url>/2019/07/24/Leet%20Code/RemoveNthNodeEndOfList-19/</url>
    <content><![CDATA[<blockquote>
<p>Given a linked list, remove the <em>n</em>-th node from the end of list and return its head.</p>
<p><strong>Example:</strong></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt; Given linked list: 1-&gt;2-&gt;3-&gt;4-&gt;5, and n = 2.</span><br><span class="line">&gt; </span><br><span class="line">&gt; After removing the second node from the end, the linked list becomes 1-&gt;2-&gt;3-&gt;5.</span><br><span class="line">&gt;</span><br></pre></td></tr></table></figure>
</blockquote>
<blockquote>
<p><strong>Note:</strong></p>
<p>Given <em>n</em> will always be valid.</p>
<p><strong>Follow up:</strong></p>
<p>Could you do this in one pass?</p>
</blockquote>
<p>题目要求就是通过一次遍历删除倒数第 n 个元素。</p>
<h2 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h2><ol>
<li>一开始想着就是 <code>链表长度 - n</code>  这样遍历到这个位置把这个节点跳过就行了。可是这样的话需要遍历链表两次。</li>
<li>那么我们要想办法一边遍历一边跳过节点，这个时候想到了数组中删除某个节点使用了 p,q 指针，那么这里是否可以呢？</li>
<li>确实，我们 pq 指针开始指示的位置不同，q指针指向 n 节点后的位置，这样只要 q 指针到了链表的结尾我们就能找到 需要删除的节点就是 p 的位置。</li>
</ol>
<h2 id="优化"><a href="#优化" class="headerlink" title="优化"></a>优化</h2><p>经过上面的一些想法基本形成了解题思路但是在实现的时候会有几个特例这个算法过不了比如 <code>[1] 1  [1,2]  2</code>  ,一开始我还尝试使用了一些方案来修补代码，但是这样总觉得不是很好，算法没有比较好的适用性。然后我想到这种状态主要是head指针一开始指向一个空节点那么删除head元素的时候就不会出现各种问题了。所以有下面的代码。</p>
<h2 id="代码"><a href="#代码" class="headerlink" title="代码"></a>代码</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Definition for singly-linked list.</span></span><br><span class="line"><span class="comment"> * public class ListNode &#123;</span></span><br><span class="line"><span class="comment"> *     int val;</span></span><br><span class="line"><span class="comment"> *     ListNode next;</span></span><br><span class="line"><span class="comment"> *     ListNode(int x) &#123; val = x; &#125;</span></span><br><span class="line"><span class="comment"> * &#125;</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Solution</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ListNode <span class="title">removeNthFromEnd</span><span class="params">(ListNode head, <span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// add null head</span></span><br><span class="line">        ListNode nullNode = <span class="keyword">new</span> ListNode(<span class="number">0</span>);</span><br><span class="line">        nullNode.next = head;</span><br><span class="line">        head = nullNode;</span><br><span class="line"></span><br><span class="line">        ListNode first = head, second = head;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            second = second.next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (second != <span class="keyword">null</span> &amp;&amp; second.next != <span class="keyword">null</span>) &#123;</span><br><span class="line">            first = first.next;</span><br><span class="line">            second = second.next;</span><br><span class="line">        &#125;</span><br><span class="line">        first.next = first.next.next;</span><br><span class="line">        <span class="keyword">return</span> head.next;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="结果"><a href="#结果" class="headerlink" title="结果"></a>结果</h2><blockquote>
<p>Runtime: 0 ms, faster than 100.00% of Java online submissions for Remove Nth Node From End of List.</p>
<p>Memory Usage: 35 MB, less than 100.00% of Java online submissions for Remove Nth Node From End of List.</p>
</blockquote>
]]></content>
      <categories>
        <category>LeetCode</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 - 装饰者模式</title>
    <url>/2019/06/04/DesignPattern/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%A3%85%E9%A5%B0%E8%80%85%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="装饰者模式"><a href="#装饰者模式" class="headerlink" title="装饰者模式"></a>装饰者模式</h1><blockquote>
<p>本文转载自 <a href="https://mrdear.cn/" target="_blank" rel="noopener">屈定’s Blog</a></p>
</blockquote>
<p>装饰者模式实际上是一直提倡的<strong>组合代替继承</strong>的实践方式,个人认为要理解装饰者模式首先需要理解为什么需要<strong>组合代替继承</strong>,继承又是为什么让人深恶痛绝.</p>
<h2 id="为什么建议使用组合代替继承"><a href="#为什么建议使用组合代替继承" class="headerlink" title="为什么建议使用组合代替继承?"></a>为什么建议使用组合代替继承?</h2><p>面向对象的特性有继承与封装,但两者却又有一点矛盾,继承意味子类依赖了父类中的实现,一旦父类中改变实现则会对子类造成影响,这是打破了封装性的一种表现.<br>而组合就是巧用封装性来实现继承功能的代码复用.<br>举一个Effective Java中的案例,当前需求是为HashSet提供一个计数,要求统计它创建以来曾经添加了多少个元素,那么可以写出下面的代码.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class InstrumentedHashSet &lt;E&gt; extends HashSet&lt;E&gt; &#123;</span><br><span class="line"></span><br><span class="line">  private int addCount = 0;</span><br><span class="line"></span><br><span class="line">  @Override</span><br><span class="line">  public boolean add(E e) &#123;</span><br><span class="line">    this.addCount++;</span><br><span class="line">    return super.add(e);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  @Override</span><br><span class="line">  public boolean addAll(Collection&lt;? extends E&gt; c) &#123;</span><br><span class="line">    this.addCount += c.size();</span><br><span class="line">    return super.addAll(c);</span><br><span class="line">  &#125;</span><br><span class="line">  public int getAddCount() &#123;</span><br><span class="line">    return this.addCount;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面测试代码会抛出异常,正确结果是6,是不是匪夷所思,这种匪夷所思需要你去看<code>HashSet</code>的具体实现,其<code>addAll</code>实际上是调用了<code>add</code>方法.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">InstrumentedHashSet&lt;String&gt; hashSet = new InstrumentedHashSet&lt;&gt;();</span><br><span class="line">  hashSet.addAll(Arrays.asList(&quot;张三&quot;, &quot;李四&quot;, &quot;王二&quot;));</span><br><span class="line">  Assert.assertEquals(hashSet.getAddCount(), 3);</span><br></pre></td></tr></table></figure>

<p>这个案例说明了继承导致子类变得很脆弱,其不知道父类的细节,但是却实实在在的依赖了父类的实现.出现了问题也很难找出bug.本质原因是<strong>HashSet并不是专门为继承所设计的类</strong>,因此强行继承那会出现意想不到的问题.有关什么时候该用继承在<a href="https://mrdear.cn/2018/03/25/experience/design_patterns--template_method/" target="_blank" rel="noopener">设计模式–模板方法模式的思考</a>一文章有相关讨论,感兴趣的可以去看看.</p>
<p>回到正题那么换成组合模式,让<code>InstrumentedHashSet</code>持有<code>HashSet</code>的私有实例,add以及addAll方法由<code>HashSet</code>的私有实例代理执行.这就是组合所带来的优势,充分利用其它类的特点,降低耦合度,我只需要你已完成的功能,相比继承而并不受到你内部实现的制约.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class InstrumentedHashSet &lt;E&gt;&#123;</span><br><span class="line"></span><br><span class="line">  private int addCount = 0;</span><br><span class="line"></span><br><span class="line">  private HashSet&lt;E&gt; hashSet = new HashSet&lt;&gt;();</span><br><span class="line"></span><br><span class="line">  public boolean add(E e) &#123;</span><br><span class="line">    this.addCount++;</span><br><span class="line">    return hashSet.add(e);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  public boolean addAll(Collection&lt;? extends E&gt; c) &#123;</span><br><span class="line">    this.addCount += c.size();</span><br><span class="line">    return hashSet.addAll(c);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  public int getAddCount() &#123;</span><br><span class="line">    return this.addCount;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="装饰者模式-1"><a href="#装饰者模式-1" class="headerlink" title="装饰者模式"></a>装饰者模式</h2><p>装饰者模式定义为:动态的给一对象添加一些额外的职责,对该对象进行功能性的增强.(只是增强,并没有改变使用原对象的意图)<br>装饰器模式类图:<br><img src="http://imgblog.mrdear.cn/1520506012.png?imageMogr2/thumbnail/!100p" alt="img"><br>以上是标准的装饰器模式,其中<code>AbstractDecorator</code>为一个装饰器模板,目的是为了提高代码复用,简化具体装饰器子类的实现成本,当然不需要的话也是可以省略的,其最主要的功能是持有了<code>ComponentInterface</code>这个被装饰者对象,然后子类可以利用类似AOP环绕通知形式来在被装饰类执行<code>sayHello()</code>前后执行自己的逻辑.这是装饰者模式的本质.</p>
<p>比如<code>ContreteDecoratorA</code>增强了<code>sayHello()</code></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class ContreteDecoratorA extends AbstractDecorator &#123;</span><br><span class="line"></span><br><span class="line">  public ContreteDecoratorA(ComponentInterface componentInterface) &#123;</span><br><span class="line">    super(componentInterface);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  @Override</span><br><span class="line">  public void sayHello() &#123;</span><br><span class="line">    System.out.println(&quot;A start&quot;);</span><br><span class="line">    super.sayHello();</span><br><span class="line">    System.out.println(&quot;A end&quot;);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>具体使用方式</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">   final ContreteDecoratorA decoratorA = new ContreteDecoratorA(new ComponentInterfaceImpl());</span><br><span class="line">   decoratorA.sayHello();</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<p>输出</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">A start</span><br><span class="line">hello world</span><br><span class="line">A end</span><br></pre></td></tr></table></figure>

<p>其中默认实现<code>ComponentInterfaceImpl</code>的sayHello()功能被装饰后增强.</p>
<h2 id="Java-I-O与装饰者"><a href="#Java-I-O与装饰者" class="headerlink" title="Java I/O与装饰者"></a>Java I/O与装饰者</h2><h3 id="字节流"><a href="#字节流" class="headerlink" title="字节流"></a>字节流</h3><p>Java I/O框架就是一个很好的装饰者模式的实例.如下<code>InputStream</code>关系图<br><img src="http://imgblog.mrdear.cn/1520512559.png?imageMogr2/thumbnail/!100p" alt="img"><br>其中<code>FileInputStream</code>,<code>ObjectInputStream</code>等直接实现类提供了最基本字节流读取功能.<br>而<code>FilterInputStream</code>作为装饰者,其内部引用了另一个<code>InputStream</code>(实际被装饰的对象),然后以AOP环绕通知的形式来进行功能增强,笔者认为这里应该把该类定义为abstract更为合适.其承担的角色只是代码复用,帮助具体的装饰者类更加容易的实现功能增强.<br><img src="http://imgblog.mrdear.cn/1520512740.png?imageMogr2/thumbnail/!100p" alt="img"><br>具体的装饰者<code>BufferedInputStream</code>为其他字节流提供了缓冲输入的支持.<code>DataInputStream</code>则提供了直接解析Java原始数据流的功能.</p>
<p>由于装饰者模式的存在,原本一个字节一个字节读的<code>FileInputStream</code>只需要嵌套一层<code>BufferedInputStream</code>即可支持缓冲输入,</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">BufferedInputStream br = new BufferedInputStream(new FileInputStream(new File(&quot;path&quot;)));</span><br></pre></td></tr></table></figure>

<h3 id="字符流"><a href="#字符流" class="headerlink" title="字符流"></a>字符流</h3><p>相比较字节流,字符流这边的关系则有点混乱,主要集中在<code>BufferedReader</code>与<code>FilterReader</code>,其两个角色都是装饰者,而<code>FilterReader</code>是更加基本的装饰者其相对于字节流中的<code>FilterInputStream</code>已经升级为abstract了,目的就是便于具体装饰者实现类更加容易的编写.那么为什么<code>BufferedReader</code>不继承<code>FilterReader</code>呢?这个问题暂时不知道答案,有兴趣的可以关注下知乎,等大牛回答.<br><a href="https://www.zhihu.com/question/57094582/answer/337119885" target="_blank" rel="noopener">为什么BufferedReader 不是 FilterReader的子类，而直接是Reader的子类？</a></p>
<p>不过从另一个角度来说,设计模式并不是套用模板,其最主要的是思想,对于装饰者模式最重要的是利用组合代替了继承,原有逻辑交给内部引用的类来实现,而自己只做增强功能,只要符合这一思想都可以称之为装饰者模式.<br><img src="http://imgblog.mrdear.cn/1520514188.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<h2 id="Mybatis与装饰者"><a href="#Mybatis与装饰者" class="headerlink" title="Mybatis与装饰者"></a>Mybatis与装饰者</h2><p>Mybatis中有不少利用到装饰者模式,比如二级缓存<code>Cache</code>,另外其<code>Executor</code>也正在朝着装饰者模式改变.这里以Cache接口为主,类图如下:<br><img src="http://imgblog.mrdear.cn/1520515773.png?imageMogr2/thumbnail/!100p" alt="img"><br>从类图来看和装饰者模式似乎无半毛钱关系,实际上其省略了<code>AbstractDecorator</code>这一公共的装饰者基类.那么要实现装饰者其实现类中必须有一个Cache的被装饰对象,以LruCache为例.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class LruCache implements Cache &#123;</span><br><span class="line"></span><br><span class="line">  private final Cache delegate;</span><br><span class="line">  private Map&lt;Object, Object&gt; keyMap;</span><br><span class="line">  private Object eldestKey;</span><br><span class="line">  </span><br><span class="line">  @Override</span><br><span class="line">  public String getId() &#123;</span><br><span class="line">    return delegate.getId();</span><br><span class="line">  &#125;</span><br><span class="line">  ....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其内部拥有<code>Cache delegate</code>这一被装饰者,也就是无论什么Cache,只要套上了<code>LruCache</code>那么就有了LRU这一特性.<br>在<code>org.apache.ibatis.mapping.CacheBuilder#setStandardDecorators</code>构造时则根据配置参数来决定增强哪些功能,下面代码则很好的体现了装饰者模式的优势,还望好好体会.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">private Cache setStandardDecorators(Cache cache) &#123;</span><br><span class="line">  try &#123;</span><br><span class="line">    MetaObject metaCache = SystemMetaObject.forObject(cache);</span><br><span class="line">    if (size != null &amp;&amp; metaCache.hasSetter(&quot;size&quot;)) &#123;</span><br><span class="line">      metaCache.setValue(&quot;size&quot;, size);</span><br><span class="line">    &#125;</span><br><span class="line">    if (clearInterval != null) &#123;</span><br><span class="line">      cache = new ScheduledCache(cache);</span><br><span class="line">      ((ScheduledCache) cache).setClearInterval(clearInterval);</span><br><span class="line">    &#125;</span><br><span class="line">    if (readWrite) &#123;</span><br><span class="line">      cache = new SerializedCache(cache);</span><br><span class="line">    &#125;</span><br><span class="line">    cache = new LoggingCache(cache);</span><br><span class="line">    cache = new SynchronizedCache(cache);</span><br><span class="line">    if (blocking) &#123;</span><br><span class="line">      cache = new BlockingCache(cache);</span><br><span class="line">    &#125;</span><br><span class="line">    return cache;</span><br><span class="line">  &#125; catch (Exception e) &#123;</span><br><span class="line">    throw new CacheException(&quot;Error building standard cache decorators.  Cause: &quot; + e, e);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="线程安全与装饰者"><a href="#线程安全与装饰者" class="headerlink" title="线程安全与装饰者"></a>线程安全与装饰者</h2><p>装饰者模式的功能是增强原有类，因此其经常被用来包装一个非线程安全的类，使其提供线程安全的访问，在JDK中的体现则是<code>Collections.synchronizedXXX</code>方法以及与其类似的一些方法。以<code>synchronizedList</code>为例，其本意是将线程不安全的<code>List</code>实例包装成线程安全的实例，包装方式是使用<code>SynchronizedList</code>提供同步包装，如下所示：对相关方法都使用独占锁来修饰，保证了并发访问的线程安全性。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">static class SynchronizedList&lt;E&gt; extends SynchronizedCollection&lt;E&gt; implements List&lt;E&gt; &#123;</span><br><span class="line">  final List&lt;E&gt; list;</span><br><span class="line"></span><br><span class="line">  SynchronizedList(List&lt;E&gt; list) &#123;</span><br><span class="line">    super(list);</span><br><span class="line">    this.list = list;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  public E get(int index) &#123;</span><br><span class="line">    synchronized (mutex) &#123;</span><br><span class="line">      return list.get(index);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  public void add(int index, E element) &#123;</span><br><span class="line">    synchronized (mutex) &#123;</span><br><span class="line">      list.add(index, element);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  ....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="函数式编程与装饰者"><a href="#函数式编程与装饰者" class="headerlink" title="函数式编程与装饰者"></a>函数式编程与装饰者</h2><p>在函数式编程中因为函数是一等公民，因此互相嵌套是常有的事情，比如以下对于加锁解锁的一个函数封装调用</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public static &lt;T&gt; T  lockTemplate(Lock lock, Supplier&lt;T&gt; supplier) &#123;</span><br><span class="line">  lock.lock();</span><br><span class="line">  try &#123;</span><br><span class="line">    return supplier.get();</span><br><span class="line">  &#125; finally &#123;</span><br><span class="line">    lock.unlock();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>该函数接收一个锁以及一个<code>supplier</code>提供者，其使用方式也很简单，比如下面方式使得<code>i++</code>变得线程安全。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ReentrantLock lock = new ReentrantLock();</span><br><span class="line"></span><br><span class="line">  int[] boxInt = new int[1];</span><br><span class="line">  </span><br><span class="line">  Integer value = LockTemplate.lockTemplate(lock, () -&gt; &#123;</span><br><span class="line">    // 线程不安全的操作</span><br><span class="line">    return boxInt[0]++;</span><br><span class="line">  &#125;);</span><br></pre></td></tr></table></figure>

<p>由于Java是面向对象范式语言，对函数式编程支持的并不是很好，所以这个例子并不能很好的描述函数式编程，不过思想上来看这是一种装饰者模式的实践，只不过装饰者与被装饰都变成了函数，装饰者函数的功能也是对被装饰者功能的增强。</p>
<h2 id="装饰者模式与桥接模式"><a href="#装饰者模式与桥接模式" class="headerlink" title="装饰者模式与桥接模式"></a>装饰者模式与桥接模式</h2><p>这两个模式起初笔者很疑惑，两者的本质都是组合，并且从类图上来看两者几乎是一致的，那么他们的区别是什么呢？<br>我认为从继承树上来看装饰者模式的目的是纵向的扩展类（增加树的深度），从而为现有的实现类提供更强大的支援。<br>桥接模式则是水平扩展（增加树的宽度），以现有的类为代码复用的基础，然后在这个基础上水平扩展出另外的业务实现，这里更加注重的是解耦，把变化的与不变的分离开。</p>
<p>另外就是组合模式，基本看起来他的结构和装饰者模式一模一样，只是他们的用法是不一样的，两个都是继承一个，并且里面包含实例，但是装饰者时使用了实例的功能，而组合模式则是为了形成一个结构，比如Mybatis里面的SqlNode。</p>
<p>另外设计模式本身之间相互影响，没必要纠结于是某一种特定的模式，只要理解其背后的思想就可以了。</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>装饰者模式本质上来说是AOP思想的一种实现方式，其持有被装饰者，因此可以控制被装饰者的行为从而达到了AOP的效果。</p>
<h2 id="扩展"><a href="#扩展" class="headerlink" title="扩展"></a>扩展</h2><p>偶然看到一篇博文: <a href="http://javatar.iteye.com/blog/40188" target="_blank" rel="noopener">项目中用到的一个小工具类(字符过滤器)</a>,里面运用了装饰者设计模式,工厂模式,模板方法模式设计了这样一个符合开闭原则的工具类.感兴趣的也可以看看.</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式,装饰者</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 - 责任链模式</title>
    <url>/2019/06/04/DesignPattern/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%B4%A3%E4%BB%BB%E9%93%BE%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="责任链模式"><a href="#责任链模式" class="headerlink" title="责任链模式"></a>责任链模式</h1><blockquote>
<p>本文转载自<a href="https://mrdear.cn/" target="_blank" rel="noopener">屈定’s Blog</a></p>
</blockquote>
<h2 id="标准责任链模式"><a href="#标准责任链模式" class="headerlink" title="标准责任链模式"></a>标准责任链模式</h2><p>责任链模式: 客户端发出的请求,客户端本身并不知道被哪一个对象处理,而直接扔给对象链,该请求在对象链中共享,由对象本身决定是否处理. <strong>当请求被处理后该链终止</strong>.本质目的是把客户端请求与接收者解耦,但是解耦的太彻底了,只能让接收者一个个来看看是不是自己该处理的请求.<br>标准的责任链模式<strong>一个请求只被一个对象处理</strong>,一旦处理成功后则链终止,请求不再被继续传递.标准的责任链模式并不是很通用,这种一对一模式大多场景可以用策略模式来代替,只有在客户端并不清楚具体的执行者是哪个对象的时候,责任链才比较适合.<br>举个例子:你想在天朝办理一个证,但是你不知道去哪比较好,因此你的选择就是一条链路,先去A局,A局让你去B局,B局让你去C局等等,直到解决你的问题,当然也存在白跑一趟的结果.这也是标准责任链的缺点,产生了太多没必要的调用.标准的责任链实际上应用场景并不是很多,而常使用的是升级版的功能链.</p>
<h2 id="功能链"><a href="#功能链" class="headerlink" title="功能链"></a>功能链</h2><p>功能链是责任链的演变,结构上并没有实质的变化,只是每一个节点都可以处理请求,处理完转向下一个,也就是每一个请求都经历全部的链.这种应用场景就比较多了,比如我要办一件事,先去A再去B最后去C,这个例子还有点说明ABC三者的关系,取决于构造链时的顺序,另外每一步没处理好可以自由的选择退出链.<br>文字说的不是很理解,下面举几个实际中的代码实例.</p>
<h3 id="Java中Filter链"><a href="#Java中Filter链" class="headerlink" title="Java中Filter链"></a>Java中Filter链</h3><p>对于Filter,其是由<code>FilterChain</code>来进行链的组合调用,请求的request与返回response实际上是共享的上下文信息,每一个处理的Filter都可以查看与修改.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public interface FilterChain &#123;</span><br><span class="line">    public void doFilter(ServletRequest request, ServletResponse response)</span><br><span class="line">            throws IOException, ServletException;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在Tomcat中实现类为<code>org.apache.catalina.core.ApplicationFilterChain</code>,其结构如下图:<br><img src="http://imgblog.mrdear.cn/1521466345.png?imageMogr2/thumbnail/!100p" alt="img"><br>其中数组filters就是所谓的filter链,利用pos(当前执行到的位置)与n(filter链长度)来进行链的调用.<br>那么怎么让链节点选择继续执行还是停止执行呢?答案的<code>Filter</code>的<code>doFilter</code>方法,该方法把责任链作为参数<code>FilterChain</code>一直传递下去,继续就调用chain的<code>doFilter</code>方法,不继续则不调用.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">void doFilter(ServletRequest request, ServletResponse response,</span><br><span class="line">           FilterChain chain) throws IOException, ServletException;</span><br></pre></td></tr></table></figure>

<h3 id="Spring-Security中拦截链"><a href="#Spring-Security中拦截链" class="headerlink" title="Spring Security中拦截链"></a>Spring Security中拦截链</h3><p>Spring Security中的链实际上是由<code>SecurityFilterChain</code>接口所定义,其很简单的就是暴露出一个list的链.其中<code>matches</code>判断请求是否是这条链来处理.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public interface SecurityFilterChain &#123;</span><br><span class="line"></span><br><span class="line">    boolean matches(HttpServletRequest request);</span><br><span class="line"></span><br><span class="line">    List&lt;Filter&gt; getFilters();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在其入口处类<code>org.springframework.security.web.FilterChainProxy</code>中包含着多条链.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">private List&lt;SecurityFilterChain&gt; filterChains;</span><br></pre></td></tr></table></figure>

<p>每一个链处理的是一类请求.Spring Security使用简单的for循环判断定位到具体执行的链.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">private List&lt;Filter&gt; getFilters(HttpServletRequest request)  &#123;</span><br><span class="line">      for (SecurityFilterChain chain : filterChains) &#123;</span><br><span class="line">          if (chain.matches(request)) &#123;</span><br><span class="line">              return chain.getFilters();</span><br><span class="line">          &#125;</span><br><span class="line">      &#125;</span><br><span class="line">      return null;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>那么这个结构因为这个设计模式就很清晰了(这也是熟悉了设计模式之后的优势,看源码可以有一种全局把控感)<br><img src="http://imgblog.mrdear.cn/1521468002.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<p>还有一个问题,链是如何自由执行的?<br>这一点与Java Filter一模一样,Spring Security实现了一个<code>org.springframework.security.web.FilterChainProxy.VirtualFilterChain</code>类,该类同样实现了<code>FilterChain</code>接口,里面的调用逻辑也与tomcat方式一致.具体就不讨论了.</p>
<p>另外Spring Security也提供了一种数据共享的方式,利用<code>ThreadLocal</code>保证线程安全,达到共享数据的目的.另外这里的<code>SecurityContextHolderStrategy</code>是策略模式的一种应用,值得一看.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">final class ThreadLocalSecurityContextHolderStrategy implements</span><br><span class="line">		SecurityContextHolderStrategy &#123;</span><br><span class="line">    		</span><br><span class="line">	private static final ThreadLocal&lt;SecurityContext&gt; contextHolder = new ThreadLocal&lt;SecurityContext&gt;();</span><br><span class="line"></span><br><span class="line">	public void clearContext() &#123;</span><br><span class="line">		contextHolder.remove();</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	public SecurityContext getContext() &#123;</span><br><span class="line">		SecurityContext ctx = contextHolder.get();</span><br><span class="line"></span><br><span class="line">		if (ctx == null) &#123;</span><br><span class="line">			ctx = createEmptyContext();</span><br><span class="line">			contextHolder.set(ctx);</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		return ctx;</span><br><span class="line">	&#125;</span><br><span class="line">	public void setContext(SecurityContext context) &#123;</span><br><span class="line">		Assert.notNull(context, &quot;Only non-null SecurityContext instances are permitted&quot;);</span><br><span class="line">		contextHolder.set(context);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	public SecurityContext createEmptyContext() &#123;</span><br><span class="line">		return new SecurityContextImpl();</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="Mybatis中插件链"><a href="#Mybatis中插件链" class="headerlink" title="Mybatis中插件链"></a>Mybatis中插件链</h3><p>Mybatis中插件使用的是类似责任链的一种模式,当然也可以称之为责任链模式,毕竟思想都是类似的.其中插件是通过<code>Interceptor</code>接口实现的,其中<code>plugin</code>方法就是为目标对象套上该链的一个节点.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public interface Interceptor &#123;</span><br><span class="line"></span><br><span class="line">  Object intercept(Invocation invocation) throws Throwable;</span><br><span class="line"></span><br><span class="line">  Object plugin(Object target);</span><br><span class="line"></span><br><span class="line">  void setProperties(Properties properties);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>那么如何构造这个链?在<code>InterceptorChain</code>中有如下方法,<code>InterceptorChain</code>是在构造配置时组装好的,燃后对目标使用<code>pluginAll</code>方法,构造完整链.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class InterceptorChain &#123;</span><br><span class="line">  private final List&lt;Interceptor&gt; interceptors = new ArrayList&lt;Interceptor&gt;();</span><br><span class="line">  public Object pluginAll(Object target) &#123;</span><br><span class="line">    for (Interceptor interceptor : interceptors) &#123;</span><br><span class="line">      target = interceptor.plugin(target);</span><br><span class="line">    &#125;</span><br><span class="line">    return target;</span><br><span class="line">  &#125;</span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其中<code>plugin</code>官方推荐<code>Plugin.wrap(target, this)</code>方法,该方法本质上是用代理模式嵌套住目标类</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public static Object wrap(Object target, Interceptor interceptor) &#123;</span><br><span class="line">  Map&lt;Class&lt;?&gt;, Set&lt;Method&gt;&gt; signatureMap = getSignatureMap(interceptor);</span><br><span class="line">  Class&lt;?&gt; type = target.getClass();</span><br><span class="line">  Class&lt;?&gt;[] interfaces = getAllInterfaces(type, signatureMap);</span><br><span class="line">  if (interfaces.length &gt; 0) &#123;</span><br><span class="line">    return Proxy.newProxyInstance(</span><br><span class="line">        type.getClassLoader(),</span><br><span class="line">        interfaces,</span><br><span class="line">        new Plugin(target, interceptor, signatureMap));</span><br><span class="line">  &#125;</span><br><span class="line">  return target;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>那么这种构造出的链大概如下面这种嵌套结构,这种链可以说是彻底的功能链,其一旦组装好就无法变化了.当然这种也适合Mybatis这种从配置中就定死了执行链.<br><img src="http://imgblog.mrdear.cn/1521471003.png?imageMogr2/thumbnail/!70p" alt="img"></p>
<h3 id="业务开发中可以常用到的链"><a href="#业务开发中可以常用到的链" class="headerlink" title="业务开发中可以常用到的链"></a>业务开发中可以常用到的链</h3><p>在业务开发中常常能遇到这类需求,比如退款操作,退款后可以恢复商品库存,恢复活动库存,退掉用户的优惠券,退掉用户的活动资格等等,该一系列的操作就是一条线性链,那么就可以利用责任链的思想来完成在这样的需求.<br>先提取出一个公共接口,链节点实现该接口,完成具体的退款操作</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public interface RegainAfterRefundOrder &#123;</span><br><span class="line">  /**</span><br><span class="line">   * 退回操作</span><br><span class="line">   * @param bo 该订单,可能是子订单,也可能是主订单,自行判断</span><br><span class="line">   * @param operator 操作人</span><br><span class="line">   * @return true成功</span><br><span class="line">   */</span><br><span class="line">  boolean regain(BizOrderDO bo, Long operator);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>接下来是链的统一管理,也就是需要<code>Chain</code>这个类来管理,可以按照下面的实现,其调用链只是简单的在<code>applyAllPlugin</code>循环调用,该过程可以按照Spring Security等方式实现更加灵活的调用.<br>可以根据需求设计为一旦创建就不可改变的类,包括类中的<code>interceptors</code>,这样使得代码更加健壮.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Component</span><br><span class="line">public class RefundOrderAndRegainChain &#123;</span><br><span class="line"></span><br><span class="line">  private final List&lt;RegainAfterRefundOrder&gt; interceptors = new ArrayList&lt;&gt;();</span><br><span class="line"></span><br><span class="line">  public void applyAllPlugin(BizOrderDO bo, Long operator) &#123;</span><br><span class="line">    for (RegainAfterRefundOrder interceptor : interceptors) &#123;</span><br><span class="line">      interceptor.regain(bo, operator);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  public void addInterceptor(RegainAfterRefundOrder interceptor) &#123;</span><br><span class="line">    interceptors.add(interceptor);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  public List&lt;RegainAfterRefundOrder&gt; getInterceptors() &#123;</span><br><span class="line">    return Collections.unmodifiableList(interceptors);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后是借助IOC实现链的组装,假设有<code>RegainCoupon</code>,<code>RegainInventoryCount</code>,<code>RegainInvitationCodeWithDraw</code>等RegainAfterRefundOrder的实现类,依次在Spring的Configuration类中实现注入,并构造出需要的<code>RefundOrderAndRegainChain</code>.最后再业务需要的地方直接注入该Chain即可.对于这种逻辑实现了解耦与灵活的组合.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Configuration</span><br><span class="line">public class RefundOrderAndRegainConfig &#123;</span><br><span class="line"></span><br><span class="line">  @Bean</span><br><span class="line">  public RefundOrderAndRegainChain paidToRefund(</span><br><span class="line">      RegainInventoryCount regainInventoryCount,</span><br><span class="line">      RegainCoupon regainCoupon,</span><br><span class="line">      RegainPromotionRegistered regainPromotionRegistered,</span><br><span class="line">      RegainInvitationCodeWithDraw regainInvitationCode) &#123;</span><br><span class="line">    RefundOrderAndRegainChain chain = new RefundOrderAndRegainChain();</span><br><span class="line">    chain.addInterceptor(regainInventoryCount);</span><br><span class="line">    chain.addInterceptor(regainPromotionRegistered);</span><br><span class="line">    chain.addInterceptor(regainCoupon);</span><br><span class="line">    chain.addInterceptor(regainInvitationCode);</span><br><span class="line">    return chain;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="责任链模式的本质"><a href="#责任链模式的本质" class="headerlink" title="责任链模式的本质"></a>责任链模式的本质</h2><ol>
<li>让请求者不关心具体接收者是谁,只需要得到自己的具体结果</li>
<li>在一个请求对应多个接收者情况下(Spring Security这种),接收者之间可以自由组合,灵活性很高</li>
<li>新增接收者处理也只需要增加链中的一个节点,不需要改动太多.</li>
</ol>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式,责任链</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 - 模板方法模式</title>
    <url>/2019/06/04/DesignPattern/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E6%A8%A1%E6%9D%BF%E6%96%B9%E6%B3%95%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="设计模式–模板方法模式的思考"><a href="#设计模式–模板方法模式的思考" class="headerlink" title="设计模式–模板方法模式的思考"></a>设计模式–模板方法模式的思考</h1><blockquote>
<p>本文转载自<a href="https://mrdear.cn/" target="_blank" rel="noopener">屈定’s Blog</a></p>
</blockquote>
<p>模板方法同样也是一种很实用的方法,目的是提高代码复用,并且统一大体的算法流程,比如一个一台电脑主机,定义好放置CPU,硬盘,内存等空位后,就形成了一个骨架,那么这个就是模板,具体的CPU,内存,硬盘是什么牌子型号则不需要考虑,这些是具体到业务中的实现类所负责的事情.</p>
<h2 id="模板方法模式"><a href="#模板方法模式" class="headerlink" title="模板方法模式"></a>模板方法模式</h2><p>模板方法模式可以说是抽象类的一种特性,可以定义抽象(abstract)方法与常规方法,抽象方法延迟到子类中实现.因此标准的模板方法一般是一个抽象类+具体的实现子类,抽象类(AbstractClass)负责整个执行流程的定义,而子类(ConcreteClass)负责某一具体流程的实现策略,类图如下:<br><img src="http://imgblog.mrdear.cn/1521982796.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<h2 id="Mybatis中的模板方法模式"><a href="#Mybatis中的模板方法模式" class="headerlink" title="Mybatis中的模板方法模式"></a>Mybatis中的模板方法模式</h2><p>实际中由于模板方法很好的兼容性,因此经常与其他设计模式混用,并且在模板类之上增加一个接口来提高系统的灵活性.因此模板类经常作为中间层来使用,比如Mybatis的<code>Executor</code>的设计,其中在<code>Executor</code>与具体实现类之间增加中间层<code>BaseExecutor</code>作为模板类.<br><img src="http://imgblog.mrdear.cn/1521983235.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<p>作为模板类的<code>BaseExecutor</code>到底做了什么呢?举一个代码比较短的例子,下面的代码是Mybatis缓存中获取不到时执行去DB查询所需要的结果,顺便再放入缓存中的流程.其中<code>doQuery()</code>方法便是一个抽象方法,其被延迟到子类中来实现.而缓存是所有查询都需要的功能,因此每一个查询都会去执行.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">private &lt;E&gt; List&lt;E&gt; queryFromDatabase(MappedStatement ms, Object parameter, RowBounds rowBounds, ResultHandler resultHandler, CacheKey key, BoundSql boundSql) throws SQLException &#123;</span><br><span class="line">  List&lt;E&gt; list;</span><br><span class="line">  localCache.putObject(key, EXECUTION_PLACEHOLDER);</span><br><span class="line">  try &#123;</span><br><span class="line">      // doQuery具体查询策略延迟到子类中来实现</span><br><span class="line">    list = doQuery(ms, parameter, rowBounds, resultHandler, boundSql);</span><br><span class="line">  &#125; finally &#123;</span><br><span class="line">    localCache.removeObject(key);</span><br><span class="line">  &#125;</span><br><span class="line">  localCache.putObject(key, list);</span><br><span class="line">  if (ms.getStatementType() == StatementType.CALLABLE) &#123;</span><br><span class="line">    localOutputParameterCache.putObject(key, parameter);</span><br><span class="line">  &#125;</span><br><span class="line">  return list;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>BaseExecutor</code>作为模板类的同时其还是抽象父类,因此还可以实现一些子类锁需要的公共方法,比如事务的提交与回滚,模板类的本质还是抽象类,同时也是父类,当然可以有这些公共方法的定义.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Override</span><br><span class="line">public void commit(boolean required) throws SQLException &#123;</span><br><span class="line">  if (closed) &#123;</span><br><span class="line">    throw new ExecutorException(&quot;Cannot commit, transaction is already closed&quot;);</span><br><span class="line">  &#125;</span><br><span class="line">  clearLocalCache();</span><br><span class="line">  flushStatements();</span><br><span class="line">  if (required) &#123;</span><br><span class="line">    transaction.commit();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">@Override</span><br><span class="line">public void rollback(boolean required) throws SQLException &#123;</span><br><span class="line">  if (!closed) &#123;</span><br><span class="line">    try &#123;</span><br><span class="line">      clearLocalCache();</span><br><span class="line">      flushStatements(true);</span><br><span class="line">    &#125; finally &#123;</span><br><span class="line">      if (required) &#123;</span><br><span class="line">        transaction.rollback();</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>总结来说<strong>模板方法类作为上级,那么其要做的事情就是针对接口提出的需求进行规划,自己实现一部分,然后把需求拆分成更加细小的任务延迟到子类中实现,这是模板的责任与目的.</strong></p>
<h2 id="Spring-JDBC中的模板方法模式"><a href="#Spring-JDBC中的模板方法模式" class="headerlink" title="Spring JDBC中的模板方法模式"></a>Spring JDBC中的模板方法模式</h2><p>模板的另一种实现方式就是Java的接口回调机制,固定好方法模板后接收一个行为策略接口作为参数,模板中执行该接口的方法,比如Spring中的JdbcTemplate就是这样的设计.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public &lt;T&gt; T execute(StatementCallback&lt;T&gt; action) throws DataAccessException &#123;</span><br><span class="line">       ...</span><br><span class="line">		stmt = con.createStatement();</span><br><span class="line">		applyStatementSettings(stmt);</span><br><span class="line">		 // 执行传入的策略接口</span><br><span class="line">		T result = action.doInStatement(stmt);</span><br><span class="line">		handleWarnings(stmt);</span><br><span class="line">		return result;</span><br><span class="line">   	...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>因为篇幅原因,这里删减了很多代码,但是可以看出来这种方式实现有点策略模式的味道.其需要两个东西</p>
<ol>
<li>方法模板,在这里是该<code>execute()</code>方法</li>
<li>策略接口,这里是<code>StatementCallback</code>,其本质上是一个函数是接口.</li>
</ol>
<p>这种模式的好处自然是灵活,通过策略接口可以把行为分离出来并且可以灵活的在运行时替换掉对应的行为,雨点策略模式的味道.<br>那么这种到底是策略模式还是模板方法模式呢?个人认为没必要纠结这些,说他是哪个都有挺充分的理由,但是设计模式本身就是思想的体现,很多模式与模式之间都互相有思想的重叠,具体业务场景不同选择不同.</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>模板方法在我看来更像是一个产品经理,而接口就是需求方,面对需求方模板做的事情是制定合理的统一执行计划,然后把需求拆分成更加细小的任务,分配到对应的程序员身上.<br>另外模板方法模式是一种变与不变的思想体现,固定不变的,提出变化的,这样增加系统的灵活性,就像圆规画圆一样,先固定中心点,然后另一个脚随意扩展.这种思想是很实用,比如产品往往提出需求后,程序员就需要考虑具体的对象模型,那么此时比较好的做法就是尽早固定出不会变化的对象,然后其他功能在此基础上做关联来扩展,最后希望本文对你有启发.</p>
<h2 id="扩展想法"><a href="#扩展想法" class="headerlink" title="扩展想法"></a>扩展想法</h2><p>在日常的业务开发中我很少看到继承相关的代码,可能是和面向对象设计中提到多用组合少用继承这一原则有关.在<code>Effective Java</code>中<code>第16条: 复合优先于继承</code>这一小节中中举了如下例子:<br>实现<code>HashSet</code>的计数功能,因此复写了<code>add</code>,<code>addAll</code>方法,然而因为对于父类实现逻辑的不了解(addAll实际上是循环调用add)导致了bug.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class InstrumentedHashSet&lt;E&gt; extends HashSet&lt;E&gt; &#123;</span><br><span class="line">  private Integer count;</span><br><span class="line"></span><br><span class="line">  @Override</span><br><span class="line">  public boolean add(E e) &#123;</span><br><span class="line">    count++;</span><br><span class="line">    return super.add(e);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  @Override</span><br><span class="line">  public boolean addAll(Collection&lt;? extends E&gt; c) &#123;</span><br><span class="line">    count = count + c.size();</span><br><span class="line">    return super.addAll(c);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>这个问题的根本原因是什么?</strong><br>我认为是 <strong>HashSet并不是专门为继承设计的类</strong>,因此去继承就出现了上述的问题.这么就代表代码中不应该使用继承吗?当然不是.</p>
<p>随后在<code>第17条: 要么为继承而设计,并提供说明文档,要么就禁止继承</code>指出为继承而设计是一种可取的行为,在我看来模板方法设计模式就是一种为继承而设计的方式.模板方法设计模式主要有两点本意:<br>1.尽早的使用模板类,也就是Abstract或者Base开头的类来让实现类分叉,分叉的越早,对于结构上的理解就越清晰,比如下方Spring MVC对URL的处理,可以很清晰的看到一种处理是定位到具体的执行方法<code>AbstractHandlerMethodMapping</code>,一种是定位到另一个URL,可能是静态资源,可能是其他页面<code>AbstractUrlHandlerMapping</code>.<br><img src="http://imgblog.mrdear.cn/1524840843.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<p>2.降低子类的实现接口的复杂度,主要是模板类中实现了接口的方法,然后把不变的固定,变化的使用抽象接口延迟到子类中,让子类的任务更加清晰合理.比如<code>Mybatis的BaseExector</code>就通过<code>doQuery()</code>把变化的查询步骤延迟到了子类中实现.另外有一种模板类是单纯的提供代码复用,其可以当成是不含有业务属性的一个方法库,提供对所有子类都有用的公共方法.这个我在我公司订单系统中采用,如下图所示(这里只列出一部分,实际上最下层的Service还会承担更多角色),<code>AbstractOrderService</code>只是单纯的提供数据获取,比如获取用户信息,获取优惠券信息等方法,具体的创建逻辑在子类中,比如<code>BizVipOrderService</code>创建vip订单,<code>BizResearchOrderService</code>创建研究员订单.当子类有通性是则可以在上层增加专属抽象类来提前分叉,最终保证每一个订单创建走的流程都是可控的,当要修改某一个订单的规则时,比如vip订单可以使用优惠券,则只需要改其子类而不用担心对其他的订单类型创建有影响.<br>最后通过组合类提供对外的入口访问.降低外部操作的复杂性.另外最底层子类也可以实现其他接口,比如观察者来实现状态更改的通知处理.<br><img src="http://imgblog.mrdear.cn/1524841389.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<p>那么<strong>这种设计就是为继承而设计</strong>,这种设计出来的类有一个特点,通常是以<code>Abstract/Base</code>开头,其就是为了继承,而不想让其他人实例化自身.最后继承作为面向对象的一大特性,掖着不用还能叫面向对象编程吗?</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式,模板方法</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 - 策略设计模式</title>
    <url>/2019/06/04/DesignPattern/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%AD%96%E7%95%A5%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="策略设计模式"><a href="#策略设计模式" class="headerlink" title="策略设计模式"></a>策略设计模式</h1><blockquote>
<p>本文转载自<a href="https://mrdear.cn/" target="_blank" rel="noopener">屈定’s Blog</a></p>
</blockquote>
<p>策略模式是一种简单的设计模式,但是其在业务开发中是一种非常有用的设计模式.举个例子,当你的业务需要针对不同的场景(可以简单理解为枚举类),执行不同的策略时那么使用策略模式可以帮助你更好的写出低耦合与高可扩展的代码.</p>
<hr>
<h2 id="标准策略模式"><a href="#标准策略模式" class="headerlink" title="标准策略模式"></a>标准策略模式</h2><p>策略模式: 把具体的算法从业务逻辑中分离出来,使得业务类不必膨胀,并且业务与具体算法分离,便于扩展新算法.类图如下:<br><img src="http://imgblog.mrdear.cn/1521374929.png?imageMogr2/thumbnail/!100p" alt="img"><br>使用策略模式往往策略上有着相似的输入参数以及输出结果,或者有一个公共的上下文,便于抽象出策略接口<code>Strategy</code>,然后对应的业务Service只需要引用<code>StrategyContext</code>填充具体的策略完成自己的需求.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">new StrategyContext(new CouponStrategy()).sendPrize(uid, prize)</span><br></pre></td></tr></table></figure>

<p>这是标准的策略模式,这种模式在如今的IOC下应用场景并不是很多,该模式有不少缺点</p>
<ol>
<li>客户端必须知道所有的策略,然后手动选择具体的策略放入到Context中执行.</li>
<li>仍旧无法避免if/else逻辑,针对大多数场景下都是根据条件分支判断来选择具体的策略,那么在客户端耦合具体策略的情况下这个是没法避免的</li>
</ol>
<h2 id="策略枚举模式"><a href="#策略枚举模式" class="headerlink" title="策略枚举模式"></a>策略枚举模式</h2><p>Java的枚举类可以实现接口,而且枚举常量天然的可以与具体行为绑定,那么两者结合起来就是一种很棒的策略枚举模式(笔者自己起的名字).</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public enum StrategyEnum implements Strategy&#123;</span><br><span class="line">  COUPON(1) &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public boolean sendPrize(Long uid, String prize) &#123;</span><br><span class="line">      //发放优惠券</span><br><span class="line">      return true;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;,</span><br><span class="line">  RMB(2) &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public boolean sendPrize(Long uid, String prize) &#123;</span><br><span class="line">      //发放RMB</span><br><span class="line">      return true;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  ;</span><br><span class="line">  private int code;</span><br><span class="line">  StrategyEnum(int code) &#123;</span><br><span class="line">    this.code = code;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>相比标准的模式,该模式省掉了Context类,而且符合大多数场景,比如用户获得礼品,该礼品对应的是Mysql的一条记录,该记录有type标识是优惠券(Coupon)还是RMB,当DAO从DB查询出来后根据typeCode,定位到具体的枚举类,然后执行其<code>sendPrize(Long uid, String prize)</code>完成逻辑.这个流程很清晰.<br>基于枚举的策略模式也有一些问题:</p>
<ol>
<li>枚举类无法外部实例化,因此无法被IOC管理,往往策略实现都是复杂的依赖众多其他服务,那么这种时候枚举类就无从下手</li>
</ol>
<h2 id="IOC配合下的策略模式"><a href="#IOC配合下的策略模式" class="headerlink" title="IOC配合下的策略模式"></a>IOC配合下的策略模式</h2><p>实践中,客户端往往不关心具体的实现类是如何实现的,他只需要知道有这个实现类的存在,其能帮我完成任务,得到我要的结果,所以在标准的策略模式基础上,扩展Context类,让其担任选择策略的能力,而不是客户端手动选择具体的策略,也就是具体策略实现与客户端解耦,转用枚举常量来代表其所希望的策略.改进后的Context(依赖Spring IOC)如下:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Component</span><br><span class="line">public class StrategyContext implements InitializingBean &#123;</span><br><span class="line"></span><br><span class="line">  @Resource</span><br><span class="line">  private Strategy couponStrategy;</span><br><span class="line"></span><br><span class="line">  @Resource</span><br><span class="line">  private Strategy RMBStrategy;</span><br><span class="line">  /**</span><br><span class="line">   * 保存策略与具体实现的Map</span><br><span class="line">   */</span><br><span class="line">  private static Map&lt;StrategyEnum, Strategy&gt; strategyMap = new HashMap&lt;&gt;(2);</span><br><span class="line"></span><br><span class="line">  public Strategy getStrategy(StrategyEnum strategyEnum) &#123;</span><br><span class="line">    return strategyMap.get(strategyEnum);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  @Override</span><br><span class="line">  public void afterPropertiesSet() throws Exception &#123;</span><br><span class="line">    strategyMap.put(StrategyEnum.COUPON, couponStrategy);</span><br><span class="line">    strategyMap.put(StrategyEnum.RMB, RMBStrategy);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>客户端调用时使用</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">strategyContext.getStrategy(StrategyEnum.COUPON).sendPrize(uid,prize)</span><br></pre></td></tr></table></figure>

<p>这里的Context相当于中间层,提供的是外观模式的功能,当新增策略时只需要新增对应的枚举类,以及具体的实现策略,在Context中添加一个新的枚举与实现类关系.客户端代码基本不要任何改变.<br><strong>补充:</strong> 更加优雅的做法是利用Spring的事件机制,在Spring初始化完毕后再构建整个策略Map,可以参考我在观察者模式中所使用到的方法.<br><a href="https://mrdear.cn/2018/04/20/experience/design_patterns--observer/" target="_blank" rel="noopener">设计模式–观察者模式的思考</a></p>
<h2 id="策略模式的本质"><a href="#策略模式的本质" class="headerlink" title="策略模式的本质"></a>策略模式的本质</h2><p>策略模式的本质是把复杂的算法从一个类中提取出来,用一种合理的方式管理起来,避免业务类的膨胀.<br>对于扩展只需要新增策略,而不需要怎么动业务代码.对于修改也只需要修改具体的策略类.业务类与策略成功的实现了低耦合.<br>与IOC的配合下可以更加彻底的与业务类解耦,其间只需要枚举类与策略接口进行联系,对于代码的扩展性更加有力.</p>
<h2 id="与状态模式的关系"><a href="#与状态模式的关系" class="headerlink" title="与状态模式的关系"></a>与状态模式的关系</h2><p><strong>状态设计模式</strong>的类图结构与策略模式几乎是一致的.从逻辑上状态是平行的无法互相替换,但是策略与策略之间是可以完全替换的,只是实现方式的不同.在选择设计模式的时候是根据这一点来区分,代码上的体现是对于<strong>状态设计模式以State结尾</strong>,对于<strong>策略设计模式以Strategy</strong>结尾,让开发人员第一眼看过去就能明白整个设计的思路最佳.</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式,策略设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 - 适配器模式</title>
    <url>/2019/06/04/DesignPattern/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E9%80%82%E9%85%8D%E5%99%A8%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="设计模式–适配器模式的思考"><a href="#设计模式–适配器模式的思考" class="headerlink" title="设计模式–适配器模式的思考"></a>设计模式–适配器模式的思考</h1><hr>
<blockquote>
<p>本文转载自<a href="https://mrdear.cn/" target="_blank" rel="noopener">屈定’s Blog</a></p>
</blockquote>
<p>个人认为适配器模式是一种加中间层来解决问题的思想,为的是减少开发工作量,提高代码复用率.另外在对于第三方的服务中使用适配器层则可以很好的把自己系统与第三方依赖解耦,降低依赖.</p>
<h2 id="什么是适配器模式"><a href="#什么是适配器模式" class="headerlink" title="什么是适配器模式"></a>什么是适配器模式</h2><p>适配器模式: 将一个类的接口转换为客户所期望的另一个接口.适配器让原本接口不兼容的类可以合作无间.类图如下:<br><img src="http://imgblog.mrdear.cn/1520949114.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<p>Client: 调用方<br>Target: 需要提供的新功能<br>AdaptedObject: 系统中原本存在的类似本次需要提供的新功能的类<br>Adapter: Target的实现类,主要负责该功能的实现,其内部持有<code>AdaptedObject</code>的对象,利用其对象完成本次需要提供的新功能.</p>
<p>整个流程大概如下:<br>1.客户通过目标接口调用适配器的方法发出请求.<br>2.适配器(Adapter)使用被适配器(AdaptedObject)已有的功能完成客户所期望的新功能<br>3.客户收到调用结果,但是并不知道是适配器起到的转换作用.<br>那么<code>Adapter</code>利用已经完成的<code>AdaptedObject</code>类实现本次提供的新功能,这一过程就是<strong>适配</strong>.</p>
<h2 id="Java-I-O中的适配器"><a href="#Java-I-O中的适配器" class="headerlink" title="Java I/O中的适配器"></a>Java I/O中的适配器</h2><p>在Java I/O中有把字节流转换为字符流的类<code>java.io.InputStreamReader</code>以及<code>java.io.OutputStreamWriter</code>.那么这两个类实际上使用的就是适配器模式<br>以<code>InputStreamReader</code>为例,其继承了<code>Reader</code>类,所提供的功能是把字节流转换为字符流,其内部拥有<code>StreamDecoder</code>这一实例,所有的转换工作是由该实例完成.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public int read(char cbuf[], int offset, int length) throws IOException &#123;</span><br><span class="line">    // 使用被适配器的功能</span><br><span class="line">    return sd.read(cbuf, offset, length);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>那么在这个例子中<br>Client是调用方,也就是我们开发人员<br>Target是<code>Reader</code>这个抽象类.<br>AdaptedObject是<code>StreamDecoder</code>,利用的是其功能.<br>Adapter是<code>InputStreamReader</code></p>
<h2 id="Java-Set集合中的适配器"><a href="#Java-Set集合中的适配器" class="headerlink" title="Java Set集合中的适配器"></a>Java Set集合中的适配器</h2><p>Java中的Set集合有者无序,唯一元素,查找复杂度O(1)等特性.这些特性Map数据结构的key是完全符合的,那么就可以利用适配器模式来完成Set的功能.<br>以<code>HashSet</code>为例,其内部持有的是一个值为固定Object的Map,如下图<br><img src="http://imgblog.mrdear.cn/1520951534.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<p>其所有的操作会通过<code>HashSet</code>这个适配器来操作<code>HashMap</code>这个被适配器.比如:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public Iterator&lt;E&gt; iterator() &#123;</span><br><span class="line">        return map.keySet().iterator();</span><br><span class="line">&#125;</span><br><span class="line"> public boolean add(E e) &#123;</span><br><span class="line">        return map.put(e, PRESENT)==null;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>Client是调用方,也就是我们开发人员<br>Target是<code>Set</code>这个接口.<br>AdaptedObject是<code>HashMap</code>,利用的是其功能.<br>Adapter是<code>HashSet</code></p>
<h2 id="Mybatis中的适配器模式"><a href="#Mybatis中的适配器模式" class="headerlink" title="Mybatis中的适配器模式"></a>Mybatis中的适配器模式</h2><p>Mybatis作为一款通用框架,对于日志处理必然需要适配到各种日志框架,比如<code>slf4j</code>,<code>log4j</code>,<code>logback</code>等,每个日志的API多多少少有点不同,这种情况下适配器模式就起到了转换的作用.<br>以下图由于实现类太多,只列取了几个.<br><img src="http://imgblog.mrdear.cn/1520995813.png?imageMogr2/thumbnail/!100p" alt="img"><br>Mybatis有自己的<code>org.apache.ibatis.logging.Log</code>接口,框架内部使用的都是自己的Log,具体使用哪一个Log是由配置中的适配器决定的.<br>以<code>org.apache.ibatis.logging.log4j2.Log4j2LoggerImpl</code>适配器为例,<code>org.apache.logging.log4j.Logger</code>为被适配者.<code>Log4j2LoggerImpl</code>是适配器,起到了转换的作用.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class Log4j2LoggerImpl implements Log &#123;</span><br><span class="line">  </span><br><span class="line">  private static final Marker MARKER = MarkerManager.getMarker(LogFactory.MARKER);</span><br><span class="line">  //被适配者</span><br><span class="line">  private final org.apache.logging.log4j.Logger log;</span><br><span class="line"></span><br><span class="line">  public Log4j2LoggerImpl(Logger logger) &#123;</span><br><span class="line">    log = logger;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  @Override</span><br><span class="line">  public boolean isDebugEnabled() &#123;</span><br><span class="line">    return log.isDebugEnabled();</span><br><span class="line">  &#125;</span><br><span class="line">  .....</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="与装饰者模式的区别"><a href="#与装饰者模式的区别" class="headerlink" title="与装饰者模式的区别"></a>与装饰者模式的区别</h2><p>个人认为这两种设计模式是完全不同的思想:<br><strong>装饰者模式</strong>本意是增强功能,其装饰者与被装饰者对于调用方是很清晰的,比如<code>ContreteDecoratorA decoratorA = new ContreteDecoratorA(new ComponentInterfaceImpl());</code>就很清晰的知道使用<code>ContreteDecoratorA</code>装饰了<code>ComponentInterfaceImpl</code>.另外<code>ContreteDecoratorA</code>并没有改变<code>ComponentInterfaceImpl</code>的功能提供出去,而是为其进行了增强处理.<br><strong>适配器模式</strong>本意是复用已有的代码,对已经存在的功能进行包装转换,以另一种形式提供出去.比如<code>HashSet</code>,对于调用方来说其内部使用的<code>HashMap</code>是不可见的,调用方不关心内部被适配者是谁,只是关注该功能本身也就是<code>Set</code>接口.<br>要说相同点的话那就是都是组合复用思想对一个对象进行包装,但其目的有着本质的区别.还望好好理解.</p>
<h2 id="与外观模式的区别"><a href="#与外观模式的区别" class="headerlink" title="与外观模式的区别"></a>与外观模式的区别</h2><p><strong>外观模式</strong>本意是把一组复杂的关联行为进行包装,提供一个面向开发人员更为简单的使用方式.举个例子,你觉得JDBC方式不太好用,因此写了个DBUtils这种封装类,实际上就是一种外观模式,与适配器还是有着很大的区别.</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式,适配器</tag>
      </tags>
  </entry>
  <entry>
    <title>你写的代码,是别人的噩梦吗</title>
    <url>/2019/06/04/SoftwareEngineer/%E4%BD%A0%E5%86%99%E7%9A%84%E4%BB%A3%E7%A0%81%E6%98%AF%E5%88%AB%E4%BA%BA%E7%9A%84%E5%99%A9%E6%A2%A6%E5%90%97/</url>
    <content><![CDATA[<h1 id="你写的代码-是别人的噩梦吗"><a href="#你写的代码-是别人的噩梦吗" class="headerlink" title="你写的代码,是别人的噩梦吗"></a>你写的代码,是别人的噩梦吗</h1><hr>
<blockquote>
<p>本文转自<a href="https://mrdear.cn/" target="_blank" rel="noopener">屈定’s Blog</a></p>
<p>Frank，是来自阿里国际技术事业部的高级技术专家，从业十年，也是一位英语说到飞起的型男。今天他将与大家聊聊关于企业应用架构实践的话题。</p>
</blockquote>
<p><img src="http://imgblog.mrdear.cn/1520910556.png?imageMogr2/thumbnail/!100p" alt="阿里高级技术专家Frank"><br>从业这么多年，接触过银行的应用，Apple的应用，eBay的应用和现在阿里的应用，虽然分属于不同的公司，使用了不同的架构，但有一个共同点就是都很复杂。导致复杂性的原因有很多，如果从架构的层面看，主要有两点，一个是架构设计过于复杂，层次太多能把人绕晕。另一个是根本就没架构，ServiceImpl作为上帝类包揽一切，一杆捅到DAO（就简单场景而言，这种Transaction Script也还凑合，至少实现上手都快），这种人为的复杂性导致系统越来越臃肿，越来越难维护，酱缸的老代码发出一阵阵恶臭，新来的同学，往往要捂着鼻子抠几天甚至几个月，才能理清系统和业务脉络，然后又一头扎进各种bug fix，业务修补的恶性循环中，暗无天日！<br><img src="http://imgblog.mrdear.cn/1520910594.png?imageMogr2/thumbnail/!100p" alt="img"><br>CRM作为阿里最老的应用系统，自然也逃不过这样的宿命。不甘如此的我们开始反思到底是什么造成了系统复杂性？我们到底能不能通过架构来治理这种复杂性？基于这个出发点，我们团队开始了一段非常有意义的架构重构之旅（Redefine theArch），期间我们参考了SalesForce，TMF2.0，汇金和盒马的架构，从他们那里汲取了很多有价值的输入，再结合我们自己的思考最终形成了我们自己现在的基于扩展点+元数据+CQRS+DDD的应用架构。该架构的特点是可扩展性好，很好的贯彻了OO思想，有一套完整的规范标准，并采用了CQRS和领域建模技术，在很大程度上可以降低应用的复杂度。本文主要阐述了我们的思考过程和架构实现，希望能对在路上的你有所帮助。</p>
<h2 id="复杂性来自哪里？"><a href="#复杂性来自哪里？" class="headerlink" title="复杂性来自哪里？"></a>复杂性来自哪里？</h2><p>经过我们分析、讨论，发现造成现在系统异常复杂的罪魁祸首主要来自以下四个方面：</p>
<h3 id="可扩展性差"><a href="#可扩展性差" class="headerlink" title="可扩展性差"></a>可扩展性差</h3><p>对于只有一个业务的简单场景，并不需要扩展，问题也不突出，这也是为什么这个点经常被忽略的原因，因为我们大部分的系统都是从单一业务开始的。但是随着支持的业务越来越多，代码里面开始出现大量的if-else逻辑，这个时候代码开始有坏味道，没闻到的同学就这么继续往上堆，闻到的同学会重构一下，但因为系统没有统一的可扩展架构，重构的技法也各不相同，这种代码的不一致性也是一种理解上的复杂度。久而久之，系统就变得复杂难维护。</p>
<p>像我们CRM应用，有N个业务方，每个业务方又有N个租户，如果都要用if-else判断业务差异，那简直就是惨绝人寰。其实这种扩展点（ExtensionPoint），或者叫插件（Plug-in）的设计在架构设计中是非常普遍的。比较成功的案例有eclipse的plug-in机制，集团的TMF2.0架构。还有一个扩展性需求就是字段扩展，这一点对SaaS应用尤为重要，因为有很多客户定制化需求，但是我们很多系统也没有统一的字段扩展方案。</p>
<h3 id="面向过程"><a href="#面向过程" class="headerlink" title="面向过程"></a>面向过程</h3><p>是的，不管你承认与否，很多时候，我们都是操着面向对象的语言干着面向过程的勾当。面向对象不仅是一个语言，更是一种思维方式。在我们追逐云计算、深度学习、区块链这些技术热点的时候，静下心来问问自己我们是不是真的掌握了OOD；在我们强调工程师要具备业务Sense，产品Sense，数据Sense，算法Sense，XXSense的时候，是不是忽略了对工程能力的要求。</p>
<p>据我观察大部分工程师（包括我自己）的OO能力还远没有达到精通的程度，这种OO思想的缺乏主要体现在两个方面，一个是很多同学不了解SOLID原则，不懂设计模式，不会画UML图，或者只是知道，但从来不会运用到实践中；另一个是不会进行领域建模，关于领域建模争论已经很多了，我的观点是DDD很好，但不是银弹，用和不用取决于场景。但不管怎样，请你抛开偏见，好好的研读一下EricEvans的《领域驱动设计》，如果有认知升级的感悟，恭喜你，你进阶了。</p>
<p>我个人认为DDD最大的好处是将业务语义显现化，把原先晦涩难懂的业务算法逻辑，通过领域对象（Domain Object），统一语言（Ubiquitous Language）将领域概念清晰的显性化表达出来。相信我，这种表达带来的代码可读性的提升，会让接手你代码的人对你心怀感恩的。借用Abelson的一句话是</p>
<blockquote>
<p>Programs must be written for people to read, and only incidentally for machines to execute.</p>
</blockquote>
<p>所以强烈谴责那些不顾他人感受的编码行为。<br><img src="http://imgblog.mrdear.cn/1520910683.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<h3 id="分层不合理"><a href="#分层不合理" class="headerlink" title="分层不合理"></a>分层不合理</h3><p>俗话说的好，All problemsin computer science can be solved by another level of indirection（计算机科学领域的任何问题都可以通过增加一个间接的中间层来解决），怎样？是不是感受到间接层的强大了。分层最大的好处就是分离关注点，让每一层只解决该层关注的问题，从而将复杂的问题简化，起到分而治之的作用。我们平时看到的MVC，pipeline，以及各种valve的模式，都是这个道理。</p>
<p>好吧，那是不是层次越多越好，越灵活呢。当然不是，就像我开篇说的，过多的层次不仅不能带来好处，反而会增加系统的复杂性和降低系统性能。就拿ISO的网络七层协议来说，你这个七层分的很清楚，很好，但也很繁琐，四层就够了嘛。再比如我前面提到的过度设计的例子，如果没记错的话应该是Apple的Directory Service应用，整个系统有7层之多，把什么validator，assembler都当成一个层次来对待，能不复杂么。所以分层太多和没有分层都会导致系统复杂度的上升，因此我们的原则是不可以没有分层，但是只分有必要的层。</p>
<h3 id="随心所欲"><a href="#随心所欲" class="headerlink" title="随心所欲"></a>随心所欲</h3><p>随心所欲是因为缺少规范和约束。这个规范非常非常非常的重要（重要事情说三遍），但也是最容易被无视的点，其结果就是架构的consistency被严重破坏，代码的可维护性将急剧下降，国将不国，架构将形同虚设。有同学会说不就是个naming的问题么，不就是个分包的问题么，不就是2个module还是3个module的问题么，只要功能能跑起来，这些问题都是小问题。是的，对于这些同学，我再丢给你一句名言“Just because you can, doesn’t mean you should”。就拿package来说，它不仅仅是一个放一堆类的地方，更是一种表达机制，当你将一些类放到Package中时，相当于告诉下一位看到你设计的开发人员要把这些类放在一起考虑。</p>
<p>理想很丰满，现实很骨感，规范的执行是个大问题，最好能在架构层面进行约束，例如在我们架构中，扩展点必须以ExtPt结尾，扩展实现必须以Ext结尾，你不这么写就会给你抛异常。但是架构的约束毕竟有限，更多的还是要靠Code Review，暂时没想到什么更好的办法。这种对架构约束的近似严苛follow，确保了系统的consistency，最终形成了一个规整的收纳箱（如下图所示），就像我和团队说的，我们在评估代码改动点时，应该可以像Hash查找一样，直接定位到对应的module，对应的package里面对应的class。而不是到“一锅粥”里去慢慢抠。<br><img src="http://imgblog.mrdear.cn/1520910731.png?imageMogr2/thumbnail/!100p" alt="img"><br>本章节最后，上一张我们老系统中比较典型的代码，也许你可以从中看到你自己应用的影子。<br><img src="http://imgblog.mrdear.cn/1520910892.png?imageMogr2/thumbnail/!100p" alt="复杂性应对之道"><br>知道了问题所在，接下来看下我们是如何一个个解决这些问题的。回头站在山顶再看这些解决方案时，每个都不足为奇，但当你还“身在此山中”的时候，这个拨开层层迷雾，看到山的全貌的过程，并不是想象的那么容易。庆幸的是我团队在艰难跋涉之后，终有所收获。</p>
<h2 id="1、扩展点设计"><a href="#1、扩展点设计" class="headerlink" title="1、扩展点设计"></a>1、扩展点设计</h2><p>扩展点的设计思想主要得益于TMF2.0的启发，其实这种设计思想也一直在用，但都是在局部的代码重构和优化，比如基于Strategy Pattern的扩展，但是一直没有找到一个很好的固化到框架中的方法。直到毗卢到团队分享，给了我们两个关键的提示，一个是业务身份识别，用他的话说，如果当时TMF1.0如果有身份识别的话，就没有TMF2.0什么事了；另一个是抽象的扩展点机制。</p>
<h3 id="身份识别"><a href="#身份识别" class="headerlink" title="身份识别"></a>身份识别</h3><p>业务身份识别在我们的应用中非常重要，因为我们的CRM系统要服务不同的业务方，而且每个业务方又有多个租户。比如中供销售，中供拍档，中供商家都是不同的业务方，而拍档下的每个公司，中供商家下的每个供应商又是不同的租户。所以传统的基于多租户（TenantId）的业务身份识别还不能满足我们的要求，于是在此基础上我们又引入了业务码（BizCode）来标识业务。</p>
<p>所以我们的业务身份实际上是（BizCode，TenantId）二元组。在每一个业务身份下面，又可以有多个扩展点（ExtensionPoint），所以一个扩展点实现（Extension）实际上是一个三维空间中的向量。借鉴Maven Coordinate的概念我给它起了个名字叫扩展坐标（Extension Coordinate），这个坐标可以用（ExtensionPoint，BizCode，TenantId）来唯一标识。<br><img src="http://imgblog.mrdear.cn/1520911038.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<h3 id="扩展点"><a href="#扩展点" class="headerlink" title="扩展点"></a>扩展点</h3><p>扩展点的设计是这样的，所有的扩展点（ExtensionPoint）必须通过接口申明，扩展实现（Extension）是通过Annotation的方式标注的，Extension里面使用BizCode和TenantId两个属性用来标识身份，框架的Bootstrap类会在Spring启动的时候做类扫描，进行Extension注册，在Runtime的时候，通过TenantContext来选择要使用的Extension。TenantContext是通过Interceptor在调用业务逻辑之前进行初始化的。整个过程如下图所示：<br><img src="http://imgblog.mrdear.cn/1520911573.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<h2 id="2、面向对象"><a href="#2、面向对象" class="headerlink" title="2、面向对象"></a>2、面向对象</h2><h3 id="领域建模"><a href="#领域建模" class="headerlink" title="领域建模"></a>领域建模</h3><p>准确的说DDD不是一个架构，而是思想和方法论。所以在架构层面我们并没有强制约束要使用DDD，但对于像我们这样的复杂业务场景，我们强烈建议使用DDD代替事务脚本（TS: Transaction Script）。因为TS的贫血模式，里面只有数据结构，完全没有对象（数据+行为）的概念，这也是为什么我们叫它是面向过程的原因。然而DDD是面向对象的，是一种知识丰富的设计（Knowledge Rich Design），怎么理解？，就是通过领域对象（Domain Object），领域语言（Ubiquitous Language）将核心的领域概念通过代码的形式表达出来，从而增加代码的可理解性。这里的领域核心不仅仅是业务里的“名词”，所有的业务活动和规则如同实体一样，都需要明确的表达出来。</p>
<p>例如前面典型代码图中所展示的，分配策略（DistributionPolicy）你把它隐藏在一堆业务逻辑中，没有人知道它是干什么的，也不会把它当成一个重要的领域概念去重视。但是你把它抽出来，凸显出来，给它一个合理的命名叫DistributionPolicy，后面的人一看就明白了，哦，这是一个分配策略，这样理解和使用起来就容易的多了，添加新的策略也更方便，不需要改原来的代码了。</p>
<p>所以说好的代码不仅要让程序员能读懂，还要能让领域专家也能读懂。再比如在CRM领域中，公海和私海是非常重要领域概念，是用来做领地（Territory）划分的，每个销售人员只能销售私海（自己领地）内的客户，不能越界。但是在我们的代码中却没有这两个实体（Entity），也没有相应的语言和其对应，这就导致了领域专家描述的，和我们日常沟通的，以及我们模型和代码呈现的都是相互割裂的，没有关联性。这就给后面系统维护的同学造成了极大的困扰，因为所有关于公海私海的操作，都是散落着各处的repeat itself的逻辑代码，导致看不懂也没办法维护。</p>
<p>所以当尚学把这两个领域概念抽象成实体之后，整个模型和代码都一下子变清晰很多。在加上上面介绍的把业务规则显现化，极大的提升了代码的可读性和可扩展性。用尚学的话说，用DDD写代码，他找到了创作的感觉，而不仅仅是码农式Coding。下图是销售域的简要领域模型，但基本上能表达出销售域的核心领域概念。<br><img src="http://imgblog.mrdear.cn/1520912884.png?imageMogr2/thumbnail/!100p" alt="img"><br>关于CQRS简要说一下，我们只使用了Command，Query分离的概念，并没有使用Event Sourcing，原因很简单—不需要。关于Command的实现我们使用了命令模式，因此以前的ServiceImpl的职责就只是一个Facade，所有的处理逻辑都在CommandExecutor里面。</p>
<h3 id="SOLID"><a href="#SOLID" class="headerlink" title="SOLID"></a>SOLID</h3><p>SOLID是单一职责原则(SRP)，开闭原则(OCP)，里氏替换原则(LSP)，接口隔离原则(ISP)和依赖倒置原则(DIP)的缩写，原则是要比模式（Design Pattern）更基础更重要的指导准则，是面向对象设计的Bible。深入理解后，会极大的提升我们的OOD能力和代码质量。比如我在开篇提到的ServiceImpl上帝类的例子，很明显就是违背了单一职责，你一个类把所有事情都做了，把不是你的功能也往自己身上揽，所以你的内聚性就会很差，内聚性差将导致代码很难被复用，不能复用，只能复制（Repeat Yourself），其结果就是一团乱麻。<br><img src="http://imgblog.mrdear.cn/1520912916.png?imageMogr2/thumbnail/!100p" alt="img"><br>再比如在java应用中使用logger框架有很多选择，什么log4j，logback，common logging等，每个logger的API和用法都稍有不同，有的需要用isLoggable()来进行预判断以便提高性能，有的则不需要。对于要切换不同的logger框架的情形，就更是头疼了，有可能要改动很多地方。产生这些不便的原因是我们直接依赖了logger框架，应用和框架的耦合性很高。</p>
<p>怎么破？ 遵循下依赖倒置原则就能很容易解决，依赖倒置就是你不要直接依赖我，你和我都同时依赖一个接口（所以有时候也叫面向接口的编程），这样我们之间就解耦了，依赖和被依赖方都可以自由改动了。<br><img src="http://imgblog.mrdear.cn/1520912931.png?imageMogr2/thumbnail/!100p" alt="img"><br>在我们的框架设计中，这种对SOLID的遵循也是随处可见，Service Facade设计思想来自于单一职责SRP；扩展点设计符合关闭原则OCP；日志设计，以及Repository和Tunnel的交互就用到了依赖倒置DIP原则，这样的点还有很多，就不一一枚举了。当然了，SOLID不是OO的全部。抽象能力，设计模式，架构模式，UML，以及阅读优秀框架源码（我们的Command设计就是参考了Activiti的Command）也都很重要。只是SOLID更基础，更重要，所以我在这里重点拿出来讲一下，希望能得到大家的重视。</p>
<h2 id="3、分层设计"><a href="#3、分层设计" class="headerlink" title="3、分层设计"></a>3、分层设计</h2><p>这一块的设计比较直观，整个应用层划分为三个大的层次，分别是App层，Domain层和Repostiory层。</p>
<p>1.App层主要负责获取输入，组装context，做输入校验，发送消息给领域层做业务处理，监听确认消息，如果需要的话使用MetaQ进行消息通知；</p>
<p>2.Domain层主要是通过领域服务（Domain Service），领域对象（Domain Object）的交互，对上层提供业务逻辑的处理，然后调用下层Repository做持久化处理；</p>
<p>3.Repository层主要负责数据的CRUD操作，这里我们借用了盒马的数据通道（Tunnel）的概念，通过Tunnel的抽象概念来屏蔽具体的数据来源，来源可以是MySQL，NoSql，Search，甚至是HSF等。<br><img src="http://imgblog.mrdear.cn/1520912963.png?imageMogr2/thumbnail/!100p" alt="img"><br>这里需要注意的是从其他系统获取的数据是有界上下文（Bounded Context）下的数据，为了弥合Bounded Context下的语义Gap，通常有两种方式，一个是用大领域（Big Domain）把两边的差异都合起来，另一个是增加防腐层（Anticorruption Layer）做转换。什么是Bounded Context？ 简单阐述一下，就是我们的领域概念是有作用范围的（Context）的，例如摇头这个动作，在中国的Context下表示NO，但是在印度的Context下却是YES。</p>
<h2 id="4、规范设计"><a href="#4、规范设计" class="headerlink" title="4、规范设计"></a>4、规范设计</h2><p>我们规范设计主要是要满足收纳原则的两个约束：放对位置东西不要乱放，我们的每一个组件（Module），每一个包（Package）都有明确的职责定义和范围，不可以放错，例如extension包就只是用来放扩展实现的，不允许放其他东西，而Interceptor包就只是放拦截器的，validator包就只是放校验器的。我们的主要构件如下图所示：<br><img src="http://imgblog.mrdear.cn/1520912993.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<h3 id="贴好标签"><a href="#贴好标签" class="headerlink" title="贴好标签"></a>贴好标签</h3><p>东西放在合适位置后还要贴上合适的标签，也就是要按照规范合理命名，例如我们架构里面和数据有关的Object，主要有Client Object，Domain Object和Data Object，Client Object是放在二方库中和外部交互使用的DTO，其命名必须以CO结尾，相应的Data Object主要是持久层使用的，命名必须以DO结尾。这个类名应该是自明的（self-evident)，也就是看到类名就知道里面是干了什么事，这也就反向要求我们的类也必须是单一职责的（Single Responsibility）的，如果你做的事情不单纯，自然也就很难自明了。如果我们Class Name是自明的，Package Name是自明的，Module Name也是自明的，那么我们整个应用系统就会很容易被理解，看起来就会很舒服，维护效率会提高很多。我们的命名规则如下图所示：<br><img src="http://imgblog.mrdear.cn/1520913021.png?imageMogr2/thumbnail/!100p" alt="GTS应用架构"><br>经过上面的长篇大论，我希望我把我们的架构理念阐述清楚了，最后再从整体上看下我们的架构吧。如果觉得不错，也可以把framework code拉下来自己玩一下。</p>
<h3 id="整体架构"><a href="#整体架构" class="headerlink" title="整体架构"></a>整体架构</h3><p>我们的架构原则很简单，即在高内聚，低耦合，可扩展，易理解大的指导思想下，尽可能的贯彻OO的设计思想和原则。我们最终形成的架构是集成了扩展点+元数据+CQRS+DDD的思想，关于元数据前面没怎么提到，这里稍微说一下，对于字段扩展，简单一点的解决方案就是预留扩展字段，复杂一点的就是使用元数据引擎。</p>
<p>使用元数据的好处是不仅能支持字段扩展，还提供了丰富的字段描述，等于是为以后的SaaS化配置提供了可能性，所以我们选择了使用元数据引擎。和DDD一样，元数据也是可选的，如果对没有字段扩展的需求，就不要用。最后的整体架构图如下：<br><img src="http://imgblog.mrdear.cn/1520913063.png?imageMogr2/thumbnail/!100p" alt="-END-"></p>
]]></content>
      <categories>
        <category>Spark</category>
      </categories>
      <tags>
        <tag>Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 - 组合模式</title>
    <url>/2019/06/04/DesignPattern/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E7%BB%84%E5%90%88%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="设计模式–组合模式的思考"><a href="#设计模式–组合模式的思考" class="headerlink" title="设计模式–组合模式的思考"></a>设计模式–组合模式的思考</h1><blockquote>
<p>本文转载自<a href="https://mrdear.cn/" target="_blank" rel="noopener">屈定’s Blog</a></p>
</blockquote>
<hr>
<p>组合模式是一种抽象树形结构的模式,其在业务开发中也是一种很有用的设计模式,下面开始分析.</p>
<h2 id="组合模式"><a href="#组合模式" class="headerlink" title="组合模式"></a>组合模式</h2><p>业务中有很多树形结构的表示,比如下面的目录结构</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-- 男装</span><br><span class="line">    -- 上衣</span><br><span class="line">        -- 品牌1</span><br><span class="line">        -- 品牌2</span><br><span class="line">    -- 裤子</span><br><span class="line">        -- 品牌1</span><br><span class="line">        -- 品牌3</span><br></pre></td></tr></table></figure>

<p>针对<code>男装</code>可以认为其是树的<strong>根节点</strong>,<code>上衣</code>,<code>裤子</code>这种下面还可以有节点的称为<strong>树枝节点</strong>,<code>品牌</code>这种下面不再有分支的称为<strong>叶子节点</strong><br>那么转换成面向对象该怎么表示呢?</p>
<h3 id="一般做法"><a href="#一般做法" class="headerlink" title="一般做法"></a>一般做法</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 根节点</span><br><span class="line">public class RootNode &#123;</span><br><span class="line">  private List&lt;CompositeNode&gt; compositeNodes;// 针对节点区别对待,导致处理麻烦</span><br><span class="line">  private List&lt;LeafNode&gt; leafNodes; // 针对节点区别对待,导致处理麻烦</span><br><span class="line">&#125;</span><br><span class="line">// 树枝节点</span><br><span class="line">public class CompositeNode &#123;</span><br><span class="line">  private List&lt;LeafNode&gt; leafNodes; </span><br><span class="line">  private List&lt;CompositeNode&gt; compositeNodes; </span><br><span class="line">&#125;</span><br><span class="line">// 叶子节点</span><br><span class="line">public class LeafNode &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这种做法是面向对象的思想,但是其最大的问题是对这三种类型的节点区别对待了,那么客户端就必须明确的得知这个节点到底是根还是树枝或者是叶子,那么对于客户端来说无疑是比较辛苦的,另外从功能上来说节点之间区别并不是很大,可以说是完全一样的.那么组合模式的作用就是统一这三种类型的节点,让客户端当成一种节点来处理.下面是组合模式下的方式</p>
<h3 id="组合设计"><a href="#组合设计" class="headerlink" title="组合设计"></a>组合设计</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 其为节点的约束,主要暴露给客户端,客户端不需要了解子类是什么.</span><br><span class="line">public abstract class Node &#123;</span><br><span class="line">&#125;</span><br><span class="line">// 树枝节点,当然也可以是根节点</span><br><span class="line">public class CompositeNode extends Node &#123;</span><br><span class="line">    // 持有Node集合,可以无限往下延伸</span><br><span class="line">  private List&lt;Node&gt; nodes;</span><br><span class="line">&#125;</span><br><span class="line">// 叶子节点,其下面不再有其他节点</span><br><span class="line">public class LeafNode extends Node &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>那么相比之前的设计好在了哪里?组合体现在了哪里?</p>
<ol>
<li>相比之前设计,这里用了一个抽象类暴露出去给客户端,只需要把客户端需要的方法定义在抽象类中,那么大大减少了客户端的理解成本,对于客户端来说节点都是一个性质的,没必要区分根,树枝,叶子等.</li>
<li>组合体现在<code>CompositeNode</code>节点的设计,其内部引用的是<code>Node</code>抽象类实例,也就是可以一直往下延伸.</li>
<li>组合模式更多的是一种面向接口编程的思想,大多数日常开发中总会有意无意的使用了这种模式思想.</li>
</ol>
<h2 id="Mybatis中的组合模式应用"><a href="#Mybatis中的组合模式应用" class="headerlink" title="Mybatis中的组合模式应用"></a>Mybatis中的组合模式应用</h2><p>开发中我们写的动态Sql,Mybatis会按照下面方式去理解这个结构,比如</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;select id=&quot;findById&quot; resultMap=&quot;RM-CLASSROOM&quot;&gt;</span><br><span class="line">	SELECT &lt;include refid=&quot;RM-CLASSROOM-ALLCOLS&quot;/&gt;</span><br><span class="line">	FROM classroom WHERE status = 0</span><br><span class="line">	&lt;if test=&quot;!ids.isEmpty()&quot;&gt;</span><br><span class="line">		AND id in </span><br><span class="line">		&lt;foreach collection=&quot;list&quot; item=&quot;item&quot; open=&quot;(&quot; close=&quot;)&quot; separator=&quot;,&quot;&gt;</span><br><span class="line">			#&#123;item&#125;</span><br><span class="line">		&lt;/foreach&gt;</span><br><span class="line">	&lt;/if&gt;</span><br><span class="line">&lt;/select&gt;</span><br></pre></td></tr></table></figure>

<p>Mybatis解析后大概会是下面的这种树形结构,最后在拼接成需要的Sql.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-- select  根节点</span><br><span class="line">    -- select  叶子节点</span><br><span class="line">    -- &lt;include refid=&quot;RM-CLASSROOM-ALLCOLS&quot;/&gt; 叶子节点</span><br><span class="line">    -- where status = 1 叶子节点</span><br><span class="line">    -- &lt;if test=&quot;!ids.isEmpty()&quot; 树枝节点</span><br><span class="line">        -- AND id IN  叶子节点</span><br><span class="line">        -- &lt;foreach item=&quot;list&quot; ..... 树枝节点</span><br><span class="line">            -- #&#123;item&#125; 叶子节点</span><br></pre></td></tr></table></figure>

<p>那么这种情况下是很适合用组合模式,因此Mybatis抽象出<code>SqlNode</code>接口暴露给客户端</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public interface SqlNode &#123;</span><br><span class="line">  boolean apply(DynamicContext context);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其有如下子类(子类太多,省略了一些),按照这些子类再翻译下上面的sql<br><img src="http://imgblog.mrdear.cn/1522562685.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">-- select  MixedSqlNode</span><br><span class="line">    -- select  StaticTextSqlNode</span><br><span class="line">    -- &lt;include refid=&quot;RM-CLASSROOM-ALLCOLS&quot;/&gt; StaticTextSqlNode</span><br><span class="line">    -- where status = 1 StaticTextSqlNode</span><br><span class="line">    -- &lt;if test=&quot;!ids.isEmpty()&quot; IfSqlNode 内部的contents为MixedSqlNode</span><br><span class="line">        -- AND id IN  StaticTextSqlNode</span><br><span class="line">        -- &lt;foreach item=&quot;list&quot; ..... ForEachSqlNode 内部的contents为MixedSqlNode</span><br><span class="line">            -- #&#123;item&#125; StaticTextSqlNode</span><br></pre></td></tr></table></figure>

<p><img src="http://imgblog.mrdear.cn/1522563076.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<p>从结构上来说,非叶子节点,例如<code>IfSqlNode</code>,<code>ForEachSqlNode</code>是可以一直嵌套的,所实现的关键就是<code>SqlNode</code>接口与<code>MixedSqlNode</code>实现类.<br>从客户端角度来说里面的节点这些都是不关心的,其只需要拿到<code>SqlNode rootSqlNode</code>实例,然后调用下<code>rootSqlNode.apply(context)</code>即可获取到自己想要的sql原型.<br>这两个也是组合模式要解决的问题.</p>
<h2 id="SpringMVC中的组合模式"><a href="#SpringMVC中的组合模式" class="headerlink" title="SpringMVC中的组合模式"></a>SpringMVC中的组合模式</h2><p>SpringMVC中对参数的解析使用的是<code>HandlerMethodArgumentResolver</code>接口,该类有一个实现类为<code>HandlerMethodArgumentResolverComposite</code>,该类为一个组合类,其结构如下:<br><img src="http://imgblog.mrdear.cn/1524061073.png?imageMogr2/thumbnail/!100p" alt="img"><br>其本身实现了<code>HandlerMethodArgumentResolver</code>接口,又持有其他<code>HandlerMethodArgumentResolver</code>对象,那么这种设计就是组合模式设计.,在它的实现方法中是对其他组合模式中的节点进行循环处理,从而选择最适合的一个.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">private HandlerMethodArgumentResolver getArgumentResolver(MethodParameter parameter) &#123;</span><br><span class="line">  HandlerMethodArgumentResolver result = this.argumentResolverCache.get(parameter);</span><br><span class="line">  if (result == null) &#123;</span><br><span class="line">    // 对其所拥有的对象循环,找到最适合处理的一个</span><br><span class="line">    for (HandlerMethodArgumentResolver methodArgumentResolver : this.argumentResolvers) &#123;</span><br><span class="line">      if (methodArgumentResolver.supportsParameter(parameter)) &#123;</span><br><span class="line">        result = methodArgumentResolver;</span><br><span class="line">        break;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  return result;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于<code>HandlerMethodArgumentResolver</code>来说,其虽然拥有众多子类,但是对于调用方来说却只关心参数所解析的结果,它并不知道该使用哪一个具体的子类,它所希望的是能以整体的形式去访问这些子类,从而选择最适合自己的一个参数解析器.那么<code>HandlerMethodArgumentResolverComposite</code>在这里扮演的就是一个整体的角色,对客户端来说调用的是这个整体.</p>
<h2 id="Netty中的组合模式"><a href="#Netty中的组合模式" class="headerlink" title="Netty中的组合模式"></a>Netty中的组合模式</h2><p>Netty中的<code>CompositeByteBuf</code>使用了组合设计模式，但是其有点特殊，Netty所描述的零拷贝是应用层面上不做任意的数据复制，而是使用组合的方式拷贝，比如有两个Buf，<code>headByteBuf</code>与<code>tailByteBuf</code>，那么现在的需求是把两个合在一起，很自然的想到先创建一个新的buf，然后把<code>headByteBuf</code>复制进去，再把<code>tailByteBuf</code>复制进去，这个过程中涉及到两次应用层面的拷贝，自然不是高效的做法，那么<code>CompositeByteBuf</code>的实现是什么样子的呢？</p>
<p><code>CompositeByteBuf</code>的意思是组合，他所采取的方式是把<code>headByteBuf</code>与<code>tailByteBuf</code>组合起来，对外相当于一个新的Buf，这样的方式不会产生任何应用层面的数据拷贝，原理如下示意图所示：<br><img src="http://imgblog.mrdear.cn/1531922378.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<p>那么这也是一种组合设计模式的思想，更可以说是一种妙用。</p>
<h2 id="安全性与透明性"><a href="#安全性与透明性" class="headerlink" title="安全性与透明性"></a>安全性与透明性</h2><p><strong>透明性</strong><br>所谓的透明性是客户在使用组合模式对象时不需要关心这个节点到底是根还是树枝或者是叶子,对于自己来说都是组件对象,只需要获取一个起始点就能拿到自己想要的东西,所谓的透明性表现在接口中暴露出了所有节点的公共方法,比如添加子节点,移除子节点等,那么就必然会存在叶子节点的添加子节点功能不支持的情况,此时调用应该抛出<code>UnsupportedOperationException</code>.<br>举个反例Mybatis中客户只需要拿到<code>SqlNode rootSqlNode</code>就可以获取到想要的sql,对于客户端唯一的入口就是这个<code>rootSqlNode.apply(context)</code>获取到对应的sql,客户端本身无法修改这个节点.那么这种行为是非透明的.</p>
<p><strong>安全性</strong><br>非透明性实现一般就是安全性的实现,所谓的安全性保证就是一旦节点构建完毕,客户端就无法更改,只需要获取到自己想要的东西就好.<code>SqlNode</code>就是一种安全性的实现,所谓的安全性表现在<code>SqlNode</code>接口中没有暴露修改的方法,节点是在构造阶段就组装完毕的.</p>
<p>具体选择哪种,需要根据业务来定夺,如果是类似Mybatis这种先准备好所有数据再执行的模式,那么安全性实现则是最好的选择.如果是业务处理模式下边处理边构造,则透明性最佳.</p>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><ol>
<li>组合模式在于结构上的统一,对外接口的一致,给客户端提供更加统一或者只提供必要的操作.</li>
<li>组合模式是面向接口编程的思想体现,通过接口实现客户端的操作便捷与约束,同时实现更加灵活的自由组合.</li>
</ol>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式,组合模式</tag>
      </tags>
  </entry>
  <entry>
    <title>设计模式 - 观察者模式</title>
    <url>/2019/06/04/DesignPattern/%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F-%E8%A7%82%E5%AF%9F%E8%80%85%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="设计模式-观察者模式"><a href="#设计模式-观察者模式" class="headerlink" title="设计模式 - 观察者模式"></a>设计模式 - 观察者模式</h1><hr>
<blockquote>
<p>本文转载自<a href="https://mrdear.cn/" target="_blank" rel="noopener">屈定’s Blog</a></p>
</blockquote>
<h2 id="观察者模式"><a href="#观察者模式" class="headerlink" title="观察者模式"></a>观察者模式</h2><p>观察者模式描述的是一种一对多的关系,这里的一可能是某个状态发生变化,也可能是某一个事件产生.举个例子,针对订单付款,这一事件产生后可能需要经过很多个处理步骤,比如积分,入库,消费排行榜之类的操作,这些操作之间并没有任何关联性甚至可以并行处理,那么就可以理解为订单付款与处理步骤之间的一对多关系.<br>还有一个特点就是单向依赖,处理步骤对于订单付款是单向依赖的,比如有订单付款,才能有处理步骤.但是反之就不依赖,订单付款对于有没有处理步骤是不关心的,这一点在下文实战中会详细讲解.</p>
<h3 id="观察者模式结构"><a href="#观察者模式结构" class="headerlink" title="观察者模式结构"></a>观察者模式结构</h3><p>观察者模式主要结构如下:<br><img src="http://imgblog.mrdear.cn/1524063218.png?imageMogr2/thumbnail/!100p" alt="img"></p>
<ul>
<li>Subject: 负责事件产生后通知到具体观察者的角色,所谓的通知实际上是循环调用其所持有的观察者接口</li>
<li>Observer: 负责对事件的处理,该接口可以很好的做到任务分离,每一个不同的任务都是其一个实现子类,互相不关心对方,很好的描述了业务上的关系.</li>
</ul>
<p>那么本质什么?用一个<code>Subject</code>来聚合所有的<code>Observer</code>,那么调用者只需要关心对应的<code>Subject</code>就可以.<br>为什么可以这样? 因为<code>Observer</code>之间没有任何关系,只是单纯的做自己要做的事情,也并不需要返回值之类的东西.</p>
<h2 id="观察者模式的实战案例"><a href="#观察者模式的实战案例" class="headerlink" title="观察者模式的实战案例"></a>观察者模式的实战案例</h2><p>如笔者一开始描述的需求,再订单付款完成之后执行一些处理步骤,具体如下:</p>
<ol>
<li>如果是虚拟产品订单,那么就发放虚拟产品</li>
<li>如果是会员订单那么就开通会员</li>
<li>根据付款金额增加积分</li>
<li>如果有消费排行榜活动则更新用户金额.</li>
<li>…..</li>
</ol>
<p>这种是开发中很常见的付款后一些对应的操作需求,并且随着活动之类的增加后续还很容易增加其他的处理步骤需求,对于观察者模式其符合以下两点特征:</p>
<ol>
<li>订单付款完成这一事件对应对个处理步骤,典型一对多关系</li>
<li>处理步骤之间并无关联性,每一个都是独立的处理</li>
</ol>
<h3 id="观察者模式设计"><a href="#观察者模式设计" class="headerlink" title="观察者模式设计"></a>观察者模式设计</h3><p>上述用观察模式可以设计出如下结构:<br><img src="http://imgblog.mrdear.cn/1524065151.png?imageMogr2/thumbnail/!120p" alt="img"></p>
<p><strong>OrderPaidHandlerObserver</strong><br>其是观察者需要实现的接口,主要功能是判断是不是自己可以处理,可以处理的话就处理,其子类各司其职,比如<code>IntegralOrderService</code>是处理积分相关的观察者,<code>VipOrderService</code>则是处理会员相关的Service.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public interface OrderPaidHandlerObserver &#123;</span><br><span class="line">  /**</span><br><span class="line">   * 是否支持处理该消息</span><br><span class="line">   * @param paidMsg 消息体</span><br><span class="line">   * @return true 支持</span><br><span class="line">   */</span><br><span class="line">  boolean supportHandler(OrderPaidMsgDTO paidMsg);</span><br><span class="line">  /**</span><br><span class="line">   * 处理方法</span><br><span class="line">   * @param paidMsg 消息体</span><br><span class="line">   */</span><br><span class="line">  void handler(OrderPaidMsgDTO paidMsg);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>OrderPaidHandlerSubject</strong><br>其是负责通知所有观察者的接口,实现了该接口就有了通知观察者的义务.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public interface OrderPaidHandlerSubject &#123;</span><br><span class="line">  /**</span><br><span class="line">   * 提醒所有的观察者</span><br><span class="line">   * @param paidMsg 付款消息</span><br><span class="line">   */</span><br><span class="line">  void notifyObservers(OrderPaidMsgDTO paidMsg);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>OrderCompositeService</strong><br>其是<code>OrderPaidHandlerSubject</code>的实现类,主要实现负责通知所有观察者的逻辑,所谓的通知本质上就是调用自己所持有的观察者对象,那么当订单付款事件产生后<code>OrderCompositeService</code>只需要调用下<code>notifyObservers()</code>方法就可以完成通知,完成所有的处理步骤.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class OrderCompositeService implements OrderPaidHandlerSubject &#123;</span><br><span class="line"></span><br><span class="line">  private List&lt;OrderPaidHandlerObserver&gt; observers;</span><br><span class="line"></span><br><span class="line">  @Override</span><br><span class="line">  public void notifyObservers(OrderPaidMsgDTO paidMsg) &#123;</span><br><span class="line">    // 所谓的通知本质上就是调用对应观察者的方法</span><br><span class="line">    for (OrderPaidHandlerObserver observer : observers) &#123;</span><br><span class="line">      try &#123;</span><br><span class="line">        if (observer.supportHandler(paidMsg)) &#123;</span><br><span class="line">          observer.handler(paidMsg);</span><br><span class="line">        &#125;</span><br><span class="line">      &#125; catch (Exception e) &#123;</span><br><span class="line">        log.error(&quot;OrderPaidHandlerObserver fail&quot;, e);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="使用Spring管理观察者"><a href="#使用Spring管理观察者" class="headerlink" title="使用Spring管理观察者"></a>使用Spring管理观察者</h3><p><code>OrderCompositeService</code>作为Subject来说,其持有了全部的<code>Observer</code>,那么如果利用IOC把<code>Observer</code>全部注入进该类中,那么当下次新增加一个<code>Observer</code>实现类时就不需要改这边的任何代码,完完全全的解耦.<br>思路是利用IOC管理所有的观察者,当Spring容器启动完毕后获取所有的观察者,添加到对应的<code>observers</code>集合中,具体做法就是让<code>OrderCompositeService</code>实现<code>ApplicationListener&lt;ContextRefreshedEvent&gt;</code>接口,Spring在启动完毕后会发出通知,在该接口中利用<code>BeanFactoryUtils</code>初始化所需要的观察者集合.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line">   * 容器初始化完毕后所执行的事件</span><br><span class="line">   */</span><br><span class="line">  @Override</span><br><span class="line">  public void onApplicationEvent(ContextRefreshedEvent event) &#123;</span><br><span class="line">    initPaidObserver(event);</span><br><span class="line">  &#125;</span><br><span class="line">  /**</span><br><span class="line">   * 初始化订单付款所要执行的事件</span><br><span class="line">   */</span><br><span class="line">  private void initPaidObserver(ContextRefreshedEvent event) &#123;</span><br><span class="line">    // 从Spring容器中取出所有的观察者</span><br><span class="line">    Map&lt;String, OrderPaidHandlerObserver&gt; matchingBeans =</span><br><span class="line">        BeanFactoryUtils.beansOfTypeIncludingAncestors(event.getApplicationContext(), OrderPaidHandlerObserver.class, true, false);</span><br><span class="line">    // 实例化观察者集合</span><br><span class="line">    this.observers = Collections.unmodifiableList(Lists.newArrayList(matchingBeans.values()));</span><br><span class="line">    if (this.observers.size() &lt;= 0) &#123;</span><br><span class="line">      throw new IllegalStateException(&quot;OrderPaidHandlerObserver not found&quot;);</span><br><span class="line">    &#125; else &#123;</span><br><span class="line">      log.info(&quot;initPaidObserver finish, observer is &#123;&#125;&quot;, matchingBeans.keySet());</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>这样做的好处就是把观察者的实例化与Subject解耦,对于观察者只需要知道自己一旦实现了观察者接口,那么就一定会有相应的Subject通知自己就足够了.</p>
<h2 id="观察者的-“感兴趣”-粒度"><a href="#观察者的-“感兴趣”-粒度" class="headerlink" title="观察者的 “感兴趣” 粒度"></a>观察者的 “感兴趣” 粒度</h2><p>在观察者模式中<code>Observer</code>会像<code>Subject</code>注册自己,那么当Subject对应多个事件时怎么处理呢?<br><strong>1.Subject管理多组Observer</strong><br>在<code>Subject</code>中存放着多组<code>Observer</code>,当一个事件触发时只会通知其中一组.这样做法个人感觉是比较合理的.缺点是管理不方便,对于<code>Subject</code>来说要管理多组,对应的removeOvserver或者addObserver就会比较麻烦了,此时可以依赖IOC等工具完成这个过程.<br><strong>2.Observer多角色</strong><br>这种方案下,对于一个<code>Subject</code>他管理的只有一组<code>Observer</code>,但是<code>Observer</code>本身要承担多个责任,并且对自己不感兴趣的责任要留空方法处理.<code>Observer</code>可能只对一件事情感兴趣却不得不实现一堆空方法,不符合最少知道原则.Java的<code>AWT</code>就是这种设计.<br><strong>3.使用弱类型参数</strong><br>JDK自带的<code>Observer</code>就是类似的形式,其使用<code>Object</code>作为观察者参数,当接收到消息时需要用<code>instance</code>判断是否是自己感兴趣的事件,然后才执行逻辑,当事件很少的话这种方式是比较合适的,事件多的话则对一堆事件要分开处理,依然很麻烦.Eclipse的SWT是这种设计.</p>
]]></content>
      <categories>
        <category>设计模式</category>
      </categories>
      <tags>
        <tag>设计模式,观察者</tag>
      </tags>
  </entry>
  <entry>
    <title>Filter 链</title>
    <url>/2019/06/04/Java%20SE/Fliter%E9%93%BE/</url>
    <content><![CDATA[<p> 在写JavaWeb的时候我们经常会遇到一个概念就是 Filter 链，比如对于路由拦截，以及文件目录拦截都有一个Filter 和Filter链，那么他们底层具体到底是怎么工作的呢？</p>
<p>听到Filter 链这个名词我们觉得一个 Filter 链应该可以进行链式调用，来判断我们的条件是否成立，也就是类似于下面的规则:</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">filterChain.doFilter(pattern).doFilter(pattern)</span><br></pre></td></tr></table></figure>

<p>这样想是没错，那每次就需要返回下一个 Filter 这么想是没错，可是我们怎么知道下一次 Filter 是谁呢？</p>
<p>链表！ 对滴，这时候链表就起作用了，我们直接采用了 LinkedList 就能把这些 Filter 串起来。然后根据当前的 Filter 找出下一个 Filter 。</p>
<p>实际上在某些框架之中不是这么实现了的，但是基本的思路已经非常对了。例如这里是 SpringSecurity 中的 Filter 的简单实现：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.LinkedList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test1</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">interface</span> <span class="title">Filter</span></span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doFilter</span><span class="params">(FilterChain filterChain)</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">MyFilter</span> <span class="keyword">implements</span> <span class="title">Filter</span> </span>&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doFilter</span><span class="params">(FilterChain filterChain)</span> </span>&#123;</span><br><span class="line">            System.out.println(<span class="string">"filter invoke"</span>);</span><br><span class="line">            filterChain.doFilter();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">interface</span> <span class="title">FilterChain</span></span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doFilter</span><span class="params">()</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">MyFilterChain</span> <span class="keyword">implements</span> <span class="title">FilterChain</span></span>&#123;</span><br><span class="line">        List&lt;Filter&gt; filters;</span><br><span class="line">        <span class="keyword">int</span> pos = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        MyFilterChain(List&lt;Filter&gt; filters) &#123;</span><br><span class="line">            <span class="keyword">this</span>.filters = filters;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">doFilter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (pos == filters.size()) &#123;</span><br><span class="line">                System.out.println(<span class="string">"end"</span>);</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            Filter filter = filters.get(pos++);</span><br><span class="line">            filter.doFilter(<span class="keyword">this</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        LinkedList&lt;Filter&gt; list = <span class="keyword">new</span> LinkedList&lt;&gt;();</span><br><span class="line">        list.add(<span class="keyword">new</span> MyFilter());</span><br><span class="line">        list.add(<span class="keyword">new</span> MyFilter());</span><br><span class="line">        list.add(<span class="keyword">new</span> MyFilter());</span><br><span class="line">        <span class="keyword">new</span> MyFilterChain(list).doFilter();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>Filter</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 新特性之 Stream</title>
    <url>/2019/05/11/Java%20SE/Java%20%E6%96%B0%E7%89%B9%E6%80%A7%20-%20Stream/</url>
    <content><![CDATA[<h2 id="Stream"><a href="#Stream" class="headerlink" title="Stream"></a>Stream</h2><p>使用这个方法创建一个 Stream 对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> ArrayList&lt;&gt;().stream()</span><br></pre></td></tr></table></figure>

<h2 id="Filter"><a href="#Filter" class="headerlink" title="Filter"></a>Filter</h2><p>过滤器，里面传递一个函数，这个函数的返回结果如果为 true 则保留这个元素，否则的话丢弃这个元素。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">stringCollection</span><br><span class="line">        .stream()</span><br><span class="line">        .filter((s) -&gt; s.startsWith(<span class="string">"a"</span>))</span><br><span class="line">        .forEach(System.out::println);</span><br></pre></td></tr></table></figure>

<h2 id="Foreach"><a href="#Foreach" class="headerlink" title="Foreach"></a>Foreach</h2><p>遍历，消费。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">stringCollection</span><br><span class="line">        .stream()</span><br><span class="line">        .filter((s) -&gt; s.startsWith(<span class="string">"a"</span>))</span><br><span class="line">        .forEach(System.out::println);</span><br></pre></td></tr></table></figure>

<h2 id="Map"><a href="#Map" class="headerlink" title="Map"></a>Map</h2><p>这个功能也是遍历，但是他是有返回值的，而上面的 Foreach 是没有返回值的，仅仅是单纯的消费。而且 Foreach 不能够链式调用，因为没有返回值，但是 Map 没问题。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">stringCollection</span><br><span class="line">        .stream()</span><br><span class="line">        .map(String::toUpperCase)</span><br><span class="line">        .sorted(Comparator.reverseOrder())</span><br><span class="line">        .forEach(System.out::println);</span><br></pre></td></tr></table></figure>

<h2 id="Sorted"><a href="#Sorted" class="headerlink" title="Sorted"></a>Sorted</h2><p>这个方法是用来排序的，里面传递的函数就是一个比较器，也可以不传递参数，使用默认的就好。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">stringCollection</span><br><span class="line">        .stream()</span><br><span class="line">        .sorted(( x, y)-&gt; y.length()-x.length())</span><br><span class="line">        .filter((s) -&gt; s.startsWith(<span class="string">"a"</span>))</span><br><span class="line">        .forEach(System.out::println);</span><br></pre></td></tr></table></figure>

<h2 id="Match"><a href="#Match" class="headerlink" title="Match"></a>Match</h2><p>根据在给定的 stream 对象中是否含有指定内容返回 true 或者 false 。</p>
<p>具体的有：</p>
<ul>
<li>allMatch</li>
<li>anyMatch</li>
<li>noneMatch</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">boolean anyStartsWithA = stringCollection</span><br><span class="line">        .stream()</span><br><span class="line">        .anyMatch((s) -&gt; s.startsWith(&quot;a&quot;));</span><br><span class="line"></span><br><span class="line">boolean allStartsWithA = stringCollection</span><br><span class="line">        .stream()</span><br><span class="line">        .allMatch((s) -&gt; s.startsWith(&quot;a&quot;));</span><br><span class="line"></span><br><span class="line">boolean noneStartsWithZ = stringCollection</span><br><span class="line">        .stream()</span><br><span class="line">        .noneMatch((s) -&gt; s.startsWith(&quot;z&quot;));</span><br></pre></td></tr></table></figure>

<h2 id="count"><a href="#count" class="headerlink" title="count"></a>count</h2><p>计算集合中的元素的个数。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">long</span> startsWithB = stringCollection</span><br><span class="line">        .stream()</span><br><span class="line">        .filter((s) -&gt; s.startsWith(<span class="string">"b"</span>))</span><br><span class="line">        .count();</span><br></pre></td></tr></table></figure>

<h2 id="reduce"><a href="#reduce" class="headerlink" title="reduce"></a>reduce</h2><p>这个函数就是类似于斐波那契数列，每次传递的参数是上一次的结果和从集合中取出的新元素。第一次默认取出了第一个元素和第二个元素。</p>
<p>简单的例子就是，第一次取出 0,1 第二次取出 第一次reduce的结果作为第一个参数，取出 2 作为第二个参数，以此类推。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Optional&lt;String&gt; reduced =</span><br><span class="line">        stringCollection</span><br><span class="line">                .stream()</span><br><span class="line">                .sorted()</span><br><span class="line">                .reduce((s1, s2) -&gt; s1 + <span class="string">"#"</span> + s2);</span><br></pre></td></tr></table></figure>

<h2 id="parallelStream"><a href="#parallelStream" class="headerlink" title="parallelStream"></a>parallelStream</h2><p>并行的 steam 流，可以进行并行处理，这样会效率更高。在使用stream.foreach时这个遍历没有线程安全问题，但是使用parallelStream就会有线程安全问题，所有在parallelStream里面使用的外部变量，比如集合一定要使用线程安全集合，不然就会引发多线程安全问题。如果说需要保证安全性需要使用 reduce 和 collect，不过这个用起来超级麻烦！！！</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">long</span> count = values.parallelStream().sorted().count();</span><br></pre></td></tr></table></figure>

<h2 id="IntStream-range-a-b"><a href="#IntStream-range-a-b" class="headerlink" title="IntStream.range(a,b)"></a>IntStream.range(a,b)</h2><p>可以直接生成 从 a 到 b 的整数这里还是遵循编程语言的大多数约定，那就是含头不含尾。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">IntStream.range(<span class="number">0</span>, <span class="number">10</span>)</span><br><span class="line">    .forEach(System.out::println);</span><br></pre></td></tr></table></figure>

<p>输出的结果是</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">0</span><br><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td></tr></table></figure>

<h2 id="new-Random-ints"><a href="#new-Random-ints" class="headerlink" title="new Random().ints()"></a>new Random().ints()</h2><p>获取一系列的随机值，这个接口出来的数据是连续不断的，所以需要用limit来限制一下。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> Random().ints().limit(<span class="number">10</span>).forEach(System.out::println);</span><br></pre></td></tr></table></figure>

<h2 id="Supplier"><a href="#Supplier" class="headerlink" title="Supplier"></a>Supplier</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Supplier&lt;String&gt; stringSupplier=String::<span class="keyword">new</span>;</span><br><span class="line">stringSupplier.get();</span><br></pre></td></tr></table></figure>

<p>该接口就一个抽象方法get方法,不用传入任何参数,直接返回一个泛型T的实例.就如同无参构造一样</p>
<h2 id="Consumer"><a href="#Consumer" class="headerlink" title="Consumer"></a>Consumer</h2><h3 id="1-accept方法"><a href="#1-accept方法" class="headerlink" title="1.    accept方法"></a>1.    accept方法</h3><p>​        该函数式接口的唯一的抽象方法,接收一个参数,没有返回值.</p>
<h3 id="2-andThen方法"><a href="#2-andThen方法" class="headerlink" title="2.    andThen方法"></a>2.    andThen方法</h3><p>​        在执行完调用者方法后再执行传入参数的方法.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConsumerTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Consumer&lt;Integer&gt; consumer = (x) -&gt; &#123;</span><br><span class="line">            <span class="keyword">int</span> num = x * <span class="number">2</span>;</span><br><span class="line">            System.out.println(num);</span><br><span class="line">        &#125;;</span><br><span class="line">        Consumer&lt;Integer&gt; consumer1 = (x) -&gt; &#123;</span><br><span class="line">            <span class="keyword">int</span> num = x * <span class="number">3</span>;</span><br><span class="line">            System.out.println(num);</span><br><span class="line">        &#125;;</span><br><span class="line">        consumer.andThen(consumer1).accept(<span class="number">10</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>先执行了 consumer.accept(10) 然后执行了 consumer1.accept(10)</p>
<h2 id="ifPresent"><a href="#ifPresent" class="headerlink" title="ifPresent"></a>ifPresent</h2><p>针对一个optional 如果有值的话就执行否则不执行。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">IntStream</span><br><span class="line">    .builder()</span><br><span class="line">    .add(<span class="number">1</span>)</span><br><span class="line">    .add(<span class="number">3</span>)</span><br><span class="line">    .add(<span class="number">5</span>)</span><br><span class="line">    .add(<span class="number">7</span>)</span><br><span class="line">    .add(<span class="number">11</span>)</span><br><span class="line">    .build()</span><br><span class="line">    .average()</span><br><span class="line">    .ifPresent(System.out::println);</span><br></pre></td></tr></table></figure>

<p>average 执行结果就是一个 optional</p>
<h2 id="Collect"><a href="#Collect" class="headerlink" title="Collect"></a>Collect</h2><p>他有两种调用方式</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> &lt;R&gt; <span class="function">R <span class="title">collect</span><span class="params">(Supplier&lt;R&gt; supplier,</span></span></span><br><span class="line"><span class="function"><span class="params">                 BiConsumer&lt;R, ? <span class="keyword">super</span> T&gt; accumulator,</span></span></span><br><span class="line"><span class="function"><span class="params">                 BiConsumer&lt;R, R&gt; combiner)</span></span>;</span><br><span class="line"></span><br><span class="line">&lt;R, A&gt; <span class="function">R <span class="title">collect</span><span class="params">(Collector&lt;? <span class="keyword">super</span> T, A, R&gt; collector)</span></span>;</span><br></pre></td></tr></table></figure>

<p>下面主要介绍一下这两种方式的使用方法：</p>
<h3 id="1-函数"><a href="#1-函数" class="headerlink" title="1. 函数"></a>1. 函数</h3><p>第一种调用方式的接口如下</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;R&gt; <span class="function">R <span class="title">collect</span><span class="params">(Supplier&lt;R&gt; supplier,</span></span></span><br><span class="line"><span class="function"><span class="params">                BiConsumer&lt;R, ? <span class="keyword">super</span> T&gt; accumulator,</span></span></span><br><span class="line"><span class="function"><span class="params">                BiConsumer&lt;R, R&gt; combiner)</span></span>;</span><br></pre></td></tr></table></figure>

<ul>
<li>supplier 这个参数就是提供一个容器，可以看到最后 collect 操作的结果是一个 R 类型变量，而 supplier 接口最后需要返回的也是一个 R 类型的变量，所以说这里返回的是收集元素的容器。</li>
<li>accumulator 参数，看到这个函数的定义是传入一个 R 容器，后面则是 T 类型的元素，需要将这个 T 放到 R 容器中，即这一步是用来将元素添加到容器中的操作。</li>
<li>conbiner 这个参数是两个容器，即当出现多个容器的时候容器如何进行聚合。</li>
</ul>
<p>一个简单的例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">String concat = stringStream.collect(StringBuilder::<span class="keyword">new</span>, StringBuilder::append,StringBuilder::append).toString();</span><br><span class="line"><span class="comment">//等价于上面,这样看起来应该更加清晰</span></span><br><span class="line">String concat = stringStream.collect(() -&gt; <span class="keyword">new</span> StringBuilder(),(l, x) -&gt; l.append(x), (r1, r2) -&gt; r1.append(r2)).toString();</span><br></pre></td></tr></table></figure>

<h3 id="2-Collector-接口"><a href="#2-Collector-接口" class="headerlink" title="2. Collector 接口"></a>2. Collector 接口</h3><p>第二种方案是更高级的用法采用了 Collector 接口：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">&lt;R, A&gt; <span class="function">R <span class="title">collect</span><span class="params">(Collector&lt;? <span class="keyword">super</span> T, A, R&gt; collector)</span></span>;</span><br></pre></td></tr></table></figure>

<p>可以看到他返回的还是一个 R 类型的变量，也就是容器。</p>
<p><code>Collector</code>接口是使得<code>collect</code>操作强大的终极武器,对于绝大部分操作可以分解为旗下主要步骤,<strong>提供初始容器-&gt;加入元素到容器-&gt;并发下多容器聚合-&gt;对聚合后结果进行操作</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">CollectorImpl</span>&lt;<span class="title">T</span>, <span class="title">A</span>, <span class="title">R</span>&gt; <span class="keyword">implements</span> <span class="title">Collector</span>&lt;<span class="title">T</span>, <span class="title">A</span>, <span class="title">R</span>&gt; </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> Supplier&lt;A&gt; supplier;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> BiConsumer&lt;A, T&gt; accumulator;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> BinaryOperator&lt;A&gt; combiner;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> Function&lt;A, R&gt; finisher;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> Set&lt;Characteristics&gt; characteristics;</span><br><span class="line"></span><br><span class="line">        CollectorImpl(Supplier&lt;A&gt; supplier,</span><br><span class="line">                      BiConsumer&lt;A, T&gt; accumulator,</span><br><span class="line">                      BinaryOperator&lt;A&gt; combiner,</span><br><span class="line">                      Function&lt;A,R&gt; finisher,</span><br><span class="line">                      Set&lt;Characteristics&gt; characteristics) &#123;</span><br><span class="line">            <span class="keyword">this</span>.supplier = supplier;</span><br><span class="line">            <span class="keyword">this</span>.accumulator = accumulator;</span><br><span class="line">            <span class="keyword">this</span>.combiner = combiner;</span><br><span class="line">            <span class="keyword">this</span>.finisher = finisher;</span><br><span class="line">            <span class="keyword">this</span>.characteristics = characteristics;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        CollectorImpl(Supplier&lt;A&gt; supplier,</span><br><span class="line">                      BiConsumer&lt;A, T&gt; accumulator,</span><br><span class="line">                      BinaryOperator&lt;A&gt; combiner,</span><br><span class="line">                      Set&lt;Characteristics&gt; characteristics) &#123;</span><br><span class="line">            <span class="keyword">this</span>(supplier, accumulator, combiner, castingIdentity(), characteristics);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> BiConsumer&lt;A, T&gt; <span class="title">accumulator</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> accumulator;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> Supplier&lt;A&gt; <span class="title">supplier</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> supplier;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> BinaryOperator&lt;A&gt; <span class="title">combiner</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> combiner;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> Function&lt;A, R&gt; <span class="title">finisher</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> finisher;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> Set&lt;Characteristics&gt; <span class="title">characteristics</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> characteristics;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>可以看到我们可以直接 <code>new CollectorImpl</code> 然后将这些函数传入，另外还有一种简单的方式就是 使用 <code>Collector.of()</code>依然可以直接传入函数。和 <code>new CollectorImpl</code> 是等价的。</p>
<h3 id="3-工具函数"><a href="#3-工具函数" class="headerlink" title="3. 工具函数"></a>3. 工具函数</h3><h4 id="1-toList"><a href="#1-toList" class="headerlink" title="1. toList()"></a>1. toList()</h4><p>容器: <code>ArrayList::new</code><br>加入容器操作: <code>List::add</code><br>多容器合并: <code>left.addAll(right); return left;</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt;</span><br><span class="line"> Collector&lt;T, ?, List&lt;T&gt;&gt; toList() &#123;</span><br><span class="line">     <span class="keyword">return</span> <span class="keyword">new</span> CollectorImpl&lt;&gt;((Supplier&lt;List&lt;T&gt;&gt;) ArrayList::<span class="keyword">new</span>, List::add,</span><br><span class="line">                                (left, right) -&gt; &#123; left.addAll(right); <span class="keyword">return</span> left; &#125;,</span><br><span class="line">                                CH_ID);</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-joining"><a href="#2-joining" class="headerlink" title="2.joining()"></a>2.joining()</h4><p>容器: <code>StringBuilder::new</code><br>加入容器操作: <code>StringBuilder::append</code><br>多容器合并: <code>r1.append(r2); return r1;</code><br>聚合后的结果操作: <code>StringBuilder::toString</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Collector&lt;CharSequence, ?, String&gt; joining() &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> CollectorImpl&lt;CharSequence, StringBuilder, String&gt;(</span><br><span class="line">            StringBuilder::<span class="keyword">new</span>, StringBuilder::append,</span><br><span class="line">            (r1, r2) -&gt; &#123; r1.append(r2); <span class="keyword">return</span> r1; &#125;,</span><br><span class="line">            StringBuilder::toString, CH_NOID);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-groupingBy"><a href="#3-groupingBy" class="headerlink" title="3.groupingBy()"></a>3.groupingBy()</h4><p><code>roupingBy</code>是<code>toMap</code>的一种高级方式,弥补了<code>toMap</code>对值无法提供多元化的收集操作,比如对于返回<code>Map&lt;T,List&lt;E&gt;&gt;</code>这样的形式<code>toMap</code>就不是那么顺手,那么<code>groupingBy</code>的重点就是对Key和Value值的处理封装.分析如下代码,其中<code>classifier</code>是对key值的处理,<code>mapFactory</code>则是指定Map的容器具体类型,<code>downstream</code>为对Value的收集操作.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;T, K, D, A, M extends Map&lt;K, D&gt;&gt;</span><br><span class="line"> Collector&lt;T, ?, M&gt; groupingBy(Function&lt;? <span class="keyword">super</span> T, ? extends K&gt; classifier,</span><br><span class="line">                               Supplier&lt;M&gt; mapFactory,</span><br><span class="line">                               Collector&lt;? <span class="keyword">super</span> T, A, D&gt; downstream) &#123;</span><br><span class="line">    .......</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<p>一个简单的例子</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//原生形式</span></span><br><span class="line">   Lists.&lt;Person&gt;newArrayList().stream()</span><br><span class="line">        .collect(() -&gt; <span class="keyword">new</span> HashMap&lt;Integer,List&lt;Person&gt;&gt;(),</span><br><span class="line">            (h, x) -&gt; &#123;</span><br><span class="line">              List&lt;Person&gt; value = h.getOrDefault(x.getType(), Lists.newArrayList());</span><br><span class="line">              value.add(x);</span><br><span class="line">              h.put(x.getType(), value);</span><br><span class="line">            &#125;,</span><br><span class="line">            HashMap::putAll</span><br><span class="line">        );</span><br><span class="line"><span class="comment">//groupBy形式</span></span><br><span class="line">Lists.&lt;Person&gt;newArrayList().stream()</span><br><span class="line">        .collect(Collectors.groupingBy(Person::getType, HashMap::<span class="keyword">new</span>, Collectors.toList()));</span><br><span class="line"><span class="comment">//因为对值有了操作,因此我可以更加灵活的对值进行转换</span></span><br><span class="line">Lists.&lt;Person&gt;newArrayList().stream()</span><br><span class="line">        .collect(Collectors.groupingBy(Person::getType, HashMap::<span class="keyword">new</span>, Collectors.mapping(Person::getName,Collectors.toSet())));</span><br><span class="line"><span class="comment">// 还有一种比较简单的使用方式 只需要传递一个参数按照key来划分</span></span><br><span class="line">Map&lt;Integer, List&lt;Person&gt;&gt; personsByAge = persons</span><br><span class="line">            .stream()</span><br><span class="line">    .collect(Collectors.groupingBy(p -&gt; p.age));</span><br></pre></td></tr></table></figure>

<h4 id="4-reducing"><a href="#4-reducing" class="headerlink" title="4.reducing()"></a>4.reducing()</h4><p><code>reducing</code>是针对单个值的收集,其返回结果不是集合家族的类型,而是单一的实体类T<br>容器: <code>boxSupplier(identity)</code>,这里包裹用的是一个长度为1的Object[]数组,至于原因自然是不可变类型的锅<br>加入容器操作: <code>a[0] = op.apply(a[0], t)</code><br>多容器合并: <code>a[0] = op.apply(a[0], b[0]); return a;</code><br>聚合后的结果操作: 结果自然是Object[0]所包裹的数据<code>a -&gt; a[0]</code><br>优化操作状态字段: <code>CH_NOID</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> &lt;T&gt; Collector&lt;T, ?, T&gt;</span><br><span class="line">  reducing(T identity, BinaryOperator&lt;T&gt; op) &#123;</span><br><span class="line">      <span class="keyword">return</span> <span class="keyword">new</span> CollectorImpl&lt;&gt;(</span><br><span class="line">              boxSupplier(identity),</span><br><span class="line">              (a, t) -&gt; &#123; a[<span class="number">0</span>] = op.apply(a[<span class="number">0</span>], t); &#125;,</span><br><span class="line">              (a, b) -&gt; &#123; a[<span class="number">0</span>] = op.apply(a[<span class="number">0</span>], b[<span class="number">0</span>]); <span class="keyword">return</span> a; &#125;,</span><br><span class="line">              a -&gt; a[<span class="number">0</span>],</span><br><span class="line">              CH_NOID);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>简单来说这个地方做的事情和 reduce 是一样的，第一个 id 传入的就是 reduce 的初始值，只是他把它包装成一个 长度为1的数组了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//原生操作</span></span><br><span class="line"><span class="keyword">final</span> Integer[] integers = Lists.newArrayList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>)</span><br><span class="line">        .stream()</span><br><span class="line">        .collect(() -&gt; <span class="keyword">new</span> Integer[]&#123;<span class="number">0</span>&#125;, (a, x) -&gt; a[<span class="number">0</span>] += x, (a1, a2) -&gt; a1[<span class="number">0</span>] += a2[<span class="number">0</span>]);</span><br><span class="line"><span class="comment">//reducing操作</span></span><br><span class="line"><span class="keyword">final</span> Integer collect = Lists.newArrayList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>)</span><br><span class="line">        .stream()</span><br><span class="line">        .collect(Collectors.reducing(<span class="number">0</span>, Integer::sum));    </span><br><span class="line"><span class="comment">//当然Stream也提供了reduce操作</span></span><br><span class="line"><span class="keyword">final</span> Integer collect = Lists.newArrayList(<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>)</span><br><span class="line">        .stream().reduce(<span class="number">0</span>, Integer::sum)</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Java</category>
      </categories>
      <tags>
        <tag>Java8</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 新特性之 Time</title>
    <url>/2019/05/10/Java%20SE/Java%20%E6%96%B0%E7%89%B9%E6%80%A7%20-%20Time/</url>
    <content><![CDATA[<h2 id="日期类-LocalDate"><a href="#日期类-LocalDate" class="headerlink" title="日期类 LocalDate"></a>日期类 LocalDate</h2><p>能够简单的获取当天的日期，并且可以方便的对日期进行加减。</p>
<p>他是通过静态方法或者 from/of 等方法创建对象的。</p>
<p>这个类不存储时区，所以他没有时区的概念，如果需要时区的话需要使用 <code>ZonedDateTime</code></p>
<p>这个类只能进行日期的相关操作，没有具体的时间。</p>
<p>下面介绍常用的几个方法</p>
<h3 id="atTime"><a href="#atTime" class="headerlink" title="atTime"></a>atTime</h3><p>生成一个带有时间的日期，返回结果是 <code>LocalDateTime</code> 所以很明显这个就是带有时间的日期类。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">LocalDateTime time = LocalDate.now().atTime(<span class="number">12</span>, <span class="number">3</span>, <span class="number">22</span>,<span class="number">232</span>);</span><br><span class="line">System.out.println(time); <span class="comment">//2019-05-10T12:03:22.000000232</span></span><br></pre></td></tr></table></figure>

<h3 id="compareTo"><a href="#compareTo" class="headerlink" title="compareTo()"></a>compareTo()</h3><p>比较两个时间返回的值是 int ，大于 0 则是比较者大，否则是被比较者大，为0相等。</p>
<h3 id="format"><a href="#format" class="headerlink" title="format()"></a>format()</h3><p>格式化时间，需要传递一个时间的 formatter 类型为 <code>[DateTimeFormatter]</code> </p>
<h3 id="parse"><a href="#parse" class="headerlink" title="parse()"></a>parse()</h3><p>同上第一个参数是解析的字符串，第二个就是传入formatter</p>
<h3 id="plus-minus"><a href="#plus-minus" class="headerlink" title="plus/minus"></a>plus/minus</h3><p>对日期进行加减，第一个参数传入的是 int 第二个就是单位，单位使用 <code>ChronoUnit</code> 枚举类型</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LocalDate1</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        LocalDate today = LocalDate.now();</span><br><span class="line">        <span class="comment">// 添加时间</span></span><br><span class="line">        LocalDate i22day = today.plusDays(<span class="number">22</span>);</span><br><span class="line">        LocalDate tomorrow = today.plus(<span class="number">1</span>, ChronoUnit.DAYS);</span><br><span class="line">        <span class="comment">// 减去时间</span></span><br><span class="line">        LocalDate s22day = today.minus(<span class="number">22</span>, ChronoUnit.DAYS);</span><br><span class="line">        LocalDate yesterday = tomorrow.minusDays(<span class="number">2</span>);</span><br><span class="line"></span><br><span class="line">        System.out.println(today);</span><br><span class="line">        System.out.println(tomorrow);</span><br><span class="line">        System.out.println(yesterday);</span><br><span class="line">        System.out.println(i22day);</span><br><span class="line">        System.out.println(s22day);</span><br><span class="line"></span><br><span class="line">        LocalDate independenceDay = LocalDate.of(<span class="number">2014</span>, Month.JULY, <span class="number">4</span>);</span><br><span class="line">        DayOfWeek dayOfWeek = independenceDay.getDayOfWeek();</span><br><span class="line">        System.out.println(dayOfWeek);    <span class="comment">// FRIDAY</span></span><br><span class="line"></span><br><span class="line">        DateTimeFormatter germanFormatter =</span><br><span class="line">                DateTimeFormatter</span><br><span class="line">                        .ofLocalizedDate(FormatStyle.MEDIUM)</span><br><span class="line">                        .withLocale(Locale.GERMAN);</span><br><span class="line"></span><br><span class="line">        DateTimeFormatter dateTimeFormatter = DateTimeFormatter.ofPattern(<span class="string">"yyyy/mm/dd hh:mm:ss"</span>);</span><br><span class="line">        LocalDate xmas = LocalDate.parse(<span class="string">"24.12.2014"</span>, germanFormatter);</span><br><span class="line">        <span class="comment">// System.out.println(LocalDate.parse("2014/02/01 12:03:22", dateTimeFormatter));</span></span><br><span class="line">        System.out.println(xmas);   <span class="comment">// 2014-12-24</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        System.out.println(LocalDate.now().atStartOfDay());</span><br><span class="line"></span><br><span class="line">        LocalDateTime time = LocalDate.now().atTime(<span class="number">12</span>, <span class="number">3</span>, <span class="number">22</span>,<span class="number">232</span>);</span><br><span class="line">        System.out.println(time);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="时间-LocalDateTime"><a href="#时间-LocalDateTime" class="headerlink" title="时间 LocalDateTime"></a>时间 LocalDateTime</h2><p>这个和上面类似是一个时间类，不仅仅有日期还是时间。</p>
<h3 id="atZone"><a href="#atZone" class="headerlink" title="atZone"></a>atZone</h3><p>产生带有时区日期</p>
<h3 id="format-1"><a href="#format-1" class="headerlink" title="format"></a>format</h3><p>格式化时间</p>
<h3 id="of"><a href="#of" class="headerlink" title="of"></a>of</h3><p>从一个时间创建对象 <code>of(int year, int month, int dayOfMonth, int hour, int minute, int second)</code></p>
<h3 id="parse-1"><a href="#parse-1" class="headerlink" title="parse"></a>parse</h3><p>解析时间 <code>**parse**([CharSequence]text, [DateTimeFormatter]</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LocalDateTime1</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        LocalDateTime sylvester = LocalDateTime.of(<span class="number">2014</span>, Month.DECEMBER, <span class="number">31</span>, <span class="number">23</span>, <span class="number">59</span>, <span class="number">59</span>);</span><br><span class="line"></span><br><span class="line">        DayOfWeek dayOfWeek = sylvester.getDayOfWeek();</span><br><span class="line">        System.out.println(dayOfWeek);      <span class="comment">// WEDNESDAY</span></span><br><span class="line"></span><br><span class="line">        Month month = sylvester.getMonth();</span><br><span class="line">        System.out.println(month);          <span class="comment">// DECEMBER</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">long</span> minuteOfDay = sylvester.getLong(ChronoField.MINUTE_OF_DAY);</span><br><span class="line">        System.out.println(minuteOfDay);    <span class="comment">// 1439</span></span><br><span class="line"></span><br><span class="line">        Instant instant = sylvester</span><br><span class="line">                .atZone(ZoneId.systemDefault())</span><br><span class="line">                .toInstant();</span><br><span class="line"></span><br><span class="line">        Date legacyDate = Date.from(instant);</span><br><span class="line">        System.out.println(legacyDate);     <span class="comment">// Wed Dec 31 23:59:59 CET 2014</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        DateTimeFormatter formatter =</span><br><span class="line">                DateTimeFormatter</span><br><span class="line">                        .ofPattern(<span class="string">"MMM dd, yyyy - HH:mm"</span>);</span><br><span class="line"></span><br><span class="line">        LocalDateTime parsed = LocalDateTime.parse(<span class="string">"Nov 03, 2014 - 07:13"</span>, formatter);</span><br><span class="line">        String string = parsed.format(formatter);</span><br><span class="line">        System.out.println(string);     <span class="comment">// Nov 03, 2014 - 07:13</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="LocalTime"><a href="#LocalTime" class="headerlink" title="LocalTime"></a>LocalTime</h2><p>这个类是上面的LocaDateTime中的 Time 部分，也就是只具有时间没有日期。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class LocalDate1 &#123;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        LocalDate today = LocalDate.now();</span><br><span class="line">        // 添加时间</span><br><span class="line">        LocalDate i22day = today.plusDays(22);</span><br><span class="line">        LocalDate tomorrow = today.plus(1, ChronoUnit.DAYS);</span><br><span class="line">        // 减去时间</span><br><span class="line">        LocalDate s22day = today.minus(22, ChronoUnit.DAYS);</span><br><span class="line">        LocalDate yesterday = tomorrow.minusDays(2);</span><br><span class="line"></span><br><span class="line">        System.out.println(today);</span><br><span class="line">        System.out.println(tomorrow);</span><br><span class="line">        System.out.println(yesterday);</span><br><span class="line">        System.out.println(i22day);</span><br><span class="line">        System.out.println(s22day);</span><br><span class="line"></span><br><span class="line">        LocalDate independenceDay = LocalDate.of(2014, Month.JULY, 4);</span><br><span class="line">        DayOfWeek dayOfWeek = independenceDay.getDayOfWeek();</span><br><span class="line">        System.out.println(dayOfWeek);    // FRIDAY</span><br><span class="line"></span><br><span class="line">        DateTimeFormatter germanFormatter =</span><br><span class="line">                DateTimeFormatter</span><br><span class="line">                        .ofLocalizedDate(FormatStyle.MEDIUM)</span><br><span class="line">                        .withLocale(Locale.GERMAN);</span><br><span class="line"></span><br><span class="line">        DateTimeFormatter dateTimeFormatter = DateTimeFormatter.ofPattern(&quot;yyyy/mm/dd hh:mm:ss&quot;);</span><br><span class="line">        LocalDate xmas = LocalDate.parse(&quot;24.12.2014&quot;, germanFormatter);</span><br><span class="line">        // System.out.println(LocalDate.parse(&quot;2014/02/01 12:03:22&quot;, dateTimeFormatter));</span><br><span class="line">        System.out.println(xmas);   // 2014-12-24</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        System.out.println(LocalDate.now().atStartOfDay());</span><br><span class="line"></span><br><span class="line">        LocalDateTime time = LocalDate.now().atTime(12, 3, 22,232);</span><br><span class="line">        System.out.println(time);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="Clock"><a href="#Clock" class="headerlink" title="Clock"></a>Clock</h2><p>这个类可以获取当前的时间戳</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Clock1</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Clock millis = Clock.tickMillis(ZoneOffset.UTC);</span><br><span class="line">        Clock seconds = Clock.tickSeconds(ZoneOffset.UTC);</span><br><span class="line">        Clock minutes = Clock.tickMinutes(ZoneOffset.UTC);</span><br><span class="line">        <span class="comment">// 精确到毫秒的时间戳</span></span><br><span class="line">        System.out.println(millis.millis());</span><br><span class="line">        <span class="comment">// 精确到秒的时间戳</span></span><br><span class="line">        System.out.println(seconds.millis());</span><br><span class="line">        <span class="comment">// 精确到分钟的时间戳</span></span><br><span class="line">        System.out.println(minutes.millis());</span><br><span class="line">        <span class="comment">// 精确到毫秒的时间</span></span><br><span class="line">        System.out.println(millis.instant());</span><br><span class="line">        <span class="comment">// 精确到秒的时间</span></span><br><span class="line">        System.out.println(seconds.instant());</span><br><span class="line">        <span class="comment">// 精确到分钟的时间</span></span><br><span class="line">        System.out.println(minutes.instant());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的输出结果是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">1557567339397</span><br><span class="line">1557567339000</span><br><span class="line">1557567300000</span><br><span class="line">2019-05-11T09:35:39.397Z</span><br><span class="line">2019-05-11T09:35:39Z</span><br><span class="line">2019-05-11T09:35:00Z</span><br></pre></td></tr></table></figure>

<h2 id="Map初始化小技巧"><a href="#Map初始化小技巧" class="headerlink" title="Map初始化小技巧"></a>Map初始化小技巧</h2><p>再看Clock类中的 ZoneId 接口的时候发现了这个接口中使用了一个Map，然对Map做了初始化。一般初始化必须使用 put 放数据，这里采用了 ofEntries 来初始化多组数据。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> Map&lt;String, String&gt; SHORT_IDS = Map.ofEntries(</span><br><span class="line">        entry(<span class="string">"ACT"</span>, <span class="string">"Australia/Darwin"</span>),</span><br><span class="line">        entry(<span class="string">"AET"</span>, <span class="string">"Australia/Sydney"</span>),</span><br><span class="line">        entry(<span class="string">"AGT"</span>, <span class="string">"America/Argentina/Buenos_Aires"</span>),</span><br><span class="line">        entry(<span class="string">"ART"</span>, <span class="string">"Africa/Cairo"</span>),</span><br><span class="line">        entry(<span class="string">"AST"</span>, <span class="string">"America/Anchorage"</span>),</span><br><span class="line">        entry(<span class="string">"BET"</span>, <span class="string">"America/Sao_Paulo"</span>),</span><br><span class="line">        entry(<span class="string">"BST"</span>, <span class="string">"Asia/Dhaka"</span>),</span><br><span class="line">        entry(<span class="string">"CAT"</span>, <span class="string">"Africa/Harare"</span>),</span><br><span class="line">        entry(<span class="string">"CNT"</span>, <span class="string">"America/St_Johns"</span>),</span><br><span class="line">        entry(<span class="string">"CST"</span>, <span class="string">"America/Chicago"</span>),</span><br><span class="line">        entry(<span class="string">"CTT"</span>, <span class="string">"Asia/Shanghai"</span>),</span><br><span class="line">        entry(<span class="string">"EAT"</span>, <span class="string">"Africa/Addis_Ababa"</span>),</span><br><span class="line">        entry(<span class="string">"ECT"</span>, <span class="string">"Europe/Paris"</span>),</span><br><span class="line">        entry(<span class="string">"IET"</span>, <span class="string">"America/Indiana/Indianapolis"</span>),</span><br><span class="line">        entry(<span class="string">"IST"</span>, <span class="string">"Asia/Kolkata"</span>),</span><br><span class="line">        entry(<span class="string">"JST"</span>, <span class="string">"Asia/Tokyo"</span>),</span><br><span class="line">        entry(<span class="string">"MIT"</span>, <span class="string">"Pacific/Apia"</span>),</span><br><span class="line">        entry(<span class="string">"NET"</span>, <span class="string">"Asia/Yerevan"</span>),</span><br><span class="line">        entry(<span class="string">"NST"</span>, <span class="string">"Pacific/Auckland"</span>),</span><br><span class="line">        entry(<span class="string">"PLT"</span>, <span class="string">"Asia/Karachi"</span>),</span><br><span class="line">        entry(<span class="string">"PNT"</span>, <span class="string">"America/Phoenix"</span>),</span><br><span class="line">        entry(<span class="string">"PRT"</span>, <span class="string">"America/Puerto_Rico"</span>),</span><br><span class="line">        entry(<span class="string">"PST"</span>, <span class="string">"America/Los_Angeles"</span>),</span><br><span class="line">        entry(<span class="string">"SST"</span>, <span class="string">"Pacific/Guadalcanal"</span>),</span><br><span class="line">        entry(<span class="string">"VST"</span>, <span class="string">"Asia/Ho_Chi_Minh"</span>),</span><br><span class="line">        entry(<span class="string">"EST"</span>, <span class="string">"-05:00"</span>),</span><br><span class="line">        entry(<span class="string">"MST"</span>, <span class="string">"-07:00"</span>),</span><br><span class="line">        entry(<span class="string">"HST"</span>, <span class="string">"-10:00"</span>)</span><br><span class="line">    );</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>Java8</tag>
      </tags>
  </entry>
  <entry>
    <title>扬帆起航</title>
    <url>/2019/04/14/Life/%E6%89%AC%E5%B8%86%E8%B5%B7%E8%88%AA/</url>
    <content><![CDATA[<h1 id="扬帆起航"><a href="#扬帆起航" class="headerlink" title="扬帆起航"></a>扬帆起航</h1><p>其实很早就想写这篇文章，只是一直不知道从何说起。或者说，没能腾出一片完整的时间，给自己的大学生活写一个完整的回忆录。其实细细想来，这段时光更像是平静的水面，偶然也有湍急之处泛着白色的浪花。</p>
<h2 id="开始"><a href="#开始" class="headerlink" title="开始"></a>开始</h2><p>​    和大多数人一样，以萌新的身份来到大学，看着即将成为自己学长学姐的那群人，一时不知道从哪里开始了解这个陌生而又神秘的校园。好，美好的大学生活就先从图书馆开始。良好的习惯是成功一半，也得要先熟悉熟悉自己以后学习的地方。</p>
<p>​    摸索了挺久，才找到了计算机学科的阅览室。怎么说呢，被一大堆《Photoshop 从入门到精通》、《21 天精通 C 语言》、《JAVA语言精粹》吸引，幻想着自己真的能21天完成菜鸟到码神的逆袭。</p>
<p>​    好吧… 想多了，不过，我还是爱上了这个地方。接下来的一学期，就是扎根图书馆，进行名词式学习了。之所以称之为名词式学习，是真的觉得自己的学习，没有正确的方式。一股脑的恨不得把没听过的东西都去学一遍，没见过的语言都想办法 <strong>『21天精通』</strong> 。于是，我成了一个彻底的 “宅男” ，只不过宅的是图书馆。</p>
<p>​    所以还是想说，要在正确的路上做正确的事。这时候，一个好的引导者就显得尤为重要。他会带着你，踩尽量少的坑，走弯路更少的路。这个人，可以是你的老师，你的学长学姐，偶然发现的你们班里的大神。记得，找最适合自己的人，走最适合自己的路。</p>
<h2 id="LAMP-光"><a href="#LAMP-光" class="headerlink" title="LAMP - 光"></a>LAMP - 光</h2><p>​    庆幸的是，我的野路子并没有持续太久，我找到了方向——加入实验室。这就是我想说的另一点了，加入实验室。任何时候都不要说 “等我在多学点，再怎么怎么样”。要知道，实验室本身就是，可以更好的更快的学习新东西并快速实践的地方。</p>
<p>​    我在大一下学期就进入了网络中心，也是在这里下定了决心一门心思的去做开发，不再一味的折腾 Linux 。（PS：如果不是想要看内核源码或者做系统运维，就不用花大量时间折腾 Linux 软件了，时间上比较不划算）</p>
<p>​    加入了正规军，也接受了更正规的训练。我更加清楚，作为一名计算机专业的学生，我以后要走的是一条什么样的路，要成为一个怎么样的人。我们的团队叫做 “LMAP” ，取自我们开发使用的技术栈，同时也有 “光” 的含义！在这里，我度过了我的整个大学时光。</p>
<p>​    已经说不清楚，那些曾经帮助过我的老师和学长们，是我所尊敬的师长，还是陪我一起成长的家人了。我们一起撸过凌晨两点的小院烧烤，也一起看过凌晨四点钟的西大，我想在这里特别感谢帮助过我的 『杨建丰』老师，以及 『金刚』 学长， 『文科』 学长！</p>
<h2 id="嘉木"><a href="#嘉木" class="headerlink" title="嘉木"></a>嘉木</h2><p>​    我想说一件，我大学里很骄傲的事。也许没什么人听过，但确确实实是我大学里很珍贵的财富。事情源于大二上学期，在一个学长的组织下我们准备建设西北大学自己的 PT 站点。奈何出师未捷身先死，本来组建的团队还没开始开发就解散了。我很感谢20岁的我，可以那么勇敢得，去一个人做我现在可能有顾虑的事。</p>
<p>​    项目基于开源的 “NexusPHP 项目” 。其实关于这个项目的案例很多，但是资料很少，很多高校采用了这套代码，并且进行了魔改，但是遗憾的是并没有开源。怀着雄心壮志，我一个人上路了。我想把它做好，然后开源出去，哪怕困难重重（事实证明遇到的困恼是重重重重重）。</p>
<p>​    项目从九月中旬一直到十二月份，出了三个测试版本。这段时间基本除了上课时间我都是坐在电脑前面赶项目。由于 NexusPHP 是 2006 年开发的，当时的开发团队采用了比较原始的前后端夹杂的方式，代码基本没有可维护性而言，大部分还是 function block 基本不存在 OO 设计，HTML 和 CSS 基本全部嵌在 PHP 中，一个文件 1000 - 3000 行，甚至还有 15000 行的。那段时间真的是日常崩溃 ，不知道因为改代码电脑死机多少次，又熬了多少次夜。经常为了一个问题和功能，想到睡不着觉，有时候做梦都在写代码，想要看着自己的成果，早点问世。</p>
<p>​    十二月份，第一个上线的版本终于确定了，第一周的注册量只有 500 。不过，当时上线的时间确实也是选择的不对，那时候快要期末考试，大家一门心思复习，没什么时间关注别的东西。第二年又对界面进行了重构，基本是全新的 UI。之后，我们也想了很多办法去提高用户量。我们组织了一些宣传活动有推出了嘉木的纪念文化衫，还想了很多办法降低用户使用站点的门槛，但是最终用户活跃度始终稳定在几百人，并没有达到预期。</p>
<p>​    最终，站点只运行了一年，由于多方面的问题被关停。这次的失败也让我明白，一个产品的孵化并不简简单单的依靠技术，产品的运营和团队管理是上层建筑。没有好的运营和管理，再好的产品也没有办法发挥价值。</p>
<p>​    另外比较庆幸的是，这次之后，我确实发现自己的技术得到了质的飞跃——毕竟 10 多万行代码的编码经验和调试上的经验比看书来的要真切的多，也算是真切的体会到 “纸上得来终觉浅，绝知此事要躬行”  。</p>
<h2 id="迷茫"><a href="#迷茫" class="headerlink" title="迷茫"></a>迷茫</h2><p>​    其实在嘉木比较稳定运行期间，就没有太多额外的开发工作了。那段时间除了处理实验室老师交代的任务之外，就全身心的投入到 <code>ThinkPHP</code> 和 <code>OneThink</code> 的源码。其实源码看了会很容易忘掉，所以不仅仅是看，看完以后我都会凭着印象和思路再敲一遍，实现框架和系统的核心部分。</p>
<p>​    我在阅读读 <code>OneThink</code> 源码时就明显感觉到了自己和巨佬们之间的差距。虽然说 <code>OneThink</code> 源码不是那种 “Bible 级别” ，但官方对 TP 的运用确实称的上炉火纯青。一边阅读，一边发现原来<code>ThinkPHP</code> 可以这么写，原来那么多特性是可以这么使用的，原来一个简单的模块需要这么多代码保证高扩展性，健壮性，和稳定性。</p>
<p>​    大二快结束时，我心里却越来越觉得慌张。自己只熟悉 PHP 一门语言，其他的只能算是入门，这样还怎么成为*<em>『全栈工程师』 *</em>？还怎么愉快的写 Bug ？加上那段敏感时间里，实验室学长们也都签了不错的工作，有去 BAT 的，也有去其他知名互联网公司的。他们的离开让我觉得到伤感和不舍。偶尔还会思考，实验室的下一批成员会是什么样的，我们会不会相处的很好，已经成为学长的我，能不能像我的学长们那样，给别人提供正确的引导和帮助？</p>
<h2 id="选择"><a href="#选择" class="headerlink" title="选择"></a>选择</h2><p>​    空闲期的时间总是过得异常的快，转眼自己就已经大三了。突然发现，热爱了这么久的计算机，捣鼓着写了两年的代码，自己却没有一门能够拿得出手的技术。按照自己的规划，我是希望能够找一个不错的实习的。打开招聘网站却发现很多公司并没有 <code>PHP</code> 职位。当时的我，除了PHP就只会写前端了，还只能勉强算是个半吊子前端。我明确地，感受到了自己的压力和慌张。</p>
<p>​    想了段时间，我觉得剩下的一年里，学一门纯正的面向对象语言吧！可是具体要猛攻哪一门呢？ Java 在大一的有学过一段时间，但是很久不写，很多基本语法都已经不记得了。C++ 吧，当时确实用功学了半年，高级特性，内存管理，STL ，Boost 也都有好好看，只是后来也没好好用它写过项目。</p>
<p>​    其实这个问题我纠结了很久。也许很多人说 “其实不论什么编程语言只要你精通了一门，如果你能融汇贯通其实其他的语言也一样”。其实我是很抵触这种说法的，简单对于 C++ 和 Java 来说，就算你敢说你熟悉 C++ 我觉得短时间熟悉 Java 语法，能使用 Java 做开发是没问题的。就像我在大一的很大一段时间内是使用的以前编写 C++ 的思维去 Java 写的，这样不仅不能表现出这个语言的特性和优雅，反而会觉得使用起来有些蹩脚。就像一个长时间写驱动和系统软件的人去接手一个大型的 Web 项目，我想他很可能会写出垃圾代码来。</p>
<p>​    经过综合各方面的原因，我选择了Java。我知道，当我选择了一条路，另一条路上的风景便与我无关。一旦选择，我便不再摇摆不定。之后，全身心的学习Java是我每天都习以为常的事了。偶尔想想自己真的在面向‘’对象‘’编程，生活倒也怡然自得。</p>
<h2 id="转折"><a href="#转折" class="headerlink" title="转折"></a>转折</h2><p>​    良好的基础决定了成功的一半。当时的我，把 《疯狂 Java 讲义》 和 《 JDK 1.8 API 》 里面重要章节 Demo 都敲了一遍，把语言基础、集合框架、OO、IO、反射、多线程、Servlet、JSP、Hibernate …  理解的异常透彻，日常觉得自己是一个 “人工语法分析器” 。</p>
<p>​    可是当我掌握这些东西以后，我发现我好像一直停留在使用的阶段。这是一种有力气没出事的难受。后来，我开始思考，为什么我不去深入到底层探一探究竟？JDK 里的 <code>集合框架</code>、<code>并发工具包</code>、<code>IO</code>  究竟是如何实现的？Java 虚拟机是怎么运行起来的？JVM 的内存、GC、类加载、性能优化是怎么做到的？那些主流框架是如何设计的？DB 作为一个很重要的部分，我又该如何优化、如何做运维呢？这样的思考让我恐慌，却也促进了我的进步。我开始从另一个角度学习了。</p>
<p>​    回想那段时间，内心还是会燃气一股热血。那是知识的输入和输出都到达最大化的一个阶段。基本两天我就可以刷掉一本 200 - 400 页的书，然后整理笔记。记不起来的、感觉模糊的、牵扯到其他不理解的继续看书，找博客，弄清楚之后把自己的理解写在笔记上。有时候一天就坐在那敲一篇几万字的读书笔记。其实说不清痛苦还是快乐 …</p>
<p>​    当时差不多看了 30 多本经典书籍，也算是形成了比较完整的知识结构。那种高强度的训练让我把从大一学到的各种计算机专业知识都串起来了，好像突然打通了任督二脉，总能通过一个知识点发散出很多很多相关的知识。算得上是，真正跨入了开发的门槛。</p>
<h2 id="四月"><a href="#四月" class="headerlink" title="四月"></a>四月</h2><p>​    2018 年刚开学我就泡在图书馆看书了。看以前写的笔记，整理博客，重新思考之前刷过的错题。我发现不论是以前学习的东西还是以前做的项目都还有很大的提升空间。对于以前看的书做的笔记，我又重新做了一遍，发现自己又有了很多新的理解。对于项目也会思考以前为什么那么实现，现在让我实现会不会有更好的实现方式呢？剩下的就是刷题了，LeetCode 、牛客，熟悉一些常用的算法，为找实习做准备。</p>
<p>​    三月尾，我试着投了一些简历。四月初就开始陆陆续续面试了。说到面试，其实第一次面试大家都会紧张的，但是不要让过度的紧张让自己看起来应接不暇就可以了。之后的面试就会慢慢放松下来。其实面试，也就是一个双向的沟通学习，展示自己的过程，本没有太恐怖。找准定位，变现自己，就做到了能到到的最好了。</p>
<p>​    后面就是 腾讯、阿里、百度 、携程的面试了，基本这几家面试官都很和善的，一路也算是比较顺利，只是当时腾讯最后一个主管面的时候直接给了五道算法题，还必须用 C 写，然后下载一个 gcc + CLion 基本时间过去一大半了，最后只有十多分钟做了两道题，然后就没有然后了… </p>
<p>​    这里面印象比较好的就是阿里和携程了。阿里问的技术点都很深入，主要是因为问的点都偏向实际应用，我个人又更加喜欢这种关于实际开发的问题，另外也是我并不擅长算法。携程面试官则是对应聘者很热情，虽然当时面试场地没有腾讯那么大，但是面试官很 nice 的给我提了很多建议。</p>
<p>​    最后我还是去了阿里，毕竟那里是 Javaer 的天堂，也是我学Java最初的心愿了。</p>
<h2 id="实习"><a href="#实习" class="headerlink" title="实习"></a>实习</h2><p>​    阿里的师兄和师姐们都非常的 nice ，并且 对于新人的培养非常的用心。虽然感觉他们也特别忙碌，但师兄始终都很关心我，并给我找了很多资料让我熟悉业务，还抽出自己的空闲时间给我讲业务逻辑和项目结构。虽然在短时间内不能完全弄清楚公司业务，但是师兄的讲解还是让我受益颇多！</p>
<p>​    另外，在阿里的实习也让我再一次发现自己真的很渺小。我会发现原来还有这么多自己不知道的框架、系统和工具。于是我又开始宅了，只不过这一次，我宅的地方变成了公司。我每天熟悉系统的业务模型和组内维护的系统代码，还要安排时间学习集团内的新技术。忙碌而紧张的学习甚至让我来不及为自己的进步开心，只是在内心深处觉得自己应该在多学一点，在努力一点。不仅仅是为了对得起自己最初的心愿，也是为了对得起那些默默支持着我的，默默帮助着我的人。</p>
<h2 id="前行"><a href="#前行" class="headerlink" title="前行"></a>前行</h2><p>​    大四的时间过得异常的快，对于我来说，还没有好好感受过感受大学的美好就要匆匆说再见了。虽然我总是会在与别人交谈时避免谈到毕业的问题，可是自己心里清楚，自己留下了多少的遗憾。那些没有用心欣赏的玉兰花，没有好好感受过的西操场的风，以及那些来不及参与的一次次的同学聚会。</p>
<p>​    大四的生活也没有我曾经以为的那么空闲。我还是写着代码，改着论文，过着平凡而又规律的生活。也许有人会觉得，只有奋斗的大学不会遗憾吗？会的，当然会。可是，没有奋斗的大学可能就不只是遗憾了，而是悔恨。也许是习惯了忙碌而充实的生活，也许是自己骨子里无趣又枯燥的灵魂，总之，我想我还是会继续这般生活——全力以赴，问心无愧。</p>
]]></content>
      <categories>
        <category>生活感悟</category>
      </categories>
      <tags>
        <tag>生活感悟</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring 源码分析(一)</title>
    <url>/2019/03/15/Spring/Spring%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90(%E4%B8%80)/</url>
    <content><![CDATA[<h2 id="Spring-源码分析-一"><a href="#Spring-源码分析-一" class="headerlink" title="Spring 源码分析 (一)"></a>Spring 源码分析 (一)</h2>]]></content>
      <categories>
        <category>Spring 源码分析</category>
        <category>Spring</category>
      </categories>
      <tags>
        <tag>Spring</tag>
        <tag>Spring 源码</tag>
        <tag>源码</tag>
      </tags>
  </entry>
  <entry>
    <title>WebStrom 使用技巧</title>
    <url>/2019/01/21/FrontEnd/WebStrom%20%E6%8A%80%E5%B7%A7/</url>
    <content><![CDATA[<h2 id="1-React-live-template"><a href="#1-React-live-template" class="headerlink" title="1. React live template"></a>1. React live template</h2><ol>
<li>state    this.state</li>
<li>sst       this.setState</li>
<li>rpc    生成组件类</li>
<li>rsfp      生成组件函数</li>
<li>rrc        具有redux的组件类</li>
<li>rpt        propTypes</li>
<li>ren       render 方法</li>
<li>ptsr   string required</li>
<li>pts     string 类型</li>
<li>pto  object 类型</li>
<li>ptor    object required</li>
<li>ptn/ptnr ptf/ptfr  ptb/ptbf  pta/ptar     number / function / bool /array</li>
<li>props     this.props</li>
<li>est     this.state={}</li>
<li>cdm    componentDidMount</li>
</ol>
]]></content>
      <categories>
        <category>工具</category>
      </categories>
      <tags>
        <tag>工具 WebStrom JetBrains</tag>
      </tags>
  </entry>
  <entry>
    <title>React 实战</title>
    <url>/2019/01/17/FrontEnd/React%20%E5%AE%9E%E6%88%98/</url>
    <content><![CDATA[<h2 id="1-目录"><a href="#1-目录" class="headerlink" title="1.目录"></a>1.目录</h2><p><img src="http://images.heniankj.com/20190117112438.png" alt></p>
<h2 id="2-依赖安装"><a href="#2-依赖安装" class="headerlink" title="2.依赖安装"></a>2.依赖安装</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">cnpm install antd-mobile -S </span><br><span class="line">//组件按需加载  第二个是用来做react脚手架的二次配置的，因为我们看不到Webpack的配置文件了 https://www.cnblogs.com/xiaohuochai/p/8491055.html</span><br><span class="line">cnpm install --save-dev babel-plugin-import react-app-rewired@2.0.2-next.0 //注意指定版本号否则不兼容</span><br><span class="line">cnpm install --save-dev less@2.7.3 less-loader</span><br></pre></td></tr></table></figure>

<h2 id="3-配置项目"><a href="#3-配置项目" class="headerlink" title="3.配置项目"></a>3.配置项目</h2><h3 id="1-修改index添加触摸事件"><a href="#1-修改index添加触摸事件" class="headerlink" title="1.修改index添加触摸事件"></a>1.修改index添加触摸事件</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"width=device-width, initial-scale=1, maximum-scale=1, minimum-scale=1, user-scalable=no"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"https://as.alipayobjects.com/g/component/fastclick/1.0.6/fastclick.js"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">      <span class="keyword">if</span> (<span class="string">'addEventListener'</span> <span class="keyword">in</span> <span class="built_in">document</span>) &#123;</span></span><br><span class="line"><span class="javascript">        <span class="built_in">document</span>.addEventListener(<span class="string">'DOMContentLoaded'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span></span><br><span class="line"><span class="javascript">          FastClick.attach(<span class="built_in">document</span>.body);</span></span><br><span class="line"><span class="actionscript">        &#125;, <span class="literal">false</span>);</span></span><br><span class="line">      &#125;</span><br><span class="line"><span class="javascript">      <span class="keyword">if</span>(!<span class="built_in">window</span>.Promise) &#123;</span></span><br><span class="line"><span class="javascript">        <span class="built_in">document</span>.writeln(<span class="string">'&lt;script src="https://as.alipayobjects.com/g/component/es6-promise/3.2.2/es6-promise.min.js"'</span>+<span class="string">'&gt;'</span>+<span class="string">'&lt;'</span>+<span class="string">'/'</span>+<span class="string">'script&gt;'</span>);</span></span><br><span class="line">      &#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-添加-antd-配置文件-config-overrides-js"><a href="#2-添加-antd-配置文件-config-overrides-js" class="headerlink" title="2.添加 antd 配置文件 config-overrides.js"></a>2.添加 antd 配置文件 config-overrides.js</h3><p>注意这个文件不是在 src 下面而是在整个项目的与 package,json 同级的目录。定义加载配置的 js 模块 !</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> &#123;injectBabelPlugin&#125; = <span class="built_in">require</span>(<span class="string">'react-app-rewired'</span>);</span><br><span class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span> <span class="title">override</span>(<span class="params">config, env</span>) </span>&#123;</span><br><span class="line">config = injectBabelPlugin([<span class="string">'import'</span>, &#123;<span class="attr">libraryName</span>: <span class="string">'antd-mobile'</span>, <span class="attr">style</span>: <span class="string">'css'</span>&#125;],</span><br><span class="line">config);</span><br><span class="line"><span class="keyword">return</span> config;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>修改配置: package.json </p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">"scripts": &#123;</span><br><span class="line">    "start": "react-app-rewired start",</span><br><span class="line">    "build": "react-app-rewired build",</span><br><span class="line">    "test": "react-app-rewired test --env=jsdom",</span><br><span class="line">    "eject": "react-scripts eject"</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这是关于自定义配置的内容  <a href="https://ant.design/docs/react/use-with-create-react-app-cn" target="_blank" rel="noopener">Antd配置</a></p>
]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>React</tag>
      </tags>
  </entry>
  <entry>
    <title>MangoDB 入门</title>
    <url>/2019/01/16/DataBase/MangoDB%20%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h2 id="1-设置为windows启动项"><a href="#1-设置为windows启动项" class="headerlink" title="1. 设置为windows启动项"></a>1. 设置为windows启动项</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">mongod -dbpath <span class="string">"C:\Program Files\MongoDB\Server\4.0\data"</span> -logpath <span class="string">"C:\Program Files\MongoDB\Server\4.0\log\MongoDB.log"</span> -install -serviceName <span class="string">"MongoDB"</span></span><br></pre></td></tr></table></figure>

<h2 id="2-基本概念"><a href="#2-基本概念" class="headerlink" title="2.基本概念"></a>2.基本概念</h2><ol>
<li>数据库（database）:数据的仓库可以在仓库中存放集合</li>
<li>集合（collection）：集合是数组可以在集合中存放文档</li>
<li>文档（document）：存储操作的都是文档，文档是数据库中最小的单位。</li>
</ol>
<p>在MongoDB中，数据库和集合都不需要手动创建，当我们创建文档时，如果文档所在的集合或数据库不存在会自动创建数据库和集合.MongoDB的文档的属性值也可以是一个文档，当一个文档的属性值是一个文档时，我们称这个文档叫做 内嵌文档</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.users.update(&#123;username:&quot;sunwukong&quot;&#125;,&#123;$set:&#123;hobby:&#123;cities:[&quot;beijing&quot;,&quot;shanghai&quot;,&quot;shenzhen&quot;] , movies:[&quot;sanguo&quot;,&quot;hero&quot;]&#125;&#125;&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="3-基本指令"><a href="#3-基本指令" class="headerlink" title="3.基本指令"></a>3.基本指令</h2><h3 id="1-显示数据库"><a href="#1-显示数据库" class="headerlink" title="1. 显示数据库"></a>1. 显示数据库</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">show dbs</span><br><span class="line">show databases</span><br></pre></td></tr></table></figure>

<h3 id="2-进入到指定的数据库中"><a href="#2-进入到指定的数据库中" class="headerlink" title="2.进入到指定的数据库中"></a>2.进入到指定的数据库中</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">use 数据库名</span><br></pre></td></tr></table></figure>

<p>​    如果我们use了一个不存在的数据库他也不会报错而是在我们第一次创建文档的时候创建这个数据库，在MongoDB中所有的数据库和集合都不需要我们去创建的 。</p>
<h3 id="3-当前所处的数据库"><a href="#3-当前所处的数据库" class="headerlink" title="3.当前所处的数据库"></a>3.当前所处的数据库</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db</span><br></pre></td></tr></table></figure>

<p>注意一下 db 就类似于 this 只想当前的上下文，我们在后面的 crud 操作可以直接使用数据库名也可以在进入到数据库之后直接使用 db</p>
<h3 id="4-显示数据库中所有的集合"><a href="#4-显示数据库中所有的集合" class="headerlink" title="4.显示数据库中所有的集合"></a>4.显示数据库中所有的集合</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">show collections</span><br></pre></td></tr></table></figure>

<h3 id="5-数据库的CRUD（增删改查）的操作"><a href="#5-数据库的CRUD（增删改查）的操作" class="headerlink" title="5.数据库的CRUD（增删改查）的操作"></a>5.数据库的CRUD（增删改查）的操作</h3><h4 id="1-向数据库中插入文档"><a href="#1-向数据库中插入文档" class="headerlink" title="1.向数据库中插入文档"></a>1.向数据库中插入文档</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.&lt;collection&gt;.insert()  向集合中插入一个或多个文档</span><br></pre></td></tr></table></figure>

<p>当我们向集合中插入文档时，如果没有给文档指定_id属性，则数据库会自动为文档添加  <code>_id</code> 该属性用来作为文档的唯一标识 , <code>_id</code>我们可以自己指定，如果我们指定了数据库就不会在添加了，如果自己指定_id 也必须确保它的唯一性</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.collection.insertOne()  插入一个文档对象</span><br><span class="line">db.collection.insertMany()  插入多个文档对象</span><br></pre></td></tr></table></figure>

<p>上面的是在3.2 版本中新加的操作，为了语义更加的明确。</p>
<p>例子：向test数据库中的，stus集合中插入一个新的学生对象<br>{name:”孙悟空”,age:18,gender:”男”}</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.stus.insert(&#123;name:&quot;孙悟空&quot;,age:18,gender:&quot;男&quot;&#125;)</span><br><span class="line">test.stus.insert(&#123;name:“lwne”,age:12,gender:“女”&#125;)</span><br><span class="line">use test;</span><br><span class="line">db.stus.insert(&#123;name:&quot;lwen&quot;,age:12&#125;)</span><br><span class="line">db.stus.insert([</span><br><span class="line">&#123;name:&quot;lwen&quot;,age:12&#125;,</span><br><span class="line">&#123;name:&quot;lwen&quot;,age:12&#125;,</span><br><span class="line">&#123;name:&quot;lwen&quot;,age:12&#125;</span><br><span class="line">])</span><br><span class="line">db.stus.find()</span><br></pre></td></tr></table></figure>

<h4 id="2-查询"><a href="#2-查询" class="headerlink" title="2.查询"></a>2.查询</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.collection.find()</span><br></pre></td></tr></table></figure>

<p>find()用来查询集合中所有符合条件的文档，find()可以接收一个对象作为条件参数<br>{} 空对象或者{ }表示查询集合中所有的文档<br>{属性:值} 查询属性是指定值的文档</p>
<p>find()返回的是一个数组</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.collection.findOne()</span><br></pre></td></tr></table></figure>

<p>用来查询集合中符合条件的第一个文档  ，findOne()返回的是一个文档对象 </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.collection.find(&#123;&#125;).count()</span><br></pre></td></tr></table></figure>

<p>查询所有结果的数量</p>
<p>MongoDB支持直接通过内嵌文档的属性进行查询，如果要查询内嵌文档则可以通过.的形式来匹配<br>如果要通过内嵌文档来对文档进行查询，此时属性名必须使用引号 </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.users.find(&#123;&apos;hobby.movies&apos;:&quot;hero&quot;&#125;);</span><br></pre></td></tr></table></figure>

<p>逻辑条件查询：</p>
<p>19.查询numbers中num大于5000的文档</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.numbers.find(&#123;num:&#123;$gt:500&#125;&#125;); 大于500</span><br><span class="line">db.numbers.find(&#123;num:&#123;$eq:500&#125;&#125;); 等于500</span><br><span class="line">db.numbers.find().limit(10); 查看numbers集合中的前10条数据</span><br><span class="line">db.numbers.find(); 开发时，我们绝对不会执行不带条件的查询</span><br><span class="line">db.numbers.find().skip(10).limit(10);</span><br><span class="line">db.emp.find(&#123;sal:&#123;$lt:2000 , $gt:1000&#125;&#125;); 查询工资在1000-2000之间的员工</span><br><span class="line">db.emp.find(&#123;$or:[&#123;sal:&#123;$lt:1000&#125;&#125; , &#123;sal:&#123;$gt:2500&#125;&#125;]&#125;); 查询工资小于1000或大于2500的员工</span><br></pre></td></tr></table></figure>

<p>skip()用于跳过指定数量的数据  </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">skip((页码-1) * 每页显示的条数).limit(每页显示的条数);  数据分页</span><br></pre></td></tr></table></figure>

<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.stus.find(&#123;_id:&quot;hello&quot;&#125;);</span><br><span class="line">db.stus.find(&#123;age:16 , name:&quot;白骨精&quot;&#125;);</span><br><span class="line">db.stus.find(&#123;age:28&#125;);</span><br><span class="line">db.stus.findOne(&#123;age:28&#125;);</span><br><span class="line">db.stus.find(&#123;&#125;).count();</span><br></pre></td></tr></table></figure>

<h4 id="3-修改"><a href="#3-修改" class="headerlink" title="3.修改"></a>3.修改</h4><p> db.collection.update(查询条件,新对象)</p>
<p>update()默认情况下会使用新对象来替换旧的对象</p>
<p>如果需要修改指定的属性，而不是替换需要使用“修改操作符”来完成修改<br><code>$set</code> 可以用来修改文档中的指定属性<br><code>$unset</code> 可以用来删除文档的指定属性</p>
<p><code>$push</code> 用于向数组中添加一个新的元素<br><code>$addToSet</code> 向数组中添加一个新元素 ， 如果数组中已经存在了该元素，则不会添加</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.users.update(&#123;username:&quot;tangseng&quot;&#125;,&#123;$push:&#123;&quot;hobby.movies&quot;:&quot;Interstellar&quot;&#125;&#125;);</span><br><span class="line">db.users.update(&#123;username:&quot;tangseng&quot;&#125;,&#123;$addToSet:&#123;&quot;hobby.movies&quot;:&quot;Interstellar&quot;&#125;&#125;);</span><br></pre></td></tr></table></figure>

<p>update()默认只会修改一个</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.collection.updateMany()</span><br></pre></td></tr></table></figure>

<p>同时修改多个符合条件的文档</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.collection.updateOne()</span><br></pre></td></tr></table></figure>

<p>修改一个符合条件的文档    </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.collection.replaceOne()</span><br></pre></td></tr></table></figure>

<p>替换一个文档</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">//替换</span><br><span class="line">db.stus.update(&#123;name:&quot;沙和尚&quot;&#125;,&#123;age:28&#125;);</span><br><span class="line"></span><br><span class="line">db.stus.update(</span><br><span class="line">    &#123;&quot;_id&quot; : ObjectId(&quot;59c219689410bc1dbecc0709&quot;)&#125;,</span><br><span class="line">    &#123;$set:&#123;</span><br><span class="line">        gender:&quot;男&quot;,</span><br><span class="line">        address:&quot;流沙河&quot;</span><br><span class="line">    &#125;&#125;    </span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">db.stus.update(</span><br><span class="line">    &#123;&quot;_id&quot; : ObjectId(&quot;59c219689410bc1dbecc0709&quot;)&#125;,</span><br><span class="line">    &#123;$unset:&#123;</span><br><span class="line">        address:1</span><br><span class="line">    &#125;&#125;    </span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">db.stus.updateMany(</span><br><span class="line">    &#123;&quot;name&quot; : &quot;猪八戒&quot;&#125;,</span><br><span class="line">    &#123;</span><br><span class="line">        $set:&#123;</span><br><span class="line">            address:&quot;猪老庄&quot;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;    </span><br><span class="line">);</span><br><span class="line">    </span><br><span class="line">db.stus.update(</span><br><span class="line">    &#123;&quot;name&quot; : &quot;猪八戒&quot;&#125;,</span><br><span class="line">    </span><br><span class="line">    &#123;</span><br><span class="line">        $set:&#123;</span><br><span class="line">        address:&quot;呵呵呵&quot;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;  ,</span><br><span class="line">    &#123;</span><br><span class="line">        multi:true</span><br><span class="line">    &#125;    </span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<h4 id="4-删除"><a href="#4-删除" class="headerlink" title="4.删除"></a>4.删除</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.collection.remove()</span><br></pre></td></tr></table></figure>

<p>删除一个或多个，可以第二个参数传递一个true，则只会删除一个</p>
<p>如果传递一个空对象作为参数，则会删除所有的</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.collection.deleteOne()</span><br><span class="line">db.collection.deleteMany()</span><br><span class="line">db.collection.drop() 删除集合</span><br><span class="line">db.dropDatabase() 删除数据库</span><br></pre></td></tr></table></figure>

<p>一般数据库中的数据都不会删除，所以删除的方法很少调用，一般会在数据中添加一个字段，来表示数据是否被删除</p>
<h3 id="6-排序投影"><a href="#6-排序投影" class="headerlink" title="6.排序投影"></a>6.排序投影</h3><p>查询文档时，默认情况是按照_id的值进行排列（升序），sort()可以用来指定文档的排序的规则,sort()需要传递一个对象来指定排序规则 1表示升序 -1表示降序</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">//limit skip sort 可以以任意的顺序进行调用</span><br><span class="line">db.emp.find(&#123;&#125;).sort(&#123;sal:1,empno:-1&#125;);</span><br></pre></td></tr></table></figure>

<p>在查询时，可以在第二个参数的位置来设置查询结果的 投影</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">db.emp.find(&#123;&#125;,&#123;ename:1 , _id:0 , sal:1&#125;);</span><br></pre></td></tr></table></figure>

<h2 id="4-mongoose"><a href="#4-mongoose" class="headerlink" title="4. mongoose"></a>4. mongoose</h2><h3 id="1-连接数据库"><a href="#1-连接数据库" class="headerlink" title="1. 连接数据库"></a>1. 连接数据库</h3><ol>
<li>下载安装Mongoose</li>
</ol>
<p>​        npm i mongoose –save</p>
<ol start="2">
<li>在项目中引入mongoose</li>
</ol>
<p>​        var mongoose = require(“mongoose”);</p>
<ol start="3">
<li><p>连接MongoDB数据库</p>
<p> mongoose.connect(‘mongodb://数据库的ip地址:端口号/数据库名’, { useMongoClient: true});如果端口号是默认端口号（27017） 则可以省略不写</p>
</li>
<li><p>断开数据库连接(一般不需要调用)</p>
<p>MongoDB数据库，一般情况下，只需要连接一次，连接一次以后，除非项目停止服务器关闭，否则连接一般不会断开mongoose.disconnect()</p>
</li>
<li><p>监听MongoDB数据库的连接状态</p>
<p>在mongoose对象中，有一个属性叫做connection，该对象表示的就是数据库连接.通过监视该对象的状态，可以来监听数据库的连接与断开</p>
<ol>
<li>数据库连接成功的事件</li>
</ol>
</li>
</ol>
<p>​        mongoose.connection.once(“open”,function(){})}</p>
<p>​    2. 数据库断开的事件</p>
<p>​        mongoose.connection.once(“close”,function(){});</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">//引入</span><br><span class="line">var mongoose = require(&quot;mongoose&quot;);</span><br><span class="line">//连接数据库</span><br><span class="line">mongoose.connect(&quot;mongodb://127.0.0.1/mongoose_test&quot; , &#123; useMongoClient: true&#125;);</span><br><span class="line"></span><br><span class="line">mongoose.connection.once(&quot;open&quot;,function()&#123;</span><br><span class="line">	console.log(&quot;数据库连接成功~~~&quot;);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">mongoose.connection.once(&quot;close&quot;,function()&#123;</span><br><span class="line">	console.log(&quot;数据库连接已经断开~~~&quot;);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">//断开数据库连接</span><br><span class="line">mongoose.disconnect();</span><br></pre></td></tr></table></figure>

<h3 id="2-Schema"><a href="#2-Schema" class="headerlink" title="2. Schema"></a>2. Schema</h3><p>这个东西可以看做我们的关系数据库的表的约束。比如哪些字段必须有，而且他的值是什么类型，默认值是什么，这样我们在语言中就不会出现类型转换错误的情况，并且在mongoose中也会帮我们去做转换。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">var mongoose = require(&quot;mongoose&quot;);</span><br><span class="line">mongoose.connect(&quot;mongodb://127.0.0.1/mongoose_test&quot;,&#123;useMongoClient:true&#125;);</span><br><span class="line">mongoose.connection.once(&quot;open&quot;,function () &#123;</span><br><span class="line">	console.log(&quot;数据库连接成功~~~&quot;);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">//将mongoose.Schema 赋值给一个变量</span><br><span class="line">var Schema = mongoose.Schema;</span><br><span class="line"></span><br><span class="line">//创建Schema（模式）对象</span><br><span class="line">var stuSchema = new Schema(&#123;</span><br><span class="line"></span><br><span class="line">	name:String,</span><br><span class="line">	age:Number,</span><br><span class="line">	gender:&#123;</span><br><span class="line">		type:String,</span><br><span class="line">		default:&quot;female&quot;</span><br><span class="line">	&#125;,</span><br><span class="line">	address:String</span><br><span class="line"></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">//通过Schema来创建Model</span><br><span class="line">//Model代表的是数据库中的集合，通过Model才能对数据库进行操作</span><br><span class="line">//mongoose.model(modelName, schema):</span><br><span class="line">//modelName 就是要映射的集合名 mongoose会自动将集合名变成复数</span><br><span class="line">var StuModel = mongoose.model(&quot;student&quot; , stuSchema);</span><br><span class="line"></span><br><span class="line">//向数据库中插入一个文档</span><br><span class="line">//StuModel.create(doc, function(err)&#123;&#125;);</span><br><span class="line">StuModel.create(&#123;</span><br><span class="line">	name:&quot;白骨精&quot;,</span><br><span class="line">	age:16,</span><br><span class="line">	address:&quot;白骨洞&quot;</span><br><span class="line">&#125;,function (err) &#123;</span><br><span class="line">	if(!err)&#123;</span><br><span class="line">		console.log(&quot;插入成功~~~&quot;);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="3-Model"><a href="#3-Model" class="headerlink" title="3. Model"></a>3. Model</h3><p>有了Model，我们就可以来对数据库进行增删改查的操作了</p>
<p>​    Model.create(doc(s), [callback])  用来创建一个或多个文档并添加到数据库中</p>
<p>参数：</p>
<p>​        doc(s) 可以是一个文档对象，也可以是一个文档对象的数组</p>
<p>​        callback 当操作完成以后调用的回调函数</p>
<p>查询的：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Model.find(conditions, [projection], [options], [callback])  查询所有符合条件的文档 总会返回一个数组</span><br><span class="line">Model.findById(id, [projection], [options], [callback])  根据文档的id属性查询文档</span><br><span class="line">Model.findOne([conditions], [projection], [options], [callback])  查询符合条件的第一个文档 总和返回一个具体的文档对象</span><br></pre></td></tr></table></figure>

<p>conditions 查询的条件</p>
<p>projection 投影 需要获取到的字段，两种方式</p>
<ol>
<li>{name:1,_id:0}</li>
<li>“name -_id”</li>
</ol>
<p>options  查询选项（skip limit）</p>
<p>​    skip:3 , limit:1}</p>
<p>callback 回调函数，查询结果会通过回调函数返回</p>
<p>​         回调函数必须传，如果不传回调函数，压根不会查询</p>
<h4 id="使用Model进行增删改查"><a href="#使用Model进行增删改查" class="headerlink" title="使用Model进行增删改查"></a>使用Model进行增删改查</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">StuModel.find(&#123;<span class="attr">name</span>:<span class="string">"唐僧"</span>&#125;,<span class="function"><span class="keyword">function</span> (<span class="params">err , docs</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!err)&#123;</span><br><span class="line">		<span class="built_in">console</span>.log(docs);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;);</span><br><span class="line">StuModel.find(&#123;&#125;,&#123;<span class="attr">name</span>:<span class="number">1</span> , <span class="attr">_id</span>:<span class="number">0</span>&#125;,<span class="function"><span class="keyword">function</span> (<span class="params">err , docs</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!err)&#123;</span><br><span class="line">		<span class="built_in">console</span>.log(docs);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;);</span><br><span class="line">StuModel.find(&#123;&#125;,<span class="string">"name age -_id"</span>, &#123;<span class="attr">skip</span>:<span class="number">3</span> , <span class="attr">limit</span>:<span class="number">1</span>&#125; , <span class="function"><span class="keyword">function</span> (<span class="params">err , docs</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!err)&#123;</span><br><span class="line">		<span class="built_in">console</span>.log(docs);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line">StuModel.findOne(&#123;&#125; , <span class="function"><span class="keyword">function</span> (<span class="params">err , doc</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!err)&#123;</span><br><span class="line">		<span class="built_in">console</span>.log(doc);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;);</span><br><span class="line">StuModel.findById(<span class="string">"59c4c3cf4e5483191467d392"</span> , <span class="function"><span class="keyword">function</span> (<span class="params">err , doc</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!err)&#123;</span><br><span class="line">		<span class="comment">//console.log(doc);</span></span><br><span class="line">		<span class="comment">//通过find()查询的结果，返回的对象，就是Document，文档对象</span></span><br><span class="line">		<span class="comment">//Document对象是Model的实例</span></span><br><span class="line">		<span class="built_in">console</span>.log(doc <span class="keyword">instanceof</span> StuModel);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;);StuModel.create([</span><br><span class="line">	&#123;</span><br><span class="line">		name:<span class="string">"沙和尚"</span>,</span><br><span class="line">		age:<span class="number">38</span>,</span><br><span class="line">		gender:<span class="string">"male"</span>,</span><br><span class="line">		address:<span class="string">"流沙河"</span></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">],<span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!err)&#123;</span><br><span class="line">		<span class="built_in">console</span>.log(<span class="built_in">arguments</span>);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">	修改</span></span><br><span class="line"><span class="comment"> Model.update(conditions, doc, [options], [callback])</span></span><br><span class="line"><span class="comment"> Model.updateMany(conditions, doc, [options], [callback])</span></span><br><span class="line"><span class="comment"> Model.updateOne(conditions, doc, [options], [callback])</span></span><br><span class="line"><span class="comment"> 	- 用来修改一个或多个文档</span></span><br><span class="line"><span class="comment"> 	- 参数：</span></span><br><span class="line"><span class="comment"> 		conditions 查询条件</span></span><br><span class="line"><span class="comment"> 		doc 修改后的对象</span></span><br><span class="line"><span class="comment"> 		options 配置参数</span></span><br><span class="line"><span class="comment"> 		callback 回调函数</span></span><br><span class="line"><span class="comment"> Model.replaceOne(conditions, doc, [options], [callback])</span></span><br><span class="line"><span class="comment">* */</span></span><br><span class="line"></span><br><span class="line">StuModel.updateOne(&#123;<span class="attr">name</span>:<span class="string">"唐僧"</span>&#125;,&#123;<span class="attr">$set</span>:&#123;<span class="attr">age</span>:<span class="number">20</span>&#125;&#125;,<span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!err)&#123;</span><br><span class="line">		<span class="built_in">console</span>.log(<span class="string">"修改成功"</span>);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">删除：</span></span><br><span class="line"><span class="comment"> Model.remove(conditions, [callback])</span></span><br><span class="line"><span class="comment"> Model.deleteOne(conditions, [callback])</span></span><br><span class="line"><span class="comment"> Model.deleteMany(conditions, [callback])</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">StuModel.remove(&#123;<span class="attr">name</span>:<span class="string">"白骨精"</span>&#125;,<span class="function"><span class="keyword">function</span> (<span class="params">err</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!err)&#123;</span><br><span class="line">		<span class="built_in">console</span>.log(<span class="string">"删除成功~~"</span>);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"> Model.count(conditions, [callback])</span><br><span class="line"> 	- 统计文档的数量的</span><br><span class="line"></span><br><span class="line">StuModel.count(&#123;&#125;,<span class="function"><span class="keyword">function</span> (<span class="params">err , count</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!err)&#123;</span><br><span class="line">		<span class="built_in">console</span>.log(count);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<h3 id="4-document"><a href="#4-document" class="headerlink" title="4.document"></a>4.document</h3><p>​    Document 和 集合中的文档一一对应 ， Document是Model的实例，通过Model查询到结果都是Document</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> mongoose = <span class="built_in">require</span>(<span class="string">"mongoose"</span>);</span><br><span class="line">mongoose.connect(<span class="string">"mongodb://127.0.0.1/mongoose_test"</span>,&#123;<span class="attr">useMongoClient</span>:<span class="literal">true</span>&#125;);</span><br><span class="line">mongoose.connection.once(<span class="string">"open"</span>,<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">	<span class="built_in">console</span>.log(<span class="string">"数据库连接成功~~~"</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> Schema = mongoose.Schema;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> stuSchema = <span class="keyword">new</span> Schema(&#123;</span><br><span class="line"></span><br><span class="line">	name:<span class="built_in">String</span>,</span><br><span class="line">	age:<span class="built_in">Number</span>,</span><br><span class="line">	gender:&#123;</span><br><span class="line">		type:<span class="built_in">String</span>,</span><br><span class="line">		<span class="keyword">default</span>:<span class="string">"female"</span></span><br><span class="line">	&#125;,</span><br><span class="line">	address:<span class="built_in">String</span></span><br><span class="line"></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> StuModel = mongoose.model(<span class="string">"student"</span> , stuSchema);</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">	Document 和 集合中的文档一一对应 ， Document是Model的实例</span></span><br><span class="line"><span class="comment">		通过Model查询到结果都是Document</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//创建一个Document</span></span><br><span class="line"><span class="keyword">var</span> stu = <span class="keyword">new</span> StuModel(&#123;</span><br><span class="line">	name:<span class="string">"奔波霸"</span>,</span><br><span class="line">	age:<span class="number">48</span>,</span><br><span class="line">	gender:<span class="string">"male"</span>,</span><br><span class="line">	address:<span class="string">"碧波潭"</span></span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">	document的方法</span></span><br><span class="line"><span class="comment"> 		Model#save([options], [fn])</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">stu.save(function (err) &#123;</span></span><br><span class="line"><span class="comment">	if(!err)&#123;</span></span><br><span class="line"><span class="comment">		console.log("保存成功~~~");</span></span><br><span class="line"><span class="comment">	&#125;</span></span><br><span class="line"><span class="comment">&#125;);*/</span></span><br><span class="line"></span><br><span class="line">StuModel.findOne(&#123;&#125;,<span class="function"><span class="keyword">function</span> (<span class="params">err , doc</span>) </span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(!err)&#123;</span><br><span class="line">		<span class="comment">/*</span></span><br><span class="line"><span class="comment">		 	update(update,[options],[callback])</span></span><br><span class="line"><span class="comment">		 		- 修改对象</span></span><br><span class="line"><span class="comment">		 	remove([callback])</span></span><br><span class="line"><span class="comment">		 		- 删除对象</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">		 */</span></span><br><span class="line">		<span class="comment">//console.log(doc);</span></span><br><span class="line">		<span class="comment">/*doc.update(&#123;$set:&#123;age:28&#125;&#125;,function (err) &#123;</span></span><br><span class="line"><span class="comment">			if(!err)&#123;</span></span><br><span class="line"><span class="comment">				console.log("修改成功~~~");</span></span><br><span class="line"><span class="comment">			&#125;</span></span><br><span class="line"><span class="comment">		&#125;);*/</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">/*doc.age = 18;</span></span><br><span class="line"><span class="comment">		doc.save();*/</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">/*doc.remove(function (err) &#123;</span></span><br><span class="line"><span class="comment">			if(!err)&#123;</span></span><br><span class="line"><span class="comment">				console.log("大师兄再见~~~");</span></span><br><span class="line"><span class="comment">			&#125;</span></span><br><span class="line"><span class="comment">		&#125;);*/</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">		<span class="comment">/*</span></span><br><span class="line"><span class="comment">			get(name)</span></span><br><span class="line"><span class="comment">				- 获取文档中的指定属性值</span></span><br><span class="line"><span class="comment">			set(name , value)</span></span><br><span class="line"><span class="comment">				- 设置文档的指定的属性值</span></span><br><span class="line"><span class="comment">			id</span></span><br><span class="line"><span class="comment">				- 获取文档的_id属性值</span></span><br><span class="line"><span class="comment">			 toJSON() ******</span></span><br><span class="line"><span class="comment">			 	- 转换为一个JSON对象</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">			 toObject()</span></span><br><span class="line"><span class="comment">			 	- 将Document对象转换为一个普通的JS对象</span></span><br><span class="line"><span class="comment">			 		转换为普通的js对象以后，注意所有的Document对象的方法或属性都不能使用了</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">		 */</span></span><br><span class="line">		<span class="comment">//console.log(doc.get("age"));</span></span><br><span class="line">		<span class="comment">//console.log(doc.age);</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">//doc.set("name","猪小小");</span></span><br><span class="line">		<span class="comment">//doc.name = "hahaha";</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">//console.log(doc._id);</span></span><br><span class="line">		<span class="comment">//var j = doc.toJSON();</span></span><br><span class="line">		<span class="comment">//console.log(j);</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">//var o = doc.toObject();</span></span><br><span class="line"></span><br><span class="line">		<span class="comment">//console.log(o);</span></span><br><span class="line"></span><br><span class="line">		doc = doc.toObject();</span><br><span class="line"></span><br><span class="line">		<span class="keyword">delete</span> doc.address;</span><br><span class="line"></span><br><span class="line">		<span class="built_in">console</span>.log(doc._id);</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>DB MangoDB</tag>
      </tags>
  </entry>
  <entry>
    <title>块级元素居中方案</title>
    <url>/2019/01/10/FrontEnd/%E5%9D%97%E7%BA%A7%E5%85%83%E7%B4%A0%E5%B1%85%E4%B8%AD%E6%96%B9%E6%A1%88/</url>
    <content><![CDATA[<h2 id="1-方案一"><a href="#1-方案一" class="headerlink" title="1. 方案一"></a>1. 方案一</h2><p>html:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div class=&quot;main&quot;&gt;&lt;/main&gt;</span><br></pre></td></tr></table></figure>

<p>css:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.main&#123;</span><br><span class="line">  width:400px;</span><br><span class="line">  height:200px;</span><br><span class="line">  background:#eee;</span><br><span class="line">  position:absolute;</span><br><span class="line">  left:50%;</span><br><span class="line">  top:50%;</span><br><span class="line">  margin-left:-200px;</span><br><span class="line">  margin-top:-100px;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>利用定位，移动宽高的一半，然后margin负值回去一半的一半。</p>
<h2 id="2-方案二"><a href="#2-方案二" class="headerlink" title="2.方案二"></a>2.方案二</h2><p>html:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div class=&quot;main&quot;&gt;&lt;/div&gt;</span><br></pre></td></tr></table></figure>

<p>css:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">body &#123;</span><br><span class="line">    height: 100%;</span><br><span class="line">&#125;</span><br><span class="line">.main&#123;</span><br><span class="line"> width: 400px;</span><br><span class="line">   height: 200px; </span><br><span class="line">  background:#eee;</span><br><span class="line">  position:absolute;</span><br><span class="line">  top: 0;</span><br><span class="line">  left: 0;</span><br><span class="line">  bottom: 0;</span><br><span class="line">  right: 0;</span><br><span class="line">  margin: auto;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最常用，直接一个布局搞定，在水平方向垂直方向都可以自定义的居中。</p>
<h2 id="3-方案三"><a href="#3-方案三" class="headerlink" title="3.方案三"></a>3.方案三</h2><p>html:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;div class=&quot;main&quot;&gt;</span><br><span class="line">    hello world</span><br><span class="line">&lt;/div&gt;</span><br></pre></td></tr></table></figure>

<p>css:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.main&#123;</span><br><span class="line">  background:#eee;</span><br><span class="line">  position:absolute;</span><br><span class="line">  left:50%;</span><br><span class="line">  top:50%;</span><br><span class="line">  transform: translate(-50%,-50%);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个其实和第一个非常类似，他们是做了一个位移，位移的方位分别是 x 和 y 两个轴上。移动自身的 50% 注意这个地方位移不是指外部容器的 50% 而是自身。</p>
<p><a href="https://stackoverflow.com/questions/46184458/transform-translate-50-50" target="_blank" rel="noopener">Stack Overflow中的解释</a></p>
<h2 id="4-方案四"><a href="#4-方案四" class="headerlink" title="4.方案四"></a>4.方案四</h2><p>html:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;body&gt;</span><br><span class="line">    &lt;div class=&quot;main&quot;&gt;</span><br><span class="line">    &lt;/div&gt;</span><br><span class="line">&lt;/body&gt;</span><br></pre></td></tr></table></figure>

<p>css：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">html,body&#123;</span><br><span class="line">    width: 100%;</span><br><span class="line">    height: 100%;</span><br><span class="line">    display: -webkit-flex;</span><br><span class="line">    display: flex;</span><br><span class="line">    justify-content:center;</span><br><span class="line">    align-items:center;</span><br><span class="line">&#125;</span><br><span class="line">.container&#123;</span><br><span class="line">    width: 400px;</span><br><span class="line">    height: 200px;</span><br><span class="line">    background: #ccc;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>Webpack 入门</title>
    <url>/2019/01/10/FrontEnd/webpack%20%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<p>从 webpack v4.0.0 开始，可以不用引入一个配置文件。然而，webpack 仍然还是<a href="https://www.webpackjs.com/configuration" target="_blank" rel="noopener">高度可配置的</a>。在开始前你需要先理解四个<strong>核心概念</strong>：</p>
<ul>
<li>入口(entry)</li>
<li>输出(output)</li>
<li>loader</li>
<li>插件(plugins)</li>
</ul>
<h2 id="1-入口-entry"><a href="#1-入口-entry" class="headerlink" title="1.入口(entry)"></a>1.入口(entry)</h2><p><strong>入口起点(entry point)</strong>指示 webpack 应该使用哪个模块，来作为构建其内部<em>依赖图</em>的开始。进入入口起点后，webpack 会找出有哪些模块和库是入口起点（直接和间接）依赖的。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">  entry: <span class="string">'./path/to/my/entry/file.js'</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="2-出口-output"><a href="#2-出口-output" class="headerlink" title="2.出口(output)"></a>2.出口(output)</h2><p><strong>output</strong> 属性告诉 webpack 在哪里输出它所创建的 <em>bundles</em>，以及如何命名这些文件，默认值为 <code>./dist</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">'path'</span>);</span><br><span class="line"></span><br><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">  entry: <span class="string">'./path/to/my/entry/file.js'</span>,</span><br><span class="line">  output: &#123;</span><br><span class="line">    path: path.resolve(__dirname, <span class="string">'dist'</span>),</span><br><span class="line">    filename: <span class="string">'my-first-webpack.bundle.js'</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h2 id="3-loader"><a href="#3-loader" class="headerlink" title="3.loader"></a>3.loader</h2><p><em>loader</em> 让 webpack 能够去处理那些非 JavaScript 文件（webpack 自身只理解 JavaScript 和 Json）。loader 可以将所有类型的文件转换为 webpack 能够处理的有效<a href="https://www.webpackjs.com/concepts/modules" target="_blank" rel="noopener">模块</a>，然后你就可以利用 webpack 的打包能力，对它们进行处理。</p>
<p>在更高层面，在 webpack 的配置中 <strong>loader</strong> 有两个目标：</p>
<ol>
<li><code>test</code> 属性，用于标识出应该被对应的 loader 进行转换的某个或某些文件。</li>
<li><code>use</code> 属性，表示进行转换时，应该使用哪个 loader。</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">'path'</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> config = &#123;</span><br><span class="line">  output: &#123;</span><br><span class="line">    filename: <span class="string">'my-first-webpack.bundle.js'</span></span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="built_in">module</span>: &#123;</span><br><span class="line">    rules: [</span><br><span class="line">      &#123; <span class="attr">test</span>: <span class="regexp">/\.txt$/</span>, <span class="attr">use</span>: <span class="string">'raw-loader'</span> &#125;</span><br><span class="line">    ]</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">module</span>.exports = config;</span><br></pre></td></tr></table></figure>

<h2 id="4-插件-plugins"><a href="#4-插件-plugins" class="headerlink" title="4.插件(plugins)"></a>4.插件(plugins)</h2><p>​    loader 被用于转换某些类型的模块，而插件则可以用于执行范围更广的任务。插件的范围包括，从打包优化和压缩，一直到重新定义环境中的变量。<a href="https://www.webpackjs.com/api/plugins" target="_blank" rel="noopener">插件接口</a>功能极其强大，可以用来处理各种各样的任务。</p>
<p>​    想要使用一个插件，你只需要 <code>require()</code> 它，然后把它添加到 <code>plugins</code> 数组中。多数插件可以通过选项(option)自定义。你也可以在一个配置文件中因为不同目的而多次使用同一个插件，这时需要通过使用 <code>new</code> 操作符来创建它的一个实例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> HtmlWebpackPlugin = <span class="built_in">require</span>(<span class="string">'html-webpack-plugin'</span>); <span class="comment">// 通过 npm 安装</span></span><br><span class="line"><span class="keyword">const</span> webpack = <span class="built_in">require</span>(<span class="string">'webpack'</span>); <span class="comment">// 用于访问内置插件</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> config = &#123;</span><br><span class="line">  <span class="built_in">module</span>: &#123;</span><br><span class="line">    rules: [</span><br><span class="line">      &#123; <span class="attr">test</span>: <span class="regexp">/\.txt$/</span>, <span class="attr">use</span>: <span class="string">'raw-loader'</span> &#125;</span><br><span class="line">    ]</span><br><span class="line">  &#125;,</span><br><span class="line">  plugins: [</span><br><span class="line">    <span class="keyword">new</span> HtmlWebpackPlugin(&#123;<span class="attr">template</span>: <span class="string">'./src/index.html'</span>&#125;)</span><br><span class="line">  ]</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="built_in">module</span>.exports = config;</span><br></pre></td></tr></table></figure>

<p>webpack 提供许多开箱可用的插件！查阅我们的<a href="https://www.webpackjs.com/plugins" target="_blank" rel="noopener">插件列表</a>获取更多信息。</p>
<h2 id="5-模式"><a href="#5-模式" class="headerlink" title="5.模式"></a>5.模式</h2><p>通过选择 <code>development</code> 或 <code>production</code> 之中的一个，来设置 <code>mode</code> 参数，你可以启用相应模式下的 webpack 内置的优化</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">module</span>.exports = &#123;</span><br><span class="line">  mode: <span class="string">'production'</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>Webpack</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 模块化</title>
    <url>/2019/01/10/FrontEnd/JavaScript%20%E6%A8%A1%E5%9D%97%E5%8C%96/</url>
    <content><![CDATA[<h2 id="1-模块化过程"><a href="#1-模块化过程" class="headerlink" title="1. 模块化过程"></a>1. 模块化过程</h2><h3 id="1-全局函数"><a href="#1-全局函数" class="headerlink" title="1. 全局函数"></a>1. 全局函数</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 全局函数模式: 将不同的功能封装成不同的全局函数</span></span><br><span class="line"><span class="comment"> * 问题: Global被污染了, 很容易引起命名冲突</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">//数据</span></span><br><span class="line"><span class="keyword">let</span> data = <span class="string">'atguigu.com'</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'foo()'</span>)</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'bar()'</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-对象空间"><a href="#2-对象空间" class="headerlink" title="2. 对象空间"></a>2. 对象空间</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * namespace模式: 简单对象封装</span></span><br><span class="line"><span class="comment"> * 作用: 减少了全局变量</span></span><br><span class="line"><span class="comment"> * 问题: 不安全(数据不是私有的, 外部可以直接修改)</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">let</span> myModule = &#123;</span><br><span class="line">  data: <span class="string">'atguigu.com'</span>,</span><br><span class="line">  foo() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`foo() <span class="subst">$&#123;<span class="keyword">this</span>.data&#125;</span>`</span>)</span><br><span class="line">  &#125;,</span><br><span class="line">  bar() &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`bar() <span class="subst">$&#123;<span class="keyword">this</span>.data&#125;</span>`</span>)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-立即执行函数"><a href="#3-立即执行函数" class="headerlink" title="3. 立即执行函数"></a>3. 立即执行函数</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * IIFE模式: 匿名函数自调用(闭包)</span></span><br><span class="line"><span class="comment"> * IIFE : immediately-invoked function expression(立即调用函数表达式)</span></span><br><span class="line"><span class="comment"> * 作用: 数据是私有的, 外部只能通过暴露的方法操作</span></span><br><span class="line"><span class="comment"> * 问题: 如果当前这个模块依赖另一个模块怎么办?</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">window</span>) </span>&#123;</span><br><span class="line">  <span class="comment">//数据</span></span><br><span class="line">  <span class="keyword">let</span> data = <span class="string">'atguigu.com'</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">//操作数据的函数</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123; <span class="comment">//用于暴露有函数</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`foo() <span class="subst">$&#123;data&#125;</span>`</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;<span class="comment">//用于暴露有函数</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`bar() <span class="subst">$&#123;data&#125;</span>`</span>)</span><br><span class="line">    otherFun() <span class="comment">//内部调用</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">otherFun</span>(<span class="params"></span>) </span>&#123; <span class="comment">//内部私有的函数</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'otherFun()'</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">//暴露行为</span></span><br><span class="line">  <span class="built_in">window</span>.myModule = &#123;foo, bar&#125;</span><br><span class="line">&#125;)(<span class="built_in">window</span>)</span><br></pre></td></tr></table></figure>

<h3 id="4-依赖注入的自执行函数"><a href="#4-依赖注入的自执行函数" class="headerlink" title="4.依赖注入的自执行函数"></a>4.依赖注入的自执行函数</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * IIFE模式增强 : 引入依赖</span></span><br><span class="line"><span class="comment"> * 这就是现代模块实现的基石</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params">window, $</span>) </span>&#123;</span><br><span class="line">  <span class="comment">//数据</span></span><br><span class="line">  <span class="keyword">let</span> data = <span class="string">'atguigu.com'</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">//操作数据的函数</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>) </span>&#123; <span class="comment">//用于暴露有函数</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`foo() <span class="subst">$&#123;data&#125;</span>`</span>)</span><br><span class="line">    $(<span class="string">'body'</span>).css(<span class="string">'background'</span>, <span class="string">'red'</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">bar</span>(<span class="params"></span>) </span>&#123;<span class="comment">//用于暴露有函数</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">`bar() <span class="subst">$&#123;data&#125;</span>`</span>)</span><br><span class="line">    otherFun() <span class="comment">//内部调用</span></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">otherFun</span>(<span class="params"></span>) </span>&#123; <span class="comment">//内部私有的函数</span></span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'otherFun()'</span>)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">//暴露行为</span></span><br><span class="line">  <span class="built_in">window</span>.myModule = &#123;foo, bar&#125;</span><br><span class="line">&#125;)(<span class="built_in">window</span>, jQuery)</span><br></pre></td></tr></table></figure>

<h2 id="2-CommonJS"><a href="#2-CommonJS" class="headerlink" title="2.CommonJS"></a>2.CommonJS</h2><p>​    commonJs 是同步进行依赖引入的，并且他是相当于一个文件一个模块，对于模块的导出采用了 <code>module.exports={}</code> 或者使用 <code>exports={}</code> 来完成的，而模块的引入则是通过 <code>let mo=require(&quot;./module.js&quot;)</code> 但是注意 nodejs 采用的就是 CommonJs 所以他是直接可用这个语句的，但是浏览器不行，所以浏览器需要使用 bowserify 工具打包编译才能让浏览器识别。</p>
<h3 id="1-在服务端应用"><a href="#1-在服务端应用" class="headerlink" title="1.在服务端应用"></a>1.在服务端应用</h3><h4 id="1-下载安装node-js"><a href="#1-下载安装node-js" class="headerlink" title="1.下载安装node.js"></a>1.下载安装node.js</h4><h4 id="2-创建项目结构"><a href="#2-创建项目结构" class="headerlink" title="2.创建项目结构"></a>2.创建项目结构</h4>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">|-modules</span><br><span class="line">  |-module1.js</span><br><span class="line">  |-module2.js</span><br><span class="line">  |-module3.js</span><br><span class="line">|-app.js</span><br><span class="line">|-package.json</span><br><span class="line">  &#123;</span><br><span class="line">    &quot;name&quot;: &quot;commonJS-node&quot;,</span><br><span class="line">    &quot;version&quot;: &quot;1.0.0&quot;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-下载第三方模块"><a href="#3-下载第三方模块" class="headerlink" title="3.下载第三方模块"></a>3.下载第三方模块</h4><ul>
<li><p>npm install uniq –save</p>
<h4 id="4-模块化编码"><a href="#4-模块化编码" class="headerlink" title="4.模块化编码"></a>4.模块化编码</h4></li>
<li><p>module1.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">module.exports = &#123;</span><br><span class="line">  foo() &#123;</span><br><span class="line">    console.log(&apos;moudle1 foo()&apos;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>module2.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">module.exports = function () &#123;</span><br><span class="line">  console.log(&apos;module2()&apos;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>module3.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">exports.foo = function () &#123;</span><br><span class="line">  console.log(&apos;module3 foo()&apos;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">exports.bar = function () &#123;</span><br><span class="line">  console.log(&apos;module3 bar()&apos;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>app.js </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/**</span><br><span class="line">  1. 定义暴露模块:</span><br><span class="line">    module.exports = value;</span><br><span class="line">    exports.xxx = value;</span><br><span class="line">  2. 引入模块:</span><br><span class="line">    var module = require(模块名或模块路径);</span><br><span class="line"> */</span><br><span class="line">&quot;use strict&quot;;</span><br><span class="line">//引用模块</span><br><span class="line">let module1 = require(&apos;./modules/module1&apos;)</span><br><span class="line">let module2 = require(&apos;./modules/module2&apos;)</span><br><span class="line">let module3 = require(&apos;./modules/module3&apos;)</span><br><span class="line"></span><br><span class="line">let uniq = require(&apos;uniq&apos;)</span><br><span class="line">let fs = require(&apos;fs&apos;)</span><br><span class="line"></span><br><span class="line">//使用模块</span><br><span class="line">module1.foo()</span><br><span class="line">module2()</span><br><span class="line">module3.foo()</span><br><span class="line">module3.bar()</span><br><span class="line"></span><br><span class="line">console.log(uniq([1, 3, 1, 4, 3]))</span><br><span class="line"></span><br><span class="line">fs.readFile(&apos;app.js&apos;, function (error, data) &#123;</span><br><span class="line">  console.log(data.toString())</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="5-通过node运行app-js"><a href="#5-通过node运行app-js" class="headerlink" title="5.通过node运行app.js"></a>5.通过node运行app.js</h4><p>命令: node app.js</p>
<h3 id="2-在浏览器的运行"><a href="#2-在浏览器的运行" class="headerlink" title="2.在浏览器的运行"></a>2.在浏览器的运行</h3><h4 id="1-创建项目结构"><a href="#1-创建项目结构" class="headerlink" title="1.创建项目结构"></a>1.创建项目结构</h4>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">|-js</span><br><span class="line">  |-dist //打包生成文件的目录</span><br><span class="line">  |-src //源码所在的目录</span><br><span class="line">    |-module1.js</span><br><span class="line">    |-module2.js</span><br><span class="line">    |-module3.js</span><br><span class="line">    |-app.js //应用主源文件</span><br><span class="line">|-index.html</span><br><span class="line">|-package.json</span><br><span class="line">  &#123;</span><br><span class="line">    &quot;name&quot;: &quot;browserify-test&quot;,</span><br><span class="line">    &quot;version&quot;: &quot;1.0.0&quot;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-下载browserify"><a href="#2-下载browserify" class="headerlink" title="2.下载browserify"></a>2.下载browserify</h4><ul>
<li><p>全局: npm install browserify -g</p>
</li>
<li><p>局部: npm install browserify –save-dev</p>
<ol start="3">
<li>定义模块代码</li>
</ol>
</li>
<li><p>module1.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">module.exports = &#123;</span><br><span class="line">  foo() &#123;</span><br><span class="line">    console.log(&apos;moudle1 foo()&apos;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>module2.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">module.exports = function () &#123;</span><br><span class="line">  console.log(&apos;module2()&apos;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>module3.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">exports.foo = function () &#123;</span><br><span class="line">  console.log(&apos;module3 foo()&apos;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">exports.bar = function () &#123;</span><br><span class="line">  console.log(&apos;module3 bar()&apos;)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>app.js (应用的主js)</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">//引用模块</span><br><span class="line">let module1 = require(&apos;./module1&apos;)</span><br><span class="line">let module2 = require(&apos;./module2&apos;)</span><br><span class="line">let module3 = require(&apos;./module3&apos;)</span><br><span class="line"></span><br><span class="line">let uniq = require(&apos;uniq&apos;)</span><br><span class="line"></span><br><span class="line">//使用模块</span><br><span class="line">module1.foo()</span><br><span class="line">module2()</span><br><span class="line">module3.foo()</span><br><span class="line">module3.bar()</span><br><span class="line"></span><br><span class="line">console.log(uniq([1, 3, 1, 4, 3]))</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="3-打包处理js"><a href="#3-打包处理js" class="headerlink" title="3.打包处理js:"></a>3.打包处理js:</h4><ul>
<li>browserify js/src/app.js -o js/dist/bundle.js</li>
</ul>
<h4 id="4-页面使用引入"><a href="#4-页面使用引入" class="headerlink" title="4.页面使用引入:"></a>4.页面使用引入:</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;script type=&quot;text/javascript&quot; src=&quot;js/dist/bundle.js&quot;&gt;&lt;/script&gt;</span><br></pre></td></tr></table></figure>

<h2 id="3-AMD"><a href="#3-AMD" class="headerlink" title="3. AMD"></a>3. AMD</h2><p>异步模块定义。<strong>显示声明依赖注入</strong> </p>
<ol>
<li>定义模块采用 define</li>
<li>导出模块在回调函数中 return</li>
<li>引入模块使用 require</li>
</ol>
<h3 id="1-下载require-js-并引入"><a href="#1-下载require-js-并引入" class="headerlink" title="1.下载require.js, 并引入"></a>1.下载require.js, 并引入</h3><ul>
<li><p>官网: <a href="http://www.requirejs.cn/" target="_blank" rel="noopener">http://www.requirejs.cn/</a></p>
</li>
<li><p>github : <a href="https://github.com/requirejs/requirejs" target="_blank" rel="noopener">https://github.com/requirejs/requirejs</a></p>
</li>
<li><p>将require.js导入项目: js/libs/require.js </p>
<h3 id="2-创建项目结构-1"><a href="#2-创建项目结构-1" class="headerlink" title="2.创建项目结构"></a>2.创建项目结构</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">|-js</span><br><span class="line">  |-libs</span><br><span class="line">    |-require.js</span><br><span class="line">  |-modules</span><br><span class="line">    |-alerter.js</span><br><span class="line">    |-dataService.js</span><br><span class="line">  |-main.js</span><br><span class="line">|-index.html</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="3-定义require-js的模块代码"><a href="#3-定义require-js的模块代码" class="headerlink" title="3.定义require.js的模块代码"></a>3.定义require.js的模块代码</h3><ul>
<li><p>dataService.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">define(function () &#123;</span><br><span class="line">  let msg = &apos;test&apos;</span><br><span class="line"></span><br><span class="line">  function getMsg() &#123;</span><br><span class="line">    return msg.toUpperCase()</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  return &#123;getMsg&#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
</li>
<li><p>alerter.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">define([&apos;dataService&apos;, &apos;jquery&apos;], function (dataService, $) &#123;</span><br><span class="line">  let name = &apos;Tom2&apos;</span><br><span class="line"></span><br><span class="line">  function showMsg() &#123;</span><br><span class="line">    $(&apos;body&apos;).css(&apos;background&apos;, &apos;gray&apos;)</span><br><span class="line">    alert(dataService.getMsg() + &apos;, &apos; + name)</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  return &#123;showMsg&#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="4-应用主-入口-js-main-js"><a href="#4-应用主-入口-js-main-js" class="headerlink" title="4.应用主(入口)js: main.js"></a>4.应用主(入口)js: main.js</h3>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(function () &#123;</span><br><span class="line">  //配置</span><br><span class="line">  require.config(&#123;</span><br><span class="line">    //基本路径</span><br><span class="line">    baseUrl: &quot;js/&quot;,</span><br><span class="line">    //模块标识名与模块路径映射</span><br><span class="line">    paths: &#123;</span><br><span class="line">      &quot;alerter&quot;: &quot;modules/alerter&quot;,</span><br><span class="line">      &quot;dataService&quot;: &quot;modules/dataService&quot;,</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">  //引入使用模块</span><br><span class="line">  require( [&apos;alerter&apos;], function(alerter) &#123;</span><br><span class="line">    alerter.showMsg()</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)()</span><br></pre></td></tr></table></figure>

<h3 id="6-页面使用模块"><a href="#6-页面使用模块" class="headerlink" title="6.页面使用模块:"></a>6.页面使用模块:</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;script data-main=&quot;js/main&quot; src=&quot;js/libs/require.js&quot;&gt;&lt;/script&gt;</span><br></pre></td></tr></table></figure>

<h3 id="7-使用第三方基于require-js"><a href="#7-使用第三方基于require-js" class="headerlink" title="7.使用第三方基于require.js"></a>7.使用第三方基于require.js</h3><h4 id="1-将jquery的库文件导入到项目"><a href="#1-将jquery的库文件导入到项目" class="headerlink" title="1.将jquery的库文件导入到项目:"></a>1.将jquery的库文件导入到项目:</h4><p>js/libs/jquery-1.10.1.js</p>
<h4 id="2-在main-js中配置jquery路径"><a href="#2-在main-js中配置jquery路径" class="headerlink" title="2.在main.js中配置jquery路径"></a>2.在main.js中配置jquery路径</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">paths: &#123;</span><br><span class="line">          &apos;jquery&apos;: &apos;libs/jquery-1.10.1&apos;</span><br><span class="line">      &#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-在alerter-js中使用jquery"><a href="#3-在alerter-js中使用jquery" class="headerlink" title="3.在alerter.js中使用jquery"></a>3.在alerter.js中使用jquery</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">define([&apos;dataService&apos;, &apos;jquery&apos;], function (dataService, $) &#123;</span><br><span class="line">    var name = &apos;xfzhang&apos;</span><br><span class="line">    function showMsg() &#123;</span><br><span class="line">        $(&apos;body&apos;).css(&#123;background : &apos;red&apos;&#125;)</span><br><span class="line">        alert(name + &apos; &apos;+dataService.getMsg())</span><br><span class="line">    &#125;</span><br><span class="line">    return &#123;showMsg&#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>

<h3 id="8-第三方不基于require-js的框架"><a href="#8-第三方不基于require-js的框架" class="headerlink" title="8.第三方不基于require.js的框架"></a>8.第三方不基于require.js的框架</h3><h4 id="1-将angular-js和angular-messages-js导入项目"><a href="#1-将angular-js和angular-messages-js导入项目" class="headerlink" title="1.将angular.js和angular-messages.js导入项目"></a>1.将angular.js和angular-messages.js导入项目</h4><ul>
<li>js/libs/angular.js</li>
<li>js/libs/angular-messages.js</li>
</ul>
<h4 id="2-在main-js中配置"><a href="#2-在main-js中配置" class="headerlink" title="2.在main.js中配置"></a>2.在main.js中配置</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(function () &#123;</span><br><span class="line">  require.config(&#123;</span><br><span class="line">    //基本路径</span><br><span class="line">    baseUrl: &quot;js/&quot;,</span><br><span class="line">    //模块标识名与模块路径映射</span><br><span class="line">    paths: &#123;</span><br><span class="line">      //第三方库</span><br><span class="line">      &apos;jquery&apos; : &apos;libs/jquery-1.10.1&apos;,</span><br><span class="line">      &apos;angular&apos; : &apos;libs/angular&apos;,</span><br><span class="line">      &apos;angular-messages&apos; : &apos;libs/angular-messages&apos;,</span><br><span class="line">      //自定义模块</span><br><span class="line">      &quot;alerter&quot;: &quot;modules/alerter&quot;,</span><br><span class="line">      &quot;dataService&quot;: &quot;modules/dataService&quot;</span><br><span class="line">    &#125;,</span><br><span class="line">    /*</span><br><span class="line">     配置不兼容AMD的模块</span><br><span class="line">     exports : 指定导出的模块名</span><br><span class="line">     deps  : 指定所有依赖的模块的数组</span><br><span class="line">     */</span><br><span class="line">    shim: &#123;</span><br><span class="line">      &apos;angular&apos; : &#123;</span><br><span class="line">        exports : &apos;angular&apos;</span><br><span class="line">      &#125;,</span><br><span class="line">      &apos;angular-messages&apos; : &#123;</span><br><span class="line">        exports : &apos;angular-messages&apos;,</span><br><span class="line">        deps : [&apos;angular&apos;]</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;)</span><br><span class="line">  //引入使用模块</span><br><span class="line">  require( [&apos;alerter&apos;, &apos;angular&apos;, &apos;angular-messages&apos;], function(alerter, angular) &#123;</span><br><span class="line">    alerter.showMsg()</span><br><span class="line">    angular.module(&apos;myApp&apos;, [&apos;ngMessages&apos;])</span><br><span class="line">    angular.bootstrap(document,[&quot;myApp&quot;])</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;)()</span><br></pre></td></tr></table></figure>

<h4 id="3-页面"><a href="#3-页面" class="headerlink" title="3.页面"></a>3.页面</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;form name=&quot;myForm&quot;&gt;</span><br><span class="line">  用户名: &lt;input type=&quot;text&quot; name=&quot;username&quot; ng-model=&quot;username&quot; ng-required=&quot;true&quot;&gt;</span><br><span class="line">  &lt;div style=&quot;color: red;&quot; ng-show=&quot;myForm.username.$dirty&amp;&amp;myForm.username.$invalid&quot;&gt;用户名是必须的&lt;/div&gt;</span><br><span class="line">&lt;/form&gt;</span><br></pre></td></tr></table></figure>

<h2 id="4-ES6"><a href="#4-ES6" class="headerlink" title="4. ES6"></a>4. ES6</h2><p>​    es6这种方式就是定义模块直接向我们定义一个对象或者函数一样，而需要导出就是采用 export 导出函数或者对象或变量。在一个模块中可以多次 export 但是在引入的时候必须采用对象解构的方式导入我们 export 的内容。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">//------------------module.js</span></span><br><span class="line"><span class="keyword">export</span> foo=<span class="function"><span class="params">()</span>=&gt;</span>&#123;&#125;</span><br><span class="line"><span class="keyword">export</span> boo=<span class="function"><span class="params">()</span>=&gt;</span>&#123;&#125;</span><br><span class="line"><span class="comment">//------------------main.js</span></span><br><span class="line"><span class="keyword">import</span> &#123;foo,boo&#125; <span class="keyword">from</span> <span class="string">'./module.js'</span></span><br></pre></td></tr></table></figure>

<p>​    如果我们使用默认导出则是可以直接用一个变量来接受我们导出的内容。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="built_in">module</span> <span class="keyword">from</span> <span class="string">'./module.js'</span></span><br><span class="line"><span class="built_in">module</span>.foo();</span><br></pre></td></tr></table></figure>

<h3 id="1-定义package-json文件"><a href="#1-定义package-json文件" class="headerlink" title="1.定义package.json文件"></a>1.定义package.json文件</h3>  <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  &quot;name&quot; : &quot;es6-babel-browserify&quot;,</span><br><span class="line">  &quot;version&quot; : &quot;1.0.0&quot;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-安装babel-cli-babel-preset-es2015和browserify"><a href="#2-安装babel-cli-babel-preset-es2015和browserify" class="headerlink" title="2.安装babel-cli, babel-preset-es2015和browserify"></a>2.安装babel-cli, babel-preset-es2015和browserify</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install babel-cli browserify -g  // babel 的命令行工具</span><br><span class="line">npm install babel-preset-es2015 --save-dev  // babel的工具包，babel的工具包作用有很多比如这里 es6 转 es5 还有把jsx转html等</span><br></pre></td></tr></table></figure>

<h3 id="3-定义-babelrc文件"><a href="#3-定义-babelrc文件" class="headerlink" title="3.定义.babelrc文件"></a>3.定义.babelrc文件</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line"> <span class="string">"presets"</span>: [<span class="string">"es2015"</span>] <span class="comment">//在babel工作之前会读取这个文件，然后表明babel调用什么模块做什么事情，这个就是 es6转es5 比如说还有 react 就是转jsx语法</span></span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-编码"><a href="#4-编码" class="headerlink" title="4.编码"></a>4.编码</h3><ul>
<li><p>js/src/module1.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">export function foo() &#123;</span><br><span class="line">  console.log(&apos;module1 foo()&apos;);</span><br><span class="line">&#125;</span><br><span class="line">export let bar = function () &#123;</span><br><span class="line">  console.log(&apos;module1 bar()&apos;);</span><br><span class="line">&#125;</span><br><span class="line">export const DATA_ARR = [1, 3, 5, 1]</span><br></pre></td></tr></table></figure>
</li>
<li><p>js/src/module2.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let data = &apos;module2 data&apos;</span><br><span class="line"></span><br><span class="line">function fun1() &#123;</span><br><span class="line">  console.log(&apos;module2 fun1() &apos; + data);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">function fun2() &#123;</span><br><span class="line">  console.log(&apos;module2 fun2() &apos; + data);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">export &#123;fun1, fun2&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>js/src/module3.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">export default &#123;</span><br><span class="line">  name: &apos;Tom&apos;,</span><br><span class="line">  setName: function (name) &#123;</span><br><span class="line">    this.name = name</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>js/src/app.js</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">import &#123;foo, bar&#125; from &apos;./module1&apos;</span><br><span class="line">import &#123;DATA_ARR&#125; from &apos;./module1&apos;</span><br><span class="line">import &#123;fun1, fun2&#125; from &apos;./module2&apos;</span><br><span class="line">import person from &apos;./module3&apos;</span><br><span class="line"></span><br><span class="line">import $ from &apos;jquery&apos;</span><br><span class="line"></span><br><span class="line">$(&apos;body&apos;).css(&apos;background&apos;, &apos;red&apos;)</span><br><span class="line"></span><br><span class="line">foo()</span><br><span class="line">bar()</span><br><span class="line">console.log(DATA_ARR);</span><br><span class="line">fun1()</span><br><span class="line">fun2()</span><br><span class="line"></span><br><span class="line">person.setName(&apos;JACK&apos;)</span><br><span class="line">console.log(person.name);</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="5-编译"><a href="#5-编译" class="headerlink" title="5.编译"></a>5.编译</h3><ol>
<li><p>使用Babel将ES6编译为ES5代码(但包含CommonJS语法) : babel js/src -d js/lib</p>
</li>
<li><p>使用Browserify编译js : browserify js/lib/app.js -o js/lib/bundle.js</p>
<h3 id="6-页面中引入测试"><a href="#6-页面中引入测试" class="headerlink" title="6.页面中引入测试"></a>6.页面中引入测试</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;script type=&quot;text/javascript&quot; src=&quot;js/lib/bundle.js&quot;&gt;&lt;/script&gt;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h3 id="7-引入第三方模块-jQuery"><a href="#7-引入第三方模块-jQuery" class="headerlink" title="7.引入第三方模块(jQuery)"></a>7.引入第三方模块(jQuery)</h3><h4 id="1-下载jQuery模块"><a href="#1-下载jQuery模块" class="headerlink" title="1.下载jQuery模块:"></a>1.下载jQuery模块:</h4><p><code>npm install jquery@1 --save</code></p>
<h4 id="2-在app-js中引入并使用"><a href="#2-在app-js中引入并使用" class="headerlink" title="2.在app.js中引入并使用"></a>2.在app.js中引入并使用</h4><pre><code><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> $ <span class="keyword">from</span> <span class="string">'jquery'</span></span><br><span class="line">$(<span class="string">'body'</span>).css(<span class="string">'background'</span>, <span class="string">'red'</span>)</span><br></pre></td></tr></table></figure></code></pre>]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>ECMAScript 从入门到精通</title>
    <url>/2019/01/10/FrontEnd/ECMAScript%206/</url>
    <content><![CDATA[<h2 id="1-es5"><a href="#1-es5" class="headerlink" title="1. es5"></a>1. es5</h2><h3 id="1-严格模式"><a href="#1-严格模式" class="headerlink" title="1. 严格模式"></a>1. 严格模式</h3><h4 id="1-理解"><a href="#1-理解" class="headerlink" title="1.理解:"></a>1.理解:</h4><ul>
<li><p>除了正常运行模式(混杂模式)，ES5添加了第二种运行模式：”严格模式”（strict mode）。</p>
</li>
<li><p>顾名思义，这种模式使得Javascript在更严格的语法条件下运行</p>
<h4 id="2-目的-作用"><a href="#2-目的-作用" class="headerlink" title="2.目的/作用"></a>2.目的/作用</h4><ul>
<li>消除Javascript语法的一些不合理、不严谨之处，减少一些怪异行为</li>
<li>消除代码运行的一些不安全之处，为代码的安全运行保驾护航</li>
<li>为未来新版本的Javascript做好铺垫<h4 id="3-使用"><a href="#3-使用" class="headerlink" title="3.使用"></a>3.使用</h4></li>
</ul>
</li>
<li><p>在全局或函数的第一条语句定义为: ‘use strict’;</p>
</li>
<li><p>如果浏览器不支持, 只解析为一条简单的语句, 没有任何副作用</p>
<h4 id="4-语法和行为改变"><a href="#4-语法和行为改变" class="headerlink" title="4.语法和行为改变"></a>4.语法和行为改变</h4></li>
</ul>
<ul>
<li>必须用var声明变量</li>
<li>禁止自定义的函数中的this指向window，也就是不允许使用全局函数</li>
<li>创建eval作用域，本来的eval中定义的变量直接做变量提升为全局的作用域。可能会被攻击。</li>
<li>对象不能有重名的属性</li>
</ul>
<h3 id="2-object的拓展方法"><a href="#2-object的拓展方法" class="headerlink" title="2.object的拓展方法"></a>2.object的拓展方法</h3><p>ES5给Object扩展了一些静态方法, 常用的2个:</p>
<h4 id="1-Object-create-prototype-descriptors"><a href="#1-Object-create-prototype-descriptors" class="headerlink" title="1.Object.create(prototype, [descriptors])"></a>1.Object.create(prototype, [descriptors])</h4><ul>
<li><p>作用: 以指定对象为原型创建新的对象</p>
</li>
<li><p>为新的对象指定新的属性, 并对属性进行描述</p>
<ul>
<li>value : 指定值</li>
<li>writable : 标识当前属性值是否是可修改的, 默认为false</li>
<li>configurable: 标识当前属性是否可以被删除 默认为false</li>
<li>enumerable： 标识当前属性是否能用for in 枚举 默认为false</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;<span class="attr">name</span> : <span class="string">'curry'</span>, <span class="attr">age</span> : <span class="number">29</span>&#125;</span><br><span class="line">obj1 = <span class="built_in">Object</span>.create(obj, &#123;</span><br><span class="line">        sex : &#123;</span><br><span class="line">            value : <span class="string">'男'</span>,</span><br><span class="line">            writable : <span class="literal">true</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>

<p><img src="http://images.heniankj.com/20190110082907.png" alt></p>
<p>obj1 的原型对象就是 obj，并企里面有一个 sex 值，但是要注意设置 sex的一些属性，比如现在 sex 就是只能够读写，不能被删除，也不能采用 for in 遍历。</p>
<h4 id="2-Object-defineProperties-object-descriptors"><a href="#2-Object-defineProperties-object-descriptors" class="headerlink" title="2.Object.defineProperties(object, descriptors)"></a>2.Object.defineProperties(object, descriptors)</h4></li>
<li><p>作用: 为指定对象定义扩展多个属性</p>
<ul>
<li>get ：用来获取当前属性值得回调函数</li>
<li>set ：修改当前属性值得触发的回调函数，并且实参即为修改后的值</li>
<li>存取器属性：setter,getter一个用来存值，一个用来取值</li>
</ul>
</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj2 = &#123;</span><br><span class="line">    firstName : <span class="string">'curry'</span>,</span><br><span class="line">    lastName : <span class="string">'stephen'</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="built_in">Object</span>.defineProperties(obj2, &#123;</span><br><span class="line">    fullName : &#123;</span><br><span class="line">        <span class="keyword">get</span> : function () &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">this</span>.firstName + <span class="string">'-'</span> + <span class="keyword">this</span>.lastName</span><br><span class="line">        &#125;,</span><br><span class="line">        <span class="keyword">set</span> : function (data) &#123;</span><br><span class="line">            <span class="keyword">var</span> names = data.split(<span class="string">'-'</span>);</span><br><span class="line">            <span class="keyword">this</span>.firstName = names[<span class="number">0</span>];</span><br><span class="line">            <span class="keyword">this</span>.lastName = names[<span class="number">1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>

<p><img src="http://images.heniankj.com/20190110083053.png" alt></p>
<p>可以看到新设置的 fullName 属性和其他的是不一样的，因为我们设置了 get 和 set 那么他就会惰性求值，只有我们在调用这个属性的时候会调用 get 方法求出元素的值，set 也是在设置值的时候才会调用。如果没有 get 就获取不到值，没有set则不能设置值。</p>
<p>除了上面这种设置 get 和 set 的方式之外还可以使用对象本身的方法：</p>
<ol>
<li>get propertyName(){} 用来得到当前属性值的回调函数</li>
<li>set propertyName(){} 用来监视当前属性值变化的回调函数</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">    firstName : <span class="string">'kobe'</span>,</span><br><span class="line">    lastName : <span class="string">'bryant'</span>,</span><br><span class="line">    <span class="keyword">get</span> fullName()&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.firstName + <span class="string">' '</span> + <span class="keyword">this</span>.lastName</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="keyword">set</span> fullName(data)&#123;</span><br><span class="line">        <span class="keyword">var</span> names = data.split(<span class="string">' '</span>);</span><br><span class="line">        <span class="keyword">this</span>.firstName = names[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">this</span>.lastName = names[<span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<p>代码同理。</p>
<h3 id="3-Array-拓展"><a href="#3-Array-拓展" class="headerlink" title="3. Array 拓展"></a>3. Array 拓展</h3><ol>
<li>Array.prototype.indexOf(value) : 得到值在数组中的第一个下标</li>
<li>Array.prototype.lastIndexOf(value) : 得到值在数组中的最后一个下标</li>
<li>Array.prototype.forEach(function(item, index){}) : 遍历数组</li>
<li>Array.prototype.map(function(item, index){}) : 遍历数组返回一个新的数组，返回加工之后的值</li>
<li>Array.prototype.filter(function(item, index){}) : 遍历过滤出一个新的子数组， 返回条件为true的值</li>
</ol>
<p>在原型上定义了这么多方法，意味着 array 的实例都可以调用这些方法。</p>
<h3 id="4-函数拓展"><a href="#4-函数拓展" class="headerlink" title="4. 函数拓展"></a>4. 函数拓展</h3><h4 id="1-Function-prototype-bind-obj"><a href="#1-Function-prototype-bind-obj" class="headerlink" title="1.Function.prototype.bind(obj)"></a>1.Function.prototype.bind(obj)</h4><p>作用: 将函数内的this绑定为obj, 并将函数返回</p>
<h4 id="2-区别bind-与call-和apply"><a href="#2-区别bind-与call-和apply" class="headerlink" title="2.区别bind()与call()和apply()?"></a>2.区别bind()与call()和apply()?</h4><p>都能指定函数中的this，但是call()/apply()是立即调用函数而bind()是将函数返回。所以我们的 bind 一般是用来指定回调函数的 this。</p>
<h2 id="2-es6"><a href="#2-es6" class="headerlink" title="2. es6"></a>2. es6</h2><h3 id="1-let"><a href="#1-let" class="headerlink" title="1. let"></a>1. let</h3><h4 id="1-作用"><a href="#1-作用" class="headerlink" title="1.作用:"></a>1.作用:</h4><ul>
<li>与var类似, 用于声明一个变量</li>
</ul>
<h4 id="2-特点"><a href="#2-特点" class="headerlink" title="2.特点:"></a>2.特点:</h4><ul>
<li>在块作用域内有效，以前只有文件和函数作用域，这个东西有块级作用域</li>
<li>不能重复声明，否则会报错</li>
<li>不会预处理, 不存在提升</li>
</ul>
<h4 id="3-应用"><a href="#3-应用" class="headerlink" title="3.应用:"></a>3.应用:</h4><ul>
<li>循环遍历加监听</li>
<li>使用let取代var是趋势</li>
</ul>
<h3 id="2-const"><a href="#2-const" class="headerlink" title="2. const"></a>2. const</h3><h4 id="1-作用-1"><a href="#1-作用-1" class="headerlink" title="1.作用:"></a>1.作用:</h4><p>定义一个常量</p>
<h4 id="2-特点-1"><a href="#2-特点-1" class="headerlink" title="2.特点:"></a>2.特点:</h4><p>不能修改其它特点同let</p>
<h4 id="3-应用-1"><a href="#3-应用-1" class="headerlink" title="3.应用:"></a>3.应用:</h4><p>保存不用改变的数据</p>
<h3 id="3-结构赋值"><a href="#3-结构赋值" class="headerlink" title="3. 结构赋值"></a>3. 结构赋值</h3><h4 id="1-理解-1"><a href="#1-理解-1" class="headerlink" title="1.理解:"></a>1.理解:</h4><p>从对象或数组中提取数据, 并赋值给变量(多个)</p>
<h4 id="2-对象的解构赋值"><a href="#2-对象的解构赋值" class="headerlink" title="2.对象的解构赋值"></a>2.对象的解构赋值</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let &#123;n, a&#125; = &#123;n:&apos;tom&apos;, a:12&#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-数组的解构赋值"><a href="#3-数组的解构赋值" class="headerlink" title="3.数组的解构赋值"></a>3.数组的解构赋值</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let [a,b] = [1, &apos;atguigu&apos;];</span><br></pre></td></tr></table></figure>

<h4 id="4-用途"><a href="#4-用途" class="headerlink" title="4.用途"></a>4.用途</h4><p>给多个形参赋值</p>
<h4 id="5-例子"><a href="#5-例子" class="headerlink" title="5. 例子"></a>5. 例子</h4><p>对象</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> obj = &#123;<span class="attr">name</span> : <span class="string">'kobe'</span>, <span class="attr">age</span> : <span class="number">39</span>,<span class="attr">site</span>:&#123;<span class="attr">github</span>:<span class="string">'aaa'</span>,<span class="attr">weibo</span>:<span class="string">'bbb'</span>&#125;&#125;;</span><br><span class="line"><span class="comment">//对象的解构赋值</span></span><br><span class="line"><span class="keyword">let</span> &#123;age,<span class="attr">site</span>:&#123;github&#125;&#125; = obj;</span><br><span class="line"><span class="built_in">console</span>.log(age,github);</span><br></pre></td></tr></table></figure>

<p>数组</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr = [<span class="string">'abc'</span>, <span class="number">23</span>, <span class="literal">true</span>];</span><br><span class="line"><span class="keyword">let</span> [, , c, d] = arr;</span><br><span class="line"><span class="built_in">console</span>.log(c, d); <span class="comment">//按照下标寻找</span></span><br></pre></td></tr></table></figure>

<p>参数解构</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">person1</span>(<span class="params">&#123;name, age&#125;</span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(name, age);</span><br><span class="line">&#125;</span><br><span class="line">person1(obj);</span><br></pre></td></tr></table></figure>

<h3 id="4-模板字符串"><a href="#4-模板字符串" class="headerlink" title="4.模板字符串"></a>4.模板字符串</h3><p> 简化字符串的拼接,模板字符串必须用 `` 包含, 变化的部分使用${xxx}定义</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(<span class="string">`我叫:<span class="subst">$&#123;obj.name&#125;</span>, 我的年龄是：<span class="subst">$&#123;obj.age&#125;</span>`</span>);</span><br></pre></td></tr></table></figure>

<h3 id="5-对象的简写方法"><a href="#5-对象的简写方法" class="headerlink" title="5.对象的简写方法"></a>5.对象的简写方法</h3><h4 id="1-同名属性省略名字"><a href="#1-同名属性省略名字" class="headerlink" title="1.同名属性省略名字"></a>1.同名属性省略名字</h4><h4 id="2-省略function关键字"><a href="#2-省略function关键字" class="headerlink" title="2.省略function关键字"></a>2.省略function关键字</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> x = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">let</span> y = <span class="number">2</span>;</span><br><span class="line"><span class="keyword">let</span> point = &#123;</span><br><span class="line">  x,</span><br><span class="line">  y,</span><br><span class="line">  setX (x) &#123;<span class="keyword">this</span>.x = x&#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="6-箭头函数"><a href="#6-箭头函数" class="headerlink" title="6.箭头函数"></a>6.箭头函数</h3><h4 id="1-作用-定义匿名函数"><a href="#1-作用-定义匿名函数" class="headerlink" title="1.作用: 定义匿名函数"></a>1.作用: 定义匿名函数</h4><h4 id="2-基本语法"><a href="#2-基本语法" class="headerlink" title="2.基本语法:"></a>2.基本语法:</h4><ul>
<li>没有参数: () =&gt; console.log(‘xxxx’)</li>
<li>一个参数: i =&gt; i+2</li>
<li>大于一个参数: (i,j) =&gt; i+j</li>
<li>函数体不用大括号: 默认返回结果</li>
<li>函数体如果有多个语句, 需要用{}包围，若有需要返回的内容，需要手动返回</li>
</ul>
<h4 id="3-使用场景-多用来定义回调函数"><a href="#3-使用场景-多用来定义回调函数" class="headerlink" title="3.使用场景: 多用来定义回调函数"></a>3.使用场景: 多用来定义回调函数</h4><h4 id="4-箭头函数的特点："><a href="#4-箭头函数的特点：" class="headerlink" title="4.箭头函数的特点："></a>4.箭头函数的特点：</h4><ol>
<li>简洁</li>
<li>箭头函数没有自己的this，箭头函数的this不是调用的时候决定的，而是在定义的时候处在的对象就是它的this</li>
<li>扩展理解： 箭头函数的this看外层的是否有函数，如果有，外层函数的this就是内部箭头函数的this，如果没有，则this是window。</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这个this是window因为内层的箭头函数往外找找到有函数，则就看他的this，而他往外找找不到说明就是window</span></span><br><span class="line"><span class="keyword">let</span> obj = &#123;</span><br><span class="line">    name : <span class="string">'kobe'</span>,</span><br><span class="line">    age : <span class="number">39</span>,</span><br><span class="line">    getName : <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">        btn2.onclick = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">            <span class="built_in">console</span>.log(<span class="keyword">this</span>);<span class="comment">//obj</span></span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="comment">// 这个this是window因为内层的箭头函数往外找找到有函数，则就看他的this，而他是一个普通函数就看是谁调用他了。如果是obj调用的话 this 就是 obj</span></span><br><span class="line"><span class="keyword">let</span> obj = &#123;</span><br><span class="line">    name : <span class="string">'kobe'</span>,</span><br><span class="line">    age : <span class="number">39</span>,</span><br><span class="line">    getName : <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">        btn2.onclick = <span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">            <span class="built_in">console</span>.log(<span class="keyword">this</span>);<span class="comment">//obj</span></span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="7-打包解包运算符"><a href="#7-打包解包运算符" class="headerlink" title="7. 打包解包运算符"></a>7. 打包解包运算符</h3><h4 id="1-rest-可变-参数"><a href="#1-rest-可变-参数" class="headerlink" title="1.rest(可变)参数"></a>1.rest(可变)参数</h4><p>用来取代arguments 但比arguments灵活,他只能作为最后部分形参参数，也就是打包最后一部分形参</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">add</span>(<span class="params">...values</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">let</span> sum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span>(value <span class="keyword">of</span> values) &#123;</span><br><span class="line">    sum += value;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> sum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-扩展运算符"><a href="#2-扩展运算符" class="headerlink" title="2.扩展运算符"></a>2.扩展运算符</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> arr1 = [<span class="number">1</span>,<span class="number">3</span>,<span class="number">5</span>];</span><br><span class="line"><span class="keyword">let</span> arr2 = [<span class="number">2</span>,...arr1,<span class="number">6</span>];</span><br><span class="line">arr2.push(...arr1);</span><br></pre></td></tr></table></figure>

<h3 id="8-形参默认值"><a href="#8-形参默认值" class="headerlink" title="8. 形参默认值"></a>8. 形参默认值</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span>(<span class="params">a=<span class="number">1</span></span>)</span>&#123;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="9-promise"><a href="#9-promise" class="headerlink" title="9. promise"></a>9. promise</h3><h4 id="1-理解-2"><a href="#1-理解-2" class="headerlink" title="1.理解:"></a>1.理解:</h4><p>​    Promise对象: 代表了未来某个将要发生的事件(通常是一个异步操作)，有了promise对象, 可以将异步操作以同步的流程表达出来, 避免了层层嵌套的回调函数(俗称’回调地狱’)，ES6的Promise是一个构造函数, 用来生成promise实例。</p>
<h4 id="2-使用promise基本步骤-2步"><a href="#2-使用promise基本步骤-2步" class="headerlink" title="2.使用promise基本步骤(2步):"></a>2.使用promise基本步骤(2步):</h4><ul>
<li><p>创建promise对象</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">let promise = new Promise((resolve, reject) =&gt; &#123;</span><br><span class="line">    //初始化promise状态为 pending</span><br><span class="line">  //执行异步操作</span><br><span class="line">  if(异步操作成功) &#123;</span><br><span class="line">    resolve(value);//自动修改promise的状态为 fullfilled ，设置了promise的状态以后然后调用then或者catch，根据promise的状态执行对应的then或者catch内容。或者说在then里面有两个函数，分别是成功和失败，他会地总调用。 里面的value 就就会传递给then里面的回调成功的入参。</span><br><span class="line">  &#125; else &#123;</span><br><span class="line">    reject(errMsg);//自动修改promise的状态为 rejected</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
</li>
<li><p>调用promise的then()</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">promise.then(function(</span><br><span class="line">  result =&gt; console.log(result), //这个result就是resolve里面的参数</span><br><span class="line">  errorMsg =&gt; alert(errorMsg)</span><br><span class="line">))</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="3-promise对象的3个状态"><a href="#3-promise对象的3个状态" class="headerlink" title="3.promise对象的3个状态"></a>3.promise对象的3个状态</h4><ul>
<li>pending: 初始化状态</li>
<li>fullfilled: 成功状态</li>
<li>rejected: 失败状态</li>
</ul>
<h4 id="4-应用"><a href="#4-应用" class="headerlink" title="4.应用:"></a>4.应用:</h4><ul>
<li><p>使用promise实现超时处理</p>
</li>
<li><p>使用promise封装处理ajax请求<br>let request = new XMLHttpRequest();<br>request.onreadystatechange = function () {<br>}<br>request.responseType = ‘json’;<br>request.open(“GET”, url);<br>request.send();</p>
</li>
</ul>
<h4 id="5-流程"><a href="#5-流程" class="headerlink" title="5. 流程"></a>5. 流程</h4><p><img src="http://images.heniankj.com/20190110093914.png" alt></p>
<h3 id="10-symbol"><a href="#10-symbol" class="headerlink" title="10. symbol"></a>10. symbol</h3><p>ES5中对象的属性名都是字符串，容易造成重名，污染环境</p>
<h4 id="1-概念："><a href="#1-概念：" class="headerlink" title="1.概念："></a>1.概念：</h4><p>ES6中的添加了一种原始数据类型symbol(已有的原始数据类型：String, Number, boolean, null, undefined, 对象)</p>
<h4 id="2-特点："><a href="#2-特点：" class="headerlink" title="2.特点："></a>2.特点：</h4><p>​    1、Symbol属性对应的值是唯一的，解决命名冲突问题<br>​    2、Symbol值不能与其他数据进行计算，包括同字符串拼串<br>​    3、for in, for of遍历时不会遍历symbol属性</p>
<h4 id="3-使用："><a href="#3-使用：" class="headerlink" title="3.使用："></a>3.使用：</h4><p>​    1、调用Symbol函数得到symbol值</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> symbol = <span class="built_in">Symbol</span>();</span><br><span class="line"><span class="keyword">let</span> obj = &#123;&#125;;</span><br><span class="line">obj[symbol] = <span class="string">'hello'</span>;</span><br></pre></td></tr></table></figure>

<p>​    2、传参标识</p>
<p>​    2、传参标识</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> symbol = <span class="built_in">Symbol</span>(<span class="string">'one'</span>);</span><br><span class="line"><span class="keyword">let</span> symbol2 = <span class="built_in">Symbol</span>(<span class="string">'two'</span>);</span><br><span class="line"><span class="built_in">console</span>.log(symbol);<span class="comment">// Symbol('one')</span></span><br><span class="line"><span class="built_in">console</span>.log(symbol2);<span class="comment">// Symbol('two')</span></span><br></pre></td></tr></table></figure>

<p>​    3、内置Symbol值</p>
<p>​    3、内置Symbol值</p>
<p>除了定义自己使用的Symbol值以外，ES6还提供了11个内置的Symbol值，指向语言内部使用的方法。对象的Symbol.iterator属性，指向该对象的默认遍历器方法</p>
<h3 id="11-iterator"><a href="#11-iterator" class="headerlink" title="11. iterator"></a>11. iterator</h3><h4 id="1-概念：-1"><a href="#1-概念：-1" class="headerlink" title="1.概念："></a>1.概念：</h4><p>iterator是一种接口机制，为各种不同的数据结构提供统一的访问机制</p>
<h4 id="2-作用："><a href="#2-作用：" class="headerlink" title="2.作用："></a>2.作用：</h4><p>为各种数据结构，提供一个统一的、简便的访问接口；<br>使得数据结构的成员能够按某种次序排列<br>ES6创造了一种新的遍历命令for…of循环，Iterator接口主要供for…of消费。</p>
<h4 id="3-工作原理："><a href="#3-工作原理：" class="headerlink" title="3.工作原理："></a>3.工作原理：</h4><ul>
<li>创建一个指针对象，指向数据结构的起始位置。</li>
<li>第一次调用next方法，指针自动指向数据结构的第一个成员</li>
<li>接下来不断调用next方法，指针会一直往后移动，直到指向最后一个成员</li>
<li>每调用next方法返回的是一个包含value和done的对象，{value: 当前成员的值,done: 布尔值}<ul>
<li>value表示当前成员的值，done对应的布尔值表示当前的数据的结构是否遍历结束。</li>
<li>当遍历结束的时候返回的value值是undefined，done值为false</li>
</ul>
</li>
</ul>
<p>原生具备iterator接口的数据(可用for of遍历)<br>1、Array<br>2、arguments<br>3、set容器<br>4、map容器<br>5、String</p>
<h4 id="4-模拟为对象部署-iterator-接口"><a href="#4-模拟为对象部署-iterator-接口" class="headerlink" title="4. 模拟为对象部署 iterator 接口"></a>4. 模拟为对象部署 iterator 接口</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> iteratorableObj=&#123;</span><br><span class="line">    [<span class="built_in">Symbol</span>.iteraor]:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">        <span class="keyword">return</span> &#123;</span><br><span class="line">            <span class="keyword">let</span> nextIndex=<span class="number">0</span>;</span><br><span class="line">            next:<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">                <span class="keyword">return</span> &#123;</span><br><span class="line">                    value:<span class="keyword">this</span>[nextIndex++],</span><br><span class="line">                    done:<span class="keyword">this</span>.length==nextIndex</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="12-generator"><a href="#12-generator" class="headerlink" title="12. generator"></a>12. generator</h3><h4 id="1-概念：-2"><a href="#1-概念：-2" class="headerlink" title="1.概念："></a>1.概念：</h4><ol>
<li>ES6提供的解决异步编程的方案之一</li>
<li>Generator函数是一个状态机，内部封装了不同状态的数据，</li>
<li>用来生成遍历器对象</li>
<li>可暂停函数(惰性求值), yield可暂停，next方法可启动。每次返回的是yield后的表达式结果</li>
</ol>
<h4 id="2-特点：-1"><a href="#2-特点：-1" class="headerlink" title="2.特点："></a>2.特点：</h4><ol>
<li><p>function 与函数名之间有一个星号</p>
</li>
<li><p>内部用yield表达式来定义不同的状态</p>
<p>例如：<br>​     function* generatorExample(){<br>​       let result = yield ‘hello’;  // 状态值为hello<br>​       yield ‘generator’; // 状态值为generator<br>​     }</p>
</li>
<li><p>generator函数返回的是指针对象({next:function{….}})，而不会执行函数内部逻辑</p>
</li>
<li><p>调用next方法函数内部逻辑开始执行，遇到yield表达式停止，返回{value: yield后的表达式结果/undefined, done: false/true}</p>
</li>
<li><p>再次调用next方法会从上一次停止时的yield处开始，直到最后</p>
</li>
<li><p>yield语句返回结果通常为undefined， 当调用next方法时传参内容会作为启动时yield语句的返回值。</p>
</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">generatorTest</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'函数开始执行'</span>);</span><br><span class="line">  <span class="keyword">let</span> ret=<span class="keyword">yield</span> <span class="string">'hello'</span>;</span><br><span class="line">  <span class="built_in">console</span>.log(ret); <span class="comment">// 这个值就是 aaa 就是这次启动的时候next传入的值</span></span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'函数暂停后再次启动'</span>);</span><br><span class="line">  <span class="keyword">yield</span> <span class="string">'generator'</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 生成遍历器对象</span></span><br><span class="line"><span class="keyword">let</span> Gt = generatorTest();</span><br><span class="line"><span class="comment">// 执行函数，遇到yield后即暂停</span></span><br><span class="line"><span class="built_in">console</span>.log(Gt); <span class="comment">// 遍历器对象</span></span><br><span class="line"><span class="keyword">let</span> result = Gt.next(); <span class="comment">// 函数执行,遇到yield暂停</span></span><br><span class="line"><span class="built_in">console</span>.log(result); <span class="comment">// &#123;value: "hello", done: false&#125;</span></span><br><span class="line">result = Gt.next(<span class="string">'aaa'</span>); <span class="comment">// 函数再次启动</span></span><br><span class="line"><span class="built_in">console</span>.log(result); <span class="comment">// &#123;value: 'generator', done: false&#125;</span></span><br><span class="line">result = Gt.next();</span><br><span class="line"><span class="built_in">console</span>.log(result); <span class="comment">// &#123;value: undefined, done: true&#125;表示函数内部状态已经遍历完毕</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 真正的为对象部署接口采用Symbol.iterator属性;</span></span><br><span class="line"><span class="keyword">let</span> myIterable = &#123;&#125;;</span><br><span class="line">myIterable[<span class="built_in">Symbol</span>.iterator] = <span class="function"><span class="keyword">function</span>* (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">yield</span> <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">yield</span> <span class="number">2</span>;</span><br><span class="line">  <span class="keyword">yield</span> <span class="number">4</span>;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">let</span> i <span class="keyword">of</span> myIterable)&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(i);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">let</span> obj = [...myIterable];</span><br><span class="line"><span class="built_in">console</span>.log(obj);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'-------------------------------'</span>);</span><br><span class="line"><span class="comment">// 案例练习</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">* 需求：</span></span><br><span class="line"><span class="comment">* 1、发送ajax请求获取新闻内容</span></span><br><span class="line"><span class="comment">* 2、新闻内容获取成功后再次发送请求，获取对应的新闻评论内容</span></span><br><span class="line"><span class="comment">* 3、新闻内容获取失败则不需要再次发送请求。</span></span><br><span class="line"><span class="comment">* </span></span><br><span class="line"><span class="comment">* */</span> </span><br><span class="line"><span class="function"><span class="keyword">function</span>* <span class="title">sendXml</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">// url为next传参进来的数据</span></span><br><span class="line"> <span class="keyword">let</span> url = <span class="keyword">yield</span> getNews(<span class="string">'http://localhost:3000/news?newsId=2'</span>);</span><br><span class="line">  <span class="keyword">yield</span> getNews(url);</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getNews</span>(<span class="params">url</span>) </span>&#123;</span><br><span class="line">  $.<span class="keyword">get</span>(url, function (data) &#123;</span><br><span class="line">    <span class="built_in">console</span>.log(data);</span><br><span class="line">    <span class="keyword">let</span> commentsUrl = data.commentsUrl;</span><br><span class="line">    <span class="keyword">let</span> url = <span class="string">'http://localhost:3000'</span> + commentsUrl;</span><br><span class="line">    <span class="comment">// 当获取新闻内容成功，发送请求获取对应的评论内容</span></span><br><span class="line">    <span class="comment">// 调用next传参会作为上次暂停是yield的返回值</span></span><br><span class="line">    sx.next(url);</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> sx = sendXml();</span><br><span class="line"><span class="comment">// 发送请求获取新闻内容</span></span><br><span class="line">sx.next();</span><br></pre></td></tr></table></figure>

<h3 id="13-async"><a href="#13-async" class="headerlink" title="13. async"></a>13. async</h3><p>这个其实是generator的语法糖，这个是 es2017 的内容，但是已经被广泛的用开了。</p>
<h4 id="1-概念：-3"><a href="#1-概念：-3" class="headerlink" title="1.概念："></a>1.概念：</h4><p> 真正意义上去解决异步回调的问题，同步流程表达异步操作</p>
<h4 id="2-语法："><a href="#2-语法：" class="headerlink" title="2.语法："></a>2.语法：</h4><figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">foo</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">    <span class="keyword">await</span> 异步操作;</span><br><span class="line">    <span class="keyword">await</span> 异步操作；</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-特点："><a href="#3-特点：" class="headerlink" title="3.特点："></a>3.特点：</h4><ol>
<li>不需要像Generator去调用next方法，遇到await等待，当前的异步操作成功就往下执行</li>
<li>返回的总是Promise对象，可以用then方法进行下一步操作</li>
<li>async取代Generator函数的星号*，await取代Generator的yield 。await 的返回值就是 reslove 和 reject 的参数</li>
<li>语意上更为明确，使用简单，经临床验证，暂时没有任何副作用</li>
</ol>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">timeout</span>(<span class="params">ms</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">    setTimeout(resolve, ms);</span><br><span class="line">  &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">asyncPrint</span>(<span class="params">value, ms</span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'函数执行'</span>, <span class="keyword">new</span> <span class="built_in">Date</span>().toTimeString());</span><br><span class="line">  <span class="keyword">await</span> timeout(ms);</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'延时时间'</span>, <span class="keyword">new</span> <span class="built_in">Date</span>().toTimeString());</span><br><span class="line">  <span class="built_in">console</span>.log(value);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(asyncPrint(<span class="string">'hello async'</span>, <span class="number">2000</span>));</span><br><span class="line"></span><br><span class="line"><span class="comment">// await </span></span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">awaitTest</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> result = <span class="keyword">await</span> <span class="built_in">Promise</span>.resolve(<span class="string">'执行成功'</span>); <span class="comment">// 这个直接用来创建并设定 Promise 的状态</span></span><br><span class="line">  <span class="built_in">console</span>.log(result);</span><br><span class="line">  <span class="keyword">let</span> result2 = <span class="keyword">await</span> <span class="built_in">Promise</span>.reject(<span class="string">'执行失败'</span>);  <span class="comment">// 这个直接用来创建并设定 Promise 的状态</span></span><br><span class="line">  <span class="built_in">console</span>.log(result2);</span><br><span class="line">  <span class="keyword">let</span> result3 = <span class="keyword">await</span> <span class="built_in">Promise</span>.resolve(<span class="string">'还想执行一次'</span>);<span class="comment">// 执行不了，失败了就不会继续执行下去</span></span><br><span class="line">  <span class="built_in">console</span>.log(result3);</span><br><span class="line">&#125;</span><br><span class="line">awaitTest();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 案例演示</span></span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">sendXml</span>(<span class="params">url</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function">(<span class="params">resolve, reject</span>) =&gt;</span> &#123;</span><br><span class="line">      $.ajax(&#123;</span><br><span class="line">        url,</span><br><span class="line">        type: <span class="string">'GET'</span>,</span><br><span class="line">        success: <span class="function"><span class="params">data</span> =&gt;</span>  resolve(data),</span><br><span class="line">        error: <span class="function"><span class="params">error</span> =&gt;</span> reject(error)</span><br><span class="line">      &#125;)</span><br><span class="line">    &#125;)</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">getNews</span>(<span class="params">url</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> result = <span class="keyword">await</span> sendXml(url); <span class="comment">//如果这里报错了就会执行 reject 导致控制台抛异常，但是为了用户体验我们可以在错误的时候使用resolve但是要不让下面的函数继续执行。</span></span><br><span class="line">  <span class="keyword">let</span> result2 = <span class="keyword">await</span> sendXml(result.commentsUrl);</span><br><span class="line">  <span class="built_in">console</span>.log(result, result2);</span><br><span class="line">&#125;</span><br><span class="line">getNews(<span class="string">'http://localhost:3000/news?id=2'</span>)</span><br></pre></td></tr></table></figure>

<h3 id="14-class"><a href="#14-class" class="headerlink" title="14. class"></a>14. class</h3><ol>
<li>通过class定义类/实现类的继承</li>
<li>在类中通过constructor定义构造方法，因为原型中的 constructor 就是我们定义的工厂方法，在我们使用工厂方法的时候。</li>
<li>通过new来创建类的实例</li>
<li>通过extends来实现类的继承</li>
<li>通过super调用父类的构造方法</li>
<li>重写从父类中继承的一般方法</li>
<li>在class中写方法只能通过省略 function 的简写方式或者箭头函数书写，否则会报错</li>
</ol>
<h3 id="15-数组扩展"><a href="#15-数组扩展" class="headerlink" title="15. 数组扩展"></a>15. 数组扩展</h3><ol>
<li>Array.from(v) 将伪数组转成真数组</li>
<li>Array.of(v1,v2,v3….) 将一系列的元素变成数组</li>
<li>Array.find((value,index,arr)=&gt;{return true/false}) 找出第一个满足条件的元素</li>
<li>Array.findIndex((value,index,arr)=&gt;{return true/false})  同上只是返回的不是元素而是下标</li>
</ol>
<h3 id="16-Obj拓展"><a href="#16-Obj拓展" class="headerlink" title="16.Obj拓展"></a>16.Obj拓展</h3><ol>
<li><p>Object.is(v1, v2)</p>
<p>判断2个数据是否完全相等</p>
</li>
<li><p>Object.assign(target, source1, source2..)</p>
<p>将源对象的属性复制到目标对象上</p>
</li>
<li><p>直接操作 <strong>proto</strong> 属性</p>
  <figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="keyword">let</span> obj2 = &#123;&#125;;</span><br><span class="line">obj2.__proto__ = obj1;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h3 id="17-深度克隆"><a href="#17-深度克隆" class="headerlink" title="17. 深度克隆"></a>17. 深度克隆</h3><h4 id="1、数据类型："><a href="#1、数据类型：" class="headerlink" title="1、数据类型："></a>1、数据类型：</h4><ul>
<li>数据分为基本的数据类型(String, Number, boolean, Null, Undefined)和对象数据类型</li>
</ul>
<ul>
<li>基本数据类型：<br>特点： 存储的是该对象的实际数据</li>
<li>对象数据类型：<br>特点： 存储的是该对象在栈中引用，真实的数据存放在堆内存里</li>
</ul>
<h4 id="2、复制数据"><a href="#2、复制数据" class="headerlink" title="2、复制数据"></a>2、复制数据</h4><ul>
<li>基本数据类型存放的就是实际的数据，可直接复制<br>let number2 = 2;<br>let number1 = number2;</li>
<li>克隆数据：对象/数组<br>1、区别： 浅拷贝/深度拷贝<br>   判断： 拷贝是否产生了新的数据还是拷贝的是数据的引用<br>   知识点：对象数据存放的是对象在栈内存的引用，直接复制的是对象的引用<br>   let obj = {username: ‘kobe’}<br>   let obj1 = obj; // obj1 复制了obj在栈内存的引用<br>2、常用的拷贝技术<br>  1). arr.concat(): 数组浅拷贝<br>  2). arr.slice(): 数组浅拷贝<br>  3). JSON.parse(JSON.stringify(arr/obj)): 数组或对象深拷贝, 但不能处理函数数据<br>  4). 浅拷贝包含函数数据的对象/数组<br>  5). 深拷贝包含函数数据的对象/数组</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 复制的对象的方式</span></span><br><span class="line"><span class="comment">// 浅度复制</span></span><br><span class="line"><span class="keyword">let</span> obj = &#123;<span class="attr">username</span>: <span class="string">'kobe'</span>, <span class="attr">age</span>: <span class="number">39</span>, <span class="attr">sex</span>: &#123;<span class="attr">option1</span>: <span class="string">'男'</span>, <span class="attr">option2</span>: <span class="string">'女'</span>&#125;&#125;;</span><br><span class="line"><span class="keyword">let</span> obj1 = obj;</span><br><span class="line"><span class="built_in">console</span>.log(obj1);</span><br><span class="line">obj1.sex.option1 = <span class="string">'不男不女'</span>; <span class="comment">// 修改复制的对象会影响原对象</span></span><br><span class="line"><span class="built_in">console</span>.log(obj1, obj);</span><br><span class="line"></span><br><span class="line"><span class="built_in">console</span>.log(<span class="string">'-----------'</span>);</span><br><span class="line"><span class="comment">// Object.assign();  浅复制</span></span><br><span class="line"><span class="keyword">let</span> obj2 = &#123;&#125;;</span><br><span class="line"><span class="built_in">Object</span>.assign(obj2, obj);</span><br><span class="line"><span class="built_in">console</span>.log(obj2);</span><br><span class="line">obj2.sex.option1 = <span class="string">'男'</span>; <span class="comment">// 修改复制的对象会影响原对象</span></span><br><span class="line"><span class="built_in">console</span>.log(obj2, obj);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 深度克隆(复制)</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getObjClass</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> result = <span class="built_in">Object</span>.prototype.toString.call(obj).slice(<span class="number">8</span>, <span class="number">-1</span>);</span><br><span class="line">  <span class="keyword">if</span>(result === <span class="string">'Null'</span>)&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'Null'</span>;</span><br><span class="line">  &#125;<span class="keyword">else</span> <span class="keyword">if</span>(result === <span class="string">'Undefined'</span>)&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">'Undefined'</span>;</span><br><span class="line">  &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> result;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// for in 遍历数组的时候遍历的是下标</span></span><br><span class="line"><span class="keyword">let</span> testArr = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>];</span><br><span class="line"><span class="keyword">for</span>(<span class="keyword">let</span> i <span class="keyword">in</span> testArr)&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(i); <span class="comment">// 对应的下标索引</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 深度克隆</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">deepClone</span>(<span class="params">obj</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> result, objClass = getObjClass(obj);</span><br><span class="line">  <span class="keyword">if</span>(objClass === <span class="string">'Object'</span>)&#123;</span><br><span class="line">    result = &#123;&#125;;</span><br><span class="line">  &#125;<span class="keyword">else</span> <span class="keyword">if</span>(objClass === <span class="string">'Array'</span>)&#123;</span><br><span class="line">    result = [];</span><br><span class="line">  &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> obj; <span class="comment">// 如果是其他数据类型不复制，直接将数据返回</span></span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">// 遍历目标对象</span></span><br><span class="line">  <span class="keyword">for</span>(<span class="keyword">let</span> key <span class="keyword">in</span> obj)&#123;</span><br><span class="line">    <span class="keyword">let</span> value = obj[key];</span><br><span class="line">    <span class="keyword">if</span>(getObjClass(value) === <span class="string">"Object"</span> || <span class="string">'Array'</span>)&#123;</span><br><span class="line">      result[key] = deepClone(value);</span><br><span class="line">    &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">      result[key] = obj[key];</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> result;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> obj3 = &#123;<span class="attr">username</span>: <span class="string">'kobe'</span>,<span class="attr">age</span>: <span class="number">39</span>, <span class="attr">sex</span>: &#123;<span class="attr">option1</span>: <span class="string">'男'</span>, <span class="attr">option2</span>: <span class="string">'女'</span>&#125;&#125;;</span><br><span class="line"><span class="keyword">let</span> obj4 = deepClone(obj3);</span><br><span class="line"><span class="built_in">console</span>.log(obj4);</span><br><span class="line">obj4.sex.option1 = <span class="string">'不男不女'</span>; <span class="comment">// 修改复制后的对象不会影响原对象</span></span><br><span class="line"><span class="built_in">console</span>.log(obj4, obj3);</span><br></pre></td></tr></table></figure>

<h2 id="3-es7"><a href="#3-es7" class="headerlink" title="3.es7"></a>3.es7</h2><ol>
<li>指数运算符(幂): **</li>
<li>Array.prototype.includes(value) : 判断数组中是否包含指定value</li>
</ol>
]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaScript 从入门到精通</title>
    <url>/2019/01/09/FrontEnd/Javascript%20%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="1-this"><a href="#1-this" class="headerlink" title="1. this"></a>1. this</h2><p>​    解析器在调用函数每次都会向函数内部传递进一个隐含的参数,这个隐含的参数就是this，this指向的是一个对象， 这个对象我们称为函数执行的 上下文对象，根据函数的调用方式的不同，this会指向不同的对象，以方法的形式调用时，this就是调用方法的那个对象。当我们直接调用一个全局的函数的时候其实就是 window 对像上面的方法。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fun</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">   <span class="built_in">console</span>.log(<span class="keyword">this</span>.name);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//创建一个对象</span></span><br><span class="line"><span class="keyword">var</span> obj = &#123;</span><br><span class="line">   name:<span class="string">"孙悟空"</span>,</span><br><span class="line">   sayName:fun</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> obj2 = &#123;</span><br><span class="line">   name:<span class="string">"沙和尚"</span>,</span><br><span class="line">   sayName:fun</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> name = <span class="string">"全局的name属性"</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">//以函数形式调用，this是window</span></span><br><span class="line"><span class="comment">//fun();</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//以方法的形式调用，this是调用方法的对象</span></span><br><span class="line"><span class="comment">//obj.sayName();</span></span><br><span class="line">obj2.sayName();</span><br></pre></td></tr></table></figure>

<h2 id="2-构造函数"><a href="#2-构造函数" class="headerlink" title="2. 构造函数"></a>2. 构造函数</h2><p>创建一个构造函数，专门用来创建Person对象的， 构造函数就是一个普通的函数，创建方式和普通函数没有区别,不同的是构造函数习惯上首字母大写构造函数和普通函数的区别就是调用方式的不同，普通函数是直接调用，而构造函数需要使用new关键字来调用<br>构造函数的执行流程：</p>
<ol>
<li>立刻创建一个新的对象</li>
<li>将新建的对象设置为函数中this,在构造函数中可以使用this来引用新建</li>
<li>逐行执行函数中的代码</li>
<li>将新建的对象作为返回值返回</li>
</ol>
<p>使用同一个构造函数创建的对象，我们称为一类对象，也将一个构造函数称为一个类。我们将通过一个构造函数创建的对象，称为是该类的实例。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Person</span>(<span class="params">name , age , gender</span>)</span>&#123;</span><br><span class="line">   <span class="keyword">this</span>.name = name;</span><br><span class="line">   <span class="keyword">this</span>.age = age;</span><br><span class="line">   <span class="keyword">this</span>.gender = gender;</span><br><span class="line">   <span class="keyword">this</span>.sayName = <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">      alert(<span class="keyword">this</span>.name);</span><br><span class="line">   &#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">Dog</span>(<span class="params"></span>)</span>&#123;</span><br><span class="line">   </span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> per = <span class="keyword">new</span> Person(<span class="string">"孙悟空"</span>,<span class="number">18</span>,<span class="string">"男"</span>);</span><br><span class="line"><span class="keyword">var</span> per2 = <span class="keyword">new</span> Person(<span class="string">"玉兔精"</span>,<span class="number">16</span>,<span class="string">"女"</span>);</span><br><span class="line"><span class="keyword">var</span> per3 = <span class="keyword">new</span> Person(<span class="string">"奔波霸"</span>,<span class="number">38</span>,<span class="string">"男"</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> dog = <span class="keyword">new</span> Dog();</span><br></pre></td></tr></table></figure>

<p>使用instanceof可以检查一个对象是否是一个类的实例语法：</p>
<blockquote>
<p>对象 instanceof 构造函数 </p>
</blockquote>
<p>如果是，则返回true，否则返回false</p>
<h2 id="3-原型"><a href="#3-原型" class="headerlink" title="3. 原型"></a>3. 原型</h2><p>​    我们所创建的每一个函数，解析器都会向函数中添加一个属性prototype，这个属性对应着一个对象，这个对象就是我们所谓的原型对象，如果函数作为普通函数调用prototype没有任何作用，当函数以构造函数的形式调用时，它所创建的对象中都会有一个隐含的属性，指向该构造函数的原型对象，我们可以通过<strong>proto</strong>来访问该属性<br>       原型对象就相当于一个公共的区域，所有同一个类的实例都可以访问到这个原型对象，我们可以将对象中共有的内容，统一设置到原型对象中。<br>​    当我们访问对象的一个属性或方法时，它会先在对象自身中寻找，如果有则直接使用，如果没有则会去原型对象中寻找，如果找到则直接使用。以后我们创建构造函数时，可以将这些对象共有的属性和方法，统一添加到构造函数的原型对象中，这样不用分别为每一个对象添加，也不会影响到全局作用域，就可以使每个对象都具有这些属性和方法了。</p>
<p>​    使用in检查对象中是否含有某个属性时，如果对象中没有但是原型中有，也会返回true。可以使用对象的<code>hasOwnProperty()</code> 来检查对象自身中是否含有该属性，使用该方法只有当对象自身中含有属性时，才会返回true。</p>
<p>​    原型对象也是对象，所以它也有原型，当我们使用一个对象的属性或方法时，会现在自身中寻找，自身中如果有，则直接使用，如果没有则去原型对象中寻找，如果原型对象中有，则使用， 如果没有则去原型的原型中寻找,直到找到Object对象的原型，Object对象的原型没有原型，如果在Object原型中依然没有找到，则返回 null。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="built_in">console</span>.log(mc.__proto__.__proto__.hasOwnProperty(<span class="string">"hasOwnProperty"</span>));</span><br></pre></td></tr></table></figure>

<p><img src="http://images.heniankj.com/20190109233612.png" alt></p>
<p>构造函数和让他的原型对象是互相引用的：</p>
<p><img src="http://images.heniankj.com/20190110002509.png" alt></p>
<h2 id="4-call-和-apply"><a href="#4-call-和-apply" class="headerlink" title="4. call 和 apply"></a>4. call 和 apply</h2><ul>
<li>这两个方法都是函数对象的方法，需要通过函数对象来调用</li>
<li>当对函数调用call()和apply()都会调用函数执行</li>
<li>在调用call()和apply()可以将一个对象指定为第一个参数<br> 此时这个对象将会成为函数执行时的this</li>
<li>call()方法可以将实参在对象之后依次传递</li>
<li>apply()方法需要将实参封装到一个数组中统一传递</li>
<li>this的情况：<br> 1.以函数形式调用时，this永远都是window<br> 2.以方法的形式调用时，this是调用方法的对象<br> 3.以构造函数的形式调用时，this是新创建的那个对象<br> 4.使用call和apply调用时，this是指定的那个对象</li>
</ul>
<h2 id="5-数组的方法"><a href="#5-数组的方法" class="headerlink" title="5. 数组的方法"></a>5. 数组的方法</h2><h3 id="1-slice"><a href="#1-slice" class="headerlink" title="1. slice()"></a>1. slice()</h3><ul>
<li><p>可以用来从数组提取指定元素</p>
</li>
<li><p>该方法不会改变元素数组，而是将截取到的元素封装到一个新数组中返回</p>
</li>
<li><p>参数：<br> 1.截取开始的位置的索引,包含开始索引<br> 2.截取结束的位置的索引,不包含结束索引</p>
<p> 第二个参数可以省略不写,此时会截取从开始索引往后的所有索引可以传递一个负值，如果传递一个负值，则从后往前计算  -1 倒数第一个  -2 倒数第二个</p>
</li>
</ul>
<h3 id="2-splice"><a href="#2-splice" class="headerlink" title="2.splice()"></a>2.splice()</h3><ul>
<li>可以用于删除数组中的指定元素</li>
<li>使用splice()会影响到原数组，会将指定元素从原数组中删除，并将被删除的元素作为返回值返回</li>
<li>参数：<br> 第一个，表示开始位置的索引<br> 第二个，表示删除的数量<br> 第三个及以后，可以传递一些新的元素，这些元素将会自动插入到开始位置索引前边</li>
</ul>
<h3 id="3-concat"><a href="#3-concat" class="headerlink" title="3.concat()"></a>3.concat()</h3><p>可以连接两个或多个数组，并将新的数组</p>
<ul>
<li>该方法不会对原数组产生影响</li>
</ul>
<h3 id="4-join"><a href="#4-join" class="headerlink" title="4. join()"></a>4. join()</h3><p>​    该方法可以将数组转换为一个字符串，该方法不会对原数组产生影响，而是将转换后的字符串作为结果返回，在join()中可以指定一个字符串作为参数，这个字符串将会成为数组中元素的连接符如果不指定连接符，则默认使用,作为连接符。</p>
<h3 id="5-reverse"><a href="#5-reverse" class="headerlink" title="5. reverse()"></a>5. reverse()</h3><p>​    该方法用来反转数组（前边的去后边，后边的去前边）该方法会直接修改原数组</p>
<h3 id="6-sort"><a href="#6-sort" class="headerlink" title="6.sort()"></a>6.sort()</h3><p>​    可以用来对数组中的元素进行排序也会影响原数组，默认会按照Unicode编码进行排序.</p>
<p>​    即使对于纯数字的数组，使用sort()排序时，也会按照Unicode编码来排序，所以对数字进排序时，可能会得到错误的结果。我们可以自己来指定排序的规则,我们可以在sort()添加一个回调函数，来指定排序规则。</p>
<h2 id="6-Date"><a href="#6-Date" class="headerlink" title="6. Date"></a>6. Date</h2><p>创建一个Date对象如果直接使用构造函数创建一个Date对象，则会封装为当前代码执行的时间。创建一个指定的时间对象需要在构造函数中传递一个表示时间的字符串作为参数日期的格式  月份/日/年 时:分:秒</p>
<h3 id="1-getDate"><a href="#1-getDate" class="headerlink" title="1. getDate()"></a>1. getDate()</h3><p>​    获取当前日期对象是几日。</p>
<h3 id="2-getDay"><a href="#2-getDay" class="headerlink" title="2. getDay()"></a>2. getDay()</h3><p>​    获取当前日期对象时周几会返回一个0-6的值 0 表示周日1表示周一</p>
<h3 id="3-getMonth"><a href="#3-getMonth" class="headerlink" title="3. getMonth()"></a>3. getMonth()</h3><p>​    获取当前时间对象的月份会返回一个0-11的值0 表示1月1 表示2月11 表示12月</p>
<h3 id="4-getFullYear"><a href="#4-getFullYear" class="headerlink" title="4.getFullYear()"></a>4.getFullYear()</h3><p>​    获取当前日期对象的年份</p>
<h3 id="5-getTime"><a href="#5-getTime" class="headerlink" title="5. getTime()"></a>5. getTime()</h3><p>​    获取当前日期对象的时间戳</p>
<h2 id="7-Math"><a href="#7-Math" class="headerlink" title="7. Math"></a>7. Math</h2><h3 id="1-Math-ceil"><a href="#1-Math-ceil" class="headerlink" title="1.Math.ceil()"></a>1.Math.ceil()</h3><ul>
<li>可以对一个数进行向上取整，小数位只有有值就自动进1</li>
</ul>
<h3 id="2-Math-floor"><a href="#2-Math-floor" class="headerlink" title="2.Math.floor()"></a>2.Math.floor()</h3><p>可以对一个数进行向下取整，小数部分会被舍掉</p>
<h3 id="3-Math-round"><a href="#3-Math-round" class="headerlink" title="3.Math.round()"></a>3.Math.round()</h3><p>可以对一个数进行四舍五入取整</p>
<h3 id="4-Math-random"><a href="#4-Math-random" class="headerlink" title="4.Math.random()"></a>4.Math.random()</h3><p>可以用来生成一个0-1之间的随机数</p>
<h2 id="8-事件冒泡"><a href="#8-事件冒泡" class="headerlink" title="8. 事件冒泡"></a>8. 事件冒泡</h2><p>​    所谓的冒泡指的就是事件的向上传导，当后代元素上的事件被触发时，其祖先元素的相同事件也会被触发。在开发中大部分情况冒泡都是有用的,如果不希望发生事件冒泡可以通过事件对象来取消冒泡。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">s1.onclick = <span class="function"><span class="keyword">function</span>(<span class="params">event</span>)</span>&#123;</span><br><span class="line">   event = event || <span class="built_in">window</span>.event;</span><br><span class="line">   alert(<span class="string">"我是span的单击响应函数"</span>);</span><br><span class="line">   <span class="comment">//取消冒泡</span></span><br><span class="line">   <span class="comment">//可以将事件对象的cancelBubble设置为true，即可取消冒泡</span></span><br><span class="line">   event.cancelBubble = <span class="literal">true</span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>

<h3 id="1-事件的委派"><a href="#1-事件的委派" class="headerlink" title="1. 事件的委派"></a>1. 事件的委派</h3><p>​    指将事件统一绑定给元素的共同的祖先元素，这样当后代元素上的事件触发时，会一直冒泡到祖先元素<br>从而通过祖先元素的响应函数来处理事件。事件委派是利用了冒泡，通过委派可以减少事件绑定的次数，提高程序的性能。</p>
<h3 id="2-事件绑定"><a href="#2-事件绑定" class="headerlink" title="2. 事件绑定"></a>2. 事件绑定</h3><p><code>addEventListener()</code> 通过这个方法也可以为元素绑定响应函数，参数：</p>
<ol>
<li>事件的字符串，不要on</li>
<li>回调函数，当事件触发时该函数会被调用</li>
<li>是否在捕获阶段触发事件，需要一个布尔值，一般都传false</li>
</ol>
<p>使用addEventListener()可以同时为一个元素的相同事件同时绑定多个这样的事件。当事件被触发时，响应函数将会按照函数的绑定顺序执行。</p>
<h3 id="3-事件传播"><a href="#3-事件传播" class="headerlink" title="3. 事件传播"></a>3. 事件传播</h3><p>将事件传播分成了三个阶段</p>
<ol>
<li>捕获阶段：在捕获阶段时从最外层的祖先元素，向目标元素进行事件的捕获，但是默认此时不会触发事件</li>
<li>目标阶段：事件捕获到目标元素，捕获结束开始在目标元素上触发事件</li>
<li>冒泡阶段：事件从目标元素向他的祖先元素传递，依次触发祖先元素上的事件</li>
</ol>
<p>如果希望在捕获阶段就触发事件，可以将addEventListener()的第三个参数设置为true，一般情况下我们不会希望在捕获阶段触发事件，所以这个参数一般都是false。</p>
<h2 id="8-数据类型"><a href="#8-数据类型" class="headerlink" title="8.数据类型"></a>8.数据类型</h2><p>undefined 指的是变量定义了但是没有赋值过，而 null 则是指他被赋值了，并且赋值为 null</p>
<h2 id="9-闭包"><a href="#9-闭包" class="headerlink" title="9. 闭包"></a>9. 闭包</h2><p>​    闭包在一个函数内部声明了一个内部函数，并且这个内部函数引用了外部函数的变量。</p>
<p>​    <strong>闭包：闭包是包含被引用变量的对象，或者简单的理解为嵌套的内部函数</strong></p>
<p>​    闭包在执行外部函数的时候就产生了，不用等到内部不函数执行。</p>
<h3 id="1-闭包的作用："><a href="#1-闭包的作用：" class="headerlink" title="1.闭包的作用："></a>1.闭包的作用：</h3><ol>
<li><p>使用函数内部的变量在函数执行完后, 仍然存活在内存中(延长了局部变量的生命周期</p>
</li>
<li><p>让函数外部可以操作(读写)到函数内部的数据(变量/函数)</p>
</li>
</ol>
<h3 id="2-生命周期"><a href="#2-生命周期" class="headerlink" title="2. 生命周期"></a>2. 生命周期</h3><ol>
<li>产生: 在嵌套内部函数定义执行完时就产生了(不是在调用)</li>
<li>死亡: 在嵌套的内部函数成为垃圾对象时</li>
</ol>
<h3 id="3-应用"><a href="#3-应用" class="headerlink" title="3. 应用"></a>3. 应用</h3><p>​    具有特定功能的js文件，将所有的数据和功能都封装在一个函数内部(私有的)，只向外暴露一个包信n个方法的对象或函数，模块的使用者, 只需要通过模块暴露的对象调用方法来实现对应的功能。</p>
<p>方案一：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">myModule</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">//私有数据</span></span><br><span class="line">  <span class="keyword">var</span> msg = <span class="string">'Hello'</span></span><br><span class="line">  <span class="comment">//操作数据的函数</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">doSomething</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'doSomething() '</span>+msg.toUpperCase())</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">doOtherthing</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'doOtherthing() '</span>+msg.toLowerCase())</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//向外暴露对象(给外部使用的方法)</span></span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    doSomething: doSomething,</span><br><span class="line">    doOtherthing: doOtherthing</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>方案二：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">(<span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="comment">//私有数据</span></span><br><span class="line">  <span class="keyword">var</span> msg = <span class="string">'My atguigu'</span></span><br><span class="line">  <span class="comment">//操作数据的函数</span></span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">doSomething</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'doSomething() '</span>+msg.toUpperCase())</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="function"><span class="keyword">function</span> <span class="title">doOtherthing</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">'doOtherthing() '</span>+msg.toLowerCase())</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="comment">//向外暴露对象(给外部使用的方法)</span></span><br><span class="line">  <span class="built_in">window</span>.myModule2 = &#123;</span><br><span class="line">    doSomething: doSomething,</span><br><span class="line">    doOtherthing: doOtherthing</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)()</span><br></pre></td></tr></table></figure>

<h3 id="4-缺点及解决方案"><a href="#4-缺点及解决方案" class="headerlink" title="4. 缺点及解决方案"></a>4. 缺点及解决方案</h3><h4 id="1-缺点"><a href="#1-缺点" class="headerlink" title="1.缺点"></a>1.缺点</h4><ul>
<li><p>函数执行完后, 函数内的局部变量没有释放, 占用内存时间会变长</p>
</li>
<li><p>容易造成内存泄露</p>
<h4 id="2-解决"><a href="#2-解决" class="headerlink" title="2.解决"></a>2.解决</h4></li>
<li><p>能不用闭包就不用</p>
</li>
<li><p>及时释放，将闭包设置为null </p>
</li>
</ul>
]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>JavaScript</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS3 从入门到精通</title>
    <url>/2019/01/08/FrontEnd/CSS3%20%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="1-选择器"><a href="#1-选择器" class="headerlink" title="1. 选择器"></a>1. 选择器</h2><h3 id="1-一些注意的点"><a href="#1-一些注意的点" class="headerlink" title="1. 一些注意的点"></a>1. 一些注意的点</h3><h4 id="1-love-amp-hate-lvht"><a href="#1-love-amp-hate-lvht" class="headerlink" title="1. love &amp; hate (lvht)"></a>1. love &amp; hate (lvht)</h4><p>​    这是什么意思呢？其实是我们在写 a 表的伪类的时候会有一些优先级的问题，如果说我们没有按照这个优先级写css 的话很可能会导致我们样式不生效或者说被覆盖的问题。具体来说就是 <code>link</code> , <code>visited</code>，<code>hover</code>，<code>target</code> 。</p>
<h4 id="2-nth-child-n"><a href="#2-nth-child-n" class="headerlink" title="2. nth-child(n)"></a>2. nth-child(n)</h4><p>​    注意这个的 n 是从 1 开始的而不是 0 开始的，另外如果我们写了 <code>#wrap p:nth-child(1)</code> 的含义是指 id 为 wrap 下面的第一个子元素，并且这个字元素必须是 <code>p</code> 才会应用样式。</p>
<p>​    同样的还有一个类似的 就是 <code>nth-of-type(n)</code>  这个其实和上面的很像只是他的要求稍微低一些。<code>#wrap p:nth-of-type(1)</code> 比如这个就是表示 <code>wrap</code> 下面的第一个 <code>p</code> 标签。</p>
<p>​    上面两个的重要的区别(坑！)。就是 <code>nth-of-type</code> 是以元素标签为中心的，下面看个例子来理解这句话。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">			*&#123;</span><br><span class="line">				margin: 0;</span><br><span class="line">				padding: 0;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">             <span class="selector-id">#wrap</span> <span class="selector-class">.inner</span><span class="selector-pseudo">:nth-of-type(1)</span>&#123;</span></span><br><span class="line">				border: 1px solid;</span><br><span class="line">			&#125;</span><br><span class="line">			/*#wrap div:nth-of-type(1)&#123;</span><br><span class="line">				border: 1px solid;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#wrap</span> <span class="selector-tag">p</span><span class="selector-pseudo">:nth-of-type(1)</span>&#123;</span></span><br><span class="line">				border: 1px solid;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#wrap</span> <span class="selector-tag">span</span><span class="selector-pseudo">:nth-of-type(1)</span>&#123;</span></span><br><span class="line">				border: 1px solid;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#wrap</span> <span class="selector-tag">h1</span><span class="selector-pseudo">:nth-of-type(1)</span>&#123;</span></span><br><span class="line">				border: 1px solid;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#wrap</span> <span class="selector-tag">h2</span><span class="selector-pseudo">:nth-of-type(1)</span>&#123;</span></span><br><span class="line">				border: 1px solid;</span><br><span class="line">			&#125;*/</span><br><span class="line">			</span><br><span class="line"><span class="css">			<span class="comment">/*nth-of-type以元素为中心*/</span></span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"wrap"</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"inner"</span>&gt;</span>div<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">p</span> <span class="attr">class</span>=<span class="string">"inner"</span>&gt;</span>p<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"inner"</span>&gt;</span>span<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">h1</span> <span class="attr">class</span>=<span class="string">"inner"</span>&gt;</span>h1<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">h2</span> <span class="attr">class</span>=<span class="string">"inner"</span>&gt;</span>h2<span class="tag">&lt;/<span class="name">h2</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>​    注意上面的代码看起来就是第一个被选中，但是由于它是以元素为中心的那么，他会被展开成下面的语法结构。所以说所有的都会被选中。而 <code>nth-child</code> 则是正常的选中第一个。</p>
<h2 id="2-字体图标"><a href="#2-字体图标" class="headerlink" title="2. 字体图标"></a>2. 字体图标</h2><p>​    字体图标减少了页面的请求，而且不失真非常可靠，推荐使用。如果我们需要 使用一些特殊的紫图需要自定义字体因为为了兼容不同的用户，但是这样会增加网络负担。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@font-face&#123;</span><br><span class="line">    font-family:&quot;test&quot;;</span><br><span class="line">    src:url(&apos;&apos;);/*字体文件的位置*/</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    使用字体图标，主要就是ttf，wof还有一个样式表，直接引入样式表就可以使用了。</p>
<h2 id="3-新的UI方案"><a href="#3-新的UI方案" class="headerlink" title="3. 新的UI方案"></a>3. 新的UI方案</h2><h3 id="1-文本新增样式"><a href="#1-文本新增样式" class="headerlink" title="1. 文本新增样式"></a>1. 文本新增样式</h3><h4 id="1-opactiy"><a href="#1-opactiy" class="headerlink" title="1. opactiy"></a>1. opactiy</h4><p>​    他不是继承属性。但是子元素也会被透明掉。</p>
<h4 id="2-rgba"><a href="#2-rgba" class="headerlink" title="2. rgba"></a>2. rgba</h4><p>​    这个前三个就是 rgb 最后一个是透明度。正式有了这个现在的文字透明背景不透明或者背景透明文字不透明就好做了，直接给某一方设置rgba而另外一方设置正常的颜色就好。而不用opacity导致前后后透明。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">a</span>&#123;</span><br><span class="line">    <span class="attribute">color</span>:<span class="built_in">rgba</span>(1,1,1,.5);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    创建一个模糊背景的图片。首先需要有一个图片背景(让图片做背景一般都是图片有多大背景就能看到多少图片，为了让图片完全的铺满背景需要把 background-size:100% 100%)，然后需要对他做一个模糊使用了一个函数 filter:blur(10px) 就是把它模糊掉。然后需要一个外面的黑色遮罩，让他出现半透明的状态就可以看到后面的内容了。</p>
<p>​    *<em>小技巧：如果希望我们内层的元素铺满外层的内容区可以使用绝对定位然后让他的四个方向都为 0 *</em></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"width=device-width,initial-scale=1.0,user-scalable=no"</span> /&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">         *&#123;</span><br><span class="line">            margin: 0;</span><br><span class="line">            padding: 0;</span><br><span class="line">         &#125;</span><br><span class="line"><span class="css">         <span class="selector-id">#wrap</span>&#123;</span></span><br><span class="line">            height: 100px;</span><br><span class="line"><span class="css">            <span class="selector-tag">background</span>: <span class="selector-tag">rgba</span>(0,0,0,<span class="selector-class">.5</span>);</span></span><br><span class="line">            position: relative;</span><br><span class="line">         &#125;</span><br><span class="line"><span class="css">         <span class="selector-id">#wrap</span> <span class="selector-id">#bg</span>&#123;</span></span><br><span class="line">            position: absolute;</span><br><span class="line">            left: 0;</span><br><span class="line">            right: 0;</span><br><span class="line">            top: 0;</span><br><span class="line">            bottom: 0;</span><br><span class="line">            background: url(img/avatar.jpg) no-repeat;</span><br><span class="line"><span class="css">            <span class="selector-tag">background-size</span><span class="selector-pseudo">:100</span>% 100% ;</span></span><br><span class="line">            z-index: -1;</span><br><span class="line">            filter: blur(10px);</span><br><span class="line">         &#125;</span><br><span class="line">         img&#123;</span><br><span class="line">            margin: 24px 0 0 24px;</span><br><span class="line">         &#125;</span><br><span class="line">      <span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"wrap"</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">"img/avatar.jpg"</span> <span class="attr">width</span>=<span class="string">"64px"</span> <span class="attr">height</span>=<span class="string">"64px"</span>/&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"bg"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="3-文字阴影"><a href="#3-文字阴影" class="headerlink" title="3. 文字阴影"></a>3. 文字阴影</h4><p>text-shadow用来为文字添加阴影，而且可以添加多层，阴影值之间用逗号隔开。（多个阴影时，第一个阴影在最上边）</p>
<p>默认值：none        不可继承 </p>
<p>值<br>​    <color><br>​       可选。可以在偏移量之前或之后指定。如果没有指定颜色，则使用UA（用户代理）选择的颜色。<br>​    <offset-x> <offset-y><br>​       必选。这些长度值指定阴影相对文字的偏移量。<br>​       <offset-x> 指定水平偏移量，若是负值则阴影位于文字左边。<br>​       <offset-y> 指定垂直偏移量，若是负值则阴影位于文字上面。<br>​       如果两者均为0，则阴影位于文字正后方(如果设置了<blur- radius> 则会产生模糊效果)。<br>​    <blur-radius><br>​       可选。这是 <length> 值。如果没有指定，则默认为0。<br>​       值越大，模糊半径越大，阴影也就越大越淡    </length></blur-radius></blur-></offset-y></offset-x></offset-y></offset-x></color></p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">h1</span>&#123;</span><br><span class="line">    <span class="attribute">text-shadow</span>:gray <span class="number">10px</span> <span class="number">10px</span> <span class="number">10px</span>,pink <span class="number">10px</span> <span class="number">10px</span> <span class="number">10px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="4-文字排版"><a href="#4-文字排版" class="headerlink" title="4. 文字排版"></a>4. 文字排版</h4><p>​    溢出显示省略号，最常用的！但是使用这个东西盒子需要是一个 block</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE HTML&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"utf-8"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">title</span>&gt;</span>无标题文档<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">style</span>&gt;</span></span><br><span class="line">	div&#123;</span><br><span class="line">		width: 200px;</span><br><span class="line">		height: 200px;</span><br><span class="line">		border: 1px solid;</span><br><span class="line">		margin: 0 auto;</span><br><span class="line"><span class="css">        <span class="comment">/*这三样式是一直在一起的不能去掉，并且盒子不能靠内容撑开*/</span></span></span><br><span class="line">		white-space: nowrap;</span><br><span class="line">		overflow: hidden;</span><br><span class="line">		text-overflow: ellipsis;</span><br><span class="line">	&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">div</span>&gt;</span>aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-盒模型阴影"><a href="#2-盒模型阴影" class="headerlink" title="2. 盒模型阴影"></a>2. 盒模型阴影</h3><p>​    首先是需要进行一个盒子的居中：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">div</span>&#123;</span><br><span class="line">    <span class="attribute">position</span>: absolute;</span><br><span class="line">    <span class="attribute">left</span>: <span class="number">0</span>;</span><br><span class="line">    <span class="attribute">right</span>: <span class="number">0</span>;</span><br><span class="line">    <span class="attribute">bottom</span>: <span class="number">0</span>;</span><br><span class="line">    <span class="attribute">top</span>: <span class="number">0</span>;</span><br><span class="line">    <span class="attribute">margin</span>: auto;</span><br><span class="line">    <span class="attribute">width</span>: <span class="number">100px</span>;</span><br><span class="line">    <span class="attribute">height</span>: <span class="number">100px</span>;</span><br><span class="line">    <span class="attribute">background</span>: pink;</span><br><span class="line">    <span class="attribute">text-align</span>: center;</span><br><span class="line">    <span class="attribute">line-height</span>: <span class="number">100px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    注意需要margin为auto而不能为0，盒子唏嘘也要有长宽。</p>
<p>box-shadow<br>​    以逗号分割列表来描述一个或多个阴影效果，可以用到几乎任何元素上。 如果元素同时设置了 border-radius ，阴影也会有圆角效果。多个阴影时和多个 text shadows 规则相同(第一个阴影在最上面)。</p>
<p>默认值:  none    不可继承</p>
<p>值：<br>​    inset<br>​       默认阴影在边框外。<br>​       使用inset后，阴影在边框内。<br>​    <offset-x> <offset-y><br>​       这是头两个 <length> 值，用来设置阴影偏移量。<br>​        <offset-x> 设置水平偏移量，如果是负值则阴影位于元素左边。<br>​        <offset-y> 设置垂直偏移量，如果是负值则阴影位于元素上面。<br>​       如果两者都是0，那么阴影位于元素后面。<br>​       这时如果设置了<blur-radius> 或<spread-radius> 则有模糊效果。<br>​    <blur-radius><br>​       这是第三个 <length> 值。值越大，模糊面积越大，阴影就越大越淡。<br>​       不能为负值。默认为0，此时阴影边缘锐利。<br>​    <spread-radius><br>​       这是第四个 <length> 值。取正值时，阴影扩大；取负值时，阴影.收缩。默认为0，此时阴影与元素同样大。<br>​    <color><br>​       阴影颜色，如果没有指定，则由浏览器决定</color></length></spread-radius></length></blur-radius></spread-radius></blur-radius></offset-y></offset-x></length></offset-y></offset-x></p>
 <figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">div</span>&#123;</span><br><span class="line">    <span class="attribute">box-shadow</span>: -<span class="number">10px</span> -<span class="number">10px</span> <span class="number">10px</span> <span class="number">0px</span> black , <span class="number">20px</span> <span class="number">20px</span> <span class="number">10px</span> -<span class="number">10px</span> deeppink;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-box-sizing"><a href="#3-box-sizing" class="headerlink" title="3. box-sizing"></a>3. box-sizing</h3><p>​    由于我们一般的额 height 和 width 的值指的是内容区的大小，所以如果我们用了 padding的话会导致整个盒子的变化，那么为了不让这种计算出现，可以使用 <code>box-sizing:border-box</code> 就可以了。此时采用padding的话就是从内容区扣掉的。 </p>
<h3 id="4-border-radius"><a href="#4-border-radius" class="headerlink" title="4. border-radius"></a>4. border-radius</h3><p>​    圆角</p>
<h3 id="5-背景"><a href="#5-背景" class="headerlink" title="5. 背景"></a>5. 背景</h3><ul>
<li><p>background-color 默认为transparent 也就是透明的</p>
</li>
<li><p>background-image 指定图片做背景，有的图片在z轴上面绘制，先写得图片会在最后绘制也就是层叠图片的时候是按照写得顺序从上往下绘制的。</p>
</li>
<li><p>no-repeat</p>
</li>
<li><p>background-position 如果图片的大小大于框的大小，这个属性就可以决定图片是从哪开始显示的。</p>
</li>
<li><p>background-origin 表示图片是从哪开始渲染的，它有三个值：border-box ,padding-box ,content-box 分别就是从边框，内边距开始的地方，内容区开始的地方。</p>
</li>
<li><p>background-clip:从那个位置开始裁剪，属性值也是和origin一样的。</p>
</li>
<li><p>background-size：百分比：  指定背景图片相对背景区（background positioning area）的百分比。背景区由background-origin设置，默认为盒模型的内容区与内边距 auto：  以背景图片的比例缩放背景图片。</p>
<p>注意：<br>  单值时，这个值指定图片的宽度，图片的高度隐式的为auto<br>  两个值: 第一个值指定图片的宽度，第二个值指定图片的高度     </p>
</li>
</ul>
<h3 id="6-线性渐变"><a href="#6-线性渐变" class="headerlink" title="6. 线性渐变"></a>6. 线性渐变</h3><p>​    <strong>注意：渐变是针对于图片的而不是颜色。</strong><br>       为了创建一个线性渐变，你需要设置一个起始点和一个方向（指定为一个角度）。你还要定义终止色。终止色就是你想让浏览器去平滑的过渡过去，并且你必须指定至少两种，当然也会可以指定更多的颜色去创建更复杂的渐变效果。</p>
<p>​    如果在里面设置了透明度他也会跟着渐变的，就是 rgba</p>
<p>-默认从上到下发生渐变<br>​              linear-gradient(red,blue);</p>
<p>-改变渐变方向：（top bottom left right）<br>​              linear-gradient(to 结束的方向,red,blue);</p>
<p>-使用角度<br>​              linear-gradient(角度,red,blue);</p>
<p>-颜色节点的分布（第一个不写为0%，最后一个不写为100%）<br>​            linear-gradient(red 长度或者百分比,blue 长度或者百分比);<br>-重复渐变<br>​           repeating-linear-gradient(60deg,red 0,blue 30%);</p>
 <figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">p</span>&#123;</span><br><span class="line">    <span class="attribute">background</span>:<span class="built_in">repeating-linear-gradient</span>(135deg,black 0px,black 10px,white 10px,white 20px);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意这个写法他的意思就是1-10是黑到黑的渐变就是一个完整的黑色，而10-20 是白到白的渐变。为什么要这么做，主要就是我们使用了 <code>repeating-linear-gradient</code>  这个 repeat 就是重复的渐变，如果我们不是设置的渐变而是直接的纯色的话他就不会重复了。所以这种方式巧妙的让这些黑白重复了。发廊灯的例子：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">			*&#123;</span><br><span class="line">				margin: 0;</span><br><span class="line">				padding: 0;</span><br><span class="line">			&#125;</span><br><span class="line">			</span><br><span class="line">			html,body&#123;</span><br><span class="line">				height: 100%;</span><br><span class="line">				overflow: hidden;</span><br><span class="line">			&#125;</span><br><span class="line">			</span><br><span class="line"><span class="css">			<span class="selector-id">#wrap</span>&#123;</span></span><br><span class="line">				width: 40px;</span><br><span class="line">				height: 300px;</span><br><span class="line">				border: 1px solid;</span><br><span class="line">				margin: 100px auto;</span><br><span class="line">				overflow: hidden;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#wrap</span> &gt; <span class="selector-class">.inner</span>&#123;</span></span><br><span class="line">				height: 600px;</span><br><span class="line"><span class="css">				<span class="selector-tag">background</span><span class="selector-pseudo">:repeating-linear-gradient(135deg</span>,<span class="selector-tag">black</span> 0<span class="selector-tag">px</span>,<span class="selector-tag">black</span> 10<span class="selector-tag">px</span>,<span class="selector-tag">white</span> 10<span class="selector-tag">px</span>,<span class="selector-tag">white</span> 20<span class="selector-tag">px</span>);</span></span><br><span class="line">			&#125;</span><br><span class="line">			</span><br><span class="line">		<span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"wrap"</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"inner"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="javascript">		<span class="keyword">var</span> inner = <span class="built_in">document</span>.querySelector(<span class="string">"#wrap &gt; .inner"</span>);</span></span><br><span class="line"><span class="actionscript">		<span class="keyword">var</span> flag =<span class="number">0</span>;</span></span><br><span class="line">		</span><br><span class="line"><span class="actionscript">		setInterval(<span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line">			flag++;</span><br><span class="line"><span class="actionscript">			<span class="keyword">if</span>(flag==<span class="number">300</span>)&#123;</span></span><br><span class="line">				flag=0;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="actionscript">			inner.style.marginTop = -flag+<span class="string">"px"</span>;</span></span><br><span class="line">		&#125;,1000/60)</span><br><span class="line">		</span><br><span class="line">	<span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>光板动画，iPhone的开机动画：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">			*&#123;</span><br><span class="line">				margin: 0;</span><br><span class="line">				padding: 0;</span><br><span class="line">			&#125;</span><br><span class="line">			</span><br><span class="line">			html,body&#123;</span><br><span class="line">				height: 100%;</span><br><span class="line">				overflow: hidden;</span><br><span class="line">				background: black;</span><br><span class="line">				text-align: center;</span><br><span class="line">			&#125;</span><br><span class="line">			</span><br><span class="line">			h1&#123;</span><br><span class="line"><span class="css">				<span class="comment">/*transition: 3s;*/</span></span></span><br><span class="line">				margin-top: 50px;</span><br><span class="line">				display: inline-block;</span><br><span class="line"><span class="css">				<span class="selector-tag">color</span>: <span class="selector-tag">rgba</span>(255, 255, 255,<span class="selector-class">.3</span>);</span></span><br><span class="line"><span class="css">				<span class="selector-tag">font</span><span class="selector-pseudo">:bold</span> 80<span class="selector-tag">px</span> "微软雅黑";</span></span><br><span class="line">				background: linear-gradient(120deg,rgba(255,255,255,0) 100px ,rgba(255,255,255,1) 180px ,rgba(255,255,255,0) 260px);</span><br><span class="line"><span class="css">				<span class="selector-tag">background-repeat</span><span class="selector-pseudo">:no-repeat</span> ;</span></span><br><span class="line">				-webkit-background-clip: text ;</span><br><span class="line">			&#125;</span><br><span class="line">			</span><br><span class="line">			/*h1:hover&#123;</span><br><span class="line">				background-position: 500px 0;</span><br><span class="line">			&#125;*/</span><br><span class="line">		<span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">h1</span>&gt;</span>atguigu尚硅谷<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="javascript">		<span class="keyword">var</span> h1 = <span class="built_in">document</span>.querySelector(<span class="string">"h1"</span>);</span></span><br><span class="line"><span class="actionscript">		<span class="keyword">var</span> flag =<span class="number">-160</span>;</span></span><br><span class="line">		</span><br><span class="line"><span class="actionscript">		setInterval(<span class="function"><span class="keyword">function</span><span class="params">()</span></span>&#123;</span></span><br><span class="line">			flag+=10;</span><br><span class="line"><span class="actionscript">			<span class="keyword">if</span>(flag==<span class="number">600</span>)&#123;</span></span><br><span class="line">				flag=-160;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="actionscript">			h1.style.backgroundPosition = flag+<span class="string">"px"</span>;</span></span><br><span class="line">		&#125;,30)</span><br><span class="line">		</span><br><span class="line">	<span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="7-径向渐变"><a href="#7-径向渐变" class="headerlink" title="7. 径向渐变"></a>7. 径向渐变</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">         *&#123;</span><br><span class="line">            margin: 0;</span><br><span class="line">            padding: 0;</span><br><span class="line">         &#125;</span><br><span class="line"><span class="css">         <span class="selector-id">#test</span>&#123;</span></span><br><span class="line">            width: 400px;</span><br><span class="line">            height: 300px;</span><br><span class="line">            border: 1px solid;</span><br><span class="line">            margin: 0 auto;</span><br><span class="line"><span class="css">            <span class="selector-tag">background-image</span><span class="selector-pseudo">:radial-gradient(</span> <span class="selector-tag">closest-corner</span> <span class="selector-tag">circle</span> <span class="selector-tag">at</span> 20<span class="selector-tag">px</span> 20<span class="selector-tag">px</span>,<span class="selector-tag">yellow</span>, <span class="selector-tag">green</span> 50%,<span class="selector-tag">pink</span>) ;</span></span><br><span class="line">         &#125;</span><br><span class="line">      <span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"test"</span>&gt;</span></span><br><span class="line">         </span><br><span class="line">      <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="4-过渡"><a href="#4-过渡" class="headerlink" title="4.过渡"></a>4.过渡</h2><h3 id="1-transition-property"><a href="#1-transition-property" class="headerlink" title="1. transition-property"></a>1. transition-property</h3><p>​    这个属性是用来指定哪些属性需要做渐变的，而且并不是所有的属性都是可以做渐变的，可以在mdn上查询到。多个属性可以采用逗号分隔。默认值就是 all</p>
<h3 id="2-transition-duration"><a href="#2-transition-duration" class="headerlink" title="2. transition-duration"></a>2. transition-duration</h3><p>​    这个用来规定动画间隔的时间，注意如果这个列表不满足上面的property的个数，他默认采用复制来匹配，比如上面有三个属性，而duration只有两个时间 3s,5s 那么最后的结果应该是 3s.5s.3s,5s 这样上面三个属性是 3s,5s ,3s 注意点就是他们需要带上单位。</p>
<h3 id="3-transition-timing-function"><a href="#3-transition-timing-function" class="headerlink" title="3. transition-timing-function"></a>3. transition-timing-function</h3><p>​    这个就是用来指定整个动画的运行的过程或者速率：</p>
<p>属性值：<br>​         1、ease：（加速然后减速）默认值，ease函数等同于贝塞尔曲线(0.25, 0.1, 0.25, 1.0).<br>​         2、linear：（匀速），linear 函数等同于贝塞尔曲线(0.0, 0.0, 1.0, 1.0).<br>​         3、ease-in：(加速)，ease-in 函数等同于贝塞尔曲线(0.42, 0, 1.0, 1.0).<br>​         4、ease-out：（减速），ease-out 函数等同于贝塞尔曲线(0, 0, 0.58, 1.0).<br>​         5、ease-in-out：（加速然后减速），ease-in-out 函数等同于贝塞尔曲线(0.42, 0, 0.58, 1.0)<br>​         6、cubic-bezier： 贝塞尔曲线，这是一个函数里面传入对应的参数就可以呈现出不同的曲线    </p>
<p>​        <a href="http://cubic-bezier.com" target="_blank" rel="noopener">http://cubic-bezier.com</a> 在这个地址里面就能找到对应的被萨尔曲线对应的函数值和参数了。</p>
<p>比如说下面的这个曲线的意思就是横轴代表的是时间而纵轴代表的就是距离或者位置了，或者说速度。在这给对方代表的就是速度。</p>
<p><img src="http://images.heniankj.com/20190109144545.png" alt></p>
<p>​         7、step-start：等同于steps(1,start)<br>​              step-end：等同于steps(1,end)    </p>
<p>​            start表示整个时间的开始，而end表示整个时间的结束。</p>
<p>​          steps(<integer>,[,[start|end]]?)<br>​                      第一个参数：必须为正整数，指定函数的步数<br>​                      第二个参数：指定每一步的值发生变化的时间点（默认值end）在每步的时间开始还是结束的时候执行。</integer></p>
<p>​        多个列表会使用默认值，如果不够的话。</p>
<h3 id="4-transition-delay"><a href="#4-transition-delay" class="headerlink" title="4.  transition-delay"></a>4.  transition-delay</h3><p>​    表示在多久之后才开始动画。这个如果缺省了参数不够列表的长度的话也是会重复列表的。</p>
<h3 id="5-transitionend"><a href="#5-transitionend" class="headerlink" title="5. transitionend"></a>5. transitionend</h3><p>​    表示动画执行结束，这是一个js事件可以使用js事件来监听，他是基于属性的一个事件，也就是等一个属性变化完成以后他就会调用一次而不是针对于元素的。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">node.addEventListener(<span class="string">"transitionend"</span>,<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;); <span class="comment">//这种添加事件的方式是 dom2 事件</span></span><br><span class="line">node.ontransitionend(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;&#125;) <span class="comment">//这种是 dom0 事件</span></span><br></pre></td></tr></table></figure>

<h3 id="6-过渡的一些坑"><a href="#6-过渡的一些坑" class="headerlink" title="6. 过渡的一些坑"></a>6. 过渡的一些坑</h3><h4 id="1-浏览器解析"><a href="#1-浏览器解析" class="headerlink" title="1. 浏览器解析"></a>1. 浏览器解析</h4><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">         *&#123;</span><br><span class="line">            margin: 0;</span><br><span class="line">            padding: 0;</span><br><span class="line">         &#125;</span><br><span class="line">      </span><br><span class="line">         html&#123;</span><br><span class="line">            height: 100%;</span><br><span class="line">         &#125;</span><br><span class="line">         body&#123;</span><br><span class="line">            width: 60%;</span><br><span class="line">            height: 60%;</span><br><span class="line">            border: 1px solid;</span><br><span class="line">            margin: 100px auto 0;</span><br><span class="line">         &#125;</span><br><span class="line">      </span><br><span class="line"><span class="css">         <span class="selector-id">#test</span>&#123;</span></span><br><span class="line">            width: 100px;</span><br><span class="line">            height: 100px;</span><br><span class="line">            background: pink;</span><br><span class="line">            text-align: center;</span><br><span class="line">            position: absolute;</span><br><span class="line">            left: 0;</span><br><span class="line">            right: 0;</span><br><span class="line">            bottom: 0;</span><br><span class="line">            top: 0;</span><br><span class="line">            margin: auto;</span><br><span class="line">            </span><br><span class="line">            transition-property: width;</span><br><span class="line">            transition-duration: 2s;</span><br><span class="line">            transition-timing-function: linear;</span><br><span class="line">         &#125;</span><br><span class="line">         </span><br><span class="line"><span class="css">         <span class="selector-tag">body</span><span class="selector-pseudo">:hover</span> <span class="selector-id">#test</span>&#123;</span></span><br><span class="line">            transition-property: height;</span><br><span class="line">            width: 200px;</span><br><span class="line">            height: 200px;</span><br><span class="line">         &#125;</span><br><span class="line">         </span><br><span class="line">      <span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"test"</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以看到上面是鼠标画上去的时候回选中 test 这个元素，然后将它动画设置为 height 变化，由于浏览器解析 css 太快了，在划上去的瞬间内存被修改了也就是变换的元素被修改了所以一开始变化的应该是height。而移除的时候同理变化的是 width</p>
<h4 id="2-transition在元素首次渲染还没有结束的情况下是不会触发的"><a href="#2-transition在元素首次渲染还没有结束的情况下是不会触发的" class="headerlink" title="2. transition在元素首次渲染还没有结束的情况下是不会触发的"></a>2. transition在元素首次渲染还没有结束的情况下是不会触发的</h4><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">			*&#123;</span><br><span class="line">				margin: 0;</span><br><span class="line">				padding: 0;</span><br><span class="line">			&#125;</span><br><span class="line">		</span><br><span class="line">			html&#123;</span><br><span class="line">				height: 100%;</span><br><span class="line">			&#125;</span><br><span class="line">			body&#123;</span><br><span class="line">				width: 60%;</span><br><span class="line">				height: 60%;</span><br><span class="line">				border: 1px solid;</span><br><span class="line">				margin: 100px auto 0;</span><br><span class="line">			&#125;</span><br><span class="line">		</span><br><span class="line"><span class="css">			<span class="selector-id">#test</span>&#123;</span></span><br><span class="line">				width: 100px;</span><br><span class="line">				height: 100px;</span><br><span class="line">				background: pink;</span><br><span class="line">				text-align: center;</span><br><span class="line">				position: absolute;</span><br><span class="line">				left: 0;</span><br><span class="line">				right: 0;</span><br><span class="line">				bottom: 0;</span><br><span class="line">				top: 0;</span><br><span class="line">				margin: auto;</span><br><span class="line">				</span><br><span class="line">				transition-property: width;</span><br><span class="line">				transition-duration: 2s;</span><br><span class="line">				transition-timing-function: linear;</span><br><span class="line">			&#125;</span><br><span class="line">		<span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"test"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span>&gt;</span></span><br><span class="line"><span class="actionscript">		<span class="comment">//transition在元素首次渲染还没有结束的情况下是不会被触发的	</span></span></span><br><span class="line"><span class="actionscript"><span class="comment">//		window.onload=function()&#123;</span></span></span><br><span class="line"><span class="javascript">			<span class="keyword">var</span> test = <span class="built_in">document</span>.querySelector(<span class="string">"#test"</span>);</span></span><br><span class="line"><span class="actionscript">			test.style.width=<span class="string">"300px"</span>;</span></span><br><span class="line"><span class="actionscript"><span class="comment">//		&#125;</span></span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>​    如果没有打开注释部分的内容我们的动画是不会执行的，而是直接跳到300的样子，如果我们打开了注释意外这我们所有的元素已经渲染完了，这个时候再去做上面的额动画是会被执行的。</p>
<p>​    那么这么想的话我们如果加上一个 settimeout 也是没问题的。</p>
<h4 id="3-坑"><a href="#3-坑" class="headerlink" title="3.坑"></a>3.坑</h4><p><strong>1. 在绝大部分变换样式切换时,如果变换函数的位置 个数不相同也不会触发过渡</strong></p>
<p><strong>2. transition必须在transform之后声明才有效！！！！</strong></p>
<h2 id="5-2D变幻"><a href="#5-2D变幻" class="headerlink" title="5. 2D变幻"></a>5. 2D变幻</h2><p><strong>注意所有的变幻都是和transition配合使用的。</strong></p>
<h3 id="1-transform"><a href="#1-transform" class="headerlink" title="1. transform"></a>1. transform</h3><h4 id="1-rotate-angle-旋转"><a href="#1-rotate-angle-旋转" class="headerlink" title="1.rotate(angle)  旋转"></a>1.rotate(angle)  旋转</h4><p>   正值:顺时针旋转  rotate(360deg)<br>   负值:逆时针旋转  rotate(-360deg)<br>   只能设单值。正数表示顺时针旋转，负数表示逆时针旋转</p>
<h4 id="2-translate-移动"><a href="#2-translate-移动" class="headerlink" title="2. translate 移动"></a>2. translate 移动</h4><p>X方向平移:transform:  translateX(tx)<br>Y方向平移:transform:  translateY(ty)<br>二维平移：transform:  translate(tx[, ty])； 如果ty没有指定，它的值默认为0。</p>
<p>可设单值，也可设双值。<br>​       正数表示XY轴正向位移，负数为反向位移。设单值表示只X轴位移，Y轴坐标不变，<br>​       例如transform: translate(100px);等价于transform: translate(100px,0);</p>
<h4 id="3-skew-倾斜"><a href="#3-skew-倾斜" class="headerlink" title="3. skew 倾斜"></a>3. skew 倾斜</h4><p>transform:skewX(45deg);<br>​    X方向倾斜:transform:  skewX(angle)<br>​               skewX(45deg):参数值以deg为单位 代表与y轴之间的角度<br>​    Y方向倾斜:transform:  skewY(angle)<br>​               skewY(45deg):参数值以deg为单位 代表与x轴之间的角度<br>​     二维倾斜:transform:  skew(ax[, ay]);  如果ay未提供，在Y轴上没有倾斜<br>​               skew(45deg,15deg):参数值以deg为单位 第一个参数代表与y轴之间的角度<br>​                                                                        第二个参数代表与x轴之间的角度<br>单值时表示只X轴扭曲，Y轴不变，如transform: skew(30deg);等价于 transform: skew(30deg, 0);<br>考虑到可读性，不推荐用单值，应该用transform: skewX(30deg);。skewY表示只Y轴扭曲，X轴不变<br> 正值:拉正斜杠方向的两个角<br> 负值:拉反斜杠方向的两个角</p>
<h4 id="4-scale-缩放"><a href="#4-scale-缩放" class="headerlink" title="4. scale 缩放"></a>4. scale 缩放</h4><p>transform:scale(2);<br>  X方向缩放:transform:  scaleX(sx);<br>  Y方向缩放:transform:  scaleY(sy);<br>  二维缩放 :transform:  scale(sx[, sy]);  (如果sy 未指定，默认认为和sx的值相同)  </p>
<p>  要缩小请设0.01～0.99之间的值，要放大请设超过1的值。<br>  例如缩小一倍可以transform: scale(.5);放大一倍可以transform: scale(2);</p>
<p> 如果只想X轴缩放，可以用scaleX(.5)相当于scale(.5, 1)。<br> 同理只想Y轴缩放，可以用scaleY(.5)相当于scale(1, .5)</p>
<p> 正值:缩放的程度<br> 负值:不推荐使用（有旋转效果）<br> 单值时表示只X轴,Y轴上缩放粒度一样，如transform: scale(2);等价于transform: scale(2,2);</p>
<h3 id="2-transform-origin"><a href="#2-transform-origin" class="headerlink" title="2. transform-origin"></a>2. transform-origin</h3><p>参考点 transform-origin 这个值可以是：</p>
<p>top left 左上</p>
<p>10px 10px 参照左上角的向右向下10px</p>
<p>10% 10% 同px参照的是自身的宽高。</p>
<h2 id="6-3D-旋转"><a href="#6-3D-旋转" class="headerlink" title="6. 3D 旋转"></a>6. 3D 旋转</h2><h3 id="1-transform-1"><a href="#1-transform-1" class="headerlink" title="1. transform"></a>1. transform</h3><p>里面的一些函数和2D的一模一样只是多了 X,Y,Z的后缀例如 <code>rotateY(360deg)</code></p>
<h3 id="2-perspective"><a href="#2-perspective" class="headerlink" title="2. perspective"></a>2. perspective</h3><p>​    景深，让3D物体看起来有近大远小的感觉。但是这个属性只能用在舞台上，也就是如果要过三D动画，必须有两层外面一层叫做舞台，里面才是真实的物体。</p>
<h3 id="3-transform-style"><a href="#3-transform-style" class="headerlink" title="3. transform-style"></a>3. transform-style</h3><p>建立有层级的 3d 舞台，他作用于舞台。</p>
<p>### </p>
<h2 id="7-动画"><a href="#7-动画" class="headerlink" title="7.动画"></a>7.动画</h2><h3 id="1-常用属性"><a href="#1-常用属性" class="headerlink" title="1. 常用属性"></a>1. 常用属性</h3><h4 id="1-animation-name"><a href="#1-animation-name" class="headerlink" title="1. animation-name"></a>1. animation-name</h4><p>​    指定所使用的关键帧的名字，比如 <code>animation-name: move;</code> 关键帧的样子：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">@<span class="keyword">keyframes</span> move&#123;</span><br><span class="line">   <span class="selector-tag">from</span>&#123;</span><br><span class="line">      <span class="attribute">transform</span>: <span class="built_in">translateY</span>(-100px);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="selector-tag">to</span>&#123;</span><br><span class="line">      <span class="attribute">transform</span>: <span class="built_in">translateY</span>(100px);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-animation-duration"><a href="#2-animation-duration" class="headerlink" title="2. animation-duration"></a>2. animation-duration</h4><p>​    设置动画持续的时间。需要带上单位  <code>animation-duration:3s ;</code></p>
<h4 id="3-animation-direction"><a href="#3-animation-direction" class="headerlink" title="3.animation-direction"></a>3.animation-direction</h4><p>​    动画翻转，他变化的是关键帧的 from 到 to 的两个顺序，并且变化了<code>animation-timing-function</code>  动画的速度变化曲线。</p>
<p>​    他有四个值：</p>
<ol>
<li>normal 就是正常的每一次执行动画都是从 from 到 to 这样的执行。</li>
<li>reverse 这样关键帧执行的顺序从 to 到 from ，并且会让 animation-timing-function`  动画的速度变化曲线翻转。</li>
<li>alternate 如果有多次启动关键帧，表示从 from到to再从to到from 这样一直往返。</li>
<li>alternate-reverse 同上，只是方向是反的。</li>
</ol>
<h4 id="4-animation-delay"><a href="#4-animation-delay" class="headerlink" title="4.animation-delay"></a>4.animation-delay</h4><p>​    设置动画还有多久才开始，需要设置单位 <code>animation-delay:1s;</code></p>
<h4 id="5-animation-iteration-count"><a href="#5-animation-iteration-count" class="headerlink" title="5. animation-iteration-count"></a>5. animation-iteration-count</h4><p>​    设置关键帧执行的次数，也就是动画执行的次数。 <code>animation-iteration-count: 3;</code></p>
<h4 id="6-animation-fill-mode"><a href="#6-animation-fill-mode" class="headerlink" title="6. animation-fill-mode"></a>6. animation-fill-mode</h4><p>​    设置动画之外的状态，就是规定from和to在动画开始之前的位置和状态。</p>
<ol>
<li>backwards：from之前的状态与form的状态保持一致</li>
<li>forwards：to之后的状态与to的状态保持一致</li>
<li>both：backwards+forwards</li>
</ol>
<h4 id="7-animation-play-state"><a href="#7-animation-play-state" class="headerlink" title="7.animation-play-state"></a>7.animation-play-state</h4><p>​    表示当前的动画的状态，可以通过他来暂停动画和开始动画</p>
<ol>
<li>running 启动动画</li>
<li>paused 暂停动画</li>
</ol>
<h4 id="8-animation-timing-function"><a href="#8-animation-timing-function" class="headerlink" title="8. animation-timing-function"></a>8. animation-timing-function</h4><p>​    规定了动画的速率，具体和 transition 是一样的。</p>
<h3 id="2-关键帧"><a href="#2-关键帧" class="headerlink" title="2. 关键帧"></a>2. 关键帧</h3><p>语法：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">@<span class="keyword">keyframes</span> animiationName&#123;</span><br><span class="line">    <span class="selector-tag">keyframes-selector</span>&#123;</span><br><span class="line">        css-style;</span><br><span class="line">    &#125;</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<ol>
<li>animiationName:必写项，定义动画的名称</li>
<li>keyframes-selector：必写项，动画所在的时间点 from等价于 0% 而 to等价于100%，我么也可以定义其他的一些百分比。</li>
<li>css-style：css声明</li>
</ol>
<h2 id="8-flex"><a href="#8-flex" class="headerlink" title="8. flex"></a>8. flex</h2><p>​    flex分为两个版本，分别为老版本的和新版本的，新版本的功能更强大但是他不支持很多的老的移动端。但是我们可以使用css后置处理器来处理这种兼容性问题。</p>
<h3 id="1-基本概念"><a href="#1-基本概念" class="headerlink" title="1. 基本概念"></a>1. 基本概念</h3><p>容器：就是flex布局的容器，也是一个包裹层然后里面样式设置为 flex</p>
<p>项目：item 就是在容器中排列的内容就是项目，<strong>项目永远在主轴的正方向排列</strong></p>
<p>主轴：默认是x轴，正方向右</p>
<p>侧轴：默认y轴，正方向向下</p>
<h3 id="2-容器管理"><a href="#2-容器管理" class="headerlink" title="2. 容器管理"></a>2. 容器管理</h3><h4 id="1-设置容器"><a href="#1-设置容器" class="headerlink" title="1. 设置容器"></a>1. 设置容器</h4><p>老版本：<code>display：-webkit-box</code></p>
<p>新版本：<code>display：flex</code></p>
<h4 id="2-设置主轴"><a href="#2-设置主轴" class="headerlink" title="2. 设置主轴"></a>2. 设置主轴</h4><p>老版本：<code>-webkit-box-orient：vertical/horizontal</code></p>
<p>新版本：<code>flex-direction:row/column</code></p>
<h4 id="3-设置排列方向，控制主轴方向"><a href="#3-设置排列方向，控制主轴方向" class="headerlink" title="3. 设置排列方向，控制主轴方向"></a>3. 设置排列方向，控制主轴方向</h4><p>老版本：<code>-webkit-box-direction：normal/reverse</code></p>
<p>新版本：<code>flex-direction:row/column/row-reverse/column-reverse</code>  用一个属性设置主轴以及主轴的方向</p>
<h4 id="4-富裕空间管理"><a href="#4-富裕空间管理" class="headerlink" title="4. 富裕空间管理"></a>4. 富裕空间管理</h4><p>主侧轴的空白的空间管理。他管理的是富裕空间的位置并不影响项目的空间大小  </p>
<h5 id="1-主轴"><a href="#1-主轴" class="headerlink" title="1.主轴"></a>1.主轴</h5><p>老版本：<code>-webkit-box-pack：start(右，下)/end(左，上)/center(两边)/jusity(项目之间)</code>  与主轴无关，只和x，y轴有关，前面指的是x轴的情况，后面是y轴的情况    </p>
<p>新版本：<code>justify-content:flex-start(主轴正方向)/flex-end(主轴负方向)/center(主轴两边)/space-between(项目之间)/space-around(项目两边)</code>  </p>
<h5 id="2-侧轴"><a href="#2-侧轴" class="headerlink" title="2. 侧轴"></a>2. 侧轴</h5><p>老版本：<code>-webkit-box-align：start(右，下)/end(左，上)/center(两边)/jusity(项目之间)</code>  与主轴无关，只和x，y轴有关，前面指的是x轴的情况，后面是y轴的情况    </p>
<p>新版本：<code>align-items:flex-start(侧轴正方向)/flex-end(侧轴负方向)/content(侧轴两边)/base-line(按照基线)/strech(没有的，当没有高度的时候就是等高布局)</code>  </p>
<h3 id="3-项目管理"><a href="#3-项目管理" class="headerlink" title="3.  项目管理"></a>3.  项目管理</h3><p>弹性空间管理就是把富裕空间匀到每一个项目之上。</p>
<p>老版本：<code>-webkit-box-flex：n</code></p>
<p>新版本：<code>flex-grow:n</code>  </p>
<p>相当于每一个项目的弹性因子就是 n 那么我们的富裕空间的分配就是 (nx/n1+n2+n3+…+nx) * 富裕空间大小 简单来说这个弹性因子就表示将来会分到的富裕空间的比例。</p>
<h3 id="4-新版flex特性"><a href="#4-新版flex特性" class="headerlink" title="4. 新版flex特性"></a>4. 新版flex特性</h3><h4 id="1-容器新增特性"><a href="#1-容器新增特性" class="headerlink" title="1.容器新增特性"></a>1.容器新增特性</h4><p>在新版的flex中使用 <code>flex-wrap</code> 来控制侧轴的方向 规定了侧轴的方向那么在元素在主轴的方向排列不下的时候不会对元素进行压缩而是采用向侧轴排列的方式进行，这样就会产生多行多列，此时我们侧轴的富裕空间 <code>align-items</code> 就失效了，必须采用 <code>align-content</code> 来解决。</p>
<h5 id="1-flex-wrap"><a href="#1-flex-wrap" class="headerlink" title="1. flex-wrap"></a>1. flex-wrap</h5><p>flex-wrap 属性控制了容器为单行/列还是多行/列。并且定义了侧轴的方向，新行/列将沿侧轴方向堆砌。</p>
<p>默认值：nowrap     不可继承</p>
<p>值：nowrap | wrap | wrap-reverse </p>
<h5 id="2-align-content"><a href="#2-align-content" class="headerlink" title="2.align-content"></a>2.align-content</h5><p>align-content 属性定义弹性容器的侧轴方向上有额外空间时，如何排布每一行/列。当弹性容器只有一行/列时无作用<br>默认值：stretch    不可继承  </p>
<p>值：<br>flex-start<br>​    所有行/列从侧轴起点开始填充。第一行/列的侧轴起点边和容器的侧轴起点边对齐。<br>​    接下来的每一行/列紧跟前一行/列。<br>flex-end<br>​    所有弹性元素从侧轴末尾开始填充。最后一个弹性元素的侧轴终点和容器的侧轴终点对齐。<br>​    同时所有后续元素与前一个对齐。<br>center<br>​    所有行/列朝向容器的中心填充。每行/列互相紧挨，相对于容器居中对齐。<br>​    容器的侧轴起点边和第一行/列的距离相等于容器的侧轴终点边和最后一行/列的距离。<br>space-between<br>​    所有行/列在容器中平均分布。相邻两行/列间距相等。<br>​    容器的侧轴起点边和终点边分别与第一行/列和最后一行/列的边对齐。<br>space-around<br>​    所有行/列在容器中平均分布，相邻两行/列间距相等。<br>​    容器的侧轴起点边和终点边分别与第一行/列和最后一行/列的距离是相邻两行/列间距的一半。<br>stretch<br>​    拉伸所有行/列来填满剩余空间。剩余空间平均的分配给每一行/列       </p>
<h5 id="3-flex-flow"><a href="#3-flex-flow" class="headerlink" title="3. flex-flow"></a>3. flex-flow</h5><p>flex-flow 属性是设置“flex-direction”和“flex-wrap”的简写</p>
<p>默认值：row nowrap    不可继承</p>
<p>控制主轴和侧轴的位置以及方向</p>
<h4 id="2-项目新增特性"><a href="#2-项目新增特性" class="headerlink" title="2. 项目新增特性"></a>2. 项目新增特性</h4><h5 id="1-order"><a href="#1-order" class="headerlink" title="1.order"></a>1.order</h5><p>设置项目在主轴的排列优先级，数字越大优先级越高。</p>
<h5 id="2-align-self"><a href="#2-align-self" class="headerlink" title="2.align-self"></a>2.align-self</h5><p>项目自己管理自己的侧轴的富裕空间，而不归 <code>align-items</code> 管了。</p>
<h5 id="3-flex-basis"><a href="#3-flex-basis" class="headerlink" title="3. flex-basis"></a>3. flex-basis</h5><p>flex-basis 在弹性空间管理的时候非常有用的，我们在上面的弹性空间管理的时候也就是 <code>flex-grows</code> 我们直接是把整个容器大小减去所有项目的大小然后除以增长因子的大小来计算的，实际上我们应该减去的不是容器的总大小而是<code>flex-basis</code> 的值，默认值就是容器的大小。所以他们的计算公式应该是：</p>
<blockquote>
<p>   可用空间 = (容器大小 - 所有相邻项目flex-basis的总和)<br>   可扩展空间 = (可用空间/所有相邻项目flex-grow的总和)<br>   每项获得的伸缩大小 = (伸缩基准值 + (可扩展空间 x flex-grow值))</p>
</blockquote>
<h5 id="4-flex-shrink"><a href="#4-flex-shrink" class="headerlink" title="4.flex-shrink"></a>4.flex-shrink</h5><p>收缩因子，实际上就是我们所有的item的总大小大于容器的大小他就会进行收缩但是收缩也会有一个收缩因子，但是他的收缩因子的默认值不是 0 而是 1，计算公式：</p>
<blockquote>
<p>   1.计算收缩因子与基准值乘的总和<br>   2.计算收缩因数<br>​              收缩因数=（项目的收缩因子*项目基准值）/第一步计算总和<br>   3.移除空间的计算<br>​              移除空间= 项目收缩因数 x 负溢出的空间   </p>
</blockquote>
<h3 id="5-flex应用"><a href="#5-flex应用" class="headerlink" title="5. flex应用"></a>5. flex应用</h3><h4 id="1-等分布局"><a href="#1-等分布局" class="headerlink" title="1.等分布局"></a>1.等分布局</h4><p>根据规则我们就需要将赋予空间全部分配给每一个项目，而我们需要的全部的富裕空间就是容器的所有的宽度。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span>&#123;</span><br><span class="line">    <span class="attribute">flex-grows</span>:<span class="number">1</span>;</span><br><span class="line">    <span class="attribute">flex-shrink</span>:<span class="number">1</span>;</span><br><span class="line">    <span class="attribute">flex-basis</span>:<span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的这一堆代码为了等分布局，所以我们有一个简写的方式就是</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.item</span>&#123;</span><br><span class="line">    <span class="attribute">flex</span>:<span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>CSS2 从入门到精通</title>
    <url>/2019/01/08/FrontEnd/CSS2%20%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<h2 id="1-常用的选择器"><a href="#1-常用的选择器" class="headerlink" title="1. 常用的选择器"></a>1. 常用的选择器</h2><h3 id="1-元素选择器"><a href="#1-元素选择器" class="headerlink" title="1. 元素选择器"></a>1. 元素选择器</h3><ul>
<li>作用：通过元素选择器可以选择指定的元素</li>
<li>语法：tag{}</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">p</span>&#123;</span><br><span class="line">  <span class="attribute">color</span>: red;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">h1</span>&#123;</span><br><span class="line">  <span class="attribute">color</span>: red;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-id-选择器"><a href="#2-id-选择器" class="headerlink" title="2. id 选择器"></a>2. id 选择器</h3><ul>
<li>作用：通过元素的id属性值选中唯一的一个元素</li>
<li>用法: #id{}</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#p1</span>&#123;</span><br><span class="line">	<span class="attribute">font-size</span>: <span class="number">20px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-类选择器"><a href="#3-类选择器" class="headerlink" title="3.类选择器"></a>3.类选择器</h3><ul>
<li>通过元素的class属性值选中一组元素</li>
<li>语法：.class{}</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.p2</span>&#123;</span><br><span class="line">  <span class="attribute">color</span>: red;</span><br><span class="line">&#125;</span><br><span class="line">			</span><br><span class="line"><span class="selector-class">.hello</span>&#123;</span><br><span class="line">  <span class="attribute">font-size</span>: <span class="number">50px</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-选择器分组"><a href="#4-选择器分组" class="headerlink" title="4. 选择器分组"></a>4. 选择器分组</h3><ul>
<li>通过选择器分组可以同时选中多个选择器对应的元素,简单来说就是同时为这些选择的元素应用相同的样式。他也称作为 *<em>并集选择器 *</em>就是上面的每一个逗号分开的相当于一个集合，那么相当于把他们合并起来到一个集合中只要元素命中其中一条规则就会应用样式。</li>
<li>语法：选择器1,选择器2,选择器N{}</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#p1</span> , <span class="selector-class">.p2</span> , <span class="selector-tag">h1</span>&#123;</span><br><span class="line">  <span class="attribute">background-color</span>: yellow;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-通配选择器"><a href="#5-通配选择器" class="headerlink" title="5. 通配选择器"></a>5. 通配选择器</h3><ul>
<li><em>他可以用来选中页面中的所有的元素</em></li>
<li>语法：*{}</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">*&#123;</span><br><span class="line">  <span class="attribute">color</span>: red;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="6-交集选择器"><a href="#6-交集选择器" class="headerlink" title="6. 交集选择器"></a>6. 交集选择器</h3><ul>
<li>作用：选择<strong>同时满足所有选择器规则</strong>的元素，所以也称作为交集选择器，元素必须命中所有的选择条件才能应用样式。下面的例子就相当于必须满足元素名为span并且拥有p3这个类</li>
<li>语法：选择器1选择器2选择器N{}</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">span</span><span class="selector-class">.p3</span>&#123;</span><br><span class="line">				<span class="attribute">background-color</span>: yellow;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="7-后代选择器"><a href="#7-后代选择器" class="headerlink" title="7. 后代选择器"></a>7. 后代选择器</h3><ul>
<li>作用：选中指定元素的指定后代元素，下面的例子就是选择id为d1的span后代，注意后代是不用和祖先元素相邻。</li>
<li>语法：祖先元素 后代元素{}</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-id">#d1</span> <span class="selector-tag">span</span>&#123;</span><br><span class="line">	<span class="attribute">color</span>: greenyellow;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="8-子选择器"><a href="#8-子选择器" class="headerlink" title="8. 子选择器"></a>8. 子选择器</h3><ul>
<li>作用：选中指定父元素的指定子元素,两者必须是相邻的</li>
<li>语法：父元素 &gt; 子元素</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">div</span> &gt; <span class="selector-tag">span</span>&#123;</span><br><span class="line">				<span class="attribute">background-color</span>: yellow;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<blockquote>
<p>Tips:<br>父元素：直接包含子元素的元素<br>子元素：直接被父元素包含的元素<br>祖先元素：直接或间接包含后代元素的元素，父元素也是祖先元素<br>后代元素：直接或间接被祖先元素包含的元素，子元素也是后代元素<br>兄弟元素：拥有相同父元素的元素叫做兄弟元素</p>
</blockquote>
<h3 id="9-伪类选择器"><a href="#9-伪类选择器" class="headerlink" title="9. 伪类选择器"></a>9. 伪类选择器</h3><p>伪类专门用来表示元素的一种的特殊的状态，比如：访问过的超链接，比如普通的超链接，比如获取焦点的文本框<br>当我们需要为处在这些特殊状态的元素设置样式时，就可以使用伪类</p>
<h5 id="1-link-表示普通的链接（没访问过的链接）"><a href="#1-link-表示普通的链接（没访问过的链接）" class="headerlink" title="1. :link 表示普通的链接（没访问过的链接）"></a>1. :link 表示普通的链接（没访问过的链接）</h5><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">a</span><span class="selector-pseudo">:link</span>&#123;<span class="attribute">color</span>: yellowgreen;&#125;</span><br></pre></td></tr></table></figure>

<h5 id="2-visited-表示访问过的链接"><a href="#2-visited-表示访问过的链接" class="headerlink" title="2. :visited 表示访问过的链接"></a>2. :visited 表示访问过的链接</h5><p>浏览器是通过历史记录来判断一个链接是否访问过,由于涉及到用户的隐私问题，所以使用visited伪类只能设置字体的颜色</p>
<h5 id="3-hover伪类表示鼠标移入"><a href="#3-hover伪类表示鼠标移入" class="headerlink" title="3. :hover伪类表示鼠标移入"></a>3. :hover伪类表示鼠标移入</h5><h5 id="4-active表示的是超链接被点击的状态"><a href="#4-active表示的是超链接被点击的状态" class="headerlink" title="4. :active表示的是超链接被点击的状态"></a>4. :active表示的是超链接被点击的状态</h5><h5 id="5-focus-光标为获得焦点"><a href="#5-focus-光标为获得焦点" class="headerlink" title="5.:focus 光标为获得焦点"></a>5.:focus 光标为获得焦点</h5><h5 id="6-selection选中的内容"><a href="#6-selection选中的内容" class="headerlink" title="6.::selection选中的内容"></a>6.::selection选中的内容</h5><p>这个伪类在火狐中需要采用另一种方式编写::-moz-selection兼容火狐的</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">p</span><span class="selector-pseudo">::-moz-selection</span>&#123;<span class="attribute">background-color</span>: orange;&#125;</span><br></pre></td></tr></table></figure>

<p>兼容大部分浏览器的</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">p</span><span class="selector-pseudo">::selection</span>&#123;<span class="attribute">background-color</span>: orange;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="10-伪元素选择器"><a href="#10-伪元素选择器" class="headerlink" title="10. 伪元素选择器"></a>10. 伪元素选择器</h3><p>使用伪元素来表示元素中的一些特殊的位置.</p>
<h5 id="1-first-letter-内容中的第一个字母"><a href="#1-first-letter-内容中的第一个字母" class="headerlink" title="1. :first-letter 内容中的第一个字母"></a>1. :first-letter 内容中的第一个字母</h5><h5 id="2-first-line内容的第一行"><a href="#2-first-line内容的第一行" class="headerlink" title="2. :first-line内容的第一行"></a>2. :first-line内容的第一行</h5><h5 id="3-before表示元素最前边的部分"><a href="#3-before表示元素最前边的部分" class="headerlink" title="3.:before表示元素最前边的部分"></a>3.:before表示元素最前边的部分</h5><h5 id="4-after表示元素的最后边的部分"><a href="#4-after表示元素的最后边的部分" class="headerlink" title="4. :after表示元素的最后边的部分"></a>4. :after表示元素的最后边的部分</h5><p>   一般before和after都需要结合content这个样式一起使用，   通过content可以向before或after的位置添加一些内容<br>:after</p>
<h3 id="11-属性选择器"><a href="#11-属性选择器" class="headerlink" title="11. 属性选择器"></a>11. 属性选择器</h3><ul>
<li>作用：可以根据元素中的属性或属性值来选取指定元素</li>
<li>语法：<ul>
<li>[属性名] 选取含有指定属性的元素</li>
<li>[属性名=”属性值”] 选取含有指定属性值的元素</li>
<li>[属性名^=”属性值”] 选取属性值以指定内容开头的元素</li>
<li>[属性名$=”属性值”] 选取属性值以指定内容结尾的元素<h3 id="12-子元素选择器"><a href="#12-子元素选择器" class="headerlink" title="12. 子元素选择器"></a>12. 子元素选择器</h3><h5 id="1-first-child-可以选中第一个子元素"><a href="#1-first-child-可以选中第一个子元素" class="headerlink" title="1. :first-child 可以选中第一个子元素:"></a>1. :first-child 可以选中第一个子元素:</h5><h5 id="2-last-child-可以选中最后一个子元素"><a href="#2-last-child-可以选中最后一个子元素" class="headerlink" title="2.last-child 可以选中最后一个子元素"></a>2.last-child 可以选中最后一个子元素</h5></li>
</ul>
</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">body</span> &gt; <span class="selector-tag">p</span><span class="selector-pseudo">:first-child</span>&#123;<span class="attribute">background-color</span>: yellow;&#125;<span class="selector-tag">p</span><span class="selector-pseudo">:last-child</span>&#123;<span class="attribute">background-color</span>: yellow;&#125;</span><br></pre></td></tr></table></figure>

<h5 id="3-nth-child-可以选中任意位置的子元素"><a href="#3-nth-child-可以选中任意位置的子元素" class="headerlink" title="3. :nth-child 可以选中任意位置的子元素"></a>3. :nth-child 可以选中任意位置的子元素</h5><p>该选择器后边可以指定一个参数，指定要选中第几个子元素<br>even 表示偶数位置的子元素<br>odd 表示奇数位置的子元素</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">p</span><span class="selector-pseudo">:nth-child(odd)</span>&#123;<span class="attribute">background-color</span>: yellow;&#125;</span><br></pre></td></tr></table></figure>

<p><code>:first-of-type</code>,<code>:last-of-type</code>，<code>:nth-of-type</code>,<code>:first-child</code>这些非常的类似，只不过child，是在所有的子元素中排列而type，是在当前类型的子元素中排列</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">p</span><span class="selector-pseudo">:first-of-type</span>&#123;<span class="attribute">background-color</span>: yellow;&#125;</span><br></pre></td></tr></table></figure>

<h3 id="13-兄弟选择器"><a href="#13-兄弟选择器" class="headerlink" title="13.兄弟选择器"></a>13.兄弟选择器</h3><p>为挨着的兄弟元素添加样式，其中 + 表示后一个，而 ~ 则表示前一个</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">span</span> ~ <span class="selector-tag">p</span>&#123;</span><br><span class="line">  <span class="attribute">background-color</span>: yellow;</span><br><span class="line">&#125;</span><br><span class="line"><span class="selector-tag">span</span> + <span class="selector-tag">p</span>&#123;</span><br><span class="line">  <span class="attribute">background-color</span>: yellow;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-样式继承"><a href="#2-样式继承" class="headerlink" title="2. 样式继承"></a>2. 样式继承</h2><p>像儿子可以继承父亲的遗产一样，在CSS中，祖先元素上的样式，也会被他的后代元素所继承,利用继承，可以将一些基本的样式设置给祖先元素，这样所有的后代元素将会自动继承这些样式。但是并不是所有的样式都会被子元素所继承，比如：<code>背景相关的样式</code>都不会被继承 <code>边框相关的样式</code> <code>定位相关</code>的</p>
<h2 id="3-样式优先级"><a href="#3-样式优先级" class="headerlink" title="3. 样式优先级"></a>3. 样式优先级</h2><p>优先级的规则：</p>
<ol>
<li>内联样式 ， 优先级  1000</li>
<li>id选择器，优先级   100</li>
<li>类和伪类， 优先级   10</li>
<li>元素选择器，优先级 1</li>
<li>通配* ，    优先级 0</li>
<li>继承的样式，没有优先级</li>
<li>并集选择器的优先级是单独计算</li>
<li>可以在样式的最后，添加一个!important，则此时该样式将会获得一个最高的优先级，将会优先于所有的样式显示甚至超过内联样式，但是在开发中尽量避免使用!important<h2 id="4-长度单位"><a href="#4-长度单位" class="headerlink" title="4. 长度单位"></a>4. 长度单位</h2><h3 id="1-像素-px"><a href="#1-像素-px" class="headerlink" title="1.像素 px"></a>1.像素 px</h3>像素是我们在网页中使用的最多的一个单位，一个像素就相当于我们屏幕中的一个小点，我们的屏幕实际上就是由这些像素点构成的但是这些像素点，是不能直接看见。不同显示器一个像素的大小也不相同，显示效果越好越清晰，像素就越小，反之像素越大。<h3 id="2-百分比"><a href="#2-百分比" class="headerlink" title="2. 百分比 %"></a>2. 百分比 %</h3>也可以将单位设置为一个百分比的形式，这样浏览器将会根据其父元素的样式来计算该值使用百分比的好处是，当父元素的属性值发生变化时，子元素也会按照比例发生改变在我们创建一个自适应的页面时，经常使用百分比作为单位<h3 id="3-em"><a href="#3-em" class="headerlink" title="3. em"></a>3. em</h3>em和百分比类似，它是相对于当前元素的字体大小来计算的 1em = 1font-size 使用em时，当字体大小发生改变时，em也会随之改变当设置字体相关的样式时，经常会使用em<h3 id="4-行间距"><a href="#4-行间距" class="headerlink" title="4. 行间距"></a>4. 行间距</h3>在CSS并没有为我们提供一个直接设置行间距的方式， 我们只能通过设置行高来间接的设置行间距，行高越大行间距越大 使用line-height来设置行高 行高类似于我们上学单线本，单线本是一行一行，线与线之间的距离就是行高， 网页中的文字实际上也是写在一个看不见的线中的，而文字会默认在行高中垂直居中显示。<br>行间距 = 行高 - 字体大小<br><br>通过设置line-height可以间接的设置行高，<br>可以接收的值：<br>1.直接就收一个大小<br>2.可以指定一个百分数，则会相对于字体去计算行高<br>3.可以直接传一个数值，则行高会设置字体大小相应的倍数<br></li>
</ol>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-tag">p</span>&#123;</span><br><span class="line">    <span class="attribute">line-height</span>: <span class="number">200%</span>;</span><br><span class="line">    <span class="attribute">line-height</span>: <span class="number">20px</span>;</span><br><span class="line">    <span class="attribute">line-height</span>: <span class="number">2</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于单行文本来说，可以将行高设置为和父元素的高度一致， 这样可以是单行文本在父元素中垂直居中</p>
<h2 id="5-文本样式"><a href="#5-文本样式" class="headerlink" title="5. 文本样式"></a>5. 文本样式</h2><h3 id="1-text-transform可以用来设置文本的大小写"><a href="#1-text-transform可以用来设置文本的大小写" class="headerlink" title="1. text-transform可以用来设置文本的大小写"></a>1. text-transform可以用来设置文本的大小写</h3><p>可选值：</p>
<ul>
<li>none 默认值，该怎么显示就怎么显示，不做任何处理</li>
<li>capitalize 单词的首字母大写，通过空格来识别单词</li>
<li>uppercase 所有的字母都大写</li>
<li>lowercase 所有的字母都小写<br><h3 id="2-text-decoration可以用来设置文本的修饰"><a href="#2-text-decoration可以用来设置文本的修饰" class="headerlink" title="2.text-decoration可以用来设置文本的修饰"></a>2.text-decoration可以用来设置文本的修饰</h3>可选值：</li>
<li>none：默认值，不添加任何修饰，正常显示</li>
<li>underline 为文本添加下划线</li>
<li>overline 为文本添加上划线</li>
<li>line-through 为文本添加删除线</li>
</ul>
<p>超链接会默认添加下划线，也就是超链接的text-decoration的默认值是underline如果需要去除超链接的下划线则需要将该样式设置为none<br></p>
<h3 id="3-letter-spacing可以指定字符间距"><a href="#3-letter-spacing可以指定字符间距" class="headerlink" title="3.letter-spacing可以指定字符间距"></a>3.letter-spacing可以指定字符间距</h3><h3 id="4-word-spacing可以设置单词之间的距离"><a href="#4-word-spacing可以设置单词之间的距离" class="headerlink" title="4. word-spacing可以设置单词之间的距离"></a>4. word-spacing可以设置单词之间的距离</h3><p>实际上就是设置词与词之间空格的大小<br></p>
<h3 id="5-text-align用于设置文本的对齐方式"><a href="#5-text-align用于设置文本的对齐方式" class="headerlink" title="5.text-align用于设置文本的对齐方式"></a>5.text-align用于设置文本的对齐方式</h3><p>可选值：</p>
<ul>
<li>left 默认值，文本靠左对齐</li>
<li>right ， 文本靠右对齐</li>
<li>center ， 文本居中对齐</li>
<li>justify ， 两端对齐通过调整文本之间的空格的大小，来达到一个两端对齐的目的<br><h3 id="6-text-indent用来设置首行缩进"><a href="#6-text-indent用来设置首行缩进" class="headerlink" title="6.text-indent用来设置首行缩进"></a>6.text-indent用来设置首行缩进</h3>当给它指定一个正值时，会自动向右侧缩进指定的像素 如果为它指定一个负值，则会向左移动指定的像素, 通过这种方式可以将一些不想显示的文字隐藏起来 这个值一般都会使用em作为单位</li>
</ul>
<h2 id="6-盒模型"><a href="#6-盒模型" class="headerlink" title="6. 盒模型"></a>6. 盒模型</h2><p>使用width来设置盒子内容区的宽度，使用height来设置盒子内容区的高度。<strong>width和height只是设置的盒子内容区的大小，而不是盒子的整个大小，盒子可见框的大小由内容区，内边距和边框共同决定。 **内联元素不能设置 height 和 width ，如果非要设置必须修改 display 。</strong></p>
<h3 id="1-为元素设置边框"><a href="#1-为元素设置边框" class="headerlink" title="1. 为元素设置边框"></a>1. 为元素设置边框</h3><p>要为一个元素设置边框必须指定三个样式<br></p>
<h4 id="1-border-width-边框的宽度"><a href="#1-border-width-边框的宽度" class="headerlink" title="1. border-width:边框的宽度"></a>1. border-width:边框的宽度<br></h4><p>使用border-width可以分别指定四个边框的宽度如果在border-width指定了四个值，则四个值会分别设置给 上 右 下 左，按照顺时针的方向设置的<br>    如果指定三个值，则三个值会分别设置给    上  左右 下<br>    如果指定两个值，则两个值会分别设置给 上下 左右<br>    如果指定一个值，则四边全都是该值<br></p>
<h4 id="2-border-color-边框颜色"><a href="#2-border-color-边框颜色" class="headerlink" title="2.border-color:边框颜色"></a>2.border-color:边框颜色<br></h4><p>和宽度一样，color也提供四个方向的样式，可以分别指定颜色</p>
<h4 id="3-border-style-边框的样式"><a href="#3-border-style-边框的样式" class="headerlink" title="3. border-style:边框的样式"></a>3. border-style:边框的样式<br></h4><ul>
<li>none，默认值，没有边框</li>
<li>solid 实线</li>
<li>dotted 点状边框</li>
<li>dashed 虚线</li>
<li>double 双线</li>
</ul>
<h3 id="2-内边距（padding）"><a href="#2-内边距（padding）" class="headerlink" title="2. 内边距（padding）"></a>2. 内边距（padding）</h3><p>指的是盒子的内容区与盒子边框之间的距离*  一共有四个方向的内边距,内边距会影响盒子的可见框的大小，元素的背景会延伸到内边距.</p>
<h3 id="3-外边距"><a href="#3-外边距" class="headerlink" title="3. 外边距"></a>3. 外边距</h3><p>外边距指的是当前盒子与其他盒子之间的距离，他不会影响可见框的大小，而是会影响到盒子的位置。<strong>由于页面中的元素都是靠左靠上摆放的，所以注意当我们设置上和左外边距时，会导致盒子自身的位置发生改变</strong>。_<br>margin还可以设置为auto，auto一般只设置给水平方向的margin如果只指定，左外边距或右外边距的margin为auto则会将外边距设置为最大值垂直方向外边距如果设置为auto，则外边距默认就是0如果将left和right同时设置为auto，则会将两侧的外边距设置为相同的值，就可以使元素自动在父元素中居中，所以我们经常将左右外边距设置为auto。</p>
<h4 id="1-垂直外边距重叠"><a href="#1-垂直外边距重叠" class="headerlink" title="1. 垂直外边距重叠"></a>1. 垂直外边距重叠</h4><p>垂直外边距的重叠在网页中<strong>相邻</strong>垂直方向<strong>的外边距会发生外边距的重叠所谓的外边距重叠指兄弟元素之间的相邻外边距会取最大值而不是取和.<br>      如果父子元素的</strong>垂直外边  距相邻 **了，则子元素的外边距会设置给父元素.</p>
<h4 id="2-如何解决外边距重叠"><a href="#2-如何解决外边距重叠" class="headerlink" title="2. 如何解决外边距重叠"></a>2. 如何解决外边距重叠</h4><ol>
<li>在两个元素之家加其他元素</li>
<li>添加 border</li>
<li>添加 padding </li>
</ol>
<p><strong>注意：内联元素是不支持垂直方向的外边距的，其他的都和块级元素相同。</strong><br></p>
<h2 id="7-元素展示方式"><a href="#7-元素展示方式" class="headerlink" title="7. 元素展示方式"></a>7. 元素展示方式</h2><h3 id="1-display"><a href="#1-display" class="headerlink" title="1. display"></a>1. display</h3><p>将一个内联元素变成块元素， 通过display样式可以修改元素的类型<br>可选值：<br></p>
<ul>
<li>inline：可以将一个元素作为内联元素显示</li>
<li>block: 可以将一个元素设置块元素显示</li>
<li>inline-block：将一个元素转换为行内块元素可以使一个元素既有行内元素的特点又有块元素的特点既可以设置宽高，又不会独占一行</li>
<li>none: 不显示元素，并且元素不会在页面中继续占有位置使用该方式隐藏的元素，不会在页面中显示，并且不再占据页面的位置<br><h3 id="2-visibility"><a href="#2-visibility" class="headerlink" title="2. visibility"></a>2. visibility</h3>可以用来设置元素的隐藏和显示的状态<br>可选值：</li>
<li>visible 默认值，元素默认会在页面显示</li>
<li>hidden 元素会隐藏不显示,使用 visibility:hidden;隐藏的元素虽然不会在页面中显示，但是它的位置会依然保持</li>
</ul>
<h3 id="3-overflow"><a href="#3-overflow" class="headerlink" title="3. overflow"></a>3. <em>overflow</em></h3><p>子元素默认是存在于父元素的内容区中，<br>      理论上讲子元素的最大可以等于父元素内容区大小<br>   如果子元素的大小超过了父元素的内容区，则超过的大小会在父元素以外的位置显<br>      超出父元素的内容，我们称为溢出的内容<br> 父元素默认是将溢出内容，在父元素外边显示，<br>   通过overflow可以设置父元素如何处理溢出内容：<br>      可选值：<br>         - visible，默认值，不会对溢出内容做处理，元素会显示<br>         - hidden, 溢出的内容，会被修剪，不会显示<br>         - scroll, 会为父元素添加滚动条，通过拖动滚动条该属性不论内容是否溢出，都会添加水平<br>         - auto，会根据需求自动添加滚动条，需要水平就添加水平需要垂直就添加垂直都不需要就都不加</p>
<h2 id="8-文档流"><a href="#8-文档流" class="headerlink" title="8. 文档流"></a>8. 文档流</h2><p>文档流文档流处在网页的最底层，它表示的是一个页面中的位置，我们所创建的元素默认都处在文档流中<br>元素在文档流中的特点<br></p>
<h3 id="1-块元素"><a href="#1-块元素" class="headerlink" title="1.块元素"></a>1.块元素</h3><p>1.块元素在文档流中会独占一行，块元素会自上向下排列。<br>      2.块元素在文档流中默认宽度是父元素的100%<br>      3.块元素在文档流中的高度默认被内容撑开<br></p>
<h3 id="2-内联元素"><a href="#2-内联元素" class="headerlink" title="2.内联元素"></a>2.内联元素</h3><p>1.内联元素在文档流中只占自身的大小，会默认从左向右排列，如果一行中不足以容纳所有的内联元素，则换到下一行，继续自左向右。<br>      2.在文档流中，内联元素的宽度和高度默认都被内容撑开</p>
<h2 id="9-浮动"><a href="#9-浮动" class="headerlink" title="9. 浮动"></a>9. 浮动</h2><p>块元素在文档流中默认垂直排列， 如果希望块元素在页面中水平排列，可以使块元素脱离文档流使用float来使元素浮动，从而脱离文档流<br>可选值：</p>
<ul>
<li>none，默认值，元素默认在文档流中排列</li>
<li>left，元素会立即脱离文档流，向页面的左侧浮动</li>
<li>right，元素会立即脱离文档流，向页面的右侧浮动</li>
</ul>
<h3 id="1-浮动的规则"><a href="#1-浮动的规则" class="headerlink" title="1. 浮动的规则"></a>1. 浮动的规则</h3><p>当为一个元素设置浮动以后（float属性是一个非none的值），   元素会立即脱离文档流，元素脱离文档流以后，它下边的元素会立即向上移动   元素浮动以后，会尽量向页面的左上或右上漂浮，   直到遇到父元素的边框或者其他的浮动元素<br>如果浮动元素上边是一个没有浮动的块元素，则浮动元素不会超过块元素。<br>浮动的元素不会超过他上边的兄弟元素，最多最多一边齐。<br>浮动的元素不会盖住文字，文字会自动环绕在浮动元素的周围<br>在文档流中，子元素的宽度默认占父元素的全部当元素设置浮动以后，会完全脱离文档流.块元素脱离文档流以后，高度和宽度都被内容撑开<br>       开启span的浮动内联元素脱离文档流以后会变成块元素</p>
<h3 id="2-浮动后高度塌陷"><a href="#2-浮动后高度塌陷" class="headerlink" title="2. 浮动后高度塌陷"></a>2. 浮动后高度塌陷</h3><p>在文档流中，父元素的高度默认是被子元素撑开的，也就是子元素多高，父元素就多高。但是当为子元素设置浮动以后，子元素会完全脱离文档流，此时将会导致子元素无法撑起父元素的高度，导致父元素的高度塌陷。由于父元素的高度塌陷了，则父元素下的所有元素都会向上移动，这样将会导致页面布局混乱。<br> 所以在开发中一定要避免出现高度塌陷的问题,我们可以将父元素的高度写死，以避免塌陷的问题出现，但是一旦高度写死，父元素的高度将不能自动适应子元素的高度，所以这种方案是不推荐使用的。<br></p>
<h3 id="3-解决高度塌陷问题"><a href="#3-解决高度塌陷问题" class="headerlink" title="3. 解决高度塌陷问题"></a>3. 解决高度塌陷问题</h3><h4 id="1-开启BFC"><a href="#1-开启BFC" class="headerlink" title="1. 开启BFC"></a>1. 开启BFC</h4><p>根据W3C的标准，在页面中元素都一个隐含的属性叫做Block Formatting Context简称BFC，该属性可以设置打开或者关闭，默认是关闭的。当开启元素的BFC以后，元素将会具有如下的特性：<br>   1.父元素的垂直外边距不会和子元素重叠    <br>   2.开启BFC的元素不会被浮动元素所覆盖<br>   3.开启BFC的元素可以包含浮动的子元素<br>如何开启元素的BFC<br>   1.设置元素浮动使用这种方式开启，虽然可以撑开父元素，但是会导致父元素的宽度丢失而且使用这种方式也会导致下边的元素上移，不能解决问题。这是因为开启浮动的会计元素宽高都是默认被撑起来的，除非自动设置宽高。<br>   2.设置元素绝对定位<br>   3.设置元素为inline-block可以解决问题，但是会导致宽度丢失，不推荐使用这种方式<br>   4.将元素的overflow设置为一个非visible的值<br>*<em>推荐方式：将overflow设置为hidden是副作用最小的开启BFC的方式。 *</em><br>是在IE6及以下的浏览器中并不支持BFC，所以使用这种方式不能兼容IE6。在IE6中虽然没有BFC，但是具有另一个隐含的属性叫做hasLayout，该属性的作用和BFC类似，所在IE6浏览器可以通过开hasLayout来解决该问题开启方式很多，我们直接使用一种副作用最小的：直接将元素的zoom设置为1即可<br></p>
<ul>
<li>zoom表示放大的意思，后边跟着一个数值，写几就将元素放大几倍</li>
<li>zoom:1表示不放大元素，但是通过该样式可以开启hasLayout</li>
<li>zoom这个样式，只在IE中支持，其他浏览器都不支持</li>
</ul>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.parent</span>&#123;</span><br><span class="line">	<span class="attribute">zoom</span>:<span class="number">1</span>;</span><br><span class="line">  <span class="attribute">overflow</span>: hidden;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-清除浮动"><a href="#2-清除浮动" class="headerlink" title="2. 清除浮动"></a>2. 清除浮动</h4><p>可以直接在高度塌陷的父元素的最后，添加一个空白的div， 然后在对其进行清除浮动，   由于这个div并没有受到浮动元素的影响，所以他应该处在原来的元素在的时候的位置这样就相当于原来的元素还在，所以是可以撑开父元素的高度的，这样可以通过这个空白的div来撑开父元素的   基本没有副作用使用这种方式虽然可以解决问题，但是会在页面中添加多余的结构。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.clear</span>&#123;</span><br><span class="line">				<span class="attribute">clear</span>: both;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-伪类"><a href="#3-伪类" class="headerlink" title="3. 伪类"></a>3. 伪类</h4><p>通过after伪类，选中父元素的后边可以通过after伪类向元素的最后添加一个空白的块元素，然后对其清除浮动，这样做和添加一个div的原理一样，可以达到一个相同的效果，而且不会在页面中添加多余的div，这是我们最推荐使用的方式，几乎没有副作用。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.clearfix</span><span class="selector-pseudo">:after</span>&#123;</span><br><span class="line">				<span class="comment">/*添加一个内容*/</span></span><br><span class="line">				<span class="attribute">content</span>: <span class="string">""</span>;</span><br><span class="line">				<span class="comment">/*转换为一个块元素*/</span></span><br><span class="line">				<span class="attribute">display</span>: block;</span><br><span class="line">				<span class="comment">/*清除两侧的浮动*/</span></span><br><span class="line">				<span class="attribute">clear</span>: both;</span><br><span class="line">			&#125;</span><br></pre></td></tr></table></figure>

<h4 id="4-最佳实践"><a href="#4-最佳实践" class="headerlink" title="4. 最佳实践"></a>4. 最佳实践</h4><figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.clearfix</span><span class="selector-pseudo">:before</span>,</span><br><span class="line"><span class="selector-class">.clearfix</span><span class="selector-pseudo">:after</span>&#123;</span><br><span class="line">  <span class="attribute">content</span>: <span class="string">""</span>;</span><br><span class="line">  <span class="attribute">display</span>: table;</span><br><span class="line">  <span class="attribute">clear</span>: both;</span><br><span class="line">&#125;</span><br><span class="line">			</span><br><span class="line"><span class="selector-class">.clearfix</span>&#123;</span><br><span class="line">  <span class="attribute">zoom</span>: <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>经过修改后的clearfix是一个多功能的,既可以解决高度塌陷，又可以确保父元素和子元素的垂直外边距不会重叠。子元素和父元素相邻的垂直外边距会发生重叠，子元素的外边距会传递给父元素 使用空的table标签可以隔离父子元素的外边距，阻止外边距的重叠</p>
<h3 id="4-清除浮动"><a href="#4-清除浮动" class="headerlink" title="4. 清除浮动"></a>4. 清除浮动</h3><p>由于受到box1浮动的影响，box2整体向上移动了100px<br>我们有时希望清除掉其他元素浮动对当前元素产生的影响，这时可以使用<br>clear可以用来清除其他浮动元素对当前元素的影响<br>可选值：<br>      none，默认值，不清除浮动<br>      left，清除左侧浮动元素对当前元素的影响<br>      right，清除右侧浮动元素对当前元素的影响<br>      both，清除两侧浮动元素对当前元素的影响清除对他影响最大的那个元素的浮动</p>
<h3 id="5-总结"><a href="#5-总结" class="headerlink" title="5. 总结"></a>5. 总结</h3><p>​    在元素浮动的时候我们将元素看成两层。上面一层是文字相关的而下面一层则是和盒模型相关的。而一个元素开启浮动那么这个元素就被提升了半层。也就是两个排列的 box1 box2 ，在box1 浮动以后由于他之浮动半层，所以box2的盒模型部分会插入到box1下面，也就是box1会有三层，而box2的文字那一层是被卡在外面了，不会进去。</p>
<h2 id="10-定位"><a href="#10-定位" class="headerlink" title="10. 定位"></a>10. 定位</h2><p>定位指的就是将指定的元素摆放到页面的任意位置 通过定位可以任意的摆放元素 通过position属性来设置元素的定位<br>可选值：<br></p>
<ul>
<li>static：默认值，元素没有开启定位<br></li>
<li>relative：开启元素的相对定位<br></li>
<li>absolute：开启元素的绝对定位<br></li>
<li>fixed：开启元素的固定定位（也是绝对定位的一种）</li>
</ul>
<p>当开启了元素的定位（position属性值是一个非static的值）时， 可以通过left right top bottom四个属性来设置元素的偏移量<br></p>
<ul>
<li>left：元素相对于其定位位置的左侧偏移量<br></li>
<li>right：元素相对于其定位位置的右侧偏移量<br></li>
<li>top：元素相对于其定位位置的上边的偏移量<br></li>
<li>bottom：元素相对于其定位位置下边的偏移量<br></li>
</ul>
<p>通常偏移量只需要使用两个就可以对一个元素进行定位， 一般选择水平方向的一个偏移量和垂直方向的偏移量来为一个元素进行定位</p>
<h3 id="1-相对定位"><a href="#1-相对定位" class="headerlink" title="1. 相对定位"></a>1. 相对定位</h3><p>当元素的position属性设置为relative时，则开启了元素的相对定位<br></p>
<ol>
<li>当开启了元素的相对定位以后，而不设置偏移量时，元素不会发生任何变化</li>
<li>相对定位是相对于元素在文档流中原来的位置进行定位</li>
<li>相对定位的元素不会脱离文档流</li>
<li>相对定位会使元素提升一个层级</li>
<li>相对定位不会改变元素的性质，块还是块，内联还是内联</li>
</ol>
<h3 id="2-绝对定位"><a href="#2-绝对定位" class="headerlink" title="2. 绝对定位"></a>2. 绝对定位</h3><p>当position属性值设置为absolute时，则开启了元素的绝对定位<br>绝对定位：<br></p>
<ol>
<li>开启绝对定位，会使元素脱离文档流<br></li>
<li>开启绝对定位以后，如果不设置偏移量，则元素的位置不会发生变化<br></li>
<li>绝对定位是相对于离他最近的开启了定位的祖先元素进行定位的（一般情况，开启了子元素的绝对定位都会同时开启父元素的相对定位）如果所有的祖先元素都没有开启定位，则会相对于浏览器窗口进行定位</li>
<li>绝对定位会使元素提升一个层级<br></li>
<li>绝对定位会改变元素的性质，内联元素变成块元素，块元素的宽度和高度默认都被内容撑开<h3 id="3-固定定位"><a href="#3-固定定位" class="headerlink" title="3.  固定定位"></a>3.  固定定位</h3>当元素的position属性设置fixed时，则开启了元素的固定定位 固定定位也是一种绝对定位，它的大部分特点都和绝对定位一样 不同的是： 固定定位永远都会相对于浏览器窗口进行定位 固定定位会固定在浏览器窗口某个位置，不会随滚动条滚动IE6不支持固定定位.</li>
</ol>
<h3 id="4-总结"><a href="#4-总结" class="headerlink" title="4. 总结"></a>4. 总结</h3><p>浮动元素的包含块为最近的块级祖先元素。而定位则比较复杂：</p>
<ul>
<li>初始包含块由用户浏览器创建，在html中就是html标签。但是有些浏览器也会采用body作为根元素，大多数浏览器中初始包含块都是一个视窗大小的矩形。但是视口不等于初始包含块。</li>
<li>对于非根元素如果是relative或者static则包含块是最近的块级框。（或者说是他本来应该处于的位置。）</li>
<li>对于非根元素如果是absolute则包含块是最近的开启定位的元素(非static)的内边距的边界，也就是说是由边框的边界确定的。</li>
</ul>
<p>由于我们的行业规范是 div+css 所以我们不考虑内联元素的情况，因为div是块级元素。</p>
<p><strong>定位是提升一层，和浮动不一样。</strong></p>
<p><strong>注意：</strong> </p>
<p>对于一些常用的一些样式我们需要知道他们的默认值是什么比如：</p>
<p>left top right bottom width height 的默认值都是auto不是 0 也不是100%</p>
<p>margin 和 padding 默认则是 0 如果我们采用百分比百分比参照的是父级元素的 width 的而不是当前元素的大小。</p>
<h2 id="11-常用布局"><a href="#11-常用布局" class="headerlink" title="11. 常用布局"></a>11. 常用布局</h2><h3 id="1-三列布局"><a href="#1-三列布局" class="headerlink" title="1. 三列布局"></a>1. 三列布局</h3><h4 id="1-定位实现方式"><a href="#1-定位实现方式" class="headerlink" title="1.定位实现方式"></a>1.定位实现方式</h4><p>​    三列的话我们可以让其中的两列分别使用绝对定位，然后由于绝对定位默认是依照浏览器的窗口（或者将body设为realtive 然后相对于 body），所以可以让两边的直接在两边，而中间的使用块元素直接让宽度充满整个空闲部分。</p>
<p>​    然后由于随着我们浏览器变小我们需要设置一个body的最小宽否则有可能让我们的中间部分压没了。</p>
<p>​    其实这个是有问题的，就是当我们压缩矿口的时候会出现滚动条如果我们设置了 min-width ，这样我们的右边以浏览器窗口对其就会有问题。</p>
<p>​    <strong>所以不推荐使用。</strong></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">			</span><br><span class="line">			*&#123;</span><br><span class="line">				margin: 0;</span><br><span class="line">				padding: 0;</span><br><span class="line">			&#125;</span><br><span class="line">			body&#123;</span><br><span class="line"><span class="css">				<span class="comment">/*2*left+right*/</span></span></span><br><span class="line">				min-width: 600px;</span><br><span class="line">			&#125;</span><br><span class="line">			div&#123;</span><br><span class="line">				height: 100px;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#left</span>,<span class="selector-id">#right</span>&#123;</span></span><br><span class="line">				width: 200px;</span><br><span class="line">				background: pink;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#middle</span>&#123;</span></span><br><span class="line">				background: deeppink;</span><br><span class="line">				padding: 0 200px;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#left</span>&#123;</span></span><br><span class="line">				position: absolute;</span><br><span class="line">				left: 0;</span><br><span class="line">				top: 0;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#right</span>&#123;</span></span><br><span class="line">				position: absolute;</span><br><span class="line">				right: 0;</span><br><span class="line">				top: 0;</span><br><span class="line">			&#125;</span><br><span class="line">		<span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span> <span class="attr">style</span>=<span class="string">"position: relative;"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"left"</span>&gt;</span>left<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"middle"</span>&gt;</span>middle<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"right"</span>&gt;</span>right<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="2-使用浮动"><a href="#2-使用浮动" class="headerlink" title="2. 使用浮动"></a>2. 使用浮动</h4><p>将左右两列用浮动分别让他们在两边展示，而中间的为块级元素这样他的盒子布局的那一层就会浮上去，而文字刚好就被卡住。但是这个东西的前提就是内容区必须放在最后内容区的第二层才会自动的浮动上去。这样一来就有一个问题就是内容区是在文档流中最后被加载的。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!--</span></span><br><span class="line"><span class="comment">			1.两边固定  当中自适应</span></span><br><span class="line"><span class="comment">			2.当中列要完整显示</span></span><br><span class="line"><span class="comment">			3.当中列要优先加载</span></span><br><span class="line"><span class="comment">		--&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">			</span><br><span class="line">			*&#123;</span><br><span class="line">				margin: 0;</span><br><span class="line">				padding: 0;</span><br><span class="line">			&#125;</span><br><span class="line">			body&#123;</span><br><span class="line"><span class="css">				<span class="comment">/*2*left+right*/</span></span></span><br><span class="line">				min-width: 600px;</span><br><span class="line">			&#125;</span><br><span class="line">			div&#123;</span><br><span class="line">				height: 100px;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#left</span>,<span class="selector-id">#right</span>&#123;</span></span><br><span class="line">				width: 200px;</span><br><span class="line">				background: pink;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#left</span>&#123;</span></span><br><span class="line">				float: left;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#right</span>&#123;</span></span><br><span class="line">				float: right;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#middle</span>&#123;</span></span><br><span class="line">				background: deeppink;</span><br><span class="line">			&#125;</span><br><span class="line">		<span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"left"</span>&gt;</span>left<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"right"</span>&gt;</span>right<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"middle"</span>&gt;</span>middle<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="3-圣杯布局"><a href="#3-圣杯布局" class="headerlink" title="3. 圣杯布局"></a>3. 圣杯布局</h4><p>​    首先我们还是使用 float:left 让左右两部的元素浮动起来，但是由于我们的content是块级元素没办法让下面的元素上来，那么我们只能把它也设置为浮动，这样一来他的宽度就是被内容撑开了，我们需要设置他的宽度为body的100%，现在是content占据100%但是它允许下面浮动的元素上去。如果我们使用浮动让他们上去肯定就和上面的方法一样了，我们需要的是中间的元素有限加载也就是他应该出现在文档流的最前面。</p>
<p>​    可以采用负的margin，可以想象，我们的left的外边界和content是重叠的，那么我们如果移动他的外边界到content的起始位置那么他是不是就上去了呢？确实如此但是注意一个问题就是我们改变了外边距元素的位置实际上是不会动的，也就是在文档流中他还是在下面那一行，right的左边还是left这个块。所以我们right需要上去的时候必须margin为 -200</p>
<p>​    这样就好了，但是content的内容被挡住了。我们可以设把body变小，然后通过定位完成整个布局。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!--</span></span><br><span class="line"><span class="comment">			1.两边固定  当中自适应</span></span><br><span class="line"><span class="comment">			2.当中列要完整显示</span></span><br><span class="line"><span class="comment">			3.当中列要优先加载</span></span><br><span class="line"><span class="comment">		--&gt;</span></span><br><span class="line">		</span><br><span class="line">		<span class="comment">&lt;!--</span></span><br><span class="line"><span class="comment">			浮动:	搭建完整的布局框架</span></span><br><span class="line"><span class="comment">			margin 为赋值:调整旁边两列的位置(使三列布局到一行上)</span></span><br><span class="line"><span class="comment">			使用相对定位:调整旁边两列的位置（使两列位置调整到两头）</span></span><br><span class="line"><span class="comment">		--&gt;</span></span><br><span class="line">		</span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">			*&#123;</span><br><span class="line">				margin: 0;</span><br><span class="line">				padding: 0;</span><br><span class="line">			&#125;</span><br><span class="line">			body&#123;</span><br><span class="line">				min-width: 600px;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#content</span>&#123;</span></span><br><span class="line">				padding: 0 200px;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#header</span>,<span class="selector-id">#footer</span>&#123;</span></span><br><span class="line">				height: 20px;</span><br><span class="line">				text-align: center;</span><br><span class="line">				border: 1px solid  deeppink;</span><br><span class="line">				background: gray;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#content</span> <span class="selector-class">.middle</span>&#123;</span></span><br><span class="line">				float: left;</span><br><span class="line">				width: 100%;</span><br><span class="line">				background: pink;</span><br><span class="line"><span class="css">				<span class="comment">/*padding: 0 200px;*/</span></span></span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#content</span> <span class="selector-class">.left</span>&#123;</span></span><br><span class="line">				position: relative;</span><br><span class="line">				left: -200px;</span><br><span class="line">				margin-left: -100%;</span><br><span class="line">				float: left;</span><br><span class="line">				width: 200px;</span><br><span class="line">				background: yellow;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#content</span> <span class="selector-class">.right</span>&#123;</span></span><br><span class="line">				position: relative;</span><br><span class="line">				right: -200px;</span><br><span class="line">				margin-left: -200px;</span><br><span class="line">				float: left;</span><br><span class="line">				width: 200px;</span><br><span class="line">				background: yellow;</span><br><span class="line">			&#125;</span><br><span class="line">			</span><br><span class="line">			</span><br><span class="line">			</span><br><span class="line"><span class="css">			<span class="selector-class">.clearfix</span>&#123;</span></span><br><span class="line">				*zoom: 1;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-class">.clearfix</span><span class="selector-pseudo">:after</span>&#123;</span></span><br><span class="line">				content: "";</span><br><span class="line">				display: block;</span><br><span class="line">				clear: both;</span><br><span class="line">			&#125;</span><br><span class="line">		<span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"header"</span>&gt;</span>header<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"content"</span> <span class="attr">class</span>=<span class="string">"clearfix"</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"middle"</span>&gt;</span></span><br><span class="line">				<span class="tag">&lt;<span class="name">h4</span>&gt;</span>middle<span class="tag">&lt;/<span class="name">h4</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"left"</span>&gt;</span>left<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"right"</span>&gt;</span>right<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"footer"</span>&gt;</span>footer<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="4-双飞翼布局"><a href="#4-双飞翼布局" class="headerlink" title="4. 双飞翼布局"></a>4. 双飞翼布局</h4><p>​    在上面的圣杯布局的基础之上做的改进，其实做到两边都浮动然后只是content的内容不能居中，那么他们之间的区别就是如何让内容居中上。</p>
<p>​    圣杯是把padding应用在外面的body上，然后使用定位让他们分到两边。我们会考虑为什么不能用padding在conten上呢，实际上我们在content浮动以后使用padding就会把整个元素变大变宽，那么我们以前设置的left和right的margin的值需要重新设置不然就是错乱的。所以他在内部加了一层结构然后使用padding，这样的话由于left和right的margin只和外面的content的外边界有关系，所以里面的padding并不能影响整体的布局。</p>
<h3 id="2-伪等高布局"><a href="#2-伪等高布局" class="headerlink" title="2. 伪等高布局"></a>2. 伪等高布局</h3><p>​    如果我们两列高度不一样，需要让两列一一样高，我们采用的套路就是让他们的padding超级大，然后把整个的元素高度很高，然后让margin为负值把边界收回来，那么也就是他们的外部的盒子的外边距收回来了，此时再用 overflow:hidden;就好。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">			*&#123;</span><br><span class="line">				margin: 0;</span><br><span class="line">				padding: 0;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#wrap</span>&#123;</span></span><br><span class="line">				width: 750px;</span><br><span class="line">				border: 1px solid;</span><br><span class="line">				margin: 0 auto;</span><br><span class="line">				overflow: hidden;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#wrap</span> <span class="selector-class">.left</span>&#123;</span></span><br><span class="line">				float: left;</span><br><span class="line">				width: 200px;</span><br><span class="line">				background: pink;</span><br><span class="line">				padding-bottom: 1000px;</span><br><span class="line">				margin-bottom: -1000px;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#wrap</span> <span class="selector-class">.right</span>&#123;</span></span><br><span class="line">				float: left;</span><br><span class="line">				width: 500px;</span><br><span class="line">				background: deeppink;</span><br><span class="line">				padding-bottom: 1000px;</span><br><span class="line">				margin-bottom: -1000px;</span><br><span class="line">			&#125;</span><br><span class="line">			</span><br><span class="line">			</span><br><span class="line">			</span><br><span class="line"><span class="css">			<span class="selector-class">.clearfix</span>&#123;</span></span><br><span class="line">				*zoom: 1;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-class">.clearfix</span><span class="selector-pseudo">:after</span>&#123;</span></span><br><span class="line">				content: "";</span><br><span class="line">				display: block;</span><br><span class="line">				clear: both;</span><br><span class="line">			&#125;</span><br><span class="line">		<span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"wrap"</span> <span class="attr">class</span>=<span class="string">"clearfix"</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"left"</span>&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				left <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">			<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"right"</span>&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				right<span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">			<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="12-滚动条"><a href="#12-滚动条" class="headerlink" title="12. 滚动条"></a>12. 滚动条</h2><p>​    一般的在我们内容大于浏览器视窗的时候会出现滚动条，这时滚动条是应用在文档上的不是在html也不是在body上。如果我们希望修改这个设置我们有一条规则就是：</p>
<p>​    当我们只设置 body 和 html 中的overflow的时候他们这个属性是应用给文档的，但是如果两者同时使用这个属性那么html的设置给文档而body的设置给body。</p>
<p>​    实际上我们的固定定位和绝对定位的区别就在于滚动条上面，如果我们自己设置了滚动条那么就可以采用绝对定位来模拟固定定位。在移动端一般我们都是这么做的，因为我们需要禁用掉系统默认的滚动条。</p>
<p>​    <strong>注意：如果我们希望获得视窗的大小的话我们需要从html到body一层层的100%的继承下来不然height只会被内容撑开</strong></p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">			*&#123;</span><br><span class="line">				margin: 0;</span><br><span class="line">				padding: 0;</span><br><span class="line">			&#125;</span><br><span class="line">			html&#123;</span><br><span class="line">				overflow: hidden;</span><br><span class="line">				height: 100%;</span><br><span class="line">			&#125;</span><br><span class="line">			</span><br><span class="line">			body&#123;</span><br><span class="line">				overflow: auto;</span><br><span class="line">				height: 100%;</span><br><span class="line">			&#125;</span><br><span class="line">			</span><br><span class="line"><span class="css">			<span class="selector-id">#test</span>&#123;</span></span><br><span class="line">				position: absolute;</span><br><span class="line">				left: 50px;</span><br><span class="line">				top: 50px;</span><br><span class="line">				width: 100px;</span><br><span class="line">				height: 100px;</span><br><span class="line">				background: pink;</span><br><span class="line">			&#125;</span><br><span class="line">		<span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"test"</span>&gt;</span></span><br><span class="line">			</span><br><span class="line">		<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">style</span>=<span class="string">"height: 1000px;"</span>&gt;</span></span><br><span class="line">			csjkahcjk <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">			csjkahcjk <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">			csjkahcjk <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">			csjkahcjk <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">			csjkahcjk <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">			csjkahcjk <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">			csjkahcjk <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">			csjkahcjk <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="13-黏贴布局"><a href="#13-黏贴布局" class="headerlink" title="13. 黏贴布局"></a>13. 黏贴布局</h2><p>​    黏贴布局就是当内容没有达到底部的时候我们的内容在底部不动，而内容太多的时候可以把我们的底部挤到下面去。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">meta</span> <span class="attr">name</span>=<span class="string">"viewport"</span> <span class="attr">content</span>=<span class="string">"width=device-width,initial-scale=1.0,user-scalable=no"</span>/&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">title</span>&gt;</span><span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span>&gt;</span></span><br><span class="line">			*&#123;</span><br><span class="line">				margin: 0;</span><br><span class="line">				padding: 0;</span><br><span class="line">			&#125;</span><br><span class="line">			html,body&#123;</span><br><span class="line">				height: 100%;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#wrap</span>&#123;</span></span><br><span class="line">				min-height: 100%;</span><br><span class="line">				background: pink;</span><br><span class="line">				text-align: center;</span><br><span class="line">				overflow: hidden;</span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#wrap</span> <span class="selector-class">.main</span>&#123;</span></span><br><span class="line"><span class="css">				<span class="selector-tag">padding-bottom</span><span class="selector-pseudo">:50px</span> ;</span></span><br><span class="line">			&#125;</span><br><span class="line"><span class="css">			<span class="selector-id">#footer</span>&#123;</span></span><br><span class="line">				height: 50px;</span><br><span class="line">				line-height: 50px;</span><br><span class="line">				background: deeppink;</span><br><span class="line">				text-align: center;</span><br><span class="line">				margin-top: -50px;</span><br><span class="line">			&#125;</span><br><span class="line">		<span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"wrap"</span> &gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"main"</span>&gt;</span></span><br><span class="line">				main <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				main <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">				main <span class="tag">&lt;<span class="name">br</span> /&gt;</span></span><br><span class="line">			<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"footer"</span>&gt;</span></span><br><span class="line">			footer</span><br><span class="line">		<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>FrontEnd</category>
      </categories>
      <tags>
        <tag>CSS</tag>
      </tags>
  </entry>
  <entry>
    <title>Composer 使用指南</title>
    <url>/2019/01/05/PHP/Composer%E4%BD%BF%E7%94%A8%E6%8C%87%E5%8D%97/</url>
    <content><![CDATA[<h2 id="1-安装依赖"><a href="#1-安装依赖" class="headerlink" title="1. 安装依赖"></a>1. 安装依赖</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">composer install</span><br></pre></td></tr></table></figure>

<p>这个语句会检查当前目录下的 <strong>composer.json</strong> 然后检查是否有 <strong>composer.lock</strong> 如果有这个文件那么就按照 lock 文件中的版本号进行安装依赖，如果没有lock文件就按照 composer.json 中定义的版本进行安装。</p>
<p>这里就需要注意，composer.json 定义的版本号可能是一个区间也就是他可以是一个范围，随着类库的更新而更新，但是 lock文件保证了此项目的版本号是确定的，也就是项目组成员得到的版本号是一致的，就不会有很多麻烦问题了。</p>
<p>composer.json 中的版本的定义方式有：</p>
<table>
<thead>
<tr>
<th>名称</th>
<th>实例</th>
<th>描述</th>
</tr>
</thead>
<tbody><tr>
<td>确切的版本号</td>
<td><code>1.0.2</code></td>
<td>你可以指定包的确切版本。</td>
</tr>
<tr>
<td>范围</td>
<td><code>&gt;=1.0</code> `&gt;=1.0,&lt;2.0``&gt;=1.0,&lt;1.1</td>
<td>&gt;=1.2`</td>
</tr>
<tr>
<td>通配符</td>
<td><code>1.0.*</code></td>
<td>你可以使用通配符<code>*</code>来指定一种模式。<code>1.0.*</code>与<code>&gt;=1.0,&lt;1.1</code>是等效的。</td>
</tr>
<tr>
<td>赋值运算符</td>
<td><code>~1.2</code></td>
<td>一个常见的用法是标记你所依赖的最低版本，像 <code>~1.2</code> （允许1.2以上的任何版本，但不包括2.0）。<code>~1.2</code> 相当于 <code>&gt;=1.2,&lt;2.0</code>，而 <code>~1.2.3</code> 相当于 <code>&gt;=1.2.3,&lt;1.3</code>。<code>~1.2</code> 只意味着 <code>.2</code> 部分可以改变，但是 <code>1.</code> 部分是固定的。</td>
</tr>
</tbody></table>
<p>对的，这样就解决了项目成员的类库版本一致的问题，但是如果我们希望能够升级类库呢？那么就执行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">composer update</span><br></pre></td></tr></table></figure>

<p>如果只想安装或更新一个依赖，你可以白名单它们：</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">php composer.phar update monolog/monolog [...]</span><br></pre></td></tr></table></figure>

<h2 id="2-Packagist"><a href="#2-Packagist" class="headerlink" title="2. Packagist"></a>2. Packagist</h2><p><a href="https://packagist.org/" target="_blank" rel="noopener">packagist</a> 是 Composer 的主要资源库。 一个 Composer 的库基本上是一个包的源：记录了可以得到包的地方。但是没必要去国外的网站下载，我们可以采用国内的镜像加速：</p>
<h3 id="方法一：-修改-composer-的全局配置文件（推荐方式）"><a href="#方法一：-修改-composer-的全局配置文件（推荐方式）" class="headerlink" title="方法一： 修改 composer 的全局配置文件（推荐方式）"></a><strong>方法一：</strong> 修改 composer 的全局配置文件<strong>（推荐方式）</strong></h3><p>打开命令行窗口（windows用户）或控制台（Linux、Mac 用户）并执行如下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">composer config -g repo.packagist composer https://packagist.phpcomposer.com</span><br></pre></td></tr></table></figure>

<h3 id="方法二：-修改当前项目的-composer-json-配置文件："><a href="#方法二：-修改当前项目的-composer-json-配置文件：" class="headerlink" title="方法二： 修改当前项目的 composer.json 配置文件："></a><strong>方法二：</strong> 修改当前项目的 <code>composer.json</code> 配置文件：</h3><p>打开命令行窗口（windows用户）或控制台（Linux、Mac 用户），进入你的项目的根目录（也就是 <code>composer.json</code> 文件所在目录），执行如下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">composer config repo.packagist composer https://packagist.phpcomposer.com</span><br></pre></td></tr></table></figure>

<p>上述命令将会在当前项目中的 <code>composer.json</code> 文件的末尾自动添加镜像的配置信息（你也可以自己手工添加）：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">"repositories": &#123;</span><br><span class="line">    "packagist": &#123;</span><br><span class="line">        "type": "composer",</span><br><span class="line">        "url": "https://packagist.phpcomposer.com"</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-类库自动加载"><a href="#3-类库自动加载" class="headerlink" title="3. 类库自动加载"></a>3. 类库自动加载</h2><p>除了库的下载，Composer 还准备了一个自动加载文件，它可以加载 Composer 下载的库中所有的类文件。使用它，你只需要将下面这行代码添加到你项目的引导文件中：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">require</span> <span class="string">'vendor/autoload.php'</span>;</span><br></pre></td></tr></table></figure>

<h2 id="4-创建项目-create-project"><a href="#4-创建项目-create-project" class="headerlink" title="4. 创建项目 create-project"></a>4. 创建项目 create-project</h2><p>你可以使用 Composer 从现有的包中创建一个新的项目。这相当于执行了一个 <code>git clone</code> 或 <code>svn checkout</code> 命令后将这个包的依赖安装到它自己的 vendor 目录。</p>
<p>此命令有几个常见的用途：</p>
<ol>
<li>你可以快速的部署你的应用。</li>
<li>你可以检出任何资源包，并开发它的补丁。</li>
<li>多人开发项目，可以用它来加快应用的初始化。</li>
</ol>
<p>要创建基于 Composer 的新项目，你可以使用 “create-project” 命令。传递一个包名，它会为你创建项目的目录。你也可以在第三个参数中指定版本号，否则将获取最新的版本。</p>
<p>如果该目录目前不存在，则会在安装过程中自动创建。</p>
<figure class="highlight sh"><table><tr><td class="code"><pre><span class="line">php composer.phar create-project doctrine/orm path 2.2.*</span><br></pre></td></tr></table></figure>

<p>此外，你也可以无需使用这个命令，而是通过现有的 <code>composer.json</code> 文件来启动这个项目。</p>
<p>默认情况下，这个命令会在 packagist.org 上查找你指定的包。</p>
]]></content>
      <categories>
        <category>PHP</category>
      </categories>
      <tags>
        <tag>PHP,包管理器</tag>
      </tags>
  </entry>
  <entry>
    <title>记一次微信支付踩过的坑</title>
    <url>/2019/01/05/PHP/%E8%AE%B0%E4%B8%80%E6%AC%A1%E5%BE%AE%E4%BF%A1%E6%94%AF%E4%BB%98%E8%B8%A9%E8%BF%87%E7%9A%84%E5%9D%91/</url>
    <content><![CDATA[<p>​    最近在做一个微信小程序，然后里面涉及到了支付的内容，本来以为微信支付挺简单的，当我开始写支付后台去看微信提供的支付文档的时候我就崩溃了，文档写了跟没写一样。</p>
<p>​    本来是准备原生的php写后来觉得还是不太安全，而且真的文档不全很麻烦，就去找了一个类库。一会也会介绍一下这个类库的坑。</p>
<h2 id="1-微信支付流程"><a href="#1-微信支付流程" class="headerlink" title="1 . 微信支付流程"></a>1 . 微信支付流程</h2><p><img src="http://images.heniankj.com/20190105204258.png" alt></p>
<p>​    好了，这大概就是微信支付的流程了。下面来具体的说一下：</p>
<ol>
<li><p>首先是小程序调用 wx.login 获取用户当前登陆的 code 这个code是会变化的，并不能作为用户的标识</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><span class="line">wx.login(&#123;</span><br><span class="line">    success(res) &#123;</span><br><span class="line">        <span class="built_in">console</span>.log(res.code); <span class="comment">//获取的code</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>然后把 code 当做参数请求微信接口获取 opencode 这个就是用户的标识。这一步操作我把它放在了服务端，也就是我们的服务器会去请求微信的接口获取 opencode 返回给小程序。</p>
<p>后端做的就是请求微信接口：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line">$ret = curl_get_https(<span class="string">"https://api.weixin.qq.com/sns/jscode2session?appid=&#123;$config['appId']&#125;&amp;secret=&#123;$config['secret']&#125;&amp;js_code=$code&amp;grant_type=authorization_code"</span>);</span><br></pre></td></tr></table></figure>

<p>可以看到这里后台就是请求了这个 url 里面带了三个动态参数，有appid，secret都是在小程序的管理界面能看到的。code就是我们上一步获取到的，最后一个参数固定的。</p>
</li>
<li><p>获取到用户的opencode之后就可以让后台请求微信支付接口进行订单生成了。</p>
<p>后台请求支付接口，生成订单：</p>
<p>请求地址为：<code>https://api.mch.weixin.qq.com/pay/unifiedorder</code> 一些常见的必传参数是：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="string">'appid'</span> 微信支付分配的公众账号ID（企业号corpid即为此appId）</span><br><span class="line"> <span class="string">'mch_id'</span> 	微信支付分配的商户号,这个需要登录商户平台，申请通过以后直接就能看到的</span><br><span class="line"><span class="string">'prepayid'</span> $prepay_id</span><br><span class="line"><span class="string">'noncestr'</span> 随机字符串，长度要求在<span class="number">32</span>位以内，这个随便生成。</span><br><span class="line"><span class="string">'sign'</span> 通过签名算法计算得出的签名值，这个就是把请求的所有参数按照key的字典序拼串key1=val1&amp;key2=val2，最后拼上密钥key 的key和value</span><br><span class="line"><span class="string">'package'</span> prepay_id=$prepay_id</span><br><span class="line"><span class="string">'body'</span> 商品简单描述</span><br><span class="line"><span class="string">'total_fee'</span> 订单总金额，单位为分</span><br><span class="line"><span class="string">'notify_url'</span> 异步接收微信支付结果通知的回调地址，通知url必须为外网可访问的url，不能携带参数。</span><br><span class="line"><span class="string">'out_trade_no'</span>   商户系统内部订单号，要求<span class="number">32</span>个字符内，只能是数字、大小写字母</span><br><span class="line"><span class="string">'spbill_create_ip'</span> 支持IPV4和IPV6两种格式的IP地址。调用微信支付API的机器IP</span><br><span class="line"><span class="string">'trade_type '</span> JSAPI -JSAPI支付NATIVE -Native支付APP -APP支付</span><br></pre></td></tr></table></figure>
</li>
<li><p>上述的操作完成以后，需要写一个通知回调的地址，这个地址里面的逻辑代码就是处理我们数据库订单的内容，比如把订单状态设置为已支付，或者创建订单将货物设置为已发货。这个回调在不出任何问题的情况下我们需要返回一段特定的字符串，否则微信认为逻辑没有执行成功，他会继续回调这个回调地址。如果你发现你的逻辑是错的他一直回调这个地址貌似没有办法让他停止回调，他可能有自己的策略在一定时间后停止回调。成功的字符串是：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="keyword">echo</span> <span class="string">'&lt;xml&gt;&lt;return_code&gt;&lt;![CDATA[SUCCESS]]&gt;&lt;/return_code&gt;&lt;return_msg&gt;&lt;![CDATA[OK]]&gt;&lt;/return_msg&gt;&lt;/xml&gt;'</span>;</span><br></pre></td></tr></table></figure>

<p>注意不要再有其他的任何输出，并且字符串中不要有空格。</p>
</li>
<li><p>最后会返回订单的信息，此时只需要调用微信小程序的  wx.requestPayment 并且传递以下必要参数：</p>
<figure class="highlight php"><table><tr><td class="code"><pre><span class="line"><span class="string">'timeStamp'</span>: data[<span class="string">'timestamp'</span>],</span><br><span class="line"><span class="string">'nonceStr'</span>: data[<span class="string">'noncestr'</span>],</span><br><span class="line"><span class="string">'package'</span>: data[<span class="string">'package'</span>],</span><br><span class="line"><span class="string">'signType'</span>: <span class="string">'MD5'</span>,</span><br><span class="line"><span class="string">'paySign'</span>: data[<span class="string">'sign'</span>],</span><br></pre></td></tr></table></figure>

<p>这时小程序会自动拉起微信支付进行支付。</p>
</li>
</ol>
<h2 id="2-微信支付类库解析"><a href="#2-微信支付类库解析" class="headerlink" title="2. 微信支付类库解析"></a>2. 微信支付类库解析</h2>]]></content>
      <categories>
        <category>PHP</category>
      </categories>
      <tags>
        <tag>PHP,微信</tag>
      </tags>
  </entry>
  <entry>
    <title>七牛云关联Windows图床</title>
    <url>/2019/01/03/Tools/%E4%B8%83%E7%89%9B%E4%BA%91%E5%85%B3%E8%81%94Windows%E5%9B%BE%E5%BA%8A/</url>
    <content><![CDATA[<h1 id="1-注册七牛云"><a href="#1-注册七牛云" class="headerlink" title="1. 注册七牛云"></a>1. 注册七牛云</h1><p><a href="https://portal.qiniu.com/create" target="_blank" rel="noopener">七牛云</a> 地址，需要在这里进行注册</p>
<h1 id="2-完成实名认证"><a href="#2-完成实名认证" class="headerlink" title="2.完成实名认证"></a>2.完成实名认证</h1><p>需要上传身份证的正反面以及支付宝做一下认证即可。</p>
<p>首先进入个人中心</p>
<p><img src="http://images.heniankj.com/20190103152010.png" alt></p>
<p>然后进行实名认证</p>
<p><img src="http://images.heniankj.com/20190103152132.png" alt></p>
<p>由于我已经认证过了，所以显示认证完成，未认证的用户需要按照提示认证，一般来说 5分钟就能完成认证。</p>
<h1 id="3-创建对象存储"><a href="#3-创建对象存储" class="headerlink" title="3. 创建对象存储"></a>3. 创建对象存储</h1><p><img src="http://images.heniankj.com/20190103152527.png" alt></p>
<p>只需要填一下名字，然后因为是图床所以肯定是公开的访问权限。</p>
<p><img src="F:%5COneDrive%5CBlog%5Csource%5Cimages%5C20190103152647.png" alt></p>
<h1 id="4-绑定域名"><a href="#4-绑定域名" class="headerlink" title="4. 绑定域名"></a>4. 绑定域名</h1><p>配置完空间以后就是需要关联域名，配置 CNAME </p>
<p><img src="http://images.heniankj.com/20190103152918.png" alt></p>
<p>绑定域名，这个域名需要是一个已经备案过的域名。</p>
<p><img src="F:%5COneDrive%5CBlog%5Csource%5Cimages%5C20190103153019.png" alt></p>
<p>只需要填写域名这一项即可，最后就是需要到你的域名管理后台配置一个域名的解析，首先打开这个地址 <a href="https://portal.qiniu.com/cdn/domain/" target="_blank" rel="noopener">https://portal.qiniu.com/cdn/domain/</a> 然后找到你的域名选择复制CNAME</p>
<p><img src="http://images.heniankj.com/20190103153215.png" alt></p>
<p>然后需要做一个域名解析，比如到阿里云上</p>
<p><img src="http://images.heniankj.com/20190103153419.png" alt></p>
<p>等一会会 <a href="https://portal.qiniu.com/cdn/domain/" target="_blank" rel="noopener">https://portal.qiniu.com/cdn/domain/</a> 打开这个链接如果看到解析成功则说明配置完成了。</p>
<h1 id="5-关联-PicGo"><a href="#5-关联-PicGo" class="headerlink" title="5. 关联 PicGo"></a>5. 关联 PicGo</h1><p>这款软件是一个跨平台的图床软件个人觉得很好用。直接百度就可以下载到。</p>
<p><img src="http://images.heniankj.com/20190103153719.png" alt></p>
<p>做完配置，然后确定，设置为默认图床就可以在上传区上传文件了！</p>
]]></content>
      <categories>
        <category>工具</category>
      </categories>
      <tags>
        <tag>工具</tag>
      </tags>
  </entry>
  <entry>
    <title>Apache配置详解</title>
    <url>/2019/01/03/Linux/Apache%E9%85%8D%E7%BD%AE%E8%99%9A%E6%8B%9F%E4%B8%BB%E6%9C%BA/</url>
    <content><![CDATA[<h1 id="1-虚拟主机概念"><a href="#1-虚拟主机概念" class="headerlink" title="1. 虚拟主机概念"></a>1. 虚拟主机概念</h1><p>我们要想实现一个web站点，而且能够在互联网上被访问，首先它再能运行在操作系统，而且这个操作系统还要运行在物理主机上（第一它是一个主机）。在互联网上能够被访问，那我们需要一个主机，需要一个IP地址，需要一个时时在线的服务器，这需要多少资源？对众多小型站点来讲或者说对某种需求来讲，有可能都用不到服务器，也就是每天就10个人左右访问，只是需要我们在线而已，如果我们就为这一点点的需求就投入重大的资源的话是非常浪费的。我们就期望能够像我们使用虚拟机一样，虚拟的OS一样或虚拟的PC一样，能够在一台物理主机上虚拟出来多个可以同时运行的站点或者我们把它称为主机因此就把它称为虚拟主机。</p>
<h1 id="2-Apache-主机的类型"><a href="#2-Apache-主机的类型" class="headerlink" title="2. Apache 主机的类型"></a>2. Apache 主机的类型</h1><h3 id="1-中心主机"><a href="#1-中心主机" class="headerlink" title="1. 中心主机"></a>1. 中心主机</h3><h3 id="2-虚拟主机"><a href="#2-虚拟主机" class="headerlink" title="2. 虚拟主机"></a>2. 虚拟主机</h3><ol>
<li><p>基于IP: 端口相同，IP地址不同。</p>
</li>
<li><p>基于端口：IP相同，端口不同。</p>
</li>
<li><p>基于域名：IP地址相同，端口相同主机名不同</p>
</li>
</ol>
<p><strong>注意：所有的虚拟主机的配置我们都需要取消中心主机，也就是注释掉 DocumentRoot 这是配置虚拟主机的前提</strong></p>
<h1 id="3-基于域名的虚拟主机"><a href="#3-基于域名的虚拟主机" class="headerlink" title="3. 基于域名的虚拟主机"></a>3. 基于域名的虚拟主机</h1><p>例如我采用的 xampp 所以配置虚拟主机就在 <code>C:\xampp\apache\conf\extra\httpd-vhosts.conf</code> 中配置，由于这个文件默认被 include 到主配置文件中了，所以在这里的修改都可以生效。</p>
<p>首先需要保证主配置文件中的中心主机被取消了也就是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#DocumentRoot "C:/xampp/htdocs"</span></span><br></pre></td></tr></table></figure>

<p>然后打开 <code>httpd-vhosts.conf</code> 配置文件，按照下面的格式配置虚拟主机</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;VirtualHost *:80&gt;</span><br><span class="line">	DocumentRoot &quot;C:/xampp/htdocs&quot;</span><br><span class="line">	ServerName localhost</span><br><span class="line">	&lt;Directory &quot;C:/xampp/htdocs&quot;&gt;</span><br><span class="line">		Options Indexes FollowSymLinks Includes ExecCGI</span><br><span class="line">		AllowOverride All</span><br><span class="line">		Require all granted</span><br><span class="line">	&lt;/Directory&gt;</span><br><span class="line">&lt;/VirtualHost&gt;</span><br></pre></td></tr></table></figure>

<h2 id="1-lt-VirtualHost-80-gt"><a href="#1-lt-VirtualHost-80-gt" class="headerlink" title="1.  &lt;VirtualHost *:80&gt;"></a>1.  <code>&lt;VirtualHost *:80&gt;</code></h2><p> apache监听本机的所有 IP 和 80 端口做多域名虚拟主机</p>
<h2 id="2-DocumentRoot"><a href="#2-DocumentRoot" class="headerlink" title="2. DocumentRoot"></a>2. DocumentRoot</h2><p>表示服务器的根目录</p>
<h2 id="3-ServerName"><a href="#3-ServerName" class="headerlink" title="3. ServerName"></a>3. ServerName</h2><p>就是表示域名，我们采用域名方式配置虚拟主机，所以每个虚拟主机的域名应该是不一样的才行</p>
<h2 id="4-Directory"><a href="#4-Directory" class="headerlink" title="4. Directory"></a>4. Directory</h2><p>对根目录的规则应用，其中涉及到对于目录的访问权限和其他配置问题</p>
<h2 id="5-对于-Directory-指令解析"><a href="#5-对于-Directory-指令解析" class="headerlink" title="5. 对于 Directory 指令解析"></a>5. 对于 Directory 指令解析</h2><h3 id="1-Options"><a href="#1-Options" class="headerlink" title="1.  Options"></a>1.  Options</h3><p>配置在特定目录使用哪些特性，常用的值和基本含义如下</p>
<ol>
<li>ExecCGI: 在该目录下允许执行CGI脚本。</li>
<li>FollowSymLinks: 在该目录下允许文件系统使用符号连接。</li>
<li>Indexes: 当用户访问该目录时，如果用户找不到DirectoryIndex指定的主页文件(例如index.html),则返回该目录下的文件列表给用户。</li>
<li>SymLinksIfOwnerMatch: 当使用符号连接时，只有当符号连接的文件拥有者与实际文件的拥有者相同时才可以访问。</li>
</ol>
<p>所以我们一般在配置 PHP 的时候所配置的内容是</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Options Indexes FollowSymLinks Includes ExecCGI</span><br></pre></td></tr></table></figure>

<h3 id="2-AllowOverride"><a href="#2-AllowOverride" class="headerlink" title="2. AllowOverride"></a>2. AllowOverride</h3><p>允许存在于.htaccess文件中的指令类型(.htaccess文件名是可以改变的，其文件名由AccessFileName指令决定)：</p>
<ul>
<li>None: 当AllowOverride被设置为None时。不搜索该目录下的.htaccess文件（可以减小服务器开销）。</li>
<li>All: 在.htaccess文件中可以使用所有的指令。</li>
</ul>
<p>建议关闭这个选项，因为apache在文档中已经明确支持不建议使用它了，主要是会降低服务器性能，.htaccess 文件可以做到的我们都可以在 Directory 指令中做的更好。</p>
<h3 id="3-Order"><a href="#3-Order" class="headerlink" title="3. Order"></a>3. Order</h3><p>控制在访问时Allow和Deny两个访问规则哪个优先，也就是黑白名单的匹配顺序</p>
<ul>
<li>Allow：允许访问的主机列表(可用域名或子网，例如：Allow from 192.168.0.0/16)。</li>
<li>Deny：拒绝访问的主机列表。</li>
</ul>
<blockquote>
<p>更详细的用法可参看：<a href="https://httpd.apache.org/docs/trunk/zh-cn/mod/mod_access_compat.html#order" target="_blank" rel="noopener">order用法</a></p>
</blockquote>
<p>匹配原则：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Allow,Deny</span><br></pre></td></tr></table></figure>

<p>First, all <code>Allow</code> directives are evaluated; at least one must match, or the request is rejected. Next, all <code>Deny</code> directives are evaluated. If any matches, the request is rejected. Last, any requests which do not match an <code>Allow</code> or a <code>Deny</code> directive are denied by default.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Deny,Allow</span><br></pre></td></tr></table></figure>

<p>First, all <code>Deny</code> directives are evaluated; if any match, the request is denied <strong>unless</strong> it also matches an <code>Allow</code> directive. Any requests which do not match any <code>Allow</code> or <code>Deny</code> directives are permitted.</p>
<h3 id="4-DirectoryIndex"><a href="#4-DirectoryIndex" class="headerlink" title="4. DirectoryIndex"></a>4. DirectoryIndex</h3><p>主页文件设置 一般都是 index.html  index.php</p>
<h3 id="5-Deny-Allow"><a href="#5-Deny-Allow" class="headerlink" title="5. Deny /Allow"></a>5. Deny /Allow</h3><p>黑白名单书写规则如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Allow from example.org</span><br><span class="line">Allow from .net example.edu</span><br><span class="line">Allow from 10.1.2.3</span><br><span class="line">Allow from 192.168.1.104 192.168.1.205</span><br><span class="line">Allow all</span><br></pre></td></tr></table></figure>

<h1 id="4-基于端口的虚拟主机"><a href="#4-基于端口的虚拟主机" class="headerlink" title="4. 基于端口的虚拟主机"></a>4. 基于端口的虚拟主机</h1><p>对于同一个 ip 来做虚拟主机，就是需要监听不同的端口，首先需要在主配置文件中添加新的监听端口：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Listen 80</span><br><span class="line">Listen 81</span><br></pre></td></tr></table></figure>

<p>然后再虚拟主机里面配置</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;Virtualhost *:80&gt;</span><br><span class="line">	DocumentRoot &quot;/var1&quot;</span><br><span class="line">&lt;/Virtualhost&gt;</span><br><span class="line">&lt;Virtualhost *:81&gt;</span><br><span class="line">	DocumentRoot &quot;/var2&quot;</span><br><span class="line">&lt;/Virtualhost&gt;</span><br></pre></td></tr></table></figure>

<h1 id="5-基于ip的虚拟主机"><a href="#5-基于ip的虚拟主机" class="headerlink" title="5. 基于ip的虚拟主机"></a>5. 基于ip的虚拟主机</h1><p>这个就不过多赘述了，完全和基于端口的配置方式一样。也就是修改的是ip 而非端口、</p>
<h1 id="6-服务器的优化-MPM-Multi-Processing-Modules"><a href="#6-服务器的优化-MPM-Multi-Processing-Modules" class="headerlink" title="6.服务器的优化 (MPM: Multi-Processing Modules)"></a>6.服务器的优化 (MPM: Multi-Processing Modules)</h1><p>apache2主要的优势就是对多处理器的支持更好，在编译时同过使用–with-mpm选项来决定apache2的工作模式。如果知道当前的apache2使用什么工作机制，可以通过httpd -l命令列出apache的所有模块，就可以知道其工作方式：</p>
<ul>
<li><p>prefork：如果httpd -l列出prefork.c，则需要对下面的段进行配置。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;IfModule prefork.c&gt;</span><br><span class="line"> StartServers 5 #启动apache时启动的httpd进程个数。</span><br><span class="line"> MinSpareServers 5 #服务器保持的最小空闲进程数。</span><br><span class="line"> MaxSpareServers 10 #服务器保持的最大空闲进程数。</span><br><span class="line"> MaxClients 150 #最大并发连接数。</span><br><span class="line"> MaxRequestsPerChild 1000 #每个子进程被请求服务多少次后被kill掉。0表示不限制，推荐设置为1000。</span><br><span class="line"> &lt;/IfModule&gt;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<p>在该工作模式下，服务器启动后起动5个httpd进程(加父进程共6个，通过ps -ax|grep httpd命令可以看到)。当有用户连接时，apache会使用一个空闲进程为该连接服务，同时父进程会fork一个子进程。直到内存中的空闲进程达到MaxSpareServers。该模式是为了兼容一些旧版本的程序。我缺省编译时的选项。</p>
<ul>
<li>worker：如果httpd -l列出worker.c，则需要对下面的段进行配置：</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;IfModule worker.c&gt; </span><br><span class="line">    StartServers 2 #启动apache时启动的httpd进程个数。 </span><br><span class="line">    MaxClients 150 #最大并发连接数。 </span><br><span class="line">    MinSpareThreads 25 #服务器保持的最小空闲线程数。 </span><br><span class="line">    MaxSpareThreads 75 #服务器保持的最大空闲线程数。 </span><br><span class="line">    ThreadsPerChild 25 #每个子进程的产生的线程数。 </span><br><span class="line">    MaxRequestsPerChild 0 #每个子进程被请求服务多少次后被kill掉。0表示不限制，推荐设置为1000。 </span><br><span class="line">&lt;/IfModule&gt;</span><br></pre></td></tr></table></figure>

<p>该模式是由线程来监听客户的连接。当有新客户连接时，由其中的一个空闲线程接受连接。服务器在启动时启动两个进程，每个进程产生的线程数是固定的(ThreadsPerChild决定)，因此启动时有50个线程。当50个线程不够用时，服务器自动fork一个进程，再产生25个线程。</p>
<ul>
<li>perchild：如果httpd -l列出perchild.c，则需要对下面的段进行配置：</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;IfModule perchild.c&gt; </span><br><span class="line">    NumServers 5 #服务器启动时启动的子进程数 </span><br><span class="line">    StartThreads 5 #每个子进程启动时启动的线程数 </span><br><span class="line">    MinSpareThreads 5 #内存中的最小空闲线程数 </span><br><span class="line">    MaxSpareThreads 10 #最大空闲线程数 </span><br><span class="line">    MaxThreadsPerChild 2000 #每个线程最多被请求多少次后退出。0不受限制。 </span><br><span class="line">    MaxRequestsPerChild 10000 #每个子进程服务多少次后被重新fork。0表示不受限制。 </span><br><span class="line">&lt;/IfModule&gt;</span><br></pre></td></tr></table></figure>

<p>该模式下，子进程的数量是固定的，线程数不受限制。当客户端连接到服务器时，又空闲的线程提供服务。 如果空闲线程数不够，子进程自动产生线程来为新的连接服务。该模式用于多站点服务器。</p>
<h1 id="7-别名设置"><a href="#7-别名设置" class="headerlink" title="7.别名设置"></a>7.别名设置</h1><p>对于不在DocumentRoot指定的目录内的页面，既可以使用符号连接，也可以使用别名。别名的设置如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Alias /download/ &quot;/var/www/download/&quot; #访问时可以输入:http://www.custing.com/download/ </span><br><span class="line"> </span><br><span class="line">&lt;Directory &quot;/var/www/download&quot;&gt; #对该目录进行访问控制设置 </span><br><span class="line">    Options Indexes MultiViews </span><br><span class="line">    AllowOverride AuthConfig </span><br><span class="line">    Order allow,deny </span><br><span class="line">    Allow from all </span><br><span class="line">&lt;/Directory&gt;</span><br></pre></td></tr></table></figure>

<h4 id="6、CGI设置"><a href="#6、CGI设置" class="headerlink" title="6、CGI设置"></a>6、CGI设置</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 访问时可以：http://www.clusting.com/cgi-bin/，但是该目录下的CGI脚本文件要加可执行权限</span><br><span class="line">ScriptAlias /cgi-bin/ &quot;/mnt/software/apache2/cgi-bin/&quot; </span><br><span class="line"> </span><br><span class="line">&lt;Directory &quot;/usr/local/apache2/cgi-bin&quot;&gt; #设置目录属性 </span><br><span class="line">    AllowOverride None </span><br><span class="line">    Options None </span><br><span class="line">    Order allow,deny </span><br><span class="line">    Allow from all </span><br><span class="line">&lt;/Directory&gt;</span><br></pre></td></tr></table></figure>

<h1 id="8、日志的设置"><a href="#8、日志的设置" class="headerlink" title="8、日志的设置"></a>8、日志的设置</h1><h5 id="1-错误日志的设置"><a href="#1-错误日志的设置" class="headerlink" title="(1) 错误日志的设置"></a>(1) 错误日志的设置</h5><ul>
<li><p>ErrorLog logs/error_log :日志的保存位置</p>
</li>
<li><p>LogLevel warn #日志的级别</p>
</li>
</ul>
<p>显示的格式日下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[Mon Oct 10 15:54:29 2005] [error] [client 192.168.10.22] access to /download/ failed, reason: user admin not allowed access</span><br></pre></td></tr></table></figure>

<h5 id="2-访问日志设置"><a href="#2-访问日志设置" class="headerlink" title="(2) 访问日志设置"></a>(2) 访问日志设置</h5><p>日志的缺省格式有如下几种：</p>
<ul>
<li>LogFormat “%h %l %u %t “%r” %&gt;s %b “%{Referer}i” “%{User-Agent}i”” combined</li>
<li>LogFormat “%h %l %u %t “%r” %&gt;s %b” common #common为日志格式名称</li>
<li>LogFormat “%{Referer}i -&gt; %U” referer</li>
<li>LogFormat “%{User-agent}i” agent</li>
<li>CustomLog logs/access_log common</li>
</ul>
<p>格式中的各个参数如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">%h --客户端的ip地址或主机名 </span><br><span class="line">%l --The 这是由客户端 identd 判断的RFC 1413身份，输出中的符号 &quot;-&quot; 表示此处信息无效。 </span><br><span class="line">%u --由HTTP认证系统得到的访问该网页的客户名。有认证时才有效，输出中的符号 &quot;-&quot; 表示此处信息无效。 </span><br><span class="line">%t --服务器完成对请求的处理时的时间。 </span><br><span class="line">&quot;%r&quot; --引号中是客户发出的包含了许多有用信息的请求内容。 </span><br><span class="line">%&gt;s --这个是服务器返回给客户端的状态码。 </span><br><span class="line">%b --最后这项是返回给客户端的不包括响应头的字节数。 </span><br><span class="line">&quot;%&#123;Referer&#125;i&quot; --此项指明了该请求是从被哪个网页提交过来的。 </span><br><span class="line">&quot;%&#123;User-Agent&#125;i&quot; --此项是客户浏览器提供的浏览器识别信息。</span><br></pre></td></tr></table></figure>

<p>下面是一段访问日志的实例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">192.168.10.22 - bearzhang [10/Oct/2005:16:53:06 +0800] &quot;GET /download/ HTTP/1.1&quot; 200 1228 </span><br><span class="line">192.168.10.22 - - [10/Oct/2005:16:53:06 +0800] &quot;GET /icons/blank.gif HTTP/1.1&quot; 304 - </span><br><span class="line">192.168.10.22 - - [10/Oct/2005:16:53:06 +0800] &quot;GET /icons/back.gif HTTP/1.1&quot; 304 –</span><br></pre></td></tr></table></figure>

<h1 id="9、用户认证的配置"><a href="#9、用户认证的配置" class="headerlink" title="9、用户认证的配置"></a>9、用户认证的配置</h1><h5 id="1-httpd-conf用户认证配置"><a href="#1-httpd-conf用户认证配置" class="headerlink" title="(1) httpd.conf用户认证配置:"></a>(1) httpd.conf用户认证配置:</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">AccessFileName .htaccess </span><br><span class="line">......... </span><br><span class="line">Alias /download/ &quot;/var/www/download/&quot; </span><br><span class="line">&lt;Directory &quot;/var/www/download&quot;&gt; </span><br><span class="line">    Options Indexes </span><br><span class="line">    AllowOverride AuthConfig </span><br><span class="line">&lt;/Directory&gt;</span><br></pre></td></tr></table></figure>

<h5 id="2-create-a-password-file"><a href="#2-create-a-password-file" class="headerlink" title="(2) create a password file:"></a>(2) create a password file:</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/usr/local/apache2/bin/htpasswd -c /var/httpuser/passwords bearzhang</span><br></pre></td></tr></table></figure>

<h5 id="3-configure-the-server-to-request-a-password-and-tell-the-server-which-users-are-allowed-access"><a href="#3-configure-the-server-to-request-a-password-and-tell-the-server-which-users-are-allowed-access" class="headerlink" title="(3) configure the server to request a password and tell the server which users are allowed access."></a>(3) configure the server to request a password and tell the server which users are allowed access.</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">vi /var/www/download/.htaccess: </span><br><span class="line"> </span><br><span class="line">AuthType Basic </span><br><span class="line">AuthName &quot;Restricted Files&quot; </span><br><span class="line">AuthUserFile /var/httpuser/passwords </span><br><span class="line">Require user bearzhang </span><br><span class="line">#Require valid-user #all valid user</span><br></pre></td></tr></table></figure>

<h1 id="10、虚拟主机的配置总结"><a href="#10、虚拟主机的配置总结" class="headerlink" title="10、虚拟主机的配置总结"></a>10、虚拟主机的配置总结</h1><h5 id="1-基于IP地址的虚拟主机配置"><a href="#1-基于IP地址的虚拟主机配置" class="headerlink" title="(1)基于IP地址的虚拟主机配置"></a>(1)基于IP地址的虚拟主机配置</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Listen 80 </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.40&gt; </span><br><span class="line">    DocumentRoot /www/example1 </span><br><span class="line">    ServerName www.example1.com </span><br><span class="line">&lt;/VirtualHost&gt; </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.50&gt; </span><br><span class="line">    DocumentRoot /www/example2 </span><br><span class="line">    ServerName www.example2.org </span><br><span class="line">&lt;/VirtualHost&gt;</span><br></pre></td></tr></table></figure>

<h5 id="2-基于IP和多端口的虚拟主机配置"><a href="#2-基于IP和多端口的虚拟主机配置" class="headerlink" title="(2) 基于IP和多端口的虚拟主机配置"></a>(2) 基于IP和多端口的虚拟主机配置</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Listen 172.20.30.40:80 </span><br><span class="line">Listen 172.20.30.40:8080 </span><br><span class="line">Listen 172.20.30.50:80 </span><br><span class="line">Listen 172.20.30.50:8080 </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.40:80&gt; </span><br><span class="line">    DocumentRoot /www/example1-80 </span><br><span class="line">    ServerName www.example1.com </span><br><span class="line">&lt;/VirtualHost&gt; </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.40:8080&gt; </span><br><span class="line">    DocumentRoot /www/example1-8080</span><br><span class="line">    ServerName www.example1.com </span><br><span class="line">&lt;/VirtualHost&gt; </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.50:80&gt; </span><br><span class="line">    DocumentRoot /www/example2-80 </span><br><span class="line">    ServerName www.example1.org </span><br><span class="line">&lt;/VirtualHost&gt; </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.50:8080&gt; </span><br><span class="line">    DocumentRoot /www/example2-8080 </span><br><span class="line">    ServerName www.example2.org </span><br><span class="line">&lt;/VirtualHost&gt;</span><br></pre></td></tr></table></figure>

<h5 id="3-单个IP地址的服务器上基于域名的虚拟主机配置："><a href="#3-单个IP地址的服务器上基于域名的虚拟主机配置：" class="headerlink" title="(3) 单个IP地址的服务器上基于域名的虚拟主机配置："></a>(3) 单个IP地址的服务器上基于域名的虚拟主机配置：</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Ensure that Apache listens on port 80 </span><br><span class="line">Listen 80 </span><br><span class="line"> </span><br><span class="line"># Listen for virtual host requests on all IP addresses </span><br><span class="line">NameVirtualHost *:80 </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost *:80&gt; </span><br><span class="line">    DocumentRoot /www/example1 </span><br><span class="line">    ServerName www.example1.com </span><br><span class="line">    ServerAlias example1.com. *.example1.com </span><br><span class="line">    # Other directives here </span><br><span class="line">&lt;/VirtualHost&gt; </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost *:80&gt; </span><br><span class="line">    DocumentRoot /www/example2 </span><br><span class="line">    ServerName www.example2.org </span><br><span class="line">    # Other directives here </span><br><span class="line">&lt;/VirtualHost&gt;</span><br></pre></td></tr></table></figure>

<h5 id="4-在多个IP地址的服务器上配置基于域名的虚拟主机："><a href="#4-在多个IP地址的服务器上配置基于域名的虚拟主机：" class="headerlink" title="(4) 在多个IP地址的服务器上配置基于域名的虚拟主机："></a>(4) 在多个IP地址的服务器上配置基于域名的虚拟主机：</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Listen 80 </span><br><span class="line"> </span><br><span class="line"># This is the &quot;main&quot; server running on 172.20.30.40 </span><br><span class="line">ServerName server.domain.com </span><br><span class="line">DocumentRoot /www/mainserver </span><br><span class="line"> </span><br><span class="line"># This is the other address </span><br><span class="line">NameVirtualHost 172.20.30.50 </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.50&gt; </span><br><span class="line">    DocumentRoot /www/example1 </span><br><span class="line">    ServerName www.example1.com </span><br><span class="line">    # Other directives here ... </span><br><span class="line">&lt;/VirtualHost&gt; </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.50&gt; </span><br><span class="line">    DocumentRoot /www/example2 </span><br><span class="line">    ServerName www.example2.org </span><br><span class="line">    # Other directives here ... </span><br><span class="line">&lt;/VirtualHost&gt;</span><br></pre></td></tr></table></figure>

<h5 id="5-在不同的端口上运行不同的站点-基于多端口的服务器上配置基于域名的虚拟主机-："><a href="#5-在不同的端口上运行不同的站点-基于多端口的服务器上配置基于域名的虚拟主机-：" class="headerlink" title="(5) 在不同的端口上运行不同的站点(基于多端口的服务器上配置基于域名的虚拟主机)："></a>(5) 在不同的端口上运行不同的站点(基于多端口的服务器上配置基于域名的虚拟主机)：</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Listen 80 </span><br><span class="line">Listen 8080 </span><br><span class="line"> </span><br><span class="line">NameVirtualHost 172.20.30.40:80 </span><br><span class="line">NameVirtualHost 172.20.30.40:8080 </span><br><span class="line"></span><br><span class="line">&lt;VirtualHost 172.20.30.40:80&gt; </span><br><span class="line">    ServerName www.example1.com </span><br><span class="line">    DocumentRoot /www/domain-80 </span><br><span class="line">&lt;/VirtualHost&gt; </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.40:8080&gt; </span><br><span class="line">    ServerName www.example1.com</span><br><span class="line">    DocumentRoot /www/domain-8080 </span><br><span class="line">&lt;/VirtualHost&gt; </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.40:80&gt;</span><br><span class="line">    ServerName www.example2.org </span><br><span class="line">    DocumentRoot /www/otherdomain-80 </span><br><span class="line">&lt;/VirtualHost&gt; </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.40:8080&gt; </span><br><span class="line">    ServerName www.example2.org </span><br><span class="line">    DocumentRoot /www/otherdomain-8080 </span><br><span class="line">&lt;/VirtualHost&gt;</span><br></pre></td></tr></table></figure>

<h5 id="6-基于域名和基于IP的混合虚拟主机的配置"><a href="#6-基于域名和基于IP的混合虚拟主机的配置" class="headerlink" title="(6) 基于域名和基于IP的混合虚拟主机的配置:"></a>(6) 基于域名和基于IP的混合虚拟主机的配置:</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Listen 80 </span><br><span class="line"> </span><br><span class="line">NameVirtualHost 172.20.30.40 </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.40&gt; </span><br><span class="line">    DocumentRoot /www/example1 </span><br><span class="line">    ServerName www.example1.com </span><br><span class="line">&lt;/VirtualHost&gt; </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.40&gt; </span><br><span class="line">    DocumentRoot /www/example2 </span><br><span class="line">    ServerName www.example2.org </span><br><span class="line">&lt;/VirtualHost&gt; </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost 172.20.30.40&gt; </span><br><span class="line">    DocumentRoot /www/example3 </span><br><span class="line">    ServerName www.example3.net </span><br><span class="line">&lt;/VirtualHost&gt;</span><br></pre></td></tr></table></figure>

<h1 id="11-SSL加密的配置"><a href="#11-SSL加密的配置" class="headerlink" title="11.SSL加密的配置"></a>11.SSL加密的配置</h1><p>首先在配置之前先来了解一些基本概念：</p>
<blockquote>
<p>a. 证书的概念：首先要有一个根证书，然后用根证书来签发服务器证书和客户证书，一般理解：服务器证书和客户证书是平级关系。SSL必须安装 服务器证书来认证。 因此：在此环境中，至少必须有三个证书：根证书，服务器证书，客户端证书。 在生成证书之前，一般会有一个私钥，同时用私钥生成证书请求，再利用证书服务器的根证来签发证书。 SSL所使用的证书可以自己生成，也可以通过一个商业性CA（如Verisign 或 Thawte）签署证书。</p>
</blockquote>
<blockquote>
<p>b. 签发证书的问题：如果使用的是商业证书，具体的签署方法请查看相关销售商的说明；如果是知己签发的证书，可以使用openssl自带的CA.sh  脚本工具。</p>
</blockquote>
<p>如果不为单独的客户端签发证书，客户端证书可以不用生成，客户端与服务器端使用相同的证书。</p>
<h4 id="1-conf-ssl-conf-配置文件中的主要参数配置如下："><a href="#1-conf-ssl-conf-配置文件中的主要参数配置如下：" class="headerlink" title="(1) conf/ssl.conf 配置文件中的主要参数配置如下："></a>(1) conf/ssl.conf 配置文件中的主要参数配置如下：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Listen 443 </span><br><span class="line"> </span><br><span class="line">SSLPassPhraseDialog buildin </span><br><span class="line">#SSLPassPhraseDialog exec:/path/to/program </span><br><span class="line">SSLSessionCache dbm:/usr/local/apache2/logs/ssl_scache </span><br><span class="line">SSLSessionCacheTimeout 300 </span><br><span class="line">SSLMutex file:/usr/local/apache2/logs/ssl_mutex </span><br><span class="line"> </span><br><span class="line">&lt;VirtualHost _default_:443&gt; </span><br><span class="line"># General setup for the virtual host </span><br><span class="line">DocumentRoot &quot;/usr/local/apache2/htdocs&quot; </span><br><span class="line">ServerName www.example.com:443 </span><br><span class="line">ServerAdmin you@example.com </span><br><span class="line">ErrorLog /usr/local/apache2/logs/error_log </span><br><span class="line">TransferLog /usr/local/apache2/logs/access_log </span><br><span class="line">  </span><br><span class="line">SSLEngine on </span><br><span class="line">SSLCipherSuite ALL:!ADH:!EXPORT56:RC4+RSA:+HIGH:+MEDIUM:+LOW:+SSLv2:+EXP:+eNULL </span><br><span class="line">  </span><br><span class="line">SSLCertificateFile /usr/local/apache2/conf/ssl.crt/server.crt </span><br><span class="line">SSLCertificateKeyFile /usr/local/apache2/conf/ssl.key/server.key </span><br><span class="line">CustomLog /usr/local/apache2/logs/ssl_request_log &quot;%t %h %&#123;SSL_PROTOCOL&#125;x %&#123;SSL_CIPHER&#125;x &quot;%r&quot; %b&quot; </span><br><span class="line">&lt;/VirtualHost&gt;</span><br></pre></td></tr></table></figure>

<h4 id="2-创建和使用自签署的证书："><a href="#2-创建和使用自签署的证书：" class="headerlink" title="(2) 创建和使用自签署的证书："></a>(2) 创建和使用自签署的证书：</h4><p>a. Create a RSA private key for your Apache server</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/usr/local/openssl/bin/openssl genrsa -des3 -out /usr/local/apache2/conf/ssl.key/server.key 1024</span><br></pre></td></tr></table></figure>

<p>b. Create a Certificate Signing Request (CSR)</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/usr/local/openssl/bin/openssl req -new -key /usr/local/apache2/conf/ssl.key/server.key -out /usr/local/apache2/conf/ssl.key/server.csr</span><br></pre></td></tr></table></figure>

<p>c. Create a self-signed CA Certificate (X509 structure) with the RSA key of the CA</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">/usr/local/openssl/bin/openssl req -x509 -days 365 -key /usr/local/apache2/conf/ssl.key/server.key -in /usr/local/apache2/conf/ssl.key/server.csr -out /usr/local/apache2/conf/ssl.crt/server.crt </span><br><span class="line">/usr/local/openssl/bin/openssl genrsa 1024 -out server.key </span><br><span class="line">/usr/local/openssl/bin/openssl req -new -key server.key -out server.csr </span><br><span class="line">/usr/local/openssl/bin/openssl req -x509 -days 365 -key server.key -in server.csr -out server.crt</span><br></pre></td></tr></table></figure>

<h4 id="3-创建自己的CA（认证证书），并使用该CA来签署服务器的证书。"><a href="#3-创建自己的CA（认证证书），并使用该CA来签署服务器的证书。" class="headerlink" title="(3) 创建自己的CA（认证证书），并使用该CA来签署服务器的证书。"></a>(3) 创建自己的CA（认证证书），并使用该CA来签署服务器的证书。</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkdir /CA </span><br><span class="line">cd /CA </span><br><span class="line">cp openssl-0.9.7g/apps/CA.sh /CA </span><br><span class="line">./CA.sh -newca </span><br><span class="line">openssl genrsa -des3 -out server.key 1024 </span><br><span class="line">openssl req -new -key server.key -out server.csr </span><br><span class="line">cp server.csr newreq.pem </span><br><span class="line">./CA.sh -sign </span><br><span class="line">cp newcert.pem /usr/local/apache2/conf/ssl.crt/server.crt </span><br><span class="line">cp server.key /usr/local/apache2/conf/ssl.key/</span><br></pre></td></tr></table></figure>

<p><strong>参考文章</strong>：</p>
<blockquote>
<p><a href="http://blog.51cto.com/yahoon/36239" target="_blank" rel="noopener">深入理解Apache虚拟主机</a></p>
<p><a href="https://www.jianshu.com/p/c36dd3946e74" target="_blank" rel="noopener">Apache配置文件httpd.conf详解</a></p>
<p><a href="https://httpd.apache.org/docs/trunk/zh-cn/howto/htaccess.html" target="_blank" rel="noopener">Apache文档</a></p>
</blockquote>
]]></content>
      <categories>
        <category>服务器</category>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>优秀文章集锦</title>
    <url>/2018/12/18/Article/%E4%BC%98%E7%A7%80%E6%96%87%E7%AB%A0%E9%9B%86%E9%94%A6/</url>
    <content><![CDATA[<h1 id="1-安全"><a href="#1-安全" class="headerlink" title="1. 安全"></a>1. 安全</h1><p><a href="https://www.infoq.cn/article/how-to-encrypt-the-user-password-correctly" target="_blank" rel="noopener">应用程序如何正确对用户密码进行加密</a></p>
<p><a href="https://www.ibm.com/developerworks/cn/java/j-lo-java8streamapi/index.html" target="_blank" rel="noopener">Java 8 中的 Streams API 详解</a></p>
]]></content>
      <categories>
        <category>文章</category>
      </categories>
      <tags>
        <tag>文章</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringData笔记</title>
    <url>/2018/12/17/SpringBoot/SpringData%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="SpringData-笔记"><a href="#SpringData-笔记" class="headerlink" title="SpringData 笔记"></a>SpringData 笔记</h1><h1 id="1-配置项目"><a href="#1-配置项目" class="headerlink" title="1. 配置项目"></a>1. 配置项目</h1><h2 id="1-pom-xml"><a href="#1-pom-xml" class="headerlink" title="1.pom.xml"></a>1.pom.xml</h2><a id="more"></a>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>SpringData<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--MySQL Driver--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-java<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.1.38<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!--junit--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.10<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!--spring--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-jdbc<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.5.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-context<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.5.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!--spring data jpa--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.data<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-data-jpa<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.8.0.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.hibernate<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>hibernate-entitymanager<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.6.Final<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="2-bean-xml"><a href="#2-bean-xml" class="headerlink" title="2.bean.xml"></a>2.bean.xml</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:tx</span>=<span class="string">"http://www.springframework.org/schema/tx"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:jpa</span>=<span class="string">"http://www.springframework.org/schema/data/jpa"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">      http://www.springframework.org/schema/data/jpa http://www.springframework.org/schema/data/jpa/spring-jpa-1.3.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">      http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-4.0.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">      http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--1 配置数据源--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"dataSource"</span> <span class="attr">class</span>=<span class="string">"org.springframework.jdbc.datasource.DriverManagerDataSource"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"driverClassName"</span> <span class="attr">value</span>=<span class="string">"com.mysql.jdbc.Driver"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"username"</span> <span class="attr">value</span>=<span class="string">"root"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">""</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"url"</span> <span class="attr">value</span>=<span class="string">"jdbc:mysql://localhost:3306/spring_data"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--2 配置EntityManagerFactory--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"entityManagerFactory"</span> <span class="attr">class</span>=<span class="string">"org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dataSource"</span> <span class="attr">ref</span>=<span class="string">"dataSource"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"jpaVendorAdapter"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"packagesToScan"</span> <span class="attr">value</span>=<span class="string">"com.lwen"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"jpaProperties"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">props</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">prop</span> <span class="attr">key</span>=<span class="string">"hibernate.ejb.naming_strategy"</span>&gt;</span>org.hibernate.cfg.ImprovedNamingStrategy<span class="tag">&lt;/<span class="name">prop</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">prop</span> <span class="attr">key</span>=<span class="string">"hibernate.dialect"</span>&gt;</span>org.hibernate.dialect.MySQL5InnoDBDialect<span class="tag">&lt;/<span class="name">prop</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">prop</span> <span class="attr">key</span>=<span class="string">"hibernate.show_sql"</span>&gt;</span>true<span class="tag">&lt;/<span class="name">prop</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">prop</span> <span class="attr">key</span>=<span class="string">"hibernate.format_sql"</span>&gt;</span>true<span class="tag">&lt;/<span class="name">prop</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">prop</span> <span class="attr">key</span>=<span class="string">"hibernate.hbm2ddl.auto"</span>&gt;</span>update<span class="tag">&lt;/<span class="name">prop</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">props</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--3 配置事务管理器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"transactionManager"</span> <span class="attr">class</span>=<span class="string">"org.springframework.orm.jpa.JpaTransactionManager"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"entityManagerFactory"</span> <span class="attr">ref</span>=<span class="string">"entityManagerFactory"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--4 配置支持注解的事务--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tx:annotation-driven</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--5 配置spring data--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">jpa:repositories</span> <span class="attr">base-package</span>=<span class="string">"com.lwen"</span> <span class="attr">entity-manager-factory-ref</span>=<span class="string">"entityManagerFactory"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.lwen"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="3-实体类"><a href="#3-实体类" class="headerlink" title="3.实体类"></a>3.实体类</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.lwen.entry;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.persistence.Entity;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.GeneratedValue;</span><br><span class="line"><span class="keyword">import</span> javax.persistence.Id;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Student实体类</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Student</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  <span class="meta">@Id</span><span class="meta">@GeneratedValue</span></span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">int</span> id;</span><br><span class="line">  <span class="keyword">private</span> String name;</span><br><span class="line">  <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getId</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> id;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setId</span><span class="params">(<span class="keyword">int</span> id)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.id = id;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> name;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.name = name;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getAge</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> age;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAge</span><span class="params">(<span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.age = age;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">toString</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Student&#123;"</span> +</span><br><span class="line">                <span class="string">"id="</span> + id +</span><br><span class="line">                <span class="string">", name='"</span> + name + <span class="string">'\''</span> +</span><br><span class="line">                <span class="string">", age="</span> + age +</span><br><span class="line">                <span class="string">'&#125;'</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里注意的一点就是我们在使用注解的时候一定要注意导入的包，我们都是导入的javax中的类。</p>
<h1 id="2-Repository"><a href="#2-Repository" class="headerlink" title="2.Repository"></a>2.Repository</h1><h2 id="1-使用"><a href="#1-使用" class="headerlink" title="1. 使用"></a>1. 使用</h2><p>这个东西是SpringData的核心，但是我们实际去看的时候会发现他是一个空接口，也就是这个一个标记接口。我们自己的接口必须继承这个接口才会具备查询的功能，所以说我们的自定的查询器必须要继承这个接口。这个接口是泛型接口也就是我们需要输入两个参数，第一个就是我们查询器的类型，针对于那个表进行查询，另外一个就是表的Id的类型，这个类型必须是序列化接口的子类型，所以说不能使用基本类型，我们只能使用包装类型。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">EmployeeRepository</span> <span class="keyword">extends</span> <span class="title">Repository</span>&lt;<span class="title">Employee</span>,<span class="title">Integer</span>&gt; </span>&#123;</span><br><span class="line">    <span class="function">Employee <span class="title">findByName</span><span class="params">(String name)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是我们还有另外一种方式，就是使用注解的方式不用继承这个接口。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RepositoryDefinition</span>(domainClass = Student.class,idClass = Integer.class)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">StudentRepository</span> </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-常用的子接口"><a href="#2-常用的子接口" class="headerlink" title="2. 常用的子接口"></a>2. 常用的子接口</h2><ul>
<li><p>CrudRepository：继承Repository，实现了CRUD相关的方法</p>
</li>
<li><p>PagingAndSortingRepository：继承CrudRepository，实现了分页排序相关的方法</p>
</li>
<li><p>JpaRepository：继承PagingAndSortingRepository，实现JPA规范相关的方法</p>
</li>
</ul>
<p>这些接口的功能都是非常强大并且实用的我们在使用的时候就可以直接继承这些接口。</p>
<h2 id="3-查询规则"><a href="#3-查询规则" class="headerlink" title="3.查询规则"></a>3.查询规则</h2><h2 id="1-约定方法签名查询"><a href="#1-约定方法签名查询" class="headerlink" title="1.约定方法签名查询"></a>1.约定方法签名查询</h2><p><a href="https://imgchr.com/i/CcX89e" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2018/05/19/CcX89e.md.png" alt="CcX89e.md.png"></a></p>
<p><a href="https://imgchr.com/i/CcXG1H" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2018/05/19/CcXG1H.md.png" alt="CcXG1H.md.png"></a></p>
<h2 id="2-手动查询-Query"><a href="#2-手动查询-Query" class="headerlink" title="2.手动查询 @Query"></a>2.手动查询 @Query</h2><h3 id="1-复杂查询"><a href="#1-复杂查询" class="headerlink" title="1.复杂查询"></a>1.复杂查询</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Query</span>(<span class="string">"select o from Employee o where id = (select max(id) from Employee)"</span>)</span><br><span class="line"><span class="function">Employee <span class="title">getMaxIdEmployee</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>

<h3 id="2-占位符查询"><a href="#2-占位符查询" class="headerlink" title="2.占位符查询"></a>2.占位符查询</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Query</span>(<span class="string">"select o from Employee o where name=?1 and age=?2"</span>)</span><br><span class="line"><span class="function">Employee <span class="title">getByNameAndAge</span><span class="params">(String name,Integer age)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Query</span>(<span class="string">"select o from Employee o where name like %?1%"</span>)</span><br><span class="line"><span class="function">Employee <span class="title">getByLikeName</span><span class="params">(String name)</span></span>;</span><br></pre></td></tr></table></figure>

<h3 id="3-命名参数"><a href="#3-命名参数" class="headerlink" title="3.命名参数"></a>3.命名参数</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Query</span>(<span class="string">"select o from Employee o where name=:name and age=:age"</span>)</span><br><span class="line"><span class="function">Employee <span class="title">getByNameAndAge1</span><span class="params">(@Param(<span class="string">"name"</span>)</span> String name,@<span class="title">Param</span><span class="params">(<span class="string">"age"</span>)</span> Integer age)</span>;</span><br></pre></td></tr></table></figure>

<h3 id="4-原生态查询"><a href="#4-原生态查询" class="headerlink" title="4.原生态查询"></a>4.原生态查询</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Query</span>(nativeQuery = <span class="keyword">true</span>,value = <span class="string">"select * from spring_data.employee where name = ?1"</span>)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">getNative</span><span class="params">(String name)</span>；</span></span><br></pre></td></tr></table></figure>

<h2 id="3-更新删除操作"><a href="#3-更新删除操作" class="headerlink" title="3. 更新删除操作"></a>3. 更新删除操作</h2><p>在SpringData中使用插删改操作的时候我们必须定义一个Service层，然后我们在Service层调用Dao的Repository来更新数据库，接着我们需要将那个Repository的方法设置为 @Modifying ，最后最重要的就是我们在Service层的那个方法中写 @Transactional 注解。才能更新成功，所以所有的事务只能出现在 Service 层。但是注意因为我们的Service没有继承任何的Spring相关的东西我们要把它放到容器的时候需要使用@Service注解，否则是不行的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Modifying</span></span><br><span class="line"><span class="meta">@Query</span>(<span class="string">"update Employee o set o.name=:name where o.id=:id"</span>)</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">update</span><span class="params">(@Param(<span class="string">"id"</span>)</span> Integer id,@<span class="title">Param</span><span class="params">(<span class="string">"name"</span>)</span> String name)</span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EmployeeService</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    EmployeeRepository repository;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Transactional</span> <span class="comment">//这个是javax里面的</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">update</span><span class="params">()</span></span>&#123;</span><br><span class="line">        repository.update(<span class="number">1</span>, <span class="string">"lwenxu"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>小提示，很多时候我们发现有些东西在Spring中有在javax中也有，我们优先导入Javax中的，如果出现了什么方法无法调用估计就是包导错了。</strong></p>
<h1 id="3-常用的Repository"><a href="#3-常用的Repository" class="headerlink" title="3. 常用的Repository"></a>3. 常用的Repository</h1><p>这三个高级的Repository实际上是从上到下依次继承的。</p>
<h2 id="1-CrudRepository"><a href="#1-CrudRepository" class="headerlink" title="1. CrudRepository"></a>1. CrudRepository</h2><p>我们的Repository必须要继承这个接口，然后我们就有crud的一些操作了。接着我们需要创建service层，然后再service中使用事务，并且注入我们的Repository，这里我们用了一张新的表我们在bean上指定我们的表名就是使用@Table(name = “employee_test”) 注解。最后进行save操作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Transactional</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">saveAll</span><span class="params">(List&lt;Employee&gt; employees)</span></span>&#123;</span><br><span class="line">    employeeCrudRepository.save(employees);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">EmployeeCrudRepository employeeCrudRepository = ctx.getBean(EmployeeCrudRepository.class);</span><br><span class="line">ArrayList&lt;Employee&gt; employees = <span class="keyword">new</span> ArrayList&lt;Employee&gt;();</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; i++) &#123;</span><br><span class="line">    employees.add(<span class="keyword">new</span> Employee(i, <span class="string">"lwen"</span> + i, i));</span><br><span class="line">&#125;</span><br><span class="line">employeeCrudRepository.save(employees);</span><br></pre></td></tr></table></figure>

<h2 id="2-PagingAndSortingRespository"><a href="#2-PagingAndSortingRespository" class="headerlink" title="2.PagingAndSortingRespository"></a>2.PagingAndSortingRespository</h2><p>他是分页和排序功能。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">EmployeePageAndSortRepository pageAndSort = ctx.getBean(EmployeePageAndSortRepository.class);</span><br><span class="line"></span><br><span class="line"><span class="comment">//创建一个排序器，是按照id的降续排列的</span></span><br><span class="line">Sort sort = <span class="keyword">new</span> Sort(<span class="keyword">new</span> Sort.Order(Sort.Direction.DESC, <span class="string">"id"</span>));</span><br><span class="line"><span class="comment">//第一个参数是当前的页码他是从0开始的</span></span><br><span class="line"><span class="comment">//第二个参数就是每一页的大小</span></span><br><span class="line"><span class="comment">//第三个参数是可选参数，传入一个sort，就是按照哪种方式分页  由于这里用的是降续所以出来的结果应该是 0 在最后一页 99在第一页</span></span><br><span class="line">Pageable pageable = <span class="keyword">new</span> PageRequest(<span class="number">0</span>, <span class="number">9</span>, sort);</span><br><span class="line">Page&lt;Employee&gt; page = pageAndSort.findAll(pageable);</span><br><span class="line"><span class="comment">//获取当前页的内容</span></span><br><span class="line">System.out.println(page.getContent());</span><br><span class="line"><span class="comment">//获取所有的页数</span></span><br><span class="line">System.out.println(page.getTotalPages());</span><br></pre></td></tr></table></figure>

<h2 id="3-JpaRepository"><a href="#3-JpaRepository" class="headerlink" title="3.JpaRepository"></a>3.JpaRepository</h2><ul>
<li>findAll</li>
<li>save(entries)</li>
<li>deleteInBatch</li>
<li>findAll(sort)</li>
<li>flush</li>
</ul>
<h1 id="4-JpaSpecificationExecutor"><a href="#4-JpaSpecificationExecutor" class="headerlink" title="4. JpaSpecificationExecutor"></a>4. JpaSpecificationExecutor</h1><p>这里吧这个接口单独拿出来说主要是因为这个接口实际上不是继承自 Repository 这个接口，他的作用从表面上貌似也是看不出来，实际上我们前面看到我们可以进行简单的分页，但是那些分页并没有一个让我们传入查询条件的地方，我们这个接口就是实现了这个功能也就是有条件的分页查询。</p>
<p>jpa接口需要继承 JpaSpecificationExecutor 然后这里就包含了五个方法，分别是 findAll(三个重载) 和 findOne 以及 count 。</p>
<p>然后在使用的时候只调用对应的方法即可，Page<t> findAll(@Nullable Specification<t> var1, Pageable var2); 这个算是里面最复杂的方法了主要是需要传递一个 Specification 对象，这个一般我们直接传递一个匿名的对象即可，实现后的代码如下：</t></t></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">new</span> Specification&lt;User&gt;() &#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Predicate <span class="title">toPredicate</span><span class="params">(Root&lt;User&gt; root, CriteriaQuery&lt;?&gt; criteriaQuery, CriteriaBuilder criteriaBuilder)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> criteriaBuilder.gt(root.&lt;Number&gt;get(<span class="string">"id"</span>),<span class="number">4</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到这个函数式接口里面的第一个参数是 root 这个相当于一个导航器，也就是用它可以获取到我们实体类中的属性，也就是我们获取到表的字段，CriteriaQuery 则是可以进行语句的拼装，里面有 where ，groupby 以及having 等方法，进行sql组合的。二最后一个参数就是 CriteriaBuilder 用来创建 Predicate 对象的，也就是生成查询条件对象。</p>
<p>上面的程序生成的最后的条件就是获取id大于4 的所有信息，然后分页展示。</p>
<h1 id="5-表的映射关系"><a href="#5-表的映射关系" class="headerlink" title="5. 表的映射关系"></a>5. 表的映射关系</h1><h2 id="1-一对一"><a href="#1-一对一" class="headerlink" title="1.一对一"></a>1.一对一</h2><p>一对一关系这里定义了一个Person对象以及一个IDCard对象</p>
<p>Person类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name=<span class="string">"t_person"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> IDCard card;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@OneToOne</span>(mappedBy=<span class="string">"person"</span>)　　---&gt;　　指定了OneToOne的关联关系，mappedBy同样指定由对方来进行维护关联关系</span><br><span class="line">    <span class="function"><span class="keyword">public</span> IDCard <span class="title">getCard</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> card;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setCard</span><span class="params">(IDCard card)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.card = card;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Id</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getId</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setId</span><span class="params">(<span class="keyword">int</span> id)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.id = id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>IDCard类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name=<span class="string">"t_id_card"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IDCard</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String no;</span><br><span class="line">    <span class="keyword">private</span> Person person;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Id</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getId</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setId</span><span class="params">(<span class="keyword">int</span> id)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.id = id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getNo</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> no;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setNo</span><span class="params">(String no)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.no = no;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@OneToOne</span>　　---&gt;　　OnetoOne指定了一对一的关联关系，一对一中随便指定一方来维护映射关系，这里选择IDCard来进行维护</span><br><span class="line">    <span class="meta">@JoinColumn</span>(name=<span class="string">"pid"</span>)　　---&gt;　　指定外键的名字 pid</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Person <span class="title">getPerson</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> person;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setPerson</span><span class="params">(Person person)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.person = person;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>注意</strong>:在判断到底是谁维护关联关系时，可以通过查看外键，哪个实体类定义了外键，哪个类就负责维护关联关系。</p>
<h2 id="2-多对一"><a href="#2-多对一" class="headerlink" title="2.多对一"></a>2.多对一</h2><p>这里我们定义了两个实体类，一个是ClassRoom，一个是Student，这两者是一对多的关联关系。</p>
<p>ClassRoom类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name=<span class="string">"t_classroom"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ClassRoom</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Set&lt;Student&gt; students;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ClassRoom</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        students = <span class="keyword">new</span> HashSet&lt;Student&gt;();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addStudent</span><span class="params">(Student student)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        students.add(student);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@OneToMany</span>(mappedBy=<span class="string">"room"</span>)　　---&gt;　　OneToMany指定了一对多的关系，mappedBy=<span class="string">"room"</span>指定了由多的那一方来维护关联关系，mappedBy指的是多的一方对<span class="number">1</span>的这一方的依赖的属性，(注意：如果没有指定由谁来维护关联关系，则系统会给我们创建一张中间表)</span><br><span class="line">    <span class="meta">@LazyCollection</span>(LazyCollectionOption.EXTRA)　　---&gt;　　LazyCollection属性设置成EXTRA指定了当如果查询数据的个数时候，只会发出一条 count(*)的语句，提高性能</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Set&lt;Student&gt; <span class="title">getStudents</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> students;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setStudents</span><span class="params">(Set&lt;Student&gt; students)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.students = students;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><a href="javascript:void(0);" target="_blank" rel="noopener"><img src="http://common.cnblogs.com/images/copycode.gif" alt="复制代码"></a></p>
<p>Student类：</p>
<p><a href="javascript:void(0);" target="_blank" rel="noopener"><img src="http://common.cnblogs.com/images/copycode.gif" alt="复制代码"></a></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name=<span class="string">"t_student"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Student</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> ClassRoom room;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@ManyToOne</span>(fetch=FetchType.LAZY)　　---&gt; ManyToOne指定了多对一的关系，fetch=FetchType.LAZY属性表示在多的那一方通过延迟加载的方式加载对象(默认不是延迟加载)</span><br><span class="line">    <span class="meta">@JoinColumn</span>(name=<span class="string">"rid"</span>)　　---&gt;　　通过 JoinColumn 的name属性指定了外键的名称 rid　(注意：如果我们不通过JoinColum来指定外键的名称，系统会给我们声明一个名称)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ClassRoom <span class="title">getRoom</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> room;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setRoom</span><span class="params">(ClassRoom room)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.room = room;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-多对多"><a href="#3-多对多" class="headerlink" title="3.多对多"></a>3.多对多</h2><p>多对多这里通常有两种处理方式，一种是通过建立一张中间表，然后由任一一个多的一方来维护关联关系，另一种就是将多对多拆分成两个一对多的关联关系</p>
<h3 id="1-不使用中间表的实体类"><a href="#1-不使用中间表的实体类" class="headerlink" title="1. 不使用中间表的实体类"></a>1. 不使用中间表的实体类</h3><p>采用中间表的时候由任一一个多的一方来维护关联关系</p>
<p>Teacher类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name=<span class="string">"t_teacher"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Teacher</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> Set&lt;Course&gt; courses;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Teacher</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        courses = <span class="keyword">new</span> HashSet&lt;Course&gt;();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addCourse</span><span class="params">(Course course)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        courses.add(course);</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Id</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getId</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setId</span><span class="params">(<span class="keyword">int</span> id)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.id = id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@ManyToMany</span>(mappedBy=<span class="string">"teachers"</span>)　　---&gt;　　表示由Course那一方来进行维护</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Set&lt;Course&gt; <span class="title">getCourses</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> courses;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setCourses</span><span class="params">(Set&lt;Course&gt; courses)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.courses = courses;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><a href="javascript:void(0);" target="_blank" rel="noopener"><img src="http://common.cnblogs.com/images/copycode.gif" alt="复制代码"></a></p>
<p>Course类：</p>
<p><a href="javascript:void(0);" target="_blank" rel="noopener"><img src="http://common.cnblogs.com/images/copycode.gif" alt="复制代码"></a></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name=<span class="string">"t_course"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Course</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> Set&lt;Teacher&gt; teachers;</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Course</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        teachers = <span class="keyword">new</span> HashSet&lt;Teacher&gt;();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addTeacher</span><span class="params">(Teacher teacher)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        teachers.add(teacher);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@ManyToMany</span>　　　---&gt;　ManyToMany指定多对多的关联关系</span><br><span class="line">    <span class="meta">@JoinTable</span>(name=<span class="string">"t_teacher_course"</span>, joinColumns=&#123; <span class="meta">@JoinColumn</span>(name=<span class="string">"cid"</span>)&#125;, </span><br><span class="line">    inverseJoinColumns=&#123; <span class="meta">@JoinColumn</span>(name = <span class="string">"tid"</span>) &#125;)　　---&gt;　　因为多对多之间会通过一张中间表来维护两表直接的关系，所以通过 JoinTable 这个注解来声明，name就是指定了中间表的名字，JoinColumns是一个 <span class="meta">@JoinColumn</span>类型的数组，表示的是我这方在对方中的外键名称，我方是Course，所以在对方外键的名称就是 rid，inverseJoinColumns也是一个 <span class="meta">@JoinColumn</span>类型的数组，表示的是对方在我这放中的外键名称，对方是Teacher，所以在我方外键的名称就是 tid</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Set&lt;Teacher&gt; <span class="title">getTeachers</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> teachers;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setTeachers</span><span class="params">(Set&lt;Teacher&gt; teachers)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.teachers = teachers;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Id</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getId</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> id;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setId</span><span class="params">(<span class="keyword">int</span> id)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.id = id;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><a href="javascript:void(0);" target="_blank" rel="noopener"><img src="http://common.cnblogs.com/images/copycode.gif" alt="复制代码"></a></p>
<h3 id="2-采用中间表实体类"><a href="#2-采用中间表实体类" class="headerlink" title="2. 采用中间表实体类"></a>2. 采用中间表实体类</h3><p>把之前的ManyToMany拆分成两个One-to-Many的映射</p>
<p>Admin类：</p>
<p><a href="javascript:void(0);" target="_blank" rel="noopener"><img src="http://common.cnblogs.com/images/copycode.gif" alt="复制代码"></a></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name=<span class="string">"t_admin"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Admin</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> Set&lt;AdminRole&gt; ars;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Admin</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        ars = <span class="keyword">new</span> HashSet&lt;AdminRole&gt;();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(AdminRole ar)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        ars.add(ar);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Id</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getId</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setId</span><span class="params">(<span class="keyword">int</span> id)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.id = id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@OneToMany</span>(mappedBy=<span class="string">"admin"</span>)　　---&gt;　　OneToMany关联到了AdminRole这个类，由AdminRole这个类来维护多对一的关系，mappedBy=<span class="string">"admin"</span></span><br><span class="line">    <span class="meta">@LazyCollection</span>(LazyCollectionOption.EXTRA)　　</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Set&lt;AdminRole&gt; <span class="title">getArs</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> ars;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setArs</span><span class="params">(Set&lt;AdminRole&gt; ars)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.ars = ars;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><a href="javascript:void(0);" target="_blank" rel="noopener"><img src="http://common.cnblogs.com/images/copycode.gif" alt="复制代码"></a></p>
<p>Role类：</p>
<p><a href="javascript:void(0);" target="_blank" rel="noopener"><img src="http://common.cnblogs.com/images/copycode.gif" alt="复制代码"></a></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name=<span class="string">"t_role"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Role</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> Set&lt;AdminRole&gt; ars;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Role</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        ars = <span class="keyword">new</span> HashSet&lt;AdminRole&gt;();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(AdminRole ar)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        ars.add(ar);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Id</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getId</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setId</span><span class="params">(<span class="keyword">int</span> id)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.id = id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@OneToMany</span>(mappedBy=<span class="string">"role"</span>)　　---&gt;　　OneToMany指定了由AdminRole这个类来维护多对一的关联关系，mappedBy=<span class="string">"role"</span></span><br><span class="line">    <span class="meta">@LazyCollection</span>(LazyCollectionOption.EXTRA)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Set&lt;AdminRole&gt; <span class="title">getArs</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> ars;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setArs</span><span class="params">(Set&lt;AdminRole&gt; ars)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.ars = ars;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><a href="javascript:void(0);" target="_blank" rel="noopener"><img src="http://common.cnblogs.com/images/copycode.gif" alt="复制代码"></a></p>
<p>AdminRole类：</p>
<p><a href="javascript:void(0);" target="_blank" rel="noopener"><img src="http://common.cnblogs.com/images/copycode.gif" alt="复制代码"></a></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name=<span class="string">"t_admin_role"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AdminRole</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> Admin admin;</span><br><span class="line">    <span class="keyword">private</span> Role role;</span><br><span class="line">    <span class="meta">@Id</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getId</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setId</span><span class="params">(<span class="keyword">int</span> id)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.id = id;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@ManyToOne</span>　　---&gt;　　ManyToOne关联到Admin</span><br><span class="line">    <span class="meta">@JoinColumn</span>(name=<span class="string">"aid"</span>)　　</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Admin <span class="title">getAdmin</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> admin;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAdmin</span><span class="params">(Admin admin)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.admin = admin;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@ManyToOne</span>　　---&gt;　　</span><br><span class="line">    <span class="meta">@JoinColumn</span>(name=<span class="string">"rid"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Role <span class="title">getRole</span><span class="params">()</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> role;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setRole</span><span class="params">(Role role)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.role = role;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><a href="javascript:void(0);" target="_blank" rel="noopener"><img src="http://common.cnblogs.com/images/copycode.gif" alt="复制代码"></a></p>
<p><strong>小技巧</strong>:通过hibernate来进行插入操作的时候，不管是一对多、一对一还是多对多，都只需要记住一点，在哪个实体类声明了外键，就由哪个类来维护关系，在保存数据时，总是先保存的是没有维护关联关系的那一方的数据，后保存维护了关联关系的那一方的数据</p>
<h2 id="4-中间表"><a href="#4-中间表" class="headerlink" title="4.中间表"></a>4.中间表</h2><p>两个实体tb_user,tb_role<br>现在我们再tb_user或者tb_role中任意一个里面进行维护关系，多对对的情况下我们需要创建一个中间表来完成这个关系的映射，我们再tb_user中添加注解@ManyToMany然后再添加一个注解@JoinTable因为我们是要创建中间表所以要使用这个注解。JoinTable注解中我们添加如下例子中的内容，joinColumns当前表中的字段在中间表中的字段名称，inverseJoinColumns关联的外键表在中间表中的字段名称</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name = <span class="string">"tb_user"</span>)</span><br><span class="line"><span class="meta">@SequenceGenerator</span>(name = <span class="string">"tb_user_sq"</span>,sequenceName = <span class="string">"tb_user_sqe"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TbUser</span> <span class="keyword">extends</span> <span class="title">BaseEntity</span></span>&#123;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 用户名</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> String userName;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 登录名</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> String loginName;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 登陆密码</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> String passWord;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 手机号</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> String telPhone;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 一个用户可以有多个角色</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">private</span> List&lt;TbRole&gt; tbRoleList=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">getUserName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> userName;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setUserName</span><span class="params">(String userName)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.userName = userName;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">getLoginName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> loginName;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setLoginName</span><span class="params">(String loginName)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.loginName = loginName;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">getPassWord</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> passWord;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setPassWord</span><span class="params">(String passWord)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.passWord = passWord;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">getTelPhone</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> telPhone;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setTelPhone</span><span class="params">(String telPhone)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.telPhone = telPhone;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Id</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@GeneratedValue</span>(generator = <span class="string">"tb_user_sq"</span>,strategy = GenerationType.SEQUENCE)</span><br><span class="line"><span class="function"><span class="keyword">public</span> Long <span class="title">getId</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.id;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@ManyToMany</span>(cascade = CascadeType.REMOVE,fetch = FetchType.LAZY)</span><br><span class="line"><span class="meta">@JoinTable</span>(name = <span class="string">"tb_user_role"</span>,joinColumns = <span class="meta">@JoinColumn</span>(name=<span class="string">"tb_user_id"</span>,referencedColumnName = <span class="string">"id"</span>),inverseJoinColumns = <span class="meta">@JoinColumn</span>(name = <span class="string">"tb_role_id"</span>,referencedColumnName = <span class="string">"id"</span>))</span><br><span class="line"><span class="function"><span class="keyword">public</span> List&lt;TbRole&gt; <span class="title">getTbRoleList</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> tbRoleList;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setTbRoleList</span><span class="params">(List&lt;TbRole&gt; tbRoleList)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.tbRoleList = tbRoleList;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>因为在tb_user中我们维护了两个表的关系，所以如果我们在tb_role中如果不想创建关联字段的话就不用添加tbUser的关系字段</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name = <span class="string">"tb_role"</span>)</span><br><span class="line"><span class="meta">@SequenceGenerator</span>(name = <span class="string">"tb_role_sq"</span>,sequenceName = <span class="string">"tb_role_sqe"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TbRole</span> <span class="keyword">extends</span> <span class="title">BaseEntity</span></span>&#123;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@Id</span></span><br><span class="line"><span class="meta">@GeneratedValue</span>(generator = <span class="string">"tb_role_sq"</span>,strategy = GenerationType.SEQUENCE)</span><br><span class="line"><span class="function"><span class="keyword">public</span> Long <span class="title">getId</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.id;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> String roleName;</span><br><span class="line"><span class="meta">@ManyToMany</span>(mappedBy = <span class="string">"tbRoleList"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">getRoleName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> roleName;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setRoleName</span><span class="params">(String roleName)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.roleName = roleName;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果想要在tb_role中进行维护关联字段的话如下，我们在字段中添加注解 @ManyToMany然后使用直接属性mappedBy值是当前表在关联表中的字段名称</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Entity</span></span><br><span class="line"><span class="meta">@Table</span>(name = <span class="string">"tb_role"</span>)</span><br><span class="line"><span class="meta">@SequenceGenerator</span>(name = <span class="string">"tb_role_sq"</span>,sequenceName = <span class="string">"tb_role_sqe"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TbRole</span> <span class="keyword">extends</span> <span class="title">BaseEntity</span></span>&#123;</span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="meta">@Id</span></span><br><span class="line"><span class="meta">@GeneratedValue</span>(generator = <span class="string">"tb_role_sq"</span>,strategy = GenerationType.SEQUENCE)</span><br><span class="line"><span class="function"><span class="keyword">public</span> Long <span class="title">getId</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.id;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> String roleName;</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span> List&lt;TbUser&gt; tbUserList=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">getRoleName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> roleName;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setRoleName</span><span class="params">(String roleName)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.roleName = roleName;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">@ManyToMany</span>(mappedBy = <span class="string">"tbRoleList"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> List&lt;TbUser&gt; <span class="title">getTbUserList</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> tbUserList;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setTbUserList</span><span class="params">(List&lt;TbUser&gt; tbUserList)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>.tbUserList = tbUserList;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>GOF23 常用设计模式</title>
    <url>/2018/08/06/Java%20SE/Java%E8%AE%BE%E8%AE%A1%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="GOF23-常用设计模式"><a href="#GOF23-常用设计模式" class="headerlink" title="GOF23 常用设计模式"></a>GOF23 常用设计模式</h1><h2 id="一、单例设计模式"><a href="#一、单例设计模式" class="headerlink" title="一、单例设计模式"></a>一、单例设计模式</h2><h3 id="1-饿汉式"><a href="#1-饿汉式" class="headerlink" title="1.饿汉式"></a>1.饿汉式</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class GOF23 &#123;</span><br><span class="line">    // 饿汉式</span><br><span class="line">    private static final List list = new ArrayList();</span><br><span class="line"></span><br><span class="line">    public static List getInstance() &#123;</span><br><span class="line">        return list;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-懒汉式"><a href="#2-懒汉式" class="headerlink" title="2.懒汉式"></a>2.懒汉式</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    // 懒汉式</span><br><span class="line">    // 使用双检锁，并且由于 jvm 指令重排序我们需要使用 volatile 关键字抑制指令重排序</span><br><span class="line">public class GOF&#123;</span><br><span class="line">    private static volatile List list1;</span><br><span class="line">    public static List getInstanceLazy() &#123;</span><br><span class="line">        synchronized (Object.class) &#123;</span><br><span class="line">            if (list1 == null) &#123;</span><br><span class="line">                synchronized (Object.class)&#123;</span><br><span class="line">                    list1 = new ArrayList();</span><br><span class="line">                    return list;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;else &#123;</span><br><span class="line">                return list;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-静态内部类"><a href="#3-静态内部类" class="headerlink" title="3.静态内部类"></a>3.静态内部类</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class GOF&#123;</span><br><span class="line">     // 静态内部类</span><br><span class="line">    public static class SingletonClass&#123;</span><br><span class="line">        private static final List list2 = new ArrayList();</span><br><span class="line">    &#125;</span><br><span class="line">    // 首先要去加载这个静态内部类，然后这个时候会调用 cinit 方法这个是天然的线程安全的，然后返回这个对象即可  可以延时加载</span><br><span class="line">    public List getStaticInstance()&#123;</span><br><span class="line">        return SingletonClass.list2;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-枚举"><a href="#4-枚举" class="headerlink" title="4.枚举"></a>4.枚举</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class GOF23 &#123;</span><br><span class="line">    // 采用枚举的方式  不能延时加载  由 jvm 底层完成的</span><br><span class="line">    public enum SingleInstanceEnum&#123;</span><br><span class="line">        INSTANCE_ENUM</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="二、工厂设计模式"><a href="#二、工厂设计模式" class="headerlink" title="二、工厂设计模式"></a>二、工厂设计模式</h2><p>工厂设计模式的好处就是把对象的创建和使用分开。在软公众需要遵循的三个原则：开闭原则（对修改关闭，对拓展开放）、依赖翻转（面向接口依赖而不是类的强依赖）、迪米特法则（和朋友通讯而不是陌生人）。</p>
<h3 id="1-简单工厂"><a href="#1-简单工厂" class="headerlink" title="1.简单工厂"></a>1.简单工厂</h3><p>一般也称之为静态工厂，因为他的方法是一个静态的方法，并且必须要有一个父接口去容纳这些被创建的对象。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public interface Car &#123;</span><br><span class="line">    public void run();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Audi implements Car &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void run() &#123;</span><br><span class="line">        System.out.println(&quot;audi run&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class Daben implements Car &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void run() &#123;</span><br><span class="line">        System.out.println(&quot;daben run&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class CarFactory &#123;</span><br><span class="line">    public static Car createCar(String type) &#123;</span><br><span class="line">        if (type.equals(&quot;daben&quot;)) &#123;</span><br><span class="line">            return new Daben();</span><br><span class="line">        &#125; else if (type.equals(&quot;audi&quot;)) &#123;</span><br><span class="line">            return new Audi();</span><br><span class="line">        &#125;else &#123;</span><br><span class="line">            return null;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class CarClient &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        Daben daben = (Daben) CarFactory.createCar(&quot;daben&quot;);</span><br><span class="line">        daben.run();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-工厂方法"><a href="#2-工厂方法" class="headerlink" title="2.工厂方法"></a>2.工厂方法</h3><p>这个比上面好一点的就是我们不用直接修改原来的代码而是可以面向拓展而不是修改就是开闭原则是满足的。其实我们在项目中主要使用的是简单工厂而不是这个，因为这个的类文件剧增。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public interface CarFactory &#123;</span><br><span class="line">    public Car createFactory();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class BenChiFactory implements CarFactory &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public Car createFactory() &#123;</span><br><span class="line">        return new Daben();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class AudiFactory implements CarFactory &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public Car createFactory() &#123;</span><br><span class="line">        return new Audi();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-抽象工厂"><a href="#3-抽象工厂" class="headerlink" title="3.抽象工厂"></a>3.抽象工厂</h3><p>抽象工厂其实和上面的两个方式区别很大，主要是因为上面两个的维度是创建产品而这个地方的维度是创建一个产品组。也就是比如我们有一个车的工厂，其实这个工厂是创建很多车的零部件而不是一个车，那么我们有多个车的工厂的目的就是为了创建不同层次的车零件。比如下面这个例子，就是他的维度是横向的一个维度。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 座椅类型</span><br><span class="line">public interface Seat &#123;</span><br><span class="line">    public void desc();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class LuxurySeat implements Seat &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void desc() &#123;</span><br><span class="line">        System.out.println(&quot;high quality seat&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class LowSeat implements Seat &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void desc() &#123;</span><br><span class="line">        System.out.println(&quot;low seat&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 引擎类型</span><br><span class="line">public interface Engine &#123;</span><br><span class="line">    public void run();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class LuxuryEngine implements Engine &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void run() &#123;</span><br><span class="line">        System.out.println(&quot;high speed engine&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class LowEngine implements Engine &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public void run() &#123;</span><br><span class="line">        System.out.println(&quot;low engine&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 车的抽象工厂</span><br><span class="line">public interface AbstractCarFactory &#123;</span><br><span class="line">    public Seat createSeat();</span><br><span class="line">    public Engine creaateEngine();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 两个工厂的实现，分别是高级车工厂，低级车工厂</span><br><span class="line">public class LuxuryCarFactory implements AbstractCarFactory &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public Seat createSeat() &#123;</span><br><span class="line">        return new LuxurySeat();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public Engine creaateEngine() &#123;</span><br><span class="line">        return new LuxuryEngine();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class LowCarFactory implements AbstractCarFactory&#123;</span><br><span class="line">    @Override</span><br><span class="line">    public Seat createSeat() &#123;</span><br><span class="line">        return new LowSeat();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public Engine creaateEngine() &#123;</span><br><span class="line">        return new LowEngine();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="三、建造者模式"><a href="#三、建造者模式" class="headerlink" title="三、建造者模式"></a>三、建造者模式</h2><p>建造者模式分为两部分，第一个部分其实就是工厂模式创建对象，然后另外一部分就是组装创建整个大对象。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 创建工厂</span><br><span class="line">public interface BuilderFactory &#123;</span><br><span class="line">    Engine createEngine();</span><br><span class="line">    Seat createSeat();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class AirShipBuilderFactory implements BuilderFactory &#123;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public Engine createEngine() &#123;</span><br><span class="line">        return new Engine() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void run() &#123;</span><br><span class="line"></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    @Override</span><br><span class="line">    public Seat createSeat() &#123;</span><br><span class="line">        return new Seat() &#123;</span><br><span class="line">            @Override</span><br><span class="line">            public void desc() &#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">// 组装工厂</span><br><span class="line">public class AirDirectFactory &#123;</span><br><span class="line"></span><br><span class="line">    private BuilderFactory builderFactory;</span><br><span class="line"></span><br><span class="line">    public AirShip buildAirShip() &#123;</span><br><span class="line">        Engine engine = builderFactory.createEngine();</span><br><span class="line">        Seat seat = builderFactory.createSeat();</span><br><span class="line">        return new AirShip(engine, seat);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public AirDirectFactory(BuilderFactory builderFactory) &#123;</span><br><span class="line">        this.builderFactory = builderFactory;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="四、原型模式"><a href="#四、原型模式" class="headerlink" title="四、原型模式"></a>四、原型模式</h2><p>可以称之为克隆或者拷贝，然后在拷贝的对象上修改就可以产生新对象，其实js 的原型就是这么干的。</p>
<p>这里需要注意一下这里的一个深拷贝的问题，如果没有作深拷贝而是直接的做了一个 super 的调用可能导致状态关联问题。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@NoArgsConstructor</span><br><span class="line">@AllArgsConstructor</span><br><span class="line">@Builder(toBuilder = true)</span><br><span class="line">@Data</span><br><span class="line">@Accessors(chain = true,fluent = true)</span><br><span class="line">public class Sheep implements Cloneable&#123;</span><br><span class="line">    private String name;</span><br><span class="line">    private Date birthday;</span><br><span class="line"></span><br><span class="line">    /**</span><br><span class="line">     * 注意在克隆的时候需要进行深拷贝负责就会出现错误结果</span><br><span class="line">     * @return</span><br><span class="line">     * @throws CloneNotSupportedException</span><br><span class="line">     */</span><br><span class="line">    @Override</span><br><span class="line">    protected Object clone() throws CloneNotSupportedException &#123;</span><br><span class="line"></span><br><span class="line">        Sheep clone = (Sheep) super.clone();</span><br><span class="line">        clone.birthday = (Date) this.birthday.clone();</span><br><span class="line">        return clone;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class PrototypeClient &#123;</span><br><span class="line">    public static void main(String[] args) throws CloneNotSupportedException &#123;</span><br><span class="line">        Sheep sheep1 = new Sheep(&quot;A&quot;, new Date());</span><br><span class="line">        Sheep sheep2 = (Sheep) sheep1.clone();</span><br><span class="line">        sheep2.birthday(new Date()).name(&quot;sss&quot;);</span><br><span class="line">        System.out.println(sheep2);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>另外我们除了使用上面的克隆方法我们还可以使用序列化的方式直接生成一个一模一样的对象出来。具体来说就是我们可以用输入输出流来生成对象</strong></p>
<h2 id="五、适配器模式"><a href="#五、适配器模式" class="headerlink" title="五、适配器模式"></a>五、适配器模式</h2><p>将一个接口转化成另外一个接口，从而使两个原来不能一起工作的组件一起工作。</p>
<p>我们可以想像一下一个 usb 转 hdmi ，那么就有一个目标方和一个被适配方。也就是两个接口再有一个适配器就完成了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 目标方的接口</span><br><span class="line">public interface Target &#123;</span><br><span class="line">    void use();</span><br><span class="line">&#125;</span><br><span class="line">// 被适配方 其实最好是一个接口这里直接写成了一个类</span><br><span class="line">public class BeAdapter &#123;</span><br><span class="line">    public void hello()&#123;</span><br><span class="line">        System.out.println(&quot;say hello&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 适配器 实现了目标方，组合了被适配方</span><br><span class="line">@Data</span><br><span class="line">@AllArgsConstructor</span><br><span class="line">public class AdapterDemo implements Target &#123;</span><br><span class="line">    // 其实这里可以进一步抽象 也就是所有的被适配对象另起一个接口即可</span><br><span class="line">    BeAdapter beAdapter;</span><br><span class="line"></span><br><span class="line">    public void use() &#123;</span><br><span class="line">        beAdapter.hello();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class AdapterClient &#123;</span><br><span class="line">    public static void test(Target target)&#123;</span><br><span class="line">        target.use();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        AdapterDemo adapterDemo = new AdapterDemo(new BeAdapter());</span><br><span class="line">        test(adapterDemo);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="六、代理模式"><a href="#六、代理模式" class="headerlink" title="六、代理模式"></a>六、代理模式</h2><h3 id="1-静态代理"><a href="#1-静态代理" class="headerlink" title="1.静态代理"></a>1.静态代理</h3><p>这种代理模式就是在代理对象上保留一个被代理对象的指针，然后有些事情直接调用被代理对象的方法完成，并且可以做一些前置或者后置的操作。</p>
<h3 id="2-动态代理"><a href="#2-动态代理" class="headerlink" title="2.动态代理"></a>2.动态代理</h3><h4 id="1-JDK-代理"><a href="#1-JDK-代理" class="headerlink" title="1.JDK 代理"></a>1.JDK 代理</h4><p>其实关键部分就两个：第一个就是一个处理器，所有的对代理的请求的方法都走到了处理器。另外一个就是生成代理对象过程。</p>
<p>处理器</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@AllArgsConstructor</span><br><span class="line">public class SimpleHandler implements InvocationHandler &#123;</span><br><span class="line">    RealObject real;</span><br><span class="line">    public Object invoke(Object proxy, Method method, Object[] args) throws Throwable &#123;</span><br><span class="line">        System.out.println(&quot;heihei&quot;);</span><br><span class="line">        return (Object) method.invoke(real, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>被代理对象</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public interface ObjectInterface &#123;</span><br><span class="line">    public void hello();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public class RealObject implements ObjectInterface &#123;</span><br><span class="line">    public void hello()&#123;</span><br><span class="line">        System.out.println(&quot;hello&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>生成代理对象的过程</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">public class JdkClient &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        RealObject realObject = new RealObject();</span><br><span class="line">        SimpleHandler simpleHandler = new SimpleHandler(realObject);</span><br><span class="line">        ObjectInterface proxyInstance = (ObjectInterface) Proxy.newProxyInstance(ClassLoader.getSystemClassLoader(), new Class[]&#123;ObjectInterface.class&#125;, simpleHandler);</span><br><span class="line">        proxyInstance.hello();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="七、桥接模式"><a href="#七、桥接模式" class="headerlink" title="七、桥接模式"></a>七、桥接模式</h2><p>桥接模式就是为了处理两个维度的事情，比如说我们定义一个电脑类，那么我们如果需要售卖笔记本、平板、台式机而这些东西又有不同的品牌那么我们就需要建立很多的类在电脑这个类下面。但是我们采用桥接的话我们抽出一个接口这个接口代表了品牌，然后有一个抽象类（电脑类型）包含了品牌的引用，我们只需要在不同的电脑类型下面建立各个类型，在创建一个品牌类型的电脑的时候直接 new 出来就行了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">// 品牌</span><br><span class="line">public interface Brand &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">class Dell implements Brand &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line">class Levon implements Brand &#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">// 电脑的类型</span><br><span class="line">@AllArgsConstructor</span><br><span class="line">public abstract class AbstractComputer &#123;</span><br><span class="line">    private Brand brand;</span><br><span class="line">    public void sell()&#123;</span><br><span class="line">        System.out.println(&quot;sell...&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class DesktopComp extends AbstractComputer &#123;</span><br><span class="line">    public DesktopComp(Brand brand) &#123;</span><br><span class="line">        super(brand);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">class PadComp extends AbstractComputer &#123;</span><br><span class="line">    public PadComp(Brand brand) &#123;</span><br><span class="line">        super(brand);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">//使用</span><br><span class="line">public class BridgeClient &#123;</span><br><span class="line">    public static void main(String[] args) &#123;</span><br><span class="line">        DesktopComp levonDesktopComp = new DesktopComp(new Levon());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="八、策略模式"><a href="#八、策略模式" class="headerlink" title="八、策略模式"></a>八、策略模式</h2><p>策略模式就是将一个方法传入到一个地方，简单点来说就是传入一个回调方法，真实是使用的时候看我们传递进去的那个方法作用。</p>
<h2 id="九、模板设计模式"><a href="#九、模板设计模式" class="headerlink" title="九、模板设计模式"></a>九、模板设计模式</h2><p>在上层的抽象类中放一些抽象方法先调用上，子类在继承的时候实现这些类就行了。</p>
<h2 id="10、观察者模式"><a href="#10、观察者模式" class="headerlink" title="10、观察者模式"></a>10、观察者模式</h2><p>在对一些变量修改或者方法调用的时候胡触发观察者的方法调用或者状态的转变，其实就相当于埋点。</p>
]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Lombok 常用功能</title>
    <url>/2018/08/05/Java%20Web/Lombok%E5%B8%B8%E7%94%A8%E5%8A%9F%E8%83%BD/</url>
    <content><![CDATA[<h1 id="Lombok-常用功能"><a href="#Lombok-常用功能" class="headerlink" title="Lombok 常用功能"></a>Lombok 常用功能</h1><h2 id="1-导入"><a href="#1-导入" class="headerlink" title="1.导入"></a>1.导入</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&lt;dependency&gt;</span><br><span class="line">    &lt;groupId&gt;org.projectlombok&lt;/groupId&gt;</span><br><span class="line">    &lt;artifactId&gt;lombok&lt;/artifactId&gt;</span><br><span class="line">    &lt;version&gt;1.16.20&lt;/version&gt;</span><br><span class="line">    &lt;scope&gt;provided&lt;/scope&gt;</span><br><span class="line">&lt;/dependency&gt;</span><br></pre></td></tr></table></figure>

<h2 id="2-Getter-Setter"><a href="#2-Getter-Setter" class="headerlink" title="2.@Getter/@Setter"></a>2.@Getter/@Setter</h2><p>这两个注解可以在类上也可以在字段上，看需要的粒度而定。</p>
<h2 id="3-ToString"><a href="#3-ToString" class="headerlink" title="3.@ToString"></a>3.@ToString</h2><p>生成一个 json 类型的字符串</p>
<h2 id="4-EqualsAndHashCode"><a href="#4-EqualsAndHashCode" class="headerlink" title="4.@EqualsAndHashCode"></a>4.@EqualsAndHashCode</h2><p>生成 equals 和 hashcode</p>
<h2 id="5-NoArgsConstructor"><a href="#5-NoArgsConstructor" class="headerlink" title="5.@NoArgsConstructor"></a>5.@NoArgsConstructor</h2><p>生成午无参构造器</p>
<h2 id="6-RequiredArgsConstructor"><a href="#6-RequiredArgsConstructor" class="headerlink" title="6.@RequiredArgsConstructor"></a>6.@RequiredArgsConstructor</h2><p>只对标有 @NonNull 的字段生成构造器，并且初始化的时候会检查NonNull 的字段如果为空则抛出异常。</p>
<h2 id="7-AllArgsConstructor"><a href="#7-AllArgsConstructor" class="headerlink" title="7.@AllArgsConstructor"></a>7.@AllArgsConstructor</h2><p>所有字段的构造器</p>
<h2 id="8-Data"><a href="#8-Data" class="headerlink" title="8.@Data"></a>8.@Data</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Data`是一个集合体。包含`Getter`,`Setter`,`RequiredArgsConstructor`,`ToString`,`EqualsAndHashCode</span><br></pre></td></tr></table></figure>

<h2 id="9-Value"><a href="#9-Value" class="headerlink" title="9.@Value"></a>9.@Value</h2><p>可以帮忙生成一个不可变对象。对于所有的字段都将生成final的。同<code>@Data</code>， <code>@Value</code>是一个集合体。包含<code>Getter</code>,<code>AllArgsConstructor</code>,<code>ToString</code>,<code>EqualsAndHashCode</code>。</p>
<h2 id="10-Builder"><a href="#10-Builder" class="headerlink" title="10.@Builder"></a>10.@Builder</h2><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Room room = builder().name(&quot;name&quot;).id(&quot;id&quot;).createTime(new Date()).occupation(&quot;1&quot;).occupation(&quot;2&quot;).build();</span><br></pre></td></tr></table></figure>

<h2 id="11-Log"><a href="#11-Log" class="headerlink" title="11.@Log"></a>11.@Log</h2><p>一个日志属性，可以直接使用的。</p>
<h2 id="12-UtilityClass"><a href="#12-UtilityClass" class="headerlink" title="12.@UtilityClass"></a>12.@UtilityClass</h2><p>工具类</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@UtilityClass</span><br><span class="line">public class Utility &#123;</span><br><span class="line"></span><br><span class="line">    public String getName() &#123;</span><br><span class="line">        return &quot;name&quot;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">public static void main(String[] args) &#123;</span><br><span class="line">    // Utility utility = new Utility(); 构造函数为私有的,</span><br><span class="line">    System.out.println(Utility.getName());</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="13-Cleanup"><a href="#13-Cleanup" class="headerlink" title="13.@Cleanup"></a>13.@Cleanup</h2><p>用于流等可以不需要关闭使用流对象.</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">@Cleanup</span><br><span class="line">OutputStream outStream = new FileOutputStream(new File(&quot;text.txt&quot;));</span><br><span class="line">@Cleanup</span><br><span class="line">InputStream inStream = new FileInputStream(new File(&quot;text2.txt&quot;));</span><br><span class="line">byte[] b = new byte[65536];</span><br><span class="line">while (true) &#123;</span><br><span class="line">   int r = inStream.read(b);</span><br><span class="line">   if (r == -1) break;</span><br><span class="line">   outStream.write(b, 0, r); </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>Java工具</tag>
      </tags>
  </entry>
  <entry>
    <title>Java Class对象与反射机制</title>
    <url>/2018/08/04/JVM/Java%20Class%E5%AF%B9%E8%B1%A1%E5%92%8C%E5%8F%8D%E5%B0%84/</url>
    <content><![CDATA[<h1 id="Java-Class对象与反射机制"><a href="#Java-Class对象与反射机制" class="headerlink" title="Java Class对象与反射机制"></a>Java Class对象与反射机制</h1><h2 id="一、关于Class对象"><a href="#一、关于Class对象" class="headerlink" title="一、关于Class对象"></a>一、关于Class对象</h2><h3 id="1-基本介绍"><a href="#1-基本介绍" class="headerlink" title="1.基本介绍"></a>1.基本介绍</h3><p>其实我们在任何一个 Java 应用程序中都会存在 class 对象，这个 class 对象其实就是每一个类、类型、数组、接口 等等被加载的时候在 jvm 中创建的。也就是这些类型的一个映射，这些类型在虚拟机中的实体。正是由于存在这个对象在 Jvm 中我们可以通过一定的方式获取这个对象然后去用这个对象去对我们的各种类型做一些加载获取属性等等操作，当然主要是对类进行操作并能够执行他的方法，探知未知的类的各种信息。</p>
<h3 id="2-官方文档解释"><a href="#2-官方文档解释" class="headerlink" title="2.官方文档解释"></a>2.官方文档解释</h3><p>这是我摘录的 Class 类的 javadoc 内容：</p>
<blockquote>
<ol>
<li>Instances of the class Class represent classes and interfaces in a running Java application.</li>
<li>An enum is a kind of class and an annotation is a kind of interface.</li>
<li>Every array also belongs to a class that is reflected as a Class object that is shared by all arrays with the same element type and number of dimensions.</li>
<li>The primitive Java types (boolean, byte, char, short, int, long, float, and double), and the keyword void are also represented as Class objects.</li>
<li>Class has no public constructor.Instead Class objects are constructed automatically by the Java Virtual Machine as classes are loaded and by calls to the defineClass method in the class loader.</li>
</ol>
</blockquote>
<p>其实里面主要说了这么几件事：</p>
<h4 id="1-class-对象代表类和接口"><a href="#1-class-对象代表类和接口" class="headerlink" title="1.class 对象代表类和接口"></a>1.class 对象代表类和接口</h4><p>这个比较好理解其实我们可以看一个例子就能明白：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Class clazz = Long.class;</span><br></pre></td></tr></table></figure>

<p>前面是 Class 类型后面就是一个具体的变量，那么为什么我们没有 new 这个对象就能获取到呢？ 其实在这里我们使用了 Long 这个类，那么这个类就会被 jvm 加载之后在 jvm 中形成了他所对应的 class 对象，我们在赋值的时候直接从 jvm 取到即可，这里我们也比较好理解这个类的对象其实在内存中只有一份，也就是单例的，这是因为没必要存在多份，他只是一个类的一个内存映射。</p>
<h4 id="2-枚举是类注解是接口"><a href="#2-枚举是类注解是接口" class="headerlink" title="2.枚举是类注解是接口"></a>2.枚举是类注解是接口</h4><p>注解是接口是头一回听说，但是他的定义方式比较能说明问题，应该是在编译期做了转换吧。</p>
<h4 id="3-同维同类型数组class-对象相同"><a href="#3-同维同类型数组class-对象相同" class="headerlink" title="3.同维同类型数组class 对象相同"></a>3.同维同类型数组class 对象相同</h4><p>这个也就是说他们的类型和维数一致才可以，具体看一个例子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">int[] arr1=new int[1];</span><br><span class="line">int[] arr2=new int[2];</span><br><span class="line">Assert.check(arr1.getClass()==arr2.getClass());</span><br></pre></td></tr></table></figure>

<h4 id="4-基本类型也有-class-对象"><a href="#4-基本类型也有-class-对象" class="headerlink" title="4.基本类型也有 class 对象"></a>4.基本类型也有 class 对象</h4><h4 id="5-class-对象不用创建"><a href="#5-class-对象不用创建" class="headerlink" title="5.class 对象不用创建"></a>5.class 对象不用创建</h4><p>class 对象是没有公共的构造方法，也没有静态的 getInsteance() 所以说没办法从外部构造方法，是直接的由 jvm 在加载类的时候调用了类加载的 defineClass() 方法完成 class 对象的创建的。</p>
<h3 id="3-class-对象获取"><a href="#3-class-对象获取" class="headerlink" title="3.class 对象获取"></a>3.class 对象获取</h3><p>对于 class 对象的获取方式有三种：</p>
<ol>
<li><p>使用类型属性</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Class clazz = Long.class</span><br></pre></td></tr></table></figure>
</li>
<li><p>调用对象的 getClass() 方法</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">List list = new ArrayList();</span><br><span class="line">Class clazz = list.getClass();</span><br></pre></td></tr></table></figure>
</li>
<li><p>使用forName()静态方法</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">try &#123;</span><br><span class="line">    Class.forName(&quot;com.apple.concurrent.Dispatch&quot;);</span><br><span class="line">&#125; catch (ClassNotFoundException e) &#123;</span><br><span class="line">    e.printStackTrace();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="二、关于反射"><a href="#二、关于反射" class="headerlink" title="二、关于反射"></a>二、关于反射</h2><p>先写一个简单的 javaBean 然后对这个 bean 就进行一些操作：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> grammer;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">User</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">User</span><span class="params">(String name, <span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAge</span><span class="params">(<span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getAge</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后我们主要讨论关于对于属性、方法、构造器的获取以及使用。</p>
<h4 id="1-通过-class-对象创建对象"><a href="#1-通过-class-对象创建对象" class="headerlink" title="1.通过 class 对象创建对象"></a>1.通过 class 对象创建对象</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Class&lt;User&gt; userClass = User.class;</span><br><span class="line">User userInstance = userClass.newInstance();</span><br></pre></td></tr></table></figure>

<h4 id="2-获取所有的构造方法"><a href="#2-获取所有的构造方法" class="headerlink" title="2. 获取所有的构造方法"></a>2. 获取所有的构造方法</h4><p>注意以下两个都是获取构造器列表，但是前面的是可以获取到私有的构造器，而后者并不可以，其他的都是同理的对于方法和字段来说</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Constructor&lt;?&gt;[] constructors = userClass.getDeclaredConstructors();</span><br><span class="line">Constructor&lt;?&gt;[] constructors1 = userClass.getConstructors();</span><br></pre></td></tr></table></figure>

<h4 id="3-获取所有的属性"><a href="#3-获取所有的属性" class="headerlink" title="3. 获取所有的属性"></a>3. 获取所有的属性</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Field[] fields = userClass.getDeclaredFields();</span><br></pre></td></tr></table></figure>

<h4 id="4-获取所有的方法"><a href="#4-获取所有的方法" class="headerlink" title="4.获取所有的方法"></a>4.获取所有的方法</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Method[] methods = userClass.getDeclaredMethods();</span><br></pre></td></tr></table></figure>

<h4 id="5-获取某个私有属性"><a href="#5-获取某个私有属性" class="headerlink" title="5.获取某个私有属性"></a>5.获取某个私有属性</h4><p>这个要注意对于私有属性，只有用 <code>fieldName.setAccessible(true);</code> 设置访问权限后 jvm 再运行的时候不会做权限验证 也就不会报错了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Field fieldName = userClass.getDeclaredField(<span class="string">"name"</span>);</span><br><span class="line">fieldName.setAccessible(<span class="keyword">true</span>);</span><br><span class="line">System.out.println(fieldName.getName());</span><br><span class="line"><span class="comment">// 设置值需要调用那个对应的对象  需要指明对象才可以</span></span><br><span class="line">fieldName.set(userInstance,<span class="string">"lwen"</span>);</span><br></pre></td></tr></table></figure>

<h4 id="6-获取某个方法并调用"><a href="#6-获取某个方法并调用" class="headerlink" title="6.获取某个方法并调用"></a>6.获取某个方法并调用</h4><p>这个地方需要指明我们调用的参数的具体类型，否则会由于我们的 java 重载机制而出现模糊不清的情况。当然如果是私有方法我们在调用之前必须要指定他们的访问权限，否则也会调用失败，对于构造方法也是这样的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Method setNameMethod = userClass.getDeclaredMethod(<span class="string">"setName"</span>, String.class);</span><br><span class="line"><span class="comment">//调用方法也是需要指明对象</span></span><br><span class="line">setNameMethod.invoke(userInstance,<span class="string">"lwen"</span>);</span><br></pre></td></tr></table></figure>

<h4 id="7-反射性能问题"><a href="#7-反射性能问题" class="headerlink" title="7.反射性能问题"></a>7.反射性能问题</h4><p>反射的性能一般比普通调用的性能要低很多，一般一个方法的执行时间应该是 30 倍的关系，我们希望能够加快反射的速度可以使用跳过安全检查 setAccessible(true) 来提升效率，一般相对使用反射来说的话是提升 4 倍效率。另外我们为了操作 class 我们还可以使用一些第三方的操作字节码的类库比如操作底层字节码的 ASM ，以及基于代码的 javassist 和 GCLIB 等等。</p>
<p>这里说一下关于 javassist 的使用，如何使用它来生成一个 class 文件。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> grammer;</span><br><span class="line"><span class="keyword">import</span> javassist.*;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JavassistDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> CannotCompileException, NotFoundException, IOException </span>&#123;</span><br><span class="line">        ClassPool pool = ClassPool.getDefault();</span><br><span class="line">        CtClass ctClass = pool.makeClass(<span class="string">"out.Empl"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//    创建属性</span></span><br><span class="line">        CtField int_age = CtField.make(<span class="string">"private int age;"</span>, ctClass);</span><br><span class="line">        ctClass.addField(int_age);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//    创建方法</span></span><br><span class="line">        CtMethod method = (CtMethod) CtMethod.make(<span class="string">"public int getAge()&#123;return this.age;&#125;"</span>, ctClass);</span><br><span class="line">        ctClass.addMethod(method);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//  创建构造器</span></span><br><span class="line">        CtConstructor constructor = <span class="keyword">new</span> CtConstructor(<span class="keyword">new</span> CtClass[]&#123;CtClass.intType&#125;, ctClass);</span><br><span class="line">        constructor.setBody(<span class="string">"&#123;this.age=age;&#125;"</span>);</span><br><span class="line">        ctClass.addConstructor(constructor);</span><br><span class="line"></span><br><span class="line">    <span class="comment">//    生成</span></span><br><span class="line">        ctClass.writeFile();</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>-Java -虚拟机</category>
      </categories>
      <tags>
        <tag>Java 虚拟机</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatis笔记六：整合Spring</title>
    <url>/2018/06/06/DataBase/MyBatis%E7%AC%94%E8%AE%B0%E5%85%AD%EF%BC%9A%E6%95%B4%E5%90%88Spring/</url>
    <content><![CDATA[<h1 id="MyBatis笔记六：整合Spring"><a href="#MyBatis笔记六：整合Spring" class="headerlink" title="MyBatis笔记六：整合Spring"></a>MyBatis笔记六：整合Spring</h1>]]></content>
      <categories>
        <category>MyBatis</category>
      </categories>
      <tags>
        <tag>MyBatis</tag>
        <tag>数据库</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatis笔记五：缓存</title>
    <url>/2018/06/05/DataBase/MyBatis%E7%AC%94%E8%AE%B0%E4%BA%94%EF%BC%9A%E7%BC%93%E5%AD%98/</url>
    <content><![CDATA[<h1 id="MyBatis笔记五：缓存"><a href="#MyBatis笔记五：缓存" class="headerlink" title="MyBatis笔记五：缓存"></a>MyBatis笔记五：缓存</h1><p>Mybatis 的缓存是分为两级缓存的，一个是本地缓存，也就是默认的缓存，这一个缓存是默认开启的。这个缓存是 sqlSession 级别的缓存也就是一个数据库会话的缓存，这个缓存其实说白了就是 sqlSession 级别的一个 Map 。</p>
<h2 id="1-一级缓存"><a href="#1-一级缓存" class="headerlink" title="1.一级缓存"></a>1.一级缓存</h2><p>一级缓存底层是一个 map 。虽然缓存默认开启的但是我们也会遇到缓存失效的情况：</p>
<ul>
<li>sqlSession 不同</li>
<li>查询条件不同，导致返回的数据都不同</li>
<li>在两次查询之间进行了插删改操作</li>
<li>手动使用了 <code>sqlSession.clearCache</code> 方法清除了缓存。</li>
</ul>
<h2 id="2-二级缓存"><a href="#2-二级缓存" class="headerlink" title="2.二级缓存"></a>2.二级缓存</h2><p> 二级缓存是在一级缓存的基础之上的，因为我们的一级缓存是在会话关闭之后这个缓存数据就失效了，那么我们的二级缓存的做的事情就是在会话关闭的时候我们的一级缓存的数据会被转移到一级缓存中，然后新的会话就可以参照二级缓存中的数据。</p>
<p>二级缓存是基于 namespace 的缓存，也就是每一个mapper就是一个二级缓存的空间，他们之间是互相不干扰的。不同的 namespace 查出的数据放到自己的缓存中，也就是 map 中。</p>
<h3 id="1-开启二级缓存"><a href="#1-开启二级缓存" class="headerlink" title="1.开启二级缓存"></a>1.开启二级缓存</h3><p>默认的情况是开启的，我们也应该显示的配置，就是在settings标签中配置。</p>
<h3 id="2-使用缓存"><a href="#2-使用缓存" class="headerlink" title="2.使用缓存"></a>2.使用缓存</h3><p>在 mapper 的 xml 中，使用cache标签，配置当前的 namespace 的缓存策略。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">cache</span> <span class="attr">eviction</span>=<span class="string">"FIFO"</span> <span class="attr">flushInterval</span>=<span class="string">"60000"</span> <span class="attr">readOnly</span>=<span class="string">"true"</span> <span class="attr">size</span>=<span class="string">"12"</span> <span class="attr">type</span>=<span class="string">""</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<ul>
<li><p>eviction：缓存的回收策略：</p>
<ul>
<li>LRU - 最近最少使用的：移除最长时间不被使用的对象。</li>
<li>FIFO -先进先出：按对象进入缓存的顺序来移除它们。</li>
<li>SOFT - 软引用：移除基于垃圾回收器状态和软引用规则的对象。</li>
<li>WEAK - 弱引用：更积极地移除基于垃圾收集器状态和弱引用规则的对象。</li>
<li>默认的是LRU.</li>
</ul>
</li>
<li><p>flushInterval：缓存刷新间隔。缓存多长时间清空一次，默认不清空，设置一个毫秒值</p>
</li>
<li><p>readonly：是否只读：</p>
<ul>
<li><p>true：只读； mybatis认为所有从缓存中获取数据的操作都是只读操作，不会修改数据。</p>
<p>mybatis为了加快获取速度，直接就会将数据在缓存中的引用交给用户。不安全，速度快</p>
</li>
<li><p>false：非只读： mybatis觉得获取的数据可能会被修改。mybatis会利用序列化&amp;反序列的技术克隆一份新的数据给你。 安全，速度慢</p>
</li>
</ul>
</li>
<li><p>size：缓存存放多少元素；</p>
</li>
<li><p>type=”” 指定自定义缓存的全类名，但是这个类要实现Cache接口即可；</p>
</li>
</ul>
<p><strong>当然如果我们什么东西都不写的话，也就是只写了一个cache标签，他么上述的属性会自动的被应用。</strong></p>
<p><strong>然后就是我们的Cache的自定义，其实Cache的自定义比较简单也就是我们直接实现它的Cache 接口然后实现一些东西就好，但是更好的就是我们其实不用自己写这些东西，在Mybatis的Github上有很多已经写好的cache适配包不用我们自己写、然后再缓存标签中表明type为我们的适配包的类就好了。</strong></p>
<h3 id="3-POJO实现序列化接口"><a href="#3-POJO实现序列化接口" class="headerlink" title="3.POJO实现序列化接口"></a>3.POJO实现序列化接口</h3><p>我们的每一个POJO都需要实现序列化接口，否则就会报错，因为我们如果没有开启 readonly 的话我们就必须采用序列化的方式来获取缓存下来的POJO对象。</p>
<h3 id="4-注意"><a href="#4-注意" class="headerlink" title="4.注意"></a>4.注意</h3><p>只有当我们的 sqlSession 关闭以后我们的一级缓存内容才会放到二级缓存中去，否则一直是一级缓存在起作用的。</p>
<ol>
<li>cacheEnabled=true: false：关闭缓存(二级缓存关闭) (一级缓存一直可用）</li>
<li>每个select标签都有useCache=”true”. 如果为false：不使用缓存(一级缓存依然使用，二级缓存不使用)</li>
<li>每个增删改标签的： flushCache=”true”: 增册改执行完成后就会清除缓存。他是清除一级二级缓存。</li>
<li>sqlSession. clearCache( )；只是清除当前session的一级缓存，因为我们都是用的 sqlSession 的方法，自然清除的一级缓存。</li>
<li>localCacheScope：本地缓存作用域：(默认的一级缓存是SESSION) ；如果当前会话缓存为 STATEMENT：可以禁用一级缓存。】、</li>
</ol>
<h2 id="3-缓存原理"><a href="#3-缓存原理" class="headerlink" title="3.缓存原理"></a>3.缓存原理</h2><p><img src="https://user-images.githubusercontent.com/22151420/41044719-765d61fc-69d9-11e8-9448-2f0badc1d6e7.png" alt="image"></p>
]]></content>
      <categories>
        <category>MyBatis</category>
      </categories>
      <tags>
        <tag>MyBatis</tag>
        <tag>数据库</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatis笔记四：动态SQL</title>
    <url>/2018/06/02/DataBase/MyBatis%E7%AC%94%E8%AE%B0%E5%9B%9B%EF%BC%9A%E5%8A%A8%E6%80%81SQL/</url>
    <content><![CDATA[<h1 id="MyBatis笔记四：动态SQL"><a href="#MyBatis笔记四：动态SQL" class="headerlink" title="MyBatis笔记四：动态SQL"></a>MyBatis笔记四：动态SQL</h1><p>什么是动态SQL？ 简单来说就是类似于OGNL 表达式的这种 SQL 标签的嵌套然后帮助我们生成SQL语句而避免另外我们的拼字符串的操作。</p>
<h3 id="1-if标签"><a href="#1-if标签" class="headerlink" title="1.if标签"></a>1.if标签</h3><p>if 标签中的 test 属性就是用来测试条件的，然后里面的条件之间可以采用 and or来连接，当然我们也可以使用 &amp;&amp; 这种，但是注意我们只能使用它们的实体符号而不能直接使用 &amp;&amp; 这种。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">DynamicSelectIf</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    SqlSessionFactory sessionFactory = GettingStart.getSessionFactory(<span class="string">"mybatis.xml"</span>);</span><br><span class="line">    SqlSession sqlSession = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        sqlSession = sessionFactory.openSession(<span class="keyword">true</span>);</span><br><span class="line">        EmployeeDynamicMapper mapper = sqlSession.getMapper(EmployeeDynamicMapper.class);</span><br><span class="line">        Employee emp = mapper.getEmp(<span class="keyword">new</span> Employee(<span class="number">13</span>, <span class="string">"lwen"</span>, <span class="number">12</span>));</span><br><span class="line">        System.out.println(emp);</span><br><span class="line"></span><br><span class="line">    &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (sqlSession != <span class="keyword">null</span>) &#123;</span><br><span class="line">            sqlSession.close();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmp"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from employee where</span><br><span class="line">    <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"id!=null"</span>&gt;</span></span><br><span class="line">        id=#&#123;id&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"name!=null"</span>&gt;</span></span><br><span class="line">        and name=#&#123;name&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>但是注意的一点就是假如我们没有传入我们的 id 字段的话，我们的sql就会报错，因为拼接会出问题啊。那么出来的 sql 就会是：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><span class="line"><span class="keyword">select</span> * <span class="keyword">from</span> employee <span class="keyword">where</span> <span class="keyword">and</span> <span class="keyword">name</span>=<span class="comment">#&#123;name&#125;</span></span><br></pre></td></tr></table></figure>

<p>显然会报错！</p>
<p><strong>解决方案1：</strong></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmp"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from employee where</span><br><span class="line">    <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"1=1"</span>&gt;</span></span><br><span class="line">        1=1</span><br><span class="line">    <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"id!=null"</span>&gt;</span></span><br><span class="line">        and id=#&#123;id&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"name!=null"</span>&gt;</span></span><br><span class="line">        and name=#&#123;name&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>解决方案2：</strong></p>
<h3 id="2-where标签"><a href="#2-where标签" class="headerlink" title="2.where标签"></a>2.where标签</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmpWhere"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from employee</span><br><span class="line">    <span class="tag">&lt;<span class="name">where</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"id!=null"</span>&gt;</span></span><br><span class="line">            id=#&#123;id&#125;</span><br><span class="line">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"name!=null"</span>&gt;</span></span><br><span class="line">            and name=#&#123;name&#125;</span><br><span class="line">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">where</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以看到我们删除了 where 关键字而是加上了 where标签这样的话虽然我们不传 id 我们的 sql 也是拼装正常的，不会报错。注意的一点就是我们的 where 标签只能解决当我们的 条件前面多出来的 and 或者 or 而不能解决后面的 and 比如说我们的 sql写成了：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmpWhere"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from employee</span><br><span class="line">    <span class="tag">&lt;<span class="name">where</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"id!=null"</span>&gt;</span></span><br><span class="line">            id=#&#123;id&#125; and</span><br><span class="line">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"name!=null"</span>&gt;</span></span><br><span class="line">            name=#&#123;name&#125;</span><br><span class="line">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">where</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如果不传 name 的话就会报错。</p>
<p>可以看到我们的sql 语句是这样的：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">EmployeeDynamicMapper.getEmpWhere1 - ==&gt;  Preparing: select * from employee where id=? and ;</span><br></pre></td></tr></table></figure>

<h3 id="3-trim标签"><a href="#3-trim标签" class="headerlink" title="3.trim标签"></a>3.trim标签</h3><p>trim标签就是可以给一个语句加上一个前缀一个后缀，删除某个前缀删除某个后缀。</p>
<ul>
<li>prefix：加前缀</li>
<li>prefixOverrides：删前缀</li>
<li>suffix：加后缀</li>
<li>suffixOverrides：删后缀</li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmpWhere1"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from employee</span><br><span class="line">    <span class="tag">&lt;<span class="name">trim</span> <span class="attr">prefix</span>=<span class="string">"where"</span> <span class="attr">suffixOverrides</span>=<span class="string">"and"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"id!=null"</span>&gt;</span></span><br><span class="line">            id=#&#123;id&#125; and</span><br><span class="line">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"name!=null"</span>&gt;</span></span><br><span class="line">            name=#&#123;name&#125;</span><br><span class="line">        <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">trim</span>&gt;</span>;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>解决上述问题呢。</p>
<h3 id="4-choose标签"><a href="#4-choose标签" class="headerlink" title="4.choose标签"></a>4.choose标签</h3><p>这个标签的功能就类似于带有 break 的 switch-case 语句，就是只会进入一个分支。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmpWhere2"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from employee</span><br><span class="line">    <span class="tag">&lt;<span class="name">where</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">choose</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">when</span> <span class="attr">test</span>=<span class="string">"id!=null"</span>&gt;</span></span><br><span class="line">                id=#&#123;id&#125;</span><br><span class="line">            <span class="tag">&lt;/<span class="name">when</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">when</span> <span class="attr">test</span>=<span class="string">"name!=null"</span>&gt;</span></span><br><span class="line">                name like #&#123;name&#125;</span><br><span class="line">            <span class="tag">&lt;/<span class="name">when</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">otherwise</span>&gt;</span></span><br><span class="line">                age=1</span><br><span class="line">            <span class="tag">&lt;/<span class="name">otherwise</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">choose</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">where</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以看到这里的功能说白了就是根据我们传入了哪个属性就用哪个属性查询，如果啥都没有的话就使用我们默认的就好。</p>
<h3 id="5-set标签"><a href="#5-set标签" class="headerlink" title="5.set标签"></a>5.set标签</h3><p>我们上面都是在讨论关于选择的 条件判断问题，但是如果我们希望是更新也是条件进行的呢？我们这里就有与 where 对应的标签来保证我们的逗号不会多出来。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">update</span> <span class="attr">id</span>=<span class="string">"updateEmp"</span>&gt;</span></span><br><span class="line">    update employee</span><br><span class="line">        <span class="tag">&lt;<span class="name">set</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"name!=null"</span>&gt;</span></span><br><span class="line">                name=#&#123;name&#125;,</span><br><span class="line">            <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">if</span> <span class="attr">test</span>=<span class="string">"age!=null"</span>&gt;</span></span><br><span class="line">                age=#&#123;age&#125;</span><br><span class="line">            <span class="tag">&lt;/<span class="name">if</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">set</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">update</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>同样的既然我们上面可以使用 trim 做一个通用的查询，那我们肯定可以使用 trim 做一个更通用的更新</strong></p>
<h3 id="6-foreach标签"><a href="#6-foreach标签" class="headerlink" title="6.foreach标签"></a>6.foreach标签</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmpIn"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from employee where id in</span><br><span class="line">    <span class="tag">&lt;<span class="name">foreach</span> <span class="attr">collection</span>=<span class="string">"ids"</span> <span class="attr">item</span>=<span class="string">"id"</span> <span class="attr">separator</span>=<span class="string">","</span> <span class="attr">open</span>=<span class="string">"("</span> <span class="attr">close</span>=<span class="string">")"</span> <span class="attr">index</span>=<span class="string">"index"</span>&gt;</span></span><br><span class="line">        #&#123;id&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">foreach</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>主要用于 in 这种枚举类型的。自然的我们也是<strong>可以批量保存的</strong> 就是采用这种方式，也就是  values 的位置数据很多。</p>
<h3 id="7-内置参数"><a href="#7-内置参数" class="headerlink" title="7.内置参数"></a>7.内置参数</h3><ul>
<li>_databaseId  这个表示的就是 databaseProvider 也就是我们配置的数据源厂商的名字了</li>
<li>_parameter   这个表示的就是我们的方法的入参，如果单个参数就是它本身，如果是一个对象什么的就是一个 map</li>
</ul>
<h3 id="8-变量声明，绑定"><a href="#8-变量声明，绑定" class="headerlink" title="8.变量声明，绑定"></a>8.变量声明，绑定</h3><p>我们的某一个参数过来的值我们没办法修改，或者说给他加一些修饰，修改。比如我们的某个like查询我们希望给传过来的东西加上 % 那么我们没办法直接在 sql 中添加，这时候我们就可以使用一个新变量，然后再新变量里修饰原来的变量。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bind</span> <span class="attr">name</span>=<span class="string">"_new"</span> <span class="attr">value</span>=<span class="string">"'%'+name"</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="9-sql抽取"><a href="#9-sql抽取" class="headerlink" title="9.sql抽取"></a>9.sql抽取</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">sql</span> <span class="attr">id</span>=<span class="string">"select"</span>&gt;</span></span><br><span class="line">    select name,id,$&#123;other&#125;</span><br><span class="line">    from employee;</span><br><span class="line"><span class="tag">&lt;/<span class="name">sql</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"ha"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">include</span> <span class="attr">refid</span>=<span class="string">"select"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"other"</span> <span class="attr">value</span>=<span class="string">"age"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">include</span>&gt;</span></span><br><span class="line">    where id=#&#123;id&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>我们可以使用 sql 标签抽取 sql模板，然后使用 include 标签应用模板，之所以称为模板说的是他们是可以被重用的并且里面的内容是可变的。我们可以在 include 中使用 property 标签向模板中注入我们的定义的变量，只不过要注意的是变量的名字必须是采用 ${} 不能是 #{}</p>
]]></content>
      <categories>
        <category>MyBatis</category>
      </categories>
      <tags>
        <tag>MyBatis</tag>
        <tag>数据库</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatis笔记三：SQL映射文件</title>
    <url>/2018/05/31/DataBase/MyBatis%E7%AC%94%E8%AE%B0%E4%B8%89%EF%BC%9ASQL%E6%98%A0%E5%B0%84%E6%96%87%E4%BB%B6/</url>
    <content><![CDATA[<h1 id="MyBatis笔记三：SQL映射文件"><a href="#MyBatis笔记三：SQL映射文件" class="headerlink" title="MyBatis笔记三：SQL映射文件"></a>MyBatis笔记三：SQL映射文件</h1><h2 id="1-简单的CRUD"><a href="#1-简单的CRUD" class="headerlink" title="1.简单的CRUD"></a>1.简单的CRUD</h2><h3 id="1-绑定"><a href="#1-绑定" class="headerlink" title="1.绑定"></a>1.绑定</h3><p>首先呢我们还是需要把我们的 Mapper 接口和我们的 mapper xml 进行绑定，绑定的方式就是采用 <code>namespace</code> 了。不具体多说了。</p>
<h3 id="2-接口"><a href="#2-接口" class="headerlink" title="2.接口"></a>2.接口</h3><p>接着就是写我们的 Mapper 接口了，写Mapper接口的时候有一些注意事项，就是我们的 <code>插删改</code> 操作是可以有返回值的，默认情况下这些都是返回我们影响的行数，但是这里我们可以返回 <code>Integer</code> , <code>Long</code> , <code>Boolean</code> 类型，前两个好理解，就是我们常见的那种行数。然后后面具体就是布尔值是我们的行数大于0 就返回真。当然我们也可以不返回：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lwen.dao;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> lwen.entries.Employee;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">EmployeeMapper</span> </span>&#123;</span><br><span class="line">    <span class="function">Employee <span class="title">getEmployeeById</span><span class="params">(Integer id)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">addEmpl</span><span class="params">(Employee employee)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">updateEmpl</span><span class="params">(Employee employee)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">deleteEmpl</span><span class="params">(Integer id)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-mapper-xml"><a href="#3-mapper-xml" class="headerlink" title="3.mapper xml"></a>3.mapper xml</h3><p>然后就是在 mapper 的 xml文件中写 sql 语句了，具体就是几个 <code>动作标签</code> ，然后里面配置上我们的 sql ，至于我们的 sql 的参数我们采用了 <code>#{..}</code> 的方式，然后就是我们需要注意的一点就是我们的 sql 标签的 select 标签是含有一个返回值类型的，但是其他的标签是没有的，需要注意一下。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE mapper</span></span><br><span class="line"><span class="meta">        PUBLIC " -//mybatis.org//DTD Mapper 3.e//EN"</span></span><br><span class="line"><span class="meta">        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">mapper</span> <span class="attr">namespace</span>=<span class="string">"lwen.dao.EmployeeMapper"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"selectOneEmployee"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">      select * from employee where id=#&#123;id&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--添加--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">insert</span> <span class="attr">id</span>=<span class="string">"addEmpl"</span>&gt;</span></span><br><span class="line">        insert into employee (name, age) values (#&#123;name&#125;, #&#123;age&#125;)</span><br><span class="line">    <span class="tag">&lt;/<span class="name">insert</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--修改--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">update</span> <span class="attr">id</span>=<span class="string">"updateEmpl"</span>&gt;</span></span><br><span class="line">        update employee</span><br><span class="line">        set name = #&#123;name&#125;, age = #&#123;age&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">update</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--删除--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">delete</span> <span class="attr">id</span>=<span class="string">"deleteEmpl"</span>&gt;</span></span><br><span class="line">        delete from employee where id=#&#123;id&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">delete</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mapper</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="4-测试类"><a href="#4-测试类" class="headerlink" title="4.测试类"></a>4.测试类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">CURDTest</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    SqlSessionFactory sessionFactory = GettingStart.getSessionFactory(<span class="string">"mybatis.xml"</span>);</span><br><span class="line">    SqlSession sqlSession = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">        sqlSession = sessionFactory.openSession(<span class="keyword">true</span>);</span><br><span class="line">        EmployeeMapper mapper = sqlSession.getMapper(EmployeeMapper.class);</span><br><span class="line">        System.out.println(mapper.addEmpl(<span class="keyword">new</span> Employee(<span class="number">1</span>,<span class="string">"lwen"</span>,<span class="number">18</span>)));</span><br><span class="line">    &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (sqlSession != <span class="keyword">null</span>) &#123;</span><br><span class="line">            sqlSession.close();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>测试类的地方我们也看到了一个比较奇怪的地方就是 <code>sqlSession = sessionFactory.openSession(true);</code> 我们在获取 session 的时候加了一个参数，其实这个参数就是说我们的每一条sql是否为一个事物，或者说是不是一个自动提交的 sql 。如果我们不开启这个自动提交的话我们在执行完若干条sql以后我们需要手动的调用 sql 的 <code>sqlSession.commit();</code> 方法了。</p>
<h2 id="2-获取自增主键"><a href="#2-获取自增主键" class="headerlink" title="2.获取自增主键"></a>2.获取自增主键</h2><p>我们在 Mybatis 中需要将我们插入的值获取到，然后返回给我们传入的 JavaBean 的话我们就可以才用这个方式把自动增长的主键的值封装获取我们在 Service  层就可以获取到我们插入的数据的 id 。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">insert</span> <span class="attr">id</span>=<span class="string">"addEmpl"</span> <span class="attr">useGeneratedKeys</span>=<span class="string">"true"</span> <span class="attr">keyProperty</span>=<span class="string">"id"</span>&gt;</span></span><br><span class="line">    insert into employee (name, age) values (#&#123;name&#125;, #&#123;age&#125;)</span><br><span class="line"><span class="tag">&lt;/<span class="name">insert</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><code>useGeneratedKeys</code> 这个属性说我们需要开启自动增长的策略并获取增长的值，然后我们需要把这个值封装进我们的 Java bean 的哪个属性就是我们的 <code>keyProperty</code> 来确定了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Employee employee = <span class="keyword">new</span> Employee(<span class="number">1</span>, <span class="string">"lwen"</span>, <span class="number">18</span>);</span><br><span class="line">System.out.println(mapper.addEmpl(employee));</span><br><span class="line">System.out.println(employee);</span><br></pre></td></tr></table></figure>

<p><code>Employee(id=13, name=lwen, age=18)</code> 虽然我们传入了 id=1 但是 bean 被修改成了 13。</p>
<h2 id="3-参数处理"><a href="#3-参数处理" class="headerlink" title="3.参数处理"></a>3.参数处理</h2><h3 id="1-单个参数"><a href="#1-单个参数" class="headerlink" title="1.单个参数"></a>1.单个参数</h3><p>这个时候我们直接采用 <code>#{}</code> 取出值，不做特殊处理，我们的 <code>#{}</code> 里面写什么都可以，不用和我们的方法参数对应。</p>
<h3 id="2-多个参数"><a href="#2-多个参数" class="headerlink" title="2.多个参数"></a>2.多个参数</h3><p>多个参数就设计到绑定的问题了，也就是要么我们进行参数的绑定，要么我们就进行参数的顺序处理。</p>
<h4 id="1-param顺序处理"><a href="#1-param顺序处理" class="headerlink" title="1.param顺序处理"></a>1.param顺序处理</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmplByIdAndName"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select * from employee where id=#&#123;param1&#125; and name=#&#123;param2&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这里我们的 Java 代码是有两个参数的，所以注意我们的这个地方参数是从 1 开始的不是 0.</p>
<h4 id="2-args顺序处理"><a href="#2-args顺序处理" class="headerlink" title="2.args顺序处理"></a>2.args顺序处理</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmplByIdAndName2"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select * from employee where id=#&#123;arg0&#125; and name=#&#123;arg1&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这里的参数有是从 0 开始的。</p>
<p><strong>注意：</strong> 以上两种代码我们的 Mapper 接口不需要做任何的额外的配置，就可以直接可以工作了如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">Employee <span class="title">getEmplByIdAndName</span><span class="params">(Integer id, String name)</span></span>;</span><br></pre></td></tr></table></figure>

<h4 id="3-命名参数"><a href="#3-命名参数" class="headerlink" title="3.命名参数"></a>3.命名参数</h4><p>上面的代码虽然可以工作但是我们需要注意的就是这个地方我们的代码是没有任何的具体的意义的，因为就是我们看不出来这个变量的具体意义。我们就可以和我们的接口的参数绑定，那么就是用命名参数。</p>
<p>首先我们需要在Mapper接口中写相关的注解，确定参数：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">Employee <span class="title">getEmplByIdAndName1</span><span class="params">(@Param(<span class="string">"id"</span>)</span> Integer id,@<span class="title">Param</span><span class="params">(<span class="string">"name"</span>)</span> String name)</span>;</span><br></pre></td></tr></table></figure>

<p>然后我们在 xml 中就可以应用这些参数了：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmplByIdAndName1"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select * from employee where id=#&#123;id&#125; and name=#&#123;name&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>当然采用命名参数肯定是最好的方式了。</p>
<h3 id="3-传递pojo"><a href="#3-传递pojo" class="headerlink" title="3.传递pojo"></a>3.传递pojo</h3><p>如果我们的参数太多了我们使用命名参数就很麻烦，我们就可以自己构建 pojo 。但是如果这些属性之间没什么关系，然后我们不用自己创建一个没什么用的封装类，我们直接使用 map 就好了。然后我们在 sql 就可以直接使用 <code>#{key}</code> 就可以获取对应的 value 。但是如果这个参数经常被使用的话我们就可以自己封装一个类数据传输类 TO。</p>
<p>其实真正在 Mybatis 中他自动把我们的参数封装到了 map中所以我们这么写也是很自然的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">Employee <span class="title">getEmplByIdAndName3</span><span class="params">(Map&lt;String, Object&gt; map)</span></span>;</span><br></pre></td></tr></table></figure>

<p>我们的xml 可以直接取出我们的map中的key</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmplByIdAndName3"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select * from employee where id=#&#123;id&#125; and name=#&#123;name&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Map&lt;String, Object&gt; map = <span class="keyword">new</span> HashMap();</span><br><span class="line">map.put(<span class="string">"id"</span>, <span class="number">12</span>);</span><br><span class="line">map.put(<span class="string">"name"</span>, <span class="string">"lwen"</span>);</span><br><span class="line">Employee employee4 = mapper.getEmplByIdAndName3(map);</span><br></pre></td></tr></table></figure>

<h3 id="4-List-Array-特殊情况"><a href="#4-List-Array-特殊情况" class="headerlink" title="4.List/Array 特殊情况"></a>4.List/Array 特殊情况</h3><ul>
<li><p>public Employee getEmp(@Param(“id”)Integer id,String lastName)；</p>
<p>取值：id ： #{id/param1}  lastName ：#{param2}</p>
</li>
<li><p>public Employee getEmp(Integer id,@Param(“e”)Employee emp)；</p>
<p>取值：id： #{param1}   lastName：#{param2.lastName/e.lastName}</p>
</li>
</ul>
<p><strong>特别注意</strong>：</p>
<p>如果是Collection(List、Set)类型或者是数组，也会特殊处理。也是把传入的list或者数组封装在map中。</p>
<p>比如：public Employee getEmpById(List<integer>ids)；</integer></p>
<p>取值：取出第一个id的值：#{list[e]}</p>
<h2 id="4-参数处理原理"><a href="#4-参数处理原理" class="headerlink" title="4.参数处理原理"></a>4.参数处理原理</h2><p>我们从源码的角度来看看我们的 Mybatis 框架是如何处理我们的传入的参数，以及绑定的参数的。</p>
<h3 id="1-代理对象"><a href="#1-代理对象" class="headerlink" title="1.代理对象"></a>1.代理对象</h3><p>首先我们在获取到的 mapper 上打断点，然后 step into：</p>
<p><img src="https://user-images.githubusercontent.com/22151420/40818854-e5a7c90a-658b-11e8-93db-196954b83166.png" alt="image"></p>
<p>接着我们就能来到代理类了：<code>org.apache.ibatis.binding.MapperProxy</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">invoke</span><span class="params">(Object proxy, Method method, Object[] args)</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">  <span class="keyword">try</span> &#123;</span><br><span class="line">    <span class="comment">//放行Object对象的方法，不做代理</span></span><br><span class="line">    <span class="keyword">if</span> (Object.class.equals(method.getDeclaringClass())) &#123;</span><br><span class="line">      <span class="keyword">return</span> method.invoke(<span class="keyword">this</span>, args);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (isDefaultMethod(method)) &#123;</span><br><span class="line">      <span class="keyword">return</span> invokeDefaultMethod(proxy, method, args);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">    <span class="keyword">throw</span> ExceptionUtil.unwrapThrowable(t);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">final</span> MapperMethod mapperMethod = cachedMapperMethod(method);</span><br><span class="line">   <span class="comment">//真正执行的 sqlSession</span></span><br><span class="line">  <span class="keyword">return</span> mapperMethod.execute(sqlSession, args);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到我们在这里生成了对应方法的代理对象，也就是给我们的 Mapper 接口生成了代理对象，接着就用代理对象 <code>mapperMethod</code> 来调用我们的接口方法。</p>
<h3 id="2-execute核心逻辑"><a href="#2-execute核心逻辑" class="headerlink" title="2.execute核心逻辑"></a>2.execute核心逻辑</h3><p>我们单步进入 execute 方法可以看到主要的逻辑如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">switch</span> (command.getType()) &#123;</span><br><span class="line">  <span class="keyword">case</span> INSERT: &#123;</span><br><span class="line">    Object param = method.convertArgsToSqlCommandParam(args);</span><br><span class="line">    result = rowCountResult(sqlSession.insert(command.getName(), param));</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">case</span> UPDATE: &#123;</span><br><span class="line">    Object param = method.convertArgsToSqlCommandParam(args);</span><br><span class="line">    result = rowCountResult(sqlSession.update(command.getName(), param));</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">case</span> DELETE: &#123;</span><br><span class="line">    Object param = method.convertArgsToSqlCommandParam(args);</span><br><span class="line">    result = rowCountResult(sqlSession.delete(command.getName(), param));</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">case</span> SELECT:</span><br><span class="line">    <span class="keyword">if</span> (method.returnsVoid() &amp;&amp; method.hasResultHandler()) &#123;</span><br><span class="line">      executeWithResultHandler(sqlSession, args);</span><br><span class="line">      result = <span class="keyword">null</span>;</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (method.returnsMany()) &#123;</span><br><span class="line">      result = executeForMany(sqlSession, args);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (method.returnsMap()) &#123;</span><br><span class="line">      result = executeForMap(sqlSession, args);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (method.returnsCursor()) &#123;</span><br><span class="line">      result = executeForCursor(sqlSession, args);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      Object param = method.convertArgsToSqlCommandParam(args);</span><br><span class="line">      result = sqlSession.selectOne(command.getName(), param);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">case</span> FLUSH:</span><br><span class="line">    result = sqlSession.flushStatements();</span><br><span class="line">    <span class="keyword">break</span>;</span><br><span class="line">  <span class="keyword">default</span>:</span><br><span class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> BindingException(<span class="string">"Unknown execution method for: "</span> + command.getName());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>也就是我们所有的采用Mapper 接口的方法开发的最后采用的代理生成代理对象以后我们的插删改操作还是调用了我们 sqlSession 底层的 Select、Delete 方法等等。</p>
<p>里面的套路就是先对我们的方法的参数进行转换，然后执行对应的动作方法，传入我们的参数。</p>
<h3 id="3-参数转换"><a href="#3-参数转换" class="headerlink" title="3.参数转换"></a>3.参数转换</h3><p><code>convertArgsToSqlCommandParam（）</code> 主要完成了这个功能，这里我们就着重看看这个方法。</p>
<p>最后追溯到下面这个方法，代码补偿我就全部贴上来了：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">getNamedParams</span><span class="params">(Object[] args)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">final</span> <span class="keyword">int</span> paramCount = names.size();</span><br><span class="line">  <span class="keyword">if</span> (args == <span class="keyword">null</span> || paramCount == <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (!hasParamAnnotation &amp;&amp; paramCount == <span class="number">1</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> args[names.firstKey()];</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">final</span> Map&lt;String, Object&gt; param = <span class="keyword">new</span> ParamMap&lt;Object&gt;();</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (Map.Entry&lt;Integer, String&gt; entry : names.entrySet()) &#123;</span><br><span class="line">      param.put(entry.getValue(), args[entry.getKey()]);</span><br><span class="line">      <span class="comment">// add generic param names (param1, param2, ...)</span></span><br><span class="line">      <span class="keyword">final</span> String genericParamName = GENERIC_NAME_PREFIX + String.valueOf(i + <span class="number">1</span>);</span><br><span class="line">      <span class="comment">// ensure not to overwrite parameter named with @Param</span></span><br><span class="line">      <span class="keyword">if</span> (!names.containsValue(genericParamName)) &#123;</span><br><span class="line">        param.put(genericParamName, args[entry.getKey()]);</span><br><span class="line">      &#125;</span><br><span class="line">      i++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> param;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们看看这个方法的执行逻辑：</p>
<h4 id="1-names获取"><a href="#1-names获取" class="headerlink" title="1.names获取"></a>1.names获取</h4><p>首先就是一堆names的获取，这个names又是什么？其实这是当前类的一个属性，记录的东西是Mapper 接口的参数的名字他是在这个类的构造方法中初始化的，也就是在我们调用方法之前 names 就已经确定好了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// get names from @Param annotations</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> paramIndex = <span class="number">0</span>; paramIndex &lt; paramCount; paramIndex++) &#123;</span><br><span class="line">  <span class="keyword">if</span> (isSpecialParameter(paramTypes[paramIndex])) &#123;</span><br><span class="line">    <span class="comment">// skip special parameters</span></span><br><span class="line">    <span class="keyword">continue</span>;</span><br><span class="line">  &#125;</span><br><span class="line">  String name = <span class="keyword">null</span>;</span><br><span class="line">  <span class="keyword">for</span> (Annotation annotation : paramAnnotations[paramIndex]) &#123;</span><br><span class="line">    <span class="keyword">if</span> (annotation <span class="keyword">instanceof</span> Param) &#123;</span><br><span class="line">      hasParamAnnotation = <span class="keyword">true</span>;</span><br><span class="line">      name = ((Param) annotation).value();</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">if</span> (name == <span class="keyword">null</span>) &#123;</span><br><span class="line">    <span class="comment">// @Param was not specified.</span></span><br><span class="line">    <span class="keyword">if</span> (config.isUseActualParamName()) &#123;</span><br><span class="line">      name = getActualParamName(method, paramIndex);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (name == <span class="keyword">null</span>) &#123;</span><br><span class="line">      <span class="comment">// use the parameter index as the name ("0", "1", ...)</span></span><br><span class="line">      <span class="comment">// gcode issue #71</span></span><br><span class="line">      name = String.valueOf(map.size());</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  map.put(paramIndex, name);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这就是 <code>ParamNameResolver</code> 的构造函数，里面最重要的逻辑就是确定我们的 names ：</p>
<p>1.获取 <code>@param</code> 注解标注的参数名，然后把这个注解的value值作为 names 的value，然后把参数的位置作为 key 。比如我们的方法是 <code>findAll(@Parma(&quot;name&quot;) String name,Integer age)</code> 最后生成的names这个map里面就是 {0-&gt;name,1-&gt;1} 至于为什么是 <code>1-&gt;1</code> 我们接下来再说</p>
<p>2.如果我们的 <code>@param</code> 不存在的话并且我们配置了 <code>isUseActualParamName</code> 我们会尝试采用 JDK1.8 的新特性也就是使用 -paramters 编译参数，通过反射直接获取到我们方法的参数名。</p>
<p>3.如果还是不行的话我们就使用参数的索引值，也就是 0,1 上面的注释也是特别清楚。</p>
<h4 id="2-方法没有参数"><a href="#2-方法没有参数" class="headerlink" title="2.方法没有参数"></a>2.方法没有参数</h4><p>没有参数的时候就会直接返回null</p>
<h4 id="3-单参数"><a href="#3-单参数" class="headerlink" title="3.单参数"></a>3.单参数</h4><p>只有一个参数并且没有 <code>@param</code> 注解的时候，我们就直接获取names的第一个key也就是0</p>
<h4 id="4-多参数"><a href="#4-多参数" class="headerlink" title="4.多参数"></a>4.多参数</h4><p>这个地方有两部分，当有 <code>@Param</code> 注解的时候就是把 names 的 value 作为 key ，然后我们的真正的参数作为 value 。然后当然为了保险起见 Mybatis 还未每一个参数生成了一个 以 paramIndex 作为key 以值作为 value 的 也就是我们用的 param0 .. paramN .可是我们会说为啥我们还可以用 args0 和 args1 来取值呢？</p>
<p>这个就看上面的 names 获取值的时候我们可以采用编译器的反射机制获取，因为如果我们编译器不支持这个特性的话我们的参数就会被抹掉，用args0 args1 来代替。</p>
<h2 id="5-取值规则"><a href="#5-取值规则" class="headerlink" title="5.取值规则"></a>5.取值规则</h2><h3 id="1-与-区别"><a href="#1-与-区别" class="headerlink" title="1.#{}与${}区别"></a>1.#{}与${}区别</h3><p>#{}：是以预编译的形式，将参数设置到sq1语句中；PreparedStatement；防止sq1注入</p>
<p>${}：取出的值直接拼装在sq1语句中；会有安全问题；<br>大多情况下，我们去参数的值都应该去使用#{}；<br>原生jdbc不支持占位符的地方我们就可以使用${}进行取值,比如分表；按照年份分表拆分<br><code>select * from ${year}_salary where xxx；</code></p>
<h3 id="2-注意事项"><a href="#2-注意事项" class="headerlink" title="2.#{}注意事项"></a>2.#{}注意事项</h3><p>在#{} 中我们可以放入 javaType、jdbcType、mode(存储过程）、numericScale、<br>resultMap、typeHandler、jdbcTypeName、expression(未来准备支持的功能）；</p>
<p>比较重要的就是：jdbcType通常需要在某种特定的条件下被设置：<br>在我们数据为null的时候，有些数据库可能不能识别mybatis对nu11的默认处理。比如Oracle（报错），因为他们对null的映射是到了 Other 类型，然后就会导致JdbcType OTHER：无效的类型；因为mybatis对所有的nu11都映射的是原生Jdbc的OTHER类型，<br>由于全局配置中：jdbcTypeForNull=OTHER；oracle不支持；两种办法<br>1、<code>#{email,jdbcType=OTHER}；</code><br>2、<code>&lt;setting name=&quot;jdbcTypeForNull&quot; value=&quot;NULL&quot;/&gt;</code></p>
<h2 id="6-查询"><a href="#6-查询" class="headerlink" title="6.查询"></a>6.查询</h2><h3 id="1-返回集合结果"><a href="#1-返回集合结果" class="headerlink" title="1.返回集合结果"></a>1.返回集合结果</h3><p>返回集合结果的时候我们的 resultType 不能写成 list 、set 类型而是要写做我们的集合里面的元素的类型。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getLikeByName"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from employee</span><br><span class="line">    where name like #&#123;name&#125;;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-返回map"><a href="#2-返回map" class="headerlink" title="2.返回map"></a>2.返回map</h3><p>也就是将我们的 bean 的属性作为 key 然后 值作为 value 来存储到一个 map 中，其实这个只存一条数据，那么我们的语句的返回类型就是我们的 map 他会自动的做封装。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmplReturenMap"</span> <span class="attr">resultType</span>=<span class="string">"map"</span>&gt;</span></span><br><span class="line">    select * from employee where id=#&#123;id&#125;;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function">Map&lt;String, Object&gt; <span class="title">getEmplReturenMap</span><span class="params">(Integer id)</span></span>;</span><br></pre></td></tr></table></figure>

<h3 id="3-返回多条记录的map"><a href="#3-返回多条记录的map" class="headerlink" title="3.返回多条记录的map"></a>3.返回多条记录的map</h3><p>如果我们想让map的key是我们的某一条属性，然后value是我们的实体对象，那么我们的封装结果就必须是元素的类型，也就是我们的实体类的类型，但是我们的key则需要我们另行指定我们采用的方式就是使用一个注解在方法上。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@MapKey</span>(<span class="string">"name"</span>)</span><br><span class="line"><span class="function">Map&lt;String, Employee&gt; <span class="title">getEmplReturnMaps</span><span class="params">(String name)</span></span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmplReturnMaps"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from employee where name like #&#123;name&#125;;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="4-返回自定义结果集"><a href="#4-返回自定义结果集" class="headerlink" title="4.返回自定义结果集"></a>4.返回自定义结果集</h3><p>有时候对于Mybatis自带的一些默认封装的规则不能满足我们的需求的时候，我们可以采用 resultMap 自定义结果集。这里就演示一些自定义结果集，但是注意 resultMap 与 resultType 只能存在一个。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">"MyEmp"</span> <span class="attr">type</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span> <span class="attr">column</span>=<span class="string">"id"</span> <span class="attr">property</span>=<span class="string">"id"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"name"</span> <span class="attr">property</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"age"</span> <span class="attr">property</span>=<span class="string">"age"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmplByResultMap"</span> <span class="attr">resultMap</span>=<span class="string">"MyEmp"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from employee</span><br><span class="line">    where id = #&#123;id&#125;;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以看到重点就是写我们的 resultMap 标签，然后再标签中我们自己封装规则。注意的一点就是如果说我们在 result 中没有封装 bean 中的其他属性他会自动帮我们封装，也就是我们可以把一些特殊的字段和我们的 bean 结合起来。其他的正常的自动封装。然后就是 id 字段我们在 resultMap 中指明以后Mybatis就会帮我们自动封装，并且做一些查询优化。</p>
<h3 id="5-关联查询"><a href="#5-关联查询" class="headerlink" title="5.关联查询"></a>5.关联查询</h3><h4 id="1-union"><a href="#1-union" class="headerlink" title="1.union"></a>1.union</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getById"</span> <span class="attr">resultMap</span>=<span class="string">"EmpNew"</span>&gt;</span></span><br><span class="line">    select employee.id id,employee.name name,age,d_id did,department.name dname</span><br><span class="line">    from employee,department where employee.id=#&#123;id&#125; and employee.d_id=department.id;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>我们采用的联合查询，得到了关联查询的结果，这也是常用的套路，但是注意的一点就是我们的 result 的封装并不是使用的自带的封装规则而是采用了 我们自定义的 resultMap 因为我们的 employee 中有一个 department 的对象，我们无法直接封装，至少列名都没办法对应。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">"EmpNew"</span> <span class="attr">type</span>=<span class="string">"lwen.entries.EmployeeNew"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span> <span class="attr">column</span>=<span class="string">"id"</span> <span class="attr">property</span>=<span class="string">"id"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"name"</span> <span class="attr">property</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"age"</span> <span class="attr">property</span>=<span class="string">"age"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"did"</span> <span class="attr">property</span>=<span class="string">"department.id"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"dname"</span> <span class="attr">property</span>=<span class="string">"department.name"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="2-association关联"><a href="#2-association关联" class="headerlink" title="2.association关联"></a>2.association关联</h4><p>使用 association 标签可以给一个 javaBean 的内部引用创建关联关系，与上面的效果类似，但是方法不同。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">"EmpNew1"</span> <span class="attr">type</span>=<span class="string">"lwen.entries.EmployeeNew"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span> <span class="attr">column</span>=<span class="string">"id"</span> <span class="attr">property</span>=<span class="string">"id"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"name"</span> <span class="attr">property</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"age"</span> <span class="attr">property</span>=<span class="string">"age"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">association</span> <span class="attr">property</span>=<span class="string">"department"</span> <span class="attr">javaType</span>=<span class="string">"lwen.entries.Department"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">id</span> <span class="attr">column</span>=<span class="string">"did"</span> <span class="attr">property</span>=<span class="string">"id"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"dname"</span> <span class="attr">property</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">association</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getByIdAssociation"</span> <span class="attr">resultMap</span>=<span class="string">"EmpNew1"</span>&gt;</span></span><br><span class="line">    select employee.id id,employee.name name,age,d_id did,department.name dname</span><br><span class="line">    from employee,department where employee.id=#&#123;id&#125; and employee.d_id=department.id;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="3-association多步查询"><a href="#3-association多步查询" class="headerlink" title="3.association多步查询"></a>3.association多步查询</h4><p>当我们需要 department 的 id 然后作为我们封装 Employee 的条件的时候我们就需要用 id 查询这个 Department</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">"EmpNew2"</span> <span class="attr">type</span>=<span class="string">"EmployeeNew"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span> <span class="attr">column</span>=<span class="string">"id"</span> <span class="attr">property</span>=<span class="string">"id"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"name"</span> <span class="attr">property</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"age"</span> <span class="attr">property</span>=<span class="string">"age"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">association</span> <span class="attr">property</span>=<span class="string">"department"</span> <span class="attr">select</span>=<span class="string">"lwen.dao.DepartmentMapper.getDepById"</span> <span class="attr">column</span>=<span class="string">"d_id"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">association</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmplStepByStep"</span> <span class="attr">resultMap</span>=<span class="string">"EmpNew2"</span>&gt;</span></span><br><span class="line">    select * from employee where id=#&#123;id&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getDepById"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Department"</span>&gt;</span></span><br><span class="line">    select * from department where id=#&#123;id&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以看到我们在 <code>association</code> 中调用了  <code>lwen.dao.DepartmentMapper.getDepById</code>  这个方法其实就是只进行了一个简单的 按照 id 查询，然后我们传入的 column 就是作为我们查询 department 的参数。最后做关联。</p>
<h4 id="4-懒加载"><a href="#4-懒加载" class="headerlink" title="4.懒加载"></a>4.懒加载</h4><p>需要我们在以前的分布查询的基础之上添加上一个配置项，这个配置项是在我们的全局配置文件中的。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">setting</span> <span class="attr">name</span>=<span class="string">"lazyloadingEnabled"</span> <span class="attr">value</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">setting</span> <span class="attr">name</span>=<span class="string">"aggressivelazyLoading"</span> <span class="attr">value</span>=<span class="string">"false"</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>配置这两项的时候我们在查询获取一个对象的时候我们只有在引用他们的值得时候才真的去加载这些东西，否则不会加载。</p>
<h4 id="5-一对多映射"><a href="#5-一对多映射" class="headerlink" title="5.一对多映射"></a>5.一对多映射</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">resultMap</span> <span class="attr">id</span>=<span class="string">"EmpNew3"</span> <span class="attr">type</span>=<span class="string">"Department"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">id</span> <span class="attr">column</span>=<span class="string">"id"</span> <span class="attr">property</span>=<span class="string">"id"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">result</span> <span class="attr">column</span>=<span class="string">"name"</span> <span class="attr">property</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">association</span> <span class="attr">property</span>=<span class="string">"employees"</span> <span class="attr">select</span>=<span class="string">"lwen.dao.EmployeeNewMapper.getEmplsByDeptId"</span> <span class="attr">column</span>=<span class="string">"id"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">resultMap</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmplStepByDepIdStep"</span> <span class="attr">resultMap</span>=<span class="string">"EmpNew3"</span>&gt;</span></span><br><span class="line">    select *</span><br><span class="line">    from department where id=#&#123;id&#125;;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmplsByDeptId"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">    select * from employee where d_id=#&#123;did&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>MyBatis</category>
      </categories>
      <tags>
        <tag>MyBatis</tag>
        <tag>数据库</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatis笔记二：配置</title>
    <url>/2018/05/30/DataBase/MyBatis%E7%AC%94%E8%AE%B0%E4%BA%8C%EF%BC%9A%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<h1 id="MyBatis笔记二：配置"><a href="#MyBatis笔记二：配置" class="headerlink" title="MyBatis笔记二：配置"></a>MyBatis笔记二：配置</h1><h2 id="1-全局配置"><a href="#1-全局配置" class="headerlink" title="1.全局配置"></a>1.全局配置</h2><h3 id="1-properites"><a href="#1-properites" class="headerlink" title="1.properites"></a>1.properites</h3><p>这个配置主要是引入我们的 properites 配置文件的：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">properties</span> <span class="attr">resource</span>=<span class="string">"db.properties"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">environments</span> <span class="attr">default</span>=<span class="string">"development"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">environment</span> <span class="attr">id</span>=<span class="string">"development"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">transactionManager</span> <span class="attr">type</span>=<span class="string">"JDBC"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dataSource</span> <span class="attr">type</span>=<span class="string">"POOLED"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"driver"</span> <span class="attr">value</span>=<span class="string">"$&#123;driver&#125;"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"url"</span> <span class="attr">value</span>=<span class="string">"$&#123;url&#125;"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"username"</span> <span class="attr">value</span>=<span class="string">"$&#123;username&#125;"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">"$&#123;password&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dataSource</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">environment</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">environments</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以看到我们使用  <code>&lt;properties resource=&quot;db.properties&quot;/&gt;</code> 引入了我们的数据据库的配置文件，然后这个标签有两个属性 ： <code>resource</code> 和 <code>uri</code> 第一种直接是引用项目下的文件。第二个就是引用网络路径的和我们本地文件系统的资源。<a id="more"></a></p>
<h3 id="2-settings"><a href="#2-settings" class="headerlink" title="2.settings"></a>2.settings</h3><p><strong>非常重要</strong>！！！</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--全局配置--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">settings</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">setting</span> <span class="attr">name</span>=<span class="string">"mapUnderscoreToCamelCase"</span> <span class="attr">value</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">settings</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>装了Mybatis 插件的话我们会看到我们的设置的代码提示，都不用自己去记的。</p>
<h3 id="3-typeAliases"><a href="#3-typeAliases" class="headerlink" title="3.typeAliases"></a>3.typeAliases</h3><p>可以为我们的 Java 类型取别名。避免我们去写很长的类包名，等等。并且这里提供了三种取别名的方式：</p>
<h4 id="1-typeAlias"><a href="#1-typeAlias" class="headerlink" title="1. typeAlias"></a>1. typeAlias</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">typeAliases</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">typeAlias</span> <span class="attr">type</span>=<span class="string">"lwen.entries.Employee"</span> <span class="attr">alias</span>=<span class="string">"emp"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">typeAliases</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这就是给我们的 Java 类取的别名，我们在 xml 中配置返回值，参数，命名空间的时候就不用写那么长了。我们直接写 <code>emp</code> 即可，但是注意的是我们如果不写 <code>alias</code> 属性他就会配置默认的别名，也就是我们的类名首字母小写。<code>在这里就是employee</code></p>
<h4 id="2-package"><a href="#2-package" class="headerlink" title="2.package"></a>2.package</h4><p>批量取别名，有时候我们的一个包下面的类太多了我们希望给他们都取上默认别名，我们就可以使用这个标签，但是注意这个标签不能和 <strong>typeAlias标签共存</strong> ，这个标签指定的包其实是对我们的<strong>这个包以及他的子包</strong>进行别名操作，并且都是<strong>默认别名</strong></p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">typeAliases</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">package</span> <span class="attr">name</span>=<span class="string">"lwen.entries"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">typeAliases</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="3-Alias"><a href="#3-Alias" class="headerlink" title="3.@Alias"></a>3.@Alias</h4><p>因为上面的两个标签不能同时存在，所以我们没办法给某一个包下的特定的类取别名，这里我们就需要使用 <code>@Alias</code> 来做注解别名了，这样可以解决上面的问题。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Alias</span>(<span class="string">"emps"</span>)</span><br></pre></td></tr></table></figure>

<p>其实除了这些我们需要自定义的一些别名，<strong>系统帮我们预先设定好了很多常用的别名</strong>：</p>
<table>
<thead>
<tr>
<th>别名</th>
<th>映射的类型</th>
</tr>
</thead>
<tbody><tr>
<td>_byte</td>
<td>byte</td>
</tr>
<tr>
<td>_long</td>
<td>long</td>
</tr>
<tr>
<td>_short</td>
<td>short</td>
</tr>
<tr>
<td>_int</td>
<td>int</td>
</tr>
<tr>
<td>_integer</td>
<td>int</td>
</tr>
<tr>
<td>_double</td>
<td>double</td>
</tr>
<tr>
<td>_float</td>
<td>float</td>
</tr>
<tr>
<td>_boolean</td>
<td>boolean</td>
</tr>
<tr>
<td>string</td>
<td>String</td>
</tr>
<tr>
<td>byte</td>
<td>Byte</td>
</tr>
<tr>
<td>long</td>
<td>Long</td>
</tr>
<tr>
<td>short</td>
<td>Short</td>
</tr>
<tr>
<td>int</td>
<td>Integer</td>
</tr>
<tr>
<td>integer</td>
<td>Integer</td>
</tr>
<tr>
<td>double</td>
<td>Double</td>
</tr>
<tr>
<td>float</td>
<td>Float</td>
</tr>
<tr>
<td>boolean</td>
<td>Boolean</td>
</tr>
<tr>
<td>date</td>
<td>Date</td>
</tr>
<tr>
<td>decimal</td>
<td>BigDecimal</td>
</tr>
<tr>
<td>bigdecimal</td>
<td>BigDecimal</td>
</tr>
<tr>
<td>object</td>
<td>Object</td>
</tr>
<tr>
<td>map</td>
<td>Map</td>
</tr>
<tr>
<td>hashmap</td>
<td>HashMap</td>
</tr>
<tr>
<td>list</td>
<td>List</td>
</tr>
<tr>
<td>arraylist</td>
<td>ArrayList</td>
</tr>
<tr>
<td>collection</td>
<td>Collection</td>
</tr>
<tr>
<td>iterator</td>
<td>Iterator</td>
</tr>
</tbody></table>
<p><strong>可以看到规律就是类名小写，然后基本类型就是下划线。</strong></p>
<h3 id="4-typeHandler"><a href="#4-typeHandler" class="headerlink" title="4.typeHandler"></a>4.typeHandler</h3><p>这个东西其实就是把我们的Java类型和数据库的类型相对应，这里暂时不具体说。</p>
<h3 id="5-plugins"><a href="#5-plugins" class="headerlink" title="5.plugins"></a>5.plugins</h3><p>插件功能，对<strong>下面对象的方法</strong>进行拦截，他的原理就是动态代理。</p>
<ul>
<li>Executor (update, query, flushStatements, commit, rollback, getTransaction, close, isClosed)</li>
<li>ParameterHandler (getParameterObject, setParameters)</li>
<li>ResultSetHandler (handleResultSets, handleOutputParameters)</li>
<li>StatementHandler (prepare, parameterize, batch, update, query)</li>
</ul>
<h3 id="6-environments"><a href="#6-environments" class="headerlink" title="6.environments"></a>6.environments</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">environments</span> <span class="attr">default</span>=<span class="string">"dev"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">environment</span> <span class="attr">id</span>=<span class="string">"dev"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">transactionManager</span> <span class="attr">type</span>=<span class="string">"JDBC"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"hah"</span> <span class="attr">value</span>=<span class="string">"heh"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">transactionManager</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dataSource</span> <span class="attr">type</span>=<span class="string">"POOLED"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">"$&#123;password&#125;"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dataSource</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">environment</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">environments</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>最外层就是我们的 environments 这个就是配置各种环境（比如：开发，测试，线上…），所以说我们的这个标签有一个 <code>default</code> 属性，就是用来制定我们的具体激活哪个环境的，这里用的是 dev 。然后下面就是具体的环境了，环境的 id 就是我们配置的环境的名称，每一个环境里有且只有两个属性，就是 <code>transactionManager</code> 和 <code>dataSource</code> 他们必须配置否则会报错。</p>
<p>可以看到 <code>transactionManager</code> 有一个 Type 就是用来指定使用哪个食物管理器，这里它使用了 JDBC 的，其实在Mybatis中只有两种：<code>type=”[JDBC|MANAGED]”</code>  </p>
<ul>
<li>JDBC – 这个配置就是直接使用了 JDBC 的提交和回滚设置，它依赖于从数据源得到的连接来管理事务作用域。 </li>
<li>MANAGED – 这个配置几乎没做什么。它从来不提交或回滚一个连接，而是让容器来管理事务的整个生命周期（比如 JEE 应用服务器的上下文）。 默认情况下它会关闭连接，然而一些容器并不希望这样，因此需要将 closeConnection 属性设置为 false 来阻止它默认的关闭行为。例如: </li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">transactionManager</span> <span class="attr">type</span>=<span class="string">"MANAGED"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"closeConnection"</span> <span class="attr">value</span>=<span class="string">"false"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">transactionManager</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后我们就很好奇这些 <code>MANAGED</code> 之类的东西在哪有定义，我们是否可以配置自己的书屋管理器比如强大的 Spring的事务管理。</p>
<p><code>org.apache.ibatis.session.Configuration</code> 在这个类里我们发现了</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">typeAliasRegistry.registerAlias(<span class="string">"JDBC"</span>, JdbcTransactionFactory.class);</span><br><span class="line">typeAliasRegistry.registerAlias(<span class="string">"MANAGED"</span>, ManagedTransactionFactory.class);</span><br><span class="line"></span><br><span class="line">typeAliasRegistry.registerAlias(<span class="string">"JNDI"</span>, JndiDataSourceFactory.class);</span><br><span class="line">typeAliasRegistry.registerAlias(<span class="string">"POOLED"</span>, PooledDataSourceFactory.class);</span><br><span class="line">typeAliasRegistry.registerAlias(<span class="string">"UNPOOLED"</span>, UnpooledDataSourceFactory.class);</span><br></pre></td></tr></table></figure>

<p>也就是说他们都是别名而已。那么我们就可以配置自己的类了，<strong>我们直接在 type 位置写上我们的事务管理器全类名</strong>，或者使用别名机制也可以。具体的对应的类需要什么特性我们直接看看他本来自带的两个类就明白了。</p>
<p>显然下面的配置数据源也是如此，默认的采用了连接池，也就是我们的 sqlSession 对象会被缓存起来不用每次去数据库里获取。</p>
<h3 id="7-databaseIdProvider"><a href="#7-databaseIdProvider" class="headerlink" title="7.databaseIdProvider"></a>7.databaseIdProvider</h3><p>MyBatis 可以根据不同的数据库厂商执行不同的语句，这种多厂商的支持是基于映射语句中的 <code>databaseId</code> 属性。 MyBatis 会加载不带 <code>databaseId</code> 属性和带有匹配当前数据库 <code>databaseId</code> 属性的所有语句。 如果同时找到带有 <code>databaseId</code> 和不带 <code>databaseId</code> 的相同语句，则后者会被舍弃。  </p>
<p>我们通过设置属性别名来使其变短 ：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">databaseIdProvider</span> <span class="attr">type</span>=<span class="string">"DB_VENDOR"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"SQL Server"</span> <span class="attr">value</span>=<span class="string">"sqlserver"</span>/&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"DB2"</span> <span class="attr">value</span>=<span class="string">"db2"</span>/&gt;</span>        </span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"Oracle"</span> <span class="attr">value</span>=<span class="string">"oracle"</span> /&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"MySQL"</span> <span class="attr">value</span>=<span class="string">"mysql"</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">databaseIdProvider</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后我们在 mapper 的xml文件中就可以匹配这些数据提供商：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmployeeById"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span> <span class="attr">databaseId</span>=<span class="string">"mysql"</span>&gt;</span></span><br><span class="line">    select * from employee where id=#&#123;id&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmployeeById"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span> <span class="attr">databaseId</span>=<span class="string">"oracle"</span>&gt;</span></span><br><span class="line">    select * from employee where id=#&#123;id&#125;</span><br><span class="line"><span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>那么他会按照数据源来确定当前是哪个数据源，我们需要使用哪个sql语句。这些都是自动进行的，无需我们的干预。</p>
<h3 id="8-mappers"><a href="#8-mappers" class="headerlink" title="8.mappers"></a>8.mappers</h3><p>这个就是用来配置我们的 mapper 的 xml 标签了。我们在里面配置 xml 有以下三种方式：</p>
<h4 id="1-使用-mapper-标签"><a href="#1-使用-mapper-标签" class="headerlink" title="1.使用 mapper 标签"></a>1.使用 mapper 标签</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--我们的mapper文件的位置--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mappers</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mapper</span> <span class="attr">resource</span>=<span class="string">"EmployeeMapper.xml"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mapper</span> <span class="attr">resource</span>=<span class="string">"EmployeeMapperInterface.xml"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mappers</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>显然这个地方的 mapper 标签还有两个属性 分别就是 <code>resource</code> 和 <code>uri</code> 就是和上面是一样的。</p>
<h4 id="2-使用包扫描的方式"><a href="#2-使用包扫描的方式" class="headerlink" title="2.使用包扫描的方式"></a>2.使用包扫描的方式</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">package</span> <span class="attr">name</span>=<span class="string">"lwen"</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>lwen 包下面的xml 映射文件都被加载进去。</p>
<h4 id="3-注解"><a href="#3-注解" class="headerlink" title="3.注解"></a>3.注解</h4><p>我们可以使用对应的注解 <code>注解名</code> 就是我们的 sql 语句的动作。 <code>@Select</code> <code>Update</code> 等等</p>
<p><strong>注意以上的标签都是有顺序的，顺序不能随便配置</strong></p>
]]></content>
      <categories>
        <category>MyBatis</category>
      </categories>
      <tags>
        <tag>MyBatis</tag>
        <tag>数据库</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>MyBatis笔记一：GettingStart</title>
    <url>/2018/05/30/DataBase/MyBatis%E7%AC%94%E8%AE%B0%E4%B8%80%EF%BC%9AGettingStart/</url>
    <content><![CDATA[<h1 id="MyBatis笔记一：GettingStart"><a href="#MyBatis笔记一：GettingStart" class="headerlink" title="MyBatis笔记一：GettingStart"></a>MyBatis笔记一：GettingStart</h1><h2 id="1-MyBatis优点"><a href="#1-MyBatis优点" class="headerlink" title="1.MyBatis优点"></a>1.MyBatis优点</h2><p><img src="https://user-images.githubusercontent.com/22151420/40716284-07d07a7e-643b-11e8-8a15-3e8448bc0e5f.png" alt="image"></p>
<p>我们的工具和各种框架的作用就是为了我们操作数据库简洁，对于一些数据库的工具能帮我们少写一些处理异常等等的代码，但是他们并不是自动化的，很多的操作还是需要我们自己进行，所以我们的框架就帮我们把中间黑色的部分封装起来了，减少我们的负担，但是SQL也是重中之重，我们需要把这些东西自己来控制就有 MyBatis 这个半自动框架，以及我们需要学习更多的关于 HQL 的内容。<a id="more"></a></p>
<p>相对于Hibernate 他的优点就是可以进行SQL 的定制化，能让我们的SQL更加优化，虽然 Hibernate 也可以这么做但是有一点就是我们需要在原来的框架的基础上学习更多的 HQL 相关的东西，加重学习负担。</p>
<h2 id="2-Getting-Start"><a href="#2-Getting-Start" class="headerlink" title="2.Getting Start"></a>2.Getting Start</h2><h3 id="1-引入依赖"><a href="#1-引入依赖" class="headerlink" title="1.引入依赖"></a>1.引入依赖</h3><p>首先我们需要建立一个新的工程，第一步就是进入 MyBatis 的依赖，当然也少不了数据库的依赖，这里我们为了方便实体类的编写我们还加上了 Lombok ，以及log4j ：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.mybatis<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.4.5<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    </span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-java<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.1.35<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.projectlombok<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>lombok<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.16.20<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">scope</span>&gt;</span>provided<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>log4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>log4j<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   	 	<span class="tag">&lt;<span class="name">version</span>&gt;</span>1.2.17<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-编写配置文件"><a href="#2-编写配置文件" class="headerlink" title="2.编写配置文件"></a>2.编写配置文件</h3><p>需要在类路径下编写配置文件：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE configuration</span></span><br><span class="line"><span class="meta">        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"</span></span><br><span class="line"><span class="meta">        "http://mybatis.org/dtd/mybatis-3-config.dtd"&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">environments</span> <span class="attr">default</span>=<span class="string">"development"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">environment</span> <span class="attr">id</span>=<span class="string">"development"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">transactionManager</span> <span class="attr">type</span>=<span class="string">"JDBC"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">dataSource</span> <span class="attr">type</span>=<span class="string">"POOLED"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"driver"</span> <span class="attr">value</span>=<span class="string">"com.mysql.jdbc.Driver"</span>/&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"url"</span> <span class="attr">value</span>=<span class="string">"jdbc:mysql:///spring_data"</span>/&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"username"</span> <span class="attr">value</span>=<span class="string">"root"</span>/&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">""</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">dataSource</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">environment</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">environments</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-log4j配置文件"><a href="#2-log4j配置文件" class="headerlink" title="2.log4j配置文件"></a>2.log4j配置文件</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE log4j:configuration SYSTEM "log4j.dtd"&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">log4j:configuration</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">appender</span> <span class="attr">name</span>=<span class="string">"myConsole"</span> <span class="attr">class</span>=<span class="string">"org.apache.log4j.ConsoleAppender"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">layout</span> <span class="attr">class</span>=<span class="string">"org.apache.log4j.PatternLayout"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"ConversionPattern"</span></span></span><br><span class="line"><span class="tag">                   <span class="attr">value</span>=<span class="string">"[%d&#123;dd HH:mm:ss,SSS\&#125; %-5p] [%t] %c&#123;2\&#125; - %m%n"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">layout</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--过滤器设置输出的级别--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter</span> <span class="attr">class</span>=<span class="string">"org.apache.log4j.varia.LevelRangeFilter"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"levelMin"</span> <span class="attr">value</span>=<span class="string">"debug"</span> /&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"levelMax"</span> <span class="attr">value</span>=<span class="string">"warn"</span> /&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"AcceptOnMatch"</span> <span class="attr">value</span>=<span class="string">"true"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">filter</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">appender</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">appender</span> <span class="attr">name</span>=<span class="string">"myFile"</span> <span class="attr">class</span>=<span class="string">"org.apache.log4j.RollingFileAppender"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"File"</span> <span class="attr">value</span>=<span class="string">"D:/output.log"</span> /&gt;</span><span class="comment">&lt;!-- 设置日志输出文件名 --&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 设置是否在重新启动服务时，在原有日志的基础添加新日志 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"Append"</span> <span class="attr">value</span>=<span class="string">"true"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"MaxBackupIndex"</span> <span class="attr">value</span>=<span class="string">"10"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">layout</span> <span class="attr">class</span>=<span class="string">"org.apache.log4j.PatternLayout"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"ConversionPattern"</span> <span class="attr">value</span>=<span class="string">"%p (%c:%L)- %m%n"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">layout</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">appender</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">appender</span> <span class="attr">name</span>=<span class="string">"activexAppender"</span> <span class="attr">class</span>=<span class="string">"org.apache.log4j.DailyRollingFileAppender"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"File"</span> <span class="attr">value</span>=<span class="string">"E:/activex.log"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"DatePattern"</span> <span class="attr">value</span>=<span class="string">"'.'yyyy-MM-dd'.log'"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">layout</span> <span class="attr">class</span>=<span class="string">"org.apache.log4j.PatternLayout"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"ConversionPattern"</span></span></span><br><span class="line"><span class="tag">                   <span class="attr">value</span>=<span class="string">"[%d&#123;MMdd HH:mm:ss SSS\&#125; %-5p] [%t] %c&#123;3\&#125; - %m%n"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">layout</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">appender</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 指定logger的设置，additivity指示是否遵循缺省的继承机制--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">logger</span> <span class="attr">name</span>=<span class="string">"com.runway.bssp.activeXdemo"</span> <span class="attr">additivity</span>=<span class="string">"false"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"activexAppender"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">logger</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- 根logger的设置--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">root</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">priority</span> <span class="attr">value</span> =<span class="string">"debug"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"myConsole"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">appender-ref</span> <span class="attr">ref</span>=<span class="string">"myFile"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">root</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">log4j:configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="3-编写实体类"><a href="#3-编写实体类" class="headerlink" title="3.编写实体类"></a>3.编写实体类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lwen.entries;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> lombok.AllArgsConstructor;</span><br><span class="line"><span class="keyword">import</span> lombok.Data;</span><br><span class="line"><span class="keyword">import</span> lombok.NoArgsConstructor;</span><br><span class="line"><span class="keyword">import</span> lombok.experimental.Accessors;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="meta">@Accessors</span>(chain = <span class="keyword">true</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Employee</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-编写测试类"><a href="#4-编写测试类" class="headerlink" title="4.编写测试类"></a>4.编写测试类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.apache.ibatis.io.Resources;</span><br><span class="line"><span class="keyword">import</span> org.apache.ibatis.session.SqlSession;</span><br><span class="line"><span class="keyword">import</span> org.apache.ibatis.session.SqlSessionFactory;</span><br><span class="line"><span class="keyword">import</span> org.apache.ibatis.session.SqlSessionFactoryBuilder;</span><br><span class="line"><span class="keyword">import</span> org.junit.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> java.io.InputStream;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GettingStart</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sessionFactoryTest</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        String resource = <span class="string">"mybatis.xml"</span>;</span><br><span class="line">        InputStream stream = Resources.getResourceAsStream(resource);</span><br><span class="line">        SqlSession sqlSession = <span class="keyword">null</span>;</span><br><span class="line">       <span class="keyword">try</span> &#123;</span><br><span class="line">           <span class="comment">//获取sql工厂</span></span><br><span class="line">           SqlSessionFactory sessionFactory = <span class="keyword">new</span> SqlSessionFactoryBuilder().build(stream);</span><br><span class="line">           <span class="comment">//获取sql session</span></span><br><span class="line">           sqlSession = sessionFactory.openSession();</span><br><span class="line"></span><br><span class="line">           <span class="comment">// 1.sql标识 2.绑定的参数</span></span><br><span class="line">           Object employee = sqlSession.selectOne(<span class="string">"selectOneEmployee"</span>, <span class="number">3</span>);</span><br><span class="line">           System.out.println(employee);</span><br><span class="line">       &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">           <span class="keyword">if</span> (sqlSession != <span class="keyword">null</span>) &#123;</span><br><span class="line">               sqlSession.close();</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-编写mapper文件"><a href="#5-编写mapper文件" class="headerlink" title="5.编写mapper文件"></a>5.编写mapper文件</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE mapper</span></span><br><span class="line"><span class="meta">        PUBLIC " -//mybatis.org//DTD Mapper 3.e//EN"</span></span><br><span class="line"><span class="meta">        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">mapper</span> <span class="attr">namespace</span>=<span class="string">"org.mybatis.example.BlogMapper"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"selectOneEmployee"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">      select * from employee where id=#&#123;id&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mapper</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>注意这里其实我们的mapper和测试类里的参数必须是需要匹配的才行。比如我们的 id 就是我们的测试类中的 select 方法需要传的参数，然后我们的 resultType 就是一个实体类的具体路径。</p>
<h3 id="6-总结基本套路"><a href="#6-总结基本套路" class="headerlink" title="6.总结基本套路"></a>6.总结基本套路</h3><ol>
<li><p>根据xml配置文件（全局配置文件）创建一个Sq1SessionFactory对象，xml是用来配置数据源一些运行环境信息</p>
</li>
<li><p>sql映射文件：配置了每一个sql，以及sql的封装规则等。</p>
</li>
<li><p>将sq1映射文件注册在全局配置文件中</p>
</li>
<li><p>写Java代码：</p>
<p>1）、根据全局置文件得到Sq1SessionFactory；<br>2）、使用sqlSession工厂，获取到sqlSession对象使用他来执行增改查，一个sqlSession就是代表和数据库的一次会话，用完关闭<br>3）、使用sql的唯一标志来告诉MyBatis执行哪个sql，语句都是保存在Sql映射文件中的。</p>
</li>
</ol>
<p><strong>上面我们使用的方式是以前比较老的方式，现在不再使用那种方式了，我们现在都是面向的接口编程的，所以我们使用Mapper 类来完成数据库访问。</strong></p>
<h2 id="3-面向接口编程"><a href="#3-面向接口编程" class="headerlink" title="3.面向接口编程"></a>3.面向接口编程</h2><h3 id="1-编写接口Mapper"><a href="#1-编写接口Mapper" class="headerlink" title="1.编写接口Mapper"></a>1.编写接口Mapper</h3><p>创建一个mapper接口，然后里面写方法即可：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lwen.dao;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> lwen.entries.Employee;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">EmployeeMapper</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Employee <span class="title">getEmployeeById</span><span class="params">(Integer id)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到就是这么简单，但是我们一开始是通过方法的参数和我们的 xml 的查询语句链接起来的，这里我们又怎么做这个映射关系呢？其实就是需要在我们的 xml 中配置我们的 这个Mapper 的路径，以及我们的方法作为查询语句的 id 属性，还有返回值类型，等等。接下来就来看这些配置。</p>
<h3 id="2-配置Mapper-xml"><a href="#2-配置Mapper-xml" class="headerlink" title="2.配置Mapper.xml"></a>2.配置Mapper.xml</h3><p>接下来就是配置我们的 mapper 的xml了，让他和我们的接口挂钩。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8" ?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE mapper</span></span><br><span class="line"><span class="meta">        PUBLIC " -//mybatis.org//DTD Mapper 3.e//EN"</span></span><br><span class="line"><span class="meta">        "http://mybatis.org/dtd/mybatis-3-mapper.dtd"&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">mapper</span> <span class="attr">namespace</span>=<span class="string">"lwen.dao.EmployeeMapper"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">select</span> <span class="attr">id</span>=<span class="string">"getEmployeeById"</span> <span class="attr">resultType</span>=<span class="string">"lwen.entries.Employee"</span>&gt;</span></span><br><span class="line">      select * from employee where id=#&#123;id&#125;</span><br><span class="line">    <span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">mapper</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>注意事项：</strong></p>
<ol>
<li>第一点就是我们的 namespace 以前事随便写，但是这个地方就是需要我们和Mapper 接口绑定，也就是我们的接口的全类名。</li>
<li>然后就是我们的id属性，就是我们的Mapper的方法名。</li>
<li>还有一点疑问就是我们的参数，我们都没传，其实这里他做了默认绑定，参数名和这里的sql模板参数名一致就行。</li>
</ol>
<h3 id="3-测试类"><a href="#3-测试类" class="headerlink" title="3.测试类"></a>3.测试类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">InterfaceMapperTest</span><span class="params">()</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">    String resource = <span class="string">"mybatis.xml"</span>;</span><br><span class="line">    InputStream stream = Resources.getResourceAsStream(resource);</span><br><span class="line">    SqlSession sqlSession = <span class="keyword">null</span>;</span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">       <span class="comment">//获取sql工厂</span></span><br><span class="line">       SqlSessionFactory sessionFactory = getSessionFactory(<span class="string">"mybatis.xml"</span>);</span><br><span class="line">       <span class="comment">//获取sql session</span></span><br><span class="line">       sqlSession = sessionFactory.openSession();</span><br><span class="line"></span><br><span class="line">       <span class="comment">// 获取 mapper 接口的实例对象</span></span><br><span class="line">       EmployeeMapper employeeMapper = sqlSession.getMapper(EmployeeMapper.class);</span><br><span class="line">       <span class="comment">//直接调用的 mapper 里的方法</span></span><br><span class="line">       System.out.println(employeeMapper.getEmployeeById(<span class="number">3</span>));</span><br><span class="line"></span><br><span class="line">   &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">       <span class="keyword">if</span> (sqlSession != <span class="keyword">null</span>) &#123;</span><br><span class="line">           sqlSession.close();</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-一个神奇的点"><a href="#4-一个神奇的点" class="headerlink" title="4.一个神奇的点"></a>4.一个神奇的点</h3><p>这里我们会发现我们根本没有写接口的实现类，我们怎么获取到我们的 Mapper 的对象，并且还可以调用他的方法呢？实际上我们去打印一下我们使用 <code>sqlSession.getMapper(EmployeeMapper.class)</code> 这句话获取到我们的对象，并且能执行相应的操作呢？</p>
<p>我们打印出来这个对象我们就会发现，获取到的对象实际上是一个代理对象，那么也就是说我们 Mybatis 底层采用的代理方式做的具体的功能。</p>
<p>其实我们通过SqlSession.getMapper(XXXMapper.class) 方法，<strong>MyBatis</strong> 会根据相应的接口声明的方法信息，通过动态代理机制生成一个<strong>Mapper</strong> 实例，我们使用<strong>Mapper</strong> 接口的某一个方法时，<strong>MyBatis</strong> 会根据这个方法的方法名和参数类型，确定<strong>Statement Id</strong>，底层还是通过<code>SqlSession.select(&quot;statementId&quot;,parameterObject);</code>或者<code>SqlSession.update(&quot;statementId&quot;,parameterObject);</code> 等等来实现对数据库的操作，（至于这里的动态机制是怎样实现的，我想专门用一篇文章来讨论） </p>
<p><strong>MyBatis</strong> 引用 <strong>Mapper</strong> 接口这种调用方式，纯粹是为了满足面向接口编程的需要。（其实还有一个原因是在于，面向接口的编程，使得用户在接口上可以使用注解来配置SQL语句，这样就可以脱离XML配置文件，实现“0配置”）。 因为面向接口编程其实有非常多的好处。因为现在我们的 dao 代码就是我们的 mapper 接口了，但是如果有一天我们希望不采用 MyBatis 来做数据访问了，我们在底层就可以直接写对应的实现类使用 Hibernate 等等其他框架来顶替，也是一件很容易的事。</p>
<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4.总结"></a>4.总结</h2><ol>
<li>SqlSession代表和数据库的一次会话：用完必须关闭，这也是一个资源，所以我们用完了必须进行关闭操作，避免数据库不必要的问题。</li>
<li>SqlSession和connection一样都是非线程安全，每次使用都应该去获取新的对象，不可以当作一个成员变量 ，不然自然会导致紊乱，也就是我们需要把他放在一个局部方法中，用完关闭。其实说清楚点他的底层就是 connection。</li>
<li>mapper接口没有实现类，但是mybatis会为这个接口生成一个代理对象。（将接口和xml进行绑定）</li>
<li>两个重要的配置文件：<ol>
<li>mybatis的全局配置文件：包含数据库连接池信息，事务管理器信息等。。。系统运行环境信息</li>
<li>sql映射文件：保存了每一个sq1语句的映射信息：将sql抽取出来。</li>
</ol>
</li>
</ol>
]]></content>
      <categories>
        <category>MyBatis</category>
      </categories>
      <tags>
        <tag>MyBatis</tag>
        <tag>数据库</tag>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringCloud：Eureka服务注册与发现</title>
    <url>/2018/05/26/SpringCloud/SpringCloud%EF%BC%9AEureka%E6%9C%8D%E5%8A%A1%E6%B3%A8%E5%86%8C%E4%B8%8E%E5%8F%91%E7%8E%B0/</url>
    <content><![CDATA[<h1 id="SpringCloud：Eureka服务注册与发现"><a href="#SpringCloud：Eureka服务注册与发现" class="headerlink" title="SpringCloud：Eureka服务注册与发现"></a>SpringCloud：Eureka服务注册与发现</h1><p>Eureka 其实就是一个 服务注册与发现的中心，也就是相当于我们前面做的一些生产者的服务需要注册到我们的注册中心，那么我们的消费者就不用把代码写死，而是可以去服务中心订阅对应的服务，获取服务的最新地址，并且进行逻辑解耦。</p>
<p>说的更简单一点它就相当于我们的 Dubbo 中的zookeeper 的功能就是用来服务发现的和注册的。他是一个CS架构的一个应用，也就是我们会有客户端和服务端，接下来就准备使用这个服务注册中心。</p>
<p><img src="SpringCloud%EF%BC%9AEureka%E6%9C%8D%E5%8A%A1%E6%B3%A8%E5%86%8C%E4%B8%8E%E5%8F%91%E7%8E%B0.assets/40571908-d4d43274-60d3-11e8-88fa-965317a12105.png" alt="image"></p>
<p>那么现在我们就只需要在我们以前的那个项目上在进行进一步的操作，也就是加上注册中心就好，那么现在就开始搭建！</p>
<a id="more"></a>

<h2 id="1-配置Eureka服务端"><a href="#1-配置Eureka服务端" class="headerlink" title="1.配置Eureka服务端"></a>1.配置Eureka服务端</h2><p>前面也提到了Eureka是一个CS架构的应用，所以说我们的服务器端说白了就是一个新的SpringBoot的应用，所以我们可以直接开一个Eureka 的服务端并且它带有一个Dashboard 。接着我们就需要整合我们的客户端，客户端说白了就是我们的服务提供者，我们的服务消费者，我们的服务提供者以及消费者需要去Eureka上面看哪些服务注册了能够消费哪些服务。</p>
<p>ok，还是先建一个module，然后再这个module中创建我们的服务端。</p>
<h3 id="1-pom文件"><a href="#1-pom文件" class="headerlink" title="1.pom文件"></a>1.pom文件</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>eureka-server-7001<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--eureka-server服务端 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-eureka-server<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 修改后立即生效，热部署 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>springloaded<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-devtools<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-配置文件"><a href="#2-配置文件" class="headerlink" title="2.配置文件"></a>2.配置文件</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line"><span class="attr">  port:</span> <span class="number">7001</span></span><br><span class="line"></span><br><span class="line"><span class="attr">eureka:</span></span><br><span class="line"><span class="attr">  instance:</span></span><br><span class="line"><span class="attr">    hostname:</span> <span class="string">eureka7001.com</span> <span class="comment">#eureka服务端的实例名称</span></span><br><span class="line"><span class="attr">  client:</span></span><br><span class="line"><span class="attr">    register-with-eureka:</span> <span class="literal">false</span>     <span class="comment">#false表示不向注册中心注册自己。</span></span><br><span class="line"><span class="attr">    fetch-registry:</span> <span class="literal">false</span>     <span class="comment">#false表示自己端就是注册中心，我的职责就是维护服务实例，并不需要去检索服务</span></span><br><span class="line"><span class="attr">    service-url:</span></span><br><span class="line">      <span class="comment">#单机 defaultZone: http://$&#123;eureka.instance.hostname&#125;:$&#123;server.port&#125;/eureka/       #设置与Eureka Server交互的地址查询服务和注册服务都需要依赖这个地址（单机）。</span></span><br><span class="line"><span class="attr">      defaultZone:</span> <span class="attr">http://eureka7003.com:7003,http://eureka7003.com:7002</span></span><br></pre></td></tr></table></figure>

<p>注意一下这个地方我们使用了 <code>http://eureka7003.com</code> url 这个东西其实就是我们在 host 中配置了 <code>127.0.0.1</code> 形成的。</p>
<h3 id="3-启动类"><a href="#3-启动类" class="headerlink" title="3.启动类"></a>3.启动类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@EnableEurekaServer</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EurekaServer7001</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(EurekaServer7001.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里唯一需要注意的就是我们这是一个Eureka的服务端，所以我们需要配置这个注解 <code>@EnableEurekaServer</code> 。然后同样的方法我们在建一个 Eureka的server端，就形成一个集群。</p>
<p>最后启动的时候我们就能发现我们的Eureka集群是有备份的。</p>
<p><img src="SpringCloud%EF%BC%9AEureka%E6%9C%8D%E5%8A%A1%E6%B3%A8%E5%86%8C%E4%B8%8E%E5%8F%91%E7%8E%B0.assets/1527409895357.png" alt="1527409895357"></p>
<h2 id="2-配置Eureka客户端"><a href="#2-配置Eureka客户端" class="headerlink" title="2.配置Eureka客户端"></a>2.配置Eureka客户端</h2><h3 id="1-配置生产者"><a href="#1-配置生产者" class="headerlink" title="1.配置生产者"></a>1.配置生产者</h3><h4 id="1-配置pom"><a href="#1-配置pom" class="headerlink" title="1.配置pom"></a>1.配置pom</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- actuator监控信息完善 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-actuator<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 将微服务provider侧注册进eureka --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-eureka<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="2-配置启动类"><a href="#2-配置启动类" class="headerlink" title="2.配置启动类"></a>2.配置启动类</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@EnableEurekaClient</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@MapperScan</span>(<span class="string">"lwen.dao"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeptApp8001</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(DeptApp8001.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>主要就是添加 <code>@EnableEurekaClient</code> 注解。</p>
<h4 id="3-配置yml"><a href="#3-配置yml" class="headerlink" title="3.配置yml"></a>3.配置yml</h4><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">eureka:</span></span><br><span class="line"><span class="attr">  client:</span> <span class="comment">#客户端注册进eureka服务列表内</span></span><br><span class="line"><span class="attr">    service-url:</span></span><br><span class="line">      <span class="comment">#defaultZone: http://localhost:7001/eureka</span></span><br><span class="line"><span class="attr">       defaultZone:</span> <span class="attr">http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/</span></span><br><span class="line"><span class="attr">  instance:</span></span><br><span class="line"><span class="attr">    instance-id:</span> <span class="string">cloud-dept8001</span></span><br><span class="line"><span class="attr">    prefer-ip-address:</span> <span class="literal">true</span>     <span class="comment">#访问路径可以显示IP地址</span></span><br></pre></td></tr></table></figure>

<h3 id="3-配置消费者"><a href="#3-配置消费者" class="headerlink" title="3.配置消费者"></a>3.配置消费者</h3><h4 id="1-配置pom-1"><a href="#1-配置pom-1" class="headerlink" title="1.配置pom"></a>1.配置pom</h4><p>同上</p>
<h4 id="2-配置启动类-1"><a href="#2-配置启动类-1" class="headerlink" title="2.配置启动类"></a>2.配置启动类</h4><p>需要加上Ribbon 的配置，因为我们需要使用为服务名去调用对应的服务，而不再采用以前的url地址了。<code>@RibbonClient(name = &quot;CLOUD-DEPT&quot;)</code></p>
<h4 id="3-配置RestTemplate"><a href="#3-配置RestTemplate" class="headerlink" title="3.配置RestTemplate"></a>3.配置RestTemplate</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RestConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="meta">@LoadBalanced</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> RestTemplate <span class="title">restTemplate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> RestTemplate();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>进行负载均衡</p>
<h4 id="4-配置yml"><a href="#4-配置yml" class="headerlink" title="4.配置yml"></a>4.配置yml</h4><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">eureka:</span></span><br><span class="line"><span class="attr">  client:</span></span><br><span class="line"><span class="attr">    register-with-eureka:</span> <span class="literal">false</span></span><br><span class="line"><span class="attr">    service-url:</span></span><br><span class="line"><span class="attr">      defaultZone:</span> <span class="attr">http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/</span></span><br></pre></td></tr></table></figure>

<p>这里就是相对于上面我们没有配置 instance 这是因为我们的消费者只是在上面寻找服务提供者消费这些提供者，而不用去注册这些消费者。</p>
<h2 id="3-配置负载均衡"><a href="#3-配置负载均衡" class="headerlink" title="3.配置负载均衡"></a>3.配置负载均衡</h2><p><code>Ribbon</code> 就是用来做负载均衡的。只是和一般的负载均衡的处理方式不同的是这个东西是客户端的负载均衡，所谓的客户端负载均衡就是我们的服务消费者会过来消费服务的时候对当前的生产者的状态进行判断，然后消费者主动去找负载比较轻的服务端去做消费。</p>
<p>所以说我们的客户端（消费者）就自带了负载均衡的策略，而不是说我们的客户端来了以后我们有一个中间的件，例如我们常用的 Nginx ，来判断我们采用什么策略来完成这个客户端的访问请求。</p>
<p>那么很明显，我们的代码必须是写在客户端这一方，其实真正的负载均衡在 SpringCloud 中非常简单，就是开启负载均衡，然后在我们的 rest 客户端添加一个负载均衡的标志，也就是我们的 rest 客户端是最后真正去访问我们的客户端的，所以说配置起来就两步，最后我们可以对他们的负载均衡的策略进行调整。</p>
<h3 id="1-开启负载均衡"><a href="#1-开启负载均衡" class="headerlink" title="1.开启负载均衡"></a>1.开启负载均衡</h3><p><code>@RibbonClient(name = &quot;CLOUD-DEPT&quot;)</code> 我们只需要在主类上加上这个我们就算是开启了负载均衡的功能，需要注意的一点就是我们需要在上面说明对哪个服务访问的时候采用负载均衡，因为有的服务可以负债均衡有的根本不需要，接下来我们只需要对 rest 客户端修改修改即可。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RibbonClient</span>(name = <span class="string">"CLOUD-DEPT"</span>)</span><br><span class="line"><span class="meta">@EnableEurekaClient</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeptConsumerApp81</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(DeptConsumerApp81.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-rest-客户端启用负载均衡"><a href="#2-rest-客户端启用负载均衡" class="headerlink" title="2.rest 客户端启用负载均衡"></a>2.rest 客户端启用负载均衡</h3><p>可以看到下面的代码我们只需要在这个rest客户端的获取的时候添加一个注解我们的rest客户端就拥有了负载均衡的功能。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RestConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="meta">@LoadBalanced</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> RestTemplate <span class="title">restTemplate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> RestTemplate();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-自定义负载均衡算法"><a href="#3-自定义负载均衡算法" class="headerlink" title="3.自定义负载均衡算法"></a>3.自定义负载均衡算法</h3><p>对于Nginx我们肯定熟悉的就是他有很多的自定义算法，也就是用来做负载均衡的。当然这个地方默认的负载均衡的算法就是轮询，然后我们还可以自定义一些：</p>
<p><img src="SpringCloud%EF%BC%9AEureka%E6%9C%8D%E5%8A%A1%E6%B3%A8%E5%86%8C%E4%B8%8E%E5%8F%91%E7%8E%B0.assets/1527507569023.png" alt="1527507569023"></p>
<p>可以看到这里有很多的负载均衡的算法，那些具体的类其实都是算法，然后可以看到有一个 MyLBRule1 这个是我自定义的一个负载均衡算法，其实很简单就是拷贝一个类然后照着他的样子重写 <code>choose</code> 方法即可。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> rules;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.netflix.client.config.IClientConfig;</span><br><span class="line"><span class="keyword">import</span> com.netflix.loadbalancer.AbstractLoadBalancerRule;</span><br><span class="line"><span class="keyword">import</span> com.netflix.loadbalancer.ILoadBalancer;</span><br><span class="line"><span class="keyword">import</span> com.netflix.loadbalancer.Server;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RandomRule_ZY</span> <span class="keyword">extends</span> <span class="title">AbstractLoadBalancerRule</span></span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="comment">// total = 0 // 当total==5以后，我们指针才能往下走，</span></span><br><span class="line">	<span class="comment">// index = 0 // 当前对外提供服务的服务器地址，</span></span><br><span class="line">	<span class="comment">// total需要重新置为零，但是已经达到过一个5次，我们的index = 1</span></span><br><span class="line">	<span class="comment">// 分析：我们5次，但是微服务只有8001 8002 8003 三台，OK？</span></span><br><span class="line">	<span class="comment">// </span></span><br><span class="line">	</span><br><span class="line">	</span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">int</span> total = <span class="number">0</span>; 			<span class="comment">// 总共被调用的次数，目前要求每台被调用5次</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">int</span> currentIndex = <span class="number">0</span>;	<span class="comment">// 当前提供服务的机器号</span></span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">public</span> Server <span class="title">choose</span><span class="params">(ILoadBalancer lb, Object key)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">if</span> (lb == <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		Server server = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">while</span> (server == <span class="keyword">null</span>) &#123;</span><br><span class="line">			<span class="keyword">if</span> (Thread.interrupted()) &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">			&#125;</span><br><span class="line">			List&lt;Server&gt; upList = lb.getReachableServers();</span><br><span class="line">			List&lt;Server&gt; allList = lb.getAllServers();</span><br><span class="line"></span><br><span class="line">			<span class="keyword">int</span> serverCount = allList.size();</span><br><span class="line">			<span class="keyword">if</span> (serverCount == <span class="number">0</span>) &#123;</span><br><span class="line">				<span class="comment">/*</span></span><br><span class="line"><span class="comment">				 * No servers. End regardless of pass, because subsequent passes only get more</span></span><br><span class="line"><span class="comment">				 * restrictive.</span></span><br><span class="line"><span class="comment">				 */</span></span><br><span class="line">				<span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//			int index = rand.nextInt(serverCount);// java.util.Random().nextInt(3);</span></span><br><span class="line"><span class="comment">//			server = upList.get(index);</span></span><br><span class="line"></span><br><span class="line">			</span><br><span class="line"><span class="comment">//			private int total = 0; 			// 总共被调用的次数，目前要求每台被调用5次</span></span><br><span class="line"><span class="comment">//			private int currentIndex = 0;	// 当前提供服务的机器号</span></span><br><span class="line">            <span class="keyword">if</span>(total &lt; <span class="number">5</span>)</span><br><span class="line">            &#123;</span><br><span class="line">	            server = upList.get(currentIndex);</span><br><span class="line">	            total++;</span><br><span class="line">            &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">	            total = <span class="number">0</span>;</span><br><span class="line">	            currentIndex++;</span><br><span class="line">	            <span class="keyword">if</span>(currentIndex &gt;= upList.size())</span><br><span class="line">	            &#123;</span><br><span class="line">	              currentIndex = <span class="number">0</span>;</span><br><span class="line">	            &#125;</span><br><span class="line">            &#125;			</span><br><span class="line">			</span><br><span class="line">			</span><br><span class="line">			<span class="keyword">if</span> (server == <span class="keyword">null</span>) &#123;</span><br><span class="line">				<span class="comment">/*</span></span><br><span class="line"><span class="comment">				 * The only time this should happen is if the server list were somehow trimmed.</span></span><br><span class="line"><span class="comment">				 * This is a transient condition. Retry after yielding.</span></span><br><span class="line"><span class="comment">				 */</span></span><br><span class="line">				Thread.yield();</span><br><span class="line">				<span class="keyword">continue</span>;</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="keyword">if</span> (server.isAlive()) &#123;</span><br><span class="line">				<span class="keyword">return</span> (server);</span><br><span class="line">			&#125;</span><br><span class="line"></span><br><span class="line">			<span class="comment">// Shouldn't actually happen.. but must be transient or a bug.</span></span><br><span class="line">			server = <span class="keyword">null</span>;</span><br><span class="line">			Thread.yield();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">return</span> server;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> Server <span class="title">choose</span><span class="params">(Object key)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="keyword">return</span> choose(getLoadBalancer(), key);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">initWithNiwsConfig</span><span class="params">(IClientConfig clientConfig)</span></span></span><br><span class="line"><span class="function">	</span>&#123;</span><br><span class="line">		<span class="comment">// TODO Auto-generated method stub</span></span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这是一个简单的算法，也就是用来每个服务器轮询5次的算法。</p>
<h2 id="4-面向接口的服务调用"><a href="#4-面向接口的服务调用" class="headerlink" title="4.面向接口的服务调用"></a>4.面向接口的服务调用</h2><p>前面我们都看到了关于我们的微服务的调用其实就是使用我们的 rest 客户端去请求我们的生产者的 controller ，那么我们是不是能够使用更简洁的方式去调用我们的生产者的服务呢？也就是我们不再用手动的注入我们的 restTemplate 而是直接类似于我们以前那样直接调用我们的服务端的controller。</p>
<p>并且当我们可以使用这个面向接口的服务以后我们又如何进行负载均衡呢，因为我们以前的负载均衡的方式就是采用的对 rest 客户端添加注解，这里我们因为采用了接口的调用方式又如何使用负载均衡，这里 Feign 就帮助我们解决了这一系列的问题。</p>
<p>首先可以知道的是： Feign 也是一个基于客户端的面向接口的服务，所以我们需要在客户端进行配置，由于他是一个发布的controller 服务我们直接把他定义到 api 层面即可。然后我们在需要调用这个服务的地方也需要进行 Feign 的配置，也就是需要配置消费者。这样我们才能识别到 Feign 才对。</p>
<h3 id="1-配置-API"><a href="#1-配置-API" class="headerlink" title="1.配置 API"></a>1.配置 API</h3><p>我们建立一个 service 包，然后我们在这个包里写一个<code>接口</code> 注意是一个接口，因为我们是面向接口编程 的，这里主要要注意的就是我们的 url 就是我们以前的 rest 客户端的请求的 url 说啊比了就是我们服务提供者的方法的 url 。</p>
<p>那么另外一个非常重要的地方就是我们的 <code>@FeignClient</code> 这个说明了我们的接口就是一个 Feign 的 rest 调用接口 .</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">package lwen.service;</span><br><span class="line"></span><br><span class="line">import lwen.entries.Dept;</span><br><span class="line">import org.springframework.cloud.netflix.feign.FeignClient;</span><br><span class="line">import org.springframework.web.bind.annotation.GetMapping;</span><br><span class="line">import org.springframework.web.bind.annotation.PostMapping;</span><br><span class="line">import java.util.List;</span><br><span class="line">@FeignClient(&quot;CLOUD-DEPT&quot;)</span><br><span class="line">public interface DeptClientService &#123;</span><br><span class="line"></span><br><span class="line">    @GetMapping(&quot;/dept/list&quot;)</span><br><span class="line">    List&lt;Dept&gt; findAll();</span><br><span class="line"></span><br><span class="line">    @PostMapping(&quot;/dept&quot;)</span><br><span class="line">    Boolean insertDept(Dept dept);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-配置消费者"><a href="#2-配置消费者" class="headerlink" title="2.配置消费者"></a>2.配置消费者</h3><p>我们配置好了客户端其实我们就可以进行服务访问了,这里我们为了可以直接访问接口服务我们创建一个新的服务端的服务。</p>
<p>首先我们需要在主类上开启我们的 Feign 也就是使用注解 <code>@EnableFeignClients</code> </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@EnableFeignClients</span>(<span class="string">"lwen.service"</span>)</span><br><span class="line"><span class="meta">@EnableEurekaClient</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeptConsumerFeignApp82</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(DeptConsumerFeignApp82.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后就是我们的，controller了这里就是去请求我们的后台的的服务提供者的 controller 层。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lwen.controller;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> lwen.entries.Dept;</span><br><span class="line"><span class="keyword">import</span> lwen.service.DeptClientService;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.GetMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.PostMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RestController;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeptController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> DeptClientService deptClientService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/dept/list"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List <span class="title">findAll</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> deptClientService.findAll();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@PostMapping</span>(<span class="string">"/dept"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Boolean <span class="title">insertDept</span><span class="params">(Dept dept)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> deptClientService.insertDept(dept);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>其实当我们写完这些东西配置都正确了以后我们就会发现我们的 ide 会有非常智能的提示，也就是把我们暴露的服务的controller 的方法显示成可运行的状态。</p>
<p><img src="SpringCloud%EF%BC%9AEureka%E6%9C%8D%E5%8A%A1%E6%B3%A8%E5%86%8C%E4%B8%8E%E5%8F%91%E7%8E%B0.assets/1527511615518.png" alt="1527511615518"></p>
<p>很神奇，接着我们就可以准备解决负载均衡的问题了。</p>
<h3 id="3-负载均衡"><a href="#3-负载均衡" class="headerlink" title="3.负载均衡"></a>3.负载均衡</h3><p>好的，我们的 rest 客户端被省略了，那么我们如何进行负载均衡这是一个问题。但是事实上我们完全不用担心如何进行负载均衡，这是因为我们的 Feign 已经帮我们整合好了这些东西。</p>
<p>是的，也就是我们在标注 <code>@FeignClient</code> 这个注解的时候我们已经配置了负载均衡，在我们的每一个方法之上，并且他的默认的负载均衡的策略就是轮训方式，现在我们就可以启动我们的服务然后测试一下。的确是这样。</p>
<p>可是我们以前还可以对服务是可以进行负载均衡的策略进行定制的，其实这个也很简单，我们只需要在我们的API端写好我们的配置类即可。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@FeignClient</span>(value = <span class="string">"CLOUD-DEPT"</span>,configuration = MyRuleConfig.class)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">DeptClientService</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/dept/list"</span>)</span><br><span class="line">    <span class="function">List&lt;Dept&gt; <span class="title">findAll</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@PostMapping</span>(<span class="string">"/dept"</span>)</span><br><span class="line">    <span class="function">Boolean <span class="title">insertDept</span><span class="params">(Dept dept)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到我们的注解上加了新的属性，就是用来配置负载均衡的，并且值得注意的是他和 Ribbon 的负载均衡的属性一模一样。</p>
<h2 id="5-服务熔断与服务降级"><a href="#5-服务熔断与服务降级" class="headerlink" title="5.服务熔断与服务降级"></a>5.服务熔断与服务降级</h2><h3 id="1-基本概念"><a href="#1-基本概念" class="headerlink" title="1.基本概念"></a>1.基本概念</h3><p>其实在我们的应用中很有可能会出现服务压力太大的情况，这时候为了避免我们服务发生雪崩我们需要对他们进行必要的处理，比如这里提到的服务熔断和服务降级。</p>
<p><code>服务熔断</code>：服务熔断一般是指软件系统中，由于某些原因使得服务出现了过载现象，为防止造成整个系统故障，从而采用的一种保护措施，所以很多地方把熔断亦称为过载保护。 这个其实就是在我们服务被熔断的时候还需要给客户端返回一个符合预期的友好的结果。</p>
<p><code>服务降级</code> ：旅行箱是必备物，平常走走近处绰绰有余，但一旦出个远门，再大的箱子都白搭了，怎么办呢？常见的情景就是把物品拿出来分分堆，比了又比，最后一些非必需品的就忍痛放下了，等到下次箱子够用了，再带上用一用。而服务降级，就是这么回事，整体资源快不够了，忍痛将某些服务先关掉，待渡过难关，再开启回来。 但是这个过程中我们的关闭的服务也必须要返回一些友好的提示，不能让客户端调用抛出异常或者长时间等待。</p>
<h3 id="2-熔断降级的异同"><a href="#2-熔断降级的异同" class="headerlink" title="2.熔断降级的异同"></a>2.熔断降级的异同</h3><p>所以从上述分析来看，两者其实从有些角度看是有一定的类似性的：</p>
<ol>
<li><strong>目的很一致</strong>，都是从可用性可靠性着想，为防止系统的整体缓慢甚至崩溃，采用的技术手段；</li>
<li><strong>最终表现类似</strong>，对于两者来说，最终让用户体验到的是某些功能暂时不可达或不可用；</li>
<li><strong>粒度一般都是服务级别</strong>，当然，业界也有不少更细粒度的做法，比如做到数据持久层（允许查询，不允许增删改）；</li>
<li><strong>自治性要求很高</strong>，熔断模式一般都是服务基于策略的自动触发，降级虽说可人工干预，但在微服务架构下，完全靠人显然不可能，开关预置、配置中心都是必要手段；</li>
</ol>
<p>而两者的区别也是明显的：</p>
<ol>
<li><strong>触发原因不太一样</strong>，服务熔断一般是某个服务（下游服务）故障引起，而服务降级一般是从整体负荷考虑；</li>
<li><strong>管理目标的层次不太一样</strong>，熔断其实是一个框架级的处理，每个微服务都需要（无层级之分），而降级一般需要对业务有层级之分（比如降级一般是从最外围服务开始）</li>
<li><strong>实现方式不太一样</strong>，这个区别后面会单独来说</li>
</ol>
<h3 id="3-服务熔断配置"><a href="#3-服务熔断配置" class="headerlink" title="3.服务熔断配置"></a>3.服务熔断配置</h3><p>服务熔断的话一般来说我们只需要对于我们的生产者的某个方法进行熔断处理，说的简单点就是但我们的服务出现问题，需要被熔断的的时候我们需要有一个保险机制然后给客户端符合预期的结果，也就是我们的熔断以后的句柄（Handler）。</p>
<p>首先创建一个新的 Module ，然后我们需要在主启动类上面加上对熔断的支持。<code>@EnableCircuitBreaker</code> </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lwen.controller;</span><br><span class="line"><span class="keyword">import</span> com.netflix.hystrix.contrib.javanica.annotation.HystrixCommand;</span><br><span class="line"><span class="keyword">import</span> lwen.entries.Dept;</span><br><span class="line"><span class="keyword">import</span> lwen.service.DeptService;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.GetMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.PostMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RestController;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeptController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    DeptService deptService;</span><br><span class="line">    <span class="meta">@HystrixCommand</span>(fallbackMethod = <span class="string">"findAllFallBack"</span>)</span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/dept/list"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;Dept&gt; <span class="title">findAll</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> deptService.findAll();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@PostMapping</span>(<span class="string">"/dept"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Boolean <span class="title">insertDept</span><span class="params">(Dept dept)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> deptService.insertDept(dept);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Dept <span class="title">findAllFallBack</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Dept().setName(<span class="string">"No such data ..."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到上面的这个controller 就是我们对 findAll 这个方法进行了服务熔断的处理，也就是当我们服务出现问题的时候我们的熔断句柄就会被调用。这里就是我们设置的 fallback 。</p>
<p>因为这个方法肯定会抛出异常，所以我们也自然会调用我们的 fallback </p>
<h3 id="4-服务降级配置"><a href="#4-服务降级配置" class="headerlink" title="4.服务降级配置"></a>4.服务降级配置</h3><p>服务降级一般是对于整个的一个服务来说的，所以我们的降级配置直接就配置在API中就好。</p>
<p>首先就是添加一个 <code>FallbackFactory</code>  主要就是对我们的服务每一个方法进行降级的句柄。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lwen.service;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> feign.hystrix.FallbackFactory;</span><br><span class="line"><span class="keyword">import</span> lwen.entries.Dept;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Component;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Component</span> <span class="comment">// 不要忘记添加，不要忘记添加</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeptClientServiceFallbackFactory</span> <span class="keyword">implements</span> <span class="title">FallbackFactory</span>&lt;<span class="title">DeptClientService</span>&gt;</span></span><br><span class="line"><span class="class"></span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> DeptClientService <span class="title">create</span><span class="params">(Throwable throwable)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> DeptClientService() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> List&lt;Dept&gt; <span class="title">findAll</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                ArrayList&lt;Dept&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">                list.add(<span class="keyword">new</span> Dept().setName(<span class="string">"No such data"</span>));</span><br><span class="line">                <span class="keyword">return</span> list;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> Boolean <span class="title">insertDept</span><span class="params">(Dept dept)</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在我们的 API 中添加 <code>@FeignClient(value = &quot;CLOUD-DEPT&quot;, configuration = MyRuleConfig.class, fallbackFactory = DeptClientServiceFallbackFactory.class)</code> 也就是我们的 <code>fallbackFactory</code> </p>
<h2 id="6-服务网关"><a href="#6-服务网关" class="headerlink" title="6.服务网关"></a>6.服务网关</h2><p>服务网关就相当于我们网络中的网关一样，住哟啊就用着请求的转发代理作用。然后这里我们的目的就是把所有的微服务都放在内网，然后我们只对外暴露网关即可，这样我们的微服务才会更加的安全。</p>
<p>在 SpringCloud 中我们使用的网关是 Zuul ，他是一个独立的微服务，所以我们自己建立一个新的服务。这个服务其实只需要一个启动类和一个配置文件即可。</p>
<h3 id="1-配置主类"><a href="#1-配置主类" class="headerlink" title="1.配置主类"></a>1.配置主类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lwen;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.SpringApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.autoconfigure.SpringBootApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.cloud.netflix.zuul.EnableZuulProxy;</span><br><span class="line"></span><br><span class="line"><span class="meta">@EnableZuulProxy</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeptZuul9527</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(DeptZuul9527.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-配置文件-1"><a href="#2-配置文件-1" class="headerlink" title="2.配置文件"></a>2.配置文件</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line"><span class="attr">  port:</span> <span class="number">9527</span></span><br><span class="line"></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">  application:</span></span><br><span class="line"><span class="attr">    name:</span> <span class="string">microservicecloud-zuul-gateway</span></span><br><span class="line"></span><br><span class="line"><span class="attr">eureka:</span></span><br><span class="line"><span class="attr">  client:</span></span><br><span class="line"><span class="attr">    service-url:</span></span><br><span class="line"><span class="attr">      defaultZone:</span> <span class="attr">http://eureka7001.com:7001/eureka,http://eureka7002.com:7002/eureka</span></span><br><span class="line"><span class="attr">  instance:</span></span><br><span class="line"><span class="attr">    instance-id:</span> <span class="string">gateway-9527.com</span></span><br><span class="line"><span class="attr">    prefer-ip-address:</span> <span class="literal">true</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="attr">zuul:</span></span><br><span class="line">  <span class="comment">#ignored-services: microservicecloud-dept</span></span><br><span class="line"><span class="attr">  prefix:</span> <span class="string">/lwen</span>  <span class="comment">#服务前缀</span></span><br><span class="line"><span class="attr">  ignored-services:</span> <span class="string">"*"</span></span><br><span class="line"><span class="attr">  routes:</span></span><br><span class="line">    <span class="string">mydept.serviceId:</span> <span class="string">cloud-dept</span></span><br><span class="line">    <span class="string">mydept.path:</span> <span class="string">/mydept/**</span>   <span class="comment">#服务路径  后面接的就是我们的controller 的地址</span></span><br></pre></td></tr></table></figure>

<p>之后我们就可以访问对应的地址 <code>http://localhost:8080/lwen/mydept/controller_url</code></p>
<h2 id="7-统一配置服务"><a href="#7-统一配置服务" class="headerlink" title="7.统一配置服务"></a>7.统一配置服务</h2><p>我们的开发过程中有时候为了更方便的股那里我们所有的微服务的配置文件我们可以单独启动一个微服务专门用来管理我们其他的微服务的配置文件，这里我们要做的就是这么一件事，采用的东西就叫做 Config 组件。</p>
<h3 id="1-创建服务"><a href="#1-创建服务" class="headerlink" title="1.创建服务"></a>1.创建服务</h3><p>首先还是创建一个新的服务，因为我们是使用一个独立的微服务来管理其他的微服务。这个微服务基本和我们一个普通的SpringBoot 应用没区别，但是我们还是需要加入我们的配置模块。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>dept-config-3344<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- springCloud Config --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-config-server<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 避免Config的Git插件报错：org/eclipse/jgit/api/TransportConfigCallback --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.eclipse.jgit<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>org.eclipse.jgit<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.10.0.201712302008-r<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 图形化监控 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-actuator<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 熔断 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-hystrix<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-eureka<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-config<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-jetty<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-test<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 热部署插件 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>springloaded<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-devtools<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-主类"><a href="#2-主类" class="headerlink" title="2.主类"></a>2.主类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lwen;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.SpringApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.autoconfigure.SpringBootApplication;</span><br><span class="line"></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeptConfigApp3344</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(DeptConfigApp3344.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-配置文件"><a href="#3-配置文件" class="headerlink" title="3.配置文件"></a>3.配置文件</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line"><span class="attr">  port:</span> <span class="number">3344</span></span><br><span class="line"></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">  application:</span></span><br><span class="line"><span class="attr">    name:</span>  <span class="string">cloud-config</span></span><br><span class="line"><span class="attr">  cloud:</span></span><br><span class="line"><span class="attr">    config:</span></span><br><span class="line"><span class="attr">      server:</span></span><br><span class="line"><span class="attr">        git:</span></span><br><span class="line"><span class="attr">          uri:</span> <span class="attr">https://github.com/lwenxu/springcloudconfig.git</span> <span class="comment">#GitHub上面的git仓库名字</span></span><br></pre></td></tr></table></figure>

<p>其实我们的主类基本没有任何的特别的地方，最主要的就是我们的配置文件，这里我们配置了我们 config 的server 也就是我们仓库的地址。这个东西并不需要我们配置 ssh ，这是因为我们的仓库主要是公开的我们的服务就可以访问到。</p>
<h3 id="4-配置客户端"><a href="#4-配置客户端" class="headerlink" title="4.配置客户端"></a>4.配置客户端</h3><p>客户端我们需要一个新的配置文件，做一些通用的不可变的配置，这是 boostrap.yml 配置文件</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">  cloud:</span></span><br><span class="line"><span class="attr">    config:</span></span><br><span class="line"><span class="attr">      name:</span> <span class="string">application</span> <span class="comment">#需要从github上读取的资源名称，注意没有yml后缀名</span></span><br><span class="line"><span class="comment">#      profile: test   #本次访问的配置项</span></span><br><span class="line"><span class="attr">      label:</span> <span class="string">master</span>   </span><br><span class="line"><span class="attr">      uri:</span> <span class="attr">http://config-3344.com:3344</span>  <span class="comment">#本微服务启动后先去找3344号服务，通过SpringCloudConfig获取GitHub的服务地址</span></span><br></pre></td></tr></table></figure>

<p>下面是我们的 springboot 的配置文件：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">  application:</span></span><br><span class="line"><span class="attr">    name:</span> <span class="string">microservicecloud-config-client</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>SpringCloud</category>
      </categories>
      <tags>
        <tag>SpringCloud</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringCloud：基础</title>
    <url>/2018/05/25/SpringCloud/SpringCloud%EF%BC%9ASpringCloud%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<h1 id="SpringCloud：基础"><a href="#SpringCloud：基础" class="headerlink" title="SpringCloud：基础"></a>SpringCloud：基础</h1><p>SpringCloud 是微服务架构的一个实现框架，说他是一个框架更不如说他是一个生态，他包含了很多个技术，将这些技术组合起来形成我们的微服务架构应用。</p>
<p><img src="https://user-images.githubusercontent.com/22151420/40642450-b6dc07aa-634f-11e8-80d8-71bed9909e38.png" alt="image"></p>
<h2 id="1-SpringCloud-vs-Dubbo"><a href="#1-SpringCloud-vs-Dubbo" class="headerlink" title="1.SpringCloud vs Dubbo"></a>1.SpringCloud vs Dubbo</h2><p><img src="https://user-images.githubusercontent.com/22151420/40642412-a39596f2-634f-11e8-8c8e-16a42dcf3390.png" alt="image"></p>
<p><strong>最大区别：SpringCloud抛弃了Dubbo的RPC通信，采用的是基于HTTP的REST方式。</strong><br>    严格来说，这两种方式各有优劣。虽然从一定程度上来说，后者牺牲了服务调用的性能，但也避免了上面提到的原生RPC带来的问题。而且REST相比RPC更为灵活，服务提供方和调用方的依赖只依靠一纸契约，不存在代码级别的强依赖，这在强调快速演化的微服务环境下，显得更加合适。<br>    很明显，Spring Cloud的功能比DUBBO更加强大，涵盖面更广，而且作为Spring的拳头项目，它也能够与Spring Framework、Spring Boot、Spring Data、Spring Batch等其他Spring项目完美融合，这些对于微服务而言是至关重要的。使用Dubbo构建的微服务架构就像组装电脑，各环节我们的选择自由度很高，但是最终结果很有可能因为一条内存质量不行就点不亮了，总是让人不怎么放心，但是如果你是一名高手，那这些都不是问题；而Spring Cloud就像品牌机，在Spring Source的整合下，做了大量的兼容性测试，保证了机器拥有更高的稳定性，但是如果要在使用非原装组件外的东西，就需要对其基础有足够的了解。</p>
<h2 id="2-微服务搭建实例"><a href="#2-微服务搭建实例" class="headerlink" title="2.微服务搭建实例"></a>2.微服务搭建实例</h2><p>我们准备了四个模块，然后这四个模块分别就是用来做统一项目管理的 <code>parent</code> 、一个公共的 <code>api</code> 比如通用的实体类 、一个服务提供者和一个服务消费者。</p>
<p>这样我们形成了一个简单的微服务架构的程序了，接下来就是一步一步的搭建这个框架，这里采用的是 Idea 来搭建项目。</p>
<h3 id="1-创建parent工程"><a href="#1-创建parent工程" class="headerlink" title="1.创建parent工程"></a>1.创建parent工程</h3><p>首先我们创建一个Maven项目，注意一点的就是再生成这个项目的时候我们的打包方式要选择 pom 而不是 jar 或者 war，然后这个项目会自动生成一堆的文件夹，其实我们只需要这个 pom 文件，因为只需要做版本控制，而不用写代码的地方。那么我们就删除那些 java 、test 文件夹。</p>
<p><img src="SpringCloud%EF%BC%9ASpringCloud%E5%9F%BA%E7%A1%80/40546274-6edad6e8-6061-11e8-8728-c991d02793e1.png" alt="image"></p>
<p>那么现在我们的 parent 工程就创建好了，接着就是做一些版本依赖的处理了，我们就需要在pom文件中添加如下的内容。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">packaging</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--我们的子模块--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modules</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">module</span>&gt;</span>cloud-api<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">module</span>&gt;</span>provider-dept-8001<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">module</span>&gt;</span>consumer-dept-80<span class="tag">&lt;/<span class="name">module</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">modules</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">project.build.sourceEncoding</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">project.build.sourceEncoding</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">maven.compiler.source</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">maven.compiler.source</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">maven.compiler.target</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">maven.compiler.target</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">junit.version</span>&gt;</span>4.12<span class="tag">&lt;/<span class="name">junit.version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">log4j.version</span>&gt;</span>1.2.17<span class="tag">&lt;/<span class="name">log4j.version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">lombok.version</span>&gt;</span>1.16.18<span class="tag">&lt;/<span class="name">lombok.version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencyManagement</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-dependencies<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">version</span>&gt;</span>Dalston.SR1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">type</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">type</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">scope</span>&gt;</span>import<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-dependencies<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.5.9.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">type</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">type</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">scope</span>&gt;</span>import<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-java<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">version</span>&gt;</span>5.0.4<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>druid<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0.31<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.mybatis.spring.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis-spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.3.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>ch.qos.logback<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>logback-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.2.3<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">version</span>&gt;</span>$&#123;junit.version&#125;<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">scope</span>&gt;</span>test<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>log4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>log4j<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">version</span>&gt;</span>$&#123;log4j.version&#125;<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencyManagement</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">build</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">finalName</span>&gt;</span>microservicecloud<span class="tag">&lt;/<span class="name">finalName</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">resources</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">resource</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">directory</span>&gt;</span>src/main/resources<span class="tag">&lt;/<span class="name">directory</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">filtering</span>&gt;</span>true<span class="tag">&lt;/<span class="name">filtering</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">resource</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">resources</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.maven.plugins<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>maven-resources-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">configuration</span>&gt;</span></span><br><span class="line">                    <span class="tag">&lt;<span class="name">delimiters</span>&gt;</span></span><br><span class="line">                        <span class="tag">&lt;<span class="name">delimit</span>&gt;</span>$<span class="tag">&lt;/<span class="name">delimit</span>&gt;</span></span><br><span class="line">                    <span class="tag">&lt;/<span class="name">delimiters</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;/<span class="name">configuration</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">build</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>注意一点的就是因为我这个pom文件是所有工程都创建好的工程的 pom 所以说这里我的 子模块 是后来加上去的，可以直接删了后面真的在创建子模块的时候会自动生成这些东西。</p>
<h2 id="2-创建公共的api"><a href="#2-创建公共的api" class="headerlink" title="2.创建公共的api"></a>2.创建公共的api</h2><p>接着我们在我们创建好的 <strong>parent 工程上</strong> 右键创建一个新的 <code>module</code> 然后我们项目结构还是选择 <code>Maven</code> 注意一点的就是我们在创建项目的时候可以选择父工程，我们在这里就选择我们创建好的 parent 工程。</p>
<p><img src="SpringCloud%EF%BC%9ASpringCloud%E5%9F%BA%E7%A1%80/40582344-ddc006b0-61a5-11e8-8353-5aa96b5d168a.png" alt="40582344-ddc006b0-61a5-11e8-8353-5aa96b5d168a"></p>
<p><img src="SpringCloud%EF%BC%9ASpringCloud%E5%9F%BA%E7%A1%80.assets/1527404295125.png" alt="1527404295125"></p>
<p>那么生成好的项目就会有这么一段文字：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>然后我们在里面加一些我们常用的依赖以后，我们的pom就会长成这个样子：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>cloud-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span><span class="comment">&lt;!-- 当前Module需要用到的jar包，按自己需求添加，如果父类已经包含了，可以不用写版本号 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.projectlombok<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>lombok<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-feign<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>好的，现在我们的基础配置已经配置好了，就可以在 api 中写代码了。我们这里需要共享的 api 其实就是我们的实体类，实际上还有很多，这里我们就仅仅拿实体类做例子。</p>
<p>创建一个 entries 包 然后创建一个实体类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> entries;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> lombok.AllArgsConstructor;</span><br><span class="line"><span class="keyword">import</span> lombok.Data;</span><br><span class="line"><span class="keyword">import</span> lombok.NoArgsConstructor;</span><br><span class="line"><span class="keyword">import</span> lombok.experimental.Accessors;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.io.Serializable;</span><br><span class="line"></span><br><span class="line"><span class="meta">@NoArgsConstructor</span></span><br><span class="line"><span class="meta">@AllArgsConstructor</span></span><br><span class="line"><span class="meta">@Data</span></span><br><span class="line"><span class="meta">@Accessors</span>(chain = <span class="keyword">true</span>)</span><br><span class="line"><span class="comment">//必须实现序列化接口</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Dept</span> <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Long deptno;</span><br><span class="line">    <span class="keyword">private</span> String dname;</span><br><span class="line">    <span class="keyword">private</span> String db_source;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>由于我们上面添加了 <code>小辣椒</code> 的依赖，这里的实体类非常简单。</p>
<p>这步完成了就应该是这样的一个结构，然后我们就可以开始生产者的创建了。</p>
<h2 id="3-生产者"><a href="#3-生产者" class="headerlink" title="3.生产者"></a>3.生产者</h2><p>生产者里面其实我们应该包含一个完整的 web 层，也就是说我们需要有完整的 Controller 、Service、Dao 然后我们的Controller是用来给后面的消费者调用的，因为我们的SpringCloud是基于Rest请求的嘛。</p>
<p>同理我们创建一个Maven项目，然后导入父工程，接着就是导入依赖：</p>
<h3 id="1-导入依赖"><a href="#1-导入依赖" class="headerlink" title="1.导入依赖"></a>1.导入依赖</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>provider-dept-8001<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 引入自己定义的api通用包，可以使用Dept部门Entity --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>cloud-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>$&#123;project.version&#125;<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-config<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>junit<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-java<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>druid<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>ch.qos.logback<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>logback-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.mybatis.spring.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis-spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-jetty<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-test<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 修改后立即生效，热部署 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>springloaded<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-devtools<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-配置springboot配置文件"><a href="#2-配置springboot配置文件" class="headerlink" title="2.配置springboot配置文件"></a>2.配置springboot配置文件</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line"><span class="attr">  port:</span> <span class="number">8001</span></span><br><span class="line"></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">   application:</span></span><br><span class="line"><span class="attr">    name:</span> <span class="string">cloud-dept</span></span><br><span class="line"><span class="attr">   datasource:</span></span><br><span class="line"><span class="attr">    type:</span> <span class="string">com.alibaba.druid.pool.DruidDataSource</span>            <span class="comment"># 当前数据源操作类型</span></span><br><span class="line"><span class="attr">    driver-class-name:</span> <span class="string">org.gjt.mm.mysql.Driver</span>              <span class="comment"># mysql驱动包</span></span><br><span class="line"><span class="attr">    url:</span> <span class="attr">jdbc:mysql://localhost:3306/cloudDB01</span>              <span class="comment"># 数据库名称</span></span><br><span class="line"><span class="attr">    username:</span> <span class="string">root</span></span><br><span class="line"><span class="attr">    password:</span></span><br><span class="line"><span class="attr">    dbcp2:</span></span><br><span class="line"><span class="attr">      min-idle:</span> <span class="number">5</span>                                           <span class="comment"># 数据库连接池的最小维持连接数</span></span><br><span class="line"><span class="attr">      initial-size:</span> <span class="number">5</span>                                       <span class="comment"># 初始化连接数</span></span><br><span class="line"><span class="attr">      max-total:</span> <span class="number">5</span>                                          <span class="comment"># 最大连接数</span></span><br><span class="line"><span class="attr">      max-wait-millis:</span> <span class="number">200</span>                                  <span class="comment"># 等待连接获取的最大超时时间</span></span><br><span class="line">      </span><br><span class="line"><span class="attr"> mybatis:</span></span><br><span class="line"><span class="attr">  configuration:</span></span><br><span class="line"><span class="attr">    map-underscore-to-camel-case:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>

<h3 id="3-dao"><a href="#3-dao" class="headerlink" title="3.dao"></a>3.dao</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lwen.dao;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> lwen.entries.Dept;</span><br><span class="line"><span class="keyword">import</span> org.apache.ibatis.annotations.Insert;</span><br><span class="line"><span class="keyword">import</span> org.apache.ibatis.annotations.Select;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">DeptDao</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Select</span>(<span class="string">"select * from clouddb01.dept"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;Dept&gt; <span class="title">findAll</span><span class="params">()</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Insert</span>(<span class="string">"insert into clouddb01.dept(name, db_name) values (#&#123;name&#125;,database())"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">insertDept</span><span class="params">(Dept dept)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-Service"><a href="#4-Service" class="headerlink" title="4.Service"></a>4.Service</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lwen.service;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> lwen.dao.DeptDao;</span><br><span class="line"><span class="keyword">import</span> lwen.entries.Dept;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeptService</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    DeptDao deptDao;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;Dept&gt; <span class="title">findAll</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> deptDao.findAll();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Boolean <span class="title">insertDept</span><span class="params">(Dept dept)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> deptDao.insertDept(dept);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-controller"><a href="#5-controller" class="headerlink" title="5.controller"></a>5.controller</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.lwen.controller;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> entries.Dept;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.GetMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RestController;</span><br><span class="line"><span class="keyword">import</span> com.lwen.service.DeptService;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeptController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    DeptService deptService;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/dep"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;Dept&gt; <span class="title">list</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> deptService.list();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/dep/add"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">insert</span><span class="params">(Dept dept)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> deptService.add(dept);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="4-消费者"><a href="#4-消费者" class="headerlink" title="4.消费者"></a>4.消费者</h2><p>同上创建一个module，完成消费者的编写。</p>
<p>消费者其实只有一个Controller，这是因为我们的真正的业务逻辑都被封装到我们的微服务中去了，所以说不存在后面的Service以及Dao。</p>
<h3 id="1-导入依赖-1"><a href="#1-导入依赖-1" class="headerlink" title="1.导入依赖"></a>1.导入依赖</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>consumer-dept-80<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span><span class="comment">&lt;!-- 自己定义的api --&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>cloud-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>$&#123;project.version&#125;<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- Ribbon相关 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-eureka<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-ribbon<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.cloud<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-cloud-starter-config<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 修改后立即生效，热部署 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>springloaded<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-devtools<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-配置文件"><a href="#2-配置文件" class="headerlink" title="2.配置文件"></a>2.配置文件</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line"><span class="attr">  port:</span> <span class="number">81</span></span><br></pre></td></tr></table></figure>

<h3 id="3-config"><a href="#3-config" class="headerlink" title="3.config"></a>3.config</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.lwen.config;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.client.RestTemplate;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BeanConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> RestTemplate <span class="title">restTemplate</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> RestTemplate();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个地方我使用了 JavaConfig 配置了一个 RestTemplate 的Bean，然后这个东西其实就是和我们以前接触的 redisTemplate 以及 jdbc的一样，就是一个工具，用来简化操作的。这里就是简化我们的 rest 请求操作，相当于一个 Http 的客户端。</p>
<h3 id="4-Controller"><a href="#4-Controller" class="headerlink" title="4.Controller"></a>4.Controller</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.lwen.controller;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> entries.Dept;</span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.GetMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RestController;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.client.RestTemplate;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DeptController</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String PREFIX_URL = <span class="string">"http://localhost:8001/"</span>;</span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    RestTemplate restTemplate;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/list"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List <span class="title">list</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> restTemplate.getForObject(PREFIX_URL + <span class="string">"dep"</span>, List.class);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/dep/add"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">add</span><span class="params">(Dept dept)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> restTemplate.getForObject(PREFIX_URL + <span class="string">"dep/add"</span>,Integer.class);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>好，现在我们的项目搭建完毕，目前的目录架构应该是这样的：</p>
<p><img src="https://user-images.githubusercontent.com/22151420/40571728-b1576ab2-60d0-11e8-89c3-ca95fe0487bb.png" alt="image"></p>
]]></content>
      <categories>
        <category>SpringCloud</category>
      </categories>
      <tags>
        <tag>SpringCloud</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringCloud：微服务</title>
    <url>/2018/05/25/SpringCloud/SpringCloud%EF%BC%9A%E5%BE%AE%E6%9C%8D%E5%8A%A1/</url>
    <content><![CDATA[<h2 id="1-微服务"><a href="#1-微服务" class="headerlink" title="1.微服务"></a>1.微服务</h2><p>微服务其实就是我们的以前的整个应用拆分的一个个小的应用服务，也就是我们的一个个模块。每一个微服务就是一个进程，运行在一个独立的进程之上，然后通过网络进行通讯互联。</p>
<h2 id="2-微服务架构"><a href="#2-微服务架构" class="headerlink" title="2.微服务架构"></a>2.微服务架构</h2><p>微服务架构是一种架构模式，他是把传统的单体应用(All in one)，拆分成多个项目独立的微服务，然后每一个为服务都是一个独立进程，拥有自己的独立的数据库。然后各个微服务的整合形成整个的微服务架构。</p>
<h2 id="3-微服务的优缺点"><a href="#3-微服务的优缺点" class="headerlink" title="3.微服务的优缺点"></a>3.微服务的优缺点</h2><h3 id="1-优点："><a href="#1-优点：" class="headerlink" title="1.优点："></a>1.优点：</h3><ul>
<li>每一个服务就是一个聚焦一个功能</li>
<li>单个服务开发简单</li>
<li>服务解耦</li>
<li>小而精</li>
</ul>
<h3 id="2-缺点："><a href="#2-缺点：" class="headerlink" title="2.缺点："></a>2.缺点：</h3><ul>
<li>部署困难，部署依赖</li>
<li>排查困难</li>
<li>服务通讯的成本</li>
<li>数据的一致性</li>
<li>集成测试</li>
<li>性能监控</li>
</ul>
<h2 id="4-微服务技术栈"><a href="#4-微服务技术栈" class="headerlink" title="4.微服务技术栈"></a>4.微服务技术栈</h2><table>
<thead>
<tr>
<th>微服务条目</th>
<th>技术</th>
</tr>
</thead>
<tbody><tr>
<td>服务开发</td>
<td>Springboot、Spring、SpringMVC</td>
</tr>
<tr>
<td>服务配置与管理</td>
<td>Netflix公司的Archaius、阿里的Diamond等</td>
</tr>
<tr>
<td>服务注册与发现</td>
<td>Eureka、Consul、Zookeeper等</td>
</tr>
<tr>
<td>服务调用</td>
<td>Rest、RPC、gRPC</td>
</tr>
<tr>
<td>服务熔断器</td>
<td>Hystrix、Envoy等</td>
</tr>
<tr>
<td>负载均衡</td>
<td>Ribbon、Nginx等</td>
</tr>
<tr>
<td>服务接口调用（客户端调用服务的简化工具）</td>
<td>Feign等</td>
</tr>
<tr>
<td>消息队列</td>
<td>Kafka、RabbitMQ、ActiveMQ等</td>
</tr>
<tr>
<td>服务配置中心管理</td>
<td>SpringCloudConfig、Che等</td>
</tr>
<tr>
<td>服务路由（API网关)</td>
<td>Zuul</td>
</tr>
<tr>
<td>服务监控</td>
<td>Zabbix、Nagios、Metrics、Spectator等</td>
</tr>
<tr>
<td>全链路追踪</td>
<td>Zipkin,Brave、Dapper等</td>
</tr>
<tr>
<td>服务部署</td>
<td>Docker、OpenStack、Kubernetes等</td>
</tr>
<tr>
<td>数据流操作开发包</td>
<td>SpringCloud Stream（封装与Redis,Rabbit、Kafka等发送接收消息）</td>
</tr>
<tr>
<td>事件消息总线</td>
<td>Spring Cloud Bus</td>
</tr>
</tbody></table>
<h2 id="5-SpringCloud"><a href="#5-SpringCloud" class="headerlink" title="5.SpringCloud"></a>5.SpringCloud</h2><p>这里我们的SpringCloud就是一个微服务架构的框架的具体实现了。同样的还有很多其他的微服务架构，比如现在非常出名的Dubbo 以及京东的 JSF 新浪的 Motai</p>
]]></content>
      <categories>
        <category>SpringCloud</category>
      </categories>
      <tags>
        <tag>SpringCloud</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记(十)：错误处理</title>
    <url>/2018/05/24/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E5%8D%81%20)%EF%BC%9A%E9%94%99%E8%AF%AF%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-十-：错误处理"><a href="#SpringBoot-笔记-十-：错误处理" class="headerlink" title="SpringBoot 笔记 ( 十 )：错误处理"></a>SpringBoot 笔记 ( 十 )：错误处理</h1><h2 id="1）、SpringBoot默认的错误处理机制"><a href="#1）、SpringBoot默认的错误处理机制" class="headerlink" title="1）、SpringBoot默认的错误处理机制"></a>1）、SpringBoot默认的错误处理机制</h2><p>默认效果：</p>
<p>​        1）、浏览器，返回一个默认的错误页面<br>        2）、如果是其他客户端，默认响应一个json数据</p>
<a id="more"></a>

<h2 id="2）、自动配置原理"><a href="#2）、自动配置原理" class="headerlink" title="2）、自动配置原理"></a>2）、自动配置原理</h2><p>​    具体就是在    <code>ErrorMvcAutoConfiguration</code>，错误处理的自动配置。</p>
<pre><code>给容器中添加了以下组件</code></pre><h3 id="1、ErrorPageCustomizer：规定错误页面-error"><a href="#1、ErrorPageCustomizer：规定错误页面-error" class="headerlink" title="1、ErrorPageCustomizer：规定错误页面  /error"></a>1、ErrorPageCustomizer：规定错误页面  /error</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Value</span>(<span class="string">"$&#123;error.path:/error&#125;"</span>)</span><br><span class="line"><span class="keyword">private</span> String path = <span class="string">"/error"</span>;  <span class="comment">//系统出现错误以后来到error请求进行处理,（类似与我们在web.xml注册的错误页面规则）</span></span><br></pre></td></tr></table></figure>

<h3 id="2、BasicErrorController：处理默认-error-请求"><a href="#2、BasicErrorController：处理默认-error-请求" class="headerlink" title="2、BasicErrorController：处理默认 /error 请求"></a>2、BasicErrorController：处理默认 /error 请求</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"$&#123;server.error.path:$&#123;error.path:/error&#125;&#125;"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BasicErrorController</span> <span class="keyword">extends</span> <span class="title">AbstractErrorController</span> </span>&#123;</span><br><span class="line">    <span class="comment">//处理产生html类型的数据；浏览器发送的请求来到这个方法处理</span></span><br><span class="line">    <span class="meta">@RequestMapping</span>(produces = <span class="string">"text/html"</span>)</span><br><span class="line">	<span class="function"><span class="keyword">public</span> ModelAndView <span class="title">errorHtml</span><span class="params">(HttpServletRequest request,</span></span></span><br><span class="line"><span class="function"><span class="params">			HttpServletResponse response)</span> </span>&#123;</span><br><span class="line">		HttpStatus status = getStatus(request);</span><br><span class="line">		Map&lt;String, Object&gt; model = Collections.unmodifiableMap(getErrorAttributes(</span><br><span class="line">				request, isIncludeStackTrace(request, MediaType.TEXT_HTML)));</span><br><span class="line">		response.setStatus(status.value());</span><br><span class="line">        <span class="comment">//去哪个页面作为错误页面；包含页面地址和页面内容</span></span><br><span class="line">		ModelAndView modelAndView = resolveErrorView(request, response, status, model);</span><br><span class="line">		<span class="keyword">return</span> (modelAndView == <span class="keyword">null</span> ? <span class="keyword">new</span> ModelAndView(<span class="string">"error"</span>, model) : modelAndView);</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//处理非Html的类型的数据。产生json数据，其他客户端来到这个方法处理；</span></span><br><span class="line">	<span class="meta">@RequestMapping</span></span><br><span class="line">	<span class="meta">@ResponseBody</span>    </span><br><span class="line">	<span class="keyword">public</span> ResponseEntity&lt;Map&lt;String, Object&gt;&gt; error(HttpServletRequest request) &#123;</span><br><span class="line">		Map&lt;String, Object&gt; body = getErrorAttributes(request,</span><br><span class="line">				isIncludeStackTrace(request, MediaType.ALL));</span><br><span class="line">		HttpStatus status = getStatus(request);</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> ResponseEntity&lt;Map&lt;String, Object&gt;&gt;(body, status);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>其实这里就解释了为什么我们不同的客户端会返回的数据不一样，我们采用浏览器和程序请求会不同，就是因为他们的header不一样导致的。</p>
<p>但是注意上面的<code>ModelAndView</code>对象我们是通过 <code>resolveErrorView</code> 来获取的，也就是下面的逻辑：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> ModelAndView <span class="title">resolveErrorView</span><span class="params">(HttpServletRequest request,</span></span></span><br><span class="line"><span class="function"><span class="params">      HttpServletResponse response, HttpStatus status, Map&lt;String, Object&gt; model)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 所有的 ErrorViewResolver 得到 ModelAndView</span></span><br><span class="line">   <span class="keyword">for</span> (ErrorViewResolver resolver : <span class="keyword">this</span>.errorViewResolvers) &#123;</span><br><span class="line">      ModelAndView modelAndView = resolver.resolveErrorView(request, status, model);</span><br><span class="line">      <span class="keyword">if</span> (modelAndView != <span class="keyword">null</span>) &#123;</span><br><span class="line">         <span class="keyword">return</span> modelAndView;</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后上面在解析错误视图，调用了 resolver 的方法，这个方法其实是 <code>DefaultErrorViewResolver</code> 的方法。也就是下面的这个类的方法。</p>
<h3 id="3、DefaultErrorViewResolver："><a href="#3、DefaultErrorViewResolver：" class="headerlink" title="3、DefaultErrorViewResolver："></a>3、DefaultErrorViewResolver：</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> ModelAndView <span class="title">resolveErrorView</span><span class="params">(HttpServletRequest request, HttpStatus status,</span></span></span><br><span class="line"><span class="function"><span class="params">			Map&lt;String, Object&gt; model)</span> </span>&#123;</span><br><span class="line">		ModelAndView modelAndView = resolve(String.valueOf(status), model);</span><br><span class="line">		<span class="keyword">if</span> (modelAndView == <span class="keyword">null</span> &amp;&amp; SERIES_VIEWS.containsKey(status.series())) &#123;</span><br><span class="line">			modelAndView = resolve(SERIES_VIEWS.get(status.series()), model);</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">return</span> modelAndView;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="function"><span class="keyword">private</span> ModelAndView <span class="title">resolve</span><span class="params">(String viewName, Map&lt;String, Object&gt; model)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//默认SpringBoot可以去找到一个页面？  error/404</span></span><br><span class="line">		String errorViewName = <span class="string">"error/"</span> + viewName;</span><br><span class="line">        </span><br><span class="line">        <span class="comment">//模板引擎可以解析这个页面地址就用模板引擎解析</span></span><br><span class="line">		TemplateAvailabilityProvider provider = <span class="keyword">this</span>.templateAvailabilityProviders</span><br><span class="line">				.getProvider(errorViewName, <span class="keyword">this</span>.applicationContext);</span><br><span class="line">		<span class="keyword">if</span> (provider != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">//模板引擎可用的情况下返回到errorViewName指定的视图地址</span></span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> ModelAndView(errorViewName, model);</span><br><span class="line">		&#125;</span><br><span class="line">        <span class="comment">//模板引擎不可用，就在静态资源文件夹下找errorViewName对应的页面   error/404.html</span></span><br><span class="line">		<span class="keyword">return</span> resolveResource(errorViewName, model);</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>这里主要做的事情就是，根据状态码得到模板的页面地址，然后让我们的模板引擎解析一下，如果能解析到就是返回对应的ModelAndView 否则的话就直接去静态资源的static目录找对应的状态码对应的页面404.html 4xx.html …。然后这里的model也就是我们的异常错误数据，是通过下面的组件完成的。</p>
<h3 id="4、DefaultErrorAttributes"><a href="#4、DefaultErrorAttributes" class="headerlink" title="4、DefaultErrorAttributes"></a>4、DefaultErrorAttributes</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> Map&lt;String, Object&gt; <span class="title">getErrorAttributes</span><span class="params">(RequestAttributes requestAttributes,</span></span></span><br><span class="line"><span class="function"><span class="params">			<span class="keyword">boolean</span> includeStackTrace)</span> </span>&#123;</span><br><span class="line">		Map&lt;String, Object&gt; errorAttributes = <span class="keyword">new</span> LinkedHashMap&lt;String, Object&gt;();</span><br><span class="line">		errorAttributes.put(<span class="string">"timestamp"</span>, <span class="keyword">new</span> Date());</span><br><span class="line">		addStatus(errorAttributes, requestAttributes);</span><br><span class="line">		addErrorDetails(errorAttributes, requestAttributes, includeStackTrace);</span><br><span class="line">		addPath(errorAttributes, requestAttributes);</span><br><span class="line">		<span class="keyword">return</span> errorAttributes;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这个方法就是在容器里注册了一个 Map 帮我们在页面共享信息。</p>
<p>​    步骤：</p>
<ol>
<li><p>一但系统出现4xx或者5xx之类的错误，ErrorPageCustomizer就会生效（定制错误的响应规则），来到/error请求</p>
</li>
<li><p>就会被<strong>BasicErrorController</strong>处理</p>
</li>
<li><p>响应页面：去哪个页面，错误信息是由<strong>DefaultErrorViewResolver</strong>解析得到的，错误信息由 <code>getErrorAttributes</code> 获取。</p>
</li>
</ol>
<h2 id="3）、定制错误响应"><a href="#3）、定制错误响应" class="headerlink" title="3）、定制错误响应"></a>3）、定制错误响应</h2><h3 id="1）、定制错误的-Html-页面"><a href="#1）、定制错误的-Html-页面" class="headerlink" title="1）、定制错误的 Html 页面"></a>1）、定制错误的 Html 页面</h3><h4 id="1）、有模板引擎"><a href="#1）、有模板引擎" class="headerlink" title="1）、有模板引擎"></a>1）、有模板引擎</h4><p>error/状态码【将错误页面命名为  错误状态码.html 放在模板引擎文件夹里面的 error文件夹下】，发生此状态码的错误就会来到  对应的页面；</p>
<p>我们可以使用4xx和5xx作为错误页面的文件名来匹配这种类型的所有错误，精确优先（优先寻找精确的状态码.html）。</p>
<p>页面能获取的信息，是由Model来确定的，也就是我们的 <code>getErrorAttributes</code> 方法来确定的。具体的有：</p>
<ul>
<li>timestamp：时间戳</li>
<li>status：状态码</li>
<li>error：错误提示</li>
<li>exception：异常对象</li>
<li>message：异常消息</li>
<li>errors：JSR303数据校验的错误都在这里</li>
</ul>
<h4 id="2）、没有模板引擎"><a href="#2）、没有模板引擎" class="headerlink" title="2）、没有模板引擎"></a>2）、没有模板引擎</h4><p>没有模板引擎或者说模板引擎找不到这个错误页面，那就去静态资源文件夹static下找，规则同模板引擎的规则。</p>
<h4 id="3）、没有任何错误页面"><a href="#3）、没有任何错误页面" class="headerlink" title="3）、没有任何错误页面"></a>3）、没有任何错误页面</h4><p>就是默认来到SpringBoot默认的错误提示页面</p>
<h3 id="2）、如何定制错误的json数据；"><a href="#2）、如何定制错误的json数据；" class="headerlink" title="2）、如何定制错误的json数据；"></a>2）、如何定制错误的json数据；</h3><h4 id="1）、自定义异常处理-amp-返回定制json数据"><a href="#1）、自定义异常处理-amp-返回定制json数据" class="headerlink" title="1）、自定义异常处理&amp;返回定制json数据"></a>1）、自定义异常处理&amp;返回定制json数据</h4><p>这里其实就是用了 Spring 的统一异常处理，但是这样的话我们就是对所有的页面都会出现json了，没有针对不同的客户端有自适应效果。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ControllerAdvice</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyExceptionHandler</span> </span>&#123;</span><br><span class="line">    <span class="meta">@ResponseBody</span></span><br><span class="line">    <span class="meta">@ExceptionHandler</span>(UserNotExistException.class)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Map&lt;String,Object&gt; <span class="title">handleException</span><span class="params">(Exception e)</span></span>&#123;</span><br><span class="line">        Map&lt;String,Object&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        map.put(<span class="string">"code"</span>,<span class="string">"user.notexist"</span>);</span><br><span class="line">        map.put(<span class="string">"message"</span>,e.getMessage());</span><br><span class="line">        <span class="keyword">return</span> map;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2）、转发到-error进行自适应响应效果处理"><a href="#2）、转发到-error进行自适应响应效果处理" class="headerlink" title="2）、转发到/error进行自适应响应效果处理"></a>2）、转发到/error进行自适应响应效果处理</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ExceptionHandler</span>(UserNotExistException.class)</span><br><span class="line">  <span class="function"><span class="keyword">public</span> String <span class="title">handleException</span><span class="params">(Exception e, HttpServletRequest request)</span></span>&#123;</span><br><span class="line">      Map&lt;String,Object&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">      <span class="comment">//传入我们自己的错误状态码  4xx 5xx，否则就不会进入定制错误页面的解析流程</span></span><br><span class="line">      <span class="comment">/**</span></span><br><span class="line"><span class="comment">       * Integer statusCode = (Integer) request</span></span><br><span class="line"><span class="comment">       .getAttribute("javax.servlet.error.status_code");</span></span><br><span class="line"><span class="comment">       */</span></span><br><span class="line">      request.setAttribute(<span class="string">"javax.servlet.error.status_code"</span>,<span class="number">500</span>);</span><br><span class="line">      map.put(<span class="string">"code"</span>,<span class="string">"user.notexist"</span>);</span><br><span class="line">      map.put(<span class="string">"message"</span>,e.getMessage());</span><br><span class="line">      <span class="comment">//转发到 /error </span></span><br><span class="line">      <span class="keyword">return</span> <span class="string">"forward:/error"</span>;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>这里就是通过我们自动配置的默认错误页面的控制器来处理错误页面的请求，让我们吧一些特殊的错误数据发送过去，然后直接转发到我们的错误页面即可，接下来就是SpringBoot帮助我们处理自适应问题了。</p>
<p>又一点要注意，就是我们这里自己设置了状态码，为什么需要这样，这是由于我们这里拦截到了错误，然后我们并没有走默认的错误处理的逻辑也就是我们的默认的错误处理的Controller没有执行，导致一些错误的状态码没有设置，但是我们最终需要渲染视图，以及寻找错误页面都是通过我们的的错误状态码的，这里找不到状态码，我们必须手动的添加上才行。否则不是去往我们的错误页面。</p>
<p>还有一点需要注意的就是我们虽然已经设置了一个map，但是这个map并没有被我们的模板获取到，因为根本没用我们的数据，这里我们需要 定制一下 <code>getErrorAttributes（）</code> 方法才行。</p>
<h4 id="3）、将我们的定制数据携带出去；"><a href="#3）、将我们的定制数据携带出去；" class="headerlink" title="3）、将我们的定制数据携带出去；"></a>3）、将我们的定制数据携带出去；</h4><p>出现错误以后，会来到/error请求，会被BasicErrorController处理，响应出去可以获取的数据是由getErrorAttributes得到的（是AbstractErrorController（ErrorController）规定的方法）；</p>
<p>我们有两种方式来自定义数据：</p>
<p>​    1、完全来编写一个ErrorController的实现类【或者是编写AbstractErrorController的子类】，放在容器中；</p>
<p>​    2、页面上能用的数据，或者是json返回能用的数据都是通过errorAttributes.getErrorAttributes得到，容器中DefaultErrorAttributes.getErrorAttributes()，默认进行数据处理的；</p>
<p>自定义ErrorAttributes</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//给容器中加入我们自己定义的ErrorAttributes</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyErrorAttributes</span> <span class="keyword">extends</span> <span class="title">DefaultErrorAttributes</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Map&lt;String, Object&gt; <span class="title">getErrorAttributes</span><span class="params">(RequestAttributes requestAttributes, <span class="keyword">boolean</span> includeStackTrace)</span> </span>&#123;</span><br><span class="line">        Map&lt;String, Object&gt; map = <span class="keyword">super</span>.getErrorAttributes(requestAttributes, includeStackTrace);</span><br><span class="line">        map.put(<span class="string">"company"</span>,<span class="string">"atguigu"</span>);</span><br><span class="line">        <span class="keyword">return</span> map;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最终的效果：响应是自适应的，可以通过定制ErrorAttributes改变需要返回的内容，</p>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记(十一):Servlet容器</title>
    <url>/2018/05/24/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E5%8D%81%E4%B8%80%20)Servlet%E5%AE%B9%E5%99%A8/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-十一-Servlet容器"><a href="#SpringBoot-笔记-十一-Servlet容器" class="headerlink" title="SpringBoot 笔记 ( 十一 ):Servlet容器"></a>SpringBoot 笔记 ( 十一 ):Servlet容器</h1><p>SpringBoot默认使用Tomcat作为嵌入式的Servlet容器</p>
<h2 id="1）、定制和修改Servlet容器的相关配置"><a href="#1）、定制和修改Servlet容器的相关配置" class="headerlink" title="1）、定制和修改Servlet容器的相关配置"></a>1）、定制和修改Servlet容器的相关配置</h2><h3 id="1、修改配置文件中的和-server-有关的配置"><a href="#1、修改配置文件中的和-server-有关的配置" class="headerlink" title="1、修改配置文件中的和 server 有关的配置"></a>1、修改配置文件中的和 server 有关的配置</h3><p>ServerProperties【也是EmbeddedServletContainerCustomizer】</p>
<a id="more"></a>

<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">server.port</span>=<span class="string">8081</span></span><br><span class="line"><span class="meta">server.context-path</span>=<span class="string">/crud</span></span><br><span class="line"><span class="meta">server.tomcat.uri-encoding</span>=<span class="string">UTF-8</span></span><br><span class="line"><span class="attr">//通用的Servlet容器设置</span></span><br><span class="line"><span class="attr">server.xxx</span></span><br><span class="line"><span class="attr">//Tomcat的设置</span></span><br><span class="line"><span class="attr">server.tomcat.xxx</span></span><br></pre></td></tr></table></figure>

<h3 id="2、编写一个-EmbeddedServletContainerCustomizer"><a href="#2、编写一个-EmbeddedServletContainerCustomizer" class="headerlink" title="2、编写一个 EmbeddedServletContainerCustomizer"></a>2、编写一个 EmbeddedServletContainerCustomizer</h3><p>嵌入式的Servlet容器的定制器，来修改Servlet容器的配置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span>  <span class="comment">//将这个定制器加入到容器中</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> EmbeddedServletContainerCustomizer <span class="title">embeddedServletContainerCustomizer</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> EmbeddedServletContainerCustomizer() &#123;</span><br><span class="line">        <span class="comment">//定制嵌入式的Servlet容器相关的规则</span></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">customize</span><span class="params">(ConfigurableEmbeddedServletContainer container)</span> </span>&#123;</span><br><span class="line">            container.setPort(<span class="number">8083</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2）、注册Servlet三大组件【Servlet、Filter、Listener】"><a href="#2）、注册Servlet三大组件【Servlet、Filter、Listener】" class="headerlink" title="2）、注册Servlet三大组件【Servlet、Filter、Listener】"></a>2）、注册Servlet三大组件【Servlet、Filter、Listener】</h2><p>由于SpringBoot默认是以jar包的方式启动嵌入式的Servlet容器来启动SpringBoot的web应用，没有web.xml文件。</p>
<p>注册三大组件用以下方式：</p>
<h3 id="1-ServletRegistrationBean"><a href="#1-ServletRegistrationBean" class="headerlink" title="1.ServletRegistrationBean"></a>1.ServletRegistrationBean</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//注册三大组件</span></span><br><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> ServletRegistrationBean <span class="title">myServlet</span><span class="params">()</span></span>&#123;</span><br><span class="line">    ServletRegistrationBean registrationBean = <span class="keyword">new</span> ServletRegistrationBean(<span class="keyword">new</span> MyServlet(),<span class="string">"/myServlet"</span>);</span><br><span class="line">    <span class="keyword">return</span> registrationBean;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-FilterRegistrationBean"><a href="#2-FilterRegistrationBean" class="headerlink" title="2.FilterRegistrationBean"></a>2.FilterRegistrationBean</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> FilterRegistrationBean <span class="title">myFilter</span><span class="params">()</span></span>&#123;</span><br><span class="line">    FilterRegistrationBean registrationBean = <span class="keyword">new</span> FilterRegistrationBean();</span><br><span class="line">    registrationBean.setFilter(<span class="keyword">new</span> MyFilter());</span><br><span class="line">    registrationBean.setUrlPatterns(Arrays.asList(<span class="string">"/hello"</span>,<span class="string">"/myServlet"</span>));</span><br><span class="line">    <span class="keyword">return</span> registrationBean;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-ServletListenerRegistrationBean"><a href="#3-ServletListenerRegistrationBean" class="headerlink" title="3.ServletListenerRegistrationBean"></a>3.ServletListenerRegistrationBean</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> ServletListenerRegistrationBean <span class="title">myListener</span><span class="params">()</span></span>&#123;</span><br><span class="line">    ServletListenerRegistrationBean&lt;MyListener&gt; registrationBean = <span class="keyword">new</span> ServletListenerRegistrationBean&lt;&gt;(<span class="keyword">new</span> MyListener());</span><br><span class="line">    <span class="keyword">return</span> registrationBean;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4、DIspatcherServlet-配置"><a href="#4、DIspatcherServlet-配置" class="headerlink" title="4、DIspatcherServlet 配置"></a>4、DIspatcherServlet 配置</h3><p>SpringBoot帮我们自动SpringMVC的时候，自动的注册SpringMVC的前端控制器，DIspatcherServlet</p>
<p>DispatcherServletAutoConfiguration中：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span>(name = DEFAULT_DISPATCHER_SERVLET_REGISTRATION_BEAN_NAME)</span><br><span class="line"><span class="meta">@ConditionalOnBean</span>(value = DispatcherServlet.class, name = DEFAULT_DISPATCHER_SERVLET_BEAN_NAME)</span><br><span class="line"><span class="function"><span class="keyword">public</span> ServletRegistrationBean <span class="title">dispatcherServletRegistration</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">      DispatcherServlet dispatcherServlet)</span> </span>&#123;</span><br><span class="line">   ServletRegistrationBean registration = <span class="keyword">new</span> ServletRegistrationBean(</span><br><span class="line">         dispatcherServlet, <span class="keyword">this</span>.serverProperties.getServletMapping());</span><br><span class="line">    <span class="comment">//默认拦截： /  所有请求，包静态资源，但是不拦截jsp请求；   /*会拦截jsp</span></span><br><span class="line">    <span class="comment">//可以通过server.servletPath来修改SpringMVC前端控制器默认拦截的请求路径</span></span><br><span class="line">    </span><br><span class="line">   registration.setName(DEFAULT_DISPATCHER_SERVLET_BEAN_NAME);</span><br><span class="line">   registration.setLoadOnStartup(</span><br><span class="line">         <span class="keyword">this</span>.webMvcProperties.getServlet().getLoadOnStartup());</span><br><span class="line">   <span class="keyword">if</span> (<span class="keyword">this</span>.multipartConfig != <span class="keyword">null</span>) &#123;</span><br><span class="line">      registration.setMultipartConfig(<span class="keyword">this</span>.multipartConfig);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> registration;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3）、替换为其他嵌入式Servlet容器"><a href="#3）、替换为其他嵌入式Servlet容器" class="headerlink" title="3）、替换为其他嵌入式Servlet容器"></a>3）、替换为其他嵌入式Servlet容器</h2><p>默认支持：</p>
<h3 id="1-Tomcat（默认使用）"><a href="#1-Tomcat（默认使用）" class="headerlink" title="1.Tomcat（默认使用）"></a>1.Tomcat（默认使用）</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   引入web模块默认就是使用嵌入式的Tomcat作为Servlet容器；</span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-Jetty（长链接类的服务）"><a href="#2-Jetty（长链接类的服务）" class="headerlink" title="2.Jetty（长链接类的服务）"></a>2.Jetty（长链接类的服务）</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 引入web模块 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-tomcat<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!--引入其他的Servlet容器--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-jetty<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="3-Undertow（高并发的不支持JSP）"><a href="#3-Undertow（高并发的不支持JSP）" class="headerlink" title="3.Undertow（高并发的不支持JSP）"></a>3.Undertow（高并发的不支持JSP）</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 引入web模块 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-tomcat<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!--引入其他的Servlet容器--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-undertow<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="4）、嵌入式Servlet容器自动配置原理"><a href="#4）、嵌入式Servlet容器自动配置原理" class="headerlink" title="4）、嵌入式Servlet容器自动配置原理"></a>4）、嵌入式Servlet容器自动配置原理</h2><p>EmbeddedServletContainerAutoConfiguration：嵌入式的Servlet容器自动配置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@AutoConfigureOrder</span>(Ordered.HIGHEST_PRECEDENCE)</span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ConditionalOnWebApplication</span></span><br><span class="line"><span class="meta">@Import</span>(BeanPostProcessorsRegistrar.class)</span><br><span class="line"><span class="comment">//导入BeanPostProcessorsRegistrar：Spring注解版；给容器中导入一些组件</span></span><br><span class="line"><span class="comment">//导入了EmbeddedServletContainerCustomizerBeanPostProcessor：</span></span><br><span class="line"><span class="comment">//后置处理器：bean初始化前后（创建完对象，还没赋值赋值）执行初始化工作</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EmbeddedServletContainerAutoConfiguration</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Configuration</span></span><br><span class="line">	<span class="meta">@ConditionalOnClass</span>(&#123; Servlet.class, Tomcat.class &#125;)<span class="comment">//判断当前是否引入了Tomcat依赖；</span></span><br><span class="line">	<span class="meta">@ConditionalOnMissingBean</span>(value = EmbeddedServletContainerFactory.class, search = SearchStrategy.CURRENT)<span class="comment">//判断当前容器没有用户自己定义EmbeddedServletContainerFactory：嵌入式的Servlet容器工厂；作用：创建嵌入式的Servlet容器</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">EmbeddedTomcat</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Bean</span></span><br><span class="line">		<span class="function"><span class="keyword">public</span> TomcatEmbeddedServletContainerFactory <span class="title">tomcatEmbeddedServletContainerFactory</span><span class="params">()</span> </span>&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> TomcatEmbeddedServletContainerFactory();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Nested configuration if Jetty is being used.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@Configuration</span></span><br><span class="line">	<span class="meta">@ConditionalOnClass</span>(&#123; Servlet.class, Server.class, Loader.class,</span><br><span class="line">			WebAppContext.class &#125;)</span><br><span class="line">	<span class="meta">@ConditionalOnMissingBean</span>(value = EmbeddedServletContainerFactory.class, search = SearchStrategy.CURRENT)</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">EmbeddedJetty</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Bean</span></span><br><span class="line">		<span class="function"><span class="keyword">public</span> JettyEmbeddedServletContainerFactory <span class="title">jettyEmbeddedServletContainerFactory</span><span class="params">()</span> </span>&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> JettyEmbeddedServletContainerFactory();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Nested configuration if Undertow is being used.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@Configuration</span></span><br><span class="line">	<span class="meta">@ConditionalOnClass</span>(&#123; Servlet.class, Undertow.class, SslClientAuthMode.class &#125;)</span><br><span class="line">	<span class="meta">@ConditionalOnMissingBean</span>(value = EmbeddedServletContainerFactory.class, search = SearchStrategy.CURRENT)</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">EmbeddedUndertow</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Bean</span></span><br><span class="line">		<span class="function"><span class="keyword">public</span> UndertowEmbeddedServletContainerFactory <span class="title">undertowEmbeddedServletContainerFactory</span><span class="params">()</span> </span>&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> UndertowEmbeddedServletContainerFactory();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>EmbeddedServletContainerFactory（嵌入式Servlet容器工厂）,主要是用来创建 EmbeddedServletContainer 也就是嵌入式的Servlet容器。这个是一个接口，也就是我们有对应的实现类，就是上面的三种嵌入式的容器。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">EmbeddedServletContainerFactory</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="comment">//获取嵌入式的Servlet容器</span></span><br><span class="line">   <span class="function">EmbeddedServletContainer <span class="title">getEmbeddedServletContainer</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">         ServletContextInitializer... initializers)</span></span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面我们以 TomcatEmbeddedServletContainerFactory 分析</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> EmbeddedServletContainer <span class="title">getEmbeddedServletContainer</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">      ServletContextInitializer... initializers)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//创建一个Tomcat</span></span><br><span class="line">   Tomcat tomcat = <span class="keyword">new</span> Tomcat();</span><br><span class="line">    <span class="comment">//配置Tomcat的基本环节</span></span><br><span class="line">   File baseDir = (<span class="keyword">this</span>.baseDirectory != <span class="keyword">null</span> ? <span class="keyword">this</span>.baseDirectory</span><br><span class="line">         : createTempDir(<span class="string">"tomcat"</span>));</span><br><span class="line">   tomcat.setBaseDir(baseDir.getAbsolutePath());</span><br><span class="line">   Connector connector = <span class="keyword">new</span> Connector(<span class="keyword">this</span>.protocol);</span><br><span class="line">   tomcat.getService().addConnector(connector);</span><br><span class="line">   customizeConnector(connector);</span><br><span class="line">   tomcat.setConnector(connector);</span><br><span class="line">   tomcat.getHost().setAutoDeploy(<span class="keyword">false</span>);</span><br><span class="line">   configureEngine(tomcat.getEngine());</span><br><span class="line">   <span class="keyword">for</span> (Connector additionalConnector : <span class="keyword">this</span>.additionalTomcatConnectors) &#123;</span><br><span class="line">      tomcat.getService().addConnector(additionalConnector);</span><br><span class="line">   &#125;</span><br><span class="line">   prepareContext(tomcat.getHost(), initializers);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//将配置好的Tomcat传入进去，返回一个EmbeddedServletContainer</span></span><br><span class="line">    <span class="comment">//并且启动Tomcat服务器</span></span><br><span class="line">   <span class="keyword">return</span> getTomcatEmbeddedServletContainer(tomcat);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是我们对嵌入式容器的配置修改是怎么生效？实际上是  <code>ServerProperties、EmbeddedServletContainerCustomizer</code>    ：定制器帮我们修改了Servlet容器的配置，也就是我们在上面导入的 <code>@Import(BeanPostProcessorsRegistrar.class)</code> 然后导入了EmbeddedServletContainerCustomizerBeanPostProcessor（后置处理器）：bean初始化前后（创建完对象，还没赋值赋值）执行初始化工作。</p>
<p>容器中导入了<strong>EmbeddedServletContainerCustomizerBeanPostProcessor</strong>，他的配置原理</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//初始化之前</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span></span></span><br><span class="line"><span class="function">      <span class="keyword">throws</span> BeansException </span>&#123;</span><br><span class="line">    <span class="comment">//如果当前初始化的是一个ConfigurableEmbeddedServletContainer类型的组件</span></span><br><span class="line">   <span class="keyword">if</span> (bean <span class="keyword">instanceof</span> ConfigurableEmbeddedServletContainer) &#123;</span><br><span class="line">       <span class="comment">//</span></span><br><span class="line">      postProcessBeforeInitialization((ConfigurableEmbeddedServletContainer) bean);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> bean;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">postProcessBeforeInitialization</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">			ConfigurableEmbeddedServletContainer bean)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//获取所有的定制器，调用每一个定制器的customize方法来给Servlet容器进行属性赋值；</span></span><br><span class="line">    <span class="keyword">for</span> (EmbeddedServletContainerCustomizer customizer : getCustomizers()) &#123;</span><br><span class="line">        customizer.customize(bean);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> Collection&lt;EmbeddedServletContainerCustomizer&gt; <span class="title">getCustomizers</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.customizers == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="comment">// Look up does not include the parent context</span></span><br><span class="line">        <span class="keyword">this</span>.customizers = <span class="keyword">new</span> ArrayList&lt;EmbeddedServletContainerCustomizer&gt;(</span><br><span class="line">            <span class="keyword">this</span>.beanFactory</span><br><span class="line">            <span class="comment">//从容器中获取所有这葛类型的组件：EmbeddedServletContainerCustomizer</span></span><br><span class="line">            <span class="comment">//定制Servlet容器，给容器中可以添加一个EmbeddedServletContainerCustomizer类型的组件</span></span><br><span class="line">            .getBeansOfType(EmbeddedServletContainerCustomizer.class,</span><br><span class="line">                            <span class="keyword">false</span>, <span class="keyword">false</span>)</span><br><span class="line">            .values());</span><br><span class="line">        Collections.sort(<span class="keyword">this</span>.customizers, AnnotationAwareOrderComparator.INSTANCE);</span><br><span class="line">        <span class="keyword">this</span>.customizers = Collections.unmodifiableList(<span class="keyword">this</span>.customizers);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.customizers;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">ServerProperties也是定制器</span><br></pre></td></tr></table></figure>

<p>步骤：</p>
<p>1）、SpringBoot根据导入的依赖情况，给容器中添加相应的 EmbeddedServletContainerFactory 比如这里就是TomcatEmbeddedServletContainerFactory</p>
<p>2）、容器中某个组件要创建对象就会调用后置处理器：EmbeddedServletContainerCustomizerBeanPostProcessor。</p>
<p>只要是嵌入式的容器工厂，后置处理器就工作。</p>
<p>3）、后置处理器，从容器中获取所有的 <strong>EmbeddedServletContainerCustomizer</strong>，调用定制器的定制方法，而我们的<code>ServerProperties</code> 就是我们要的定制器。</p>
<h2 id="5）、嵌入式Servlet容器启动原理"><a href="#5）、嵌入式Servlet容器启动原理" class="headerlink" title="5）、嵌入式Servlet容器启动原理"></a>5）、嵌入式Servlet容器启动原理</h2><p>什么时候创建嵌入式的Servlet容器工厂？什么时候获取嵌入式的Servlet容器并启动Tomcat；</p>
<p>获取嵌入式的Servlet容器工厂：</p>
<h3 id="1）、SpringBoot应用启动运行run方法"><a href="#1）、SpringBoot应用启动运行run方法" class="headerlink" title="1）、SpringBoot应用启动运行run方法"></a>1）、SpringBoot应用启动运行run方法</h3><h3 id="2）、refreshContext-context"><a href="#2）、refreshContext-context" class="headerlink" title="2）、refreshContext(context)"></a>2）、refreshContext(context)</h3><p>SpringBoot刷新IOC容器，也就是创建IOC容器对象，并初始化容器，创建容器中的每一个组件，这里需要判断如果是web应用创建 <code>AnnotationConfigEmbeddedWebApplicationContext</code>，否则创建<code>AnnotationConfigApplicationContext</code></p>
<h3 id="3）、refresh-context"><a href="#3）、refresh-context" class="headerlink" title="3）、refresh(context)"></a>3）、refresh(context)</h3><p>刷新刚才创建好的ioc容器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">refresh</span><span class="params">()</span> <span class="keyword">throws</span> BeansException, IllegalStateException </span>&#123;</span><br><span class="line">   <span class="keyword">synchronized</span> (<span class="keyword">this</span>.startupShutdownMonitor) &#123;</span><br><span class="line">      <span class="comment">// Prepare this context for refreshing.</span></span><br><span class="line">      prepareRefresh();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Tell the subclass to refresh the internal bean factory.</span></span><br><span class="line">      ConfigurableListableBeanFactory beanFactory = obtainFreshBeanFactory();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Prepare the bean factory for use in this context.</span></span><br><span class="line">      prepareBeanFactory(beanFactory);</span><br><span class="line"></span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         <span class="comment">// Allows post-processing of the bean factory in context subclasses.</span></span><br><span class="line">         postProcessBeanFactory(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Invoke factory processors registered as beans in the context.</span></span><br><span class="line">         invokeBeanFactoryPostProcessors(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Register bean processors that intercept bean creation.</span></span><br><span class="line">         registerBeanPostProcessors(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Initialize message source for this context.</span></span><br><span class="line">         initMessageSource();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Initialize event multicaster for this context.</span></span><br><span class="line">         initApplicationEventMulticaster();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Initialize other special beans in specific context subclasses.</span></span><br><span class="line">         onRefresh();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Check for listener beans and register them.</span></span><br><span class="line">         registerListeners();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Instantiate all remaining (non-lazy-init) singletons.</span></span><br><span class="line">         finishBeanFactoryInitialization(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Last step: publish corresponding event.</span></span><br><span class="line">         finishRefresh();</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">         <span class="keyword">if</span> (logger.isWarnEnabled()) &#123;</span><br><span class="line">            logger.warn(<span class="string">"Exception encountered during context initialization - "</span> +</span><br><span class="line">                  <span class="string">"cancelling refresh attempt: "</span> + ex);</span><br><span class="line">         &#125;</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Destroy already created singletons to avoid dangling resources.</span></span><br><span class="line">         destroyBeans();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Reset 'active' flag.</span></span><br><span class="line">         cancelRefresh(ex);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Propagate exception to caller.</span></span><br><span class="line">         <span class="keyword">throw</span> ex;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">finally</span> &#123;</span><br><span class="line">         <span class="comment">// Reset common introspection caches in Spring's core, since we</span></span><br><span class="line">         <span class="comment">// might not ever need metadata for singleton beans anymore...</span></span><br><span class="line">         resetCommonCaches();</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4）、-onRefresh"><a href="#4）、-onRefresh" class="headerlink" title="4）、  onRefresh()"></a>4）、  onRefresh()</h3><p> web的ioc容器重写了onRefresh方法，web 的 ioc容器会创建嵌入式的Servlet容器，createEmbeddedServletContainer()。</p>
<h3 id="5）、获取嵌入式的Servlet容器工厂"><a href="#5）、获取嵌入式的Servlet容器工厂" class="headerlink" title="5）、获取嵌入式的Servlet容器工厂"></a>5）、获取嵌入式的Servlet容器工厂</h3><p>createEmbeddedServletContainer() 这个方法的第一步就是去创建这个工厂，getEmbeddedServletContainerFactory。</p>
<p>EmbeddedServletContainerFactory containerFactory = getEmbeddedServletContainerFactory();</p>
<p>从ioc容器中获取EmbeddedServletContainerFactory 组件，TomcatEmbeddedServletContainerFactory 创建对象，后置处理器就获取所有的定制器来先定制Servlet容器的相关配置。完成配置和启动。</p>
<h3 id="6）、使用容器工厂获取嵌入式的Servlet容器"><a href="#6）、使用容器工厂获取嵌入式的Servlet容器" class="headerlink" title="6）、使用容器工厂获取嵌入式的Servlet容器"></a>6）、使用容器工厂获取嵌入式的Servlet容器</h3><p>this.embeddedServletContainer = containerFactory.getEbeddedServletContainer(getSelfInitializer());</p>
<h3 id="7）、嵌入式的Servlet容器创建对象并启动Servlet容器"><a href="#7）、嵌入式的Servlet容器创建对象并启动Servlet容器" class="headerlink" title="7）、嵌入式的Servlet容器创建对象并启动Servlet容器"></a>7）、嵌入式的Servlet容器创建对象并启动Servlet容器</h3><p>先启动嵌入式的Servlet容器，再将ioc容器中剩下没有创建出的对象获取出来，这个时候我们自己写的Controller Service 以及各种配置类才会生效。</p>
<h2 id="6）、使用外置的Servlet容器"><a href="#6）、使用外置的Servlet容器" class="headerlink" title="6）、使用外置的Servlet容器"></a>6）、使用外置的Servlet容器</h2><p>我们采用嵌入式Servlet容器，这样我们的应用打成可执行的jar</p>
<p>优点：简单、便携</p>
<p>缺点：默认不支持JSP、优化定制比较复杂（使用定制器ServerProperties、自定义EmbeddedServletContainerCustomizer，自己编写嵌入式Servlet容器的创建工厂EmbeddedServletContainerFactory）</p>
<p>所以我们可以使用外置的Servlet容器：外面安装Tomcat—应用war包的方式打包；</p>
<h3 id="1-步骤"><a href="#1-步骤" class="headerlink" title="1.步骤"></a>1.步骤</h3><p>1）、必须创建一个war项目，我们还是使用 SpringBootInitlizer 这个工具创建项目，但是注意我们在创建项目的时候我们选择部署的方式为 war 就可以生成 war的形式。</p>
<p>然后我们需要去项目的结构地方的web模块点击我们的目录结构让他自动生成webapp 目录。生成在 src/main 下面。然后就是生成 web 应用描述符，也就是生成 web.xml</p>
<p>接着我们需要修改应用的启动配置，自己添加一个tomcat的服务器。然后在 deploy 这个地方加入我们的 war包。</p>
<p>2）、将嵌入式的Tomcat指定为provided；</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-tomcat<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">scope</span>&gt;</span>provided<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>3）、必须编写一个<strong>SpringBootServletInitializer</strong>的子类类名随意但是必须继承，并调用configure方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ServletInitializer</span> <span class="keyword">extends</span> <span class="title">SpringBootServletInitializer</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Override</span></span><br><span class="line">   <span class="function"><span class="keyword">protected</span> SpringApplicationBuilder <span class="title">configure</span><span class="params">(SpringApplicationBuilder application)</span> </span>&#123;</span><br><span class="line">       <span class="comment">//传入SpringBoot应用的主程序</span></span><br><span class="line">      <span class="keyword">return</span> application.sources(SpringBoot04WebJspApplication.class);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>4）、启动服务器就可以使用；</p>
<h3 id="2-启动原理"><a href="#2-启动原理" class="headerlink" title="2.启动原理"></a>2.启动原理</h3><h4 id="1-打包方式"><a href="#1-打包方式" class="headerlink" title="1. 打包方式"></a>1. 打包方式</h4><ol>
<li>jar包：执行SpringBoot主类的main方法，启动ioc容器，创建嵌入式的Servlet容器。</li>
<li>war包：启动服务器，然后服务器启动SpringBoot应用，使用的是 SpringBootServletInitializer，启动ioc容器。</li>
</ol>
<p>servlet3.0一个规范：</p>
<p>​    1）、服务器启动（web应用启动）会创建当前web应用里面每一个jar包里面 ServletContainerInitializer 实例</p>
<p>​    2）、ServletContainerInitializer的实现放在jar包的META-INF/services文件夹下，有一个名为javax.servlet.ServletContainerInitializer的文件，内容就是ServletContainerInitializer的实现类的全类名</p>
<p>​    3）、还可以使用 @HandlesTypes，在应用启动的时候加载我们感兴趣的类</p>
<h4 id="2-启动流程："><a href="#2-启动流程：" class="headerlink" title="2.启动流程："></a>2.启动流程：</h4><h5 id="1）、启动Tomcat"><a href="#1）、启动Tomcat" class="headerlink" title="1）、启动Tomcat"></a>1）、启动Tomcat</h5><h5 id="2）、SpringServletContainerInitializer-的调用"><a href="#2）、SpringServletContainerInitializer-的调用" class="headerlink" title="2）、SpringServletContainerInitializer 的调用"></a>2）、SpringServletContainerInitializer 的调用</h5><p>org\springframework\spring-web\4.3.14.RELEASE\spring-web-4.3.14.RELEASE.jar!\META-INF\services\javax.servlet.ServletContainerInitializer：Spring的web模块里面有这个文件：<strong>org.springframework.web.SpringServletContainerInitializer</strong> 也就是我们应用启动的时候会创建这个对象。</p>
<h5 id="3）、创建-HandlesTypes-的对象"><a href="#3）、创建-HandlesTypes-的对象" class="headerlink" title="3）、创建 @HandlesTypes 的对象"></a>3）、创建 @HandlesTypes 的对象</h5><p>SpringServletContainerInitializer 将 @HandlesTypes(WebApplicationInitializer.class) 标注的所有这个类型的类都传入到 onStartup 方法的 Set&lt;Class&lt;?&gt;&gt;，为这些 WebApplicationInitializer 类型的类创建实例</p>
<h5 id="4）、调用-onStartup-方法"><a href="#4）、调用-onStartup-方法" class="headerlink" title="4）、调用 onStartup 方法"></a>4）、调用 onStartup 方法</h5><p>每一个 WebApplicationInitializer 都调用自己的onStartup；</p>
<h5 id="5）、SpringBootServletInitializer-的-onStartup"><a href="#5）、SpringBootServletInitializer-的-onStartup" class="headerlink" title="5）、SpringBootServletInitializer 的 onStartup"></a>5）、SpringBootServletInitializer 的 onStartup</h5><p>我们的 SpringBootServletInitializer 的类是继承自 WebApplicationInitializer 并且被放到了@HandlesTypes里，所以会被创建对象，并执行onStartup方法，也就是我们采用 war 包工程生成的这个类。</p>
<h5 id="6）、创建-ICO-容器"><a href="#6）、创建-ICO-容器" class="headerlink" title="6）、创建 ICO 容器"></a>6）、创建 ICO 容器</h5><p>SpringBootServletInitializer实例执行onStartup的时候会 createRootApplicationContext 创建容器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> WebApplicationContext <span class="title">createRootApplicationContext</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">      ServletContext servletContext)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//1、创建SpringApplicationBuilder</span></span><br><span class="line">   SpringApplicationBuilder builder = createSpringApplicationBuilder();</span><br><span class="line">   StandardServletEnvironment environment = <span class="keyword">new</span> StandardServletEnvironment();</span><br><span class="line">   environment.initPropertySources(servletContext, <span class="keyword">null</span>);</span><br><span class="line">   builder.environment(environment);</span><br><span class="line">   builder.main(getClass());</span><br><span class="line">   ApplicationContext parent = getExistingRootWebApplicationContext(servletContext);</span><br><span class="line">   <span class="keyword">if</span> (parent != <span class="keyword">null</span>) &#123;</span><br><span class="line">      <span class="keyword">this</span>.logger.info(<span class="string">"Root context already created (using as parent)."</span>);</span><br><span class="line">      servletContext.setAttribute(</span><br><span class="line">            WebApplicationContext.ROOT_WEB_APPLICATION_CONTEXT_ATTRIBUTE, <span class="keyword">null</span>);</span><br><span class="line">      builder.initializers(<span class="keyword">new</span> ParentContextApplicationContextInitializer(parent));</span><br><span class="line">   &#125;</span><br><span class="line">   builder.initializers(</span><br><span class="line">         <span class="keyword">new</span> ServletContextApplicationContextInitializer(servletContext));</span><br><span class="line">   builder.contextClass(AnnotationConfigEmbeddedWebApplicationContext.class);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//调用configure方法，子类重写了这个方法，将SpringBoot的主程序类传入了进来</span></span><br><span class="line">   builder = configure(builder);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//使用builder创建一个Spring应用</span></span><br><span class="line">   SpringApplication application = builder.build();</span><br><span class="line">   <span class="keyword">if</span> (application.getSources().isEmpty() &amp;&amp; AnnotationUtils</span><br><span class="line">         .findAnnotation(getClass(), Configuration.class) != <span class="keyword">null</span>) &#123;</span><br><span class="line">      application.getSources().add(getClass());</span><br><span class="line">   &#125;</span><br><span class="line">   Assert.state(!application.getSources().isEmpty(),</span><br><span class="line">         <span class="string">"No SpringApplication sources have been defined. Either override the "</span></span><br><span class="line">               + <span class="string">"configure method or add an @Configuration annotation"</span>);</span><br><span class="line">   <span class="comment">// Ensure error pages are registered</span></span><br><span class="line">   <span class="keyword">if</span> (<span class="keyword">this</span>.registerErrorPageFilter) &#123;</span><br><span class="line">      application.getSources().add(ErrorPageFilterConfiguration.class);</span><br><span class="line">   &#125;</span><br><span class="line">    <span class="comment">//启动Spring应用</span></span><br><span class="line">   <span class="keyword">return</span> run(application);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="7）、Spring的应用就启动并且创建IOC容器"><a href="#7）、Spring的应用就启动并且创建IOC容器" class="headerlink" title="7）、Spring的应用就启动并且创建IOC容器"></a>7）、Spring的应用就启动并且创建IOC容器</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> ConfigurableApplicationContext <span class="title">run</span><span class="params">(String... args)</span> </span>&#123;</span><br><span class="line">   StopWatch stopWatch = <span class="keyword">new</span> StopWatch();</span><br><span class="line">   stopWatch.start();</span><br><span class="line">   ConfigurableApplicationContext context = <span class="keyword">null</span>;</span><br><span class="line">   FailureAnalyzers analyzers = <span class="keyword">null</span>;</span><br><span class="line">   configureHeadlessProperty();</span><br><span class="line">   SpringApplicationRunListeners listeners = getRunListeners(args);</span><br><span class="line">   listeners.starting();</span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">      ApplicationArguments applicationArguments = <span class="keyword">new</span> DefaultApplicationArguments(</span><br><span class="line">            args);</span><br><span class="line">      ConfigurableEnvironment environment = prepareEnvironment(listeners,</span><br><span class="line">            applicationArguments);</span><br><span class="line">      Banner printedBanner = printBanner(environment);</span><br><span class="line">      context = createApplicationContext();</span><br><span class="line">      analyzers = <span class="keyword">new</span> FailureAnalyzers(context);</span><br><span class="line">      prepareContext(context, environment, listeners, applicationArguments,</span><br><span class="line">            printedBanner);</span><br><span class="line">       </span><br><span class="line">       <span class="comment">//刷新IOC容器</span></span><br><span class="line">      refreshContext(context);</span><br><span class="line">      afterRefresh(context, applicationArguments);</span><br><span class="line">      listeners.finished(context, <span class="keyword">null</span>);</span><br><span class="line">      stopWatch.stop();</span><br><span class="line">      <span class="keyword">if</span> (<span class="keyword">this</span>.logStartupInfo) &#123;</span><br><span class="line">         <span class="keyword">new</span> StartupInfoLogger(<span class="keyword">this</span>.mainApplicationClass)</span><br><span class="line">               .logStarted(getApplicationLog(), stopWatch);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> context;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">      handleRunFailure(context, listeners, analyzers, ex);</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(ex);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>先启动Servlet容器，然后容器根据我们的Servlet的3.0 的标准，去启动我们SpringBoot 下生成的一个类，这个类再启动SpringBoot应用。</p>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记(十一):Servlet容器</title>
    <url>/2018/05/24/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E5%8D%81%E4%B8%80%20)%EF%BC%9AServlet%E5%AE%B9%E5%99%A8/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-十一-Servlet容器"><a href="#SpringBoot-笔记-十一-Servlet容器" class="headerlink" title="SpringBoot 笔记 ( 十一 ):Servlet容器"></a>SpringBoot 笔记 ( 十一 ):Servlet容器</h1><p>SpringBoot默认使用Tomcat作为嵌入式的Servlet容器</p>
<h2 id="1）、定制和修改Servlet容器的相关配置"><a href="#1）、定制和修改Servlet容器的相关配置" class="headerlink" title="1）、定制和修改Servlet容器的相关配置"></a>1）、定制和修改Servlet容器的相关配置</h2><h3 id="1、修改配置文件中的和-server-有关的配置"><a href="#1、修改配置文件中的和-server-有关的配置" class="headerlink" title="1、修改配置文件中的和 server 有关的配置"></a>1、修改配置文件中的和 server 有关的配置</h3><p>ServerProperties【也是EmbeddedServletContainerCustomizer】</p>
<a id="more"></a>

<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">server.port</span>=<span class="string">8081</span></span><br><span class="line"><span class="meta">server.context-path</span>=<span class="string">/crud</span></span><br><span class="line"><span class="meta">server.tomcat.uri-encoding</span>=<span class="string">UTF-8</span></span><br><span class="line"><span class="attr">//通用的Servlet容器设置</span></span><br><span class="line"><span class="attr">server.xxx</span></span><br><span class="line"><span class="attr">//Tomcat的设置</span></span><br><span class="line"><span class="attr">server.tomcat.xxx</span></span><br></pre></td></tr></table></figure>

<h3 id="2、编写一个-EmbeddedServletContainerCustomizer"><a href="#2、编写一个-EmbeddedServletContainerCustomizer" class="headerlink" title="2、编写一个 EmbeddedServletContainerCustomizer"></a>2、编写一个 EmbeddedServletContainerCustomizer</h3><p>嵌入式的Servlet容器的定制器，来修改Servlet容器的配置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span>  <span class="comment">//将这个定制器加入到容器中</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> EmbeddedServletContainerCustomizer <span class="title">embeddedServletContainerCustomizer</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> EmbeddedServletContainerCustomizer() &#123;</span><br><span class="line">        <span class="comment">//定制嵌入式的Servlet容器相关的规则</span></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">customize</span><span class="params">(ConfigurableEmbeddedServletContainer container)</span> </span>&#123;</span><br><span class="line">            container.setPort(<span class="number">8083</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2）、注册Servlet三大组件【Servlet、Filter、Listener】"><a href="#2）、注册Servlet三大组件【Servlet、Filter、Listener】" class="headerlink" title="2）、注册Servlet三大组件【Servlet、Filter、Listener】"></a>2）、注册Servlet三大组件【Servlet、Filter、Listener】</h2><p>由于SpringBoot默认是以jar包的方式启动嵌入式的Servlet容器来启动SpringBoot的web应用，没有web.xml文件。</p>
<p>注册三大组件用以下方式：</p>
<h3 id="1-ServletRegistrationBean"><a href="#1-ServletRegistrationBean" class="headerlink" title="1.ServletRegistrationBean"></a>1.ServletRegistrationBean</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//注册三大组件</span></span><br><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> ServletRegistrationBean <span class="title">myServlet</span><span class="params">()</span></span>&#123;</span><br><span class="line">    ServletRegistrationBean registrationBean = <span class="keyword">new</span> ServletRegistrationBean(<span class="keyword">new</span> MyServlet(),<span class="string">"/myServlet"</span>);</span><br><span class="line">    <span class="keyword">return</span> registrationBean;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-FilterRegistrationBean"><a href="#2-FilterRegistrationBean" class="headerlink" title="2.FilterRegistrationBean"></a>2.FilterRegistrationBean</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> FilterRegistrationBean <span class="title">myFilter</span><span class="params">()</span></span>&#123;</span><br><span class="line">    FilterRegistrationBean registrationBean = <span class="keyword">new</span> FilterRegistrationBean();</span><br><span class="line">    registrationBean.setFilter(<span class="keyword">new</span> MyFilter());</span><br><span class="line">    registrationBean.setUrlPatterns(Arrays.asList(<span class="string">"/hello"</span>,<span class="string">"/myServlet"</span>));</span><br><span class="line">    <span class="keyword">return</span> registrationBean;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-ServletListenerRegistrationBean"><a href="#3-ServletListenerRegistrationBean" class="headerlink" title="3.ServletListenerRegistrationBean"></a>3.ServletListenerRegistrationBean</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> ServletListenerRegistrationBean <span class="title">myListener</span><span class="params">()</span></span>&#123;</span><br><span class="line">    ServletListenerRegistrationBean&lt;MyListener&gt; registrationBean = <span class="keyword">new</span> ServletListenerRegistrationBean&lt;&gt;(<span class="keyword">new</span> MyListener());</span><br><span class="line">    <span class="keyword">return</span> registrationBean;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4、DIspatcherServlet-配置"><a href="#4、DIspatcherServlet-配置" class="headerlink" title="4、DIspatcherServlet 配置"></a>4、DIspatcherServlet 配置</h3><p>SpringBoot帮我们自动SpringMVC的时候，自动的注册SpringMVC的前端控制器，DIspatcherServlet</p>
<p>DispatcherServletAutoConfiguration中：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span>(name = DEFAULT_DISPATCHER_SERVLET_REGISTRATION_BEAN_NAME)</span><br><span class="line"><span class="meta">@ConditionalOnBean</span>(value = DispatcherServlet.class, name = DEFAULT_DISPATCHER_SERVLET_BEAN_NAME)</span><br><span class="line"><span class="function"><span class="keyword">public</span> ServletRegistrationBean <span class="title">dispatcherServletRegistration</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">      DispatcherServlet dispatcherServlet)</span> </span>&#123;</span><br><span class="line">   ServletRegistrationBean registration = <span class="keyword">new</span> ServletRegistrationBean(</span><br><span class="line">         dispatcherServlet, <span class="keyword">this</span>.serverProperties.getServletMapping());</span><br><span class="line">    <span class="comment">//默认拦截： /  所有请求，包静态资源，但是不拦截jsp请求；   /*会拦截jsp</span></span><br><span class="line">    <span class="comment">//可以通过server.servletPath来修改SpringMVC前端控制器默认拦截的请求路径</span></span><br><span class="line">    </span><br><span class="line">   registration.setName(DEFAULT_DISPATCHER_SERVLET_BEAN_NAME);</span><br><span class="line">   registration.setLoadOnStartup(</span><br><span class="line">         <span class="keyword">this</span>.webMvcProperties.getServlet().getLoadOnStartup());</span><br><span class="line">   <span class="keyword">if</span> (<span class="keyword">this</span>.multipartConfig != <span class="keyword">null</span>) &#123;</span><br><span class="line">      registration.setMultipartConfig(<span class="keyword">this</span>.multipartConfig);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> registration;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3）、替换为其他嵌入式Servlet容器"><a href="#3）、替换为其他嵌入式Servlet容器" class="headerlink" title="3）、替换为其他嵌入式Servlet容器"></a>3）、替换为其他嵌入式Servlet容器</h2><p>默认支持：</p>
<h3 id="1-Tomcat（默认使用）"><a href="#1-Tomcat（默认使用）" class="headerlink" title="1.Tomcat（默认使用）"></a>1.Tomcat（默认使用）</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   引入web模块默认就是使用嵌入式的Tomcat作为Servlet容器；</span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-Jetty（长链接类的服务）"><a href="#2-Jetty（长链接类的服务）" class="headerlink" title="2.Jetty（长链接类的服务）"></a>2.Jetty（长链接类的服务）</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 引入web模块 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-tomcat<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!--引入其他的Servlet容器--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-jetty<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="3-Undertow（高并发的不支持JSP）"><a href="#3-Undertow（高并发的不支持JSP）" class="headerlink" title="3.Undertow（高并发的不支持JSP）"></a>3.Undertow（高并发的不支持JSP）</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 引入web模块 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-tomcat<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!--引入其他的Servlet容器--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-undertow<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="4）、嵌入式Servlet容器自动配置原理"><a href="#4）、嵌入式Servlet容器自动配置原理" class="headerlink" title="4）、嵌入式Servlet容器自动配置原理"></a>4）、嵌入式Servlet容器自动配置原理</h2><p>EmbeddedServletContainerAutoConfiguration：嵌入式的Servlet容器自动配置</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@AutoConfigureOrder</span>(Ordered.HIGHEST_PRECEDENCE)</span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ConditionalOnWebApplication</span></span><br><span class="line"><span class="meta">@Import</span>(BeanPostProcessorsRegistrar.class)</span><br><span class="line"><span class="comment">//导入BeanPostProcessorsRegistrar：Spring注解版；给容器中导入一些组件</span></span><br><span class="line"><span class="comment">//导入了EmbeddedServletContainerCustomizerBeanPostProcessor：</span></span><br><span class="line"><span class="comment">//后置处理器：bean初始化前后（创建完对象，还没赋值赋值）执行初始化工作</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EmbeddedServletContainerAutoConfiguration</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Configuration</span></span><br><span class="line">	<span class="meta">@ConditionalOnClass</span>(&#123; Servlet.class, Tomcat.class &#125;)<span class="comment">//判断当前是否引入了Tomcat依赖；</span></span><br><span class="line">	<span class="meta">@ConditionalOnMissingBean</span>(value = EmbeddedServletContainerFactory.class, search = SearchStrategy.CURRENT)<span class="comment">//判断当前容器没有用户自己定义EmbeddedServletContainerFactory：嵌入式的Servlet容器工厂；作用：创建嵌入式的Servlet容器</span></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">EmbeddedTomcat</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Bean</span></span><br><span class="line">		<span class="function"><span class="keyword">public</span> TomcatEmbeddedServletContainerFactory <span class="title">tomcatEmbeddedServletContainerFactory</span><span class="params">()</span> </span>&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> TomcatEmbeddedServletContainerFactory();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Nested configuration if Jetty is being used.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@Configuration</span></span><br><span class="line">	<span class="meta">@ConditionalOnClass</span>(&#123; Servlet.class, Server.class, Loader.class,</span><br><span class="line">			WebAppContext.class &#125;)</span><br><span class="line">	<span class="meta">@ConditionalOnMissingBean</span>(value = EmbeddedServletContainerFactory.class, search = SearchStrategy.CURRENT)</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">EmbeddedJetty</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Bean</span></span><br><span class="line">		<span class="function"><span class="keyword">public</span> JettyEmbeddedServletContainerFactory <span class="title">jettyEmbeddedServletContainerFactory</span><span class="params">()</span> </span>&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> JettyEmbeddedServletContainerFactory();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">	<span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Nested configuration if Undertow is being used.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="meta">@Configuration</span></span><br><span class="line">	<span class="meta">@ConditionalOnClass</span>(&#123; Servlet.class, Undertow.class, SslClientAuthMode.class &#125;)</span><br><span class="line">	<span class="meta">@ConditionalOnMissingBean</span>(value = EmbeddedServletContainerFactory.class, search = SearchStrategy.CURRENT)</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">EmbeddedUndertow</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Bean</span></span><br><span class="line">		<span class="function"><span class="keyword">public</span> UndertowEmbeddedServletContainerFactory <span class="title">undertowEmbeddedServletContainerFactory</span><span class="params">()</span> </span>&#123;</span><br><span class="line">			<span class="keyword">return</span> <span class="keyword">new</span> UndertowEmbeddedServletContainerFactory();</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>EmbeddedServletContainerFactory（嵌入式Servlet容器工厂）,主要是用来创建 EmbeddedServletContainer 也就是嵌入式的Servlet容器。这个是一个接口，也就是我们有对应的实现类，就是上面的三种嵌入式的容器。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">EmbeddedServletContainerFactory</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="comment">//获取嵌入式的Servlet容器</span></span><br><span class="line">   <span class="function">EmbeddedServletContainer <span class="title">getEmbeddedServletContainer</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">         ServletContextInitializer... initializers)</span></span>;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面我们以 TomcatEmbeddedServletContainerFactory 分析</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> EmbeddedServletContainer <span class="title">getEmbeddedServletContainer</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">      ServletContextInitializer... initializers)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//创建一个Tomcat</span></span><br><span class="line">   Tomcat tomcat = <span class="keyword">new</span> Tomcat();</span><br><span class="line">    <span class="comment">//配置Tomcat的基本环节</span></span><br><span class="line">   File baseDir = (<span class="keyword">this</span>.baseDirectory != <span class="keyword">null</span> ? <span class="keyword">this</span>.baseDirectory</span><br><span class="line">         : createTempDir(<span class="string">"tomcat"</span>));</span><br><span class="line">   tomcat.setBaseDir(baseDir.getAbsolutePath());</span><br><span class="line">   Connector connector = <span class="keyword">new</span> Connector(<span class="keyword">this</span>.protocol);</span><br><span class="line">   tomcat.getService().addConnector(connector);</span><br><span class="line">   customizeConnector(connector);</span><br><span class="line">   tomcat.setConnector(connector);</span><br><span class="line">   tomcat.getHost().setAutoDeploy(<span class="keyword">false</span>);</span><br><span class="line">   configureEngine(tomcat.getEngine());</span><br><span class="line">   <span class="keyword">for</span> (Connector additionalConnector : <span class="keyword">this</span>.additionalTomcatConnectors) &#123;</span><br><span class="line">      tomcat.getService().addConnector(additionalConnector);</span><br><span class="line">   &#125;</span><br><span class="line">   prepareContext(tomcat.getHost(), initializers);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//将配置好的Tomcat传入进去，返回一个EmbeddedServletContainer</span></span><br><span class="line">    <span class="comment">//并且启动Tomcat服务器</span></span><br><span class="line">   <span class="keyword">return</span> getTomcatEmbeddedServletContainer(tomcat);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是我们对嵌入式容器的配置修改是怎么生效？实际上是  <code>ServerProperties、EmbeddedServletContainerCustomizer</code>    ：定制器帮我们修改了Servlet容器的配置，也就是我们在上面导入的 <code>@Import(BeanPostProcessorsRegistrar.class)</code> 然后导入了EmbeddedServletContainerCustomizerBeanPostProcessor（后置处理器）：bean初始化前后（创建完对象，还没赋值赋值）执行初始化工作。</p>
<p>容器中导入了<strong>EmbeddedServletContainerCustomizerBeanPostProcessor</strong>，他的配置原理</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//初始化之前</span></span><br><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Object <span class="title">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span></span></span><br><span class="line"><span class="function">      <span class="keyword">throws</span> BeansException </span>&#123;</span><br><span class="line">    <span class="comment">//如果当前初始化的是一个ConfigurableEmbeddedServletContainer类型的组件</span></span><br><span class="line">   <span class="keyword">if</span> (bean <span class="keyword">instanceof</span> ConfigurableEmbeddedServletContainer) &#123;</span><br><span class="line">       <span class="comment">//</span></span><br><span class="line">      postProcessBeforeInitialization((ConfigurableEmbeddedServletContainer) bean);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> bean;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">postProcessBeforeInitialization</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">			ConfigurableEmbeddedServletContainer bean)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//获取所有的定制器，调用每一个定制器的customize方法来给Servlet容器进行属性赋值；</span></span><br><span class="line">    <span class="keyword">for</span> (EmbeddedServletContainerCustomizer customizer : getCustomizers()) &#123;</span><br><span class="line">        customizer.customize(bean);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> Collection&lt;EmbeddedServletContainerCustomizer&gt; <span class="title">getCustomizers</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>.customizers == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="comment">// Look up does not include the parent context</span></span><br><span class="line">        <span class="keyword">this</span>.customizers = <span class="keyword">new</span> ArrayList&lt;EmbeddedServletContainerCustomizer&gt;(</span><br><span class="line">            <span class="keyword">this</span>.beanFactory</span><br><span class="line">            <span class="comment">//从容器中获取所有这葛类型的组件：EmbeddedServletContainerCustomizer</span></span><br><span class="line">            <span class="comment">//定制Servlet容器，给容器中可以添加一个EmbeddedServletContainerCustomizer类型的组件</span></span><br><span class="line">            .getBeansOfType(EmbeddedServletContainerCustomizer.class,</span><br><span class="line">                            <span class="keyword">false</span>, <span class="keyword">false</span>)</span><br><span class="line">            .values());</span><br><span class="line">        Collections.sort(<span class="keyword">this</span>.customizers, AnnotationAwareOrderComparator.INSTANCE);</span><br><span class="line">        <span class="keyword">this</span>.customizers = Collections.unmodifiableList(<span class="keyword">this</span>.customizers);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">this</span>.customizers;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">ServerProperties也是定制器</span><br></pre></td></tr></table></figure>

<p>步骤：</p>
<p>1）、SpringBoot根据导入的依赖情况，给容器中添加相应的 EmbeddedServletContainerFactory 比如这里就是TomcatEmbeddedServletContainerFactory</p>
<p>2）、容器中某个组件要创建对象就会调用后置处理器：EmbeddedServletContainerCustomizerBeanPostProcessor。</p>
<p>只要是嵌入式的容器工厂，后置处理器就工作。</p>
<p>3）、后置处理器，从容器中获取所有的 <strong>EmbeddedServletContainerCustomizer</strong>，调用定制器的定制方法，而我们的<code>ServerProperties</code> 就是我们要的定制器。</p>
<h2 id="5）、嵌入式Servlet容器启动原理"><a href="#5）、嵌入式Servlet容器启动原理" class="headerlink" title="5）、嵌入式Servlet容器启动原理"></a>5）、嵌入式Servlet容器启动原理</h2><p>什么时候创建嵌入式的Servlet容器工厂？什么时候获取嵌入式的Servlet容器并启动Tomcat；</p>
<p>获取嵌入式的Servlet容器工厂：</p>
<h3 id="1）、SpringBoot应用启动运行run方法"><a href="#1）、SpringBoot应用启动运行run方法" class="headerlink" title="1）、SpringBoot应用启动运行run方法"></a>1）、SpringBoot应用启动运行run方法</h3><h3 id="2）、refreshContext-context"><a href="#2）、refreshContext-context" class="headerlink" title="2）、refreshContext(context)"></a>2）、refreshContext(context)</h3><p>SpringBoot刷新IOC容器，也就是创建IOC容器对象，并初始化容器，创建容器中的每一个组件，这里需要判断如果是web应用创建 <code>AnnotationConfigEmbeddedWebApplicationContext</code>，否则创建<code>AnnotationConfigApplicationContext</code></p>
<h3 id="3）、refresh-context"><a href="#3）、refresh-context" class="headerlink" title="3）、refresh(context)"></a>3）、refresh(context)</h3><p>刷新刚才创建好的ioc容器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">refresh</span><span class="params">()</span> <span class="keyword">throws</span> BeansException, IllegalStateException </span>&#123;</span><br><span class="line">   <span class="keyword">synchronized</span> (<span class="keyword">this</span>.startupShutdownMonitor) &#123;</span><br><span class="line">      <span class="comment">// Prepare this context for refreshing.</span></span><br><span class="line">      prepareRefresh();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Tell the subclass to refresh the internal bean factory.</span></span><br><span class="line">      ConfigurableListableBeanFactory beanFactory = obtainFreshBeanFactory();</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Prepare the bean factory for use in this context.</span></span><br><span class="line">      prepareBeanFactory(beanFactory);</span><br><span class="line"></span><br><span class="line">      <span class="keyword">try</span> &#123;</span><br><span class="line">         <span class="comment">// Allows post-processing of the bean factory in context subclasses.</span></span><br><span class="line">         postProcessBeanFactory(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Invoke factory processors registered as beans in the context.</span></span><br><span class="line">         invokeBeanFactoryPostProcessors(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Register bean processors that intercept bean creation.</span></span><br><span class="line">         registerBeanPostProcessors(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Initialize message source for this context.</span></span><br><span class="line">         initMessageSource();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Initialize event multicaster for this context.</span></span><br><span class="line">         initApplicationEventMulticaster();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Initialize other special beans in specific context subclasses.</span></span><br><span class="line">         onRefresh();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Check for listener beans and register them.</span></span><br><span class="line">         registerListeners();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Instantiate all remaining (non-lazy-init) singletons.</span></span><br><span class="line">         finishBeanFactoryInitialization(beanFactory);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Last step: publish corresponding event.</span></span><br><span class="line">         finishRefresh();</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">catch</span> (BeansException ex) &#123;</span><br><span class="line">         <span class="keyword">if</span> (logger.isWarnEnabled()) &#123;</span><br><span class="line">            logger.warn(<span class="string">"Exception encountered during context initialization - "</span> +</span><br><span class="line">                  <span class="string">"cancelling refresh attempt: "</span> + ex);</span><br><span class="line">         &#125;</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Destroy already created singletons to avoid dangling resources.</span></span><br><span class="line">         destroyBeans();</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Reset 'active' flag.</span></span><br><span class="line">         cancelRefresh(ex);</span><br><span class="line"></span><br><span class="line">         <span class="comment">// Propagate exception to caller.</span></span><br><span class="line">         <span class="keyword">throw</span> ex;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">finally</span> &#123;</span><br><span class="line">         <span class="comment">// Reset common introspection caches in Spring's core, since we</span></span><br><span class="line">         <span class="comment">// might not ever need metadata for singleton beans anymore...</span></span><br><span class="line">         resetCommonCaches();</span><br><span class="line">      &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4）、-onRefresh"><a href="#4）、-onRefresh" class="headerlink" title="4）、  onRefresh()"></a>4）、  onRefresh()</h3><p> web的ioc容器重写了onRefresh方法，web 的 ioc容器会创建嵌入式的Servlet容器，createEmbeddedServletContainer()。</p>
<h3 id="5）、获取嵌入式的Servlet容器工厂"><a href="#5）、获取嵌入式的Servlet容器工厂" class="headerlink" title="5）、获取嵌入式的Servlet容器工厂"></a>5）、获取嵌入式的Servlet容器工厂</h3><p>createEmbeddedServletContainer() 这个方法的第一步就是去创建这个工厂，getEmbeddedServletContainerFactory。</p>
<p>EmbeddedServletContainerFactory containerFactory = getEmbeddedServletContainerFactory();</p>
<p>从ioc容器中获取EmbeddedServletContainerFactory 组件，TomcatEmbeddedServletContainerFactory 创建对象，后置处理器就获取所有的定制器来先定制Servlet容器的相关配置。完成配置和启动。</p>
<h3 id="6）、使用容器工厂获取嵌入式的Servlet容器"><a href="#6）、使用容器工厂获取嵌入式的Servlet容器" class="headerlink" title="6）、使用容器工厂获取嵌入式的Servlet容器"></a>6）、使用容器工厂获取嵌入式的Servlet容器</h3><p>this.embeddedServletContainer = containerFactory.getEbeddedServletContainer(getSelfInitializer());</p>
<h3 id="7）、嵌入式的Servlet容器创建对象并启动Servlet容器"><a href="#7）、嵌入式的Servlet容器创建对象并启动Servlet容器" class="headerlink" title="7）、嵌入式的Servlet容器创建对象并启动Servlet容器"></a>7）、嵌入式的Servlet容器创建对象并启动Servlet容器</h3><p>先启动嵌入式的Servlet容器，再将ioc容器中剩下没有创建出的对象获取出来，这个时候我们自己写的Controller Service 以及各种配置类才会生效。</p>
<h2 id="6）、使用外置的Servlet容器"><a href="#6）、使用外置的Servlet容器" class="headerlink" title="6）、使用外置的Servlet容器"></a>6）、使用外置的Servlet容器</h2><p>我们采用嵌入式Servlet容器，这样我们的应用打成可执行的jar</p>
<p>优点：简单、便携</p>
<p>缺点：默认不支持JSP、优化定制比较复杂（使用定制器ServerProperties、自定义EmbeddedServletContainerCustomizer，自己编写嵌入式Servlet容器的创建工厂EmbeddedServletContainerFactory）</p>
<p>所以我们可以使用外置的Servlet容器：外面安装Tomcat—应用war包的方式打包；</p>
<h3 id="1-步骤"><a href="#1-步骤" class="headerlink" title="1.步骤"></a>1.步骤</h3><p>1）、必须创建一个war项目，我们还是使用 SpringBootInitlizer 这个工具创建项目，但是注意我们在创建项目的时候我们选择部署的方式为 war 就可以生成 war的形式。</p>
<p>然后我们需要去项目的结构地方的web模块点击我们的目录结构让他自动生成webapp 目录。生成在 src/main 下面。然后就是生成 web 应用描述符，也就是生成 web.xml</p>
<p>接着我们需要修改应用的启动配置，自己添加一个tomcat的服务器。然后在 deploy 这个地方加入我们的 war包。</p>
<p>2）、将嵌入式的Tomcat指定为provided；</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-tomcat<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">scope</span>&gt;</span>provided<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>3）、必须编写一个<strong>SpringBootServletInitializer</strong>的子类类名随意但是必须继承，并调用configure方法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ServletInitializer</span> <span class="keyword">extends</span> <span class="title">SpringBootServletInitializer</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Override</span></span><br><span class="line">   <span class="function"><span class="keyword">protected</span> SpringApplicationBuilder <span class="title">configure</span><span class="params">(SpringApplicationBuilder application)</span> </span>&#123;</span><br><span class="line">       <span class="comment">//传入SpringBoot应用的主程序</span></span><br><span class="line">      <span class="keyword">return</span> application.sources(SpringBoot04WebJspApplication.class);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>4）、启动服务器就可以使用；</p>
<h3 id="2-启动原理"><a href="#2-启动原理" class="headerlink" title="2.启动原理"></a>2.启动原理</h3><h4 id="1-打包方式"><a href="#1-打包方式" class="headerlink" title="1. 打包方式"></a>1. 打包方式</h4><ol>
<li>jar包：执行SpringBoot主类的main方法，启动ioc容器，创建嵌入式的Servlet容器。</li>
<li>war包：启动服务器，然后服务器启动SpringBoot应用，使用的是 SpringBootServletInitializer，启动ioc容器。</li>
</ol>
<p>servlet3.0一个规范：</p>
<p>​    1）、服务器启动（web应用启动）会创建当前web应用里面每一个jar包里面 ServletContainerInitializer 实例</p>
<p>​    2）、ServletContainerInitializer的实现放在jar包的META-INF/services文件夹下，有一个名为javax.servlet.ServletContainerInitializer的文件，内容就是ServletContainerInitializer的实现类的全类名</p>
<p>​    3）、还可以使用 @HandlesTypes，在应用启动的时候加载我们感兴趣的类</p>
<h4 id="2-启动流程："><a href="#2-启动流程：" class="headerlink" title="2.启动流程："></a>2.启动流程：</h4><h5 id="1）、启动Tomcat"><a href="#1）、启动Tomcat" class="headerlink" title="1）、启动Tomcat"></a>1）、启动Tomcat</h5><h5 id="2）、SpringServletContainerInitializer-的调用"><a href="#2）、SpringServletContainerInitializer-的调用" class="headerlink" title="2）、SpringServletContainerInitializer 的调用"></a>2）、SpringServletContainerInitializer 的调用</h5><p>org\springframework\spring-web\4.3.14.RELEASE\spring-web-4.3.14.RELEASE.jar!\META-INF\services\javax.servlet.ServletContainerInitializer：Spring的web模块里面有这个文件：<strong>org.springframework.web.SpringServletContainerInitializer</strong> 也就是我们应用启动的时候会创建这个对象。</p>
<h5 id="3）、创建-HandlesTypes-的对象"><a href="#3）、创建-HandlesTypes-的对象" class="headerlink" title="3）、创建 @HandlesTypes 的对象"></a>3）、创建 @HandlesTypes 的对象</h5><p>SpringServletContainerInitializer 将 @HandlesTypes(WebApplicationInitializer.class) 标注的所有这个类型的类都传入到 onStartup 方法的 Set&lt;Class&lt;?&gt;&gt;，为这些 WebApplicationInitializer 类型的类创建实例</p>
<h5 id="4）、调用-onStartup-方法"><a href="#4）、调用-onStartup-方法" class="headerlink" title="4）、调用 onStartup 方法"></a>4）、调用 onStartup 方法</h5><p>每一个 WebApplicationInitializer 都调用自己的onStartup；</p>
<h5 id="5）、SpringBootServletInitializer-的-onStartup"><a href="#5）、SpringBootServletInitializer-的-onStartup" class="headerlink" title="5）、SpringBootServletInitializer 的 onStartup"></a>5）、SpringBootServletInitializer 的 onStartup</h5><p>我们的 SpringBootServletInitializer 的类是继承自 WebApplicationInitializer 并且被放到了@HandlesTypes里，所以会被创建对象，并执行onStartup方法，也就是我们采用 war 包工程生成的这个类。</p>
<h5 id="6）、创建-ICO-容器"><a href="#6）、创建-ICO-容器" class="headerlink" title="6）、创建 ICO 容器"></a>6）、创建 ICO 容器</h5><p>SpringBootServletInitializer实例执行onStartup的时候会 createRootApplicationContext 创建容器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> WebApplicationContext <span class="title">createRootApplicationContext</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">      ServletContext servletContext)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//1、创建SpringApplicationBuilder</span></span><br><span class="line">   SpringApplicationBuilder builder = createSpringApplicationBuilder();</span><br><span class="line">   StandardServletEnvironment environment = <span class="keyword">new</span> StandardServletEnvironment();</span><br><span class="line">   environment.initPropertySources(servletContext, <span class="keyword">null</span>);</span><br><span class="line">   builder.environment(environment);</span><br><span class="line">   builder.main(getClass());</span><br><span class="line">   ApplicationContext parent = getExistingRootWebApplicationContext(servletContext);</span><br><span class="line">   <span class="keyword">if</span> (parent != <span class="keyword">null</span>) &#123;</span><br><span class="line">      <span class="keyword">this</span>.logger.info(<span class="string">"Root context already created (using as parent)."</span>);</span><br><span class="line">      servletContext.setAttribute(</span><br><span class="line">            WebApplicationContext.ROOT_WEB_APPLICATION_CONTEXT_ATTRIBUTE, <span class="keyword">null</span>);</span><br><span class="line">      builder.initializers(<span class="keyword">new</span> ParentContextApplicationContextInitializer(parent));</span><br><span class="line">   &#125;</span><br><span class="line">   builder.initializers(</span><br><span class="line">         <span class="keyword">new</span> ServletContextApplicationContextInitializer(servletContext));</span><br><span class="line">   builder.contextClass(AnnotationConfigEmbeddedWebApplicationContext.class);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//调用configure方法，子类重写了这个方法，将SpringBoot的主程序类传入了进来</span></span><br><span class="line">   builder = configure(builder);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">//使用builder创建一个Spring应用</span></span><br><span class="line">   SpringApplication application = builder.build();</span><br><span class="line">   <span class="keyword">if</span> (application.getSources().isEmpty() &amp;&amp; AnnotationUtils</span><br><span class="line">         .findAnnotation(getClass(), Configuration.class) != <span class="keyword">null</span>) &#123;</span><br><span class="line">      application.getSources().add(getClass());</span><br><span class="line">   &#125;</span><br><span class="line">   Assert.state(!application.getSources().isEmpty(),</span><br><span class="line">         <span class="string">"No SpringApplication sources have been defined. Either override the "</span></span><br><span class="line">               + <span class="string">"configure method or add an @Configuration annotation"</span>);</span><br><span class="line">   <span class="comment">// Ensure error pages are registered</span></span><br><span class="line">   <span class="keyword">if</span> (<span class="keyword">this</span>.registerErrorPageFilter) &#123;</span><br><span class="line">      application.getSources().add(ErrorPageFilterConfiguration.class);</span><br><span class="line">   &#125;</span><br><span class="line">    <span class="comment">//启动Spring应用</span></span><br><span class="line">   <span class="keyword">return</span> run(application);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h5 id="7）、Spring的应用就启动并且创建IOC容器"><a href="#7）、Spring的应用就启动并且创建IOC容器" class="headerlink" title="7）、Spring的应用就启动并且创建IOC容器"></a>7）、Spring的应用就启动并且创建IOC容器</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> ConfigurableApplicationContext <span class="title">run</span><span class="params">(String... args)</span> </span>&#123;</span><br><span class="line">   StopWatch stopWatch = <span class="keyword">new</span> StopWatch();</span><br><span class="line">   stopWatch.start();</span><br><span class="line">   ConfigurableApplicationContext context = <span class="keyword">null</span>;</span><br><span class="line">   FailureAnalyzers analyzers = <span class="keyword">null</span>;</span><br><span class="line">   configureHeadlessProperty();</span><br><span class="line">   SpringApplicationRunListeners listeners = getRunListeners(args);</span><br><span class="line">   listeners.starting();</span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">      ApplicationArguments applicationArguments = <span class="keyword">new</span> DefaultApplicationArguments(</span><br><span class="line">            args);</span><br><span class="line">      ConfigurableEnvironment environment = prepareEnvironment(listeners,</span><br><span class="line">            applicationArguments);</span><br><span class="line">      Banner printedBanner = printBanner(environment);</span><br><span class="line">      context = createApplicationContext();</span><br><span class="line">      analyzers = <span class="keyword">new</span> FailureAnalyzers(context);</span><br><span class="line">      prepareContext(context, environment, listeners, applicationArguments,</span><br><span class="line">            printedBanner);</span><br><span class="line">       </span><br><span class="line">       <span class="comment">//刷新IOC容器</span></span><br><span class="line">      refreshContext(context);</span><br><span class="line">      afterRefresh(context, applicationArguments);</span><br><span class="line">      listeners.finished(context, <span class="keyword">null</span>);</span><br><span class="line">      stopWatch.stop();</span><br><span class="line">      <span class="keyword">if</span> (<span class="keyword">this</span>.logStartupInfo) &#123;</span><br><span class="line">         <span class="keyword">new</span> StartupInfoLogger(<span class="keyword">this</span>.mainApplicationClass)</span><br><span class="line">               .logStarted(getApplicationLog(), stopWatch);</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">return</span> context;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">      handleRunFailure(context, listeners, analyzers, ex);</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(ex);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>先启动Servlet容器，然后容器根据我们的Servlet的3.0 的标准，去启动我们SpringBoot 下生成的一个类，这个类再启动SpringBoot应用。</p>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记(十二):数据访问</title>
    <url>/2018/05/24/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E5%8D%81%E4%BA%8C%20)%E6%95%B0%E6%8D%AE%E8%AE%BF%E9%97%AE/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-十二-数据访问"><a href="#SpringBoot-笔记-十二-数据访问" class="headerlink" title="SpringBoot 笔记(十二):数据访问"></a>SpringBoot 笔记(十二):数据访问</h1><h2 id="1、JDBC"><a href="#1、JDBC" class="headerlink" title="1、JDBC"></a>1、JDBC</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-jdbc<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-java<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">scope</span>&gt;</span>runtime<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">  datasource:</span></span><br><span class="line"><span class="attr">    username:</span> <span class="string">root</span></span><br><span class="line"><span class="attr">    password:</span> <span class="number">123456</span></span><br><span class="line"><span class="attr">    url:</span> <span class="attr">jdbc:mysql://192.168.15.22:3306/jdbc</span></span><br><span class="line"><span class="attr">    driver-class-name:</span> <span class="string">com.mysql.jdbc.Driver</span></span><br></pre></td></tr></table></figure>

<h3 id="1-默认情况下："><a href="#1-默认情况下：" class="headerlink" title="1.默认情况下："></a>1.默认情况下：</h3><p>​    1. 用org.apache.tomcat.jdbc.pool.DataSource作为数据源；</p>
<p>​    2. 数据源的相关配置都在DataSourceProperties里面；</p>
<h3 id="2-自动配置原理："><a href="#2-自动配置原理：" class="headerlink" title="2.自动配置原理："></a>2.自动配置原理：</h3><p>org.springframework.boot.autoconfigure.jdbc包下就是进行自动配置的包：</p>
<h4 id="1、数据库连接池"><a href="#1、数据库连接池" class="headerlink" title="1、数据库连接池"></a>1、数据库连接池</h4><p>参考 DataSourceConfiguration，根据配置创建数据源，默认使用Tomcat连接池，可以使用spring.datasource.type指定自定义的数据源类型</p>
<h4 id="2、创建数据源"><a href="#2、创建数据源" class="headerlink" title="2、创建数据源"></a>2、创建数据源</h4><p>SpringBoot默认可以支持：<code>org.apache.tomcat.jdbc.pool.DataSource、HikariDataSource、BasicDataSource</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Generic DataSource configuration.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@ConditionalOnMissingBean</span>(DataSource.class)</span><br><span class="line"><span class="meta">@ConditionalOnProperty</span>(name = <span class="string">"spring.datasource.type"</span>)</span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Generic</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Bean</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> DataSource <span class="title">dataSource</span><span class="params">(DataSourceProperties properties)</span> </span>&#123;</span><br><span class="line">       <span class="comment">//使用DataSourceBuilder创建数据源，利用反射创建响应type的数据源，并且绑定相关属性</span></span><br><span class="line">      <span class="keyword">return</span> properties.initializeDataSourceBuilder().build();</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="3、DataSourceInitializer：ApplicationListener"><a href="#3、DataSourceInitializer：ApplicationListener" class="headerlink" title="3、DataSourceInitializer：ApplicationListener"></a>3、DataSourceInitializer：ApplicationListener</h4><p>一个监听器，主要用于sql脚本的运行</p>
<p>​    作用：</p>
<p>​        1）、runSchemaScripts();运行建表语句；</p>
<p>​        2）、runDataScripts();运行插入数据的sql语句；</p>
<p>默认只需要将文件命名为： <code>schema-*.sql 或者 data-*.sql</code> 默认规则：<code>schema.sql，schema-all.sql</code></p>
<p>当然我们也可以在配置文件中指定，然后就加载指定的sql 脚本，这里是一个 list 也就是脚本可以指定多个。</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">schema</span>:<span class="string"></span></span><br><span class="line">    <span class="meta">-</span> <span class="string">classpath:department.sql</span></span><br></pre></td></tr></table></figure>

<h2 id="2、整合Druid数据源"><a href="#2、整合Druid数据源" class="headerlink" title="2、整合Druid数据源"></a>2、整合Druid数据源</h2><h3 id="1-配置-yml"><a href="#1-配置-yml" class="headerlink" title="1.配置 yml"></a>1.配置 yml</h3><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">spring.datasource.initialSize</span>:<span class="string">5</span></span><br><span class="line"><span class="meta">spring.datasource.minIdle</span>:<span class="string">5</span></span><br><span class="line"><span class="meta">spring.datasource.maxActive</span>:<span class="string">20</span></span><br><span class="line"><span class="meta">spring.datasource.maxlait</span>:<span class="string">60800</span></span><br><span class="line"><span class="meta">spring.datasource.timeBetweenEvictionRunsMillis</span>:<span class="string">60000</span></span><br><span class="line"><span class="meta">spring.datasource.minEvictableIdleTimeMillis</span>:<span class="string">300000</span></span><br><span class="line"><span class="meta">spring.datasource.validationQuery</span>:<span class="string">SELECT 1 FROM DUAL</span></span><br><span class="line"><span class="meta">spring.datasource.testWhileIdle</span>:<span class="string">true</span></span><br><span class="line"><span class="meta">spring.datasource.testOnBorrow</span>:<span class="string">false</span></span><br><span class="line"><span class="meta">spring.datasource.testOnReturn</span>:<span class="string">false</span></span><br><span class="line"><span class="meta">spring.datasource.poolPreparedStatements</span>:<span class="string">true</span></span><br><span class="line"><span class="comment">#配置监控统计拦截的filters，去掉后监控界面sql无法统计，‘walL“用于防火墙</span></span><br><span class="line"><span class="meta">spring.datasource.filters</span>:<span class="string">stat,wall,log4j</span></span><br><span class="line"><span class="meta">spring.datasource.maxPoolPreparedStatementPerConnectionSize</span>:<span class="string">20</span></span><br><span class="line"><span class="meta">spring.datasource.useGTobalDataSourceStat</span>:<span class="string">true</span></span><br><span class="line"><span class="meta">spring.datasource.connectionProperties</span>:<span class="string">druid.stat.mergeSql=true；druid.stat.slowsqlMillis=5e0</span></span><br></pre></td></tr></table></figure>

<p>上面的配置文件中的数据实际上我们没用上，这里我们需要创建一个 DruidConfig 然后和当前的配置文件绑定。</p>
<h3 id="2-JavaConfig"><a href="#2-JavaConfig" class="headerlink" title="2.JavaConfig"></a>2.JavaConfig</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DruidConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"spring.datasource"</span>)</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> DataSource <span class="title">druid</span><span class="params">()</span></span>&#123;</span><br><span class="line">       <span class="keyword">return</span>  <span class="keyword">new</span> DruidDataSource();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//配置Druid的监控</span></span><br><span class="line">    <span class="comment">//1、配置一个管理后台的Servlet</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ServletRegistrationBean <span class="title">statViewServlet</span><span class="params">()</span></span>&#123;</span><br><span class="line">        ServletRegistrationBean bean = <span class="keyword">new</span> ServletRegistrationBean(<span class="keyword">new</span> StatViewServlet(), <span class="string">"/druid/*"</span>);</span><br><span class="line">        Map&lt;String,String&gt; initParams = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line"></span><br><span class="line">        initParams.put(<span class="string">"loginUsername"</span>,<span class="string">"admin"</span>);</span><br><span class="line">        initParams.put(<span class="string">"loginPassword"</span>,<span class="string">"123456"</span>);</span><br><span class="line">        initParams.put(<span class="string">"allow"</span>,<span class="string">""</span>);<span class="comment">//默认就是允许所有访问</span></span><br><span class="line">        initParams.put(<span class="string">"deny"</span>,<span class="string">"192.168.15.21"</span>);</span><br><span class="line"></span><br><span class="line">        bean.setInitParameters(initParams);</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">//2、配置一个web监控的filter</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> FilterRegistrationBean <span class="title">webStatFilter</span><span class="params">()</span></span>&#123;</span><br><span class="line">        FilterRegistrationBean bean = <span class="keyword">new</span> FilterRegistrationBean();</span><br><span class="line">        bean.setFilter(<span class="keyword">new</span> WebStatFilter());</span><br><span class="line"></span><br><span class="line">        Map&lt;String,String&gt; initParams = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        initParams.put(<span class="string">"exclusions"</span>,<span class="string">"*.js,*.css,/druid/*"</span>);</span><br><span class="line"></span><br><span class="line">        bean.setInitParameters(initParams);</span><br><span class="line"></span><br><span class="line">        bean.setUrlPatterns(Arrays.asList(<span class="string">"/*"</span>));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span>  bean;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3、整合MyBatis"><a href="#3、整合MyBatis" class="headerlink" title="3、整合MyBatis"></a>3、整合MyBatis</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.mybatis.spring.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis-spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">version</span>&gt;</span>1.3.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><img src="../../../../%E8%B5%84%E6%96%99/%E6%BA%90%E7%A0%81%E3%80%81%E8%B5%84%E6%96%99%E3%80%81%E8%AF%BE%E4%BB%B6/%E6%96%87%E6%A1%A3/Spring%20Boot%20%E7%AC%94%E8%AE%B0/images/%E6%90%9C%E7%8B%97%E6%88%AA%E5%9B%BE20180305194443.png" alt></p>
<h3 id="1、注解版"><a href="#1、注解版" class="headerlink" title="1、注解版"></a>1、注解版</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//指定这是一个操作数据库的mapper</span></span><br><span class="line"><span class="meta">@Mapper</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">DepartmentMapper</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Select</span>(<span class="string">"select * from department where id=#&#123;id&#125;"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Department <span class="title">getDeptById</span><span class="params">(Integer id)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Delete</span>(<span class="string">"delete from department where id=#&#123;id&#125;"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">deleteDeptById</span><span class="params">(Integer id)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Options</span>(useGeneratedKeys = <span class="keyword">true</span>,keyProperty = <span class="string">"id"</span>)  <span class="comment">//这是为了获取自动增长的id</span></span><br><span class="line">    <span class="meta">@Insert</span>(<span class="string">"insert into department(departmentName) values(#&#123;departmentName&#125;)"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">insertDept</span><span class="params">(Department department)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Update</span>(<span class="string">"update department set departmentName=#&#123;departmentName&#125; where id=#&#123;id&#125;"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">updateDept</span><span class="params">(Department department)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-自定义配置"><a href="#2-自定义配置" class="headerlink" title="2.自定义配置"></a>2.自定义配置</h3><p>自定义MyBatis的配置规则，给容器中添加一个ConfigurationCustomizer；</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@org</span>.springframework.context.annotation.Configuration</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBatisConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ConfigurationCustomizer <span class="title">configurationCustomizer</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> ConfigurationCustomizer()&#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">customize</span><span class="params">(Configuration configuration)</span> </span>&#123;</span><br><span class="line">                configuration.setMapUnderscoreToCamelCase(<span class="keyword">true</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用MapperScan批量扫描所有的Mapper接口，这样的话我们就不用在Mapper接口上写 @Mapper 注解了。就属于批量的注解了这些注解。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@MapperScan</span>(value = <span class="string">"com.springboot.mapper"</span>)</span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SpringBootMybatisApplication</span> </span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">		SpringApplication.run(SpringBootMybatisApplication.class, args);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3、配置文件版"><a href="#3、配置文件版" class="headerlink" title="3、配置文件版"></a>3、配置文件版</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">mybatis:</span></span><br><span class="line"><span class="attr">  config-location:</span> <span class="attr">classpath:mybatis/mybatis-config.xml</span> <span class="string">指定全局配置文件的位置</span></span><br><span class="line"><span class="attr">  mapper-locations:</span> <span class="attr">classpath:mybatis/mapper/*.xml</span>  <span class="string">指定sql映射文件的位置</span></span><br></pre></td></tr></table></figure>

<p>更多使用参照 <a href="http://www.mybatis.org/spring-boot-starter/mybatis-spring-boot-autoconfigure/" target="_blank" rel="noopener">http://www.mybatis.org/spring-boot-starter/mybatis-spring-boot-autoconfigure/</a></p>
<h2 id="4、整合SpringData-JPA"><a href="#4、整合SpringData-JPA" class="headerlink" title="4、整合SpringData JPA"></a>4、整合SpringData JPA</h2><h3 id="1）、SpringData简介"><a href="#1）、SpringData简介" class="headerlink" title="1）、SpringData简介"></a>1）、SpringData简介</h3><p><img src="../../../../%E8%B5%84%E6%96%99/%E6%BA%90%E7%A0%81%E3%80%81%E8%B5%84%E6%96%99%E3%80%81%E8%AF%BE%E4%BB%B6/%E6%96%87%E6%A1%A3/Spring%20Boot%20%E7%AC%94%E8%AE%B0/images/%E6%90%9C%E7%8B%97%E6%88%AA%E5%9B%BE20180306105412.png" alt></p>
<h3 id="2）、整合SpringData-JPA"><a href="#2）、整合SpringData-JPA" class="headerlink" title="2）、整合SpringData JPA"></a>2）、整合SpringData JPA</h3><p>JPA:ORM（Object Relational Mapping）；</p>
<p>1）、编写一个实体类（bean）和数据表进行映射，并且配置好映射关系；</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用JPA注解配置映射关系</span></span><br><span class="line"><span class="meta">@Entity</span> <span class="comment">//告诉JPA这是一个实体类（和数据表映射的类）</span></span><br><span class="line"><span class="meta">@Table</span>(name = <span class="string">"tbl_user"</span>) <span class="comment">//@Table来指定和哪个数据表对应;如果省略默认表名就是user；</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Id</span> <span class="comment">//这是一个主键</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span>(strategy = GenerationType.IDENTITY)<span class="comment">//自增主键</span></span><br><span class="line">    <span class="keyword">private</span> Integer id;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Column</span>(name = <span class="string">"last_name"</span>,length = <span class="number">50</span>) <span class="comment">//这是和数据表对应的一个列</span></span><br><span class="line">    <span class="keyword">private</span> String lastName;</span><br><span class="line">    <span class="meta">@Column</span> <span class="comment">//省略默认列名就是属性名</span></span><br><span class="line">    <span class="keyword">private</span> String email;</span><br></pre></td></tr></table></figure>

<p>2）、编写一个Dao接口来操作实体类对应的数据表（Repository）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//继承JpaRepository来完成对数据库的操作</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">UserRepository</span> <span class="keyword">extends</span> <span class="title">JpaRepository</span>&lt;<span class="title">User</span>,<span class="title">Integer</span>&gt; </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>3）、基本的配置JpaProperties</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span>  </span><br><span class="line"><span class="attr"> jpa:</span></span><br><span class="line"><span class="attr">    hibernate:</span></span><br><span class="line"><span class="comment">#     更新或者创建数据表结构</span></span><br><span class="line"><span class="attr">      ddl-auto:</span> <span class="string">update</span></span><br><span class="line"><span class="comment">#    控制台显示SQL</span></span><br><span class="line"><span class="attr">    show-sql:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记(十三):运行流程</title>
    <url>/2018/05/24/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E5%8D%81%E4%B8%89%20)%EF%BC%9A%E8%BF%90%E8%A1%8C%E6%B5%81%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-十三-运行流程"><a href="#SpringBoot-笔记-十三-运行流程" class="headerlink" title="SpringBoot 笔记(十三):运行流程"></a>SpringBoot 笔记(十三):运行流程</h1><p>几个重要的事件回调机制</p>
<p>配置在META-INF/spring.factories</p>
<p><strong>ApplicationContextInitializer</strong></p>
<p><strong>SpringApplicationRunListener</strong></p>
<p>只需要放在ioc容器中</p>
<p><strong>ApplicationRunner</strong></p>
<p><strong>CommandLineRunner</strong></p>
<h2 id="1-启动流程："><a href="#1-启动流程：" class="headerlink" title="1.启动流程："></a>1.启动流程：</h2><h3 id="1、创建SpringApplication对象"><a href="#1、创建SpringApplication对象" class="headerlink" title="1、创建SpringApplication对象"></a>1、创建SpringApplication对象</h3><p>先 new 了这个对象，然后调用了他的 run 方法。</p>
<h4 id="1-创建对象"><a href="#1-创建对象" class="headerlink" title="1. 创建对象"></a>1. 创建对象</h4><p>调用了initialize(sources)</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">initialize</span><span class="params">(Object[] sources)</span> </span>&#123;</span><br><span class="line">    <span class="comment">//保存主配置类</span></span><br><span class="line">    <span class="keyword">if</span> (sources != <span class="keyword">null</span> &amp;&amp; sources.length &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">this</span>.sources.addAll(Arrays.asList(sources));</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//判断当前是否一个web应用</span></span><br><span class="line">    <span class="keyword">this</span>.webEnvironment = deduceWebEnvironment();</span><br><span class="line">    <span class="comment">//从类路径下找到META-INF/spring.factories配置的所有ApplicationContextInitializer；然后保存起来</span></span><br><span class="line">    setInitializers((Collection) getSpringFactoriesInstances(</span><br><span class="line">        ApplicationContextInitializer.class));</span><br><span class="line">    <span class="comment">//从类路径下找到ETA-INF/spring.factories配置的所有ApplicationListener</span></span><br><span class="line">    setListeners((Collection) getSpringFactoriesInstances(ApplicationListener.class));</span><br><span class="line">    <span class="comment">//从多个配置类中找到有main方法的主配置类，也就是我们的主类。</span></span><br><span class="line">    <span class="keyword">this</span>.mainApplicationClass = deduceMainApplicationClass();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2、运行run方法"><a href="#2、运行run方法" class="headerlink" title="2、运行run方法"></a>2、运行run方法</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> ConfigurableApplicationContext <span class="title">run</span><span class="params">(String... args)</span> </span>&#123;</span><br><span class="line">   StopWatch stopWatch = <span class="keyword">new</span> StopWatch();</span><br><span class="line">   stopWatch.start();</span><br><span class="line">   ConfigurableApplicationContext context = <span class="keyword">null</span>;</span><br><span class="line">   FailureAnalyzers analyzers = <span class="keyword">null</span>;</span><br><span class="line">   configureHeadlessProperty();</span><br><span class="line">    </span><br><span class="line">   <span class="comment">//获取SpringApplicationRunListeners；从类路径下META-INF/spring.factories</span></span><br><span class="line">   SpringApplicationRunListeners listeners = getRunListeners(args);</span><br><span class="line">    <span class="comment">//回调所有的获取SpringApplicationRunListener.starting()方法</span></span><br><span class="line">   listeners.starting();</span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">       <span class="comment">//封装命令行参数</span></span><br><span class="line">      ApplicationArguments applicationArguments = <span class="keyword">new</span> DefaultApplicationArguments(</span><br><span class="line">            args);</span><br><span class="line">      <span class="comment">//准备环境</span></span><br><span class="line">      ConfigurableEnvironment environment = prepareEnvironment(listeners,</span><br><span class="line">            applicationArguments);</span><br><span class="line">       		<span class="comment">//创建环境完成后回调SpringApplicationRunListener.environmentPrepared()；表示环境准备完成</span></span><br><span class="line">       </span><br><span class="line">      Banner printedBanner = printBanner(environment);</span><br><span class="line">       </span><br><span class="line">       <span class="comment">//创建ApplicationContext；决定创建web的ioc还是普通的ioc</span></span><br><span class="line">      context = createApplicationContext();</span><br><span class="line">       </span><br><span class="line">      analyzers = <span class="keyword">new</span> FailureAnalyzers(context);</span><br><span class="line">       <span class="comment">//准备上下文环境;将environment保存到ioc中；而且applyInitializers()；</span></span><br><span class="line">       <span class="comment">//applyInitializers()：回调之前保存的所有的ApplicationContextInitializer的initialize方法</span></span><br><span class="line">       <span class="comment">//回调所有的SpringApplicationRunListener的contextPrepared()；</span></span><br><span class="line">       <span class="comment">//</span></span><br><span class="line">      prepareContext(context, environment, listeners, applicationArguments,</span><br><span class="line">            printedBanner);</span><br><span class="line">       <span class="comment">//prepareContext运行完成以后回调所有的SpringApplicationRunListener的contextLoaded（）；</span></span><br><span class="line">       </span><br><span class="line">       <span class="comment">//刷新容器；ioc容器初始化（如果是web应用还会创建嵌入式的Tomcat）</span></span><br><span class="line">       <span class="comment">//扫描，创建，加载所有组件的地方；（配置类，组件，自动配置）</span></span><br><span class="line">      refreshContext(context);</span><br><span class="line">       <span class="comment">//从ioc容器中获取所有的ApplicationRunner和CommandLineRunner进行回调</span></span><br><span class="line">       <span class="comment">//ApplicationRunner先回调，CommandLineRunner再回调</span></span><br><span class="line">      afterRefresh(context, applicationArguments);</span><br><span class="line">       <span class="comment">//所有的SpringApplicationRunListener回调finished方法</span></span><br><span class="line">      listeners.finished(context, <span class="keyword">null</span>);</span><br><span class="line">      stopWatch.stop();</span><br><span class="line">      <span class="keyword">if</span> (<span class="keyword">this</span>.logStartupInfo) &#123;</span><br><span class="line">         <span class="keyword">new</span> StartupInfoLogger(<span class="keyword">this</span>.mainApplicationClass)</span><br><span class="line">               .logStarted(getApplicationLog(), stopWatch);</span><br><span class="line">      &#125;</span><br><span class="line">       <span class="comment">//整个SpringBoot应用启动完成以后返回启动的ioc容器；</span></span><br><span class="line">      <span class="keyword">return</span> context;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">      handleRunFailure(context, listeners, analyzers, ex);</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(ex);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="2、事件监听机制"><a href="#2、事件监听机制" class="headerlink" title="2、事件监听机制"></a>2、事件监听机制</h2><p>配置在META-INF/spring.factories</p>
<p><strong>ApplicationContextInitializer</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloApplicationContextInitializer</span> <span class="keyword">implements</span> <span class="title">ApplicationContextInitializer</span>&lt;<span class="title">ConfigurableApplicationContext</span>&gt; </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">initialize</span><span class="params">(ConfigurableApplicationContext applicationContext)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"ApplicationContextInitializer...initialize..."</span>+applicationContext);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>SpringApplicationRunListener</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloSpringApplicationRunListener</span> <span class="keyword">implements</span> <span class="title">SpringApplicationRunListener</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//必须有的构造器</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">HelloSpringApplicationRunListener</span><span class="params">(SpringApplication application, String[] args)</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">starting</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"SpringApplicationRunListener...starting..."</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">environmentPrepared</span><span class="params">(ConfigurableEnvironment environment)</span> </span>&#123;</span><br><span class="line">        Object o = environment.getSystemProperties().get(<span class="string">"os.name"</span>);</span><br><span class="line">        System.out.println(<span class="string">"SpringApplicationRunListener...environmentPrepared.."</span>+o);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">contextPrepared</span><span class="params">(ConfigurableApplicationContext context)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"SpringApplicationRunListener...contextPrepared..."</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">contextLoaded</span><span class="params">(ConfigurableApplicationContext context)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"SpringApplicationRunListener...contextLoaded..."</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">finished</span><span class="params">(ConfigurableApplicationContext context, Throwable exception)</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"SpringApplicationRunListener...finished..."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>配置（META-INF/spring.factories）</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">org.springframework.context.ApplicationContextInitializer</span>=<span class="string">\</span></span><br><span class="line"><span class="attr">com.atguigu.springboot.listener.HelloApplicationContextInitializer</span></span><br><span class="line"></span><br><span class="line"><span class="meta">org.springframework.boot.SpringApplicationRunListener</span>=<span class="string">\</span></span><br><span class="line"><span class="attr">com.atguigu.springboot.listener.HelloSpringApplicationRunListener</span></span><br></pre></td></tr></table></figure>

<p>只需要放在ioc容器中</p>
<p><strong>ApplicationRunner</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloApplicationRunner</span> <span class="keyword">implements</span> <span class="title">ApplicationRunner</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">(ApplicationArguments args)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"ApplicationRunner...run...."</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><strong>CommandLineRunner</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloCommandLineRunner</span> <span class="keyword">implements</span> <span class="title">CommandLineRunner</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">(String... args)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"CommandLineRunner...run..."</span>+ Arrays.asList(args));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-自定义starter"><a href="#3-自定义starter" class="headerlink" title="3.自定义starter"></a>3.自定义starter</h2><p>starter：</p>
<h3 id="1、这个场景需要使用到的依赖"><a href="#1、这个场景需要使用到的依赖" class="headerlink" title="1、这个场景需要使用到的依赖"></a>1、这个场景需要使用到的依赖</h3><h3 id="2、如何编写自动配置"><a href="#2、如何编写自动配置" class="headerlink" title="2、如何编写自动配置"></a>2、如何编写自动配置</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span>  <span class="comment">//指定这个类是一个配置类</span></span><br><span class="line"><span class="meta">@ConditionalOnXXX</span>  <span class="comment">//在指定条件成立的情况下自动配置类生效</span></span><br><span class="line"><span class="meta">@AutoConfigureAfter</span>  <span class="comment">//指定自动配置类的顺序</span></span><br><span class="line"><span class="meta">@Bean</span>  <span class="comment">//给容器中添加组件</span></span><br><span class="line"></span><br><span class="line"><span class="meta">@ConfigurationPropertie</span> <span class="comment">//结合相关xxxProperties类来绑定相关的配置</span></span><br><span class="line"><span class="meta">@EnableConfigurationProperties</span> <span class="comment">//让xxxProperties生效加入到容器中</span></span><br><span class="line"></span><br><span class="line">自动配置类要能加载</span><br><span class="line">将需要启动就加载的自动配置类，配置在META-INF/spring.factories</span><br><span class="line">org.springframework.boot.autoconfigure.EnableAutoConfiguration=\</span><br><span class="line">org.springframework.boot.autoconfigure.admin.SpringApplicationAdminJmxAutoConfiguration,\</span><br><span class="line">org.springframework.boot.autoconfigure.aop.AopAutoConfiguration,\</span><br></pre></td></tr></table></figure>

<h3 id="3、模式："><a href="#3、模式：" class="headerlink" title="3、模式："></a>3、模式：</h3><p>启动器只用来做依赖导入；专门来写一个自动配置模块；</p>
<p>启动器依赖自动配置；别人只需要引入启动器（starter）</p>
<p>mybatis-spring-boot-starter；自定义启动器名-spring-boot-starter</p>
<p>步骤：</p>
<p>1）、启动器模块</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.atguigu.starter<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>atguigu-spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--启动器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!--引入自动配置模块--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.atguigu.starter<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>atguigu-spring-boot-starter-autoconfigurer<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>2）、自动配置模块</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span> <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">   <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">   <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.atguigu.starter<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>atguigu-spring-boot-starter-autoconfigurer<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.0.1-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">packaging</span>&gt;</span>jar<span class="tag">&lt;/<span class="name">packaging</span>&gt;</span></span><br><span class="line"></span><br><span class="line">   <span class="tag">&lt;<span class="name">name</span>&gt;</span>atguigu-spring-boot-starter-autoconfigurer<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;<span class="name">description</span>&gt;</span>Demo project for Spring Boot<span class="tag">&lt;/<span class="name">description</span>&gt;</span></span><br><span class="line"></span><br><span class="line">   <span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.5.10.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">relativePath</span>/&gt;</span> <span class="comment">&lt;!-- lookup parent from repository --&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line"></span><br><span class="line">   <span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">project.build.sourceEncoding</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">project.build.sourceEncoding</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">project.reporting.outputEncoding</span>&gt;</span>UTF-8<span class="tag">&lt;/<span class="name">project.reporting.outputEncoding</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">java.version</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">java.version</span>&gt;</span></span><br><span class="line">   <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br><span class="line"></span><br><span class="line">   <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line"></span><br><span class="line">      <span class="comment">&lt;!--引入spring-boot-starter；所有starter的基本配置--&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">   <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.atguigu.starter;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.context.properties.ConfigurationProperties;</span><br><span class="line"></span><br><span class="line"><span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"atguigu.hello"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloProperties</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String prefix;</span><br><span class="line">    <span class="keyword">private</span> String suffix;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getPrefix</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> prefix;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setPrefix</span><span class="params">(String prefix)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.prefix = prefix;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getSuffix</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> suffix;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setSuffix</span><span class="params">(String suffix)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.suffix = suffix;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.atguigu.starter;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloService</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    HelloProperties helloProperties;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> HelloProperties <span class="title">getHelloProperties</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> helloProperties;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setHelloProperties</span><span class="params">(HelloProperties helloProperties)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.helloProperties = helloProperties;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">sayHellAtguigu</span><span class="params">(String name)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> helloProperties.getPrefix()+<span class="string">"-"</span> +name + helloProperties.getSuffix();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.atguigu.starter;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.autoconfigure.condition.ConditionalOnWebApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.context.properties.EnableConfigurationProperties;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ConditionalOnWebApplication</span> <span class="comment">//web应用才生效</span></span><br><span class="line"><span class="meta">@EnableConfigurationProperties</span>(HelloProperties.class)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloServiceAutoConfiguration</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    HelloProperties helloProperties;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> HelloService <span class="title">helloService</span><span class="params">()</span></span>&#123;</span><br><span class="line">        HelloService service = <span class="keyword">new</span> HelloService();</span><br><span class="line">        service.setHelloProperties(helloProperties);</span><br><span class="line">        <span class="keyword">return</span> service;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="更多SpringBoot整合示例"><a href="#更多SpringBoot整合示例" class="headerlink" title="更多SpringBoot整合示例"></a>更多SpringBoot整合示例</h1><p><a href="https://github.com/spring-projects/spring-boot/tree/master/spring-boot-samples" target="_blank" rel="noopener">https://github.com/spring-projects/spring-boot/tree/master/spring-boot-samples</a></p>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记(十二):数据访问</title>
    <url>/2018/05/24/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E5%8D%81%E4%BA%8C%20)%EF%BC%9A%E6%95%B0%E6%8D%AE%E8%AE%BF%E9%97%AE/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-十二-数据访问"><a href="#SpringBoot-笔记-十二-数据访问" class="headerlink" title="SpringBoot 笔记(十二):数据访问"></a>SpringBoot 笔记(十二):数据访问</h1><h2 id="1、JDBC"><a href="#1、JDBC" class="headerlink" title="1、JDBC"></a>1、JDBC</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-jdbc<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>mysql<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mysql-connector-java<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">scope</span>&gt;</span>runtime<span class="tag">&lt;/<span class="name">scope</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">  datasource:</span></span><br><span class="line"><span class="attr">    username:</span> <span class="string">root</span></span><br><span class="line"><span class="attr">    password:</span> <span class="number">123456</span></span><br><span class="line"><span class="attr">    url:</span> <span class="attr">jdbc:mysql://192.168.15.22:3306/jdbc</span></span><br><span class="line"><span class="attr">    driver-class-name:</span> <span class="string">com.mysql.jdbc.Driver</span></span><br></pre></td></tr></table></figure>

<h3 id="1-默认情况下："><a href="#1-默认情况下：" class="headerlink" title="1.默认情况下："></a>1.默认情况下：</h3><p>​    1. 用org.apache.tomcat.jdbc.pool.DataSource作为数据源；</p>
<p>​    2. 数据源的相关配置都在DataSourceProperties里面；</p>
<h3 id="2-自动配置原理："><a href="#2-自动配置原理：" class="headerlink" title="2.自动配置原理："></a>2.自动配置原理：</h3><p>org.springframework.boot.autoconfigure.jdbc包下就是进行自动配置的包：</p>
<h4 id="1、数据库连接池"><a href="#1、数据库连接池" class="headerlink" title="1、数据库连接池"></a>1、数据库连接池</h4><p>参考 DataSourceConfiguration，根据配置创建数据源，默认使用Tomcat连接池，可以使用spring.datasource.type指定自定义的数据源类型</p>
<h4 id="2、创建数据源"><a href="#2、创建数据源" class="headerlink" title="2、创建数据源"></a>2、创建数据源</h4><p>SpringBoot默认可以支持：<code>org.apache.tomcat.jdbc.pool.DataSource、HikariDataSource、BasicDataSource</code></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Generic DataSource configuration.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@ConditionalOnMissingBean</span>(DataSource.class)</span><br><span class="line"><span class="meta">@ConditionalOnProperty</span>(name = <span class="string">"spring.datasource.type"</span>)</span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Generic</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Bean</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> DataSource <span class="title">dataSource</span><span class="params">(DataSourceProperties properties)</span> </span>&#123;</span><br><span class="line">       <span class="comment">//使用DataSourceBuilder创建数据源，利用反射创建响应type的数据源，并且绑定相关属性</span></span><br><span class="line">      <span class="keyword">return</span> properties.initializeDataSourceBuilder().build();</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="3、DataSourceInitializer：ApplicationListener"><a href="#3、DataSourceInitializer：ApplicationListener" class="headerlink" title="3、DataSourceInitializer：ApplicationListener"></a>3、DataSourceInitializer：ApplicationListener</h4><p>一个监听器，主要用于sql脚本的运行</p>
<p>​    作用：</p>
<p>​        1）、runSchemaScripts();运行建表语句；</p>
<p>​        2）、runDataScripts();运行插入数据的sql语句；</p>
<p>默认只需要将文件命名为： <code>schema-*.sql 或者 data-*.sql</code> 默认规则：<code>schema.sql，schema-all.sql</code></p>
<p>当然我们也可以在配置文件中指定，然后就加载指定的sql 脚本，这里是一个 list 也就是脚本可以指定多个。</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">schema</span>:<span class="string"></span></span><br><span class="line">    <span class="meta">-</span> <span class="string">classpath:department.sql</span></span><br></pre></td></tr></table></figure>

<h2 id="2、整合Druid数据源"><a href="#2、整合Druid数据源" class="headerlink" title="2、整合Druid数据源"></a>2、整合Druid数据源</h2><h3 id="1-配置-yml"><a href="#1-配置-yml" class="headerlink" title="1.配置 yml"></a>1.配置 yml</h3><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">spring.datasource.initialSize</span>:<span class="string">5</span></span><br><span class="line"><span class="meta">spring.datasource.minIdle</span>:<span class="string">5</span></span><br><span class="line"><span class="meta">spring.datasource.maxActive</span>:<span class="string">20</span></span><br><span class="line"><span class="meta">spring.datasource.maxlait</span>:<span class="string">60800</span></span><br><span class="line"><span class="meta">spring.datasource.timeBetweenEvictionRunsMillis</span>:<span class="string">60000</span></span><br><span class="line"><span class="meta">spring.datasource.minEvictableIdleTimeMillis</span>:<span class="string">300000</span></span><br><span class="line"><span class="meta">spring.datasource.validationQuery</span>:<span class="string">SELECT 1 FROM DUAL</span></span><br><span class="line"><span class="meta">spring.datasource.testWhileIdle</span>:<span class="string">true</span></span><br><span class="line"><span class="meta">spring.datasource.testOnBorrow</span>:<span class="string">false</span></span><br><span class="line"><span class="meta">spring.datasource.testOnReturn</span>:<span class="string">false</span></span><br><span class="line"><span class="meta">spring.datasource.poolPreparedStatements</span>:<span class="string">true</span></span><br><span class="line"><span class="comment">#配置监控统计拦截的filters，去掉后监控界面sql无法统计，‘walL“用于防火墙</span></span><br><span class="line"><span class="meta">spring.datasource.filters</span>:<span class="string">stat,wall,log4j</span></span><br><span class="line"><span class="meta">spring.datasource.maxPoolPreparedStatementPerConnectionSize</span>:<span class="string">20</span></span><br><span class="line"><span class="meta">spring.datasource.useGTobalDataSourceStat</span>:<span class="string">true</span></span><br><span class="line"><span class="meta">spring.datasource.connectionProperties</span>:<span class="string">druid.stat.mergeSql=true；druid.stat.slowsqlMillis=5e0</span></span><br></pre></td></tr></table></figure>

<p>上面的配置文件中的数据实际上我们没用上，这里我们需要创建一个 DruidConfig 然后和当前的配置文件绑定。</p>
<h3 id="2-JavaConfig"><a href="#2-JavaConfig" class="headerlink" title="2.JavaConfig"></a>2.JavaConfig</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DruidConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"spring.datasource"</span>)</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> DataSource <span class="title">druid</span><span class="params">()</span></span>&#123;</span><br><span class="line">       <span class="keyword">return</span>  <span class="keyword">new</span> DruidDataSource();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//配置Druid的监控</span></span><br><span class="line">    <span class="comment">//1、配置一个管理后台的Servlet</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ServletRegistrationBean <span class="title">statViewServlet</span><span class="params">()</span></span>&#123;</span><br><span class="line">        ServletRegistrationBean bean = <span class="keyword">new</span> ServletRegistrationBean(<span class="keyword">new</span> StatViewServlet(), <span class="string">"/druid/*"</span>);</span><br><span class="line">        Map&lt;String,String&gt; initParams = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line"></span><br><span class="line">        initParams.put(<span class="string">"loginUsername"</span>,<span class="string">"admin"</span>);</span><br><span class="line">        initParams.put(<span class="string">"loginPassword"</span>,<span class="string">"123456"</span>);</span><br><span class="line">        initParams.put(<span class="string">"allow"</span>,<span class="string">""</span>);<span class="comment">//默认就是允许所有访问</span></span><br><span class="line">        initParams.put(<span class="string">"deny"</span>,<span class="string">"192.168.15.21"</span>);</span><br><span class="line"></span><br><span class="line">        bean.setInitParameters(initParams);</span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">//2、配置一个web监控的filter</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> FilterRegistrationBean <span class="title">webStatFilter</span><span class="params">()</span></span>&#123;</span><br><span class="line">        FilterRegistrationBean bean = <span class="keyword">new</span> FilterRegistrationBean();</span><br><span class="line">        bean.setFilter(<span class="keyword">new</span> WebStatFilter());</span><br><span class="line"></span><br><span class="line">        Map&lt;String,String&gt; initParams = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        initParams.put(<span class="string">"exclusions"</span>,<span class="string">"*.js,*.css,/druid/*"</span>);</span><br><span class="line"></span><br><span class="line">        bean.setInitParameters(initParams);</span><br><span class="line"></span><br><span class="line">        bean.setUrlPatterns(Arrays.asList(<span class="string">"/*"</span>));</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span>  bean;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3、整合MyBatis"><a href="#3、整合MyBatis" class="headerlink" title="3、整合MyBatis"></a>3、整合MyBatis</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.mybatis.spring.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis-spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">version</span>&gt;</span>1.3.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><img src="../../../../%E8%B5%84%E6%96%99/%E6%BA%90%E7%A0%81%E3%80%81%E8%B5%84%E6%96%99%E3%80%81%E8%AF%BE%E4%BB%B6/%E6%96%87%E6%A1%A3/Spring%20Boot%20%E7%AC%94%E8%AE%B0/images/%E6%90%9C%E7%8B%97%E6%88%AA%E5%9B%BE20180305194443.png" alt></p>
<h3 id="1、注解版"><a href="#1、注解版" class="headerlink" title="1、注解版"></a>1、注解版</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//指定这是一个操作数据库的mapper</span></span><br><span class="line"><span class="meta">@Mapper</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">DepartmentMapper</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Select</span>(<span class="string">"select * from department where id=#&#123;id&#125;"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> Department <span class="title">getDeptById</span><span class="params">(Integer id)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Delete</span>(<span class="string">"delete from department where id=#&#123;id&#125;"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">deleteDeptById</span><span class="params">(Integer id)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Options</span>(useGeneratedKeys = <span class="keyword">true</span>,keyProperty = <span class="string">"id"</span>)  <span class="comment">//这是为了获取自动增长的id</span></span><br><span class="line">    <span class="meta">@Insert</span>(<span class="string">"insert into department(departmentName) values(#&#123;departmentName&#125;)"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">insertDept</span><span class="params">(Department department)</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Update</span>(<span class="string">"update department set departmentName=#&#123;departmentName&#125; where id=#&#123;id&#125;"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">updateDept</span><span class="params">(Department department)</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-自定义配置"><a href="#2-自定义配置" class="headerlink" title="2.自定义配置"></a>2.自定义配置</h3><p>自定义MyBatis的配置规则，给容器中添加一个ConfigurationCustomizer；</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@org</span>.springframework.context.annotation.Configuration</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBatisConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ConfigurationCustomizer <span class="title">configurationCustomizer</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> ConfigurationCustomizer()&#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">customize</span><span class="params">(Configuration configuration)</span> </span>&#123;</span><br><span class="line">                configuration.setMapUnderscoreToCamelCase(<span class="keyword">true</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用MapperScan批量扫描所有的Mapper接口，这样的话我们就不用在Mapper接口上写 @Mapper 注解了。就属于批量的注解了这些注解。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@MapperScan</span>(value = <span class="string">"com.springboot.mapper"</span>)</span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SpringBootMybatisApplication</span> </span>&#123;</span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">		SpringApplication.run(SpringBootMybatisApplication.class, args);</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3、配置文件版"><a href="#3、配置文件版" class="headerlink" title="3、配置文件版"></a>3、配置文件版</h3><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">mybatis:</span></span><br><span class="line"><span class="attr">  config-location:</span> <span class="attr">classpath:mybatis/mybatis-config.xml</span> <span class="string">指定全局配置文件的位置</span></span><br><span class="line"><span class="attr">  mapper-locations:</span> <span class="attr">classpath:mybatis/mapper/*.xml</span>  <span class="string">指定sql映射文件的位置</span></span><br></pre></td></tr></table></figure>

<p>更多使用参照 <a href="http://www.mybatis.org/spring-boot-starter/mybatis-spring-boot-autoconfigure/" target="_blank" rel="noopener">http://www.mybatis.org/spring-boot-starter/mybatis-spring-boot-autoconfigure/</a></p>
<h2 id="4、整合SpringData-JPA"><a href="#4、整合SpringData-JPA" class="headerlink" title="4、整合SpringData JPA"></a>4、整合SpringData JPA</h2><h3 id="1）、SpringData简介"><a href="#1）、SpringData简介" class="headerlink" title="1）、SpringData简介"></a>1）、SpringData简介</h3><p><img src="../../../../%E8%B5%84%E6%96%99/%E6%BA%90%E7%A0%81%E3%80%81%E8%B5%84%E6%96%99%E3%80%81%E8%AF%BE%E4%BB%B6/%E6%96%87%E6%A1%A3/Spring%20Boot%20%E7%AC%94%E8%AE%B0/images/%E6%90%9C%E7%8B%97%E6%88%AA%E5%9B%BE20180306105412.png" alt></p>
<h3 id="2）、整合SpringData-JPA"><a href="#2）、整合SpringData-JPA" class="headerlink" title="2）、整合SpringData JPA"></a>2）、整合SpringData JPA</h3><p>JPA:ORM（Object Relational Mapping）；</p>
<p>1）、编写一个实体类（bean）和数据表进行映射，并且配置好映射关系；</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用JPA注解配置映射关系</span></span><br><span class="line"><span class="meta">@Entity</span> <span class="comment">//告诉JPA这是一个实体类（和数据表映射的类）</span></span><br><span class="line"><span class="meta">@Table</span>(name = <span class="string">"tbl_user"</span>) <span class="comment">//@Table来指定和哪个数据表对应;如果省略默认表名就是user；</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Id</span> <span class="comment">//这是一个主键</span></span><br><span class="line">    <span class="meta">@GeneratedValue</span>(strategy = GenerationType.IDENTITY)<span class="comment">//自增主键</span></span><br><span class="line">    <span class="keyword">private</span> Integer id;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Column</span>(name = <span class="string">"last_name"</span>,length = <span class="number">50</span>) <span class="comment">//这是和数据表对应的一个列</span></span><br><span class="line">    <span class="keyword">private</span> String lastName;</span><br><span class="line">    <span class="meta">@Column</span> <span class="comment">//省略默认列名就是属性名</span></span><br><span class="line">    <span class="keyword">private</span> String email;</span><br></pre></td></tr></table></figure>

<p>2）、编写一个Dao接口来操作实体类对应的数据表（Repository）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//继承JpaRepository来完成对数据库的操作</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">UserRepository</span> <span class="keyword">extends</span> <span class="title">JpaRepository</span>&lt;<span class="title">User</span>,<span class="title">Integer</span>&gt; </span>&#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>3）、基本的配置JpaProperties</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span>  </span><br><span class="line"><span class="attr"> jpa:</span></span><br><span class="line"><span class="attr">    hibernate:</span></span><br><span class="line"><span class="comment">#     更新或者创建数据表结构</span></span><br><span class="line"><span class="attr">      ddl-auto:</span> <span class="string">update</span></span><br><span class="line"><span class="comment">#    控制台显示SQL</span></span><br><span class="line"><span class="attr">    show-sql:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记(九):分布式</title>
    <url>/2018/05/23/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E4%B9%9D%20)%EF%BC%9A%E5%88%86%E5%B8%83%E5%BC%8F/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-九-分布式"><a href="#SpringBoot-笔记-九-分布式" class="headerlink" title="SpringBoot 笔记(九):分布式"></a>SpringBoot 笔记(九):分布式</h1><p>我们可以使用 SpringBoot构建分布式应用，也就是我们在开发的时候可以进行多个模块的拆分，每一个功能做一个模块，然后我们使用一些分布式的框架，进行远程调用，所谓的远程调用就是RPC调用，而不是以前的WebService这个形式的.。其实这种分布式的RPC框架有很多，除了我们创常见的Doubbo还有就是我们Spring 项目自带的 SpringCloud 也是类似的东西。<a id="more"></a></p>
<p>器基本理念就是：我们一个模块写好我们的业务逻辑然后，把我们写好的逻辑当作服务发布出去，然后发布出去的具体位置我们称之为注册中心，现在注册中心主要是通过 zookeeper 了来完成的。</p>
<h2 id="1-zookeeper的安装"><a href="#1-zookeeper的安装" class="headerlink" title="1.zookeeper的安装"></a>1.zookeeper的安装</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker pull registry.docker-cn.com/library/zookeeper</span><br><span class="line">docker run -d -p 2181:2181 --name zk-01 bf5cbc9d5cac</span><br></pre></td></tr></table></figure>

<h2 id="2-添加依赖"><a href="#2-添加依赖" class="headerlink" title="2.添加依赖"></a>2.添加依赖</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.zookeeper<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>zookeeper<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.4.6<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">type</span>&gt;</span>pom<span class="tag">&lt;/<span class="name">type</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>dubbo-spring-boot-starter<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.1.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.github.sgroschupf<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>zkclient<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>0.1<span class="tag">&lt;/<span class="name">v</span>	<span class="attr">ersion</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="3-配置文件"><a href="#3-配置文件" class="headerlink" title="3.配置文件"></a>3.配置文件</h2><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">dubbo:</span></span><br><span class="line"><span class="attr">  application:</span></span><br><span class="line"><span class="attr">    name:</span> <span class="string">producer</span></span><br><span class="line"><span class="attr">  registry:</span></span><br><span class="line"><span class="attr">    address:</span> <span class="attr">zookeeper://219.245.18.94:2181</span></span><br><span class="line"><span class="attr">  scan:</span></span><br><span class="line"><span class="attr">    base-packages:</span> <span class="string">com.example.producer.service</span></span><br></pre></td></tr></table></figure>

<h2 id="4-代码"><a href="#4-代码" class="headerlink" title="4.代码"></a>4.代码</h2><h3 id="开启分布式注解"><a href="#开启分布式注解" class="headerlink" title="开启分布式注解"></a>开启分布式注解</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@EnableDubbo</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ProducerApplication</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(ProducerApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="发布服务"><a href="#发布服务" class="headerlink" title="发布服务"></a>发布服务</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.service;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">TickService</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">sell</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.service;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.alibaba.dubbo.config.annotation.Service;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Component;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TickServiceImpl</span> <span class="keyword">implements</span> <span class="title">TickService</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">sell</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"hello dubbo"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意使用的注解，发布服务的时候使用的是阿里的注解。</p>
<h3 id="消费服务"><a href="#消费服务" class="headerlink" title="消费服务"></a>消费服务</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.service;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">TickService</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">sell</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>必须有一样的接口才行。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> com.example.service;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.alibaba.dubbo.config.annotation.Reference;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UserService</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Reference</span></span><br><span class="line">    TickService tickService;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">getTick</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(tickService.sell());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>使用    @Reference 注解注入远程的 服务。</p>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记(八):任务</title>
    <url>/2018/05/22/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E5%85%AB%20)%EF%BC%9A%E4%BB%BB%E5%8A%A1/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-八-任务"><a href="#SpringBoot-笔记-八-任务" class="headerlink" title="SpringBoot 笔记 (八):任务"></a>SpringBoot 笔记 (八):任务</h1><h1 id="1-异步任务"><a href="#1-异步任务" class="headerlink" title="1.异步任务"></a>1.异步任务</h1><h2 id="1-开启异步任务注解"><a href="#1-开启异步任务注解" class="headerlink" title="1.开启异步任务注解"></a>1.开启异步任务注解</h2><p>@EnableAsync</p>
<h2 id="2-对Service层方法开启异步"><a href="#2-对Service层方法开启异步" class="headerlink" title="2.对Service层方法开启异步"></a>2.对Service层方法开启异步</h2><p>@Async</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TaskService</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Async</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">task</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"start"</span>);</span><br><span class="line">        Thread.sleep(<span class="number">3000</span>);</span><br><span class="line">        System.out.println(<span class="string">"end"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@EnableAsync</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TaskApplication</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(TaskApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="2-定时任务"><a href="#2-定时任务" class="headerlink" title="2.定时任务"></a>2.定时任务</h1><h2 id="1-开启调度"><a href="#1-开启调度" class="headerlink" title="1.开启调度"></a>1.开启调度</h2><p>@EnableScheduling</p>
<h2 id="2-Scheduled-cron-“”"><a href="#2-Scheduled-cron-“”" class="headerlink" title="2.@Scheduled(cron = “”)"></a>2.@Scheduled(cron = “”)</h2><p>调度注解，放在Service层即可。</p>
<h2 id="3-cron格式"><a href="#3-cron格式" class="headerlink" title="3.cron格式"></a>3.cron格式</h2><p>秒(0-59)  分(0-59)   时(0-23)  日(1-31)  月(1-12)   周(0-7  其中0/7表示周日)</p>
<p>这几个位置可写的值不仅仅是上面的数字，还可以是表达式：</p>
<ul>
<li>枚举：1,2,3,4</li>
<li>范围：2-5</li>
<li>任意： *</li>
<li>步长： /3  每3步</li>
<li>冲突：？ 当日和星期回冲突的时候</li>
</ul>
<p>一些例子：</p>
<p>1 * * *？ *  2-7  每周的周二到周天的每一分钟的第一秒触发</p>
<p>1-7 * * ？ *  3  每周三的每分钟的1-7秒触发</p>
<p>0  0/5  14,18  ？ *  1-6  每周的1-6 14点和18点 每个五分钟执行一次</p>
<p>0 15 10？* 1-6 每个月的周一至周六10:15分执行一次</p>
<p>0 0 2？* 6 每个月的最后一个周六凌晨2点执行一次</p>
<h1 id="3-邮件发送"><a href="#3-邮件发送" class="headerlink" title="3.邮件发送"></a>3.邮件发送</h1><h2 id="1-配置原理"><a href="#1-配置原理" class="headerlink" title="1.配置原理"></a>1.配置原理</h2><p>配置pom文件</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-mail<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">     <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.sun.mail<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">         <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>javax.mail<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">     <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>查看MailSenderAutoConfiguration的自动配置类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> JavaMailSenderImpl <span class="title">mailSender</span><span class="params">()</span> </span>&#123;</span><br><span class="line">   JavaMailSenderImpl sender = <span class="keyword">new</span> JavaMailSenderImpl();</span><br><span class="line">   <span class="keyword">if</span> (<span class="keyword">this</span>.session != <span class="keyword">null</span>) &#123;</span><br><span class="line">      sender.setSession(<span class="keyword">this</span>.session);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">else</span> &#123;</span><br><span class="line">      applyProperties(sender);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> sender;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到主要就是注入了一个 JavaMailSenderImpl 这个类的实例，然后我们就可以借助这个实例进行发送邮件。</p>
<h2 id="2-配置属性"><a href="#2-配置属性" class="headerlink" title="2.配置属性"></a>2.配置属性</h2><figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">  mail:</span></span><br><span class="line"><span class="attr">    host:</span></span><br><span class="line"><span class="attr">    username:</span></span><br><span class="line"><span class="attr">    password:</span></span><br><span class="line"><span class="attr">    default-encoding:</span> <span class="string">utf-8</span></span><br><span class="line"><span class="attr">    protocol:</span> <span class="string">smtp</span></span><br></pre></td></tr></table></figure>

<h2 id="3-发送邮件"><a href="#3-发送邮件" class="headerlink" title="3.发送邮件"></a>3.发送邮件</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line">JavaMailSenderImpl javaMailSender;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 简单的文本文件的发送</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">senderSimple</span><span class="params">()</span></span>&#123;</span><br><span class="line">    SimpleMailMessage message = <span class="keyword">new</span> SimpleMailMessage();</span><br><span class="line">    message.setSubject(<span class="string">"开会"</span>);</span><br><span class="line">    message.setText(<span class="string">"你好开会"</span>);</span><br><span class="line">    message.setFrom(<span class="string">"lwenxu"</span>);</span><br><span class="line">    message.setTo(<span class="string">"xpf199741@outlook.com"</span>);</span><br><span class="line">    javaMailSender.send(message);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">senderMulti</span><span class="params">()</span> <span class="keyword">throws</span> MessagingException </span>&#123;</span><br><span class="line">    MimeMessage mimeMessage = javaMailSender.createMimeMessage();</span><br><span class="line">    MimeMessageHelper mimeMessageHelper = <span class="keyword">new</span> MimeMessageHelper(mimeMessage, <span class="keyword">true</span>);</span><br><span class="line"></span><br><span class="line">    mimeMessageHelper.setSubject(<span class="string">"开会"</span>);</span><br><span class="line">    mimeMessageHelper.setText(<span class="string">"你好开会"</span>);</span><br><span class="line">    mimeMessageHelper.setFrom(<span class="string">"lwenxu"</span>);</span><br><span class="line">    mimeMessageHelper.setTo(<span class="string">"11@qq.com"</span>);</span><br><span class="line">    mimeMessageHelper.addAttachment(<span class="string">"1.jpg"</span>, <span class="keyword">new</span> File(<span class="string">"C:\\asd.jpg"</span>));</span><br><span class="line">    </span><br><span class="line">    javaMailSender.send(mimeMessage);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记(七):搜索</title>
    <url>/2018/05/21/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E4%B8%83%20)%EF%BC%9A%E6%90%9C%E7%B4%A2/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-七-搜索"><a href="#SpringBoot-笔记-七-搜索" class="headerlink" title="SpringBoot 笔记 (七):搜索"></a>SpringBoot 笔记 (七):搜索</h1><p>这里我们的搜索就是用 ElasticSearch 这个工具，这个其实是在 Lauce 的基础上构建的一个搜索引擎。</p>
<h1 id="1-ES入门"><a href="#1-ES入门" class="headerlink" title="1. ES入门"></a>1. ES入门</h1><h2 id="1-docker安装"><a href="#1-docker安装" class="headerlink" title="1.docker安装"></a>1.docker安装</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker pull registry.docker-cn.com/library/elasticsearch</span><br><span class="line">docker run -e ES_JAVA_OPTS=<span class="string">"-Xms256m -Xmx256m"</span> -d -p 9200:9200 -p 9300:9300 --name ES_dev</span><br></pre></td></tr></table></figure>

<p>这里指定了es的初始的java堆大小和最大堆大小，否则就是默认的 2G 。</p>
<h2 id="2-基本概念"><a href="#2-基本概念" class="headerlink" title="2.基本概念"></a>2.基本概念</h2><p>ES 是一个面向文档的数据库，他的文档的格式就是JSON的格式。</p>
<h3 id="1-索引"><a href="#1-索引" class="headerlink" title="1.索引"></a>1.索引</h3><p>这个索引指的是动词，也就是我们把数据存放到ES中的一个过程。</p>
<h3 id="2-索引"><a href="#2-索引" class="headerlink" title="2.索引"></a>2.索引</h3><p>这个索引是名词，指的是我们的ES中的一个数据库。</p>
<h3 id="3-类型"><a href="#3-类型" class="headerlink" title="3.类型"></a>3.类型</h3><p>这个就相当与我们的一个表</p>
<h3 id="4-文档"><a href="#4-文档" class="headerlink" title="4.文档"></a>4.文档</h3><p>就是我们的数据记录。</p>
<p>简单的可以用一张表来表示：</p>
<p><img src="https://user-images.githubusercontent.com/22151420/40353449-1ffca3e6-5de4-11e8-8b83-2b396388fc6b.png" alt="image"></p>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记 ( 六)：消息</title>
    <url>/2018/05/20/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E5%85%AD%20)%EF%BC%9A%E6%B6%88%E6%81%AF/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-六-消息"><a href="#SpringBoot-笔记-六-消息" class="headerlink" title="SpringBoot 笔记 (六): 消息"></a>SpringBoot 笔记 (六): 消息</h1><h1 id="1-基本概念"><a href="#1-基本概念" class="headerlink" title="1.基本概念"></a>1.基本概念</h1><h2 id="1-应用场景"><a href="#1-应用场景" class="headerlink" title="1.应用场景"></a>1.应用场景</h2><a id="more"></a>

<p><img src="https://user-images.githubusercontent.com/22151420/40310511-a4d4be00-5d3f-11e8-9e30-0014fd99d851.png" alt="image"></p>
<p><img src="https://user-images.githubusercontent.com/22151420/40310554-beba6612-5d3f-11e8-99da-35ff7787e56f.png" alt="image"></p>
<h2 id="2-重要概念"><a href="#2-重要概念" class="headerlink" title="2.重要概念"></a>2.重要概念</h2><ul>
<li>消息代理（broker）：消息队列服务器</li>
<li>目的地：消息消费者</li>
</ul>
<h3 id="1-消息队列的两种目的地："><a href="#1-消息队列的两种目的地：" class="headerlink" title="1.消息队列的两种目的地："></a>1.消息队列的两种目的地：</h3><ul>
<li>队列：点对点的通讯，这种就是消息生产者把消息发送到消息队列中，然后消息接受者去获取消息，获取后<strong>这个消息就被移除</strong>，消息的接受者可以有多个也就是可以有多个消费者，但是注意<strong>一个消息只能被其中的一个消费者消费</strong>。</li>
<li>主题：发布订阅通讯，广播形式。所有的接受者都可以收到消息。</li>
</ul>
<h3 id="2-JMS"><a href="#2-JMS" class="headerlink" title="2.JMS"></a>2.JMS</h3><p>这是一个基于JVM的消息代理的规范，但是他只是一个规范真正的实现则是 ActiveMQ和HornetMQ等等</p>
<h3 id="3-AMQP"><a href="#3-AMQP" class="headerlink" title="3.AMQP"></a>3.AMQP</h3><p>高级消息队列，他也是消息队列的规范但是他是兼容JMS的，他的具体实现就是我们常常听到的RabbitMQ</p>
<h3 id="4-JMS和AMQP对比"><a href="#4-JMS和AMQP对比" class="headerlink" title="4.JMS和AMQP对比"></a>4.JMS和AMQP对比</h3><p><img src="https://user-images.githubusercontent.com/22151420/40310950-fe85a152-5d40-11e8-9304-fb2f976a8a97.png" alt="image"></p>
<h3 id="5-Spring支持"><a href="#5-Spring支持" class="headerlink" title="5.Spring支持"></a>5.Spring支持</h3><p>spring-jms提供了对JMS的支持<br>spring-rabbit提供了对AMQP的支持<br>需要ConnectionFactory的实现来连接消息代理<br>提供JmsTemplate、Rabbit Template来发送消息<br>@JmsListener(JMS）、@RabbitListener(AMQP)注解在方法上监听消息代理发<br>布的消息<br>@EnableJms、@EnableRabbit开启支持</p>
<h3 id="6-Spring-Boot自动配置"><a href="#6-Spring-Boot自动配置" class="headerlink" title="6.Spring Boot自动配置"></a>6.Spring Boot自动配置</h3><p>JmsAutoConfiguration<br>RabbitAutoConfiguration</p>
<h3 id="7-RabbitMQ基本结构"><a href="#7-RabbitMQ基本结构" class="headerlink" title="7.RabbitMQ基本结构"></a>7.RabbitMQ基本结构</h3><p><img src="https://user-images.githubusercontent.com/22151420/40311287-f1cd9932-5d41-11e8-849b-952b3d650a87.png" alt="image"></p>
<p>首先我们产生消息的我们叫做 <code>生产者(Publisher)</code> 然后我们生产者会把消息发送给 我们的<code>消息服务器(Broker)</code> 中的一个<code>虚拟主机(Virtual Host)</code>，虚拟主机中有一个专门用来接受生产者消息的组件就是 <code>交换机Exchange</code> ，在接收到消息以后我们的路由器就和我们真正的某条 <code>消息队列Queue</code> 通过路由键连接起来，这里的消息队列有多个，然后消息队列与路由器的链接也是多对多的。最后就是我们的消费者去消息队列中消费消息，采用的就是建立TCP链接，但是我们为了节省资源开销，采用了一条TCP的多路复用，也就是在一个TCP中开辟了多个信道来传输数据。</p>
<h3 id="8-Exchange类型"><a href="#8-Exchange类型" class="headerlink" title="8.Exchange类型"></a>8.Exchange类型</h3><ul>
<li>direct 直连模式，在我们消息带过来的类型和消息键完全匹配的时候我们直接转发到对应的队列  —- 点对点</li>
<li>fanout 广播模式，对所有的消息都会广播到每一个队列中，这是最快的 —- 广播</li>
<li>topic 模式匹配方式，单词级别的匹配，<strong>消息的消费者使用的</strong>   ‘# ‘表示匹配0个或者多个单词  ’*‘ 表示匹配一个单词</li>
</ul>
<h1 id="2-RabbitMQ"><a href="#2-RabbitMQ" class="headerlink" title="2.RabbitMQ"></a>2.RabbitMQ</h1><h2 id="1-docker安装RabbitMQ"><a href="#1-docker安装RabbitMQ" class="headerlink" title="1.docker安装RabbitMQ"></a>1.docker安装RabbitMQ</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">docker pull registry.docker-cn.com/library/rabbitmq:3.7-management   #获取具有web管理面板的镜像+使用加速</span><br><span class="line">docker run -d -p 5672:5672 -p 8002:15672 --name rabbitmq_dev c51d1c73d028</span><br></pre></td></tr></table></figure>

<h2 id="2-管理界面"><a href="#2-管理界面" class="headerlink" title="2.管理界面"></a>2.管理界面</h2><p>输入 ip:15672 输入 guest/guest 登录进入管理界面</p>
<h2 id="3-SpringBoot整合RabbitMQ"><a href="#3-SpringBoot整合RabbitMQ" class="headerlink" title="3.SpringBoot整合RabbitMQ"></a>3.SpringBoot整合RabbitMQ</h2><h3 id="1-添加starter"><a href="#1-添加starter" class="headerlink" title="1.添加starter"></a>1.添加starter</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-amqp<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-自动配置原理"><a href="#2-自动配置原理" class="headerlink" title="2.自动配置原理"></a>2.自动配置原理</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ConditionalOnClass</span>(&#123; RabbitTemplate.class, Channel.class &#125;)</span><br><span class="line"><span class="meta">@EnableConfigurationProperties</span>(RabbitProperties.class)</span><br><span class="line"><span class="meta">@Import</span>(RabbitAnnotationDrivenConfiguration.class)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RabbitAutoConfiguration</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Configuration</span></span><br><span class="line">    <span class="meta">@ConditionalOnMissingBean</span>(ConnectionFactory.class)</span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">RabbitConnectionFactoryCreator</span> </span>&#123;</span><br><span class="line">         <span class="meta">@Bean</span></span><br><span class="line">		<span class="function"><span class="keyword">public</span> CachingConnectionFactory <span class="title">rabbitConnectionFactory</span><span class="params">(RabbitProperties config)</span></span>&#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">       </span><br><span class="line">    <span class="meta">@Configuration</span></span><br><span class="line">    <span class="meta">@Import</span>(RabbitConnectionFactoryCreator.class)</span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">RabbitTemplateConfiguration</span></span>&#123;</span><br><span class="line">         <span class="meta">@Bean</span></span><br><span class="line">		<span class="meta">@ConditionalOnSingleCandidate</span>(ConnectionFactory.class)</span><br><span class="line">		<span class="meta">@ConditionalOnMissingBean</span>(RabbitTemplate.class)</span><br><span class="line">		<span class="function"><span class="keyword">public</span> RabbitTemplate <span class="title">rabbitTemplate</span><span class="params">(ConnectionFactory connectionFactory)</span> </span>&#123;&#125;</span><br><span class="line">        </span><br><span class="line">         <span class="meta">@Bean</span></span><br><span class="line">		<span class="meta">@ConditionalOnSingleCandidate</span>(ConnectionFactory.class)</span><br><span class="line">		<span class="meta">@ConditionalOnProperty</span>(prefix=<span class="string">"spring.rabbitmq"</span>, name = <span class="string">"dynamic"</span>, matchIfMissing = <span class="keyword">true</span>)</span><br><span class="line">		<span class="meta">@ConditionalOnMissingBean</span>(AmqpAdmin.class)</span><br><span class="line">		<span class="function"><span class="keyword">public</span> AmqpAdmin <span class="title">amqpAdmin</span><span class="params">(ConnectionFactory connectionFactory)</span> </span>&#123;&#125;</span><br><span class="line">			</span><br><span class="line">    &#125;</span><br><span class="line">        </span><br><span class="line">    <span class="meta">@Configuration</span></span><br><span class="line">    <span class="meta">@ConditionalOnClass</span>(RabbitMessagingTemplate.class)</span><br><span class="line">    <span class="meta">@ConditionalOnMissingBean</span>(RabbitMessagingTemplate.class)</span><br><span class="line">    <span class="meta">@Import</span>(RabbitTemplateConfiguration.class)</span><br><span class="line">    <span class="keyword">protected</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">MessagingTemplateConfiguration</span> </span>&#123;</span><br><span class="line">         <span class="meta">@Bean</span></span><br><span class="line">		<span class="meta">@ConditionalOnSingleCandidate</span>(RabbitTemplate.class)</span><br><span class="line">		<span class="keyword">public</span> RabbitMessagingTemplate rabbitMessagingTemplate&#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到我们整个的自动配置类其实是我们的三个自动配置类的组合，在这个配置类里面我们配置了链接工厂，以及两个供我们便捷操作RabbitMQ的两个Template 这个和 redis 以及 jdbc 的 template 非常的类似。</p>
<h4 id="1-rabbitConnectionFactory"><a href="#1-rabbitConnectionFactory" class="headerlink" title="1.rabbitConnectionFactory"></a>1.rabbitConnectionFactory</h4><p>这个bean就是配置了他的配置信息，包括地址端口什么的，这些可以看到是从我们的 <code>RabbitProperties</code> 中获取的，也就是 <code>@EnableConfigurationProperties(RabbitProperties.class)</code> 导入的 properties ，我们点进去就可以看到它对应的配置项以及关联的配置文件的前缀。其中前缀就是 “spring.rabbitmq” 下面就是具体的配置了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"spring.rabbitmq"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RabbitProperties</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String host = <span class="string">"localhost"</span>;</span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">int</span> port = <span class="number">5672</span>;</span><br><span class="line">	<span class="keyword">private</span> String username;</span><br><span class="line">	<span class="comment">//....</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>那么我们就在配置文件中填写配置项：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">  rabbitmq:</span></span><br><span class="line"><span class="attr">    host:</span> <span class="number">219.245</span><span class="number">.18</span><span class="number">.94</span></span><br><span class="line"><span class="attr">    username:</span> <span class="string">guest</span></span><br><span class="line"><span class="attr">    password:</span> <span class="string">guest</span></span><br><span class="line"><span class="attr">    virtual-host:</span> <span class="string">/</span></span><br><span class="line"><span class="attr">    port:</span> <span class="number">5672</span></span><br></pre></td></tr></table></figure>

<h4 id="2-rabbitTemplate"><a href="#2-rabbitTemplate" class="headerlink" title="2.rabbitTemplate"></a>2.rabbitTemplate</h4><p>这个就是操作RabbitMQ的接口组件。</p>
<h4 id="3-amqpAdmin"><a href="#3-amqpAdmin" class="headerlink" title="3.amqpAdmin"></a>3.amqpAdmin</h4><p>而这个是RabbitMQ的系统管理组件</p>
<h4 id="4-rabbitMessagingTemplate"><a href="#4-rabbitMessagingTemplate" class="headerlink" title="4.rabbitMessagingTemplate"></a>4.rabbitMessagingTemplate</h4><p>消息相关的操作接口。</p>
<h3 id="3-简单的API操作"><a href="#3-简单的API操作" class="headerlink" title="3.简单的API操作"></a>3.简单的API操作</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">rabbitSend</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">//自定义消息头和消息体形式</span></span><br><span class="line">    rabbitTemplate.send(<span class="string">"lwen.direct"</span>,</span><br><span class="line">            <span class="string">"lwen.q1"</span>,</span><br><span class="line">            <span class="keyword">new</span> Message(<span class="string">"hello"</span>.getBytes(),<span class="keyword">new</span> MessageProperties()));</span><br><span class="line"></span><br><span class="line">    <span class="comment">//简单一点直接传输消息体，并且自动进行序列化  对象默认使用jdk序列化</span></span><br><span class="line">    HashMap&lt;String, Object&gt; msg = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">    msg.put(<span class="string">"m1"</span>, <span class="string">"hello"</span>);</span><br><span class="line">    msg.put(<span class="string">"m2"</span>, Arrays.asList(<span class="string">"hello"</span>, <span class="string">"world"</span>));</span><br><span class="line">    rabbitTemplate.convertAndSend(<span class="string">"lwen.direct"</span>,</span><br><span class="line">            <span class="string">"lwen.q1"</span>,</span><br><span class="line">            msg);</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">receiveMsg</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">//获取消息进行反序列化</span></span><br><span class="line">    Object q1 = rabbitTemplate.receiveAndConvert(<span class="string">"q1"</span>);</span><br><span class="line">    System.out.println(q1.getClass());</span><br><span class="line">    System.out.println(q1);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面就是简单的读写消息队列的操作。</p>
<p>以上我们看到的是单播的例子，其实广播以及模式匹配我们只是使用了不同的exchange就能达到目的。</p>
<h3 id="4-配置序列化规则"><a href="#4-配置序列化规则" class="headerlink" title="4.配置序列化规则"></a>4.配置序列化规则</h3><p>可以看到上面的自动序列化出来的东西我们是没办法看的，我们更希望使用json等序列化工具。我们看一下他的序列化的配置。</p>
<p>在 <code>public RabbitTemplate rabbitTemplate()</code> 方法中 可以看到 <code>MessageConverter messageConverter = this.messageConverter.getIfUnique();</code> 就是获取消息转换的工具。</p>
<p><img src="https://user-images.githubusercontent.com/22151420/40348682-f21c0582-5dd6-11e8-9b7e-cf67afe255cd.png" alt="image"></p>
<p>可以看到有这么多 MessageConverter 的具体实现，我们就是使用json的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AMQPConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> MessageConverter <span class="title">messageConverter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Jackson2JsonMessageConverter();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>之后我们可以在RabbitMQ的管理面板上看到这种格式的数据了，而不是二进制：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;<span class="attr">"m1"</span>:<span class="string">"hello"</span>,<span class="attr">"m2"</span>:[<span class="string">"hello"</span>,<span class="string">"world"</span>]&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-消息监听"><a href="#5-消息监听" class="headerlink" title="5.消息监听"></a>5.消息监听</h3><p>我们对一些应用解耦的话我们就需要使用消息队列，那么消息队列就需要有通知的功能，这里我们只需要两个注解就能搞定这个问题。</p>
<ol>
<li>首先我们需要开启RabbitMQ的注解 @EnableRabbit</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="meta">@EnableRabbit</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BootMqApplication</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        SpringApplication.run(BootMqApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol>
<li>然后我们对接受消息的方法加上@RabbitListener注解进行监听</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BookService</span> </span>&#123;</span><br><span class="line">    <span class="meta">@RabbitListener</span>(queues = <span class="string">"q1"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">bookReceive</span><span class="params">(Book book)</span></span>&#123;</span><br><span class="line">        System.out.println(book);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="6-amqpAdmin"><a href="#6-amqpAdmin" class="headerlink" title="6.amqpAdmin"></a>6.amqpAdmin</h3><p>前面我们看到在自动配置的时候他们帮我们创建了一个 amqpAdmin 这个组件，然后我们是可以拿这个组件进行创建路由、队列、以及绑定规则，也就是我们在管理面板上能完成的操作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line">AmqpAdmin amqpAdmin</span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">rabbitAdmins</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="comment">//创建路由  DirectExchange 是Exchange的实现类  类自己找  有很多</span></span><br><span class="line">    amqpAdmin.declareExchange(<span class="keyword">new</span> DirectExchange(<span class="string">"amqpAdmin.exchange"</span>));</span><br><span class="line">    <span class="comment">//创建队列</span></span><br><span class="line">    amqpAdmin.declareQueue(<span class="keyword">new</span> Queue(<span class="string">"amqpAdmin.queue"</span>));</span><br><span class="line">    <span class="comment">//绑定</span></span><br><span class="line">    amqpAdmin.declareBinding(<span class="keyword">new</span> Binding(<span class="string">"amqpAdmin.queue"</span>,</span><br><span class="line">            Binding.DestinationType.QUEUE,</span><br><span class="line">            <span class="string">"amqpAdmin.exchange"</span>,</span><br><span class="line">            <span class="string">"amqp.key"</span>,</span><br><span class="line">            <span class="keyword">null</span>));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记 ( 五 )：缓存</title>
    <url>/2018/05/20/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E4%BA%94)%EF%BC%9A%E7%BC%93%E5%AD%98/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-五-缓存"><a href="#SpringBoot-笔记-五-缓存" class="headerlink" title="SpringBoot 笔记 (五): 缓存"></a>SpringBoot 笔记 (五): 缓存</h1><h2 id="1-JSR107介绍"><a href="#1-JSR107介绍" class="headerlink" title="1.JSR107介绍"></a>1.JSR107介绍</h2><h3 id="1-核心概念"><a href="#1-核心概念" class="headerlink" title="1.核心概念"></a>1.核心概念</h3><p>JSR107这个规范有五个核心概念分别是：</p>
<ul>
<li>CacheProvider  它用来管理缓存的Manager也就是用来创建，删除，管理，配置CacheManager 的</li>
<li>CacheManager 是用来管理各个缓存，创建，删除，管理，配置Cache的</li>
<li>Cache 是各个具体的缓存组件</li>
<li>Entry 是缓存中具体的一个缓存项</li>
<li>Expire 缓存的过期时间</li>
</ul>
<a id="more"></a>

<h3 id="2-导入包"><a href="#2-导入包" class="headerlink" title="2.导入包"></a>2.导入包</h3><ul>
<li>javax.cache.cache-api</li>
<li>javax.cache.cache</li>
</ul>
<h2 id="2-Spring缓存抽象"><a href="#2-Spring缓存抽象" class="headerlink" title="2.Spring缓存抽象"></a>2.Spring缓存抽象</h2><p>spring保留了两个核心的概念就是 CacheManager 和 Cache 用来支持并简化 JSR107 技术。支持JSR107的注解，以及自带一些注解。</p>
<p><a href="https://imgchr.com/i/CgBCND" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2018/05/20/CgBCND.md.png" alt="CgBCND.md.png"></a></p>
<p>这几个注解其实都是标注在方法上面，主要说一些这几个注解：</p>
<ol>
<li>Cacheable 就是标注该方法在执行查询的结果是被缓存起来的，一般来说就是在dao的select的时候调用。</li>
<li>CacheEvict 这个一般放在删除某些数据的时候我们必须清空缓存让缓存失效</li>
<li>CachePut 这个住哟啊用于更新操作，不然的话缓存无法同步更新。</li>
<li>EnableCache 就是开启注解。</li>
</ol>
<h2 id="3-搭建项目工程"><a href="#3-搭建项目工程" class="headerlink" title="3.搭建项目工程"></a>3.搭建项目工程</h2><h3 id="1-给主类加上-CacheEnable"><a href="#1-给主类加上-CacheEnable" class="headerlink" title="1.给主类加上@CacheEnable"></a>1.给主类加上@CacheEnable</h3><h3 id="2-给方法加上-Cacheable"><a href="#2-给方法加上-Cacheable" class="headerlink" title="2.给方法加上@Cacheable"></a>2.给方法加上@Cacheable</h3><p>将方法的运行结果进行缓存；以后再要相同的数据，直接从缓存中获取，不用调用方法；<br>CacheManager管理多个cache组件的，对缓存的真正CRUD操作在cache组件中，每一个缓存组件有自己唯一一个名字；<br>几个属性：</p>
<ol>
<li>cacheNames/value：指定缓存组件的名字</li>
<li>key：缓存数据使用的key；可以用它来指定。默认是使用方法参数的值1-方法的返回值</li>
<li>编写SpEL；#id；参数id的值#ae#pe#root.args[e] <img src="https://user-images.githubusercontent.com/22151420/40279172-60c42a22-5c70-11e8-8768-b70619b6cb65.png" alt="image"></li>
<li>keyGenerator:key的生成器；可以自己指定key的生成器的组件id  注意：key/keyGenerator：二选一使用</li>
<li>cacheManager：指定缓存管理器；或者cacheResolver指定获取解析器</li>
<li>condition：指定符合条件的情况下才缓存；</li>
<li>unLess：否定缓存；当unLess指定的条件为true，方法的返回值就不会被缓存；可以获取到结果进行判断</li>
<li>unless=”#result==null”</li>
<li>sync：是否使用用异步</li>
</ol>
<p>[^针对dao包开启sql调试日志]: logging:  level:    com:      example:        bootcache:          dao: debug</p>
<h3 id="3-自动配置原理"><a href="#3-自动配置原理" class="headerlink" title="3.自动配置原理"></a>3.自动配置原理</h3><h4 id="1-CacheAutoConfiguration-自动配置类"><a href="#1-CacheAutoConfiguration-自动配置类" class="headerlink" title="1. CacheAutoConfiguration 自动配置类"></a>1. CacheAutoConfiguration 自动配置类</h4><h4 id="2-Import-CacheAutoConfiguration-CacheConfigurationImportSelector-class-导入了自动配置选择类"><a href="#2-Import-CacheAutoConfiguration-CacheConfigurationImportSelector-class-导入了自动配置选择类" class="headerlink" title="2.@Import({CacheAutoConfiguration.CacheConfigurationImportSelector.class}) 导入了自动配置选择类"></a>2.@Import({CacheAutoConfiguration.CacheConfigurationImportSelector.class}) 导入了自动配置选择类</h4><h4 id="3-CacheConfigurationImportSelector-selectImports-方法导入依赖"><a href="#3-CacheConfigurationImportSelector-selectImports-方法导入依赖" class="headerlink" title="3. CacheConfigurationImportSelector.selectImports() 方法导入依赖"></a>3. CacheConfigurationImportSelector.selectImports() 方法导入依赖</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="string">"org.springframework.boot.autoconfigure.cache.GenericCacheConfiguration"</span></span><br><span class="line"><span class="string">"org.springframework.boot.autoconfigure.cache.JCacheCacheConfiguration"</span></span><br><span class="line"><span class="string">"org.springframework.boot.autoconfigure.cache.EhCacheCacheConfiguration"</span></span><br><span class="line"><span class="string">"org.springframework.boot.autoconfigure.cache.HazelcastCacheConfiguration"</span></span><br><span class="line"><span class="string">"org.springframework.boot.autoconfigure.cache.InfinispanCacheConfiguration"</span></span><br><span class="line"><span class="string">"org.springframework.boot.autoconfigure.cache.CouchbaseCacheConfiguration"</span></span><br><span class="line"><span class="string">"org.springframework.boot.autoconfigure.cache.RedisCacheConfiguration"</span></span><br><span class="line"><span class="string">"org.springframework.boot.autoconfigure.cache.CaffeineCacheConfiguration"</span></span><br><span class="line"><span class="string">"org.springframework.boot.autoconfigure.cache.SimpleCacheConfiguration"</span></span><br><span class="line"><span class="string">"org.springframework.boot.autoconfigure.cache.NoOpCacheConfiguration</span></span><br></pre></td></tr></table></figure>

<h4 id="4-具体哪一个配置类会生效就看我们这些配置类的condition"><a href="#4-具体哪一个配置类会生效就看我们这些配置类的condition" class="headerlink" title="4.具体哪一个配置类会生效就看我们这些配置类的condition"></a>4.具体哪一个配置类会生效就看我们这些配置类的condition</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ConditionalOnClass</span>(&#123;Cache.class, EhCacheCacheManager.class&#125;)</span><br><span class="line"><span class="meta">@ConditionalOnMissingBean</span>(&#123;CacheManager.class&#125;)</span><br><span class="line"><span class="meta">@Conditional</span>(&#123;CacheCondition.class, EhCacheCacheConfiguration.ConfigAvailableCondition.class&#125;)</span><br></pre></td></tr></table></figure>

<h4 id="5-我们可以打开调试，那么再启动的时候就会打印出我们自动配置的日志-（debug-true）"><a href="#5-我们可以打开调试，那么再启动的时候就会打印出我们自动配置的日志-（debug-true）" class="headerlink" title="5.我们可以打开调试，那么再启动的时候就会打印出我们自动配置的日志 （debug: true）"></a>5.我们可以打开调试，那么再启动的时候就会打印出我们自动配置的日志 （debug: true）</h4><p> 搜索CacheConfiguration 可以看到</p>
<figure class="highlight tex"><table><tr><td class="code"><pre><span class="line">SimpleCacheConfiguration matched:</span><br><span class="line">   - Cache org.springframework.boot.autoconfigure.cache.SimpleCacheConfiguration automatic cache type (CacheCondition)</span><br><span class="line">   - @ConditionalOnMissingBean (types: org.springframework.cache.CacheManager; SearchStrategy: all) did not find any beans (OnBeanCondition)</span><br></pre></td></tr></table></figure>

<p>那么也就是说我们没配置的时候默认是 Simple 的Cache</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> ConcurrentMapCacheManager <span class="title">cacheManager</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    ConcurrentMapCacheManager cacheManager = <span class="keyword">new</span> ConcurrentMapCacheManager();</span><br><span class="line">    List&lt;String&gt; cacheNames = <span class="keyword">this</span>.cacheProperties.getCacheNames();</span><br><span class="line">    <span class="keyword">if</span> (!cacheNames.isEmpty()) &#123;</span><br><span class="line">        cacheManager.setCacheNames(cacheNames);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> (ConcurrentMapCacheManager)<span class="keyword">this</span>.customizerInvoker.customize(cacheManager);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>默认返回的是 ConcurrentMapCacheManager 这个CacheManager  给容器中注册缓存管理器。</p>
<h4 id="6-ConcurrentMapCacheManager-分析"><a href="#6-ConcurrentMapCacheManager-分析" class="headerlink" title="6. ConcurrentMapCacheManager 分析"></a>6. ConcurrentMapCacheManager 分析</h4><p>这个缓存管理器的主要作用分析一下，其他的基本都类似：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ConcurrentMapCacheManager</span> <span class="keyword">implements</span> <span class="title">CacheManager</span>｛  //所有的缓存管理器都继承了这个<span class="title">CacheManager</span></span></span><br><span class="line"><span class="class">    //里面最重要的方法就是 <span class="title">getCache</span> 方法  这个方法就是创建和获取缓存组件！！组件</span></span><br><span class="line"><span class="class">     @<span class="title">Nullable</span></span></span><br><span class="line"><span class="class">    <span class="title">public</span> <span class="title">Cache</span> <span class="title">getCache</span>(<span class="title">String</span> <span class="title">name</span>) </span>&#123;</span><br><span class="line">        Cache cache = (Cache)<span class="keyword">this</span>.cacheMap.get(name);</span><br><span class="line">        <span class="keyword">if</span> (cache == <span class="keyword">null</span> &amp;&amp; <span class="keyword">this</span>.dynamic) &#123;</span><br><span class="line">            ConcurrentMap var3 = <span class="keyword">this</span>.cacheMap;</span><br><span class="line">            <span class="keyword">synchronized</span>(<span class="keyword">this</span>.cacheMap) &#123;</span><br><span class="line">                cache = (Cache)<span class="keyword">this</span>.cacheMap.get(name);</span><br><span class="line">                <span class="keyword">if</span> (cache == <span class="keyword">null</span>) &#123;</span><br><span class="line">                    cache = <span class="keyword">this</span>.createConcurrentMapCache(name); <span class="comment">//创建缓存其实就是new ConcurrentHashMap(256)</span></span><br><span class="line">                    <span class="keyword">this</span>.cacheMap.put(name, cache);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> cache;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">	</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="7-Cache的执行流程"><a href="#7-Cache的执行流程" class="headerlink" title="7.Cache的执行流程"></a>7.Cache的执行流程</h4><h5 id="1-获取缓存组件"><a href="#1-获取缓存组件" class="headerlink" title="1.获取缓存组件"></a>1.获取缓存组件</h5><p>在被注解的方法调用之前会去Cache组件按照<code>cacheName</code>获取缓存组件 <strong>（注意不是缓存数据而是组件）</strong>，也就是去 <code>CacheManager</code> 的 <code>getCache()</code> 方法，如果没有Cache组件我们就new一个Cache组件</p>
<h5 id="2-获取key"><a href="#2-获取key" class="headerlink" title="2.获取key"></a>2.获取key</h5><p>待会说</p>
<h5 id="3-获取缓存数据"><a href="#3-获取缓存数据" class="headerlink" title="3.获取缓存数据"></a>3.获取缓存数据</h5><p>接着使用ConcurrentMapCache.lookup()`来查找缓存数据，也就是使用 key 来查找，默认就是方法的参数。但是又是怎样生成key的呢？</p>
<p>我们在 lookup() 上面打上断点，然后我们往上翻一下栈帧就能找到 <code>findCachedItem:491, CacheAspectSupport</code>  接着我们可以在这个方法中看到他生成了 key 也就是  <code>bject key = this.generateKey(context, result);</code>  </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> Object <span class="title">generateKey</span><span class="params">(@Nullable Object result)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (StringUtils.hasText(<span class="keyword">this</span>.metadata.operation.getKey())) &#123; <span class="comment">//获取到key的值  直接使用</span></span><br><span class="line">        EvaluationContext evaluationContext = <span class="keyword">this</span>.createEvaluationContext(result);</span><br><span class="line">        <span class="keyword">return</span> CacheAspectSupport.<span class="keyword">this</span>.evaluator.key(<span class="keyword">this</span>.metadata.operation.getKey(), <span class="keyword">this</span>.metadata.methodKey, evaluationContext);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123; <span class="comment">//没有key的值需要生成</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.metadata.keyGenerator.generate(<span class="keyword">this</span>.target, <span class="keyword">this</span>.metadata.method, <span class="keyword">this</span>.args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面的逻辑就是 获取到key的值 ，直接使用，没有key的值需要生成。好那么这个生成的 <code>generate()</code> 最后是在 一个 <code>public interface KeyGenerator</code> 接口中定义的，那么接下来这个实现类其实就是 SimpleKeyGenerator  在栈帧中是可以看出来的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title">generateKey</span><span class="params">(Object... params)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (params.length == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> SimpleKey.EMPTY;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (params.length == <span class="number">1</span>) &#123;</span><br><span class="line">            Object param = params[<span class="number">0</span>];</span><br><span class="line">            <span class="keyword">if</span> (param != <span class="keyword">null</span> &amp;&amp; !param.getClass().isArray()) &#123;</span><br><span class="line">                <span class="keyword">return</span> param;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> SimpleKey(params);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后生成了 key 值：生成策略 ：</p>
<ul>
<li>如果没有参数就是 Simple对象</li>
<li>有一个参数就是这个东西</li>
<li>多个参数就是参数传入Simple的封装</li>
</ul>
<p>如果我们在缓存中获取到了数据那么我们就不需要运行方法了，如果是没有的话我们必须运行方法执行下面的 put 操作，但是注意一点就是我们通篇使用了 AspectJ 完成的。</p>
<h5 id="4-没有值put进去"><a href="#4-没有值put进去" class="headerlink" title="4.没有值put进去"></a>4.没有值put进去</h5><p>使用了  <code>ConcurrentMapCache.put()</code> 方法来put新的值。</p>
<p><strong>所以说我们的@Cacheable代码的调用时机有两个：分别是运行之前需要查询一次缓存看有没有，以及在没有时候需要运行方法并把结果放到缓存中。所以我们不能在注解中使用#result el表达式，还有注意一点的就是这些注解都是放在Service层的别放到了Controller中，因为Controller每次都会被访问。</strong></p>
<h3 id="4-自定义key"><a href="#4-自定义key" class="headerlink" title="4.自定义key"></a>4.自定义key</h3><h5 id="1-使用注解中的key属性"><a href="#1-使用注解中的key属性" class="headerlink" title="1.使用注解中的key属性"></a>1.使用注解中的key属性</h5><p>我们可以采用 SPEL 表达式，这里举一个简单的例子，我们的可以要用方法名加上参数 Empl[1] 我们就可以用 #root.method.name+’[‘ +#id+’]’ 来完成</p>
<h5 id="2-重写-keyGenerator"><a href="#2-重写-keyGenerator" class="headerlink" title="2.重写 keyGenerator()"></a>2.重写 <code>keyGenerator()</code></h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">KeyGeneratorConfig</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> KeyGenerator <span class="title">keyGenerator</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">// 生策略就是 方法名家参数</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> KeyGenerator() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> Object <span class="title">generate</span><span class="params">(Object target, Method method, Object... params)</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> method.getName() + Arrays.asList(params).toString();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>然后在 keyGenerator的属性写上 我们的bean的id</p>
<h3 id="5-CachePut-注解"><a href="#5-CachePut-注解" class="headerlink" title="5.@CachePut 注解"></a>5.@CachePut 注解</h3><p>这个注解就是在update数据的时候使用的，在更新数据库的同时同期的更新缓存，这个注解的代码都是在方法运行以后才开始运行的，也就是我们代码之运行一次，然后他的原理就是更新后的数据重新放到缓存里面而不是真的去查那条缓存然后更新。所以说有一个非常值得注意的地方就是如果我们的@Cacheable注解使用了自定义的key的时候我们<strong>必须保证他们两个的key一样</strong> 不然的话是没有办法同步更新数据库和缓存的。</p>
<h3 id="6-CacheEvict-缓存清除"><a href="#6-CacheEvict-缓存清除" class="headerlink" title="6.@CacheEvict 缓存清除"></a>6.@CacheEvict 缓存清除</h3><ol>
<li>这个删除默认的情况就是删除指定的key的缓存而非当前Cache组件中所有的缓存。</li>
<li>我们可以指定 allEntries 属性来指定是否删除所有的缓存</li>
<li>beforeInvocation 指定缓存是在方法之前还是之后运行，这个东西的影响就是我们的方法抛出异常的时候缓存能否被清除，显然后者是不可以的。</li>
</ol>
<h3 id="7-Caching-综合注解"><a href="#7-Caching-综合注解" class="headerlink" title="7.@Caching 综合注解"></a>7.@Caching 综合注解</h3><p>这个注解其实就是上面三个注解的综合，也就是我们可以定义综合注解。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Caching</span>(</span><br><span class="line">        cacheable = &#123;</span><br><span class="line">            <span class="meta">@Cacheable</span>(cacheNames = <span class="string">"empl"</span>)</span><br><span class="line">        &#125;,</span><br><span class="line">        put = &#123;</span><br><span class="line">            <span class="comment">//多key缓存</span></span><br><span class="line">            <span class="meta">@CachePut</span>(cacheNames = <span class="string">"empl"</span>,key = <span class="string">"#id"</span>),</span><br><span class="line">            <span class="meta">@CachePut</span>(cacheNames = <span class="string">"empl"</span>,key = <span class="string">"#result"</span>)</span><br><span class="line">        &#125;,</span><br><span class="line">        evict = &#123;</span><br><span class="line">                <span class="meta">@CacheEvict</span>(cacheNames = <span class="string">"empl"</span>)</span><br><span class="line">        &#125;</span><br><span class="line">)</span><br></pre></td></tr></table></figure>

<h3 id="8-CacheConfig类上注解"><a href="#8-CacheConfig类上注解" class="headerlink" title="8.@CacheConfig类上注解"></a>8.@CacheConfig类上注解</h3><p>这个注解可以配置一些公共的项目，然后我们下面的方法就可以不用配置一些公共的东西了。这个注解是配置在类上面的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@CacheConfig</span>(cacheNames = <span class="string">"empl"</span>)</span><br></pre></td></tr></table></figure>

<h2 id="4-使用redis缓存中间件"><a href="#4-使用redis缓存中间件" class="headerlink" title="4.使用redis缓存中间件"></a>4.使用redis缓存中间件</h2><h3 id="1-配置redis客户端"><a href="#1-配置redis客户端" class="headerlink" title="1. 配置redis客户端"></a>1. 配置redis客户端</h3><h3 id="2-配置redis-starter"><a href="#2-配置redis-starter" class="headerlink" title="2.配置redis-starter"></a>2.配置redis-starter</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.data<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-data-redis<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.slf4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jcl-over-slf4j<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>io.lettuce<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>lettuce-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="3-配置redis地址"><a href="#3-配置redis地址" class="headerlink" title="3.配置redis地址"></a>3.配置redis地址</h3><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">    redis:</span></span><br><span class="line"><span class="attr">      host:</span> <span class="string">localhost</span></span><br></pre></td></tr></table></figure>

<h3 id="4-注入两个redis的客户端"><a href="#4-注入两个redis的客户端" class="headerlink" title="4.注入两个redis的客户端"></a>4.注入两个redis的客户端</h3><ul>
<li>redisTemplate    基本的redis客户端</li>
<li>stringRedisTemplate  简化字符串操作的</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line">RedisTemplate redisTemplate;  <span class="comment">//默认k-v为object</span></span><br><span class="line"><span class="meta">@Autowired</span></span><br><span class="line">StringRedisTemplate stringRedisTemplate; <span class="comment">//默认k-v为string的</span></span><br></pre></td></tr></table></figure>

<h3 id="5-Redis常见的五大数据类型"><a href="#5-Redis常见的五大数据类型" class="headerlink" title="5.Redis常见的五大数据类型"></a>5.Redis常见的五大数据类型</h3><p>String（字符串）、List（列表）、Set（集合）、Hash（散列）、ZSet（有序集合）</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">stringRedis Template.opsForValue()[String(字符串）]</span><br><span class="line">stringRedis Template.opsForlist()[List(列表）]</span><br><span class="line">stringRedis Template.opsForSet()[Set(集合）]</span><br><span class="line">stringRedisTemplate.opsForHash()[Hash(散列）]</span><br><span class="line">stringRedis Template.opsForZSet()[ZSet(有序集合）]</span><br></pre></td></tr></table></figure>

<h3 id="6-string以及对象序列化"><a href="#6-string以及对象序列化" class="headerlink" title="6.string以及对象序列化"></a>6.string以及对象序列化</h3><h4 id="1-string类型的操作："><a href="#1-string类型的操作：" class="headerlink" title="1.string类型的操作："></a>1.string类型的操作：</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">    <span class="comment">// string</span></span><br><span class="line">stringRedisTemplate.opsForValue().append(<span class="string">"name"</span>, <span class="string">"lwen"</span>);</span><br><span class="line">stringRedisTemplate.opsForValue().append(<span class="string">"age"</span>, <span class="string">"20"</span>);</span><br><span class="line">    <span class="comment">//list</span></span><br><span class="line">stringRedisTemplate.opsForList().leftPush(<span class="string">"mylist"</span>, <span class="string">"hello"</span>);</span><br><span class="line">stringRedisTemplate.opsForList().leftPush(<span class="string">"mylist"</span>, <span class="string">"world"</span>);</span><br></pre></td></tr></table></figure>

<h4 id="2-对象的操作："><a href="#2-对象的操作：" class="headerlink" title="2.对象的操作："></a>2.对象的操作：</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Employee employee = <span class="keyword">new</span> Employee(<span class="number">10</span>, <span class="number">20</span>, <span class="string">"lwen"</span>);</span><br><span class="line">redisTemplate.opsForValue().set(<span class="string">"objStr"</span>, employee);</span><br></pre></td></tr></table></figure>

<p>注意一点的就是这个对象必须要实现序列化接口否则会报错的！！！</p>
<p><img src="https://user-images.githubusercontent.com/22151420/40300305-7c99a8f2-5d1b-11e8-8464-a691e844831d.png" alt="image"></p>
<p>测试成功以后会发现我们的数据成了这个样子，value还好说应该是序列化导致的，但是key其实也是序列化，我们的string 类也是可以被序列化的。这不是我们希望的而是采用json的格式。那么我们可以配置序列化的规则，去 <code>RedisAutoConfiguration</code> 找对应的默认序列化配置。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@ConditionalOnMissingBean</span>(name = <span class="string">"redisTemplate"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> RedisTemplate&lt;Object, Object&gt; <span class="title">redisTemplate</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">      RedisConnectionFactory redisConnectionFactory)</span> <span class="keyword">throws</span> UnknownHostException </span>&#123;</span><br><span class="line">   RedisTemplate&lt;Object, Object&gt; template = <span class="keyword">new</span> RedisTemplate&lt;&gt;();</span><br><span class="line">   template.setConnectionFactory(redisConnectionFactory);</span><br><span class="line">   <span class="keyword">return</span> template;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@ConditionalOnMissingBean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> StringRedisTemplate <span class="title">stringRedisTemplate</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">      RedisConnectionFactory redisConnectionFactory)</span> <span class="keyword">throws</span> UnknownHostException </span>&#123;</span><br><span class="line">   StringRedisTemplate template = <span class="keyword">new</span> StringRedisTemplate();</span><br><span class="line">   template.setConnectionFactory(redisConnectionFactory);</span><br><span class="line">   <span class="keyword">return</span> template;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到这里就是配置了两个 <code>redisTemplate</code> 我们主要就是让他重新返回新的 <code>redisTemplate</code> 并且在这些模板里面加上我们的序列话规则就好。</p>
<p>我们去 <code>redisTemplate</code>  中看看会发现 afterPropertiesSet 这个方法中有一段逻辑： </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="keyword">this</span>.defaultSerializer == <span class="keyword">null</span>) &#123;</span><br><span class="line">    <span class="keyword">this</span>.defaultSerializer = <span class="keyword">new</span> JdkSerializationRedisSerializer(<span class="keyword">this</span>.classLoader != <span class="keyword">null</span> ? <span class="keyword">this</span>.classLoader : <span class="keyword">this</span>.getClass().getClassLoader());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>意思就是说我们没有配置 <code>defaultSerializer</code> 的话默认使用的是jdk的序列化规则。所以我们自己配置一个 <code>redisTemplate</code>  加上我们自己的规则即可。把上面配置 <code>redisTemplate</code>   的代码复制到我们的JavaConfig中。修改一下，首先我们定位到 <code>private RedisSerializer&lt;?&gt; defaultSerializer;</code> 看到这个地方就是redis的序列化器，我们看看他的具体实现就能找到我们需要的序列化器（Ctrl+H）。</p>
<p><img src="https://user-images.githubusercontent.com/22151420/40301061-7adbe334-5d1d-11e8-8767-299d181cb65e.png" alt="image"></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@ConditionalOnMissingBean</span>(name = <span class="string">"redisTemplate"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> RedisTemplate&lt;Object, Object&gt; <span class="title">redisTemplate</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">      RedisConnectionFactory redisConnectionFactory)</span> <span class="keyword">throws</span> UnknownHostException </span>&#123;</span><br><span class="line">   RedisTemplate&lt;Object, Object&gt; template = <span class="keyword">new</span> RedisTemplate&lt;&gt;();</span><br><span class="line">   template.setConnectionFactory(redisConnectionFactory);</span><br><span class="line">    template.setDefaultSerializer(<span class="keyword">new</span> Jackson2JsonRedisSerializer&lt;&gt;(Employee.class));</span><br><span class="line">   <span class="keyword">return</span> template;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="https://user-images.githubusercontent.com/22151420/40301119-afbaf608-5d1d-11e8-9e13-473277e5eddc.png" alt="image"></p>
<p>使用jackson序列化之后的，就很正常！！！</p>
<h3 id="7-自定义CacheManager"><a href="#7-自定义CacheManager" class="headerlink" title="7.自定义CacheManager"></a>7.自定义CacheManager</h3><p>上面的操作我们都是在test中进行的，我们手动的拿到了 <code>redisTemplate</code> 但是实际上我们在项目中使用的时候我们是不关心我们用的是什么 <code>redisTemplate</code> 也就是我们没办法定义序列化规则了，实际上主要是因为我们的<code>CacheManager</code>被自动配置了。那么去找一下这个自动配置类 <code>RedisCacheConfiguration</code> 里面就是把 CacheManager 放到了容器里。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> RedisCacheManager <span class="title">cacheManager</span><span class="params">(RedisConnectionFactory redisConnectionFactory,</span></span></span><br><span class="line"><span class="function"><span class="params">      ResourceLoader resourceLoader)</span> </span>&#123;</span><br><span class="line">   RedisCacheManagerBuilder builder = RedisCacheManager</span><br><span class="line">         .builder(redisConnectionFactory)</span><br><span class="line">         .cacheDefaults(determineConfiguration(resourceLoader.getClassLoader()));</span><br><span class="line">   List&lt;String&gt; cacheNames = <span class="keyword">this</span>.cacheProperties.getCacheNames();</span><br><span class="line">   <span class="keyword">if</span> (!cacheNames.isEmpty()) &#123;</span><br><span class="line">      builder.initialCacheNames(<span class="keyword">new</span> LinkedHashSet&lt;&gt;(cacheNames));</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">return</span> <span class="keyword">this</span>.customizerInvoker.customize(builder.build());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>但是注意的一点就是我们的CacheManager是每一个Service很可能不太一样的这是因为我们的泛型不一样，否则的话我们有时候就没办法反序列化回来，所以我们现在必须在每一个Service类上面使用 @CacheConfig 注解中的CacheManager属性来制定使用哪个CacheManager。<strong>并且我们还需要一个默认的缓存管理器不然的话会导致注入异常！</strong></p>
<p>!!!<strong>注意</strong> :在springBoot 2.0的版本我没有找到配置序列化的地方了，并且我发现不用配置序列化key是正常的，value是二进制的，但是我觉得没问题，因为生成CacheManager不是用template而是采用builder的方式，所以说我们可以不用配置序列化并且我们的Redis的缓存管理器的类的继承层次和其他几个也不太一样，个人感觉是更好的封装吧，不用再手动配置了。</p>
<h3 id="8-手动操作缓存"><a href="#8-手动操作缓存" class="headerlink" title="8.手动操作缓存"></a>8.手动操作缓存</h3><p>我们可以直接手动注入一个缓存管理器，然后我们使用管理器获取一个缓存，接着我们剧可以手动的对缓存进行操作了。</p>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>Kafka 入门</title>
    <url>/2018/05/18/Spark/Kafka%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h1 id="1-Kafka架构"><a href="#1-Kafka架构" class="headerlink" title="1. Kafka架构"></a>1. Kafka架构</h1><ol>
<li>生产者（producer）：生产资源的</li>
<li>消费者（consumer）：消费资源</li>
<li>broker（缓冲）：中间缓冲器</li>
<li>topic（标记）：谁来消费</li>
</ol>
<h1 id="2-配置Kafka"><a href="#2-配置Kafka" class="headerlink" title="2. 配置Kafka"></a>2. 配置Kafka</h1><h2 id="1-配置ZK"><a href="#1-配置ZK" class="headerlink" title="1. 配置ZK"></a>1. 配置ZK</h2><ol>
<li>解压</li>
<li>配置ZK_HOME</li>
<li>配置PATH</li>
<li>修改 zk.cfg 修改存储位置</li>
<li>zkServer.sh 启动</li>
</ol>
<h2 id="2-配置Kafka-1"><a href="#2-配置Kafka-1" class="headerlink" title="2. 配置Kafka"></a>2. 配置Kafka</h2><ol>
<li>配置HOME以及PATH</li>
<li>修改 server.porperties 注意以下几个条目</li>
</ol>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">$KAFKA_HOME/config/server.properties</span></span><br><span class="line"><span class="meta">broker.id</span>=<span class="string">0</span></span><br><span class="line"><span class="attr">listeners</span></span><br><span class="line"><span class="attr">host.name</span></span><br><span class="line"><span class="attr">1og.dirs</span></span><br><span class="line"><span class="attr">zookeeper.connect</span></span><br></pre></td></tr></table></figure>

<ol start="3">
<li>启动Kafka</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kafka-server-start.sh <span class="variable">$KAFKA_HOME</span>/config/server.properties</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>生产消息</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kafka-topics.sh --create --zookeeper localhost:2181 --replication-factor 1 --partitions 1 --topic hello</span><br></pre></td></tr></table></figure>

<ol start="5">
<li>启动消费者</li>
</ol>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">kafka-console-consumer.sh --zookeeper localhost:2181 --topic hello --from-beginning</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Spark</category>
      </categories>
      <tags>
        <tag>Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>Flume 入门</title>
    <url>/2018/05/18/Spark/Flume%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h1 id="1-基本架构"><a href="#1-基本架构" class="headerlink" title="1. 基本架构"></a>1. 基本架构</h1><h3 id="1-结构"><a href="#1-结构" class="headerlink" title="1. 结构"></a>1. 结构</h3><p>source    源数据：目录/</p>
<p>channel    数据管道</p>
<p>sink        数据输出</p>
<h3 id="2-几种结构形式"><a href="#2-几种结构形式" class="headerlink" title="2. 几种结构形式"></a>2. 几种结构形式</h3><ol>
<li>单个的Flume</li>
<li>多个Flume串行工作，前一个Flume的sink作为后一个的source</li>
<li>多个Flume并行工作然后最后用一个Flume进行合并这行并行收集的数据</li>
<li>多个Flume并行工作，分别传送到不同的目的地</li>
</ol>
<h1 id="2-安装"><a href="#2-安装" class="headerlink" title="2. 安装"></a>2. 安装</h1><h2 id="1-安装JDK"><a href="#1-安装JDK" class="headerlink" title="1. 安装JDK"></a>1. 安装JDK</h2><p>解压到 <del>/app<br>将java配置系统环境变量中：</del>/.bash_profile<br>export JAVA_HOME=/home/hadoop/app/jdk1.8.0_144<br>export PATH=$JAVA_HOME/bin:$PATH<br>source下让其配置生效<br>检测：java -version</p>
<h2 id="2-安装Flume"><a href="#2-安装Flume" class="headerlink" title="2. 安装Flume"></a>2. 安装Flume</h2><h3 id="1-安装"><a href="#1-安装" class="headerlink" title="1.  安装"></a>1.  安装</h3><p>export FLUME_HOME=/home/hadoop/app/apache-flume-1.6.0-cdh5.7.0-b</p>
<p>export PATH=$FLUME_HOME/bin:$PATH<br>source下让其配置生效</p>
<h3 id="2-配置"><a href="#2-配置" class="headerlink" title="2.  配置"></a>2.  配置</h3><p>flume-env.sh的配置：export JAVA_HOME=/home/hadoop/app/jdk1.8.0_144</p>
<h1 id="3-使用Flume"><a href="#3-使用Flume" class="headerlink" title="3. 使用Flume"></a>3. 使用Flume</h1><p>使用Flume的关键其实就是书写配置文件，然后启动不同的配置文件的进程就是不同的任务。</p>
<h2 id="1-配置"><a href="#1-配置" class="headerlink" title="1. 配置"></a>1. 配置</h2><ul>
<li>配置Source</li>
<li>配置Channel</li>
<li>配置Sink</li>
<li>把上面的组件串联起来</li>
</ul>
<p>给一个简单的Conf的例子，并做一些注释。</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment">#a1:agent名称 r1:source的名称 k1:sink的名称 c1:channel的名称</span></span><br><span class="line"><span class="comment">#Name the components on this agent</span></span><br><span class="line"><span class="meta">al.sources</span>=<span class="string">r1</span></span><br><span class="line"><span class="meta">al.sinks</span>=<span class="string">k1</span></span><br><span class="line"><span class="meta">al.channels</span>=<span class="string">c1</span></span><br><span class="line"><span class="comment">#Describe/configure the source</span></span><br><span class="line"><span class="meta">al.sources.rl.type</span>=<span class="string">netcat</span></span><br><span class="line"><span class="meta">al.sources.r1.bind</span>=<span class="string">localhost</span></span><br><span class="line"><span class="meta">al.sources.r1.port</span> =<span class="string">44444</span></span><br><span class="line"><span class="comment">#Describe the sink</span></span><br><span class="line"><span class="meta">al.sinks.k1.type</span>=<span class="string">logger</span></span><br><span class="line"><span class="comment"># Use a channel which buffers events in memory</span></span><br><span class="line"><span class="meta">al.channels.c1.type</span>=<span class="string">memory</span></span><br><span class="line"><span class="comment">#Bind the source and sink to the channel</span></span><br><span class="line"><span class="meta">al.sources.r1.channel.</span>=<span class="string">c1</span></span><br><span class="line"><span class="meta">al.sinks.k1.channel</span>=<span class="string">c1</span></span><br></pre></td></tr></table></figure>

<h2 id="2-启动"><a href="#2-启动" class="headerlink" title="2. 启动"></a>2. 启动</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">flume-ng agent \</span><br><span class="line">--name a1 \</span><br><span class="line">-c <span class="variable">$FLUME_HOME</span>/conf \</span><br><span class="line">-f <span class="variable">$FLUME_HOME</span>/conf/example.conf \</span><br><span class="line">-Dflume.root.logger=INFO,console</span><br></pre></td></tr></table></figure>

<p>注意 –name 选项指定的名字必须要和配置文件里面的agent名字一致才行，否则会提示找不到对应的配置。</p>
<p>启动以后使用 telnet 127.0.0.1 4444 访问，然后输入文字flume就可以收集到数据。具体数据如下：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">2018-05-18 17:01:21,693 Event: &#123; headers:&#123;&#125; body: 68 65 6C 6C 6F 0D   hello. &#125;              </span><br><span class="line">2018-05-18 17:01:26,912 Event: &#123; headers:&#123;&#125; body: 77 6F 72 6C 64 0D  world. &#125;</span><br></pre></td></tr></table></figure>

<p>可以看到他每一条消息都是一个Event 也就是说在Flume中每一个Event都是一个基本的传送单元。</p>
<p>Event = header + body</p>
<h1 id="3-实战"><a href="#3-实战" class="headerlink" title="3.  实战"></a>3.  实战</h1><h2 id="1-从文件中读取数据"><a href="#1-从文件中读取数据" class="headerlink" title="1. 从文件中读取数据"></a>1. 从文件中读取数据</h2><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Name the components on this agent</span></span><br><span class="line"><span class="meta">a1.sources</span> = <span class="string">r1</span></span><br><span class="line"><span class="meta">a1.sinks</span> = <span class="string">k1</span></span><br><span class="line"><span class="meta">a1.channels</span> = <span class="string">c1</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Describe/configure the source</span></span><br><span class="line"><span class="meta">a1.sources.r1.type</span> = <span class="string">exec</span></span><br><span class="line"><span class="meta">a1.sources.r1.command</span> = <span class="string">tail -F /root/data.log</span></span><br><span class="line"><span class="meta">a1.sources.r1.shell</span> = <span class="string">/bin/bash -c</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Describe the sink</span></span><br><span class="line"><span class="meta">a1.sinks.k1.type</span> = <span class="string">logger</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Use a channel which buffers events in memory</span></span><br><span class="line"><span class="meta">a1.channels.c1.type</span> = <span class="string">memory</span></span><br><span class="line"><span class="meta">a1.channels.c1.capacity</span> = <span class="string">1000</span></span><br><span class="line"><span class="meta">a1.channels.c1.transactionCapacity</span> = <span class="string">100</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Bind the source and sink to the channel</span></span><br><span class="line"><span class="meta">a1.sources.r1.channels</span> = <span class="string">c1</span></span><br><span class="line"><span class="meta">a1.sinks.k1.channel</span> = <span class="string">c1</span></span><br></pre></td></tr></table></figure>

<h2 id="2-A机向B机传输采集的数据"><a href="#2-A机向B机传输采集的数据" class="headerlink" title="2.  A机向B机传输采集的数据"></a>2.  A机向B机传输采集的数据</h2><p>首先配置A机 ，我们采用的是 exec 监控一个文件，然后使用 memory channel 输出到 avro ，所以有配置文件 exec-memcory-avro.conf</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">exec-memcory-avro.sources</span> = <span class="string">exec-source</span></span><br><span class="line"><span class="meta">exec-memcory-avro.sinks</span> = <span class="string">avro-sink</span></span><br><span class="line"><span class="meta">exec-memcory-avro.channels</span> = <span class="string">memory-channel</span></span><br><span class="line"></span><br><span class="line"><span class="meta">exec-memcory-avro.sources.exec-source.type</span> = <span class="string">exec</span></span><br><span class="line"><span class="meta">exec-memcory-avro.sources.exec-source.command</span> = <span class="string">tail -F /root/data.log</span></span><br><span class="line"><span class="meta">exec-memcory-avro.sources.exec-source.shell</span> = <span class="string">/bin/bash -c</span></span><br><span class="line"></span><br><span class="line"><span class="meta">exec-memcory-avro.sinks.avro-sink.type</span> = <span class="string">avro</span></span><br><span class="line"><span class="meta">exec-memcory-avro.sinks.avro-sink.hostname</span> = <span class="string">master</span></span><br><span class="line"><span class="meta">exec-memcory-avro.sinks.avro-sink.port</span> = <span class="string">4444</span></span><br><span class="line"></span><br><span class="line"><span class="meta">exec-memcory-avro.channels.memory-channel.type</span> = <span class="string">memory</span></span><br><span class="line"><span class="meta">exec-memcory-avro.channels.memory-channel.capacity</span> = <span class="string">1000</span></span><br><span class="line"><span class="meta">exec-memcory-avro.channels.memory-channel.transactionCapacity</span> = <span class="string">100</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">exec-memcory-avro.sources.exec-source.channels</span> = <span class="string">memory-channel</span></span><br><span class="line"><span class="meta">exec-memcory-avro.sinks.avro-sink.channel</span> = <span class="string">memory-channel</span></span><br></pre></td></tr></table></figure>

<p>执行如下的命令可以启动，但是不要现在启动否则会报错</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">flume-ng agent \</span><br><span class="line">--name <span class="built_in">exec</span>-memcory-avro \</span><br><span class="line">-c <span class="variable">$FLUME_HOME</span>/conf \</span><br><span class="line">-f <span class="variable">$FLUME_HOME</span>/conf/<span class="built_in">exec</span>-memcory-avro.conf \</span><br><span class="line">-Dflume.root.logger=INFO,console</span><br></pre></td></tr></table></figure>

<p>那么B机就需要使用avro source来接受了，然后使用memory做channel，最后使用logger做输出。建立配置文件avro-memory-logger.conf</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">avro-memory-logger.sources</span> = <span class="string">avro-source</span></span><br><span class="line"><span class="meta">avro-memory-logger.sinks</span> = <span class="string">logger-sink</span></span><br><span class="line"><span class="meta">avro-memory-logger.channels</span> = <span class="string">memory-channel</span></span><br><span class="line"></span><br><span class="line"><span class="meta">avro-memory-logger.sources.avro-source.type</span> = <span class="string">avro</span></span><br><span class="line"><span class="meta">avro-memory-logger.sources.avro-source.bind</span> = <span class="string">master</span></span><br><span class="line"><span class="meta">avro-memory-logger.sources.avro-source.port</span> = <span class="string">4444</span></span><br><span class="line"></span><br><span class="line"><span class="meta">avro-memory-logger.sinks.logger-sink.type</span> = <span class="string">logger</span></span><br><span class="line"></span><br><span class="line"><span class="meta">avro-memory-logger.channels.memory-channel.type</span> = <span class="string">memory</span></span><br><span class="line"><span class="meta">avro-memory-logger.channels.memory-channel.capacity</span> = <span class="string">1000</span></span><br><span class="line"><span class="meta">avro-memory-logger.channels.memory-channel.transactionCapacity</span> = <span class="string">100</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">avro-memory-logger.sources.avro-source.channels</span> = <span class="string">memory-channel</span></span><br><span class="line"><span class="meta">avro-memory-logger.sinks.logger-sink.channel</span> = <span class="string">memory-channel</span></span><br></pre></td></tr></table></figure>

<p>启动</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">flume-ng agent \</span><br><span class="line">--name avro-memory-logger \</span><br><span class="line">-c <span class="variable">$FLUME_HOME</span>/conf \</span><br><span class="line">-f <span class="variable">$FLUME_HOME</span>/conf/avro-memory-logger.conf \</span><br><span class="line">-Dflume.root.logger=INFO,console</span><br></pre></td></tr></table></figure>

<p>我们要先启动后面的那个，也就是B机，不然的话我们先启动A机就会报错。</p>
]]></content>
      <categories>
        <category>Spark</category>
      </categories>
      <tags>
        <tag>Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>Vim 使用技巧</title>
    <url>/2018/05/18/Tools/Vim%E4%BD%BF%E7%94%A8%E6%8A%80%E5%B7%A7/</url>
    <content><![CDATA[<p>1.x 删除后面的字符 X 删除前一个字符  删除3个字符就是3x<br>2.caw:改写单词<br>3.J:删除换行符，使下一行并上来。     nJ:连接连同本行的n行<br>4.u:撤销上一次操作      U:撤销当前行的所有修改<br>5.i 在光标前插入   I 在行首插入<br>6.a 在光标后插入 A 在行末插入<br>7.o:在当前行的下面另起一行，并变为插入模式 O：在当前行上面另起一行，变为插入模式<br>8.vim中Nyy可以复制光标后的N行。有时我们不容易得出行数，这时可以用做标记的方法来制定复制范围  1. 在开始行上输入ma作一个标记a </p>
<ol>
<li>移动到结束行，输入y’a会复制当前行到标记a之间的文本。d’a会删除。<br>9.将光标放在 {或者( 处，然后输入v%就可以把大括号中内容选定<br>10.想删除到“(”为止，则输入dt(就可以了，t(的作用是跳到下一个”(“前。<br>11.%: 移动到与制匹配的括号上去（），{}，[]，&lt;&gt;等<br>12.’ 移动到上一次的修改行<br>13.# 到与当前单词相同的上一个单词上， * 到与当前单词相同的下一个单词上<br>14.如果你要重复键入一个短语或一个句子, 也有一种快捷的方法。Vim有一种记录宏的机制。你键入”qa”开始把一段宏记录入寄存器变量<code>a&#39;中。
按下来你可以象平常一样键入你要的操作, 只是这些操作都会被Vim记录进它命名为</code>a’的宏中, 再次再下”q”键, 就结束了宏`a’的录制。<br>   当你要重复执行你刚才记录的那些操作时只要使用”@a”命令。共有26个可用的寄存器供你记录宏。 使用宏你可以重复多个不同的操作。<br>   而不仅仅是插入文本了。如果你要进行某种重复的操作, 记着要用这一招呀。<br>15.b、3b、w、3w:向前\后移动几个单词，标点也算一个单词。相应的大写状态为不含标点，即只把空格和换行符作为单词间隔符。<br>16.$：移动到行尾  3$：移动到3行后的行尾 ^:移动到行首，0也是 +：移到下一行的行首 -： 移到上一行的行首<br>17.33G：跳转到33行   此时按``可以返回到原来行 gg:文件头   G： 文件尾</li>
<li>:set nu 设置行号<br>19.ctrl+b\f  向上\下滚动一屏　　这个比较实用，记住。<br>20.zz:将当前行滚动于屏幕中间，方便查看上下文  zt置顶，zb置尾<br>21./string  查找string，回车后，按n键可以跳到下一个，N上一个，另外按/键后，按上下键可以找到以前查找的记录，同样的 ：也有记录<br> ?/string 同上，默认向上查找<br>22.:set ignorecase 大小写无关 :set noignorecase 大小写敏感<br>23.&gt;&gt;   向右移动本行一段距离     &lt;&lt;   向左移动本行一段距离    3&lt;&lt;    把下面3行（包括本行），向左移动一段距离     :20,30&gt;&gt;  把20行到30行向右移动一段距离<br>24.:%s/str1/str2/g   替换每一行的 str1为 str2<br> :10,20s/str1/str2/g   替换从行10到行20之间的 str1为 str2<br> :10,$s/str1/str2/g   替换从行10到最后一行之间的 str1为 str2<br> :s/str1/str2/g   替换当前行的 str1为 str2<br>25.:10,$ w test2.cpp     取行10到最后一行内容，保存到test2.cpp</li>
</ol>
<p>26.:r class/User.hpp    读取文件中的内容，插入到当前行的后面<br>27.dw:删除一个单词（光标后部分）  不如：daw实用<br>28.d4w：删除4个单词　　　　d$:删除当前光标到行尾　<br>29.d换成c效果是一样的，只是操作完会变成insert模式<br>30.dnj: 向下删除n行      dnk: 向上删除n行     dn删除本行<br>31.D:相当于d$　　C:相当于c$<br>32.r:替换当前字符，但不会进入insert模式　<br>33.yaw： 复制一个单词,光标在单词任意位置    ynw： 复制N个单词       ynj: 向下复制n行          ynk: 向上复制n行<br>34.ci’、ci”、ci(、ci[、ci{、ci&lt; - 分别更改这些配对标点符号中的文本内容<br>  di’、di”、di(或dib、di[、di{或diB、di&lt; - 分别删除这些配对标点符号中的文本内容<br>  yi’、yi”、yi(、yi[、yi{、yi&lt; - 分别复制这些配对标点符号中的文本内容<br>  vi’、vi”、vi(、vi[、vi{、vi&lt; - 分别选中这些配对标点符号中的文本内容.</p>
<ul>
<li>% 快速定位到本行的括号</li>
</ul>
]]></content>
      <categories>
        <category>工具</category>
      </categories>
      <tags>
        <tag>Vim</tag>
      </tags>
  </entry>
  <entry>
    <title>Scala 入门</title>
    <url>/2018/05/08/Spark/Scala%20%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h1 id="1-基本语法"><a href="#1-基本语法" class="headerlink" title="1. 基本语法"></a>1. 基本语法</h1><h3 id="1-变量-var"><a href="#1-变量-var" class="headerlink" title="1. 变量 var"></a>1. 变量 var</h3><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> name:string=<span class="string">"hello"</span>;</span><br><span class="line"><span class="keyword">var</span> name=<span class="string">"hello"</span>;</span><br><span class="line"><span class="keyword">var</span> num=<span class="number">3.0</span>;</span><br></pre></td></tr></table></figure>

<h3 id="2-常量-val"><a href="#2-常量-val" class="headerlink" title="2. 常量 val"></a>2. 常量 val</h3><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">val</span> name=<span class="string">"string"</span>;</span><br></pre></td></tr></table></figure>

<h3 id="3-这里是没有-操作和-–-操作的"><a href="#3-这里是没有-操作和-–-操作的" class="headerlink" title="3. 这里是没有 ++ 操作和 – 操作的"></a>3. 这里是没有 ++ 操作和 – 操作的</h3><h3 id="4-函数调用"><a href="#4-函数调用" class="headerlink" title="4. 函数调用"></a>4. 函数调用</h3><p>不用使用对象来调用，导入包以后直接调用即可</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> scala.math._</span><br><span class="line">sqrt(<span class="number">2</span>);</span><br><span class="line">pow(<span class="number">1</span>,<span class="number">2</span>);</span><br><span class="line">min(<span class="number">1</span>,<span class="number">2</span>);</span><br></pre></td></tr></table></figure>

<h3 id="5-apply"><a href="#5-apply" class="headerlink" title="5.apply()"></a>5.apply()</h3><p>默认调用了object的apply函数</p>
<figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="string">"hello"</span>&lt;<span class="number">6</span>&gt;;</span><br></pre></td></tr></table></figure>

<h3 id="6-if-else"><a href="#6-if-else" class="headerlink" title="6.if-else"></a>6.if-else</h3>]]></content>
      <categories>
        <category>Spark</category>
      </categories>
      <tags>
        <tag>Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>SparkStreaming 入门</title>
    <url>/2018/05/08/Spark/SparkStreming/</url>
    <content><![CDATA[<h1 id="1-基本原理"><a href="#1-基本原理" class="headerlink" title="1. 基本原理"></a>1. 基本原理</h1><p>其实在 SparkStreaming 中和之前的Core不同的就是他会把任务分成批次的进行处理，也就是我们需要设置间隔多久计算一次。</p>
<p>我们从网络，文件系统，Kafka 等等数据源产生的地方获取数据，然后SparkStreaming放到内存中，接着进行对数据进行计算，获取结果。<a id="more"></a></p>
<p>在一个Spark应用程序启动以后会产生一个SparkContext和一个StreamingContext，后者是基于前者的，接着就是每一个集群的单节点上就有Executor 这些Executor中是有Receiver的，然后这些Receiver就负责来自于网络以及Kafka等等的数据源的数据收集，这些数据会被拆分成Block分发到各个集群节点上，最后Receiver就把这些block信息发给StreamingContext 他会把告诉 SparkContext 去处理 job ，然后SparkContext 就启动 job 分配给 Executor</p>
<p><a href="https://imgchr.com/i/CcWDER" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2018/05/19/CcWDER.md.png" alt="CcWDER.md.png"></a></p>
<h1 id="2-基本概念"><a href="#2-基本概念" class="headerlink" title="2. 基本概念"></a>2. 基本概念</h1><h3 id="1-StreamingContext"><a href="#1-StreamingContext" class="headerlink" title="1. StreamingContext"></a>1. StreamingContext</h3><p>这个东西就相当于所有的 Streaming 任务的主入口，所有的 Streaming 任务都需要他来完成。这个东西在定义以后我们书写计算任务的计划，完成之后我们不能在代码中 stop 后继续 start Streaming ，也就是没办法重启，只能在命令行重启。然后再JVM中只能存在一个此对象。</p>
<h3 id="2-DStream"><a href="#2-DStream" class="headerlink" title="2. DStream"></a>2. DStream</h3><p>这个东西其实就相当于一个RDD的小截断，我们可以把数据想象成一个流，然后我们从里面截取一小段流就是我们说的 DStream ，然后 里面包含的就是各个 RDD。所以说如果我们队一个DStream 进行一个操作就相当于我们对里面的所有的RDD进行操作。那么这个DStream的长度是多少？这就是我们定义的计算的长度，也就是多久计算一次。</p>
<h3 id="3-InputDStream"><a href="#3-InputDStream" class="headerlink" title="3. InputDStream"></a>3. InputDStream</h3><p>其实在每一个DStream中都包含了一个Receiver，但是除了FileDStream。这个Receiver就是从各个数据源进行获取数据用的， 他会把数据源获取的数据放到内存里面，但是我们文件系统中的数据我们可以直接处理而不需要收集这些数据。</p>
<p>我们基本的Receiver就是文件系统和TCP，然后我们有一些高级的就是 Flume 和 Kafka 等等。</p>
<p>注意一点就是我们在运行这些任务的时候我们不能写 local 或者 local[1] 因为我们在处理的时候必须要有两个线程以上，一个需要进行Receiver另外一个是数据计算。</p>
<h3 id="4-带状态的数据处理-UpdateStateByKey"><a href="#4-带状态的数据处理-UpdateStateByKey" class="headerlink" title="4. 带状态的数据处理 UpdateStateByKey"></a>4. 带状态的数据处理 UpdateStateByKey</h3><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">*把当前的数据去更新已有的或者是老的数据</span></span><br><span class="line"><span class="comment">*@param currentValues 当前的</span></span><br><span class="line"><span class="comment">*@param preValues 老的的</span></span><br><span class="line"><span class="comment">*@return</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">updateFunction</span></span>(currentValues:<span class="type">Seq</span>[<span class="type">Int</span>],preValues:<span class="type">Option</span>[<span class="type">Int</span>]):<span class="type">Option</span>[<span class="type">Int</span>]=&#123;</span><br><span class="line">    <span class="keyword">val</span> current=currentValues.sum  <span class="comment">//获取新的值</span></span><br><span class="line">    <span class="keyword">val</span> pre=preValues.getOrElse(<span class="number">0</span>)	<span class="comment">//获取以前的值，如果以前没有那么就是0</span></span><br><span class="line">    <span class="type">Some</span>(current+pre)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>要注意一点的就是当我们使用了带有状态的算子我们必须要使用，checkpoint 才行，也就是需要创建一个checkpoint目录，否则或报错。</p>
<h3 id="5-窗口计算"><a href="#5-窗口计算" class="headerlink" title="5.  窗口计算"></a>5.  窗口计算</h3><p>窗口计算说的简单点其实就是说每隔多少秒计算多少秒内的结果。windowLength 和 interval 也就是每隔 interval 计算 windowLength 的长度的窗口。</p>
<p>算子在常用的加上一个AndWindow 官网有。</p>
<h1 id="3-黑名单的例子"><a href="#3-黑名单的例子" class="headerlink" title="3. 黑名单的例子"></a>3. 黑名单的例子</h1><figure class="highlight scala"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">  12121,zs</span></span><br><span class="line"><span class="comment">  12121,ls</span></span><br><span class="line"><span class="comment">  12124,ws</span></span><br><span class="line"><span class="comment">  12126,ws</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">object</span> <span class="title">BlackFilter</span></span>&#123;</span><br><span class="line"> <span class="function"><span class="keyword">def</span> <span class="title">main</span></span>(args: <span class="type">Array</span>[<span class="type">String</span>]): <span class="type">Unit</span> = &#123;</span><br><span class="line">  <span class="type">System</span>.setProperty(<span class="string">"hadoop.home.dir"</span>, <span class="string">"D:\\hadoop\\winutils\\hadoop-2.8.3"</span>)</span><br><span class="line">  <span class="keyword">val</span> conf: <span class="type">SparkConf</span> = <span class="keyword">new</span> <span class="type">SparkConf</span>().setAppName(<span class="string">"black"</span>).setMaster(<span class="string">"local[*]"</span>)</span><br><span class="line">  <span class="keyword">val</span> session: <span class="type">SparkSession</span> = <span class="type">SparkSession</span>.builder.config(conf).getOrCreate()</span><br><span class="line">  <span class="keyword">val</span> sc: <span class="type">SparkContext</span> = session.sparkContext</span><br><span class="line">  <span class="keyword">val</span> ssc = <span class="keyword">new</span> <span class="type">StreamingContext</span>(sc,<span class="type">Seconds</span>(<span class="number">5</span>))</span><br><span class="line">  <span class="comment">//val sc = new SparkContext(conf)</span></span><br><span class="line">  <span class="keyword">val</span> blackList: <span class="type">RDD</span>[(<span class="type">String</span>, <span class="type">Boolean</span>)] = sc.parallelize(<span class="type">List</span>(<span class="string">"zs"</span>, <span class="string">"ls"</span>)).map((_, <span class="literal">true</span>))</span><br><span class="line">  <span class="keyword">val</span> logs: <span class="type">ReceiverInputDStream</span>[<span class="type">String</span>] = ssc.socketTextStream(<span class="string">"master"</span>,<span class="number">6700</span>)</span><br><span class="line">  <span class="comment">// (ls,(12121,ls))</span></span><br><span class="line">  <span class="keyword">val</span> clicklog=logs.map(log=&gt;(log.split(<span class="string">","</span>)(<span class="number">1</span>),log)).transform(rdd=&gt;&#123;</span><br><span class="line">   <span class="comment">// ((ls,(12121,ls)),(ls,true/""))</span></span><br><span class="line">   rdd.leftOuterJoin(blackList)</span><br><span class="line">     .filter(!_._2._2.getOrElse(<span class="literal">false</span>))</span><br><span class="line">     .map(_._2._1)</span><br><span class="line">  &#125;)</span><br><span class="line">   clicklog.print()</span><br><span class="line">  ssc.start()</span><br><span class="line">  ssc.awaitTermination()</span><br><span class="line"> &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h1 id="4-SparkStreaming和Flume整合"><a href="#4-SparkStreaming和Flume整合" class="headerlink" title="4. SparkStreaming和Flume整合"></a>4. SparkStreaming和Flume整合</h1><h2 id="1-配置"><a href="#1-配置" class="headerlink" title="1. 配置"></a>1. 配置</h2><p>对于这个我们有两种配置方式，使用Flume的推送机制，也就是把我们的SparkStreaming作为一个avro的客户端来接受从channel过来的数据。</p>
<h3 id="1-Flume-的推送机制"><a href="#1-Flume-的推送机制" class="headerlink" title="1. Flume 的推送机制"></a>1. Flume 的推送机制</h3><p>我们把SparkStreaming作为一个avro的客户端，来接受数据进行处理。由于是push的模型，我们的SparkStreaming必须先启动。</p>
<h4 id="1-配置Flume的配置文件"><a href="#1-配置Flume的配置文件" class="headerlink" title="1. 配置Flume的配置文件"></a>1. 配置Flume的配置文件</h4><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">netcat-memcory-avro.sources</span> = <span class="string">netcat-source</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sinks</span> = <span class="string">avro-sink</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.channels</span> = <span class="string">memory-channel</span></span><br><span class="line"></span><br><span class="line"><span class="meta">netcat-memcory-avro.sources.netcat-source.type</span> = <span class="string">netcat</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sources.netcat-source.bind</span> = <span class="string">master</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sources.netcat-source.port</span> = <span class="string">44444</span></span><br><span class="line"></span><br><span class="line"><span class="meta">netcat-memcory-avro.sinks.avro-sink.type</span> = <span class="string">avro</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sinks.avro-sink.hostname</span> = <span class="string">master</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sinks.avro-sink.port</span> = <span class="string">44445</span></span><br><span class="line"></span><br><span class="line"><span class="meta">netcat-memcory-avro.channels.memory-channel.type</span> = <span class="string">memory</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.channels.memory-channel.capacity</span> = <span class="string">1000</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.channels.memory-channel.transactionCapacity</span> = <span class="string">100</span></span><br><span class="line"></span><br><span class="line"><span class="meta">netcat-memcory-avro.sources.netcat-source.channels</span> = <span class="string">memory-channel</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sinks.avro-sink.channel</span> = <span class="string">memory-channel</span></span><br></pre></td></tr></table></figure>

<h4 id="2-Scala-代码"><a href="#2-Scala-代码" class="headerlink" title="2 . Scala 代码"></a>2 . Scala 代码</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">object FlumeWC &#123;</span><br><span class="line">	<span class="function">def <span class="title">main</span><span class="params">(args: Array[String])</span>: Unit </span>= &#123;</span><br><span class="line">		System.setProperty(<span class="string">"hadoop.home.dir"</span>, <span class="string">"D:\\hadoop\\winutils\\hadoop-2.8.3"</span>)</span><br><span class="line">		val conf: SparkConf = <span class="keyword">new</span> SparkConf().setAppName(<span class="string">"FlumeWC"</span>).setMaster(<span class="string">"local[*]"</span>)</span><br><span class="line">		val sc = <span class="keyword">new</span> SparkContext(conf)</span><br><span class="line">		val ssc = <span class="keyword">new</span> StreamingContext(sc, Seconds(<span class="number">1</span>))</span><br><span class="line">		val stream: ReceiverInputDStream[SparkFlumeEvent] = FlumeUtils.createStream(ssc, <span class="string">"219.245.31.193"</span>, <span class="number">44445</span>)</span><br><span class="line">		val res: DStream[(String, Int)] =stream.map(x =&gt; <span class="keyword">new</span> String(x.event.getBody.array).trim)</span><br><span class="line">			.flatMap(_.split(<span class="string">" "</span>))</span><br><span class="line">			.map((_, <span class="number">1</span>))</span><br><span class="line">  		.reduceByKey(_+_)</span><br><span class="line">		res.print()</span><br><span class="line">		ssc.start()</span><br><span class="line">		ssc.awaitTermination()</span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-使用pull的方式"><a href="#2-使用pull的方式" class="headerlink" title="2.  使用pull的方式"></a>2.  使用pull的方式</h3><p>这种方式是Flume将数据sink到缓冲区中，然后我们使用Spark事务的去拉取数据，如果拉取到了才会删除那些在缓冲区的数据，也就是说这里的容错性更加的高，更可靠。</p>
<h4 id="1-配置文件"><a href="#1-配置文件" class="headerlink" title="1. 配置文件"></a>1. 配置文件</h4><figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">netcat-memcory-avro.sources</span> = <span class="string">netcat-source</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sinks</span> = <span class="string">spark-sink</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.channels</span> = <span class="string">memory-channel</span></span><br><span class="line"></span><br><span class="line"><span class="meta">netcat-memcory-avro.sources.netcat-source.type</span> = <span class="string">netcat</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sources.netcat-source.bind</span> = <span class="string">127.0.0.1</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sources.netcat-source.port</span> = <span class="string">44444</span></span><br><span class="line"></span><br><span class="line"><span class="meta">netcat-memcory-avro.sinks.spark-sink.type</span> = <span class="string">org.apache.spark.streaming.flume.sink.SparkSink</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sinks.spark-sink.hostname</span> = <span class="string">219.245.31.193</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sinks.spark-sink.port</span> = <span class="string">44445</span></span><br><span class="line"></span><br><span class="line"><span class="meta">netcat-memcory-avro.channels.memory-channel.type</span> = <span class="string">memory</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.channels.memory-channel.capacity</span> = <span class="string">1000</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.channels.memory-channel.transactionCapacity</span> = <span class="string">100</span></span><br><span class="line"></span><br><span class="line"><span class="meta">netcat-memcory-avro.sources.netcat-source.channels</span> = <span class="string">memory-channel</span></span><br><span class="line"><span class="meta">netcat-memcory-avro.sinks.spark-sink.channel</span> = <span class="string">memory-channel</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>Spark</category>
      </categories>
      <tags>
        <tag>Spark</tag>
      </tags>
  </entry>
  <entry>
    <title>Python 笔记</title>
    <url>/2018/05/06/Python/Python%E7%AC%94%E8%AE%B0/</url>
    <content><![CDATA[<h2 id="字符集和编码"><a href="#字符集和编码" class="headerlink" title="字符集和编码"></a>字符集和编码</h2><p>​    <img src="http://images.heniankj.com/20190605121420.png" alt></p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
  </entry>
  <entry>
    <title>数字图像处理</title>
    <url>/2018/05/06/Others/%E6%95%B0%E5%AD%97%E5%9B%BE%E5%83%8F%E5%A4%84%E7%90%86/</url>
    <content><![CDATA[<h1 id="1-基本概念"><a href="#1-基本概念" class="headerlink" title="1.基本概念"></a>1.基本概念</h1><h2 id="1-图像的定义"><a href="#1-图像的定义" class="headerlink" title="1. 图像的定义"></a>1. 图像的定义</h2><ol>
<li>图像：一幅图像是一个东西的另一种表示，是其所表示物体信息的一个浓缩和高度概括。</li>
</ol>
<h2 id="2-图像分类"><a href="#2-图像分类" class="headerlink" title="2. 图像分类"></a>2. 图像分类</h2><h3 id="1-模拟图像：连续变化的函数"><a href="#1-模拟图像：连续变化的函数" class="headerlink" title="1. 模拟图像：连续变化的函数"></a>1. 模拟图像：连续变化的函数</h3><h3 id="2-数字图像：离散的矩阵表示"><a href="#2-数字图像：离散的矩阵表示" class="headerlink" title="2. 数字图像：离散的矩阵表示"></a>2. 数字图像：离散的矩阵表示</h3><h5 id="数字图像的定义"><a href="#数字图像的定义" class="headerlink" title="数字图像的定义:"></a>数字图像的定义:</h5><p>数字图像是指由被称为像素的小区域组成的二维矩阵。</p>
<h5 id="数字图像类型："><a href="#数字图像类型：" class="headerlink" title="数字图像类型："></a>数字图像类型：</h5><ul>
<li><p>二值图像：只有0、1 （黑、白）</p>
</li>
<li><p>灰度图像：像素取值是 0-255 ，有中间过度。<a id="more"></a></p>
</li>
<li><p>彩色（索引）图像：两个矩阵，一个就是灰度图像的矩阵，另外一个是颜色映像表矩阵对应使用。</p>
<p><img src="https://user-images.githubusercontent.com/22151420/39669253-315e3ef6-5119-11e8-9cae-8223ab4a63c1.png" alt="image"></p>
</li>
<li><p>GRB彩色图像：每个像素由 RGB 组成。这是九个像素点，每个像素点有2^24中可能，所以也叫做24位真彩。</p>
<p><img src="https://user-images.githubusercontent.com/22151420/39669280-3ca12336-511a-11e8-9823-af0265ccd48e.png" alt="image"></p>
</li>
</ul>
<h2 id="2-图像概念"><a href="#2-图像概念" class="headerlink" title="2. 图像概念"></a>2. 图像概念</h2><ul>
<li><p>图像质量</p>
<ul>
<li>灰度级：表示图像的明暗程度（例如：像素的取值范围为0-255，就称该图像为256个灰度级的图像）</li>
<li>量化级别：表示图像实际拥有的灰度级的数量（例如：具有32种不同取值的图像，可称该图像具有32个量化级别）图像数据的实际量化级别越多，视觉效果就越好，主要就是看起来过度很平滑，没有断层。</li>
</ul>
</li>
<li><p>数字图像处理概念：对数字图像信息进行加工(处理)和分析,以满足人的视觉、心理需要；或者实际应用或某种目的（如机器识别）的要求。</p>
</li>
<li><p>图像质量参数</p>
<ul>
<li>分辨率：分辨率就是图像的  水平方向的像素点数*垂直方向的像素点数</li>
<li>图像深度：一个像素点的颜色灰度所占用的二进制位的个数，如 RGB 是 256 * 256 * 256 也就是 2^24 称为 24 位真彩色。</li>
<li>图像数据量：图片占的磁盘空间。</li>
</ul>
</li>
<li><p>数字图像处理系统：</p>
<ul>
<li>采集：对某个电磁波的能量段，或者数码相机扫描仪等等</li>
<li>处理分析：可以采用软件完成也可使用硬件完成</li>
<li>通讯/存储：光盘，SD/计算机网络</li>
<li>显示：显示器，胶片等等</li>
</ul>
</li>
<li><p>图像的数字化过程：</p>
<ul>
<li>扫描：对图像进行遍历</li>
<li>采样：对于连续的图像编变成离散点的操作</li>
<li>量化：将采样得到的灰度值转换为离散的整数值</li>
</ul>
</li>
</ul>
<h2 id="3-影响清晰度的因素包括"><a href="#3-影响清晰度的因素包括" class="headerlink" title="3. 影响清晰度的因素包括"></a>3. 影响清晰度的因素包括</h2><ul>
<li><ol>
<li>亮度</li>
<li>对比度</li>
<li>尺寸大小</li>
<li>细微层次</li>
<li>颜色饱和度</li>
</ol>
</li>
</ul>
<h2 id="4-图像处理的层次："><a href="#4-图像处理的层次：" class="headerlink" title="4. 图像处理的层次："></a>4. 图像处理的层次：</h2><ol>
<li>图像处理：对图像进行处理加工以改。善视觉效果。他是一个图像到图像的过程。（比如：图像增强）</li>
<li>图像分析：以感兴趣的部分进行提取分隔和测量。他是一个图像到数据的过程。（比如：图像分割）</li>
<li>图像理解：分析的基础上做含义的理解。（比如：虹膜识别，人体识别）</li>
</ol>
<h1 id="2-Matlab-基础"><a href="#2-Matlab-基础" class="headerlink" title="2. Matlab 基础"></a>2. Matlab 基础</h1><h2 id="1-主要窗口"><a href="#1-主要窗口" class="headerlink" title="1.主要窗口"></a>1.主要窗口</h2><ul>
<li>命令窗口</li>
<li>命令历史窗口</li>
<li>工作空间窗口</li>
<li>当前路径窗口</li>
</ul>
<h2 id="2-常用命令"><a href="#2-常用命令" class="headerlink" title="2.常用命令"></a>2.常用命令</h2><ul>
<li>whos 图像信息</li>
<li>size 图像大小</li>
<li>imshow(i,[low,high]) 高于 high 为白色，低于 low 为黑色，两者之间的进行比例拉伸</li>
<li>figure；创建一个新的窗口，可以在不同窗口显示图像。</li>
<li>Subplot(m,n,p)含义为：打开一个有<strong>m</strong>行<strong>n</strong>列图像位置的窗口，并将焦点位于第<strong>p</strong>个位置上。</li>
<li>mat2gray(f,min,max) ：介于<strong>min</strong>和<strong>max</strong>的数据归一化处理， 其余小于<strong>min</strong>的元素都变为<strong>0</strong>， 大于<strong>max</strong>的元素都变为<strong>1</strong> </li>
<li>im2bw(I,T)：亮度值&lt;T的像素点全部转换为二值图像中值为0的像素点；亮度值&gt;T的像素点全部转换为二值图像中值为1的像素点。</li>
<li>rgb2gray(f)：彩色图像转换为灰度图像</li>
<li>数组：<ul>
<li>a=1:10 表示创建一个 1-10 的一个行向量</li>
<li>a=[1,2,3 ; 2,3,4] 创建一个二维的数组，用 ; 分隔</li>
<li>数组的索引是从 1 开始的</li>
<li>a(1:5) 表示取 1-5 这5个索引处的数</li>
<li>多维数组用 a(2,3) 索引，表示 2 行 3 列的元素值</li>
<li>只有一个冒号表示选择所有的行或者列  如 a(:,2) 表示选择第2列的所有元素</li>
<li>如果直接是 a(:) 则是表示把所有的列拼接成一列然后显示</li>
</ul>
</li>
<li>zeros(n,m)：n*m大小的 double 0 矩阵</li>
<li>ones：同上 1 矩阵</li>
<li>向量化循环</li>
</ul>
<h1 id="3-图像增强"><a href="#3-图像增强" class="headerlink" title="3.图像增强"></a>3.图像增强</h1><h2 id="1-图像增强："><a href="#1-图像增强：" class="headerlink" title="1.图像增强："></a>1.图像增强：</h2><p>图像增强是指对图像进行加工处理，以得到更好的视觉效果和对机器感知来说更好的图像。</p>
<ul>
<li>空域处理<ul>
<li>亮度变换</li>
<li>空间滤波</li>
</ul>
</li>
<li>频域处理</li>
</ul>
<h2 id="2-亮度变换"><a href="#2-亮度变换" class="headerlink" title="2.亮度变换"></a>2.亮度变换</h2><p>imadjust(f,[l_in,h_in],[l_out,h_out]) </p>
<ul>
<li>&lt;= l_in 映射值为 l_out</li>
<li>&gt;=h_in 映射值为 h_out</li>
<li>处于两者之间的按比例映射到 l_out 与 h_out 之间 就很像 imshow 函数</li>
</ul>
<h2 id="3-灰度直方图"><a href="#3-灰度直方图" class="headerlink" title="3. 灰度直方图"></a>3. 灰度直方图</h2><h4 id="1-直方图基础"><a href="#1-直方图基础" class="headerlink" title="1.直方图基础"></a>1.直方图基础</h4><ol>
<li>定义：图像中灰度级分布的统计，反映的是一幅图像中各灰度级像素出现的频率。</li>
<li>性质：如果一幅图像由两个不连续的区域组成，并且每个区域的直方图已知，则整幅图像的直方图是这两个区域的直方图之和。</li>
<li>核心函数：imhist(f,b) 对 f 这个图像分割成 b 个灰度级别进行统计</li>
</ol>
<h4 id="2-直方图均衡化"><a href="#2-直方图均衡化" class="headerlink" title="2. 直方图均衡化"></a>2. 直方图均衡化</h4><p>通过对原始图像的灰度进行非线性的变化，使其直方图能够均匀的分布以增加灰度值的动态范围从而达到增强图像的整体的对比度。</p>
<p>图像均衡化处理后，图像的直方图是均匀分布的，即各灰度级具有相同的出现频数</p>
<p>均衡化的具体步骤</p>
<p>均衡化的内置函数：histeq(f,b) 这个 b 就是均衡化的灰度级数，默认值是 64 一般我们手动赋值为 256 </p>
<h4 id="3-直方图规定化"><a href="#3-直方图规定化" class="headerlink" title="3.直方图规定化"></a>3.直方图规定化</h4><p>就是通过一个灰度映像函数，将原灰度直方图改造成所希望的直方图。</p>
<p>histeq(f,[]) 就是对 f 进行规定化让他近似的形成后面的那个行向量的结果。</p>
<h4 id="4-空间滤波"><a href="#4-空间滤波" class="headerlink" title="4.空间滤波"></a>4.空间滤波</h4><p>空间滤波就是使用空间模板进行图像处理，模板本身称为空间滤波器。</p>
<ul>
<li><p>分类</p>
<ul>
<li><p>按照处理的效果分类：分为平滑滤波器和锐化滤波器</p>
<p>按照处理的方式分类：分为线性滤波（均值滤波高斯滤波，也就是原始数据和滤波结果是一中算术运算关系）和非线性滤波（采用一种逻辑关系进行滤波操作，最大值、最小值、中值滤波）。总的来说当邻域中像素的运算为线性运算时，就称为线性滤波，否则称为非线性滤波。</p>
</li>
</ul>
</li>
</ul>
<h5 id="1-线性滤波"><a href="#1-线性滤波" class="headerlink" title="1.线性滤波"></a>1.线性滤波</h5><ul>
<li><p>基本概念</p>
<ul>
<li><ul>
<li>卷积：是指掩膜w在图像f中移动前，先将w  旋转180º。</li>
<li>相关：模板w在图像上移动的过程。首先需要对齐然后进行零填充，对齐的过程就是把模板的最右下角和图像的左上角对齐，然后漏出来的部分就用零来填充。</li>
<li>卷积：模板 w 在图像上移动过程，移动之前是先把 w 旋转180 度。同上。</li>
<li>上面的两个操作都有一个full 和 same 的说法，其中 full 就是保持以前的零填充不变，然后 same 则是结果去掉以前的零填充。</li>
</ul>
</li>
</ul>
</li>
<li><p>空间滤波函数</p>
<ul>
<li>imfilter(f , w , ‘corr/conv’ , ‘replicate ‘ ,’same/full’ ) 手动线性滤波函数</li>
<li>w = fspecial(‘average/gaussian/log/laplacian’,parameters[一般给0就行了])   生成线性滤波器的工具箱函数</li>
<li>最后我们使用 imfilter 的执行结果是需要去除的东西，而非是我们滤波完成后的结果，也就是我们需要用原图像减去 imfilter 的结果，才是我们最后的滤波结果。这里需要注意的就是我们原图像需要转化成double 的。也就是 im2double(f)-imfilter(f,w,’corr’,’replicate’,’same’) 才是最终的结果。</li>
</ul>
</li>
</ul>
<h5 id="2-非线性滤波"><a href="#2-非线性滤波" class="headerlink" title="2.非线性滤波"></a>2.非线性滤波</h5><ul>
<li>二维中值滤波：medfilt2(f,[n m],’zero/indexed’) 第二个参数指明的是邻域的范围。中值滤波对椒盐噪声的过滤效果非常好。</li>
</ul>
<h2 id="4-频域滤波"><a href="#4-频域滤波" class="headerlink" title="4. 频域滤波"></a>4. 频域滤波</h2><h4 id="1-基本步骤："><a href="#1-基本步骤：" class="headerlink" title="1. 基本步骤："></a>1. 基本步骤：</h4><ol>
<li>使用paddedsize获取填充参数</li>
<li>获得傅立叶变换</li>
<li>生成滤波函数</li>
<li>与滤波函数相乘进行变换</li>
<li>获取变换结果的实部</li>
<li>将左上角的居心修剪为原始大小</li>
</ol>
<p>代码：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">f=imread(<span class="string">'path'</span>);</span><br><span class="line">PQ=paddedsize(f);</span><br><span class="line">ff=fft2(f,PQ(<span class="number">1</span>),PQ(<span class="number">2</span>));</span><br><span class="line">H=lpfilter(<span class="string">'guassian'</span>,PQ(<span class="number">1</span>),PQ(<span class="number">2</span>),PQ(<span class="number">1</span>)*<span class="number">10</span>);</span><br><span class="line">g=H.*ff;</span><br><span class="line">g=<span class="built_in">real</span>(ifft2(g));</span><br><span class="line">g=g(<span class="number">1</span>:<span class="built_in">size</span>(f,<span class="number">1</span>),<span class="number">1</span>:<span class="built_in">size</span>(f,<span class="number">2</span>));</span><br><span class="line">imshow(g,[]);</span><br></pre></td></tr></table></figure>

<h4 id="2-频域滤波器和空域滤波器的比较"><a href="#2-频域滤波器和空域滤波器的比较" class="headerlink" title="2. 频域滤波器和空域滤波器的比较"></a>2. 频域滤波器和空域滤波器的比较</h4><ol>
<li><ol>
<li>如果两个域中滤波器尺寸相同，那么通常频域中进行滤波计算更为有效，更为直观，但空域中更适用更小尺寸的滤波器，更为高效。</li>
</ol>
</li>
</ol>
<h4 id="3-低通滤波器"><a href="#3-低通滤波器" class="headerlink" title="3. 低通滤波器"></a>3. 低通滤波器</h4><ol>
<li><p>从名字就可以听出来就是允许低频的成分过去而去掉高频的成分。低频成分其实就是我们的频域图的中心的白色部分。这些低频成分保留了我们的色彩等信息，也就是如果我们只有一点点低频成分的话却掉所有的高频成分我们看到的是一堆模糊的颜色没有任何的细节。</p>
<p>![屏幕快照 2018-06-30 18.00.53](/Users/lwen/Pictures/screenshot/屏幕快照 2018-06-30 18.00.53.png)</p>
</li>
</ol>
<p>振铃现象：就是图像出现加边现象。</p>
<ol start="2">
<li>理想的低通滤波器：就是他的效果不理想，会出现振铃现象。</li>
<li>巴特沃斯低通滤波器：对于2阶的这一类的低通的巴特沃斯低通滤波器是不会出现振铃现象的，但是如果随着阶数增加就会出现明显的振铃现象。</li>
<li>高斯低通滤波器：这种能够完成没有振铃现象的滤波效果，但是他的平滑效果要比巴特沃斯差一些。</li>
<li>以上三种滤波器他们的滤波效果从好到差，但是振铃现象就是从无到有。</li>
</ol>
<h4 id="4-高通滤波器"><a href="#4-高通滤波器" class="headerlink" title="4. 高通滤波器"></a>4. 高通滤波器</h4><ol>
<li>高通滤波器就是滤掉高频的成分，保留低频的，也就是会失去色彩但是保留细节，那么出来的效果其实就是锐化图像。</li>
<li>同样的我们的高频滤波器经典的还是以上三种，因为我们的高通滤波器和低通滤波器不同点就是他们用1减去后就另外一个。然后他们的滤波效果也是同上，依次递减但是振铃现象也是一次递减。越是后面的越能够凸显出图像的细节，对于细小的物体就越清晰。</li>
</ol>
<h1 id="4-图像分割"><a href="#4-图像分割" class="headerlink" title="4. 图像分割"></a>4. 图像分割</h1><h2 id="1-概念："><a href="#1-概念：" class="headerlink" title="1.概念："></a>1.概念：</h2><p>图像分割就是把图像分割成各具特征的区域，并提取感兴趣的目标的技术。</p>
<h2 id="2-方法"><a href="#2-方法" class="headerlink" title="2.方法"></a>2.方法</h2><ol>
<li>基于边缘检测的方式：找出图像的边缘，检测出图像边缘的不连续性，再将它们连成边界，这些边界把图像分成不同的区域，从而分出各个区域。</li>
<li>基于阈值的方式：根据实现提供的阈值进行对图像中不同的对象进行分类提取需要的部分。</li>
</ol>
<h2 id="3-检测方法"><a href="#3-检测方法" class="headerlink" title="3.检测方法"></a>3.检测方法</h2><h4 id="1-点检测"><a href="#1-点检测" class="headerlink" title="1.点检测"></a>1.点检测</h4><p>使用 imfilter 来检测孤立点。具体的代码可以为 <code>g=abs(imfilter(double(f),w))&gt;T</code> 也就是获取这样的点就是我们需要的点。我们的T值一般可以选择 <code>max(g)</code></p>
<h4 id="2-线检测"><a href="#2-线检测" class="headerlink" title="2. 线检测"></a>2. 线检测</h4><p>也就是和上面的点检测一模一样只是我们的模板不同而已，然后我们只需要在里面找到最大的点然后依据他来进行划分就好了。例如下面一份完整的代码：</p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">f=imread(<span class="string">'path'</span>);</span><br><span class="line">w=[...];</span><br><span class="line">gs=<span class="built_in">abs</span>(imfilter(double(f),w));</span><br><span class="line">maxVal=<span class="built_in">max</span>(gs(:));</span><br><span class="line">g=gs&gt;=maxVal;</span><br><span class="line">imshow(g);</span><br></pre></td></tr></table></figure>

<h4 id="3-边缘检测"><a href="#3-边缘检测" class="headerlink" title="3.边缘检测"></a>3.边缘检测</h4><p>边缘检测的方法有两大类：</p>
<ul>
<li>一阶导数：梯度算子</li>
<li>二阶导数：拉普拉斯算子，拉普拉斯算子一般不做真正的边缘检测只是做辅助角色。</li>
</ul>
<p>edge函数：</p>
<ul>
<li>格式：edge(f,’Sobel/Roberts/Prewitt/Canny’,parameters)</li>
</ul>
<p>一阶算子比较：</p>
<ul>
<li>Sobel 抗干扰能力强，但是检测出来的边缘很宽</li>
<li>Prewitt 既能够抗干扰也能够有合适的边缘的宽度。</li>
<li>Roberts 抗干扰能力不强，但是边缘窄</li>
</ul>
<h4 id="4-阈值分割"><a href="#4-阈值分割" class="headerlink" title="4.阈值分割"></a>4.阈值分割</h4><ol>
<li><p>迭代法：就是先取 (max+min)/2 分成两部分，然后每一部分的图像平均值为 g1 和 g2 那么就可以算出下一个阈值为 (g1+g2)/2 重复上面的操作然后一直到达某一个特定的阈值之后才停止。这个过程可以手动编码一可以使用工具箱中的自带的函数 <code>T=graythresh(f)</code></p>
</li>
<li><p><code>otsu()</code>函数计算阈值。</p>
</li>
</ol>
<h1 id="5-形态学处理"><a href="#5-形态学处理" class="headerlink" title="5.形态学处理"></a>5.形态学处理</h1><h2 id="1-膨胀"><a href="#1-膨胀" class="headerlink" title="1.膨胀"></a>1.膨胀</h2><p>腐蚀就是将一个模板进行原点的反转，然后和图形中的每一个像素进行移动，将模板与他交集不为空的位置设置为1。主要使用的函数就是 imdilate(f,w)</p>
<p>作用就是：使图像加粗变大，桥接裂缝。</p>
<h2 id="2-腐蚀"><a href="#2-腐蚀" class="headerlink" title="2.腐蚀"></a>2.腐蚀</h2><p>腐蚀同上的操作只是只是在模板完全被包裹的时候这个模板的中心才是 1 ，其余位置都为 0。和他相关的函数就是imerode(f,w)</p>
<p>同样的他的作用就是使图像变小变细，消除边界点，消除联通，消除凸起，平滑图像。</p>
<h2 id="3-开运算"><a href="#3-开运算" class="headerlink" title="3.开运算"></a>3.开运算</h2><p>开运算：使图像的轮廓变得光滑，断开狭窄的间断和消除细的突出物。    imopen(f,w)</p>
<p>先对A进行B腐蚀然后再进行B膨胀，因为主导地位的是腐蚀操作，所以基本他对外表示的特征就是腐蚀的特征。</p>
<h2 id="4-闭运算"><a href="#4-闭运算" class="headerlink" title="4.闭运算"></a>4.闭运算</h2><p>闭运算也能够平滑图像，它与开运算不同的就是它能够连接细长的鸿沟，填补裂痕。imclose(f,w)</p>
<p>他的操作也就是先进行膨胀操作然后进行腐蚀操作。</p>
<h2 id="5-击中击不中变换"><a href="#5-击中击不中变换" class="headerlink" title="5.击中击不中变换"></a>5.击中击不中变换</h2><p>这个操作就是一个图像一个模板，第一次进行的是原图像和原模板进行击中变换操作其实就是类似于腐蚀操作，然后就是进行原图像的补与原模板的补的腐蚀操作，最后两次操作的结果进行与运算。</p>
<h2 id="6-顶帽变换"><a href="#6-顶帽变换" class="headerlink" title="6.顶帽变换"></a>6.顶帽变换</h2><p>其实这个变换就是原图像减去开运算的结果。也有对应的函数就是 </p>
<figure class="highlight matlab"><table><tr><td class="code"><pre><span class="line">se=strel(<span class="string">'disk'</span>,<span class="number">10</span>);</span><br><span class="line">imtophat(f,se);</span><br></pre></td></tr></table></figure>

<h1 id="6-图像复原"><a href="#6-图像复原" class="headerlink" title="6.图像复原"></a>6.图像复原</h1><h2 id="1-基本概念-1"><a href="#1-基本概念-1" class="headerlink" title="1.基本概念"></a>1.基本概念</h2><ol>
<li>图像退化：图像退化指的就是由于成像系统的各种因素使得图像的成像质量变低</li>
<li>图像复原的目的：就是尽可能的消除图像在获取及传输过程中造成的图像的品质下降的即退化现象退服图像本来的面目。</li>
<li>图像增强：旨在改善图像质量。提高图像的可懂度。更偏向主观判断，即要突出所关心的信息，满足人的视觉系统，具有好的视觉结果。 </li>
<li>图像复原：根据图像畸变或退化的原因，进行模型化处理，将质量退化的图像重建或恢复到原始图像，即恢复退化图像的本来面目，忠实于原图像。因此必须根据一定的图像退化模型来进行图像复原。</li>
<li>噪声模型：加型噪声，乘型噪声</li>
<li>几种常见的噪声：<ol>
<li>高斯噪声：每一点都有噪声，但是噪声的幅值是随机的。</li>
<li>椒盐噪声：噪声的位置是随机的，但是噪声的幅值是基本相同。</li>
</ol>
</li>
<li>图像复原：<ol>
<li>如果是我们的图像的退化仅仅是由噪声引起的那么我们就应该使用空间滤波，至于线性和非线性都是可以的，如线性的就是 imfilter 还有非线性的 medfilt 、ordfilt</li>
<li>利用频域处理消除周期性噪声。</li>
</ol>
</li>
</ol>
<h1 id="7-图像压缩"><a href="#7-图像压缩" class="headerlink" title="7.图像压缩"></a>7.图像压缩</h1><p>1.图像数据冗余：</p>
<ul>
<li><ul>
<li><ul>
<li>编码冗余：编码冗余就是但我们的灰度级编码多余实际需要的编码的时候就会出现编码冗余。</li>
<li>像素间的冗余：单个像素携带的信息较少，单个像素的贡献是多余的。</li>
<li>心理视觉冗余：不重要的视觉信息称为心理视觉冗余。</li>
</ul>
</li>
</ul>
</li>
</ul>
<ol start="2">
<li>图像压缩系统的基本结构<ol>
<li>映射转换器：把输入图像转换成冗余度比较小的数据格式，便于压缩、</li>
<li>量化器：产生用于表示被压缩图像的有限数量的符号集合</li>
<li>符号编码器：对每一个符号分配一个码字或者二进制比特流。</li>
<li>符号解码器</li>
<li>反映射转换器</li>
</ol>
</li>
</ol>
<h1 id="8-试卷"><a href="#8-试卷" class="headerlink" title="8. 试卷"></a>8. 试卷</h1><ol>
<li>图像工程的三个层次：图像处理、图像分析、图像理解</li>
<li>低频是细节、高频是轮廓、噪声一般也是高频的。</li>
<li>细节性的图像的空间分辨率精细幅度分辨率粗糙，而那种合照这是刚好相反。</li>
<li>当图像进行缩小变化的时候傅里叶图像是被延展的。</li>
<li>K——L 变化成为最佳变换</li>
<li>WHT 计算最简单但是效果最差</li>
<li>DFT 回复图像有振铃现象</li>
<li>中值滤波器是在消除噪声的同时保护边缘信息。</li>
<li>三个小概念<ol>
<li>亮度：指的是画面的明暗度，</li>
<li>对比度：画面的黑与白的渐变层次，比值越大就说明层次越多图像越清晰，色彩明晰靓丽。</li>
<li>饱和度：指的是颜色的纯度</li>
</ol>
</li>
<li>图像分割的方法：<ol>
<li>基于区域生长的</li>
<li>边缘检测</li>
<li>基于阈值的方法</li>
</ol>
</li>
<li><em>拉普拉斯算子</em>为二阶差分，其方向信息丢失，常产生双像素，对噪声有双倍加强<em>作用</em>，因此它很少直接用于边缘检测</li>
<li></li>
</ol>
]]></content>
      <tags>
        <tag>数字图像</tag>
      </tags>
  </entry>
  <entry>
    <title>Docker 入门</title>
    <url>/2018/04/29/Linux/Docker%20%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h2 id="1-Docker-简介"><a href="#1-Docker-简介" class="headerlink" title="1. Docker 简介"></a>1. Docker 简介</h2><p>直接运行于操作系统内核上的虚拟化解决方案，他是一个操作系统级别的虚拟化也就是说容器只能运行在相同或者相似的内和操作系统之上的。所以我们只能在 docker 中运行 Linux 系统而不能运行 Windows 系统。他是依赖于 Linux 的内核特性：Namespace 和 Cgroup （Control Group）。</p>
<a id="more"></a>

<h3 id="1-VM-vs-Docker"><a href="#1-VM-vs-Docker" class="headerlink" title="1. VM vs Docker"></a>1. VM vs Docker</h3><p><img src="https://user-images.githubusercontent.com/22151420/39402559-e8dd93d6-4b94-11e8-90ca-1377aee1a335.png" alt="image"></p>
<p>可以看到，在虚拟机上我们需要包含到细腻华技术和操作系统，但是我们在 Docker 中只需要依赖底层的操作系统，和一些必要的库，所以在空间上有非常大的优势，另外就是速度上，由于启动的服务更少，无需启动 OS 所以开销更小。</p>
<h3 id="2-镜像"><a href="#2-镜像" class="headerlink" title="2. 镜像"></a>2. 镜像</h3><p><img src="https://user-images.githubusercontent.com/22151420/39402643-9a0cb338-4b97-11e8-9f8b-77c16fcf1b0d.png" alt="image"></p>
<p>docker 的镜像组成如上图，最底层就启动层，然后就是 root 文件系统，接着是我们自己叠加的各种应用。注意这里的每一层都是只读的，但是在我们的 linux 中，root 一开始是只读的，加载完毕后是可写的，但是这里不同，主要就是因为可以进行镜像的叠加。然后使用联合加载技术，同时把所有的镜像层加载进去。</p>
<h3 id="3-容器"><a href="#3-容器" class="headerlink" title="3. 容器"></a>3. 容器</h3><p>docker 的镜像相当于一个 class 类，然后容器则是 new 出来的对象。容器是基于镜像运行出来的，但是我们有时候还是需要对镜像或者容器进行修改，这里采用的方式就是在镜像的最上面一层添加上一层虚拟的层，写得操作都是在这一层，当我们要生成新的镜像的时候就是把这一层设置为只读。这也是 docker 的 CopyOnWrite 技术。 </p>
<h3 id="4-Namespace-和-Cgroups"><a href="#4-Namespace-和-Cgroups" class="headerlink" title="4. Namespace 和 Cgroups"></a>4. Namespace 和 Cgroups</h3><p>Linux 内核实现的 Namespace 就是用于文件系统，进程，网络，IPC，MNT，UTS 的隔离。而 Cgroups 是用于对上面的 namespace 的资源限制，优先级设定。</p>
<h2 id="2-容器基本操作"><a href="#2-容器基本操作" class="headerlink" title="2. 容器基本操作"></a>2. 容器基本操作</h2><h4 id="1-启动容器"><a href="#1-启动容器" class="headerlink" title="1.启动容器"></a>1.启动容器</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run image args cmd</span><br></pre></td></tr></table></figure>

<h4 id="2-创建交互容器"><a href="#2-创建交互容器" class="headerlink" title="2.创建交互容器"></a>2.创建交互容器</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run -i -t ubuntu /bin/bash</span><br></pre></td></tr></table></figure>

<h4 id="3-查看所有容器-最近创建的容器"><a href="#3-查看所有容器-最近创建的容器" class="headerlink" title="3. 查看所有容器/最近创建的容器"></a>3. 查看所有容器/最近创建的容器</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker ps -a/-l</span><br></pre></td></tr></table></figure>

<h4 id="4-检查-docker-容器，返回详细信息"><a href="#4-检查-docker-容器，返回详细信息" class="headerlink" title="4. 检查 docker 容器，返回详细信息"></a>4. 检查 docker 容器，返回详细信息</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker inspect ubuntu</span><br></pre></td></tr></table></figure>

<h4 id="5-设置容器名"><a href="#5-设置容器名" class="headerlink" title="5. 设置容器名"></a>5. 设置容器名</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run --name ubu ubuntu</span><br></pre></td></tr></table></figure>

<h4 id="6-以交互方式启动已经停止的容器"><a href="#6-以交互方式启动已经停止的容器" class="headerlink" title="6. 以交互方式启动已经停止的容器"></a>6. 以交互方式启动已经停止的容器</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker start -i ubu</span><br></pre></td></tr></table></figure>

<h4 id="7-删除停止的容器"><a href="#7-删除停止的容器" class="headerlink" title="7. 删除停止的容器"></a>7. 删除停止的容器</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker rm ubu</span><br></pre></td></tr></table></figure>

<h4 id="8-守护运行"><a href="#8-守护运行" class="headerlink" title="8. 守护运行"></a>8. 守护运行</h4><p>在运行docker 容器以后我们要退出的时候不要用 <code>ctrl+c</code> 或者 <code>exit</code> ，使用 <code>ctrl+p/q</code> 既可以实现以 Daemon 方式运行容器。而此时如果我们需要再次进入容器我们需要使用 <code>docker attach name</code> 来进入容器。</p>
<p>另外我们还可以使用 <code>-d</code> 参数在运行容器的时候让他进入 Daemon 状态。</p>
<h4 id="9-查看输出"><a href="#9-查看输出" class="headerlink" title="9. 查看输出"></a>9. 查看输出</h4><p>当我们在后台状态运行容器的时候有时候我们需要查看对应的输出，就可以使用 </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker logs -f -t --tail nun name</span><br></pre></td></tr></table></figure>

<ul>
<li>-f 是实时更新输出（follow）</li>
<li>-t 则是带有时间的日志（timestamp）</li>
<li>—tail 指定结尾行数</li>
</ul>
<h4 id="10-查看运行容器中运行的进程"><a href="#10-查看运行容器中运行的进程" class="headerlink" title="10. 查看运行容器中运行的进程"></a>10. 查看运行容器中运行的进程</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker top name</span><br></pre></td></tr></table></figure>

<h4 id="11-在运行的容器中启动新的进程-运行新的命令"><a href="#11-在运行的容器中启动新的进程-运行新的命令" class="headerlink" title="11. 在运行的容器中启动新的进程/运行新的命令"></a>11. 在运行的容器中启动新的进程/运行新的命令</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker <span class="built_in">exec</span> -i/-t  name <span class="comment">#类似 run</span></span><br></pre></td></tr></table></figure>

<h4 id="12-停止容器"><a href="#12-停止容器" class="headerlink" title="12. 停止容器"></a>12. 停止容器</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker stop name <span class="comment">#信号</span></span><br><span class="line">docker <span class="built_in">kill</span> name <span class="comment">#杀死</span></span><br></pre></td></tr></table></figure>

<h2 id="3-镜像操作"><a href="#3-镜像操作" class="headerlink" title="3. 镜像操作"></a>3. 镜像操作</h2><h4 id="1-查看镜像"><a href="#1-查看镜像" class="headerlink" title="1. 查看镜像"></a>1. 查看镜像</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker [-f --no-tunck -a -q] images</span><br></pre></td></tr></table></figure>

<ul>
<li>-f 使用过滤器，过滤部分镜像</li>
<li>—no-tunck 不进行 id 的截断</li>
<li>-a 显示所有的镜像</li>
<li>-q 只显示镜像 id</li>
</ul>
<h4 id="2-tag"><a href="#2-tag" class="headerlink" title="2. tag"></a>2. tag</h4><p>表示一个镜像的不同版本。</p>
<h4 id="3-删除镜像"><a href="#3-删除镜像" class="headerlink" title="3. 删除镜像"></a>3. 删除镜像</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker rmi name:tag</span><br></pre></td></tr></table></figure>

<p>删除镜像，当我们删除的是很多的镜像的一个 tag 则是 untag 操作。</p>
<h4 id="4-查找镜像"><a href="#4-查找镜像" class="headerlink" title="4. 查找镜像"></a>4. 查找镜像</h4><ol>
<li><p>dockerhub 网站</p>
</li>
<li><p>使用 search 命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker search [-s num] name</span><br></pre></td></tr></table></figure>

<ul>
<li>-s 过滤最低星级</li>
</ul>
</li>
</ol>
<h4 id="5-获取镜像"><a href="#5-获取镜像" class="headerlink" title="5. 获取镜像"></a>5. 获取镜像</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker [-a] pull name</span><br></pre></td></tr></table></figure>

<p>-a 这个参数就是下载所有 tag 的镜像。</p>
<h4 id="6-push-镜像"><a href="#6-push-镜像" class="headerlink" title="6. push 镜像"></a>6. push 镜像</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker push rep/name</span><br></pre></td></tr></table></figure>

<p>在上传的时候我们只上传了修改的部分，而不是全部。</p>
<h4 id="7-构建-docker-镜像"><a href="#7-构建-docker-镜像" class="headerlink" title="7. 构建 docker 镜像"></a>7. 构建 docker 镜像</h4><h5 id="1-docker-commit"><a href="#1-docker-commit" class="headerlink" title="1. docker commit"></a>1. docker commit</h5><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker commit [-a -m ] name rep/name</span><br></pre></td></tr></table></figure>

<p>把镜像提交成一个新的镜像。</p>
<ul>
<li>-a 作者信息</li>
<li>-m 提交信息</li>
</ul>
<h5 id="2-docker-file"><a href="#2-docker-file" class="headerlink" title="2. docker file"></a>2. docker file</h5><p>创建 dockerfile 然后使用 <code>docker bulid -t name filepath</code> 构建镜像。</p>
<h4 id="8-Dockerfile"><a href="#8-Dockerfile" class="headerlink" title="8. Dockerfile"></a>8. Dockerfile</h4><ol>
<li>FROM 第一条非注释命令，表示采用那个镜像作为基础镜像</li>
<li>MAINTAINER 维护者信息</li>
<li>RUN cmd  每一个 run 都会创建一个新的层</li>
<li>EXPOSE port 开放端口</li>
<li>CMD 运行时运行，会被 RUN 覆盖</li>
<li>ENTERPOINT  同上，但是不会被 RUN 覆盖</li>
<li>ADD/COPY src des 复制文件，ADD 有类似解压功能，COPY 没有</li>
</ol>
<h2 id="4-Docker-容器连接"><a href="#4-Docker-容器连接" class="headerlink" title="4. Docker 容器连接"></a>4. Docker 容器连接</h2><h3 id="1-docker-之间的连接"><a href="#1-docker-之间的连接" class="headerlink" title="1. docker 之间的连接"></a>1. docker 之间的连接</h3><p>我们可以使用 </p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">docker run --link=name:<span class="built_in">alias</span></span><br></pre></td></tr></table></figure>

<p>接下来访问响应的主机我们只需要使用 alias 即可。因为实际上是在这台机子上做了很多的环境变量还有 host 的修改导致的。</p>
<h3 id="2-拒绝连接"><a href="#2-拒绝连接" class="headerlink" title="2. 拒绝连接"></a>2. 拒绝连接</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">--icc =<span class="literal">false</span> </span><br><span class="line">--link</span><br><span class="line">--iptables=<span class="literal">true</span></span><br></pre></td></tr></table></figure>

<p>如果配置了 <code>—icc=false</code> 那么就是拒绝所有的连接。而当我们配置了 iptables 则只允许 link 指定的容器。</p>
<h2 id="5-数据卷"><a href="#5-数据卷" class="headerlink" title="5. 数据卷"></a>5. 数据卷</h2><p><img src="https://user-images.githubusercontent.com/22151420/39403129-1024c864-4ba5-11e8-8d1a-6fb0b28b2d94.png" alt="image"></p>
<p>数据卷说白了就是数据映射，将本机的数据映射到 docker 容器中。</p>
]]></content>
      <categories>
        <category>Docker</category>
      </categories>
      <tags>
        <tag>Docker</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringMVC 踩坑记录</title>
    <url>/2018/04/22/SpringMVC/Spring%E8%B8%A9%E5%9D%91%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[<h2 id="1-处理静态资源"><a href="#1-处理静态资源" class="headerlink" title="1. 处理静态资源"></a>1. 处理静态资源</h2><p>静态资源直接放在 webapp/web 下，而我们的模板一般是在 WEB-INF 下，但是 WEN-INF     下的东西一般不让访问的，模板之所以能访问到是因为有模板引擎的映射，但是我们的警惕资源应该是直接能访问的东西，直接放在 web 下类似于 jsp 直接访问，而不能放在 WEB-INF 下，并且我们要开启静态资源访问 <code>&lt;mvc:default-servlet-handler/&gt;</code></p>
<h2 id="2-Thymeleaf-乱码问题"><a href="#2-Thymeleaf-乱码问题" class="headerlink" title="2. Thymeleaf 乱码问题"></a>2. Thymeleaf 乱码问题</h2><p>一开始除了乱码想着直接在 web.xml 中配置编码过滤器，接着发现根本没用还是一样的乱码，并且返回的是 ISO-8859-1的西欧字符集。后来想着如果是不是 SpringMVC 的问题，那么可能就是因为视图是被 Thymeleaf 渲染的导致的问题，然后找到 Thymeleaf 在 SpringMVC 中的配置，然后更改编码。重新启动项目才行。</p>
]]></content>
      <categories>
        <category>SpringMVC</category>
      </categories>
      <tags>
        <tag>SpringMVC</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringMVC 整合</title>
    <url>/2018/04/21/SpringMVC/Spring%20%E6%95%B4%E5%90%88/</url>
    <content><![CDATA[<p>今天一开始直接用了 Idea 来创建一个 SpringMVC+Spring+Mybatis+Thymeleaf 的项目，一开始还是挺顺利的，除了在 Thymeleaf 那个地方卡了一下，后面项目还是顺利跑起来了。</p>
<p>接着想用 Maven 搭建，因为一开始用 Idea 生成的项目使用的手动导入 jar 这样非常费力，为了一劳永逸和简单就采用了 maven 来构建项目。最后发现自己陷入了一个大坑，好久没有跳上来。</p>
<p>接着我就把用 Maven 搭建 SpringMVC + Spring + Mybatis + Thymeleaf 项目过程写下来，免得日后再采坑，以后项目直接拷贝就可以了不用再配置了！</p>
<h2 id="1-创建-Maven-项目"><a href="#1-创建-Maven-项目" class="headerlink" title="1. 创建 Maven 项目"></a>1. 创建 Maven 项目</h2><p><a href="https://simimg.com/i/lo8Gl" target="_blank" rel="noopener"><img src="https://s1.ax2x.com/2018/04/21/lo8Gl.md.png" alt="lo8Gl.md.png"></a> </p>
<h2 id="2-添加-web-模块"><a href="#2-添加-web-模块" class="headerlink" title="2.添加 web 模块"></a>2.添加 web 模块</h2><p><img src="https://s1.ax2x.com/2018/04/21/loSLJ.png" alt="loSLJ.png"></p>
<p>在这里面勾选上 web 模块，接着我们会看到我们的项目中多了一个 web 目录也就是最重要的 WEB-INF 等等。</p>
<h2 id="3-web-模块移动"><a href="#3-web-模块移动" class="headerlink" title="3. web 模块移动"></a>3. web 模块移动</h2><p><img src="https://s1.ax1x.com/2018/04/21/CMJk0U.png" alt="CMJk0U.png"></p>
<p>我们把 web 模块移动到这里，方便观看。以及在 maven 项目之内，接着我们需要改一下项目配置，因为我们移动了这个模块。如果移动的时候更新了依赖就无需这步操作。</p>
<p><a href="https://imgchr.com/i/CMJZtJ" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2018/04/21/CMJZtJ.md.png" alt="CMJZtJ.md.png"></a></p>
<h2 id="4-配置文件"><a href="#4-配置文件" class="headerlink" title="4. 配置文件"></a>4. 配置文件</h2><p><img src="https://s1.ax1x.com/2018/04/21/CMJeh9.png" alt="CMJeh9.png"></p>
<p>创建配置文件如上。</p>
<ol>
<li>spring 的配置文件 applicationContext.xml</li>
</ol>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:mvc</span>=<span class="string">"http://www.springframework.org/schema/mvc"</span> <span class="attr">xmlns:tx</span>=<span class="string">"http://www.springframework.org/schema/tx"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--配置包扫描--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.lwen"</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置静态资源访问--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:default-servlet-handler</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置注解--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:annotation-driven</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置 thymeleaf 视图解析器  类似于 jsp 解析器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"templateResolver"</span> <span class="attr">class</span>=<span class="string">"org.thymeleaf.spring4.templateresolver.SpringResourceTemplateResolver"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"prefix"</span> <span class="attr">value</span>=<span class="string">"/WEB-INF/templates/"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"suffix"</span> <span class="attr">value</span>=<span class="string">".html"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"templateMode"</span> <span class="attr">value</span>=<span class="string">"HTML5"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"templateEngine"</span> <span class="attr">class</span>=<span class="string">"org.thymeleaf.spring4.SpringTemplateEngine"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"templateResolver"</span> <span class="attr">ref</span>=<span class="string">"templateResolver"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.thymeleaf.spring4.view.ThymeleafViewResolver"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"templateEngine"</span> <span class="attr">ref</span>=<span class="string">"templateEngine"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--配置外部配置文件--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:property-placeholder</span> <span class="attr">location</span>=<span class="string">"classpath:properties.properties"</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置druid数据库连接池--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"dataSource"</span> <span class="attr">class</span>=<span class="string">"com.alibaba.druid.pool.DruidDataSource"</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 数据库基本信息配置 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"url"</span> <span class="attr">value</span>=<span class="string">"$&#123;url&#125;"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"username"</span> <span class="attr">value</span>=<span class="string">"$&#123;username&#125;"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"password"</span> <span class="attr">value</span>=<span class="string">"$&#123;password&#125;"</span> /&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"driverClassName"</span> <span class="attr">value</span>=<span class="string">"$&#123;driverClassName&#125;"</span> /&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 最大并发连接数 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"maxActive"</span> <span class="attr">value</span>=<span class="string">"$&#123;maxActive&#125;"</span> /&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 初始化连接数量 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"initialSize"</span> <span class="attr">value</span>=<span class="string">"$&#123;initialSize&#125;"</span> /&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置 spring 事务管理器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.springframework.jdbc.datasource.DataSourceTransactionManager"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dataSource"</span> <span class="attr">ref</span>=<span class="string">"dataSource"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置注解事务--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tx:annotation-driven</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置手动路由--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:view-controller</span> <span class="attr">path</span>=<span class="string">"/"</span> <span class="attr">view-name</span>=<span class="string">"index"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ol>
<li>springMVC 配置文件 dispatcher-servlet.xml</li>
</ol>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd"</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<ol>
<li><p>数据库配置文件 properties.properties</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="attr">url</span>=<span class="string">jdbc:mysql://localhost:3306/dbspring</span></span><br><span class="line"><span class="attr">username</span>=<span class="string">root</span></span><br><span class="line"><span class="attr">password</span>=<span class="string">12345678</span></span><br><span class="line"><span class="attr">driverClassName</span>=<span class="string">com.mysql.jdbc.Driver</span></span><br><span class="line"><span class="attr">maxActive</span>=<span class="string">500</span></span><br><span class="line"><span class="attr">initialSize</span>=<span class="string">5</span></span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="5-pom-xml"><a href="#5-pom-xml" class="headerlink" title="5. pom.xml"></a>5. pom.xml</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">project</span> <span class="attr">xmlns</span>=<span class="string">"http://maven.apache.org/POM/4.0.0"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">modelVersion</span>&gt;</span>4.0.0<span class="tag">&lt;/<span class="name">modelVersion</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.lwen<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>springmvc<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0-SNAPSHOT<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">profiles</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">profile</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">id</span>&gt;</span>jdk‐1.8<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">activation</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">activeByDefault</span>&gt;</span>true<span class="tag">&lt;/<span class="name">activeByDefault</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">jdk</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">jdk</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">activation</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">maven.compiler.source</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">maven.compiler.source</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">maven.compiler.target</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">maven.compiler.target</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">maven.compiler.compilerVersion</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">maven.compiler.compilerVersion</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">profile</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">profiles</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.mybatis<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>mybatis<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.4.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.thymeleaf<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>thymeleaf<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.0.9.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.thymeleaf<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>thymeleaf-spring4<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>3.0.9.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>aopalliance<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>aopalliance<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>commons-logging<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>commons-logging<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.2<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>com.alibaba<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>druid<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.1.9<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-aop<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-beans<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-context<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-context-support<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.14.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-expression<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.13.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-instrument<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-instrument-tomcat<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-jdbc<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-jms<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-messaging<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-orm<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-oxm<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-test<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-tx<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-webmvc<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-webmvc-portlet<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-websocket<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">version</span>&gt;</span>4.3.16.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">project</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="6-配置-jar-依赖"><a href="#6-配置-jar-依赖" class="headerlink" title="6. 配置 jar 依赖"></a>6. 配置 jar 依赖</h2><p><a href="https://imgchr.com/i/CMJ10O" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2018/04/21/CMJ10O.md.png" alt="CMJ10O.md.png"></a></p>
<p>这步很重要，否则会一直报错，说找不到 class。</p>
]]></content>
      <categories>
        <category>SpringMVC</category>
      </categories>
      <tags>
        <tag>SpringMVC</tag>
      </tags>
  </entry>
  <entry>
    <title>iTerm2 配置及使用技巧</title>
    <url>/2018/04/20/Tools/iTerm2%20%E9%85%8D%E7%BD%AE%E5%8F%8A%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<h2 id="1-技巧："><a href="#1-技巧：" class="headerlink" title="1. 技巧："></a>1. 技巧：</h2><h3 id="1-选中"><a href="#1-选中" class="headerlink" title="1. 选中"></a>1. 选中</h3><p>双击选中，三击选中整行，选中即复制。即任何选中状态的字符串都被放到了系统剪切板中。</p>
<h3 id="2-command"><a href="#2-command" class="headerlink" title="2. command"></a>2. command</h3><ul>
<li>可以拖拽选中的字符串；</li>
<li>点击 url：调用默认浏览器访问该网址；</li>
<li>点击文件：调用默认程序打开文件</li>
</ul>
<h3 id="3-快捷键"><a href="#3-快捷键" class="headerlink" title="3. 快捷键"></a>3. 快捷键</h3><ul>
<li>切换 tab：⌘+←, ⌘+→</li>
<li>切换分屏：⌘+[/]</li>
<li>新建 tab：⌘+t；</li>
<li>切分屏幕：⌘+d 水平切分，⌘+Shift+d 垂直切分；</li>
<li>智能查找，支持正则查找：⌘+f。</li>
</ul>
<table>
<thead>
<tr>
<th>命令</th>
<th>说明</th>
</tr>
</thead>
<tbody><tr>
<td>command + t</td>
<td>新建标签</td>
</tr>
<tr>
<td>command + w</td>
<td>关闭标签</td>
</tr>
<tr>
<td>command + 数字 command + 左右方向键</td>
<td>切换标签</td>
</tr>
<tr>
<td>command + enter</td>
<td>切换全屏</td>
</tr>
<tr>
<td>command + f</td>
<td>查找</td>
</tr>
<tr>
<td>command + d</td>
<td>垂直分屏</td>
</tr>
<tr>
<td>command + shift + d</td>
<td>水平分屏</td>
</tr>
<tr>
<td>command + option + 方向键 command + [ 或 command + ]</td>
<td>切换屏幕</td>
</tr>
<tr>
<td>command + ;</td>
<td>查看历史命令</td>
</tr>
<tr>
<td>command + shift + h</td>
<td>查看剪贴板历史</td>
</tr>
<tr>
<td>ctrl + u</td>
<td>清除当前行</td>
</tr>
<tr>
<td>ctrl + l</td>
<td>清屏</td>
</tr>
<tr>
<td>ctrl + a</td>
<td>到行首</td>
</tr>
<tr>
<td>ctrl + e</td>
<td>到行尾</td>
</tr>
<tr>
<td>ctrl + f/b</td>
<td>前进后退</td>
</tr>
<tr>
<td>ctrl + p</td>
<td>上一条命令</td>
</tr>
<tr>
<td>ctrl + r</td>
<td>搜索命令历史</td>
</tr>
</tbody></table>
<h3 id="4-自动完成"><a href="#4-自动完成" class="headerlink" title="4. 自动完成"></a>4. 自动完成</h3><ul>
<li>cmd+; 自动完成</li>
<li>cmd+shift+h 历史记录</li>
</ul>
<h3 id="5-autojump"><a href="#5-autojump" class="headerlink" title="5. autojump"></a>5. autojump</h3><ul>
<li>使用 j 目录名，无需完整的即可。</li>
<li>输入 d 显示之前跳过的目录</li>
</ul>
<h3 id="6-suggestions"><a href="#6-suggestions" class="headerlink" title="6. suggestions"></a>6. suggestions</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/zsh-users/zsh-autosuggestions ~/.oh-my-zsh/custom/plugins/zsh-autosuggestions.git</span><br></pre></td></tr></table></figure>

<p>在 .zshrc 中添加 <code>zsh-autosuggestions</code> 接着执行如下命令</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">➜  ~ <span class="built_in">source</span> .zshrc</span><br><span class="line">➜  ~ <span class="built_in">source</span> .oh-my-zsh/custom/plugins/zsh-autosuggestions/zsh-autosuggestions.zsh</span><br></pre></td></tr></table></figure>

<p>即可使用了！</p>
]]></content>
      <categories>
        <category>开发工具</category>
      </categories>
      <tags>
        <tag>开发工具</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringMVC 基础</title>
    <url>/2018/04/18/SpringMVC/SpringMVC%20%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<h2 id="1-SpringMVC-基础原理"><a href="#1-SpringMVC-基础原理" class="headerlink" title="1.SpringMVC 基础原理"></a>1.SpringMVC 基础原理</h2><ol>
<li>C     前端控制器 ——&gt; DispatcherServlet</li>
<li>M    数据对象</li>
<li>V      视图处理器ViewResvor</li>
</ol>
<p>&lt;!—more–&gt;</p>
<h3 id="处理步骤："><a href="#处理步骤：" class="headerlink" title="处理步骤："></a>处理步骤：</h3><ol>
<li>发起请求到前端控制器 <code>DispatcherServlet</code> </li>
<li>然后这个控制器会调用 <code>HandlerMapping</code>  查找对应的 <code>Controller</code>或者说 <code>Handler</code> </li>
<li>找到了对应的 <code>Controller</code> 就让 <code>HandlerAdaptor</code> 去执行 <code>handler</code> </li>
<li>执行了 <code>handler</code> 以后返回的就是 <code>ModelAndView</code> 对象。</li>
<li>对象返回给前端控制器，然后前端控制器会丢给 <code>ViewResovr</code> 去解析</li>
<li>然后继续返回给前端控制器并返回给用户。</li>
</ol>
<h2 id="2-基础程序"><a href="#2-基础程序" class="headerlink" title="2.基础程序"></a>2.基础程序</h2><h4 id="1-配置基础的-web-xml-加载-SpringMVC"><a href="#1-配置基础的-web-xml-加载-SpringMVC" class="headerlink" title="1. 配置基础的 web.xml 加载 SpringMVC"></a>1. 配置基础的 web.xml 加载 SpringMVC</h4><p>在 web.xml 中我们需要配一个 Servlet 和一个 Listener ，这个 Servlet 其实就是我们的路由调度器，然后 Listener 则是上下文监听器。还有一个初始化参数就是指定 spring 的配置文件的位置。</p>
<p>其实这些配置基本都是固定的，在使用 idea 建立 SpringMVC 项目的时候他会自动的帮我们配置好，但是我们还是需要在进行一些配置。主要的配置如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">web-app</span> <span class="attr">xmlns</span>=<span class="string">"http://xmlns.jcp.org/xml/ns/javaee"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span> <span class="attr">xmlns:mvc</span>=<span class="string">"http://www.springframework.org/schema/mvc"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">version</span>=<span class="string">"4.0"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置 spring 的配置文件--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context-param</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>contextConfigLocation<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>/WEB-INF/applicationContext.xml<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">context-param</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">listener</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">listener-class</span>&gt;</span>org.springframework.web.context.ContextLoaderListener<span class="tag">&lt;/<span class="name">listener-class</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">listener</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>dispatcher<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span>org.springframework.web.servlet.DispatcherServlet<span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">load-on-startup</span>&gt;</span>1<span class="tag">&lt;/<span class="name">load-on-startup</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>dispatcher<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--1. /  所有的都进行解析，但是不管 jsp</span></span><br><span class="line"><span class="comment">            2. *.from</span></span><br><span class="line"><span class="comment">            3. /* 不能用 所有的都拦截</span></span><br><span class="line"><span class="comment">        --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>HttpHiddenMethods<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-class</span>&gt;</span>org.springframework.web.filter.HiddenHttpMethodFilter<span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">filter</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>HttpHiddenMethods<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">web-app</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-配置基础的-spring-配置文件"><a href="#2-配置基础的-spring-配置文件" class="headerlink" title="2. 配置基础的 spring 配置文件"></a>2. 配置基础的 spring 配置文件</h3><p>接着就是配置 spring 配置文件，可以看到在上面的 web.xml 中我们在初始化参数中指定了 spring 的配置文件就是 applicationContext.xml 放在了 WEB-INF 路径下面。</p>
<p>然后需要配置一些核心的 bean 让 spring 进行自动加载，以需要配置扫描的包。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:mvc</span>=<span class="string">"http://www.springframework.org/schema/mvc"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--&amp;lt;!&amp;ndash;处理器映射器&amp;ndash;&amp;gt;--&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--&lt;bean class="org.springframework.web.servlet.handler.BeanNameUrlHandlerMapping"/&gt;--&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--&amp;lt;!&amp;ndash;处理器适配器&amp;ndash;&amp;gt;--&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--&lt;bean class="org.springframework.web.servlet.mvc.SimpleControllerHandlerAdapter"/&gt;--&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!--视图解析器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.springframework.web.servlet.view.InternalResourceViewResolver"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"prefix"</span> <span class="attr">value</span>=<span class="string">"/WEB_INF/templates/"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"suffix"</span> <span class="attr">value</span>=<span class="string">".jsp"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 配置需要扫描的包--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"com.mvc"</span>/&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--必须要的，配置了 springMVC 的注解生效，不然的话我们的 url 映射还是通过配置文件的方式生效的</span></span><br><span class="line"><span class="comment">        不然一直处于 404 状态，这也就是下面的 dispatcher-servlet.xml 的作用配置 url 映射</span></span><br><span class="line"><span class="comment">    --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:annotation-driven</span> /&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="3-控制器"><a href="#3-控制器" class="headerlink" title="3. 控制器"></a>3. 控制器</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@RequestMapping</span>(<span class="string">"/helloTest"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">hello</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"hello"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这样我们的程序就能够跑起来了。</p>
<h2 id="3-RESTful-风格的请求"><a href="#3-RESTful-风格的请求" class="headerlink" title="3. RESTful 风格的请求"></a>3. RESTful 风格的请求</h2><p>一般的我们无法直接使用 RESTful 风格的请求，但是在 SpringMVC 中有一个过滤器可以帮我们把一个 post 请求转化成为 <code>PUT</code> 或者 <code>DELETE</code> 请求。具体的做法如下：</p>
<h4 id="1-配置过滤器（HiddenHttpMethodFilter）"><a href="#1-配置过滤器（HiddenHttpMethodFilter）" class="headerlink" title="1. 配置过滤器（HiddenHttpMethodFilter）"></a>1. 配置过滤器（HiddenHttpMethodFilter）</h4><p>​    </p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">filter</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>HttpHiddenMethods<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-class</span>&gt;</span>org.springframework.web.filter.HiddenHttpMethodFilter<span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>HttpHiddenMethods<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="2-在-post-的表单域中封装一个-hidden-的-input-标签，用来说明提交方式"><a href="#2-在-post-的表单域中封装一个-hidden-的-input-标签，用来说明提交方式" class="headerlink" title="2. 在 post 的表单域中封装一个 hidden 的 input 标签，用来说明提交方式"></a>2. 在 post 的表单域中封装一个 hidden 的 input 标签，用来说明提交方式</h4><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">method</span>=<span class="string">"post"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"hidden"</span> <span class="attr">name</span>=<span class="string">"_method"</span> <span class="attr">value</span>=<span class="string">"PUT"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">method</span>=<span class="string">"post"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"hidden"</span> <span class="attr">name</span>=<span class="string">"_method"</span> <span class="attr">value</span>=<span class="string">"DELETE"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="4-RequestMapping-及参数传递"><a href="#4-RequestMapping-及参数传递" class="headerlink" title="4. @RequestMapping 及参数传递"></a>4. @RequestMapping 及参数传递</h2><h3 id="1-RequestMapping-注解"><a href="#1-RequestMapping-注解" class="headerlink" title="1. @RequestMapping 注解"></a>1. @RequestMapping 注解</h3><p>这个注解主要就是用来做方法和类的 url 映射的，也就是相当于一个路由映射文件。这个注解可以使用在类上面也可以使用在方法上面，在类上面的话就是我们访问每一个方法的时候都需要加上类的 url 前缀。</p>
<p>他有几个比较重要的属性，用来管理 url 的：</p>
<ol>
<li><p>value: 这个是默认的，是 url 地址</p>
</li>
<li><p>method：这个是用来指定请求方式的 RequestMethod.GET/POST/DELETE/PUT ….</p>
</li>
<li><p>params：这个是用来规定我们的 url 中携带的参数的他是一个数组，可以放多个值 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">params = &#123;<span class="string">"username"</span>,<span class="string">"age!=10"</span>&#125;  params 是包含 username age!=<span class="number">10</span>   headers 也是如此只是规定了请求头而已</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h3 id="2-PathVariable-注解"><a href="#2-PathVariable-注解" class="headerlink" title="2. @PathVariable 注解"></a>2. @PathVariable 注解</h3><p>这个注解的作用是用来传递参数的，我们不仅仅可使用 <code>？</code> 来传递参数，还可以使用更优雅的 <code>/paramName/value</code> 的方式来传递参数，并且能够直接在方法中绑定这些参数。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(value = <span class="string">"/helloworld/&#123;id&#125;"</span>) </span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">hello</span><span class="params">(@PathVariable(<span class="string">"id"</span>)</span> String id)</span>&#123;</span><br><span class="line">    System.out.println(<span class="string">"hello controller"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"hello"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>这里的 id 必须和 url 中的保持一致，但是无需和方法的参数保持一致。</p>
<h3 id="3-RequestParam-注解"><a href="#3-RequestParam-注解" class="headerlink" title="3. @RequestParam 注解"></a>3. @RequestParam 注解</h3><p>但是有一个问题，当我们传递过来的参数是通过 <code>？</code> 的形式传递过来的，那么我们又该怎么去获取他呢？是的这里我们可以使用 <code>@RequestParam</code> 方法来获取这些值，当我们有些参数不是必须传递的我们就可以使用 <code>require=false</code> 来规定不用必传，这个时候如果我们没有传值，这个参数刚好是一个引用类型的就会是 <code>null</code> 但是如果是一个基本数据类型，就会报错。我们必须手动的设置一个默认值。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestParam</span>(value = <span class="string">"age"</span>,required = <span class="keyword">false</span>,defaultValue = <span class="string">"0"</span>) Integer age</span><br></pre></td></tr></table></figure>

<p>上面的参数就可以获取到 <code>http://localhost/hello?age=10</code> 这种的 url 的参数了、</p>
<h3 id="4-RequestHeader-注解"><a href="#4-RequestHeader-注解" class="headerlink" title="4. @RequestHeader 注解"></a>4. @RequestHeader 注解</h3><p>用法同上！</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestHeader</span>(<span class="string">"Content-Type"</span>) String content</span><br></pre></td></tr></table></figure>

<h3 id="5-CookieValue-注解"><a href="#5-CookieValue-注解" class="headerlink" title="5. @CookieValue 注解"></a>5. @CookieValue 注解</h3><p>这个使用同上，获取 Cookie 的值。</p>
<h3 id="6-POJO-参数（Java-类）"><a href="#6-POJO-参数（Java-类）" class="headerlink" title="6. POJO 参数（Java 类）"></a>6. POJO 参数（Java 类）</h3><p>我们的表单提交过来的数据会自动的被封装到 POJO 对象中，我们只需要配置好表单的 name 值和 Bean 的属性值一致即可，如果说里面有级联的属性，我们就使用 <code>proA.proB</code> 来封装。例如：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">method</span>=<span class="string">"post"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"age"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"address.code"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">name</span>=<span class="string">"address.name"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>这个表单就会被封装成一个 POJO 对象，这个对象里面有另外一个类的引用就导致了级联属性的出现，我们是就是使用了点的方式完成的封装。</p>
<h3 id="7-原生的-Servlet-API-参数"><a href="#7-原生的-Servlet-API-参数" class="headerlink" title="7.原生的 Servlet API 参数"></a>7.原生的 Servlet API 参数</h3><p>它支持比较多的原生的 Servlet 的 API ，其实是在他内部调用了 request 对象的一些方法获取到的。</p>
<ol>
<li>HttpServletRequest</li>
<li>HTTPServletResponse</li>
<li>HttpSession</li>
<li>Locale</li>
<li>InputStream</li>
<li>OutputStream</li>
<li>Reader</li>
<li>Writer</li>
<li>Principal</li>
</ol>
<h2 id="5-控制器与视图间数据交互"><a href="#5-控制器与视图间数据交互" class="headerlink" title="5. 控制器与视图间数据交互"></a>5. 控制器与视图间数据交互</h2><p>一般我们需要在控制器里面绑定一些数据到视图中，然后我们可以在视图里面采用标签来获取 Controller 里面的数据从而展示这些数据，在 SpringMVC 中有几种方法可以达到这个目的。</p>
<h3 id="1-ModelAndView"><a href="#1-ModelAndView" class="headerlink" title="1. ModelAndView"></a>1. ModelAndView</h3><p>这个东西其实就像他的名字一样，他是 Model 数据和模型的结合体，我们可以往里面添加数据（Model），也可以把要转发的页面放在里面让视图解析器去渲染。所以说这个对象里面有一个 Model 属性，这个属性就是用来存放数据的，其实就是一个 Map 。Map 里面的这些数据都会被遍历然后放到 request 域对象之中，我们只需要在请求域中获取就好。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * ModelAndView 来用作 Controller 与 View 之间的数据交互的介质</span></span><br><span class="line"><span class="comment"> * 也就是 Model 的载体</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> ModelAndView <span class="title">modelAndViewTest</span><span class="params">()</span></span>&#123;</span><br><span class="line">    ModelAndView modelAndView = <span class="keyword">new</span> ModelAndView(<span class="string">"hello"</span>);</span><br><span class="line">    modelAndView.addObject(<span class="string">"time"</span>, <span class="keyword">new</span> Date());</span><br><span class="line">    <span class="keyword">return</span> modelAndView;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-Model-Map-ModelMap"><a href="#2-Model-Map-ModelMap" class="headerlink" title="2. Model/Map/ModelMap"></a>2. Model/Map/ModelMap</h3><p>其实三个东西类型都是 Map 类型的，然后SpringMVC 在真正的传入的对象显然就是他们的实现类，这里我们不过分纠结，基层肯定是一个 Map 。Map 里面的这些数据都会被遍历然后放到 request 域对象之中，我们只需要在请求域中获取就好。</p>
<p>这个用起来也比较简单，就是在方法的入参里面传入这个一个东西就行了，而不是采用的返回值。Map 的具体的泛型就是 string 和 object。看下面的例子。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">modelMap</span><span class="params">(Map&lt;String,Object&gt; map)</span></span>&#123;</span><br><span class="line">       map.put(<span class="string">"time"</span>, <span class="keyword">new</span> Date());</span><br><span class="line">       <span class="keyword">return</span> <span class="string">"hello"</span>;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-SessionAttributes-注解"><a href="#3-SessionAttributes-注解" class="headerlink" title="3. @SessionAttributes 注解"></a>3. @SessionAttributes 注解</h3><p>这个注解只能放在类上面，然后我们使用 value 属性或者 types 属性来规定哪些属性需要被放在 Session 域中，这个两个属性其实都是一个数组，所以我们可以方多个值。</p>
<p>value 这个属性，就是当我们在放入 map 中的一个键名的时候我们就可以把它放到 session 域中。而 types 属性则是当我们放一个 class 的时候他会自动抓取处于 map 中的同类型的数据。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SessionAttributes</span>(value = &#123;<span class="string">"time"</span>,<span class="string">"username"</span>&#125;,types = &#123;String.class,Integer.class&#125;)</span><br></pre></td></tr></table></figure>

<h3 id="4-ModelAttribute-注解"><a href="#4-ModelAttribute-注解" class="headerlink" title="4. @ModelAttribute 注解"></a>4. @ModelAttribute 注解</h3><p>这个注解是标识在方法上的，这个注解标识的方法会在所有的方法调用前被调用。在这个被标识的方法里面我们需要从数据库中获取对应的对象，然后把这个对象放到 map 里面，但是注意 map 中的键必须要是我们的 Model 类对应的小写的一个字符串才能起作用。当我们使用其他的方法来进行某个 model 的修改动作的时候我们某个字段不传的话这时候在 map 中的那个对象的对应字段挥起一个补充作用，把对应字段填上。</p>
<p>执行流程：</p>
<ol>
<li>首先是执行了被这个注解标识的方法，将数据库中获取到的值放到了 map 里面，然后这个 map 是被放到了一个implicitModel 里面</li>
<li>然后在我们提交一个表单的时候，我们对应的方法的参数会到 implicitModel 里面查找对应的对象，查找的 key 就是先看看我们的这个方法的参数是否被 ``@ModelAttribute(value=”…”)` 修饰。如果是的话我们采用的是直接使用它的 value 属性作为 key 去查找。</li>
<li>如果没有这个注解修饰参数，则采用这个 POJO 的类名第一个字母小写作为 key 查找。</li>
<li>如果没有则看看是否这个类被  @SessionAttributes 注解 注释，如果是则是去 session 中查找，如果没有找到抛异常。</li>
<li>如果上面的情况都没找到，则是使用 POJO 反射创建一个新的对象把表单数据封装进去，而如果上面有找到的话我们就使用那个  Model 然后设置对应的属性值。</li>
<li>接着把这个修改后的 model 放到 implicitModel 进而放到 request 域中。</li>
</ol>
<h2 id="6-视图"><a href="#6-视图" class="headerlink" title="6.视图"></a>6.视图</h2><p>视图的解析步骤：</p>
<ol>
<li>首先我们是访问了我们的控制器。</li>
<li>然后我们的控制器会返回 string 或者 VIewAndModel 对象。</li>
<li>他们都会被视图解析器（我们在 spring 中配置的 bean）包装成一个 ModelAndView 对象。</li>
<li>最后渲染视图，并转发到对应的视图。</li>
</ol>
<h3 id="1-手动路由配置"><a href="#1-手动路由配置" class="headerlink" title="1. 手动路由配置"></a>1. 手动路由配置</h3><p>可以手动配置路由，不经过 controller 就可以访问到对应的视图。在 spring 配置文件里写上如下内容：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">mvc:annotation-driven</span> /&gt;</span></span><br><span class="line"><span class="comment">&lt;!--手动配置路由   直接路由到视图无需经过 controller--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">mvc:view-controller</span> <span class="attr">path</span>=<span class="string">"/success"</span> <span class="attr">view-name</span>=<span class="string">"hello"</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>那么我们访问 <code>http://localhost:8080/success</code> 就被转发到 WEB_INF/templates/hello.jsp  具体的目录取决于我们配置的视图解析器的前缀和后缀。</p>
<h3 id="2-自定义视图解析器"><a href="#2-自定义视图解析器" class="headerlink" title="2. 自定义视图解析器"></a>2. 自定义视图解析器</h3><p>我们一般采用的就是 <code>InternalResourceViewResolver</code> 这个视图解析器，我们也可以自定义视图。自定义视图则需要一个特殊的视图解析器完成解析视图的工作，就是 <code>BeanNameViewResolver</code>  就是通过视图的 bean 的 name 来获取视图的。由于我们配置了多个视图解析器则需要定义一个优先级，哪个视图解析器先工作，使用 order 属性。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--自定义的 Bean视图解析器   直接通过 bean 的 name 获取视图--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.springframework.web.servlet.view.BeanNameViewResolver"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"order"</span> <span class="attr">value</span>=<span class="string">"100"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>下面是我们使用 bean 定义的一个视图。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyViewRevsor</span> <span class="keyword">implements</span> <span class="title">View</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getContentType</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"text/html"</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">render</span><span class="params">(Map&lt;String, ?&gt; model, HttpServletRequest request, HttpServletResponse response)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        response.getWriter().write(<span class="string">"hello"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-转发和重定向"><a href="#3-转发和重定向" class="headerlink" title="3. 转发和重定向"></a>3. 转发和重定向</h3><p>我们一般在 controller 中写的东西默认都会转发的，我们需要重定向的话我们只需要在人绘制前面加上 redirect 就可以。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequestMapping</span>(<span class="string">"/beanView"</span>)</span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">beanView</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"redirect:/myViewRevsor"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-静态资源映射"><a href="#4-静态资源映射" class="headerlink" title="4. 静态资源映射"></a>4. 静态资源映射</h3><p>对于静态资源我们需要直接获取而不需要进行映射，所以说我们在获取静态资源的时候回出现 404 ，我们就配置一个</p>
<p><code>&lt;mvc:default-servlet-handler/&gt;</code> 这个就会自动的处理没有映射的 url 。</p>
<h3 id="5-表单数据到-controller-的映射"><a href="#5-表单数据到-controller-的映射" class="headerlink" title="5. 表单数据到 controller 的映射"></a>5. 表单数据到 controller 的映射</h3><p>表单数据在提交以后实际上是依赖于 SpringMVC 里面一个 WebDataBinder 类进行的数据绑定，这个 WebDataBinder 里面有很多其他的对象的引用其中就有数据格式化、数据校验、数据转换的对象，也就是说在这个数据转换的过程我们是可以添加一些对象来手动的控制数据的绑定的。</p>
<ol>
<li>converter 这个是用来数据转换的，具体的参照文档，就比如我们把前端的一个字符串转成方法入参的一个 bean 对象，就经过这个 converter 来完成。</li>
<li>@initBinder 被这个注解表示的方法，会在数据绑定之前进行运行，其功能就是对 binder 进行一些设置比如忽略一些字段。修改字段，拒绝字段等等。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@InitBinder</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">initBinder</span><span class="params">(WebDataBinder binder)</span> </span>&#123;</span><br><span class="line">      <span class="comment">// 拒绝 name 字段</span></span><br><span class="line">      binder.setDisallowedFields(<span class="string">"name"</span>);</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>数据的格式化，采用注解注解在对应的 bean 的字段上。常用的有时间还有数子。</li>
<li>JSR303 校验规范，这只是 JavaEE 的规范，真正的实现类是 Hibernate ValidData ，然后我们进行数据校验也是使用注解的方式，具体的注解在规范里面都有，都比较简单。并且我们需要在方法的入参的 bean 上加上 @Valid 注解。</li>
<li>错误消息的回显，显然如果我们的校验生效并且有错误的话我们需要回显到表单。我们就需要在方法的参数里面加上一个 BindResult 对象，然后错误的数据都会被放到这个东西里面，同时 BindResult 是一个 Error 类型的对象，所以我们亦可以放这个对象。最后放到 map 里面在前端回显即可。</li>
</ol>
<h3 id="6-返回-JSON-数据"><a href="#6-返回-JSON-数据" class="headerlink" title="6. 返回 JSON 数据"></a>6. 返回 JSON 数据</h3><p>只需要在方法上加上 @ResponseBody 就可以，返回值是一个 List 或数组。</p>
<h2 id="7-拦截器（Interceptor）"><a href="#7-拦截器（Interceptor）" class="headerlink" title="7. 拦截器（Interceptor）"></a>7. 拦截器（Interceptor）</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--配置拦截器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">mvc:interceptors</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--自定义的拦截器组件--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"com.mvc.MyInterceptor"</span>/&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--更详细的配置可以针对 url--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">mvc:interceptor</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">mvc:mapping</span> <span class="attr">path</span>=<span class="string">"/hello"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">mvc:exclude-mapping</span> <span class="attr">path</span>=<span class="string">"/hah"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"com.mvc.MyInterceptor"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">mvc:interceptor</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">mvc:interceptors</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyInterceptor</span> <span class="keyword">implements</span> <span class="title">HandlerInterceptor</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">postHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler, ModelAndView modelAndView)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">afterCompletion</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="8-统一异常处理"><a href="#8-统一异常处理" class="headerlink" title="8. 统一异常处理"></a>8. 统一异常处理</h2><p>统一异常处理就采用对应的 handler 来处理异常，使用 @ExceptionHandler 注解，然后标注要处理的异常类型，但是如果说我们需要把异常带到错误页面我们不能使用 map 而只能使用 ModelAndView 不然那就会报错。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ExceptionHandler</span>(&#123;ArithmeticException.class&#125;)</span><br><span class="line"><span class="function"><span class="keyword">public</span> ModelAndView <span class="title">error</span><span class="params">(Exception ex)</span></span>&#123;</span><br><span class="line">    ModelAndView modelAndView = <span class="keyword">new</span> ModelAndView();</span><br><span class="line">    modelAndView.addObject(<span class="string">"ex"</span>, ex);</span><br><span class="line">    <span class="keyword">return</span> modelAndView;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>如果他在当前的 controller 中找不到对应的 ExceptionHandler 就去查找对应的 @ControllerAdvise 注解表示的类，中的ExceptionHandler 注解方法。也就是默认的处理器。</p>
]]></content>
      <categories>
        <category>SpringMVC</category>
      </categories>
      <tags>
        <tag>SpringMVC</tag>
      </tags>
  </entry>
  <entry>
    <title>NioEventLoop 源码分析</title>
    <url>/2018/04/16/Netty/NioEventLoop%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="NioEventLoop-源码分析"><a href="#NioEventLoop-源码分析" class="headerlink" title="NioEventLoop 源码分析"></a>NioEventLoop 源码分析</h1><h2 id="1-SingleThreadEventExecutor-的-execute-方法"><a href="#1-SingleThreadEventExecutor-的-execute-方法" class="headerlink" title="1. SingleThreadEventExecutor 的 execute 方法"></a>1. SingleThreadEventExecutor 的 execute 方法</h2><p>NioEventLoop 的核心就在于它的 <code>run()</code> 他是在第一次添加任务的时候开始执行。那我们先看看第一次添加任务的地方，其实第一次添加任务的地方是在父类中的 <code>execute()</code> 方法。所以先去分析一下 <code>SingleThreadEventExecutor</code> 的<code>execute()</code> 方法。我把代码精简了贴出来，只看核心的部分。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(Runnable task)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">boolean</span> inEventLoop = inEventLoop();</span><br><span class="line">        <span class="comment">// 线程已经启动  直接加入任务</span></span><br><span class="line">        <span class="keyword">if</span> (inEventLoop) &#123;</span><br><span class="line">            addTask(task);</span><br><span class="line">        <span class="comment">// 否则先启动任务 再添加任务</span></span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            startThread();</span><br><span class="line">            addTask(task);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  很明显，也就是我们往线程池中添加任务的时候，首先要看看我们的线程是不是已经启动了，没有的话首先我们需要启动一下线程。接下来要看看 <code>startThread()</code> 方法干了什么事。里面做了一些检查也就是线程只能被 <code>start</code> 一次，然后直接调用了 <code>NioEventLoop</code> 封装的 <code>thread</code> 的 <code>start()</code> 很简单！</p>
<p>&emsp;&emsp;  但是，等等这个线程是从哪来的？我们并没有显示的传入，来到 <code>SingleThreadEventExecutor</code> 构造方法，我们会发现他在构造器中进行了初始化，但是不是直接 <code>new Thread</code> 而是使用了我们传的线程工厂，然后在工程里面 new 了这个线程需要执行的任务。</p>
<p>&emsp;&emsp;  他的任务就是先执行一下 run 方法，然而他的 run 方法是抽象的，自然就调用到子类去了，这也就解释了为什么说是第一次添加任务的时候调用了 <code>NioEventLoop</code> 的 <code>run</code> 方法。</p>
<p>&emsp;&emsp;  贴一下对 <code>thread</code> 初始化的代码(精简过后的)：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// new 了一个新的线程</span></span><br><span class="line">    thread = threadFactory.newThread(<span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">boolean</span> success = <span class="keyword">false</span>;</span><br><span class="line">            updateLastExecutionTime();</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">               <span class="comment">// 调用了 run 方法，这个 run 方法在这个类中是抽象的，显然在子类中实现了</span></span><br><span class="line">                SingleThreadEventExecutor.<span class="keyword">this</span>.run();</span><br><span class="line">                success = <span class="keyword">true</span>;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">                logger.warn(<span class="string">"Unexpected exception from an event executor: "</span>, t);</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                <span class="comment">// 让线程关闭</span></span><br><span class="line">            &#125;</span><br><span class="line">    &#125;);</span><br></pre></td></tr></table></figure>

<h2 id="2-再回到-NioEventLoop-的-run-方法"><a href="#2-再回到-NioEventLoop-的-run-方法" class="headerlink" title="2. 再回到 NioEventLoop 的 run 方法"></a>2. 再回到 NioEventLoop 的 run 方法</h2><p>&emsp;&emsp;  那好，我们在上面已经看到了我们在创建一个 <code>NioEventLoop</code> 的时候会创建一个线程，这个线程的任务就是去调用子类的 run 方法。当我们执行 <code>execute( task )</code> 方法，添加一个新任务去运行的时候，就会判断当前线程是不是启动了，否则启动我们一开始创建的那个线程。用一张图说明一下！！！<br><a href="https://simimg.com/i/kUQnB" target="_blank" rel="noopener"><img src="https://s1.ax2x.com/2018/04/16/kUQnB.md.png" alt="kUQnB.md.png"></a></p>
<p>&emsp;&emsp;  好的现在正式的看一下 run 方法，还是贴一下核心代码：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 有任务在线程创建之后直接开始 select</span></span><br><span class="line">                <span class="keyword">if</span> (hasTasks()) &#123;</span><br><span class="line">                    selectNow(); <span class="comment">//直接调用了 select 的 selectNow 然后再次唤醒同下面的代码</span></span><br><span class="line">                <span class="comment">// 没有任务</span></span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 自旋进行等待可进行 select 操作</span></span><br><span class="line">                    select(oldWakenUp);</span><br><span class="line">                    <span class="comment">// 再次唤醒，解决并发问题</span></span><br><span class="line">                    <span class="keyword">if</span> (wakenUp.get()) &#123;</span><br><span class="line">                        selector.wakeup();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">                <span class="comment">// 都是处理 selected 的通道的数据，并执行所有的任务，只是在 runAllTasks 传的参数不同</span></span><br><span class="line">                <span class="keyword">if</span> (ioRatio == <span class="number">100</span>) &#123;</span><br><span class="line">                    processSelectedKeys();</span><br><span class="line">                    runAllTasks();</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">final</span> <span class="keyword">long</span> ioStartTime = System.nanoTime();</span><br><span class="line">                    processSelectedKeys();</span><br><span class="line">                    <span class="keyword">final</span> <span class="keyword">long</span> ioTime = System.nanoTime() - ioStartTime;</span><br><span class="line">                    runAllTasks(ioTime * (<span class="number">100</span> - ioRatio) / ioRatio);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                           </span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  可以看到在代码里面的死循环中值做了三件事：select、processSelectedKeys、 runAllTasks .借一张图来看：</p>
<p><a href="https://simimg.com/i/kUjcp" target="_blank" rel="noopener"><img src="https://s1.ax2x.com/2018/04/16/kUjcp.md.png" alt="kUjcp.md.png"></a></p>
<h4 id="1-首先轮询注册到reactor线程对用的selector上的所有的channel的IO事件"><a href="#1-首先轮询注册到reactor线程对用的selector上的所有的channel的IO事件" class="headerlink" title="1. 首先轮询注册到reactor线程对用的selector上的所有的channel的IO事件"></a>1. 首先轮询注册到reactor线程对用的selector上的所有的channel的IO事件</h4><h4 id="2-处理产生网络IO事件的channel"><a href="#2-处理产生网络IO事件的channel" class="headerlink" title="2. 处理产生网络IO事件的channel"></a>2. 处理产生网络IO事件的channel</h4><h4 id="3-处理任务队列"><a href="#3-处理任务队列" class="headerlink" title="3. 处理任务队列"></a>3. 处理任务队列</h4><p>具体做的事情放到下面一一道来！</p>
<h3 id="1-select"><a href="#1-select" class="headerlink" title="1. select()"></a>1. select()</h3><p>&emsp;&emsp;  如果有任务的话直接去 selectNow() 也就是不进行等待的 select() ,而没有任务的时候就进行自旋等待的 select() 。下面是 select() 的核心代码,可以看待里面调用了 selectNow() 所以说这个就是一个自旋的 selectNow() 。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">    * 这个方法主要干的事情：</span></span><br><span class="line"><span class="comment">    * 1、如果不需要等待就直接 select</span></span><br><span class="line"><span class="comment">    * 2、需要等待则等一个超时时间再去 select</span></span><br><span class="line"><span class="comment">    * 这个过程是不停进行的也就是死循环直达有任务可进行 select 时 select 完毕退出循环</span></span><br><span class="line"><span class="comment">    * <span class="doctag">@param</span> oldWakenUp</span></span><br><span class="line"><span class="comment">    * <span class="doctag">@throws</span> IOException</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">   <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">select</span><span class="params">(<span class="keyword">boolean</span> oldWakenUp)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">           <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">               <span class="comment">// 不用等待进行一次 select 操作</span></span><br><span class="line">               <span class="keyword">long</span> timeoutMillis = (selectDeadLineNanos - currentTimeNanos + <span class="number">500000L</span>) / <span class="number">1000000L</span>;</span><br><span class="line">               <span class="keyword">if</span> (timeoutMillis &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">                   <span class="keyword">if</span> (selectCnt == <span class="number">0</span>) &#123;</span><br><span class="line">                       selector.selectNow();</span><br><span class="line">                       selectCnt = <span class="number">1</span>;</span><br><span class="line">                   &#125;</span><br><span class="line">                   <span class="keyword">break</span>;</span><br><span class="line">               &#125;</span><br><span class="line">               <span class="comment">// 等一个超时再去选择</span></span><br><span class="line">               <span class="keyword">int</span> selectedKeys = selector.select(timeoutMillis);</span><br><span class="line">               selectCnt ++;</span><br><span class="line"></span><br><span class="line">               <span class="keyword">if</span> (selectedKeys != <span class="number">0</span> || oldWakenUp || wakenUp.get() || hasTasks() || hasScheduledTasks()) &#123;</span><br><span class="line">                   <span class="comment">// - Selected something,</span></span><br><span class="line">                   <span class="comment">// - waken up by user, or</span></span><br><span class="line">                   <span class="comment">// - the task queue has a pending task.</span></span><br><span class="line">                   <span class="comment">// - a scheduled task is ready for processing</span></span><br><span class="line">                   <span class="keyword">break</span>;</span><br><span class="line">               &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (SELECTOR_AUTO_REBUILD_THRESHOLD &gt; <span class="number">0</span> &amp;&amp;</span><br><span class="line">                       selectCnt &gt;= SELECTOR_AUTO_REBUILD_THRESHOLD) &#123;</span><br><span class="line">                   <span class="comment">// 解决死循环问题，重建 selector</span></span><br><span class="line">                   rebuildSelector();</span><br><span class="line">                   selector = <span class="keyword">this</span>.selector;</span><br><span class="line">                   <span class="comment">//  直接是 selectNow()</span></span><br><span class="line">                   selector.selectNow();</span><br><span class="line"></span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br></pre></td></tr></table></figure>

<p><code>wakenUp</code> 表示是否应该唤醒正在阻塞的 select 操作，netty在进行一次新的loop之前，都会将 wakeUp 被设置成false，标志新的一轮loop的开始。</p>
<h3 id="2-processSelectedKeys"><a href="#2-processSelectedKeys" class="headerlink" title="2. processSelectedKeys()"></a>2. processSelectedKeys()</h3><h3 id="3-runAllTasks"><a href="#3-runAllTasks" class="headerlink" title="3. runAllTasks()"></a>3. runAllTasks()</h3>]]></content>
      <categories>
        <category>Netty 源码分析</category>
      </categories>
  </entry>
  <entry>
    <title>SpringBoot 笔记 ( 四 )：web 开发</title>
    <url>/2018/04/14/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E5%9B%9B%20)%EF%BC%9Aweb%20%E5%BC%80%E5%8F%91/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-四-web-开发"><a href="#SpringBoot-笔记-四-web-开发" class="headerlink" title="SpringBoot 笔记 (四): web 开发"></a>SpringBoot 笔记 (四): web 开发</h1><h2 id="1、SpringBoot对静态资源的映射规则"><a href="#1、SpringBoot对静态资源的映射规则" class="headerlink" title="1、SpringBoot对静态资源的映射规则"></a>1、SpringBoot对静态资源的映射规则</h2><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"spring.resources"</span>, ignoreUnknownFields = <span class="keyword">false</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ResourceProperties</span> <span class="keyword">implements</span> <span class="title">ResourceLoaderAware</span> </span>&#123;</span><br><span class="line"><span class="comment">//静态资源的路径</span></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> String[] CLASSPATH_RESOURCE_LOCATIONS = &#123;</span><br><span class="line">			<span class="string">"classpath:/META-INF/resources/"</span>, <span class="string">"classpath:/resources/"</span>,</span><br><span class="line">			<span class="string">"classpath:/static/"</span>, <span class="string">"classpath:/public/"</span> &#125;;</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<p><strong>MVC的自动配置</strong> ：WebMvcAuotConfiguration</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">WebMvcAuotConfiguration：</span><br><span class="line">	<span class="meta">@Override</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addResourceHandlers</span><span class="params">(ResourceHandlerRegistry registry)</span> </span>&#123;</span><br><span class="line">   <span class="comment">// 对 webjars 的请求，也就是我们把类似于 jquery 之类的东西放到 maven 中就叫做 webjars</span></span><br><span class="line">		<span class="keyword">if</span> (!<span class="keyword">this</span>.resourceProperties.isAddMappings()) &#123;</span><br><span class="line">			logger.debug(<span class="string">"Default resource handling disabled"</span>);</span><br><span class="line">			<span class="keyword">return</span>;</span><br><span class="line">		&#125;</span><br><span class="line">		Integer cachePeriod = <span class="keyword">this</span>.resourceProperties.getCachePeriod();</span><br><span class="line">		<span class="keyword">if</span> (!registry.hasMappingForPattern(<span class="string">"/webjars/**"</span>)) &#123;</span><br><span class="line">			customizeResourceHandlerRegistration(</span><br><span class="line">					registry.addResourceHandler(<span class="string">"/webjars/**"</span>)</span><br><span class="line">							.addResourceLocations(</span><br><span class="line">									<span class="string">"classpath:/META-INF/resources/webjars/"</span>)</span><br><span class="line">					.setCachePeriod(cachePeriod));</span><br><span class="line">		&#125;</span><br><span class="line">		String staticPathPattern = <span class="keyword">this</span>.mvcProperties.getStaticPathPattern();</span><br><span class="line">   <span class="comment">//静态资源文件夹映射  </span></span><br><span class="line">      <span class="comment">//"classpath:/META-INF/resources/", </span></span><br><span class="line">      <span class="comment">//"classpath:/resources/",</span></span><br><span class="line">      <span class="comment">//"classpath:/static/", </span></span><br><span class="line">      <span class="comment">//"classpath:/public/" </span></span><br><span class="line">		<span class="keyword">if</span> (!registry.hasMappingForPattern(staticPathPattern)) &#123;</span><br><span class="line">			customizeResourceHandlerRegistration(</span><br><span class="line">					registry.addResourceHandler(staticPathPattern)</span><br><span class="line">							.addResourceLocations(</span><br><span class="line">									<span class="keyword">this</span>.resourceProperties.getStaticLocations())</span><br><span class="line">					.setCachePeriod(cachePeriod));</span><br><span class="line">		&#125;</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">//配置欢迎页映射</span></span><br><span class="line">   	<span class="comment">//所有静态资源的index.html 就是欢迎页。</span></span><br><span class="line">	<span class="meta">@Bean</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> WelcomePageHandlerMapping <span class="title">welcomePageHandlerMapping</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">			ResourceProperties resourceProperties)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">return</span> <span class="keyword">new</span> WelcomePageHandlerMapping(resourceProperties.getWelcomePage(),</span><br><span class="line">				<span class="keyword">this</span>.mvcProperties.getStaticPathPattern());</span><br><span class="line">	&#125;</span><br><span class="line"></span><br><span class="line">   <span class="comment">//配置喜欢的图标</span></span><br><span class="line">	<span class="comment">//还是静态资源文件夹下的 favicon.ico</span></span><br><span class="line">	<span class="meta">@Configuration</span></span><br><span class="line">	<span class="meta">@ConditionalOnProperty</span>(value = <span class="string">"spring.mvc.favicon.enabled"</span>, matchIfMissing = <span class="keyword">true</span>)</span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">FaviconConfiguration</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">		<span class="keyword">private</span> <span class="keyword">final</span> ResourceProperties resourceProperties;</span><br><span class="line"></span><br><span class="line">		<span class="function"><span class="keyword">public</span> <span class="title">FaviconConfiguration</span><span class="params">(ResourceProperties resourceProperties)</span> </span>&#123;</span><br><span class="line">			<span class="keyword">this</span>.resourceProperties = resourceProperties;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Bean</span></span><br><span class="line">		<span class="function"><span class="keyword">public</span> SimpleUrlHandlerMapping <span class="title">faviconHandlerMapping</span><span class="params">()</span> </span>&#123;</span><br><span class="line">			SimpleUrlHandlerMapping mapping = <span class="keyword">new</span> SimpleUrlHandlerMapping();</span><br><span class="line">			mapping.setOrder(Ordered.HIGHEST_PRECEDENCE + <span class="number">1</span>);</span><br><span class="line">             	<span class="comment">//所有  **/favicon.ico </span></span><br><span class="line">			mapping.setUrlMap(Collections.singletonMap(<span class="string">"**/favicon.ico"</span>,</span><br><span class="line">					faviconRequestHandler()));</span><br><span class="line">			<span class="keyword">return</span> mapping;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">		<span class="meta">@Bean</span></span><br><span class="line">		<span class="function"><span class="keyword">public</span> ResourceHttpRequestHandler <span class="title">faviconRequestHandler</span><span class="params">()</span> </span>&#123;</span><br><span class="line">			ResourceHttpRequestHandler requestHandler = <span class="keyword">new</span> ResourceHttpRequestHandler();</span><br><span class="line">			requestHandler</span><br><span class="line">					.setLocations(<span class="keyword">this</span>.resourceProperties.getFaviconLocations());</span><br><span class="line">			<span class="keyword">return</span> requestHandler;</span><br><span class="line">		&#125;</span><br><span class="line"></span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>1）、所有 /webjars/** ，都去 classpath:/META-INF/resources/webjars/ 找资源<br>例如：<br>localhost:8080/webjars/jquery/3.3.1/jquery.js</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--引入jquery-webjar--&gt;</span>在访问的时候只需要写webjars下面资源的名称即可</span><br><span class="line">		<span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.webjars<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>jquery<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">version</span>&gt;</span>3.3.1<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>2）、”/**” 这个 路径的意思就是如果没有拦截器或者Servlet去处理的请求我们就映射到这个地方来，访问当前项目的任何资源，都去（静态资源的文件夹）找映射</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;classpath:/META-INF/resources/&quot;, </span><br><span class="line">&quot;classpath:/resources/&quot;,</span><br><span class="line">&quot;classpath:/static/&quot;, </span><br><span class="line">&quot;classpath:/public/&quot; </span><br><span class="line">&quot;/&quot;：当前项目的根路径</span><br></pre></td></tr></table></figure>

<p>localhost:8080/abc ===  去静态资源文件夹里面找abc</p>
<p>3）、欢迎页； 静态资源文件夹下的所有index.html页面；被”/**”映射</p>
<p>​    localhost:8080/   找index页面</p>
<p>4）、所有的 **/favicon.ico  都是在静态资源文件下找</p>
<h2 id="2、模板引擎"><a href="#2、模板引擎" class="headerlink" title="2、模板引擎"></a>2、模板引擎</h2><p>JSP、Velocity、Freemarker、Thymeleaf 但是SpringBoot推荐的Thymeleaf：语法更简单，功能更强大。</p>
<h3 id="1、引入thymeleaf；"><a href="#1、引入thymeleaf；" class="headerlink" title="1、引入thymeleaf；"></a>1、引入thymeleaf；</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-thymeleaf<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    2.1.6</span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">thymeleaf.version</span>&gt;</span>3.0.9.RELEASE<span class="tag">&lt;/<span class="name">thymeleaf.version</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!-- 切换thymeleaf版本 布局功能的支持程序  thymeleaf3主程序  layout2以上版本 --&gt;</span></span><br><span class="line">		<span class="comment">&lt;!-- thymeleaf2   layout1   不使用在 parent 工程中的定义的版本 --&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">thymeleaf-layout-dialect.version</span>&gt;</span>2.2.2<span class="tag">&lt;/<span class="name">thymeleaf-layout-dialect.version</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2、Thymeleaf使用"><a href="#2、Thymeleaf使用" class="headerlink" title="2、Thymeleaf使用"></a>2、Thymeleaf使用</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"spring.thymeleaf"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThymeleafProperties</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Charset DEFAULT_ENCODING = Charset.forName(<span class="string">"UTF-8"</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> MimeType DEFAULT_CONTENT_TYPE = MimeType.valueOf(<span class="string">"text/html"</span>);</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String DEFAULT_PREFIX = <span class="string">"classpath:/templates/"</span>;</span><br><span class="line"></span><br><span class="line">	<span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">final</span> String DEFAULT_SUFFIX = <span class="string">".html"</span>;</span><br><span class="line">  	<span class="comment">//</span></span><br></pre></td></tr></table></figure>

<p>只要我们把HTML页面放在classpath:/templates/，thymeleaf就能自动渲染；</p>
<h4 id="步骤："><a href="#步骤：" class="headerlink" title="步骤："></a>步骤：</h4><h4 id="1、导入thymeleaf的名称空间"><a href="#1、导入thymeleaf的名称空间" class="headerlink" title="1、导入thymeleaf的名称空间"></a>1、导入thymeleaf的名称空间</h4><p>为了语法提示！</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">"en"</span> <span class="attr">xmlns:th</span>=<span class="string">"http://www.thymeleaf.org"</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="2、使用thymeleaf语法"><a href="#2、使用thymeleaf语法" class="headerlink" title="2、使用thymeleaf语法"></a>2、使用thymeleaf语法</h4><p>在controller中的参数放入一个Map或者ModelAndView对象，然后我们直接在里面put数据，接着我们在返回值返回的时候只需要返回这个模板的逻辑名就行了。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">"en"</span> <span class="attr">xmlns:th</span>=<span class="string">"http://www.thymeleaf.org"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Title<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">h1</span>&gt;</span>成功！<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--th:text 将div里面的文本内容设置为 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">th:text</span>=<span class="string">"$&#123;hello&#125;"</span>&gt;</span>这是显示欢迎信息<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="3、语法规则"><a href="#3、语法规则" class="headerlink" title="3、语法规则"></a>3、语法规则</h3><p>1）、th:text：改变当前元素里面的文本内容<br>      th：任意html属性：来替换原生属性的值</p>
<p>2）、表达式</p>
<h4 id="1-…-获取值"><a href="#1-…-获取值" class="headerlink" title="1. ${…} 获取值"></a>1. ${…} 获取值</h4><p>底层就是OGNL表达式：属性调用，数组调用，方法调用，方法传参</p>
<h5 id="1）、获取对象的属性、调用方法、数组的取值"><a href="#1）、获取对象的属性、调用方法、数组的取值" class="headerlink" title="1）、获取对象的属性、调用方法、数组的取值"></a>1）、获取对象的属性、调用方法、数组的取值</h5><p>${obj.name}    ${obj[‘name’]}</p>
<h5 id="2）、使用内置的基本对象"><a href="#2）、使用内置的基本对象" class="headerlink" title="2）、使用内置的基本对象"></a>2）、使用内置的基本对象</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#ctx : the context object.</span><br><span class="line">#vars: the context variables.</span><br><span class="line">#locale : the context locale.</span><br><span class="line">#request : (only in Web Contexts) the HttpServletRequest object.</span><br><span class="line">#response : (only in Web Contexts) the HttpServletResponse object.</span><br><span class="line">#session : (only in Web Contexts) the HttpSession object.</span><br><span class="line">#servletContext : (only in Web Contexts) the ServletContext object.</span><br></pre></td></tr></table></figure>

<p>对象必须是采用 # 来引用     </p>
<h5 id="3）、内置的一些工具对象："><a href="#3）、内置的一些工具对象：" class="headerlink" title="3）、内置的一些工具对象："></a>3）、内置的一些工具对象：</h5><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#execInfo : information about the template being processed.</span><br><span class="line">#messages : methods for obtaining externalized messages inside variables expressions, in the same way as they would be obtained using #&#123;…&#125; syntax.</span><br><span class="line">#uris : methods for escaping parts of URLs/URIs</span><br><span class="line">#conversions : methods for executing the configured conversion service (if any).</span><br><span class="line">#dates : methods for java.util.Date objects: formatting, component extraction, etc.</span><br><span class="line">#calendars : analogous to #dates , but for java.util.Calendar objects.</span><br><span class="line">#numbers : methods for formatting numeric objects.</span><br><span class="line">#strings : methods for String objects: contains, startsWith, prepending/appending, etc.</span><br><span class="line">#objects : methods for objects in general.</span><br><span class="line">#bools : methods for boolean evaluation.</span><br><span class="line">#arrays : methods for arrays.</span><br><span class="line">#lists : methods for lists.</span><br><span class="line">#sets : methods for sets.</span><br><span class="line">#maps : methods for maps.</span><br><span class="line">#aggregates : methods for creating aggregates on arrays or collections.</span><br><span class="line">#ids : methods for dealing with id attributes that might be repeated (for example, as a result of an iteration).</span><br></pre></td></tr></table></figure>

<h4 id="2"><a href="#2" class="headerlink" title="2.*{..}"></a>2.*{..}</h4><p>这个东西其实和 ${} 是一样的，但是他多了一个辅助功能就是我们在一个标签中使用了 <code>th:object=&quot;obj&quot;</code> 来指定一个对象的时候，我们在这个标签里面可以直接用 <code>*{user}</code> 来用 <code>*</code> 代替我们上面的那个 obj 对象，也就是我们可以简化书写。</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:object</span>=<span class="string">"$&#123;session.user&#125;"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span>&gt;</span>Name: <span class="tag">&lt;<span class="name">span</span> <span class="attr">th:text</span>=<span class="string">"*&#123;firstName&#125;"</span>&gt;</span>Sebastian<span class="tag">&lt;/<span class="name">span</span>&gt;</span>.<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span>&gt;</span>Surname: <span class="tag">&lt;<span class="name">span</span> <span class="attr">th:text</span>=<span class="string">"*&#123;lastName&#125;"</span>&gt;</span>Pepper<span class="tag">&lt;/<span class="name">span</span>&gt;</span>.<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">p</span>&gt;</span>Nationality: <span class="tag">&lt;<span class="name">span</span> <span class="attr">th:text</span>=<span class="string">"*&#123;nationality&#125;"</span>&gt;</span>Saturn<span class="tag">&lt;/<span class="name">span</span>&gt;</span>.<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="3"><a href="#3" class="headerlink" title="3.#{..}"></a>3.#{..}</h4><p>这个是用来取国际化内容的</p>
<h4 id="4-…"><a href="#4-…" class="headerlink" title="4.@{…}"></a>4.@{…}</h4><p>定义URL，<code>@{/order/process(execId=${execId},execType=&#39;FAST&#39;)}</code> 就可以表示当前项目下面的一个url 就类似我们的 @GetMapping 里面的映射一样，然后我们的参数就不用使用 <code>？</code> 来拼接字符串了，直接使用圆括号即可。</p>
<h4 id="5-…"><a href="#5-…" class="headerlink" title="5.~{…}"></a>5.~{…}</h4><p>片段引用表达式</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:insert</span>=<span class="string">"~&#123;commons :: main&#125;"</span>&gt;</span>...<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="6-Literals（字面量）"><a href="#6-Literals（字面量）" class="headerlink" title="6.Literals（字面量）"></a>6.Literals（字面量）</h4><p>​      Text literals: ‘one text’ , ‘Another one!’ ,…<br>      Number literals: 0 , 34 , 3.0 , 12.3 ,…<br>      Boolean literals: true , false<br>      Null literal: null<br>      Literal tokens: one , sometext , main ,…</p>
<h4 id="7-Text-operations-（文本操作）"><a href="#7-Text-operations-（文本操作）" class="headerlink" title="7.Text operations:（文本操作）"></a>7.Text operations:（文本操作）</h4><p>​    String concatenation: +<br>    Literal substitutions: |The name is ${name}|</p>
<h4 id="8-Arithmetic-operations-（数学运算）"><a href="#8-Arithmetic-operations-（数学运算）" class="headerlink" title="8.Arithmetic operations:（数学运算）"></a>8.Arithmetic operations:（数学运算）</h4><p>​    Binary operators: + , - , * , / , %<br>    Minus sign (unary operator): -</p>
<h4 id="9-Boolean-operations-（布尔运算）"><a href="#9-Boolean-operations-（布尔运算）" class="headerlink" title="9.Boolean operations:（布尔运算）"></a>9.Boolean operations:（布尔运算）</h4><p>​    Binary operators: and , or<br>    Boolean negation (unary operator): ! , not</p>
<h4 id="10-Comparisons-and-equality-（比较运算）"><a href="#10-Comparisons-and-equality-（比较运算）" class="headerlink" title="10.Comparisons and equality:（比较运算）"></a>10.Comparisons and equality:（比较运算）</h4><p>​    Comparators: &gt; , &lt; , &gt;= , &lt;= ( gt , lt , ge , le )<br>    Equality operators: == , != ( eq , ne )</p>
<h4 id="11-Conditional-operators-条件运算（三元运算符）"><a href="#11-Conditional-operators-条件运算（三元运算符）" class="headerlink" title="11.Conditional operators:条件运算（三元运算符）"></a>11.Conditional operators:条件运算（三元运算符）</h4><p>​    If-then: (if) ? (then)<br>    If-then-else: (if) ? (then) : (else)<br>    Default: (value) ?: (defaultvalue)</p>
<h4 id="12-Special-tokens"><a href="#12-Special-tokens" class="headerlink" title="12.Special tokens:"></a>12.Special tokens:</h4><p>​    No-Operation: _ </p>
<h4 id="13-foreach"><a href="#13-foreach" class="headerlink" title="13.foreach"></a>13.foreach</h4><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:each</span>=<span class="string">"user:$&#123;users&#125;"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">th:text</span>=<span class="string">"$&#123;user&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span> </span><br><span class="line">    或者使用行内写法</span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">        [[$&#123;user&#125;]]  text  会转义</span><br><span class="line">        [($&#123;user&#125;)]  utext	不转义字符</span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h2 id="4、SpringMVC自动配置"><a href="#4、SpringMVC自动配置" class="headerlink" title="4、SpringMVC自动配置"></a><a href="https://docs.spring.io/spring-boot/docs/1.5.10.RELEASE/reference/htmlsingle/#boot-features-developing-web-applications" target="_blank" rel="noopener">4、SpringMVC自动配置</a></h2><h3 id="1-Spring-MVC-auto-configuration"><a href="#1-Spring-MVC-auto-configuration" class="headerlink" title="1. Spring MVC auto-configuration"></a>1. Spring MVC auto-configuration</h3><p>Spring Boot 自动配置好了SpringMVC</p>
<p>以下是SpringBoot对SpringMVC的默认配置:<strong>WebMvcAutoConfiguration</strong></p>
<ol>
<li><p>注入了 <code>ContentNegotiatingViewResolver</code> and <code>BeanNameViewResolver</code> 的 bean.</p>
<ul>
<li>自动配置了ViewResolver（视图解析器：根据方法的返回值得到视图对象（View），视图对象决定如何渲染（转发/重定向））</li>
<li>ContentNegotiatingViewResolver：组合所有的视图解析器的，也就是把容器中所有的视图解析器都放到一块，也就是放到自己的一个集合里面，我们可以在debug的时候去DispatcherServlet 这个地方查看到我们的视图解析器，然后在挑选出一个最合适的视图解析器工作，处理视图。</li>
<li>如何定制自己的视图解析器？：我们可以自己给容器中添加一个视图解析器（实现了ViewResolver接口的类）,<code>ContentNegotiatingViewResolver</code>自动的将其组合进来，放到自己的容器里面。</li>
</ul>
</li>
<li><p>静态资源文件夹路径,webjars</p>
</li>
<li><p>静态首页访问,index 页面</p>
</li>
<li><p>自定义 favicon.ico 图标</p>
</li>
<li><p>自动注册了 <code>Converter</code>, <code>GenericConverter</code>, <code>Formatter</code> 组件</p>
<ul>
<li>Converter：转换器就是把网页提交过来的参数转化成我们需要的类型  public String hello(User user)：类型转换使用Converter</li>
<li><code>Formatter</code>  格式化器，2017.12.17===Date。自己添加的格式化器转换器，我们只需要放在容器中即可，然后配置一下对应的配置文件就行 。</li>
</ul>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="meta">@ConditionalOnProperty</span>(prefix = <span class="string">"spring.mvc"</span>, name = <span class="string">"date-format"</span>)<span class="comment">//需要我们在文件中配置日期格式化的规则，他才进行格式化。</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> Formatter&lt;Date&gt; <span class="title">dateFormatter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="keyword">return</span> <span class="keyword">new</span> DateFormatter(<span class="keyword">this</span>.mvcProperties.getDateFormat());<span class="comment">//日期格式化组件</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="6">
<li><p><code>HttpMessageConverters</code> Http 请求响应转换。</p>
<ul>
<li><p>HttpMessageConverter：SpringMVC用来转换Http请求和响应的，User—Json</p>
</li>
<li><p><code>HttpMessageConverters</code> 是从容器中确定，从容器中自动获取所有的HttpMessageConverter，自己给容器中添加HttpMessageConverter，只需要将自己的组件注册容器中（@Bean,@Component）</p>
</li>
</ul>
</li>
<li><p><code>MessageCodesResolver</code> .定义错误代码生成规则，我们也可以手动的配置，就是向容器中添加bean</p>
</li>
<li><p>使用了 <code>ConfigurableWebBindingInitializer</code> 组件到容器里面。我们可以配置一个ConfigurableWebBindingInitializer来替换默认的,添加到容器。这个东西的功能就是把我们页面提交过来的数据与我们的JavaBean的属性来做绑定，就是我们在 Spring中看到的 <code>WebBinder</code> 。 </p>
</li>
</ol>
<p><strong>注意：org.springframework.boot.autoconfigure.web这个包里面有所有的web自动配置场景</strong></p>
<p>&emsp;&emsp;</p>
<p>可以看出来我们的SpringBoot的自动配置基本就是使用 AutoConfiguration 来向容器中添加Bean，条件就是如果容器中没有这个 bean 的话。然后就是如果是这个组价可以存在多个，我们配置的bean就会被SpringBoot给整合掉，形成在一起。</p>
<p>如果说我们想要保留 springboot 的对 mvc 的自动配置，并且希望添加一些新的功能(interceptors, formatters, view controllers etc.)我们可能就要添加自己的配置类，也就是有 <code>@Configuration</code> 的 <code>WebMvcConfigurerAdapter</code> 子类。但是不要有 <code>@EnableWebMvc</code> 因为这个注解是完全的接管 mvc 的配置。 <a href="https://docs.spring.io/spring/docs/4.3.14.RELEASE/spring-framework-reference/htmlsingle#mvc" target="_blank" rel="noopener">MVC configuration</a> </p>
<h3 id="2、扩展SpringMVC"><a href="#2、扩展SpringMVC" class="headerlink" title="2、扩展SpringMVC"></a>2、扩展SpringMVC</h3><p><strong>编写一个配置类（@Configuration），是WebMvcConfigurerAdapter类型，但是这个类不能标注@EnableWebMvc注解</strong>，这样的话我们既保留了所有的自动配置，也能用我们扩展的配置。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用WebMvcConfigurerAdapter可以来扩展SpringMVC的功能</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyMvcConfig</span> <span class="keyword">extends</span> <span class="title">WebMvcConfigurerAdapter</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addViewControllers</span><span class="params">(ViewControllerRegistry registry)</span> </span>&#123;</span><br><span class="line">       <span class="comment">// super.addViewControllers(registry);</span></span><br><span class="line">        <span class="comment">//浏览器发送 /hello 请求来到 success</span></span><br><span class="line">        registry.addViewController(<span class="string">"/hello"</span>).setViewName(<span class="string">"success"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//... 其他的功能我们就直接重写里面对应的方法即可。</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>原理：</p>
<p>​    1）、WebMvcAutoConfiguration是SpringMVC的自动配置类</p>
<p>​    2）、在做其他自动配置时会导入 @Import(<strong>EnableWebMvcConfiguration</strong>.class) 这个类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">   <span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">EnableWebMvcConfiguration</span> <span class="keyword">extends</span> <span class="title">DelegatingWebMvcConfiguration</span> </span>&#123;</span><br><span class="line">     <span class="keyword">private</span> <span class="keyword">final</span> WebMvcConfigurerComposite configurers = <span class="keyword">new</span> WebMvcConfigurerComposite();</span><br><span class="line"></span><br><span class="line"> <span class="comment">//从容器中获取所有的WebMvcConfigurer</span></span><br><span class="line">     <span class="meta">@Autowired</span>(required = <span class="keyword">false</span>)</span><br><span class="line">     <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setConfigurers</span><span class="params">(List&lt;WebMvcConfigurer&gt; configurers)</span> </span>&#123;</span><br><span class="line">         <span class="keyword">if</span> (!CollectionUtils.isEmpty(configurers)) &#123;</span><br><span class="line">             <span class="keyword">this</span>.configurers.addWebMvcConfigurers(configurers);</span><br><span class="line">           	<span class="comment">//一个参考实现，将所有的WebMvcConfigurer相关配置都便利一遍，然后把里面的方法都调用一遍 </span></span><br><span class="line">           	<span class="meta">@Override</span></span><br><span class="line">            <span class="comment">// public void addViewControllers(ViewControllerRegistry registry) &#123;</span></span><br><span class="line">             <span class="comment">//    for (WebMvcConfigurer delegate : this.delegates) &#123;</span></span><br><span class="line">              <span class="comment">//       delegate.addViewControllers(registry);</span></span><br><span class="line">              <span class="comment">//   &#125;</span></span><br><span class="line">             &#125;</span><br><span class="line">         &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    3）、容器中所有的WebMvcConfigurer都会一起起作用</p>
<p>​    4）、我们的配置类也会被调用；</p>
<p>​    效果：SpringMVC的自动配置和我们的扩展配置都会起作用；</p>
<h3 id="3、全面接管SpringMVC；"><a href="#3、全面接管SpringMVC；" class="headerlink" title="3、全面接管SpringMVC；"></a>3、全面接管SpringMVC；</h3><p>SpringBoot对SpringMVC的自动配置不需要了，所有都是我们自己配置；所有的SpringMVC的自动配置都失效了</p>
<p><strong>我们需要在配置类中添加@EnableWebMvc即可；</strong></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用WebMvcConfigurerAdapter可以来扩展SpringMVC的功能</span></span><br><span class="line"><span class="meta">@EnableWebMvc</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyMvcConfig</span> <span class="keyword">extends</span> <span class="title">WebMvcConfigurerAdapter</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addViewControllers</span><span class="params">(ViewControllerRegistry registry)</span> </span>&#123;</span><br><span class="line">       <span class="comment">// super.addViewControllers(registry);</span></span><br><span class="line">        <span class="comment">//浏览器发送 /atguigu 请求来到 success</span></span><br><span class="line">        registry.addViewController(<span class="string">"/atguigu"</span>).setViewName(<span class="string">"success"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>原理：</p>
<p>为什么@EnableWebMvc自动配置就失效了；</p>
<p>1）@EnableWebMvc的核心，就是帮我们导入这个类，帮我们做一些基本的配置。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Import</span>(DelegatingWebMvcConfiguration.class)</span><br><span class="line"><span class="keyword">public</span> <span class="meta">@interface</span> EnableWebMvc &#123;</span><br></pre></td></tr></table></figure>

<p>2）、</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">DelegatingWebMvcConfiguration</span> <span class="keyword">extends</span> <span class="title">WebMvcConfigurationSupport</span> </span>&#123;</span><br></pre></td></tr></table></figure>

<p>3）、</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@ConditionalOnWebApplication</span></span><br><span class="line"><span class="meta">@ConditionalOnClass</span>(&#123; Servlet.class, DispatcherServlet.class,</span><br><span class="line">		WebMvcConfigurerAdapter.class &#125;)</span><br><span class="line"><span class="comment">//容器中没有这个组件的时候，这个自动配置类才生效</span></span><br><span class="line"><span class="meta">@ConditionalOnMissingBean</span>(WebMvcConfigurationSupport.class)</span><br><span class="line"><span class="meta">@AutoConfigureOrder</span>(Ordered.HIGHEST_PRECEDENCE + <span class="number">10</span>)</span><br><span class="line"><span class="meta">@AutoConfigureAfter</span>(&#123; DispatcherServletAutoConfiguration.class,</span><br><span class="line">		ValidationAutoConfiguration.class &#125;)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WebMvcAutoConfiguration</span> </span>&#123;</span><br></pre></td></tr></table></figure>

<p>4）、@EnableWebMvc将WebMvcConfigurationSupport组件导入进来；</p>
<p>5）、导入的WebMvcConfigurationSupport只是SpringMVC最基本的功能；</p>
<h2 id="5、如何修改SpringBoot的默认配置"><a href="#5、如何修改SpringBoot的默认配置" class="headerlink" title="5、如何修改SpringBoot的默认配置"></a>5、如何修改SpringBoot的默认配置</h2><p>模式：</p>
<p>​    1）、SpringBoot在自动配置很多组件的时候，先看容器中有没有用户自己配置的（@Bean、@Component）如果有就用用户配置的，如果没有，才自动配置；如果有些组件可以有多个（ViewResolver）将用户配置的和自己默认的组合起来；</p>
<p>​    2）、在SpringBoot中会有非常多的xxxConfigurer帮助我们进行扩展配置</p>
<p>​    3）、在SpringBoot中会有很多的xxxCustomizer帮助我们进行定制配置</p>
<h2 id="6、RestfulCRUD"><a href="#6、RestfulCRUD" class="headerlink" title="6、RestfulCRUD"></a>6、RestfulCRUD</h2><h3 id="1）、默认访问首页"><a href="#1）、默认访问首页" class="headerlink" title="1）、默认访问首页"></a>1）、默认访问首页</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">//使用WebMvcConfigurerAdapter可以来扩展SpringMVC的功能</span></span><br><span class="line"><span class="comment">//@EnableWebMvc   不要接管SpringMVC</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyMvcConfig</span> <span class="keyword">extends</span> <span class="title">WebMvcConfigurerAdapter</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addViewControllers</span><span class="params">(ViewControllerRegistry registry)</span> </span>&#123;</span><br><span class="line">       <span class="comment">// super.addViewControllers(registry);</span></span><br><span class="line">        <span class="comment">//浏览器发送 /hello 请求来到 success</span></span><br><span class="line">        registry.addViewController(<span class="string">"/hello"</span>).setViewName(<span class="string">"success"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//所有的WebMvcConfigurerAdapter组件都会一起起作用，也就是可以有多个这个东西。</span></span><br><span class="line">    <span class="meta">@Bean</span> <span class="comment">//将组件注册在容器</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> WebMvcConfigurerAdapter <span class="title">webMvcConfigurerAdapter</span><span class="params">()</span></span>&#123;</span><br><span class="line">        WebMvcConfigurerAdapter adapter = <span class="keyword">new</span> WebMvcConfigurerAdapter() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addViewControllers</span><span class="params">(ViewControllerRegistry registry)</span> </span>&#123;</span><br><span class="line">                registry.addViewController(<span class="string">"/"</span>).setViewName(<span class="string">"login"</span>);</span><br><span class="line">                registry.addViewController(<span class="string">"/index.html"</span>).setViewName(<span class="string">"login"</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line">        <span class="keyword">return</span> adapter;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2）、国际化"><a href="#2）、国际化" class="headerlink" title="2）、国际化"></a>2）、国际化</h3><p>1）、编写国际化配置文件</p>
<p>2）、使用ResourceBundleMessageSource管理国际化资源文件</p>
<p>3）、在页面使用fmt:message取出国际化内容</p>
<p>步骤：</p>
<p>1）、编写国际化配置文件，抽取页面需要显示的国际化消息<br>2）、SpringBoot自动配置好了管理国际化资源文件的组件；</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"spring.messages"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MessageSourceAutoConfiguration</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">	 * Comma-separated list of basenames (essentially a fully-qualified classpath</span></span><br><span class="line"><span class="comment">	 * location), each following the ResourceBundle convention with relaxed support for</span></span><br><span class="line"><span class="comment">	 * slash based locations. If it doesn't contain a package qualifier (such as</span></span><br><span class="line"><span class="comment">	 * "org.mypackage"), it will be resolved from the classpath root.</span></span><br><span class="line"><span class="comment">	 */</span></span><br><span class="line">	<span class="keyword">private</span> String basename = <span class="string">"messages"</span>;  </span><br><span class="line">    <span class="comment">//我们的配置文件可以直接放在类路径下叫messages.properties；</span></span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> MessageSource <span class="title">messageSource</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		ResourceBundleMessageSource messageSource = <span class="keyword">new</span> ResourceBundleMessageSource();</span><br><span class="line">		<span class="keyword">if</span> (StringUtils.hasText(<span class="keyword">this</span>.basename)) &#123;</span><br><span class="line">            <span class="comment">//设置国际化资源文件的基础名（去掉语言国家代码的）</span></span><br><span class="line">			messageSource.setBasenames(StringUtils.commaDelimitedListToStringArray(</span><br><span class="line">					StringUtils.trimAllWhitespace(<span class="keyword">this</span>.basename)));</span><br><span class="line">		&#125;</span><br><span class="line">		<span class="keyword">if</span> (<span class="keyword">this</span>.encoding != <span class="keyword">null</span>) &#123;</span><br><span class="line">			messageSource.setDefaultEncoding(<span class="keyword">this</span>.encoding.name());</span><br><span class="line">		&#125;</span><br><span class="line">		messageSource.setFallbackToSystemLocale(<span class="keyword">this</span>.fallbackToSystemLocale);</span><br><span class="line">		messageSource.setCacheSeconds(<span class="keyword">this</span>.cacheSeconds);</span><br><span class="line">		messageSource.setAlwaysUseMessageFormat(<span class="keyword">this</span>.alwaysUseMessageFormat);</span><br><span class="line">		<span class="keyword">return</span> messageSource;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>3）、去页面获取国际化的值；</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">"en"</span>  <span class="attr">xmlns:th</span>=<span class="string">"http://www.thymeleaf.org"</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">link</span> <span class="attr">href</span>=<span class="string">"asserts/css/bootstrap.min.css"</span> <span class="attr">th:href</span>=<span class="string">"@&#123;/webjars/bootstrap/4.0.0/css/bootstrap.css&#125;"</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!-- Custom styles for this template --&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">link</span> <span class="attr">href</span>=<span class="string">"asserts/css/signin.css"</span> <span class="attr">th:href</span>=<span class="string">"@&#123;/asserts/css/signin.css&#125;"</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">h1</span> <span class="attr">class</span>=<span class="string">"h3 mb-3 font-weight-normal"</span> <span class="attr">th:text</span>=<span class="string">"#&#123;login.tip&#125;"</span>&gt;</span>Please sign in<span class="tag">&lt;/<span class="name">h1</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>效果：根据浏览器语言设置的信息切换了国际化；</p>
<p>原理：</p>
<p>​    国际化Locale（区域信息对象）；LocaleResolver（获取区域信息对象）；</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">		<span class="meta">@Bean</span></span><br><span class="line">		<span class="meta">@ConditionalOnMissingBean</span></span><br><span class="line">		<span class="meta">@ConditionalOnProperty</span>(prefix = <span class="string">"spring.mvc"</span>, name = <span class="string">"locale"</span>)</span><br><span class="line">		<span class="function"><span class="keyword">public</span> LocaleResolver <span class="title">localeResolver</span><span class="params">()</span> </span>&#123;</span><br><span class="line">			<span class="keyword">if</span> (<span class="keyword">this</span>.mvcProperties</span><br><span class="line">					.getLocaleResolver() == WebMvcProperties.LocaleResolver.FIXED) &#123;</span><br><span class="line">				<span class="keyword">return</span> <span class="keyword">new</span> FixedLocaleResolver(<span class="keyword">this</span>.mvcProperties.getLocale());</span><br><span class="line">			&#125;</span><br><span class="line">			AcceptHeaderLocaleResolver localeResolver = <span class="keyword">new</span> AcceptHeaderLocaleResolver();</span><br><span class="line">			localeResolver.setDefaultLocale(<span class="keyword">this</span>.mvcProperties.getLocale());</span><br><span class="line">			<span class="keyword">return</span> localeResolver;</span><br><span class="line">		&#125;</span><br><span class="line">默认的就是根据请求头带来的区域信息获取Locale进行国际化</span><br></pre></td></tr></table></figure>

<p>4）、点击链接切换国际化</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 可以在连接上携带区域信息</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyLocaleResolver</span> <span class="keyword">implements</span> <span class="title">LocaleResolver</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Locale <span class="title">resolveLocale</span><span class="params">(HttpServletRequest request)</span> </span>&#123;</span><br><span class="line">        String l = request.getParameter(<span class="string">"l"</span>);</span><br><span class="line">        Locale locale = Locale.getDefault();</span><br><span class="line">        <span class="keyword">if</span>(!StringUtils.isEmpty(l))&#123;</span><br><span class="line">            String[] split = l.split(<span class="string">"_"</span>);</span><br><span class="line">            locale = <span class="keyword">new</span> Locale(split[<span class="number">0</span>],split[<span class="number">1</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> locale;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setLocale</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Locale locale)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"> <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> LocaleResolver <span class="title">localeResolver</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> MyLocaleResolver();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3）、登陆"><a href="#3）、登陆" class="headerlink" title="3）、登陆"></a>3）、登陆</h3><p>开发期间模板引擎页面修改以后，要实时生效</p>
<p>1）、禁用模板引擎的缓存</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 禁用缓存</span><br><span class="line">spring.thymeleaf.cache=false</span><br></pre></td></tr></table></figure>

<p>2）、页面修改完成以后ctrl+f9：重新编译；</p>
<p>登陆错误消息的显示</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">p</span> <span class="attr">style</span>=<span class="string">"color: red"</span> <span class="attr">th:text</span>=<span class="string">"$&#123;msg&#125;"</span> <span class="attr">th:if</span>=<span class="string">"$&#123;not #strings.isEmpty(msg)&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">p</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>表单重复提交，我们使用post请求提交数据来到新的页面的时候如果我们再次刷新，就会询问我们是否需要重复提交表单数据，这样我们就可以登陆成功以后使用重定向来完成，而不是采用默认的行为。</p>
<h3 id="4）、拦截器进行登陆检查"><a href="#4）、拦截器进行登陆检查" class="headerlink" title="4）、拦截器进行登陆检查"></a>4）、拦截器进行登陆检查</h3><p>拦截器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 登陆检查，</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LoginHandlerInterceptor</span> <span class="keyword">implements</span> <span class="title">HandlerInterceptor</span> </span>&#123;</span><br><span class="line">    <span class="comment">//目标方法执行之前</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">preHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        Object user = request.getSession().getAttribute(<span class="string">"loginUser"</span>);</span><br><span class="line">        <span class="keyword">if</span>(user == <span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="comment">//未登陆，返回登陆页面</span></span><br><span class="line">            request.setAttribute(<span class="string">"msg"</span>,<span class="string">"没有权限请先登陆"</span>);</span><br><span class="line">            request.getRequestDispatcher(<span class="string">"/index.html"</span>).forward(request,response);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">            <span class="comment">//已登陆，放行请求</span></span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">postHandle</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler, ModelAndView modelAndView)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">afterCompletion</span><span class="params">(HttpServletRequest request, HttpServletResponse response, Object handler, Exception ex)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注册拦截器</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//所有的WebMvcConfigurerAdapter组件都会一起起作用</span></span><br><span class="line">  <span class="meta">@Bean</span> <span class="comment">//将组件注册在容器</span></span><br><span class="line">  <span class="function"><span class="keyword">public</span> WebMvcConfigurerAdapter <span class="title">webMvcConfigurerAdapter</span><span class="params">()</span></span>&#123;</span><br><span class="line">      WebMvcConfigurerAdapter adapter = <span class="keyword">new</span> WebMvcConfigurerAdapter() &#123;</span><br><span class="line">          <span class="meta">@Override</span></span><br><span class="line">          <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addViewControllers</span><span class="params">(ViewControllerRegistry registry)</span> </span>&#123;</span><br><span class="line">              registry.addViewController(<span class="string">"/"</span>).setViewName(<span class="string">"login"</span>);</span><br><span class="line">              registry.addViewController(<span class="string">"/index.html"</span>).setViewName(<span class="string">"login"</span>);</span><br><span class="line">              registry.addViewController(<span class="string">"/main.html"</span>).setViewName(<span class="string">"dashboard"</span>);</span><br><span class="line">          &#125;</span><br><span class="line"></span><br><span class="line">          <span class="comment">//注册拦截器</span></span><br><span class="line">          <span class="meta">@Override</span></span><br><span class="line">          <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addInterceptors</span><span class="params">(InterceptorRegistry registry)</span> </span>&#123;</span><br><span class="line">              <span class="comment">//super.addInterceptors(registry);</span></span><br><span class="line">              <span class="comment">//静态资源；  *.css , *.js</span></span><br><span class="line">              <span class="comment">//SpringBoot已经做好了静态资源映射</span></span><br><span class="line">              registry.addInterceptor(<span class="keyword">new</span> LoginHandlerInterceptor()).addPathPatterns(<span class="string">"/**"</span>)</span><br><span class="line">                      .excludePathPatterns(<span class="string">"/index.html"</span>,<span class="string">"/"</span>,<span class="string">"/user/login"</span>);</span><br><span class="line">          &#125;</span><br><span class="line">      &#125;;</span><br><span class="line">      <span class="keyword">return</span> adapter;</span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<h3 id="5）、CRUD-员工列表"><a href="#5）、CRUD-员工列表" class="headerlink" title="5）、CRUD-员工列表"></a>5）、CRUD-员工列表</h3><p>实验要求：</p>
<p>1）、RestfulCRUD：CRUD满足Rest风格；</p>
<p>URI：  /资源名称/资源标识       HTTP请求方式区分对资源CRUD操作</p>
<table>
<thead>
<tr>
<th></th>
<th>普通CRUD（uri来区分操作）</th>
<th>RestfulCRUD</th>
</tr>
</thead>
<tbody><tr>
<td>查询</td>
<td>getEmp</td>
<td>emp—GET</td>
</tr>
<tr>
<td>添加</td>
<td>addEmp?xxx</td>
<td>emp—POST</td>
</tr>
<tr>
<td>修改</td>
<td>updateEmp?id=xxx&amp;xxx=xx</td>
<td>emp/{id}—PUT</td>
</tr>
<tr>
<td>删除</td>
<td>deleteEmp?id=1</td>
<td>emp/{id}—DELETE</td>
</tr>
</tbody></table>
<p>2）、实验的请求架构;</p>
<table>
<thead>
<tr>
<th>实验功能</th>
<th>请求URI</th>
<th>请求方式</th>
</tr>
</thead>
<tbody><tr>
<td>查询所有员工</td>
<td>emps</td>
<td>GET</td>
</tr>
<tr>
<td>查询某个员工(来到修改页面)</td>
<td>emp/1</td>
<td>GET</td>
</tr>
<tr>
<td>来到添加页面</td>
<td>emp</td>
<td>GET</td>
</tr>
<tr>
<td>添加员工</td>
<td>emp</td>
<td>POST</td>
</tr>
<tr>
<td>来到修改页面（查出员工进行信息回显）</td>
<td>emp/1</td>
<td>GET</td>
</tr>
<tr>
<td>修改员工</td>
<td>emp</td>
<td>PUT</td>
</tr>
<tr>
<td>删除员工</td>
<td>emp/1</td>
<td>DELETE</td>
</tr>
</tbody></table>
<p>3）、员工列表：</p>
<h4 id="thymeleaf公共页面元素抽取"><a href="#thymeleaf公共页面元素抽取" class="headerlink" title="thymeleaf公共页面元素抽取"></a>thymeleaf公共页面元素抽取</h4><figure class="highlight html"><table><tr><td class="code"><pre><span class="line">1、抽取公共片段</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:fragment</span>=<span class="string">"copy"</span>&gt;</span></span><br><span class="line">&amp;copy; 2011 The Good Thymes Virtual Grocery</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line">2、引入公共片段</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:insert</span>=<span class="string">"~&#123;footer :: copy&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">~&#123;templatename::selector&#125;：模板名::选择器</span><br><span class="line">~&#123;templatename::fragmentname&#125;:模板名::片段名</span><br><span class="line"></span><br><span class="line">3、默认效果：</span><br><span class="line">insert的公共片段在div标签中</span><br><span class="line">如果使用th:insert等属性进行引入，可以不用写~&#123;&#125;：</span><br><span class="line">行内写法可以加上：[[~&#123;&#125;]];[(~&#123;&#125;)]；</span><br></pre></td></tr></table></figure>

<p>三种引入公共片段的th属性：</p>
<p><strong>th:insert</strong>：将公共片段整个插入到声明引入的元素中</p>
<p><strong>th:replace</strong>：将声明引入的元素替换为公共片段</p>
<p><strong>th:include</strong>：将被引入的片段的内容包含进这个标签中</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">footer</span> <span class="attr">th:fragment</span>=<span class="string">"copy"</span>&gt;</span></span><br><span class="line">&amp;copy; 2011 The Good Thymes Virtual Grocery</span><br><span class="line"><span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"></span><br><span class="line">引入方式</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:insert</span>=<span class="string">"footer :: copy"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:replace</span>=<span class="string">"footer :: copy"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:include</span>=<span class="string">"footer :: copy"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line">效果</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">footer</span>&gt;</span></span><br><span class="line">    &amp;copy; 2011 The Good Thymes Virtual Grocery</span><br><span class="line">    <span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">footer</span>&gt;</span></span><br><span class="line">&amp;copy; 2011 The Good Thymes Virtual Grocery</span><br><span class="line"><span class="tag">&lt;/<span class="name">footer</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span>&gt;</span></span><br><span class="line">&amp;copy; 2011 The Good Thymes Virtual Grocery</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>引入片段的时候传入参数： </p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">nav</span> <span class="attr">class</span>=<span class="string">"col-md-2 d-none d-md-block bg-light sidebar"</span> <span class="attr">id</span>=<span class="string">"sidebar"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"sidebar-sticky"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">ul</span> <span class="attr">class</span>=<span class="string">"nav flex-column"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">li</span> <span class="attr">class</span>=<span class="string">"nav-item"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">a</span> <span class="attr">class</span>=<span class="string">"nav-link active"</span></span></span><br><span class="line"><span class="tag">                   <span class="attr">th:class</span>=<span class="string">"$&#123;activeUri=='main.html'?'nav-link active':'nav-link'&#125;"</span></span></span><br><span class="line"><span class="tag">                   <span class="attr">href</span>=<span class="string">"#"</span> <span class="attr">th:href</span>=<span class="string">"@&#123;/main.html&#125;"</span>&gt;</span></span><br><span class="line">                    <span class="tag">&lt;<span class="name">svg</span> <span class="attr">xmlns</span>=<span class="string">"http://www.w3.org/2000/svg"</span> <span class="attr">width</span>=<span class="string">"24"</span> <span class="attr">height</span>=<span class="string">"24"</span> <span class="attr">viewBox</span>=<span class="string">"0 0 24 24"</span> <span class="attr">fill</span>=<span class="string">"none"</span> <span class="attr">stroke</span>=<span class="string">"currentColor"</span> <span class="attr">stroke-width</span>=<span class="string">"2"</span> <span class="attr">stroke-linecap</span>=<span class="string">"round"</span> <span class="attr">stroke-linejoin</span>=<span class="string">"round"</span> <span class="attr">class</span>=<span class="string">"feather feather-home"</span>&gt;</span></span><br><span class="line">                        <span class="tag">&lt;<span class="name">path</span> <span class="attr">d</span>=<span class="string">"M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"</span>&gt;</span><span class="tag">&lt;/<span class="name">path</span>&gt;</span></span><br><span class="line">                        <span class="tag">&lt;<span class="name">polyline</span> <span class="attr">points</span>=<span class="string">"9 22 9 12 15 12 15 22"</span>&gt;</span><span class="tag">&lt;/<span class="name">polyline</span>&gt;</span></span><br><span class="line">                    <span class="tag">&lt;/<span class="name">svg</span>&gt;</span></span><br><span class="line">                    Dashboard <span class="tag">&lt;<span class="name">span</span> <span class="attr">class</span>=<span class="string">"sr-only"</span>&gt;</span>(current)<span class="tag">&lt;/<span class="name">span</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">li</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!--引入侧边栏;传入参数--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">th:replace</span>=<span class="string">"commons/bar::#sidebar(activeUri='emps')"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="6）、CRUD-员工添加"><a href="#6）、CRUD-员工添加" class="headerlink" title="6）、CRUD-员工添加"></a>6）、CRUD-员工添加</h3><p>添加页面</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">form</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-group"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">label</span>&gt;</span>LastName<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">class</span>=<span class="string">"form-control"</span> <span class="attr">placeholder</span>=<span class="string">"zhangsan"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-group"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">label</span>&gt;</span>Email<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"email"</span> <span class="attr">class</span>=<span class="string">"form-control"</span> <span class="attr">placeholder</span>=<span class="string">"zhangsan@atguigu.com"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-group"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">label</span>&gt;</span>Gender<span class="tag">&lt;/<span class="name">label</span>&gt;</span><span class="tag">&lt;<span class="name">br</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-check form-check-inline"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">input</span> <span class="attr">class</span>=<span class="string">"form-check-input"</span> <span class="attr">type</span>=<span class="string">"radio"</span> <span class="attr">name</span>=<span class="string">"gender"</span>  <span class="attr">value</span>=<span class="string">"1"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">label</span> <span class="attr">class</span>=<span class="string">"form-check-label"</span>&gt;</span>男<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-check form-check-inline"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">input</span> <span class="attr">class</span>=<span class="string">"form-check-input"</span> <span class="attr">type</span>=<span class="string">"radio"</span> <span class="attr">name</span>=<span class="string">"gender"</span>  <span class="attr">value</span>=<span class="string">"0"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">label</span> <span class="attr">class</span>=<span class="string">"form-check-label"</span>&gt;</span>女<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-group"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">label</span>&gt;</span>department<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">select</span> <span class="attr">class</span>=<span class="string">"form-control"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">option</span>&gt;</span>1<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">option</span>&gt;</span>2<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">option</span>&gt;</span>3<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">option</span>&gt;</span>4<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">option</span>&gt;</span>5<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-group"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">label</span>&gt;</span>Birth<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">class</span>=<span class="string">"form-control"</span> <span class="attr">placeholder</span>=<span class="string">"zhangsan"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">button</span> <span class="attr">type</span>=<span class="string">"submit"</span> <span class="attr">class</span>=<span class="string">"btn btn-primary"</span>&gt;</span>添加<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>提交的数据格式不对：生日：日期；</p>
<p>2017-12-12；2017/12/12；2017.12.12；</p>
<p>日期的格式化；SpringMVC将页面提交的值需要转换为指定的类型;</p>
<p>2017-12-12—Date； 类型转换，格式化;</p>
<p>默认日期是按照/的方式；</p>
<h3 id="7）、CRUD-员工修改"><a href="#7）、CRUD-员工修改" class="headerlink" title="7）、CRUD-员工修改"></a>7）、CRUD-员工修改</h3><p>修改添加二合一表单</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--需要区分是员工修改还是添加；--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">form</span> <span class="attr">th:action</span>=<span class="string">"@&#123;/emp&#125;"</span> <span class="attr">method</span>=<span class="string">"post"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--发送put请求修改员工数据--&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--</span></span><br><span class="line"><span class="comment">1、SpringMVC中配置HiddenHttpMethodFilter;（SpringBoot自动配置好的）</span></span><br><span class="line"><span class="comment">2、页面创建一个post表单</span></span><br><span class="line"><span class="comment">3、创建一个input项，name="_method";值就是我们指定的请求方式</span></span><br><span class="line"><span class="comment">--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"hidden"</span> <span class="attr">name</span>=<span class="string">"_method"</span> <span class="attr">value</span>=<span class="string">"put"</span> <span class="attr">th:if</span>=<span class="string">"$&#123;emp!=null&#125;"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">input</span> <span class="attr">type</span>=<span class="string">"hidden"</span> <span class="attr">name</span>=<span class="string">"id"</span> <span class="attr">th:if</span>=<span class="string">"$&#123;emp!=null&#125;"</span> <span class="attr">th:value</span>=<span class="string">"$&#123;emp.id&#125;"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-group"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">label</span>&gt;</span>LastName<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">input</span> <span class="attr">name</span>=<span class="string">"lastName"</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">class</span>=<span class="string">"form-control"</span> <span class="attr">placeholder</span>=<span class="string">"zhangsan"</span> <span class="attr">th:value</span>=<span class="string">"$&#123;emp!=null&#125;?$&#123;emp.lastName&#125;"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-group"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">label</span>&gt;</span>Email<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">input</span> <span class="attr">name</span>=<span class="string">"email"</span> <span class="attr">type</span>=<span class="string">"email"</span> <span class="attr">class</span>=<span class="string">"form-control"</span> <span class="attr">placeholder</span>=<span class="string">"zhangsan@atguigu.com"</span> <span class="attr">th:value</span>=<span class="string">"$&#123;emp!=null&#125;?$&#123;emp.email&#125;"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-group"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">label</span>&gt;</span>Gender<span class="tag">&lt;/<span class="name">label</span>&gt;</span><span class="tag">&lt;<span class="name">br</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-check form-check-inline"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">input</span> <span class="attr">class</span>=<span class="string">"form-check-input"</span> <span class="attr">type</span>=<span class="string">"radio"</span> <span class="attr">name</span>=<span class="string">"gender"</span> <span class="attr">value</span>=<span class="string">"1"</span> <span class="attr">th:checked</span>=<span class="string">"$&#123;emp!=null&#125;?$&#123;emp.gender==1&#125;"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">label</span> <span class="attr">class</span>=<span class="string">"form-check-label"</span>&gt;</span>男<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-check form-check-inline"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">input</span> <span class="attr">class</span>=<span class="string">"form-check-input"</span> <span class="attr">type</span>=<span class="string">"radio"</span> <span class="attr">name</span>=<span class="string">"gender"</span> <span class="attr">value</span>=<span class="string">"0"</span> <span class="attr">th:checked</span>=<span class="string">"$&#123;emp!=null&#125;?$&#123;emp.gender==0&#125;"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">label</span> <span class="attr">class</span>=<span class="string">"form-check-label"</span>&gt;</span>女<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-group"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">label</span>&gt;</span>department<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--提交的是部门的id--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">select</span> <span class="attr">class</span>=<span class="string">"form-control"</span> <span class="attr">name</span>=<span class="string">"department.id"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">option</span> <span class="attr">th:selected</span>=<span class="string">"$&#123;emp!=null&#125;?$&#123;dept.id == emp.department.id&#125;"</span> <span class="attr">th:value</span>=<span class="string">"$&#123;dept.id&#125;"</span> <span class="attr">th:each</span>=<span class="string">"dept:$&#123;depts&#125;"</span> <span class="attr">th:text</span>=<span class="string">"$&#123;dept.departmentName&#125;"</span>&gt;</span>1<span class="tag">&lt;/<span class="name">option</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">select</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"form-group"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">label</span>&gt;</span>Birth<span class="tag">&lt;/<span class="name">label</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">input</span> <span class="attr">name</span>=<span class="string">"birth"</span> <span class="attr">type</span>=<span class="string">"text"</span> <span class="attr">class</span>=<span class="string">"form-control"</span> <span class="attr">placeholder</span>=<span class="string">"zhangsan"</span> <span class="attr">th:value</span>=<span class="string">"$&#123;emp!=null&#125;?$&#123;#dates.format(emp.birth, 'yyyy-MM-dd HH:mm')&#125;"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">button</span> <span class="attr">type</span>=<span class="string">"submit"</span> <span class="attr">class</span>=<span class="string">"btn btn-primary"</span> <span class="attr">th:text</span>=<span class="string">"$&#123;emp!=null&#125;?'修改':'添加'"</span>&gt;</span>添加<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="8）、CRUD-员工删除"><a href="#8）、CRUD-员工删除" class="headerlink" title="8）、CRUD-员工删除"></a>8）、CRUD-员工删除</h3><figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">tr</span> <span class="attr">th:each</span>=<span class="string">"emp:$&#123;emps&#125;"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span> <span class="attr">th:text</span>=<span class="string">"$&#123;emp.id&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span>&gt;</span>[[$&#123;emp.lastName&#125;]]<span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span> <span class="attr">th:text</span>=<span class="string">"$&#123;emp.email&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span> <span class="attr">th:text</span>=<span class="string">"$&#123;emp.gender&#125;==0?'女':'男'"</span>&gt;</span><span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span> <span class="attr">th:text</span>=<span class="string">"$&#123;emp.department.departmentName&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span> <span class="attr">th:text</span>=<span class="string">"$&#123;#dates.format(emp.birth, 'yyyy-MM-dd HH:mm')&#125;"</span>&gt;</span><span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">td</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">a</span> <span class="attr">class</span>=<span class="string">"btn btn-sm btn-primary"</span> <span class="attr">th:href</span>=<span class="string">"@&#123;/emp/&#125;+$&#123;emp.id&#125;"</span>&gt;</span>编辑<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">button</span> <span class="attr">th:attr</span>=<span class="string">"del_uri=@&#123;/emp/&#125;+$&#123;emp.id&#125;"</span> <span class="attr">class</span>=<span class="string">"btn btn-sm btn-danger deleteBtn"</span>&gt;</span>删除<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">td</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">tr</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript">    $(<span class="string">".deleteBtn"</span>).click(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</span></span><br><span class="line"><span class="actionscript">        <span class="comment">//删除当前员工的</span></span></span><br><span class="line"><span class="javascript">        $(<span class="string">"#deleteEmpForm"</span>).attr(<span class="string">"action"</span>,$(<span class="keyword">this</span>).attr(<span class="string">"del_uri"</span>)).submit();</span></span><br><span class="line"><span class="actionscript">        <span class="keyword">return</span> <span class="literal">false</span>;</span></span><br><span class="line">    &#125;);</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot 笔记 ( 三 )：日志系统</title>
    <url>/2018/04/13/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E4%B8%89%20)%EF%BC%9A%E6%97%A5%E5%BF%97%E7%B3%BB%E7%BB%9F/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-三-：日志系统"><a href="#SpringBoot-笔记-三-：日志系统" class="headerlink" title="SpringBoot 笔记 ( 三 )：日志系统"></a>SpringBoot 笔记 ( 三 )：日志系统</h1><h2 id="1、日志框架"><a href="#1、日志框架" class="headerlink" title="1、日志框架"></a>1、日志框架</h2><p>日志框架就是防止我们再去像以前那样，一直进行System.out.println(“”)将关键数据打印在控制台。框架来记录系统的一些运行时信息，但是随着日志框架的增长，和接口的不一致，导致了使用上的差别很大，这里采用了一个类似于数据库驱动的模式，数据库驱动是 Java 提供的一个 API，然后真正的实现是需要各个数据库厂商去完成的，而 log 也开始采用这种面向接口编程的方法采用日志抽象层。</p>
<a id="more"></a>
<p><strong>市面上的日志框架</strong><br>JUL、JCL、Jboss-logging、logback、log4j、log4j2、slf4j….</p>
<table>
<thead>
<tr>
<th>日志门面  （日志的抽象层）</th>
<th>日志实现</th>
</tr>
</thead>
<tbody><tr>
<td><del>JCL（Jakarta  Commons Logging）</del>    SLF4j（Simple  Logging Facade for Java）    <del>jboss-logging</del></td>
<td>Log4j  JUL（java.util.logging）  Log4j2（log4j 的增强版）  <strong>Logback</strong>(log4j 的重制版)</td>
</tr>
</tbody></table>
<p>左边选一个门面（抽象层）、右边来选一个实现；<br>日志门面：  SLF4J；<br>日志实现：Logback；</p>
<p>SpringBoot：底层是Spring框架，Spring框架默认是用JCL；<br><strong>==SpringBoot选用 SLF4j和logback；==</strong></p>
<h2 id="2、SLF4j使用"><a href="#2、SLF4j使用" class="headerlink" title="2、SLF4j使用"></a>2、SLF4j使用</h2><h3 id="1、如何在系统中使用SLF4j-https-www-slf4j-org"><a href="#1、如何在系统中使用SLF4j-https-www-slf4j-org" class="headerlink" title="1、如何在系统中使用SLF4j   https://www.slf4j.org"></a>1、如何在系统中使用SLF4j   <a href="https://www.slf4j.org" target="_blank" rel="noopener">https://www.slf4j.org</a></h3><p>以后开发的时候，日志记录方法的调用，不应该来直接调用日志的实现类，而是调用日志抽象层里面的方法；</p>
<p>给系统里面导入slf4j的jar和  logback的实现jar</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.slf4j.Logger;</span><br><span class="line"><span class="keyword">import</span> org.slf4j.LoggerFactory;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloWorld</span> </span>&#123;</span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">    Logger logger = LoggerFactory.getLogger(HelloWorld.class);</span><br><span class="line">    logger.info(<span class="string">"Hello World"</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2、遗留问题"><a href="#2、遗留问题" class="headerlink" title="2、遗留问题"></a>2、遗留问题</h3><p>a（slf4j+logback）: Spring（commons-logging）、Hibernate（jboss-logging）、MyBatis、xxxx</p>
<p>就是我们的springboot中我们如果配置了一些其他应用，然后这些应用需要使用一些别的日志框架我们就需要配置非常多的配置文件，但是我们希望只配置一个就好了。就统一日志框架！</p>
<p><strong>如何让系统中所有的日志都统一到slf4j；</strong></p>
<ol>
<li>将系统中其他日志框架先排除出去，也就是删除那些应用的里的日志抽象层</li>
<li>用也就是日志抽象层和底层的实现整合的中间包（适配包）来替换原有的日志框架的抽象层</li>
<li>我们导入slf4j其他的实现，就是底层的日志实现。</li>
</ol>
<p>用一个简单的例子来说明，就是我们希望所有的日志门面采用的 slf4j 然后底层使用的 logback</p>
<p><img src="https://user-images.githubusercontent.com/22151420/40474883-b0058c70-5f72-11e8-8f35-9457d399526c.png" alt="image"></p>
<p>我们就是用了 jcl 、log4j、jul 的中见替换包替换了原来的日志门面，然后我们在整合底层的日志实现，就完成了整合统一。</p>
<h2 id="3、SpringBoot日志关系"><a href="#3、SpringBoot日志关系" class="headerlink" title="3、SpringBoot日志关系"></a>3、SpringBoot日志关系</h2><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-logging<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>总结：</p>
<p>​    1）、SpringBoot底层也是使用slf4j+logback的方式进行日志记录</p>
<p>​    2）、SpringBoot也把其他的日志都替换成了slf4j，也就是他统一了所有的starter的日志框架，那么必然使用了中间替换包。</p>
<p>​    3）、中间替换包,使用中间替换包才能把其他的日志框架替换掉。</p>
<p>​    4）、如果我们要引入其他框架，一定要把这个框架的默认日志依赖移除掉。</p>
<p>比如我们的Spring框架用的是commons-logging，但是我们的依赖就是直接排除了我们的自带的日志框架，然后引入中间包。如下：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">          <span class="comment">&lt;!-- 排除包  --&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>commons-logging<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>commons-logging<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p><strong>SpringBoot能自动适配所有的日志，而且底层使用slf4j+logback的方式记录日志，引入其他框架的时候，只需要把这个框架依赖的日志框架排除掉即可</strong></p>
<h2 id="4、日志使用"><a href="#4、日志使用" class="headerlink" title="4、日志使用"></a>4、日志使用</h2><h3 id="1、默认配置"><a href="#1、默认配置" class="headerlink" title="1、默认配置"></a>1、默认配置</h3><p>SpringBoot默认帮我们配置好了日志；</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//记录器</span></span><br><span class="line">Logger logger = LoggerFactory.getLogger(getClass());</span><br><span class="line"><span class="meta">@Test</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">contextLoads</span><span class="params">()</span> </span>&#123;</span><br><span class="line">	<span class="comment">//日志的级别；</span></span><br><span class="line">	<span class="comment">//由低到高   trace&lt;debug&lt;info&lt;warn&lt;error</span></span><br><span class="line">	<span class="comment">//可以调整输出的日志级别；日志就只会在这个级别以以后的高级别生效</span></span><br><span class="line">	logger.trace(<span class="string">"这是trace日志..."</span>);</span><br><span class="line">	logger.debug(<span class="string">"这是debug日志..."</span>);</span><br><span class="line">	<span class="comment">//SpringBoot默认给我们使用的是info级别的，没有指定级别的就用SpringBoot默认规定的级别</span></span><br><span class="line">	logger.info(<span class="string">"这是info日志..."</span>);</span><br><span class="line">	logger.warn(<span class="string">"这是warn日志..."</span>);</span><br><span class="line">	logger.error(<span class="string">"这是error日志..."</span>);</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<pre><code>日志输出格式：
    %d表示日期时间，
    %thread表示线程名，
    %-5level：级别从左显示5个字符宽度
    %logger{50} 表示logger名字最长50个字符，否则按照句点分割。 
    %msg：日志消息，
    %n是换行符
--&gt;
%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n</code></pre><p>SpringBoot修改日志的默认配置</p>
<figure class="highlight properties"><table><tr><td class="code"><pre><span class="line"><span class="meta">logging.level.com.atguigu</span>=<span class="string">trace</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">#logging.path=</span></span><br><span class="line"><span class="comment"># 不指定路径在当前项目下生成springboot.log日志</span></span><br><span class="line"><span class="comment"># 可以指定完整的路径；</span></span><br><span class="line"><span class="comment">#logging.file=G:/springboot.log</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># 在当前磁盘的根路径下创建spring文件夹和里面的log文件夹；使用 spring.log 作为默认文件</span></span><br><span class="line"><span class="meta">logging.path</span>=<span class="string">/spring/log</span></span><br><span class="line"></span><br><span class="line"><span class="comment">#  在控制台输出的日志的格式</span></span><br><span class="line"><span class="meta">logging.pattern.console</span>=<span class="string">%d&#123;yyyy-MM-dd&#125; [%thread] %-5level %logger&#123;50&#125; - %msg%n</span></span><br><span class="line"><span class="comment"># 指定文件中日志输出的格式</span></span><br><span class="line"><span class="meta">logging.pattern.file</span>=<span class="string">%d&#123;yyyy-MM-dd&#125; === [%thread] === %-5level === %logger&#123;50&#125; ==== %msg%n</span></span><br></pre></td></tr></table></figure>

<table>
<thead>
<tr>
<th>logging.file</th>
<th>logging.path</th>
<th>Example</th>
<th>Description</th>
</tr>
</thead>
<tbody><tr>
<td>(none)</td>
<td>(none)</td>
<td></td>
<td>只在控制台输出</td>
</tr>
<tr>
<td>指定文件名</td>
<td>(none)</td>
<td>my.log</td>
<td>输出日志到my.log文件</td>
</tr>
<tr>
<td>(none)</td>
<td>指定目录</td>
<td>/var/log</td>
<td>输出到指定目录的 spring.log 文件中</td>
</tr>
</tbody></table>
<h3 id="2、指定配置"><a href="#2、指定配置" class="headerlink" title="2、指定配置"></a>2、指定配置</h3><p>给类路径下放上每个日志框架自己的配置文件即可；SpringBoot就不使用他默认配置的了</p>
<table>
<thead>
<tr>
<th>Logging System</th>
<th>Customization</th>
</tr>
</thead>
<tbody><tr>
<td>Logback</td>
<td><code>logback-spring.xml</code>, <code>logback-spring.groovy</code>, <code>logback.xml</code> or <code>logback.groovy</code></td>
</tr>
<tr>
<td>Log4j2</td>
<td><code>log4j2-spring.xml</code> or <code>log4j2.xml</code></td>
</tr>
<tr>
<td>JDK (Java Util Logging)</td>
<td><code>logging.properties</code></td>
</tr>
</tbody></table>
<p>logback.xml：直接就被日志框架识别了；</p>
<p><strong>logback-spring.xml</strong>：日志框架就不直接加载日志的配置项，由SpringBoot解析日志配置，可以使用SpringBoot的高级Profile功能</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">springProfile</span> <span class="attr">name</span>=<span class="string">"staging"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- configuration to be enabled when the "staging" profile is active --&gt;</span></span><br><span class="line">  	可以指定某段配置只在某个环境下生效</span><br><span class="line"><span class="tag">&lt;/<span class="name">springProfile</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">appender</span> <span class="attr">name</span>=<span class="string">"stdout"</span> <span class="attr">class</span>=<span class="string">"ch.qos.logback.core.ConsoleAppender"</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--</span></span><br><span class="line"><span class="comment">        日志输出格式：</span></span><br><span class="line"><span class="comment">			%d表示日期时间，</span></span><br><span class="line"><span class="comment">			%thread表示线程名，</span></span><br><span class="line"><span class="comment">			%-5level：级别从左显示5个字符宽度</span></span><br><span class="line"><span class="comment">			%logger&#123;50&#125; 表示logger名字最长50个字符，否则按照句点分割。 </span></span><br><span class="line"><span class="comment">			%msg：日志消息，</span></span><br><span class="line"><span class="comment">			%n是换行符</span></span><br><span class="line"><span class="comment">        --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">layout</span> <span class="attr">class</span>=<span class="string">"ch.qos.logback.classic.PatternLayout"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">springProfile</span> <span class="attr">name</span>=<span class="string">"dev"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">pattern</span>&gt;</span>%d&#123;yyyy-MM-dd HH:mm:ss.SSS&#125; ----&gt; [%thread] ---&gt; %-5level %logger&#123;50&#125; - %msg%n<span class="tag">&lt;/<span class="name">pattern</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">springProfile</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">springProfile</span> <span class="attr">name</span>=<span class="string">"!dev"</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">pattern</span>&gt;</span>%d&#123;yyyy-MM-dd HH:mm:ss.SSS&#125; ==== [%thread] ==== %-5level %logger&#123;50&#125; - %msg%n<span class="tag">&lt;/<span class="name">pattern</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">springProfile</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">layout</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">appender</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>如果使用logback.xml作为日志配置文件，还要使用profile功能，会有以下错误</p>
<p> <code>no applicable action for [springProfile]</code></p>
<h2 id="5、切换日志框架"><a href="#5、切换日志框架" class="headerlink" title="5、切换日志框架"></a>5、切换日志框架</h2><p>可以按照slf4j的日志适配图，进行相关的切换；</p>
<p>slf4j+log4j的方式；</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>logback-classic<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>ch.qos.logback<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>log4j-over-slf4j<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">      <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.slf4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.slf4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>slf4j-log4j12<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>切换为log4j2</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">   <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">exclusions</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">exclusion</span>&gt;</span></span><br><span class="line">                    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-logging<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">                    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;/<span class="name">exclusion</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">exclusions</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-log4j2<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<hr>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot笔记 ( 二 )：自定义配置</title>
    <url>/2018/04/13/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E4%BA%8C%20)%EF%BC%9A%E8%87%AA%E5%AE%9A%E4%B9%89%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-二-：自定义配置"><a href="#SpringBoot-笔记-二-：自定义配置" class="headerlink" title="SpringBoot 笔记 ( 二 )：自定义配置"></a>SpringBoot 笔记 ( 二 )：自定义配置</h1><h3 id="1-配置文件"><a href="#1-配置文件" class="headerlink" title="1. 配置文件"></a>1. 配置文件</h3><p>SpringBoot使用一个全局的配置文件，配置文件名是固定的：</p>
<ul>
<li>application.properties</li>
<li>application.yml</li>
</ul>
<p>&emsp;&emsp;  修改SpringBoot自动配置的默认值，因为在所有的自动配置类中他们都会去读取我们的配置文件，如果说有配置这些项目就按照我们配置的，没有则使用自动配置。</p>
<a id="more"></a>
<p>&emsp;&emsp;  支持两种格式，我们主要说说后面一种，前面比较简单就是采用的点的方式定义的。yml 其实也是一种标记语言，YAML 他的语法比较简洁，写起来没有 xml 那么臃肿。</p>
<h3 id="2-YAML-语法"><a href="#2-YAML-语法" class="headerlink" title="2. YAML 语法"></a>2. YAML 语法</h3><h4 id="1、基本语法"><a href="#1、基本语法" class="headerlink" title="1、基本语法"></a>1、基本语法</h4><p>k:(空格)v：表示一对键值对（空格必须有）</p>
<p>以<strong>空格</strong>的缩进来控制层级关系；只要是左对齐的一列数据，都是同一个层级的同时属性和值也是大小写敏感；</p>
<h4 id="2-字面量：普通的值（数字，字符串，布尔）"><a href="#2-字面量：普通的值（数字，字符串，布尔）" class="headerlink" title="2. 字面量：普通的值（数字，字符串，布尔）"></a>2. 字面量：普通的值（数字，字符串，布尔）</h4><p>​    k: v：字面直接来写，字符串默认不用加上单引号或者双引号；</p>
<h4 id="3-对象、Map（属性和值）（键值对）："><a href="#3-对象、Map（属性和值）（键值对）：" class="headerlink" title="3. 对象、Map（属性和值）（键值对）："></a>3. 对象、Map（属性和值）（键值对）：</h4><p>​    k: v：在下一行来写对象的属性和值的关系，注意缩进</p>
<p>​        对象还是k: v的方式</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">friends:</span></span><br><span class="line">		<span class="attr">lastName:</span> <span class="string">zhangsan</span></span><br><span class="line">		<span class="attr">age:</span> <span class="number">20</span></span><br></pre></td></tr></table></figure>

<p>行内写法：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">friends:</span> <span class="string">&#123;lastName:</span> <span class="string">zhangsan,age:</span> <span class="number">18</span><span class="string">&#125;</span></span><br></pre></td></tr></table></figure>

<h4 id="4-数组、表（List、Set）："><a href="#4-数组、表（List、Set）：" class="headerlink" title="4. 数组、表（List、Set）："></a>4. 数组、表（List、Set）：</h4><p>用- 值表示数组中的一个元素</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">pets:</span></span><br><span class="line"><span class="bullet"> -</span> <span class="string">cat</span></span><br><span class="line"><span class="bullet"> -</span> <span class="string">dog</span></span><br><span class="line"><span class="bullet"> -</span> <span class="string">pig</span></span><br></pre></td></tr></table></figure>

<p>行内写法</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">pets:</span> <span class="string">[cat,dog,pig]</span></span><br></pre></td></tr></table></figure>

<h3 id="3-配置文件注入"><a href="#3-配置文件注入" class="headerlink" title="3. 配置文件注入"></a>3. 配置文件注入</h3><h4 id="1-ConfigurationProperties"><a href="#1-ConfigurationProperties" class="headerlink" title="1.@ConfigurationProperties"></a>1.@ConfigurationProperties</h4><p>配置文件：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">person:</span></span><br><span class="line"><span class="attr">    lastName:</span> <span class="string">hello</span></span><br><span class="line"><span class="attr">    age:</span> <span class="number">18</span></span><br><span class="line"><span class="attr">    boss:</span> <span class="literal">false</span></span><br><span class="line"><span class="attr">    birth:</span> <span class="number">2017</span><span class="string">/12/12</span></span><br><span class="line"><span class="attr">    maps:</span> <span class="string">&#123;k1:</span> <span class="string">v1,k2:</span> <span class="number">12</span><span class="string">&#125;</span></span><br><span class="line"><span class="attr">    lists:</span></span><br><span class="line"><span class="bullet">      -</span> <span class="string">lisi</span></span><br><span class="line"><span class="bullet">      -</span> <span class="string">zhaoliu</span></span><br><span class="line"><span class="attr">    dog:</span></span><br><span class="line"><span class="attr">      name:</span> <span class="string">小狗</span></span><br><span class="line"><span class="attr">      age:</span> <span class="number">12</span></span><br></pre></td></tr></table></figure>

<p>对应的Bean：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 将配置文件中配置的每一个属性的值，映射到这个组件中</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@ConfigurationProperties</span>：告诉SpringBoot将本类中的所有属性和配置文件中相关的配置进行绑定；</span></span><br><span class="line"><span class="comment"> *      prefix = "person"：配置文件中哪个下面的所有属性进行一一映射</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * 只有这个组件是容器中的组件，才能容器提供的<span class="doctag">@ConfigurationProperties</span>功能；</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@ConfigurationProperties</span>(prefix = <span class="string">"person"</span>)</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> String lastName;</span><br><span class="line">    <span class="keyword">private</span> Integer age;</span><br><span class="line">    <span class="keyword">private</span> Boolean boss;</span><br><span class="line">    <span class="keyword">private</span> Date birth;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Map&lt;String,Object&gt; maps;</span><br><span class="line">    <span class="keyword">private</span> List&lt;Object&gt; lists;</span><br><span class="line">    <span class="keyword">private</span> Dog dog;</span><br></pre></td></tr></table></figure>

<p>@ConfigurationProperties：告诉SpringBoot将本类中的所有属性和配置文件中相关的配置进行绑定  prefix = “person”：配置文件中哪个下面的所有属性进行一一映射 ，这样我们就明白了我们的 springboot 的配置文件的一些属性是绑定的我们的配置类的属性，那么我们的配置类就可以读取我们的配置文件里面的内容了！</p>
<h4 id="2-Value"><a href="#2-Value" class="headerlink" title="2.@Value"></a>2.@Value</h4><p>除了使用 <code>@ConfigurationProperties</code> 让我们的bean和配置文件绑定，我们还可以使用 @Value(“配置文件属性”) 注解去获取配置文件的内容。这个注解是支持spEL表达式的，这是他的优点。</p>
<h4 id="3-PropertySource"><a href="#3-PropertySource" class="headerlink" title="3.@PropertySource"></a>3.@PropertySource</h4><p>这个注解的作用就是用来导入一些指定的配置文件里面的内容到我们当前的 JavaBean 中，与之关联。也就是方便我们的配置文件的拆分。 比如： <code>@PropertySource(value={&quot;classpath:person.properties&quot;})</code> 就是加载类路径下面的  person.properties 里面的内容。</p>
<h4 id="4-ImportResource"><a href="#4-ImportResource" class="headerlink" title="4.@ImportResource"></a>4.@ImportResource</h4><p> 导入spring的xml配置文件然后让他生效。其实这个就是Spring的基本注解，导入配置文件配置类。这个东西要标注在主类上，而不是bean中。</p>
<p>注意，如果我们的配置文件和bean关联以后没有自动提示的话我们就需要导入pom ，在导入配置文件处理器以后，编写配置就有提示了。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--导入配置文件处理器，配置文件进行绑定就会有提示--&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-configuration-processor<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">			<span class="tag">&lt;<span class="name">optional</span>&gt;</span>true<span class="tag">&lt;/<span class="name">optional</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>一个小问题就是配置文件中配置的中文读出来会乱码，这主要是 idea 的 properties 文件默认采用的 utf-8 ，我们需要使用自动转 ascii 码。<br><img src="https://s1.ax2x.com/2018/04/13/N7KhX.png" alt></p>
<h3 id="4-配置类"><a href="#4-配置类" class="headerlink" title="4. 配置类"></a>4. 配置类</h3><p>SpringBoot推荐给容器中添加组件的方式，推荐使用全注解的方式</p>
<ol>
<li>配置类 <strong>@Configuration</strong> 等价于 Spring 配置文件</li>
<li>使用 <strong>@Bean</strong> 给容器中添加组件</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Configuration</span>：指明当前类是一个配置类；就是来替代之前的Spring配置文件</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * 在配置文件中用&lt;bean&gt;&lt;bean/&gt;标签添加组件</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyAppConfig</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//将方法的返回值添加到容器中；容器中这个组件默认的id就是方法名,也可以自己指定。</span></span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> HelloService <span class="title">helloService02</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"配置类@Bean给容器中添加组件了..."</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> HelloService();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-多环境配置文件"><a href="#5-多环境配置文件" class="headerlink" title="5. 多环境配置文件"></a>5. 多环境配置文件</h3><h4 id="1-多Profile文件"><a href="#1-多Profile文件" class="headerlink" title="1. 多Profile文件"></a>1. 多Profile文件</h4><p>我们在主配置文件编写的时候，文件名可以是<code>application-{profile}.properties/yml</code>默认使用<code>application.properties</code>的配置；</p>
<h4 id="2-文档块方式"><a href="#2-文档块方式" class="headerlink" title="2. 文档块方式"></a>2. 文档块方式</h4><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="attr">server:</span></span><br><span class="line"><span class="attr">  port:</span> <span class="number">8081</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">  profiles:</span></span><br><span class="line"><span class="attr">    active:</span> <span class="string">prod</span></span><br><span class="line"></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">server:</span></span><br><span class="line"><span class="attr">  port:</span> <span class="number">8083</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">  profiles:</span> <span class="string">dev</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"></span><br><span class="line"><span class="attr">server:</span></span><br><span class="line"><span class="attr">  port:</span> <span class="number">8084</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line"><span class="attr">  profiles:</span> <span class="string">prod</span>  <span class="comment">#指定属于哪个环境</span></span><br></pre></td></tr></table></figure>

<h4 id="3-激活指定profile"><a href="#3-激活指定profile" class="headerlink" title="3. 激活指定profile"></a>3. 激活指定profile</h4><ol>
<li>在配置文件中指定  spring.profiles.active=dev</li>
<li>命令行：<code>java -jar spring-boot-02-config-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev；</code>可以直接在测试的时候，配置传入命令行参数</li>
<li>虚拟机参数；<pre><code>`-Dspring.profiles.active=dev`</code></pre></li>
</ol>
<h4 id="4-指定外部配置文件"><a href="#4-指定外部配置文件" class="headerlink" title="4. 指定外部配置文件"></a>4. 指定外部配置文件</h4><p>所有的配置都可以在命令行上进行指定<br><code>java -jar spring-boot-02-config-02-0.0.1-SNAPSHOT.jar --server.port=8087  --server.context-path=/abc</code></p>
<h3 id="6-自动配置原理"><a href="#6-自动配置原理" class="headerlink" title="6. 自动配置原理"></a>6. 自动配置原理</h3><ol>
<li><p>SpringBoot启动的时候加载主配置类，开启了自动配置功能 <code>@EnableAutoConfiguration</code></p>
</li>
<li><p><strong>@EnableAutoConfiguration 作用：</strong></p>
</li>
</ol>
<ul>
<li><p>EnableAutoConfigurationImportSelector 给容器中导入一些组件</p>
</li>
<li><p>selectImports()方法中的 <code>List&lt;String&gt; configurations = getCandidateConfigurations(annotationMetadata,      attributes);</code> 获取候选的配置</p>
</li>
<li><p><code>SpringFactoriesLoader.loadFactoryNames()</code>扫描所有jar包类路径下 <code>META-INF/spring.factories</code> 把扫描到的这些文件的内容包装成 <code>properties</code> 对象，从 <code>properties</code> 中获取到 <code>EnableAutoConfiguration.class</code> 类（类名）对应的值，然后把他们添加在容器中</p>
</li>
</ul>
<p><strong>总之一句话将类路径下  META-INF/spring.factories 里面配置的所有EnableAutoConfiguration的值加入到了容器中</strong><br>如下：</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Auto Configure</span></span><br><span class="line"><span class="meta">org.springframework.boot.autoconfigure.EnableAutoConfiguration</span>=<span class="string">\</span></span><br><span class="line">org.springframework.boot.autoconfigure.admin.SpringApplicationAdminJmxAutoConfiguration,\</span><br><span class="line">org.springframework.boot.autoconfigure.aop.AopAutoConfiguration,\</span><br></pre></td></tr></table></figure>

<p>每一个这样的  xxxAutoConfiguration 类都是容器中的一个组件，都加入到容器中，用他们来做自动配置</p>
<h3 id="7-以HttpEncodingAutoConfiguration为例解释自动配置原理"><a href="#7-以HttpEncodingAutoConfiguration为例解释自动配置原理" class="headerlink" title="7.以HttpEncodingAutoConfiguration为例解释自动配置原理"></a>7.以HttpEncodingAutoConfiguration为例解释自动配置原理</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//表示这是一个配置类，以前编写的配置文件一样，也可以给容器中添加组件</span></span><br><span class="line"><span class="meta">@Configuration</span>   </span><br><span class="line"><span class="comment">//启动指定类的ConfigurationProperties功能；将配置文件中对应的值和HttpEncodingProperties绑定起来；并把HttpEncodingProperties加入到ioc容器中   所有能在我们的配置文件中配置什么就是看我们的 Properties 中的 prefix和属性有什么。</span></span><br><span class="line"><span class="meta">@EnableConfigurationProperties</span>(HttpEncodingProperties.class) </span><br><span class="line"></span><br><span class="line"><span class="comment">//Spring底层@Conditional注解，根据不同的条件，如果满足指定的条件，整个配置类里面的配置就会生效,判断当前应用是否是web应用，如果是，当前配置类生效@ConditionalOnWebApplication </span></span><br><span class="line"><span class="comment">//判断当前项目有没有这个类CharacterEncodingFilter,SpringMVC中进行乱码解决的过滤器；</span></span><br><span class="line"><span class="meta">@ConditionalOnClass</span>(CharacterEncodingFilter.class)  </span><br><span class="line"></span><br><span class="line"><span class="comment">//判断配置文件中是否存在某个配置  spring.http.encoding.enabled；如果不存在，判断也是成立的,也就是说即使我们配置文件中不配置pring.http.encoding.enabled=true，也是默认生效的；</span></span><br><span class="line"><span class="meta">@ConditionalOnProperty</span>(prefix = <span class="string">"spring.http.encoding"</span>, value = <span class="string">"enabled"</span>, matchIfMissing = <span class="keyword">true</span>)  </span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HttpEncodingAutoConfiguration</span> </span>&#123;</span><br><span class="line">  </span><br><span class="line">  	<span class="comment">//他已经和SpringBoot的配置文件映射了</span></span><br><span class="line">  	<span class="keyword">private</span> <span class="keyword">final</span> HttpEncodingProperties properties;</span><br><span class="line"></span><br><span class="line">   <span class="comment">//只有一个有参构造器的情况下，参数的值就会从容器中拿</span></span><br><span class="line">  	<span class="function"><span class="keyword">public</span> <span class="title">HttpEncodingAutoConfiguration</span><span class="params">(HttpEncodingProperties properties)</span> </span>&#123;</span><br><span class="line">		<span class="keyword">this</span>.properties = properties;</span><br><span class="line">	&#125;</span><br><span class="line">  </span><br><span class="line">    <span class="meta">@Bean</span>   <span class="comment">//给容器中添加一个组件，这个组件的某些值需要从properties中获取</span></span><br><span class="line">	<span class="meta">@ConditionalOnMissingBean</span>(CharacterEncodingFilter.class) <span class="comment">//判断容器没有这个组件？</span></span><br><span class="line">	<span class="function"><span class="keyword">public</span> CharacterEncodingFilter <span class="title">characterEncodingFilter</span><span class="params">()</span> </span>&#123;</span><br><span class="line">		CharacterEncodingFilter filter = <span class="keyword">new</span> OrderedCharacterEncodingFilter();</span><br><span class="line">		filter.setEncoding(<span class="keyword">this</span>.properties.getCharset().name());</span><br><span class="line">		filter.setForceRequestEncoding(<span class="keyword">this</span>.properties.shouldForce(Type.REQUEST));</span><br><span class="line">		filter.setForceResponseEncoding(<span class="keyword">this</span>.properties.shouldForce(Type.RESPONSE));</span><br><span class="line">		<span class="keyword">return</span> filter;</span><br><span class="line">	&#125;</span><br></pre></td></tr></table></figure>

<p>总结：</p>
<p><strong>1）、SpringBoot启动会加载大量的自动配置类</strong><br><strong>2）、我们看我们需要的功能有没有SpringBoot默认写好的自动配置类；</strong><br><strong>3）、我们再来看这个自动配置类中到底配置了哪些组件；（只要我们要用的组件有，我们就不需要再来配置</strong><br><strong>了）</strong><br><strong>4）、给容器中自动配置类添加组件的时候，会从properties类中获取某些属性。我们就可以在配置文件中指</strong><br><strong>定这些属性的值</strong></p>
<p>  根据当前不同的条件判断，决定这个配置类是否生效，一但这个配置类生效，这个配置类就会给容器中添加各种组件，这些组件的属性是从对应的properties类中获取的，这些类里面的每一个属性又是和配置文件绑定的。<br>&emsp;&emsp;  也就是如下两个类非常重要：</p>
<ul>
<li>xxxxAutoConfigurartion：自动配置类。</li>
<li>xxxxProperties: 封装配置文件中相关属性。</li>
</ul>
<h3 id="8-Conditional注解"><a href="#8-Conditional注解" class="headerlink" title="8.@Conditional注解"></a>8.@Conditional注解</h3><p>这个是Spring中原生的，就是用一个Class这个Class中重写了 match 方法，来判断这个conditional是否为真。然后我们的SpringBoot 是自己写了很多新的 @Conditional 的新版，就拓展了原来的功能。</p>
<ul>
<li>@ConditionalOnJava系统的java版本是否符合要求</li>
<li>@ConditionalOnBean容器中存在指定Bean；</li>
<li>@ConditionalOnMissingBean容器中不存在指定Bean；</li>
<li>@ConditionalOnExpression满足SpEL表达式指定</li>
<li>@ConditionalOnClass系统中有指定的类</li>
<li>@ConditionalOnMissingClass系统中没有指定的类</li>
<li>@ConditionalonSingleCandidate容器中只有一个指定的Bean，或者这个Bean是首选Bean</li>
<li>@ConditionalOnProperty系统中指定的属性是否有指定的值</li>
<li>@ConditionalOnResource类路径下是否存在指定资源文件</li>
<li>@ConditionalOnWebApplication 当前是web环境</li>
<li>@ConditionalOnNotWebApplication 当前不是web环境</li>
<li>@ConditionalOnjndiJNDI存在指定项</li>
</ul>
<p>也就是自动配置类是在<strong>一定条件下才会生效</strong>，不是说每一个自动配置类都会被加载！！我们可以开启 debug 模式然后我们就可以看到自动配置报告。使用 <code>debug:true</code> 在主配置文件中。下面就是自动配置报告！</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">=========================</span><br><span class="line">AUTO-CONF IGURATION REPORT</span><br><span class="line">=========================</span><br><span class="line"></span><br><span class="line">Positive matches:(自动配置类启用的）</span><br><span class="line"></span><br><span class="line">DispatcherServletAutoconfiguration matched:</span><br><span class="line">-@ConditionalonClass found required class</span><br><span class="line">‘org.springframework.web.servlet.Dispatcherservlet“；ConditionalonMissingclass did not find</span><br><span class="line">unwanted class (Onclasscondition)</span><br><span class="line">-@conditionalOnwebApplication(required)found StandardServletEnvironment</span><br><span class="line">(onwebApplicationcondition)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">Negative matches:(没有启动，没有匹配成功的自动配置类）</span><br><span class="line"></span><br><span class="line">ActiveMQAutoconfiguration:</span><br><span class="line">Did not match:</span><br><span class="line">-@ConditionalOnClass did not find required classes‘javax.jms.ConnectionFactory“，</span><br><span class="line">‘org.apache.activemq.ActivemQConnectionFactory“(Onclasscondition)</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>Shiro安全框架:认证</title>
    <url>/2018/04/13/Shiro/Shiro%20%E8%AE%A4%E8%AF%81%E6%A1%86%E6%9E%B6/</url>
    <content><![CDATA[<h1 id="Shiro-安全框架"><a href="#Shiro-安全框架" class="headerlink" title="Shiro 安全框架"></a>Shiro 安全框架</h1><h2 id="1-认证"><a href="#1-认证" class="headerlink" title="1. 认证"></a>1. 认证</h2><h3 id="1-采用简单的对象登陆认证-SimpleAccountRealm"><a href="#1-采用简单的对象登陆认证-SimpleAccountRealm" class="headerlink" title="1. 采用简单的对象登陆认证(SimpleAccountRealm)"></a>1. 采用简单的对象登陆认证(SimpleAccountRealm)</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AuthenticationTest</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 创建一个简单的认证 realm 也就是认证信息存放在对象中的</span></span><br><span class="line">    SimpleAccountRealm simpleAccountRealm = <span class="keyword">new</span> SimpleAccountRealm();</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Before</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addUser</span><span class="params">()</span></span>&#123;</span><br><span class="line">        simpleAccountRealm.addAccount(<span class="string">"lwen"</span>, <span class="string">"1234"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">authentication</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">// 创建一个 securityManager 环境</span></span><br><span class="line">        SecurityManager securityManager = <span class="keyword">new</span> DefaultSecurityManager(simpleAccountRealm);</span><br><span class="line">        <span class="comment">// 设置环境</span></span><br><span class="line">        SecurityUtils.setSecurityManager(securityManager);</span><br><span class="line">        <span class="comment">// 获取认证主体</span></span><br><span class="line">        Subject subject = SecurityUtils.getSubject();</span><br><span class="line">        UsernamePasswordToken token = <span class="keyword">new</span> UsernamePasswordToken(<span class="string">"lwen"</span>, <span class="string">"1234"</span>);</span><br><span class="line">        subject.login(token);</span><br><span class="line">        System.out.println(subject.isAuthenticated());</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<a id="more"></a>

<h3 id="2-简单的角色认证"><a href="#2-简单的角色认证" class="headerlink" title="2. 简单的角色认证"></a>2. 简单的角色认证</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AuthorizationTest</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 创建一个简单的认证 realm 也就是认证信息存放在对象中的</span></span><br><span class="line">    SimpleAccountRealm simpleAccountRealm = <span class="keyword">new</span> SimpleAccountRealm();</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Before</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addUser</span><span class="params">()</span></span>&#123;</span><br><span class="line">        simpleAccountRealm.addAccount(<span class="string">"lwen"</span>, <span class="string">"1234"</span>,<span class="string">"user"</span>,<span class="string">"admin"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">authentication</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">// 创建一个 securityManager 环境</span></span><br><span class="line">        SecurityManager securityManager = <span class="keyword">new</span> DefaultSecurityManager(simpleAccountRealm);</span><br><span class="line">        <span class="comment">// 设置环境</span></span><br><span class="line">        SecurityUtils.setSecurityManager(securityManager);</span><br><span class="line">        <span class="comment">// 获取认证主体</span></span><br><span class="line">        Subject subject = SecurityUtils.getSubject();</span><br><span class="line">        UsernamePasswordToken token = <span class="keyword">new</span> UsernamePasswordToken(<span class="string">"lwen"</span>, <span class="string">"1234"</span>);</span><br><span class="line">        subject.login(token);</span><br><span class="line">        subject.checkRole(<span class="string">"user"</span>);</span><br><span class="line">        subject.checkRoles(<span class="string">"user"</span>, <span class="string">"admin"</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-内置-Realm-和自定义-Realm"><a href="#2-内置-Realm-和自定义-Realm" class="headerlink" title="2. 内置 Realm 和自定义 Realm"></a>2. 内置 Realm 和自定义 Realm</h2><h3 id="1-配置文件-initRealm"><a href="#1-配置文件-initRealm" class="headerlink" title="1. 配置文件 (initRealm)"></a>1. 配置文件 (initRealm)</h3><p>创建一个配置文件，如下</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="section">[users]</span></span><br><span class="line"><span class="attr">lwen</span>=<span class="number">1234</span>,admin</span><br><span class="line"><span class="attr">mark</span>=<span class="number">1234</span>,user</span><br><span class="line"><span class="section">[roles]</span></span><br><span class="line"><span class="attr">admin</span>=update,delete,select</span><br><span class="line"><span class="attr">user</span>=select</span><br></pre></td></tr></table></figure>

<p>在代码中使用 IniRealm 即可。可用下面的代码检查用户权限。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">subject.checkPermission(<span class="string">"update"</span>);</span><br><span class="line">subject.checkPermission(<span class="string">"select"</span>);</span><br></pre></td></tr></table></figure>

<h3 id="2-数据库-JdbcRealm"><a href="#2-数据库-JdbcRealm" class="headerlink" title="2. 数据库(JdbcRealm)"></a>2. 数据库(JdbcRealm)</h3><p>首先需要创建表，这里我们采用了默认的查询方式，但是这种查询方式是不可用的，这里只做演示用。后面会说到关于自定义 Realm ，会详细说数据库的设计。</p>
<p>这里我们采用了 Jdbc 默认的 sql 语句建表。</p>
<p><img src="https://user-images.githubusercontent.com/22151420/39239770-8314f4dc-48b4-11e8-9293-112ce08f712e.png" alt="default"></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AuthenticationJdbcRealmTest</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 创建一个简单的认证 realm 也就是认证信息存放在对象中的</span></span><br><span class="line">    JdbcRealm jdbcRealm = <span class="keyword">new</span> JdbcRealm();</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">authentication</span><span class="params">()</span></span>&#123;</span><br><span class="line">        DruidDataSource dataSource = <span class="keyword">new</span> DruidDataSource();</span><br><span class="line">        dataSource.setUrl(<span class="string">"jdbc:mysql://127.0.0.1/shiro"</span>);</span><br><span class="line">        dataSource.setUsername(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">"12345678"</span>);</span><br><span class="line">        jdbcRealm.setDataSource(dataSource);</span><br><span class="line">        jdbcRealm.setPermissionsLookupEnabled(<span class="keyword">true</span>);<span class="comment">//这是一个坑 ，默认权限表不进行查找</span></span><br><span class="line">        <span class="comment">// 创建一个 securityManager 环境</span></span><br><span class="line">        SecurityManager securityManager = <span class="keyword">new</span> DefaultSecurityManager(jdbcRealm);</span><br><span class="line">        <span class="comment">// 设置环境</span></span><br><span class="line">        SecurityUtils.setSecurityManager(securityManager);</span><br><span class="line">        <span class="comment">// 获取认证主体</span></span><br><span class="line">        Subject subject = SecurityUtils.getSubject();</span><br><span class="line">        UsernamePasswordToken token = <span class="keyword">new</span> UsernamePasswordToken(<span class="string">"lwen"</span>, <span class="string">"1234"</span>);</span><br><span class="line">        subject.login(token);</span><br><span class="line">        System.out.println(subject.isAuthenticated());</span><br><span class="line">        subject.checkPermission(<span class="string">"update"</span>);</span><br><span class="line">        subject.checkPermission(<span class="string">"select"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>注意这里有一个坑，就是默认的情况下 Jdbc 不回去查询权限表，导致失败。所以我们需要添加上 <code>setPermissionsLookupEnabled</code> 让他开启。</p>
<h3 id="3-自定义-Realm"><a href="#3-自定义-Realm" class="headerlink" title="3. 自定义 Realm"></a>3. 自定义 Realm</h3><p>自定义 Realm 就是说我们只要继承一个 <code>AuthorizingRealm</code> 类，然后重写里面的认证和授权的方法就可以了，非常的简单。一个是认证的方法也就是 <code>doGetAuthenticationInfo</code> 另外一个就是 授权的方法 <code>doGetAuthorizationInfo</code> ，里很多操作这里采用了一个静态数据表示的，实际上这些是需要从数据库或者缓存中取出来然后进行逻辑判断。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CustomRealm</span> <span class="keyword">extends</span> <span class="title">AuthorizingRealm</span> </span>&#123;</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">this</span>.setName(<span class="string">"customRealm"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 授权  -&gt; 权限 角色</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> AuthorizationInfo <span class="title">doGetAuthorizationInfo</span><span class="params">(PrincipalCollection principalCollection)</span> </span>&#123;</span><br><span class="line">        String userName = (String) principalCollection.getPrimaryPrincipal();</span><br><span class="line">        <span class="comment">// 这个需要使用 service 层然后调用 dao 层去数据库或者缓存中查询，然后返回结果这里为了方便采用静态数据</span></span><br><span class="line">        Set&lt;String&gt; roles = getRolesByUsername(userName);</span><br><span class="line">        Set&lt;String&gt; permissions = getPermissionsByUsername(userName);</span><br><span class="line">        SimpleAuthorizationInfo authorizationInfo = <span class="keyword">new</span> SimpleAuthorizationInfo(roles);</span><br><span class="line">        authorizationInfo.setStringPermissions(permissions);</span><br><span class="line">        <span class="keyword">return</span> authorizationInfo;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// 认证</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> AuthenticationInfo <span class="title">doGetAuthenticationInfo</span><span class="params">(AuthenticationToken authenticationToken)</span> <span class="keyword">throws</span> AuthenticationException </span>&#123;</span><br><span class="line">        String userName = (String) authenticationToken.getPrincipal();</span><br><span class="line">        String password = getPasswordByUsername(userName);</span><br><span class="line">        <span class="keyword">if</span> (password != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> SimpleAuthenticationInfo(userName, password,<span class="string">"customRealm"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> String <span class="title">getPasswordByUsername</span><span class="params">(String userName)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"1234"</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> Set&lt;String&gt; <span class="title">getPermissionsByUsername</span><span class="params">(String userName)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> HashSet&lt;&gt;(Arrays.asList(<span class="string">"select"</span>, <span class="string">"delete"</span>, <span class="string">"update"</span>));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> Set&lt;String&gt; <span class="title">getRolesByUsername</span><span class="params">(String userName)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> HashSet&lt;&gt;(Arrays.asList(<span class="string">"admin"</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-密码加密加盐"><a href="#4-密码加密加盐" class="headerlink" title="4. 密码加密加盐"></a>4. 密码加密加盐</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line">HashedCredentialsMatcher matcher = <span class="keyword">new</span> HashedCredentialsMatcher();</span><br><span class="line">matcher.setHashAlgorithmName(<span class="string">"md5"</span>);</span><br><span class="line">customRealm.setCredentialsMatcher(matcher);</span><br></pre></td></tr></table></figure>

<p>在代码逻辑中加上这段，然后在 Relam 中需要自定义盐。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 加盐（lwen）</span></span><br><span class="line">authenticationInfo.setCredentialsSalt(ByteSource.Util.bytes(<span class="string">"lwen"</span>));</span><br></pre></td></tr></table></figure>

<h2 id="2-整合-Spring"><a href="#2-整合-Spring" class="headerlink" title="2. 整合 Spring"></a>2. 整合 Spring</h2><h3 id="1-首先配置过滤器"><a href="#1-首先配置过滤器" class="headerlink" title="1. 首先配置过滤器"></a>1. 首先配置过滤器</h3><p>也就是类似于其他的登陆授权方式都是通过拦截器或者过滤器来完成登陆的，这里我们也是这样的思路。在 web.xml 中配置过滤器，过滤所有请求，交给 shiro 处理。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--shiro 配置--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">filter</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>shiroFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-class</span>&gt;</span>org.springframework.web.filter.DelegatingFilterProxy<span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">init-param</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param-name</span>&gt;</span>targetFilterLifecycle<span class="tag">&lt;/<span class="name">param-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">param-value</span>&gt;</span>true<span class="tag">&lt;/<span class="name">param-value</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">init-param</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>shiroFilter<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-配置-beans"><a href="#2-配置-beans" class="headerlink" title="2. 配置 beans"></a>2. 配置 beans</h3><p>以前我们手动创建的 securityManager 对象以及 Realm 和 加密函数对象现在我们都交给 Spring 管理，进行自动的依赖注入和管理。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.apache.shiro.spring.web.ShiroFilterFactoryBean"</span> <span class="attr">id</span>=<span class="string">"shiroFilter"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"securityManager"</span> <span class="attr">ref</span>=<span class="string">"securityManager"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"loginUrl"</span> <span class="attr">value</span>=<span class="string">"login.html"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"unauthorizedUrl"</span> <span class="attr">value</span>=<span class="string">"403.html"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"filterChainDefinitions"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">value</span>&gt;</span></span><br><span class="line">            /login.html=anon</span><br><span class="line">            /subLogin=anon</span><br><span class="line">            /*=authc</span><br><span class="line">        <span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.apache.shiro.web.mgt.DefaultWebSecurityManager"</span> <span class="attr">id</span>=<span class="string">"securityManager"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"realm"</span> <span class="attr">ref</span>=<span class="string">"customRealm"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"realms.CustomRealm"</span> <span class="attr">id</span>=<span class="string">"customRealm"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"credentialsMatcher"</span> <span class="attr">ref</span>=<span class="string">"matcher"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.apache.shiro.authc.credential.HashedCredentialsMatcher"</span> <span class="attr">id</span>=<span class="string">"matcher"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"hashAlgorithmName"</span> <span class="attr">value</span>=<span class="string">"md5"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"hashIterations"</span> <span class="attr">value</span>=<span class="string">"1"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>可以看到上面我们配置了登录页的 url 和未认证页，最后比较重要的就是我们是否需要认证的页面的配置。我们需要把登录页，和登录数据提交页给排除掉，因为如果有这些的页面的话我们将会进入死循环中，没办法进行登录操作。其中 anon 就是无需认证的，而后面 authc 需要认证，这个东西是按照顺序来的。所以 /* 放在最后面匹配。</p>
<h3 id="3-编写-controller-进行认证"><a href="#3-编写-controller-进行认证" class="headerlink" title="3. 编写 controller 进行认证"></a>3. 编写 controller 进行认证</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IndexController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@RequestMapping</span>(value = <span class="string">"/subLogin"</span>, method = RequestMethod.POST,produces = <span class="string">"application/json;charset=utf-8"</span>)</span><br><span class="line">    <span class="meta">@ResponseBody</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">login</span><span class="params">(User user)</span></span>&#123;</span><br><span class="line">        Subject subject = SecurityUtils.getSubject();</span><br><span class="line">        UsernamePasswordToken token = <span class="keyword">new</span> UsernamePasswordToken(user.getUsername(), user.getPassword());</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            subject.login(token);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (AuthenticationException e) &#123;</span><br><span class="line">            <span class="keyword">return</span> e.getMessage();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"登陆成功！"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>认证没有抛出异常的话我们就算认证成功了，否则的话就是认证失败。</p>
<h3 id="4-角色和权限认证"><a href="#4-角色和权限认证" class="headerlink" title="4. 角色和权限认证"></a>4. 角色和权限认证</h3><p>首先需要导入 aop 的注解，然后我们进行授权操作的时候只需要进行写一个注解就行了，当然也可以直接编码实现。</p>
<p>导入注解 jar 配置注解springmvc 中：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!--开启 aop--&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">aop:config</span> <span class="attr">proxy-target-class</span>=<span class="string">"true"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.apache.shiro.spring.security.interceptor.AuthorizationAttributeSourceAdvisor"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"securityManager"</span> <span class="attr">ref</span>=<span class="string">"securityManager"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.apache.shiro.spring.LifecycleBeanPostProcessor"</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.aspectj<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>aspectjweaver<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.8.11<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RequiresRoles</span>(<span class="string">"admin"</span>)</span><br><span class="line"><span class="meta">@GetMapping</span>(<span class="string">"/testRole"</span>)</span><br><span class="line"><span class="meta">@ResponseBody</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testRole</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"testRole"</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">@RequiresPermissions</span>(<span class="string">"select"</span>)</span><br><span class="line"><span class="meta">@GetMapping</span>(<span class="string">"/testPermission"</span>)</span><br><span class="line"><span class="meta">@ResponseBody</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> String <span class="title">testPermission</span><span class="params">()</span></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="string">"has"</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-自动登录"><a href="#3-自动登录" class="headerlink" title="3. 自动登录"></a>3. 自动登录</h2><p>要创建一个 cookie 对象，然后在自动登录管理器中注入 cookie ，接着需要传递给 securityManager。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.apache.shiro.web.mgt.CookieRememberMeManager"</span> <span class="attr">id</span>=<span class="string">"rememberMeManager"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"cookie"</span> <span class="attr">ref</span>=<span class="string">"cookie"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.apache.shiro.web.servlet.SimpleCookie"</span> <span class="attr">id</span>=<span class="string">"cookie"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">constructor-arg</span> <span class="attr">value</span>=<span class="string">"rememberMe"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"maxAge"</span> <span class="attr">value</span>=<span class="string">"200000"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">class</span>=<span class="string">"org.apache.shiro.web.mgt.DefaultWebSecurityManager"</span> <span class="attr">id</span>=<span class="string">"securityManager"</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"realm"</span> <span class="attr">ref</span>=<span class="string">"customRealm"</span>/&gt;</span></span><br><span class="line">	<span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"rememberMeManager"</span> <span class="attr">ref</span>=<span class="string">"rememberMeManager"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>接着只需要在 controller 的登陆方法中进行判断就行了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 设置记住我</span></span><br><span class="line">token.setRememberMe(user.isRememberMe());</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Shiro</category>
      </categories>
      <tags>
        <tag>Shiro</tag>
      </tags>
  </entry>
  <entry>
    <title>SpringBoot笔记(一):启动流程</title>
    <url>/2018/04/13/SpringBoot/SpringBoot%20%E7%AC%94%E8%AE%B0%20(%20%E4%B8%80%20)%EF%BC%9A%E5%90%AF%E5%8A%A8%E6%B5%81%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="SpringBoot-笔记-一-启动流程"><a href="#SpringBoot-笔记-一-启动流程" class="headerlink" title="SpringBoot 笔记(一): 启动流程"></a>SpringBoot 笔记(一): 启动流程</h1><h2 id="1-配置开发环境"><a href="#1-配置开发环境" class="headerlink" title="1. 配置开发环境"></a>1. 配置开发环境</h2><h3 id="1-创建-Maven-项目"><a href="#1-创建-Maven-项目" class="headerlink" title="1. 创建 Maven 项目"></a>1. 创建 Maven 项目</h3><p>然后我们首先在项目里面加上编译环境，防止每一次更新 Maven 的时候导致项目的语言级别自动被改成 Java5 然后导致编译不通过的问题。</p>
<a id="more"></a>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">profile</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">id</span>&gt;</span>jdk‐1.8<span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">activation</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">activeByDefault</span>&gt;</span>true<span class="tag">&lt;/<span class="name">activeByDefault</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">jdk</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">jdk</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">activation</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">maven.compiler.source</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">maven.compiler.source</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">maven.compiler.target</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">maven.compiler.target</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">maven.compiler.compilerVersion</span>&gt;</span>1.8<span class="tag">&lt;/<span class="name">maven.compiler.compilerVersion</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">profile</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-Maven-中添加依赖"><a href="#2-Maven-中添加依赖" class="headerlink" title="2. Maven 中添加依赖"></a>2. Maven 中添加依赖</h3><p>&emsp;&emsp;  一开始加上最基础的依赖就是 parent 父项目和 web 的 starter 。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-parent<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">version</span>&gt;</span>1.5.9.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;其实很明显父工程的作用就是用来版本控制的，为什么这么说？我们跟踪一下父工程，来到父工程的 pox.xml 看到如下的配置。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">parent</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-dependencies<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">version</span>&gt;</span>1.5.9.RELEASE<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">relativePath</span>&gt;</span>../../spring-boot-dependencies<span class="tag">&lt;/<span class="name">relativePath</span>&gt;</span></span><br><span class="line">	<span class="tag">&lt;/<span class="name">parent</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  继续跟踪这个父工程可以看到以下内容。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">properties</span>&gt;</span></span><br><span class="line">		<span class="comment">&lt;!-- Dependency versions --&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">activemq.version</span>&gt;</span>5.14.5<span class="tag">&lt;/<span class="name">activemq.version</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">antlr2.version</span>&gt;</span>2.7.7<span class="tag">&lt;/<span class="name">antlr2.version</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">appengine-sdk.version</span>&gt;</span>1.9.59<span class="tag">&lt;/<span class="name">appengine-sdk.version</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">artemis.version</span>&gt;</span>1.5.5<span class="tag">&lt;/<span class="name">artemis.version</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">aspectj.version</span>&gt;</span>1.8.13<span class="tag">&lt;/<span class="name">aspectj.version</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">assertj.version</span>&gt;</span>2.6.0<span class="tag">&lt;/<span class="name">assertj.version</span>&gt;</span></span><br><span class="line">		<span class="tag">&lt;<span class="name">atomikos.version</span>&gt;</span>3.9.3<span class="tag">&lt;/<span class="name">atomikos.version</span>&gt;</span></span><br><span class="line">		....</span><br><span class="line">		....</span><br><span class="line"><span class="tag">&lt;/<span class="name">properties</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  好的，现在终于算是找到源头了，这些 properties 就是用来规定 springboot 整合的这些框架的版本，所以说我们只用管好 <code>springboot</code> 的版本，对于里面封装的各个组件我们不用操心，他都帮我们配置好了。但是我们也是可以自己继续进行定制的，后面会说到相关的东西。</p>
<p>&emsp;&emsp;  可以看到里面添加的另外一个 starter 就是 web 的 starter ，其实在 springboot 中的 starter 的命名很规范一眼就能看出是干嘛的。什么是 starter ？ 简单来说就是 springboot 用来整合各个框架的包，我们需要什么框架什么功能就添加上这些对应的 starter 在 springboot 中就可以享受开箱即用的快感了！</p>
<h3 id="3-编写主类"><a href="#3-编写主类" class="headerlink" title="3. 编写主类"></a>3. 编写主类</h3><p>&emsp;&emsp;  做完了这些最简单的配置，我们就可以开始写启动代码了，代码非常简单只有几行。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="comment">// @SpringBootApplication 来标注一个主程序类</span></span><br><span class="line"><span class="meta">@SpringBootApplication</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MainApplication</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// Spring启动！</span></span><br><span class="line">SpringApplication.run(MainApplication.class,args);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  很神奇这样我们就写好了一个完全可以跑起来的 springboot 应用，我们只是写了一个注解和一行代码而已，为什么会这么简单，让我们看看 <code>@SpringBootApplication</code> 注解。</p>
<h4 id="1-SpringBootApplication-注解"><a href="#1-SpringBootApplication-注解" class="headerlink" title="1. @SpringBootApplication 注解"></a>1. @SpringBootApplication 注解</h4><p>&emsp;&emsp;  首先可以看到的是这是一个组合注解，里面有如下内容：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@SpringBootConfiguration</span></span><br><span class="line"><span class="meta">@EnableAutoConfiguration</span></span><br><span class="line"><span class="meta">@ComponentScan</span>(excludeFilters = &#123;</span><br><span class="line">		<span class="meta">@Filter</span>(type = FilterType.CUSTOM, classes = TypeExcludeFilter.class),</span><br><span class="line">		<span class="meta">@Filter</span>(type = FilterType.CUSTOM, classes = AutoConfigurationExcludeFilter.class) &#125;)</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  好一个个的来看:</p>
<ol>
<li><code>@SpringBootConfiguration</code> 这个注解就是标注这是一个 <code>springboot</code> 的配置类，也就是可以进行 <code>springboot</code> 的配置，配置类就等于配置文件，我们要有这个等价的概念。然后我们在打开这个注解发现里面就是 <code>spring</code>的一个 Java Config 注解 <code>@Configuration</code> 所以说这个实际上是 <code>SpringBoot</code> 的主配置类。</li>
<li><code>@EnableAutoConfiguration</code> 他是用来开启自动配置，也就就是 <code>springboot</code> 的核心，其实最终起作用的就是 <code>spring-boot-configuration-processor-1.5.9.RELEASE.jar</code> 这个包在起作用，后面会分析到。看看这个注解里面的内容！<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@AutoConfigurationPackage</span></span><br><span class="line"><span class="meta">@Import</span>(EnableAutoConfigurationImportSelector.class)</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>两个注解:</p>
<h5 id="1-AutoConfigurationPackage"><a href="#1-AutoConfigurationPackage" class="headerlink" title="1.@AutoConfigurationPackage"></a>1.@AutoConfigurationPackage</h5><p>第一个是自动导组件的注解，底层调用了Spring 底层注解(@Import)给容器中导入一个组件。也就是 <code>@Import(AutoConfigurationPackages.Registrar.class)</code> 可以看到他导入了一个自动配置包的注册器。Import 是Spring 的一个底层的注解，就是用来导入组件。然后我来查看这个组件到底做了什么：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Order</span>(Ordered.HIGHEST_PRECEDENCE)</span><br><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Registrar</span> <span class="keyword">implements</span> <span class="title">ImportBeanDefinitionRegistrar</span>, <span class="title">DeterminableImports</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Override</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">registerBeanDefinitions</span><span class="params">(AnnotationMetadata metadata,</span></span></span><br><span class="line"><span class="function"><span class="params">         BeanDefinitionRegistry registry)</span> </span>&#123;</span><br><span class="line">      register(registry, <span class="keyword">new</span> PackageImport(metadata).getPackageName()); <span class="comment">//获取元数据中的包信息，然后导入这些包里面的组件 核心！！！！</span></span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="meta">@Override</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> Set&lt;Object&gt; <span class="title">determineImports</span><span class="params">(AnnotationMetadata metadata)</span> </span>&#123;</span><br><span class="line">      <span class="keyword">return</span> Collections.&lt;Object&gt;singleton(<span class="keyword">new</span> PackageImport(metadata));</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    如果在上面的那个注册组件的位置打上断点的话，我们就会发现这个地方计算的报名就是我们主类所在的包的包名，也 就是需要导入我们主类所在的包以及子包所对应的组件，我们自己的组件。所以说我们必须把其他的类放到与主类平行的其他包中，否则没办法加载这些类到容器里。</p>
<h5 id="2-Import-EnableAutoConfigurationImportSelector-class"><a href="#2-Import-EnableAutoConfigurationImportSelector-class" class="headerlink" title="2.@Import(EnableAutoConfigurationImportSelector.class)"></a>2.@Import(EnableAutoConfigurationImportSelector.class)</h5><p>第二个就是导入一个 Seletor 组件，先看一下这个组件的代码，里面很简单就是一个方法，但是我们发现他是继承了一个类的所以说我们需要看看父类的方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EnableAutoConfigurationImportSelector</span>  <span class="keyword">extends</span> <span class="title">AutoConfigurationImportSelector</span></span>&#123;</span><br><span class="line">          <span class="comment">//..........</span></span><br><span class="line">      &#125;</span><br></pre></td></tr></table></figure>

<p>然后我们看到父类中的一个非常重要的方法， <code>selectImports</code> 这个方法就完成了我们的自动配置！</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> String[] selectImports(AnnotationMetadata annotationMetadata) &#123;</span><br><span class="line">   <span class="keyword">if</span> (!isEnabled(annotationMetadata)) &#123;</span><br><span class="line">      <span class="keyword">return</span> NO_IMPORTS;</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">try</span> &#123;</span><br><span class="line">      AutoConfigurationMetadata autoConfigurationMetadata = AutoConfigurationMetadataLoader</span><br><span class="line">            .loadMetadata(<span class="keyword">this</span>.beanClassLoader);</span><br><span class="line">      AnnotationAttributes attributes = getAttributes(annotationMetadata);</span><br><span class="line">      List&lt;String&gt; configurations = getCandidateConfigurations(annotationMetadata,</span><br><span class="line">            attributes);</span><br><span class="line">      configurations = removeDuplicates(configurations);</span><br><span class="line">      configurations = sort(configurations, autoConfigurationMetadata);</span><br><span class="line">      Set&lt;String&gt; exclusions = getExclusions(annotationMetadata, attributes);</span><br><span class="line">      checkExcludedClasses(configurations, exclusions);</span><br><span class="line">      configurations.removeAll(exclusions);</span><br><span class="line">      configurations = filter(configurations, autoConfigurationMetadata);</span><br><span class="line">      fireAutoConfigurationImportEvents(configurations, exclusions);</span><br><span class="line">      <span class="keyword">return</span> configurations.toArray(<span class="keyword">new</span> String[configurations.size()]);</span><br><span class="line">   &#125;</span><br><span class="line">   <span class="keyword">catch</span> (IOException ex) &#123;</span><br><span class="line">      <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(ex);</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看到里面有个 configurations 变量这个就是我们的自动配置的一些包。可以打断点看到里面的内容。</p>
<p>这个组件底层就是各种 beanFactory 也就是我们 Spring 底层的 Ioc 容器，然后就是向容器里面导入我们的自动配置类，<code>xxxAutoConfiguration</code> 这种类，也就是 <code>spring-boot-configuration-processor-1.5.9.RELEASE.jar</code> 包里面的内容。</p>
<p>那么具体把这个包里面的哪些 <code>xxxAutoConfiguration</code> 加入到容器里面呢？ 这就需要看一个方法。在这个 Selector 的底层有一个 <code>getCandidateConfigurations</code> 方法，这个方法就是去扫描所有包下面的 <code>META-INF/spring.factories</code> 文件，然后这些文件中的 <code>EnableAutoConfiguratio</code> 属性就会有需要配置组件要导入哪些 <code>xxxAutoConfiguration</code> 类对这个组件进行自动配置。可以看一下<code>spring-boot-configuration-processor-1.5.9.RELEASE.jar</code> 里面的具体内容：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">org.springframework.boot.test.autoconfigure.orm.jpa.AutoConfigureDataJpa=\</span><br><span class="line">org.springframework.boot.autoconfigure.data.jpa.JpaRepositoriesAutoConfiguration,\</span><br><span class="line">org.springframework.boot.autoconfigure.flyway.FlywayAutoConfiguration,\</span><br><span class="line">org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,\</span><br><span class="line">org.springframework.boot.autoconfigure.jdbc.DataSourceTransactionManagerAutoConfiguration,\</span><br><span class="line">org.springframework.boot.autoconfigure.jdbc.JdbcTemplateAutoConfiguration,\</span><br><span class="line">org.springframework.boot.autoconfigure.liquibase.LiquibaseAutoConfiguration,\</span><br><span class="line">org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration,\</span><br><span class="line">org.springframework.boot.autoconfigure.transaction.TransactionAutoConfiguration</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  可以看到这就是他的自动配置机制！</p>
<h4 id="2-run-方法"><a href="#2-run-方法" class="headerlink" title="2. run 方法"></a>2. run 方法</h4><p>在这个方法的底层 new 了一个 <code>SpringApplication</code> 的东西，这个很明显就是真正的 <code>springboot</code> 的启动类了。最后调用到了 <code>public ConfigurableApplicationContext run(String... args)</code> 这里面做了一些列的启动操作，就不具体看了。然后我们传入的那个类被放到了 <code>sources = new LinkedHashSet();</code> 这个数据结构里面。感觉没怎么被使用。</p>
<p>&emsp;&emsp;  这样其实我们的 springboot 应用已经可以开始跑起来了。但是我们不能访问任何页面，主要是因为我们没有 controller ！好的接着编写一个简单的 controller。</p>
<h3 id="4-controller-编写"><a href="#4-controller-编写" class="headerlink" title="4. controller 编写"></a>4. controller 编写</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Controller</span> <span class="comment">//标注他是 controller</span></span><br><span class="line"><span class="meta">@ResponseBody</span> <span class="comment">//能够返回数据</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@GetMapping</span>(<span class="string">"/hello"</span>) <span class="comment">//url映射</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">hello</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Hello World!"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  @Controller 、@ResponseBody 这两个注解我们可以合并采用 @RestController 这个其实就是上面两个注解的组合注解。然后下面的那个 @GetMapping 就是采用的 get 方式请求时候的映射，然后对应的还有 post、put、delete 等等，这些都是 restful 接口中有规定的在何种情况使用何种请求方式。</p>
<p>&emsp;&emsp;  此时我们去访问 <code>localhost:8080/hello</code> 就会显示 “Hello World！” 了！</p>
<h3 id="5-打包部署"><a href="#5-打包部署" class="headerlink" title="5. 打包部署"></a>5. 打包部署</h3><p>&emsp;&emsp;  这个在 springboot 中打包部署非常容易就是采用一个 Maven 插件我们就可以打包成 jar 而非 war 。那么也就是说我们可以直接使用 <code>java -jar</code> 命令来运行这个 jar 包。等等 ！难道不需要 tomcat 之类的 web 容器吗？ 对的！因为<code>springboot</code> 是内嵌了 tomcat 所以我们根本没有部署到 web 容器这一说，所以说部署简洁。那么我们需要加入打包插件。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">build</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">plugins</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">plugin</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-maven-plugin<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">plugin</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">plugins</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">build</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  接下来只需要在 Maven 中执行 package 命令就可以在 target 目录生成 jar 包。并且我们可以使用 <code>java -jar</code> 来执行这个 jar 。</p>
<h3 id="6-resource-目录结构"><a href="#6-resource-目录结构" class="headerlink" title="6. resource 目录结构"></a>6. resource 目录结构</h3><p>resources文件夹中目录结构：</p>
<ul>
<li>static：保存所有的静态资源如 js css  images。</li>
<li>templates：保存所有的模板页面 html 模板。</li>
<li>application.properties：Spring Boot应用的配置文件。</li>
</ul>
]]></content>
      <categories>
        <category>SpringBoot</category>
      </categories>
      <tags>
        <tag>SpringBoot</tag>
      </tags>
  </entry>
  <entry>
    <title>Bootstrap 源码分析</title>
    <url>/2018/04/07/Netty/Bootstrap%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="Netty-源码分析-Bootstrap"><a href="#Netty-源码分析-Bootstrap" class="headerlink" title="Netty 源码分析: Bootstrap"></a>Netty 源码分析: Bootstrap</h1><h2 id="1-结构"><a href="#1-结构" class="headerlink" title="1. 结构"></a>1. 结构</h2><p>先看一个这个类的类层次结构，<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-4-8/42956996.jpg" alt><br>好，这个结构还是比较明晰的，然后看他的主要字段，因为这些字段比较重要，在后面的代码分析中是用的上的。</p>
<a id="more"></a>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// options 选项</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Map&lt;ChannelOption&lt;?&gt;, Object&gt; childOptions = <span class="keyword">new</span> LinkedHashMap&lt;ChannelOption&lt;?&gt;, Object&gt;();</span><br><span class="line"><span class="comment">// 属性</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Map&lt;AttributeKey&lt;?&gt;, Object&gt; childAttrs = <span class="keyword">new</span> LinkedHashMap&lt;AttributeKey&lt;?&gt;, Object&gt;();</span><br><span class="line"><span class="comment">// worker 线程池，为啥没有 boss ？ 待会说</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> EventLoopGroup childGroup;</span><br><span class="line"><span class="comment">// handler</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> ChannelHandler childHandler;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  可以看到很多参数就是在我们进行 bootstrap 配置的时候进行设置的。但是我们注意到我们一般配置线程池的时候是有一个 boss 一个 worker ，但是这里只有一个引用是怎么回事？接着我们看一下构造方法。有一行注释 “ Specify the {@link EventLoopGroup} which is used for the parent (acceptor) and the child (client).” 这个意思就是说这里把线程池分为了 parent 和 child ，其中 parent 就是我们说的 boss 他的作用就是用来接收请求的连接，然后 worker 线程池就是用来处理读写事件的。最后我们找到两个参数的构造方法，用来初始化这两个参数，这里明显的能看到原来 boss 是在父类中定义的。所以调用父类的构造方法初始化了。过程和这个里对 Worker 初始化一样，做了检测，就不贴代码了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> ServerBootstrap <span class="title">group</span><span class="params">(EventLoopGroup parentGroup, EventLoopGroup childGroup)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 初始化 boss</span></span><br><span class="line">        <span class="keyword">super</span>.group(parentGroup);</span><br><span class="line">        <span class="keyword">if</span> (childGroup == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException(<span class="string">"childGroup"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span>.childGroup != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">"childGroup set already"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 初始化 worker</span></span><br><span class="line">        <span class="keyword">this</span>.childGroup = childGroup;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="2-常用方法分析"><a href="#2-常用方法分析" class="headerlink" title="2. 常用方法分析"></a>2. 常用方法分析</h2><p>&emsp;&emsp;  好的，上面看到了 bootstrap 里面的属性，接着分析一下我们常用的方法，主要就是 <code>group</code> 、 <code>option</code> 、<code>handler</code> 、<code>childHandler</code> 、 <code>bind</code> 、 <code>connect</code> 。</p>
<h3 id="1-option"><a href="#1-option" class="headerlink" title="1. option"></a>1. option</h3><p>&emsp;&emsp;  逻辑比较简单，如果 value 为 null 就说明要删除这个 option ，否则就放到表里面。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> &lt;T&gt; <span class="function">B <span class="title">option</span><span class="params">(ChannelOption&lt;T&gt; option, T value)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (option == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException(<span class="string">"option"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (value == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (options) &#123;</span><br><span class="line">                options.remove(option);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (options) &#123;</span><br><span class="line">                options.put(option, value);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> (B) <span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-handler-childHandler"><a href="#2-handler-childHandler" class="headerlink" title="2. handler/childHandler"></a>2. handler/childHandler</h3><p>&emsp;&emsp;  这个是直接设置了引用，就把 handler 设置为传入的参数。childHandler 同理。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> B <span class="title">handler</span><span class="params">(ChannelHandler handler)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (handler == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException(<span class="string">"handler"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">this</span>.handler = handler;</span><br><span class="line">        <span class="keyword">return</span> (B) <span class="keyword">this</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-channel"><a href="#3-channel" class="headerlink" title="3. channel"></a>3. channel</h3><p>&emsp;&emsp;  这里面直接调用了 channelFactory ，也就是在 3.x 中设置 channel 工厂，这里做了进一步的封装。这个 channel 工厂的作用就是利用反射 newInstance 。</p>
<h3 id="4-bind"><a href="#4-bind" class="headerlink" title="4. bind"></a>4. bind</h3><p>&emsp;&emsp;  好吧，这个可能是现在遇到的最麻烦的一个方法了，因为如果要说的话，里面牵扯的东西太多了，又再次引入的了新的类和方法。不着急先了解大概的思路，再看源码。</p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/18-4-9/93982359.jpg" alt><br>&emsp;&emsp;  在 bind 方法中写出来只有几行代码，但是每一个方法里面涉及的东西就比较多。首先是进行了验证，验证就是检测线程池 和 channelFactory 是不是正常被赋值了。接着就是重要的 doBind 方法。</p>
<p>&emsp;&emsp;  doBind 分为两个重要操作，第一个就是：对 channel 进行初始化和注册操作，然后把执行绑定操作。也就是这里涉及到的两个重要的方法： <code>initAndRegister</code> 和 <code>doBind0</code> 。</p>
<p>我的博客即将搬运同步至腾讯云+社区，邀请大家一同入驻：<a href="https://cloud.tencent.com/developer/support-plan" target="_blank" rel="noopener">https://cloud.tencent.com/developer/support-plan</a></p>
]]></content>
      <categories>
        <category>Netty 源码分析</category>
      </categories>
  </entry>
  <entry>
    <title>NioEventLoopGroup 源码分析</title>
    <url>/2018/04/07/Netty/NioEventLoopGroup%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="NioEventLoopGroup-源码分析"><a href="#NioEventLoopGroup-源码分析" class="headerlink" title="NioEventLoopGroup 源码分析"></a>NioEventLoopGroup 源码分析</h1><blockquote>
<h4 id="1-在阅读源码时做了一定的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限。为了方便-IDE-查看、跟踪、调试-代码，所以在-github-上提供-netty-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了一定的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限。为了方便-IDE-查看、跟踪、调试-代码，所以在-github-上提供-netty-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了一定的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限。为了方便 IDE 查看、跟踪、调试 代码，所以在 github 上提供 netty 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了一定的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限。为了方便 IDE 查看、跟踪、调试 代码，所以在 <a href="https://github.com/lwenxu/nettySourceCode" target="_blank" rel="noopener">github</a> 上提供 netty 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h4><h4 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h4></blockquote>
<p>&emsp;&emsp;  从今天开始，就准备进军 ne    tty 了，主要的想法是看看 netty4 中一些比较重要的实现，也就是能经常出现在我们面前的东西。主要是： 线程池、通道、管道、编解码器、以及常用的工具类。</p>
<p>&emsp;&emsp;  然后现在看源码应该不会像之前的 jdk 那么细致了，主要是看了一个类以后就发现 netty 对代码封装太强了，基本一个功能可能封装了七八个类去实现，很多的抽象类但是这些抽象类中的功能还非常的多。所以说主要看这个流程，以及里面写的比较好的代码或者比较新的思想会仔细的去看看。具体的子字段，每个方法不可能做到那么细致。</p>
<a id="more"></a>
<p>&emsp;&emsp;  好，正式开始 netty 源码征战 ！</p>
<h2 id="1-基本思路"><a href="#1-基本思路" class="headerlink" title="1. 基本思路"></a>1. 基本思路</h2><p>&emsp;&emsp;  这里首先讲一下结论，也就是先说我看这个类的源码整理出来的思路，主要就是因为这些类太杂，一个功能在好几个类中才完全实现。</p>
<p>&emsp;&emsp;  我们在 new 一个 worker/boss 线程的时候一般是采用的直接使用的无参的构造方法，但是无参的构造方法他创建的线程池的大小是我们 CPU 核心的 2 倍。紧接着就需要 new 这么多个线程放到线程池里面，这里的线程池采用的数据结构是一个数组存放的，每一个线程需要设置一个任务队列，显然任务队列使用的是一个阻塞队列，这里实际采用的是 <code>LinkedBlockQueue</code> ，然后回想一下在 jdk 中的线程池是不是还有一个比较重要的参数就是线程工厂，对的！这里也有这个东西，他是需要我们手动传入的，但是如果不传则会使用一个默认的线程工厂，里面有一个 <code>newThread</code> 方法，这个方法实现基本和 jdk 中的实现一模一样，就是创建一个级别为 5 的非 Daemon 线程。对这就是我们在创建一个线程池时候完成的全部工作！</p>
<p>&emsp;&emsp;  好现在来具体说一下，我们每次创建的是 <code>NioEventLoopGroup</code> 但是他又继承了 n 个类才实现了线程池，也就是线程池的祖先是 <code>ScheduledExecutorService</code> 是 jdk 中的线程池的一个接口，其中里面最重要的数据结构就是一个 children 数组，用来装线程的。</p>
<p>&emsp;&emsp;  然后具体的线程他也是进行了封装的，也就是我们常看到的 <code>NioEventLoop</code> 。这个类里面有两个比较重要的结构：taskQueue 和 thread 。很明显这个非常类似 jdk 中的线程池。</p>
<h2 id="2-NioEventLoopGroup-线程池分析"><a href="#2-NioEventLoopGroup-线程池分析" class="headerlink" title="2. NioEventLoopGroup 线程池分析"></a>2. NioEventLoopGroup 线程池分析</h2><p>&emsp;&emsp;  首先要创建线程池，传入的线程数为 0，他是一直在调用 <code>this()</code> 最后追溯到 <code>super(nThreads,threadFactory,selectorProvider)</code>  也就是使用了 <code>MultithreadEventLoopGroup</code> 的构造方法，在这一步确定了当传入的线程数为 0 时应该设置的线程数为 CPU 核心的两倍。然后再次上调，调用了 <code>MultithreadEventExecutorGroup</code> 的构造方法，在这里才是真正的开始了线程池的初始化。</p>
<p>&emsp;&emsp;  首先设置了线程池工厂，然后初始化 chooser ，接着创建 n 个线程放到 children 数组中，最后设置线程中断的监听事件。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 这个方法流程：</span></span><br><span class="line"><span class="comment">     * 1、设置了默认的线程工厂</span></span><br><span class="line"><span class="comment">     * 2、初始化 chooser</span></span><br><span class="line"><span class="comment">     * 3、创建nTreads个NioEventLoop对象保存在children数组中</span></span><br><span class="line"><span class="comment">     * 4、添加中断的监听事件</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> nThreads</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> threadFactory</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> args</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="title">MultithreadEventExecutorGroup</span><span class="params">(<span class="keyword">int</span> nThreads, ThreadFactory threadFactory, Object... args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (nThreads &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(String.format(<span class="string">"nThreads: %d (expected: &gt; 0)"</span>, nThreads));</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 默认使用线程工厂是 DefaultThreadFactory</span></span><br><span class="line">        <span class="keyword">if</span> (threadFactory == <span class="keyword">null</span>) &#123;</span><br><span class="line">            threadFactory = newDefaultThreadFactory();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        children = <span class="keyword">new</span> SingleThreadEventExecutor[nThreads];</span><br><span class="line">        <span class="comment">// 二的平方的实现是看  n&amp;-n==n</span></span><br><span class="line">        <span class="comment">//根据线程个数是否为2的幂次方，采用不同策略初始化chooser</span></span><br><span class="line">        <span class="keyword">if</span> (isPowerOfTwo(children.length)) &#123;</span><br><span class="line">            chooser = <span class="keyword">new</span> PowerOfTwoEventExecutorChooser();</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            chooser = <span class="keyword">new</span> GenericEventExecutorChooser();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//产生nTreads个NioEventLoop对象保存在children数组中</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; nThreads; i ++) &#123;</span><br><span class="line">            <span class="keyword">boolean</span> success = <span class="keyword">false</span>;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                children[i] = newChild(threadFactory, args);</span><br><span class="line">                success = <span class="keyword">true</span>;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Exception e) &#123;</span><br><span class="line">                <span class="comment">// <span class="doctag">TODO:</span> Think about if this is a good exception type</span></span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> IllegalStateException(<span class="string">"failed to create a child event loop"</span>, e);</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                <span class="comment">// 没成功，把已有的线程优雅关闭</span></span><br><span class="line">                <span class="keyword">if</span> (!success) &#123;</span><br><span class="line">                    <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; i; j ++) &#123;</span><br><span class="line">                        children[j].shutdownGracefully();</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// 没有完全关闭的线程让它一直等待</span></span><br><span class="line">                    <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; i; j ++) &#123;</span><br><span class="line">                        EventExecutor e = children[j];</span><br><span class="line">                        <span class="keyword">try</span> &#123;</span><br><span class="line">                            <span class="keyword">while</span> (!e.isTerminated()) &#123;</span><br><span class="line">                                e.awaitTermination(Integer.MAX_VALUE, TimeUnit.SECONDS);</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125; <span class="keyword">catch</span> (InterruptedException interrupted) &#123;</span><br><span class="line">                            Thread.currentThread().interrupt();</span><br><span class="line">                            <span class="keyword">break</span>;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 对每一个 children 添加中断线程时候的监听事件，就是将 terminatedChildren 自增</span></span><br><span class="line">        <span class="comment">// 判断是否到达线程总数，是则更新 terminationFuture</span></span><br><span class="line">        <span class="keyword">final</span> FutureListener&lt;Object&gt; terminationListener = <span class="keyword">new</span> FutureListener&lt;Object&gt;() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">operationComplete</span><span class="params">(Future&lt;Object&gt; future)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">                <span class="keyword">if</span> (terminatedChildren.incrementAndGet() == children.length) &#123;</span><br><span class="line">                    terminationFuture.setSuccess(<span class="keyword">null</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (EventExecutor e: children) &#123;</span><br><span class="line">            e.terminationFuture().addListener(terminationListener);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  其中有一个 if 分支用来初始化 chooser ，这个 chooser 就是用来选择使用哪个线程来执行哪些操作的。这里用到了判断一个数是否为 2 的次幂的一个方法 <code>isPowerOfTwo()</code>  实现比较有意思，贴出来。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">boolean</span> <span class="title">isPowerOfTwo</span><span class="params">(<span class="keyword">int</span> val)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> (val &amp; -val) == val;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  接下来目光要转向 <code>newChild(threadFactory, args)</code> ，因为在这个类里面这个方法是抽象的，在 <code>NioEventLoopGroup</code> 得到了实现。其实看到了也非常的简单粗暴，直接 new 了一个 <code>NioEventLoop</code> ，接下来就应该分析这个线程的包装类了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> EventExecutor <span class="title">newChild</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">        ThreadFactory threadFactory, Object... args)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">    <span class="comment">// 这里才是重点  也就是真正的线程   被放在自己的 children 数组中</span></span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> NioEventLoop(<span class="keyword">this</span>, threadFactory, (SelectorProvider) args[<span class="number">0</span>]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-NioEventLoop-线程分析"><a href="#3-NioEventLoop-线程分析" class="headerlink" title="3. NioEventLoop 线程分析"></a>3. NioEventLoop 线程分析</h2><p>&emsp;&emsp;  上面已经看到了，<code>newChild</code> 方法就是 new 了一个 <code>NioEventLoop</code> 。所以有必要好好看看这个线程包装类。</p>
<p>&emsp;&emsp;  这个类的构造方法是调用了父类 <code>SingleThreadEventLoop</code> 的构造,接着继续上调 <code>SingleThreadEventExecutor</code> 构造，在这个类中才真正的实现了线程的构造。里面就做了两件事 ： </p>
<ol>
<li><p>new 了一个新的线程，新的线程还分配了一个任务，任务的内容就是调用本类中的一个 run 方法，在 <code>NioEventLoop</code> 中实现。</p>
</li>
<li><p>设置任务队列为 <code>LinkedBlockQueue</code></p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 构造方法主要完成了：</span></span><br><span class="line"><span class="comment">     * 1、new 一个新的线程执行一个 run 方法</span></span><br><span class="line"><span class="comment">     * 2、用 LinkedBlockQueue 初始化 taskQueue</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> parent</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> threadFactory</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> addTaskWakesUp</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="title">SingleThreadEventExecutor</span><span class="params">(EventExecutorGroup parent, ThreadFactory threadFactory, <span class="keyword">boolean</span> addTaskWakesUp)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (threadFactory == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException(<span class="string">"threadFactory"</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">this</span>.parent = parent;</span><br><span class="line">        <span class="keyword">this</span>.addTaskWakesUp = addTaskWakesUp;</span><br><span class="line">        <span class="comment">// new 了一个新的线程</span></span><br><span class="line">        thread = threadFactory.newThread(<span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">boolean</span> success = <span class="keyword">false</span>;</span><br><span class="line">                updateLastExecutionTime();</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    <span class="comment">// 调用一个 run 方法</span></span><br><span class="line">                    SingleThreadEventExecutor.<span class="keyword">this</span>.run();</span><br><span class="line">                    success = <span class="keyword">true</span>;</span><br><span class="line">                &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">                    logger.warn(<span class="string">"Unexpected exception from an event executor: "</span>, t);</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    <span class="comment">// 让线程关闭</span></span><br><span class="line">                    <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                        <span class="keyword">int</span> oldState = STATE_UPDATER.get(SingleThreadEventExecutor.<span class="keyword">this</span>);</span><br><span class="line">                        <span class="keyword">if</span> (oldState &gt;= ST_SHUTTING_DOWN || STATE_UPDATER.compareAndSet(</span><br><span class="line">                                SingleThreadEventExecutor.<span class="keyword">this</span>, oldState, ST_SHUTTING_DOWN)) &#123;</span><br><span class="line">                            <span class="keyword">break</span>;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="comment">// Check if confirmShutdown() was called at the end of the loop.</span></span><br><span class="line">                    <span class="keyword">if</span> (success &amp;&amp; gracefulShutdownStartTime == <span class="number">0</span>) &#123;</span><br><span class="line">                        logger.error(</span><br><span class="line">                                <span class="string">"Buggy "</span> + EventExecutor.class.getSimpleName() + <span class="string">" implementation; "</span> +</span><br><span class="line">                                SingleThreadEventExecutor.class.getSimpleName() + <span class="string">".confirmShutdown() must be called "</span> +</span><br><span class="line">                                <span class="string">"before run() implementation terminates."</span>);</span><br><span class="line">                    &#125;</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        <span class="comment">// Run all remaining tasks and shutdown hooks.</span></span><br><span class="line">                        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                            <span class="keyword">if</span> (confirmShutdown()) &#123;</span><br><span class="line">                                <span class="keyword">break</span>;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                        <span class="keyword">try</span> &#123;</span><br><span class="line">                            cleanup();</span><br><span class="line">                        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                            STATE_UPDATER.set(SingleThreadEventExecutor.<span class="keyword">this</span>, ST_TERMINATED);</span><br><span class="line">                            threadLock.release();</span><br><span class="line">                            <span class="keyword">if</span> (!taskQueue.isEmpty()) &#123;</span><br><span class="line">                                logger.warn(<span class="string">"An event executor terminated with non-empty task queue ("</span> + taskQueue.size() + <span class="string">')'</span>);</span><br><span class="line">                            &#125;</span><br><span class="line">                            terminationFuture.setSuccess(<span class="keyword">null</span>);</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 使用 LinkedBlockQueue 初始化 taskQueue</span></span><br><span class="line">        taskQueue = newTaskQueue();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  然后看一下他要执行的 run 方法在 <code>NioEventLoop</code> 中得到了实现。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">     *'wakenUp.compareAndSet(false, true)' 一般都会在 select.wakeUp() 之前执行</span></span><br><span class="line"><span class="comment">     * 因为这样可以减少 select.wakeUp() 调用的次数，select.wakeUp() 调用是一个代价</span></span><br><span class="line"><span class="comment">     * 很高的操作</span></span><br><span class="line"><span class="comment">     * 注意：如果说我们过早的把 wakenUp 设置为 true，可能导致线程的竞争问题，过早设置的情形如下：</span></span><br><span class="line"><span class="comment">         1) Selector is waken up between 'wakenUp.set(false)' and</span></span><br><span class="line"><span class="comment">            'selector.select(...)'. (BAD)</span></span><br><span class="line"><span class="comment">         2) Selector is waken up between 'selector.select(...)' and</span></span><br><span class="line"><span class="comment">            'if (wakenUp.get()) &#123; ... &#125;'. (OK)</span></span><br><span class="line"><span class="comment">     在第一种情况中 wakenUp 被设置为 true 则 select 会立刻被唤醒直到 wakenUp 再次被设置为 false</span></span><br><span class="line"><span class="comment">     但是wakenUp.compareAndSet(false, true)会失败，并且导致所有希望唤醒他的线程都会失败导致</span></span><br><span class="line"><span class="comment">     select 进行不必要的休眠</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     为了解决这个问题我们是在 wakenUp 为 true 的时候再次对 select 进行唤醒。</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="comment">// 获取之前的线程状态，并让 select 阻塞</span></span><br><span class="line">            <span class="keyword">boolean</span> oldWakenUp = wakenUp.getAndSet(<span class="keyword">false</span>);</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 有任务在线程创建之后直接开始 select</span></span><br><span class="line">                <span class="keyword">if</span> (hasTasks()) &#123;</span><br><span class="line">                    selectNow(); <span class="comment">//直接调用了 select 的 selectNow 然后再次唤醒同下面的代码</span></span><br><span class="line">                <span class="comment">// 没有任务</span></span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 自旋进行等待可进行 select 操作</span></span><br><span class="line">                    select(oldWakenUp);</span><br><span class="line">                    <span class="comment">// 再次唤醒，解决并发问题</span></span><br><span class="line">                    <span class="keyword">if</span> (wakenUp.get()) &#123;</span><br><span class="line">                        selector.wakeup();</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                cancelledKeys = <span class="number">0</span>;</span><br><span class="line">                needsToSelectAgain = <span class="keyword">false</span>;</span><br><span class="line">                <span class="keyword">final</span> <span class="keyword">int</span> ioRatio = <span class="keyword">this</span>.ioRatio;</span><br><span class="line">                <span class="comment">// 都是处理 selected 的通道的数据，并执行所有的任务，只是在 runAllTasks 传的参数不同</span></span><br><span class="line">                <span class="keyword">if</span> (ioRatio == <span class="number">100</span>) &#123;</span><br><span class="line">                    processSelectedKeys();</span><br><span class="line">                    runAllTasks();</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="keyword">final</span> <span class="keyword">long</span> ioStartTime = System.nanoTime();</span><br><span class="line">                    processSelectedKeys();</span><br><span class="line">                    <span class="keyword">final</span> <span class="keyword">long</span> ioTime = System.nanoTime() - ioStartTime;</span><br><span class="line">                    runAllTasks(ioTime * (<span class="number">100</span> - ioRatio) / ioRatio);</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (isShuttingDown()) &#123;</span><br><span class="line">                    closeAll();</span><br><span class="line">                    <span class="keyword">if</span> (confirmShutdown()) &#123;</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">                logger.warn(<span class="string">"Unexpected exception in the selector loop."</span>, t);</span><br><span class="line">                <span class="comment">// Prevent possible consecutive immediate failures that lead to</span></span><br><span class="line">                <span class="comment">// excessive CPU consumption.</span></span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    <span class="comment">// Ignore.</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  紧接着就是分析这个 run 方法，也就是线程在被创建之后进行的一系列操作。里面主要做了三件事：</p>
<ol>
<li>进行 select </li>
<li>处理 selectedKeys</li>
<li>唤醒队列中所有的任务</li>
</ol>
<p>&emsp;&emsp;  上面的操作都是在一个循环里面一直执行的，所以说 <code>NioEventLoop</code> 这个线程的作用就只有一个那就是：进行任务处理。在这个线程被 new 出来时我们就给他分配了线程的任务就是永不停歇的进行上面的操作。</p>
<p>&emsp;&emsp;  上面的过程说的是有线程安全问题，也就是如果我们过早的把 wakenUp 设置为 true，我们的 select 就会苏醒过来，而其他的线程不清楚这种状态想要设置为 wakenUp 的时候都会失败，导致 select 休眠。主要感觉有点是因为这个东西不是线程间可见的，要是采用 volatile 可能就会解决这个问题，但是 wakenUp 是 final 的不能使用 volatile 关键字修饰。所以作者采用的解决方案就是再次手动唤醒，防止由于其他线程并发设置 wakenUp 的值导致的不必要的休眠。</p>
<p>&emsp;&emsp;  然后要说一下 select 方法，这个方法的调用主要因为在队列中没有任务，所以就暂时不用 select ，这个方法里面做的就是自旋的去 select ，没有任务就 等待一段时间再去 select。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 这个方法主要干的事情：</span></span><br><span class="line"><span class="comment">     * 1、如果不需要等待就直接 select</span></span><br><span class="line"><span class="comment">     * 2、需要等待则等一个超时时间再去 select</span></span><br><span class="line"><span class="comment">     * 这个过程是不停进行的也就是死循环直达有任务可进行 select 时 select 完毕退出循环</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> oldWakenUp</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> IOException</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">select</span><span class="params">(<span class="keyword">boolean</span> oldWakenUp)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        Selector selector = <span class="keyword">this</span>.selector;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">int</span> selectCnt = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">long</span> currentTimeNanos = System.nanoTime();</span><br><span class="line">            <span class="keyword">long</span> selectDeadLineNanos = currentTimeNanos + delayNanos(currentTimeNanos);</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="comment">// 不用等待进行一次 select 操作</span></span><br><span class="line">                <span class="keyword">long</span> timeoutMillis = (selectDeadLineNanos - currentTimeNanos + <span class="number">500000L</span>) / <span class="number">1000000L</span>;</span><br><span class="line">                <span class="keyword">if</span> (timeoutMillis &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (selectCnt == <span class="number">0</span>) &#123;</span><br><span class="line">                        selector.selectNow();</span><br><span class="line">                        selectCnt = <span class="number">1</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 等一个超时再去选择</span></span><br><span class="line">                <span class="keyword">int</span> selectedKeys = selector.select(timeoutMillis);</span><br><span class="line">                selectCnt ++;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">if</span> (selectedKeys != <span class="number">0</span> || oldWakenUp || wakenUp.get() || hasTasks() || hasScheduledTasks()) &#123;</span><br><span class="line">                    <span class="comment">// - Selected something,</span></span><br><span class="line">                    <span class="comment">// - waken up by user, or</span></span><br><span class="line">                    <span class="comment">// - the task queue has a pending task.</span></span><br><span class="line">                    <span class="comment">// - a scheduled task is ready for processing</span></span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">if</span> (Thread.interrupted()) &#123;</span><br><span class="line">                    <span class="comment">// Thread was interrupted so reset selected keys and break so we not run into a busy loop.</span></span><br><span class="line">                    <span class="comment">// As this is most likely a bug in the handler of the user or it's client library we will</span></span><br><span class="line">                    <span class="comment">// also log it.</span></span><br><span class="line">                    <span class="comment">//</span></span><br><span class="line">                    <span class="comment">// See https://github.com/netty/netty/issues/2426</span></span><br><span class="line">                    <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">                        logger.debug(<span class="string">"Selector.select() returned prematurely because "</span> +</span><br><span class="line">                                <span class="string">"Thread.currentThread().interrupt() was called. Use "</span> +</span><br><span class="line">                                <span class="string">"NioEventLoop.shutdownGracefully() to shutdown the NioEventLoop."</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                    selectCnt = <span class="number">1</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">long</span> time = System.nanoTime();</span><br><span class="line">                <span class="keyword">if</span> (time - TimeUnit.MILLISECONDS.toNanos(timeoutMillis) &gt;= currentTimeNanos) &#123;</span><br><span class="line">                    <span class="comment">// timeoutMillis elapsed without anything selected.</span></span><br><span class="line">                    selectCnt = <span class="number">1</span>;</span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (SELECTOR_AUTO_REBUILD_THRESHOLD &gt; <span class="number">0</span> &amp;&amp;</span><br><span class="line">                        selectCnt &gt;= SELECTOR_AUTO_REBUILD_THRESHOLD) &#123;</span><br><span class="line">                    <span class="comment">// The selector returned prematurely many times in a row.</span></span><br><span class="line">                    <span class="comment">// Rebuild the selector to work around the problem.</span></span><br><span class="line">                    logger.warn(</span><br><span class="line">                            <span class="string">"Selector.select() returned prematurely &#123;&#125; times in a row; rebuilding selector."</span>,</span><br><span class="line">                            selectCnt);</span><br><span class="line"></span><br><span class="line">                    rebuildSelector();</span><br><span class="line">                    selector = <span class="keyword">this</span>.selector;</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// Select again to populate selectedKeys.</span></span><br><span class="line">                    selector.selectNow();</span><br><span class="line">                    selectCnt = <span class="number">1</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                currentTimeNanos = time;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (selectCnt &gt; MIN_PREMATURE_SELECTOR_RETURNS) &#123;</span><br><span class="line">                <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">                    logger.debug(<span class="string">"Selector.select() returned prematurely &#123;&#125; times in a row."</span>, selectCnt - <span class="number">1</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">catch</span> (CancelledKeyException e) &#123;</span><br><span class="line">            <span class="keyword">if</span> (logger.isDebugEnabled()) &#123;</span><br><span class="line">                logger.debug(CancelledKeyException.class.getSimpleName() + <span class="string">" raised by a Selector - JDK bug?"</span>, e);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// Harmless exception - log anyway</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  接着就是 <code>processSelectedKeys();</code> 和<code>runAllTasks();</code> 这两个方法，前一个方法不说就是和我们写 Nio 的时候的步骤差不多，遍历 selectedKeys 处理，然后 <code>runAllTasks()</code> 执行所有的任务的 run 方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">runAllTasks</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        fetchFromDelayedQueue();</span><br><span class="line">        Runnable task = pollTask();</span><br><span class="line">        <span class="keyword">if</span> (task == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 这个循环就是用来循环任务队列中的所有任务</span></span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                task.run();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Throwable t) &#123;</span><br><span class="line">                logger.warn(<span class="string">"A task raised an exception."</span>, t);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            task = pollTask(); <span class="comment">// 循环条件</span></span><br><span class="line">            <span class="keyword">if</span> (task == <span class="keyword">null</span>) &#123;</span><br><span class="line">                lastExecutionTime = ScheduledFutureTask.nanoTime();</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4. 总结"></a>4. 总结</h2><p>&emsp;&emsp;  好了其实到这里线程池其实分析的已经差不多了，对于很多的细节问题并没有仔细的去看，单丝我们清楚流程以及里面的结构基本就差不多了。</p>
<p>&emsp;&emsp;  在 <code>NioEventLoopGroup</code> 中包装了 <code>NioEventLoop</code> 线程任务。具体包装在了 children 数组中，然后使用 newThread 工厂创建线程，接着给线程分配任务，任务就是进行 select 操作。</p>
<p>我的博客即将搬运同步至腾讯云+社区，邀请大家一同入驻：<a href="https://cloud.tencent.com/developer/support-plan" target="_blank" rel="noopener">https://cloud.tencent.com/developer/support-plan</a></p>
]]></content>
      <categories>
        <category>Netty 源码分析</category>
      </categories>
  </entry>
  <entry>
    <title>Netty 入门</title>
    <url>/2018/04/06/Netty/Netty%20%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h2 id="1-粘包问题"><a href="#1-粘包问题" class="headerlink" title="1. 粘包问题"></a>1. 粘包问题</h2><h3 id="一-长连接与短连接："><a href="#一-长连接与短连接：" class="headerlink" title="一 .长连接与短连接："></a>一 .长连接与短连接：</h3><p>1.长连接：Client方与Server方先建立通讯连接，连接建立后不断开， 然后再进行报文发送和接收。长连接在 netty 中是默认开启的，也就是我们创建了一个 Server 以后监听端口，我们的客户端去连接发现只要我们的客户端不主动的断开连接他们之间的连接是一直保持有效的。</p>
<a id="more"></a>
<p>2.短连接：Client方与Server每进行一次报文收发交易时才进行通讯连接，交易完毕后立即断开连接。此种方式常用于一点对多点通讯，比如多个Client连接一个Server。但是在 netty 中默认采用了长连接，我们如何使用短连接呢？其实很简单，在我们的 Server 端需要对客户端进行回写数据的时候我们只需要在回写的后面加上一个监听事件，就是当写完毕，我们就关闭此连接。 </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ctx.channel().writeAndFlush(<span class="string">"Hi Client I'm Server !&amp;&amp;"</span>).addListener(ChannelFutureListener.CLOSE);</span><br></pre></td></tr></table></figure>

<h3 id="二-什么时候需要考虑粘包问题"><a href="#二-什么时候需要考虑粘包问题" class="headerlink" title="二 .什么时候需要考虑粘包问题?"></a>二 .什么时候需要考虑粘包问题?</h3><p>1:如果利用tcp每次发送数据，就与对方建立连接，然后双方发送完一段数据后，就关闭连接，这样就不会出现粘包问题，因为只有一种包结构,类似于http协议。<br>2：如果发送数据无结构，如文件传输，这样发送方只管发送，接收方只管接收存储就ok，也不用考虑粘包<br>3：如果双方建立连接，需要在连接后一段时间内发送不同结构数据，则需要考虑粘包问题。</p>
<h3 id="三-粘包出现原因"><a href="#三-粘包出现原因" class="headerlink" title="三 .粘包出现原因"></a>三 .粘包出现原因</h3><p>TCP 是一个字节流的的传输，也就是在流中传输无固定结构的数据包。但是UDP不会出现粘包，因为它有消息边界。<br>1 发送端需要等缓冲区满才发送出去，造成粘包<br>2 接收方不及时接收缓冲区的包，造成多个包接收</p>
<p>具体的在 TCP 中出现这种情况的原因：</p>
<ol>
<li><p>由Nagle算法造成的发送端的粘包:Nagle算法是一种改善网络传输效率的算法.简单的说,当我们提交一段数据给TCP发送时,TCP并不立刻发送此段数据,而是等待一小段时间,看看在等待期间是否还有要发送的数据,若有则会一次把这两段数据发送出去.这是对Nagle算法一个简单的解释,详细的请看相关书籍.</p>
</li>
<li><p>接收端接收不及时造成的接收端粘包:TCP会把接收到的数据存在自己的缓冲区中,然后通知应用层取数据.当应用层由于某些原因不能及时的把TCP的数据取出来,就会造成TCP缓冲区中存放了几段数据.</p>
<h3 id="四-解决办法"><a href="#四-解决办法" class="headerlink" title="四 .解决办法"></a>四 .解决办法</h3><p>为了避免粘包现象，可采取以下几种措施：</p>
</li>
<li><p>一是对于发送方引起的粘包现象，用户可通过编程设置来避免，TCP提供了强制数据立即传送的操作指令push，TCP软件收到该操作指令后，就立即将本段数据发送出去，而不必等待发送缓冲区满；</p>
</li>
<li><p>二是对于接收方引起的粘包，则可通过优化程序设计、精简接收进程工作量、提高接收进程优先级等措施，使其及时接收数据，从而尽量避免出现粘包现象；</p>
</li>
<li><p>三是由接收方控制，将一包数据按结构字段，人为控制分多次接收，然后合并，通过这种手段来避免粘包。</p>
</li>
</ol>
<p>&emsp;&emsp;  以上提到的三种措施，都有其不足之处。第一种编程设置方法虽然可以避免发送方引起的粘包，但它关闭了优化算法，降低了网络发送效率，影响应用程序的性能，一般不建议使用。第二种方法只能减少出现粘包的可能性，但并不能完全避免粘包，当发送频率较高时，或由于网络突发可能使某个时间段数据包到达接收方较快，接收方还是有可能来不及接收，从而导致粘包。第三种方法虽然避免了粘包，但应用程序的效率较低，对实时应用的场合不适合。</p>
<p>&emsp;&emsp;  最初遇到”粘包”的问题时,我是通过在两次send之间调用sleep来休眠一小段时间来解决.这个解决方法的缺点是显而易见的,使传输效率大大降低,而且也并不可靠.后来就是通过应答的方式来解决,尽管在大多数时候是可行的,但是不能解决象B的那种情况,而且采用应答方式增加了通讯量,加重了网络负荷. 再后来就是对数据包进行封包和拆包的操作.<br>&emsp;&emsp;  封包:封包就是给一段数据加上包头,这样一来数据包就分为包头和包体两部分内容了(以后讲过滤非法包时封包会加入”包尾”内容).包头其实上是个大小固定的结构体,其中有个结构体成员变量表示包体的长度,这是个很重要的变量,其他的结构体成员可根据需要自己定义.根据包头长度固定以及包头中含有包体长度的变量就能正确的拆分出一个完整的数据包.</p>
<h3 id="五-在-netty-中解决粘包的方式"><a href="#五-在-netty-中解决粘包的方式" class="headerlink" title="五 .在 netty 中解决粘包的方式"></a>五 .在 netty 中解决粘包的方式</h3><ol>
<li>采用了分隔符，类似于链路层的那种使用一个特殊的标记来分割数据，这里主要采用了一个工具类 <code>DelimiterBasedFrameDecoder(arg0,agr1)</code> 其中第一个参数指的是这个分隔符的可占用的空间大小，只能比他大不能小于。第二个参数就是具体的分隔符了，但是不能传递一个字符串而是一个 <code>ByteBuf</code> 对象，也就是我们需要用工具转一下 <code>Unpooled.copiedBuffer(&quot;&amp;&amp;&quot;.getBytes())</code> ，代码如下：</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">pipeline.addLast(<span class="keyword">new</span> DelimiterBasedFrameDecoder(<span class="number">1024</span>, Unpooled.copiedBuffer(<span class="string">"&amp;&amp;"</span>.getBytes())));</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>第二种方式也比较简单，这个就是采用了定长的报文，当我们的报文长度不够的时候必须要采用空格补齐。比如说，我们发送了 “aaaaacc” 而我们的定长指定的是 5 那么，我们只会收到 “aaaaa” 而没有 c 这就是因为他不足 5 个，我们必须采用空格补齐才能收到，或者等待下次数据比较多的时候会跟着过来。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">.addLast(<span class="keyword">new</span> FixedLengthFrameDecoder(<span class="number">5</span>));</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>pojo 方式，也就是自定义数据的头和体部分，其中头中最重要的就是一个长度字段。</li>
</ol>
<p>参考资料：</p>
<ul>
<li><a href="https://blog.csdn.net/zhangxinrun/article/details/6721495" target="_blank" rel="noopener">https://blog.csdn.net/zhangxinrun/article/details/6721495</a> </li>
</ul>
]]></content>
      <categories>
        <category>Netty</category>
      </categories>
  </entry>
  <entry>
    <title>你好杭州</title>
    <url>/2018/04/04/Life/%E4%BD%A0%E5%A5%BD%E6%9D%AD%E5%B7%9E/</url>
    <content><![CDATA[<p>​    这次已经是我第二次来杭州了，虽然上次来了一次杭州但是待的时间很短。但是也还是各种不良反应，对突然在北方生活了三年的人，突然来到南方好像显得有点 “格格不入” 。从机场到酒店被出租车司机各种坑，下载打车的 app 然后各种交费，最后那个车只打了一半的路程我就花了 160 多，然后真的是饿的不行就在一个店里吃了一点饭，那个时候真的觉得很累很累，折腾了一天也终于吃上了一顿饭，当时心里真的是又幸福又心酸。</p>
<p>​    然后吃完以后真的是折腾不动了就想着要不都花了这么多钱了我就直接打车到酒店好了，估计我九点的时候还能到酒店多休息一会。接着又是一顿晕车（<del>晕车这个毛病真的该治治了，可怎么就是治不好呢？</del>），终于在九点的时候成功抵达公司安排的酒店。想着终于能一个人放松一下了，到了前台小姐姐跟我说这个房间是两个人住的，突然感觉又不好了，唉说的是另外一个人什么时候入住还不一定呢，也就是现在就我一个人，那好了我哈能过几天舒服的时间。</p>
<p>​    然而….然而….. 令我万万没想到的是我刚洗完澡出来这个小伙就来了。我….. 果然还是不能庆幸太早怎么就这么巧呢。好吧，那就两个人住吧！！！随便说了几句我就想着出去买点生活用品好了，那个时候已经十点多了，想着能不能顺便去阿里踩个点，出门之后我就放弃了。现在是十点多对吧？没有时差对吧？没开玩笑对吧？为啥现在的感觉跟我下午5点多的感觉一模一样。心态有点崩，原来杭州这边的保温效果这么好的啊，冬天完全不用暖气什么的吧。</p>
<p>​    就在楼下还没有关门的天猫超市（看到这还有点亲切，因为我就是这个部门的。: &gt; ），这里面的东西也是超级贵啊就买点洗衣液药膏牙刷什么的吧，到时候酒店那个不好用还是用自己的。其他的矿泉水，面包（没有牛奶）。</p>
<p>​    接着就和蠢瑾打电话到快十二点了，又聊了一会天才睡的，哎第一天是真的多灾多难！</p>
<p>​    第二天早晨七点的阳光晃得我没有了睡意，原来是昨晚没有拉上窗帘，起来洗澡然后各种检查证件。</p>
<p>​    出发去阿里！路上的感觉还是一样的，七点四五十的时候和中午的感觉一样，热！没有一点风，两路的树特别绿，桥下的水也很绿，太阳洒在树叶上亮油油的，感觉好像那些树很享受一样。接着就是惯例贴条子到大厅等候，我进去的那个门走的是停车场开始感觉有点乱，往后面走发现走了好久才经过了一栋楼这个地方是真大。走了很久才到等候区，一路上又热又赶时间一张照片也没拍，但是也看到了不少拍照的那应该是刚到的实习生吧，第一次来肯定都很新鲜。</p>
<p>​    经过上午加下午两个小时的 hr 小姐姐的絮叨之后终于到了工作区，师兄专门下来帮我搬电脑超感动，师兄也各种跟我攀谈，谁让我平时少言寡语总要别人来带起话题。然后给我分配了 “海景房” 。本来以为我会坐在这个师兄旁边，我旁边的师兄应该20来岁的样子，估计是毕业不是很久的研究生吧。接着我就开始折腾电脑，装各种软件，这个真的是我最喜欢干的事了，可以他是 windows 要是 mac 就好了，真羡慕那些拿 macbook pro 的，我也想要啊 : &lt; ，不过师兄说等我转正了也会配置这种的，怎么说还是很期待。我会努力留下来的，加油！</p>
<p>​    可是，第二天我就生病了，没错！是鼻窦炎，感觉整个人都废了，这样的话别说实习了我啥都干不了了。全身瘫软，然后硬硬的撑了一天晚上去买药，坑爹的是去了那边就没有信号，没办法用支付宝我钱刚好不够，我可是走了三公里过来买的药啊别这么坑我，折腾好好一会那个医生的儿子才给我用手机开的热点，艰辛的买完了药，真的在得了病之后才感觉每次拿药真的就像救命稻草一样，回去就喝了一遍，第二天早上还是有点严重比昨天稍微好了一点，后来才知道那个房间的空调对着我又吹了一晚上不出问题才怪。早上起来又喝了一遍药去了公司十点多的时候师兄带我去认人，我真的头都抬不起来脑子一片空白一个人都没记住反正就师兄好！其他都是师兄再帮我说，那个时候真的没办法管了因为我站起来都费劲，中午吃饭的时候把筷子都掉在地上了，拿不住，真的一点不夸张。当时不仅仅是生病的原因，那个药的副作用太大了，我敲键盘的时候手放在那都在抖。那种感觉真的不想体验第二次了。可是下午感冒就有了很大的改观，鼻子塞的也没有那么严重了，药确实有效但是这个副作用真的太可怕了。</p>
<p>​    可是这不是最要命的，最要命是我好几天都整夜整夜的失眠至于原因也很明显就是蠢瑾闹的，整天说气我的话，那几天真的让我特别不高兴，可是第二天早上还是要上班啊…. 所以那段时间过的很不开心，给爸妈打了好几个电话。不过最后几天她态度好太多了，我还挺高兴了，工作也慢慢开始上手了….</p>
<p>​    第二次来杭州，来阿里继续实习我想这次一定一定要斩杀 offer ！我是你的骄傲哦，所以我才不会让你失望呢！</p>
]]></content>
      <categories>
        <category>生活小记</category>
      </categories>
      <tags>
        <tag>阿里</tag>
      </tags>
  </entry>
  <entry>
    <title>Exectors框架 源码分析</title>
    <url>/2018/04/03/Java%20SourceCode/Exectors%E6%A1%86%E6%9E%B6%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="Exectors框架-源码分析"><a href="#Exectors框架-源码分析" class="headerlink" title="Exectors框架 源码分析"></a>Exectors框架 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-基本结构"><a href="#1-基本结构" class="headerlink" title="1. 基本结构"></a>1. 基本结构</h2><p>&emsp;&emsp;  由于 Exector 这个家族还是比较大的，所以先导出一下类图，对这个家族有一个大概的认识。<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-4-3/89479215.jpg" alt></p>
<a id="more"></a>

<h2 id="2-Exector"><a href="#2-Exector" class="headerlink" title="2. Exector"></a>2. Exector</h2><p>&emsp;&emsp;  可以看到，Exector 属于一个接口，其实它里面只有一个 <code>void execute(Runnable)</code> 方法。注意它里面的参数是 <code>Runnable</code> ，并且返回值是 void 类型。</p>
<h2 id="3-ExectorService"><a href="#3-ExectorService" class="headerlink" title="3. ExectorService"></a>3. ExectorService</h2><p>&emsp;&emsp;  紧接着就是另外一个 <code>ExectorService</code> 接口，这个接口继承了上面的 <code>Exector</code> 但是他在原来的接口上添加了非常多的方法，其中有两类方法比较重要。 其中一类就是<code>submit()</code> 这里提供了三个重载的方法：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 返回一个 Future 对象，表示将要执行的线程的状态</span></span><br><span class="line"><span class="comment">// Future 对象的 get 方法会一直阻塞直到算出值</span></span><br><span class="line">&lt;T&gt; <span class="function">Future&lt;T&gt; <span class="title">submit</span><span class="params">(Callable&lt;T&gt; task)</span></span>;</span><br><span class="line"><span class="comment">// Runnable 本来是不返回 Future 对象的，这里使用了一个参数传递</span></span><br><span class="line">&lt;T&gt; <span class="function">Future&lt;T&gt; <span class="title">submit</span><span class="params">(Runnable task, T result)</span></span>;</span><br><span class="line"><span class="comment">// 普通的执行，类似于 execute 方法</span></span><br><span class="line">Future&lt;?&gt; submit(Runnable task);</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  注意，这三个方法都是有返回值的，并且返回值都是一个 Future 对象，然后就是参数的问题，他们的参数可以是 Runnable 也可以是 Callable 。Runnable 我们都比较熟悉了，Callable 我们就可以理解成带有返回值的 Runnable。</p>
<p>&emsp;&emsp;  然后另外一类方法就是用来判断线程状态的方法，以及操作线程池中的线程的方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 启动一次顺序关闭，执行以前提交的任务，但不接受新任务。</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">shutdown</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">// 尝试终止正在执行的线程，暂停处理正在等待的任务，并返回等待执行的任务列表。</span></span><br><span class="line"><span class="function">List&lt;Runnable&gt; <span class="title">shutdownNow</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">//当前线程结束</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">isShutdown</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">//全部结束</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">isTerminated</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>

<h2 id="4-AbstractExectorService"><a href="#4-AbstractExectorService" class="headerlink" title="4. AbstractExectorService"></a>4. AbstractExectorService</h2><p>&emsp;&emsp;  虽然又是一个抽象类，但是他完全没有一个抽象方法，就像 AQS 一样。然后主要讨论这里面的几个重要方法。他增加了两个重载方法，然后实现了接口的三个 <code>submit()</code> 。</p>
<p>&emsp;&emsp;  先说 <code>submit()</code> 方法，这个方法如果接受的是 <code>Runnable</code> 参数的话就直接调用了 <code>newTaskFor(Runnable runnable, T value)</code> ，在前面会看到对于 <code>Runnable</code> 是有两个重载方法的，那么这里就给 <code>T</code> 传参不同，可以是 <code>null</code> 。然后另外一个传参为 <code>callable</code> 的就调用 <code>newTaskFor(Callable&lt;T&gt; callable)</code> 。可以看到，他们底层都是调用了 <code>execute</code> ，最后返回的还是 <code>RunnableFuture</code>。<code>RunnableFuture</code> 这个就是继承了 <code>Runnable</code> 和 <code>Future</code> 接口的一个空接口。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//  调用的都是 newTaskFor 的 Runnable 参数方法</span></span><br><span class="line"><span class="keyword">public</span> Future&lt;?&gt; submit(Runnable task) &#123;</span><br><span class="line">    <span class="keyword">if</span> (task == <span class="keyword">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    RunnableFuture&lt;Void&gt; ftask = newTaskFor(task, <span class="keyword">null</span>);</span><br><span class="line">    execute(ftask);</span><br><span class="line">    <span class="keyword">return</span> ftask;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> &lt;T&gt; <span class="function">Future&lt;T&gt; <span class="title">submit</span><span class="params">(Runnable task, T result)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (task == <span class="keyword">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    RunnableFuture&lt;T&gt; ftask = newTaskFor(task, result);</span><br><span class="line">    execute(ftask);</span><br><span class="line">    <span class="keyword">return</span> ftask;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//  调用的 newTaskFor 的 Callable 方法</span></span><br><span class="line"><span class="keyword">public</span> &lt;T&gt; <span class="function">Future&lt;T&gt; <span class="title">submit</span><span class="params">(Callable&lt;T&gt; task)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (task == <span class="keyword">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    RunnableFuture&lt;T&gt; ftask = newTaskFor(task);</span><br><span class="line">    execute(ftask);</span><br><span class="line">    <span class="keyword">return</span> ftask;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  好，既然上面的方法都调用了 <code>newTaskFor</code> ，那么这个方法就是 <code>submit</code> 的执行基础，接着看一下这个方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这两个是 AES 的核心方法，主要就是创建任务  是 submit 的依赖</span></span><br><span class="line"><span class="keyword">protected</span> &lt;T&gt; <span class="function">RunnableFuture&lt;T&gt; <span class="title">newTaskFor</span><span class="params">(Runnable runnable, T value)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> FutureTask&lt;T&gt;(runnable, value);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">protected</span> &lt;T&gt; <span class="function">RunnableFuture&lt;T&gt; <span class="title">newTaskFor</span><span class="params">(Callable&lt;T&gt; callable)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">new</span> FutureTask&lt;T&gt;(callable);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  可以看到上面的方法全都是采用了 <code>FutureTask</code> 创建的新任务。返回后交给 <code>execute</code> 方法执行。那很明显， <code>FutureTask</code> 肯定是 <code>RunnableFuture</code> 的子类，确实 <code>FutureTask</code> 只实现了 <code>RunnableFuture</code> 这一个接口。</p>
<h2 id="5-FutureTask-介绍"><a href="#5-FutureTask-介绍" class="headerlink" title="5. FutureTask 介绍"></a>5. FutureTask 介绍</h2><p>&emsp;&emsp;  在前面看到，这个类实现了 RunnableFuture 然后 RunnableFuture 又继承了 Runnable 和 Future ，这个名字取的还真是可以，直接把两个接口组合了。Runnable 我们知道他就只有一个 Run 方法，用来放任务代码。而 Future 又是干嘛的呢。我们来看一下里面的方法，比较少。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 取消任务</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">cancel</span><span class="params">(<span class="keyword">boolean</span> mayInterruptIfRunning)</span></span>;</span><br><span class="line"><span class="comment">// 状态判断</span></span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">isCancelled</span><span class="params">()</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">boolean</span> <span class="title">isDone</span><span class="params">()</span></span>;</span><br><span class="line"><span class="comment">// 阻塞获取结果</span></span><br><span class="line"><span class="function">V <span class="title">get</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException, ExecutionException</span>;</span><br><span class="line"><span class="comment">// 超时阻塞获取结果</span></span><br><span class="line"><span class="function">V <span class="title">get</span><span class="params">(<span class="keyword">long</span> timeout, TimeUnit unit)</span></span></span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  好的，其实就是在原来的任务的基础上加上了关于状态判断和结果获取的方法，主要是结果的阻塞获取是非常重要的，这也是 Runnable 的一大缺陷。</p>
<p>&emsp;&emsp; FutureTask 其实就是 RunnableFuture 的一个实现类。所以说我们大致的看一下这个实现类就好。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 任务状态</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">int</span> state;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> NEW          = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> COMPLETING   = <span class="number">1</span>;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> NORMAL       = <span class="number">2</span>;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> EXCEPTIONAL  = <span class="number">3</span>;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> CANCELLED    = <span class="number">4</span>;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> INTERRUPTING = <span class="number">5</span>;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> INTERRUPTED  = <span class="number">6</span>;</span><br><span class="line"><span class="comment">// 任务</span></span><br><span class="line"><span class="keyword">private</span> Callable&lt;V&gt; callable;</span><br><span class="line"><span class="comment">// 结果</span></span><br><span class="line"><span class="keyword">private</span> Object outcome; <span class="comment">// non-volatile, protected by state reads/writes</span></span><br><span class="line"><span class="comment">// 执行任务的线程，他是 volatile 的</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> Thread runner;</span><br></pre></td></tr></table></figure>

<p>里面的一些其他的方法就是用来对任务进行操作的，主要就是实现了父类的一些方法。里面也采用了 CAS 对线程的设定等待。</p>
<h2 id="6-ThreadPoolExector"><a href="#6-ThreadPoolExector" class="headerlink" title="6. ThreadPoolExector"></a>6. ThreadPoolExector</h2><p>&emsp;&emsp;  然后介绍完了 AES 以后，就是真正的可用的类了，这里我们首先介绍的是 <code>ThreadPoolExector</code> 也就是我们常说的线程池。但是我们很少看到看到我们直接的去 new 一个 <code>ThreadPoolExector</code> 而更常用的是采用 <code>Executors</code> 这个类去创建线程池。主要就是 <code>Executors</code> 算是一个工厂方法，new 一个线程池的过程是比较麻烦的，需要配置很多的参数，这个工厂方法把一些我们会经常用到的的线程池都通过一个工厂方法返回给我们，减少我们的工作量。</p>
<p>&emsp;&emsp;  这个类其实也是比较复杂的，他的代码比较多，这里只分析一部分比较重要的参数和方法。</p>
<h3 id="1-字段"><a href="#1-字段" class="headerlink" title="1. 字段"></a>1. 字段</h3><h4 id="1-基本字段"><a href="#1-基本字段" class="headerlink" title="1. 基本字段"></a>1. 基本字段</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 任务的后备队列</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> BlockingQueue&lt;Runnable&gt; workQueue;</span><br><span class="line">    <span class="comment">// 锁</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> ReentrantLock mainLock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line">    <span class="comment">// 用来支持等待中断的</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> Condition termination = mainLock.newCondition();</span><br><span class="line">    <span class="comment">// 存放的工作线程，只有当获取到锁的时候才能访问这个 Set</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> HashSet&lt;Worker&gt; workers = <span class="keyword">new</span> HashSet&lt;Worker&gt;();</span><br><span class="line">    <span class="comment">// 线程池最大数量</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> largestPoolSize;</span><br><span class="line">    <span class="comment">// 完成的线程数，只有在获取锁的时候才能更新这个值</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">long</span> completedTaskCount;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//==============================================================================</span></span><br><span class="line">    <span class="comment">// 这里有提到用户自定义的变量我们都是用 volatile 来修饰 以保证获取到最新的值</span></span><br><span class="line">    <span class="comment">//==============================================================================</span></span><br><span class="line">    <span class="comment">// 线程创建工厂类</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> ThreadFactory threadFactory;</span><br><span class="line">    <span class="comment">// 当任务队列饱和或者线程池关闭后 再往里面提交任务时候的执行策略</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> RejectedExecutionHandler handler;</span><br><span class="line">    <span class="comment">// 默认的执行策略是采用的 AbortPolicy (这是一个函数式接口的子类，里面实现的方法默认是抛异常)</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> RejectedExecutionHandler defaultHandler = <span class="keyword">new</span> AbortPolicy();</span><br><span class="line">    <span class="comment">// 非核心线程的存活时间</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">long</span> keepAliveTime;</span><br><span class="line">    <span class="comment">// 是否允许核心线程具有存活时间，允许则上面的参数也会作用于核心线程</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">boolean</span> allowCoreThreadTimeOut;</span><br><span class="line">    <span class="comment">// 核心线程的大小</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">int</span> corePoolSize;</span><br><span class="line">    <span class="comment">// 最大线程数</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">int</span> maximumPoolSize;</span><br><span class="line">    <span class="comment">// 池控参数  非常重要！！！！</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> AtomicInteger ctl = <span class="keyword">new</span> AtomicInteger(ctlOf(RUNNING, <span class="number">0</span>));</span><br><span class="line">    <span class="comment">// ctl 的解包 -&gt; workerCount 和 runState</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">runStateOf</span><span class="params">(<span class="keyword">int</span> c)</span>     </span>&#123; <span class="keyword">return</span> c &amp; ~CAPACITY; &#125;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">workerCountOf</span><span class="params">(<span class="keyword">int</span> c)</span>  </span>&#123; <span class="keyword">return</span> c &amp; CAPACITY; &#125;</span><br><span class="line">    <span class="comment">// 打包操作 两个变量或一下</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> <span class="title">ctlOf</span><span class="params">(<span class="keyword">int</span> rs, <span class="keyword">int</span> wc)</span> </span>&#123; <span class="keyword">return</span> rs | wc; &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  其中比较重要的属性有： workQueue(任务队列)、一个 Set<worker>(线程集合)、池参数、线程工厂类、拒绝策略、池控参数。</worker></p>
<p>&emsp;&emsp;  其中池控参数是将两个只是变量都打包进去了，分别是 workerCount 和 runState 。</p>
<p>&emsp;&emsp;    workerCount有效线程数.runState表明线程池的状态是否为运行，还是关闭。为了方便表示我们把 workerCount 和 runState 打包到了一个变量里面就是 ctl。<br>runState 表示生命周期，有以下状态：</p>
<ol>
<li><p>RUNNING：接受新任务并处理排队的任务</p>
</li>
<li><p>SHUTDOWN：不接受新任务，但处理排队的任务</p>
</li>
<li><p>STOP：不接受新任务，不处理排队的任务，并中断正在进行的任务</p>
</li>
<li><p>TIDYING：所有任务都已终止，workerCount为零，线程转换到状态TIDYING 将运行terminate() 勾子</p>
</li>
<li><p>TERMINATED：terminated()已完成</p>
<p>&emsp;&emsp; 并且这些值之间顺序很重要，以允许有序的比较。 runState在整个过程中是单调递增的但不需要经过每一个状态，具体规律如下：</p>
</li>
</ol>
<ul>
<li>RUNNING -&gt; SHUTDOWN  在执行 shutdown()的时候</li>
<li>(RUNNING or SHUTDOWN) -&gt; STOP    在执行shutdownNow()</li>
<li>SHUTDOWN -&gt; TIDYING      当任务队列和线程池为空的时候</li>
<li>STOP -&gt; TIDYING      当池为空的时候</li>
<li>TIDYING -&gt; TERMINATED    钩子方法调用完毕</li>
</ul>
<h4 id="2-Worker"><a href="#2-Worker" class="headerlink" title="2. Worker"></a>2. Worker</h4><p>&emsp;&emsp;  这个类其实比较有意思，一般我们会想到它里面必然是有一个线程的引用的，也就是把线程用 Worker 类来包装一下。然后还有一个就是他当前正在执行的任务引用。<br>&emsp;&emsp;  但是我们看这个类的继承及实现的时候就会发现比较有意思的事情，他继承了 AQS 实现了 Runnale 也就是他此时从结构来看既是一个 <code>同步器</code> 还是一个 <code>任务</code>。</p>
<h5 id="1-字段-1"><a href="#1-字段-1" class="headerlink" title="1. 字段"></a>1. 字段</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 线程</span></span><br><span class="line"><span class="keyword">final</span> Thread thread;</span><br><span class="line"><span class="comment">// 该线程当前领到的任务</span></span><br><span class="line">Runnable firstTask;</span><br></pre></td></tr></table></figure>

<h5 id="2-方法"><a href="#2-方法" class="headerlink" title="2. 方法"></a>2. 方法</h5><p>&emsp;&emsp;  里面比较重要的就是一些锁的实现，这里采用的全是互斥锁。然后实现的 Runnable 的 run 就是执行当前的任务。待会看运用这些方法再具体分析，里面的代码不多。</p>
<h4 id="3-BlockQueue"><a href="#3-BlockQueue" class="headerlink" title="3. BlockQueue"></a>3. BlockQueue</h4><p>&emsp;&emsp;  这里采用了 BlockQueue 来存放任务，然后 BlockQueue 其实是一个接口，他有很多子类，而我们使用 Exectors 工厂方法 new 出新的线程池的时候其实不同种类的线程池采用的也是不同的 BlockQueue 。<br>&emsp;&emsp;  在 doc 中提到的子类有：ArrayBlockingQueue, DelayQueue, LinkedBlockingDeque, LinkedBlockingQueue, PriorityBlockingQueue, SynchronousQueue 但是我们真正常用的就是 ：ArrayBlockingQueue,LinkedBlockingQueue, SynchronousQueue 而我们在 Exectors 中就用了 LinkedBlockingQueue, SynchronousQueue 这两种。这个到时候会专门写一篇关于阻塞队列的博客，不然这个文章的篇幅太大了。</p>
<h4 id="4-ThreadFactory"><a href="#4-ThreadFactory" class="headerlink" title="4. ThreadFactory"></a>4. ThreadFactory</h4><p>&emsp;&emsp;  好很明显，这是一个函数式接口，里面就只有一个 newThread 方法，在线程池中，如果我们没有自己传入的话采用的都是 <code>Executors.defaultThreadFactory()</code>  创建一个 非 Daemon 优先级为 NORM_PRIORITY 的线程。<br>这样主线程退出时不会直接退出JVM，而是等待线程池中的线程结束。所有线程都调为同一个级别，这样在操作系统角度来看所有系统都是公平的，不会导致竞争堆积。</p>
<h3 id="2-主要方法"><a href="#2-主要方法" class="headerlink" title="2. 主要方法"></a>2. 主要方法</h3><h4 id="1-ctor"><a href="#1-ctor" class="headerlink" title="1. ctor"></a>1. ctor</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ThreadPoolExecutor</span><span class="params">(<span class="keyword">int</span> corePoolSize,  //核心线程容量</span></span></span><br><span class="line"><span class="function"><span class="params">                          <span class="keyword">int</span> maximumPoolSize,//线程池的允许线程的最大容量</span></span></span><br><span class="line"><span class="function"><span class="params">                          <span class="keyword">long</span> keepAliveTime,//存活时间</span></span></span><br><span class="line"><span class="function"><span class="params">                          TimeUnit unit,//时间单位</span></span></span><br><span class="line"><span class="function"><span class="params">                          BlockingQueue&lt;Runnable&gt; workQueue,//任务队列</span></span></span><br><span class="line"><span class="function"><span class="params">                                                                             ThreadFactory threadFactory)</span> </span>&#123;<span class="comment">//线程工厂</span></span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-execute"><a href="#2-execute" class="headerlink" title="2. execute"></a>2. execute</h4><p>&emsp;&emsp;  这个方法是把提交到线程池中的任务在将来的某个时间运行起来，但是不一定能够保证这个任务肯定能执行到，为什么这么说？这是由于我们在前面看到提交到线程池中的线程不一定被马上执行，如果说线程池中线程都在忙现有的任务，新提交的就会被放到任务队列中 (BlockQueue)，然而我们在上面还看到了一个拒绝策略，就是当线程池饱和或者线程池马上关闭了提交的任务会被拒绝，在这里就是抛出异常！</p>
<p>&emsp;&emsp;  好，现在来理一下 <code>execute</code> 方法的执行过程。</p>
<ol>
<li><p>如果少于corePoolSize线程正在运行，开始一个新的线程领取任务。 对addWorker的调用会自动检查runState和workerCount，从而防止可能会添加的错误警报线程时它不应该通过返回false。</p>
</li>
<li><p>把任务放入后备队列，如果成功我们还是需要再次检查，因为自上次检查依赖我们可能遇到线程池关闭<br>如果有必要的话需要回退任务队列</p>
</li>
<li><p>如果排队失败，我们会尝试添加一个新的线程。而线程添加失败则说明线程池关闭了或者<br>已经处于饱和状态</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">execute</span><span class="params">(Runnable command)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (command == <span class="keyword">null</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">        <span class="keyword">int</span> c = ctl.get();</span><br><span class="line">        <span class="comment">// 如果正在运行的线程数小于核心池大小 可以添加一个新的线程领取这个任务</span></span><br><span class="line">        <span class="keyword">if</span> (workerCountOf(c) &lt; corePoolSize) &#123;</span><br><span class="line">            <span class="keyword">if</span> (addWorker(command, <span class="keyword">true</span>))</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            <span class="comment">// 如果说添加新线程失败了我们需要重新获取线程池的工作程数量，可能有变化</span></span><br><span class="line">            <span class="comment">// 等看到 addWorker 的时候再说</span></span><br><span class="line">            c = ctl.get();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 如果线程池在工作状态，我们就把这个任务放到后备队列</span></span><br><span class="line">        <span class="keyword">if</span> (isRunning(c) &amp;&amp; workQueue.offer(command)) &#123;</span><br><span class="line">            <span class="comment">// 重新检查 ctl</span></span><br><span class="line">            <span class="keyword">int</span> recheck = ctl.get();</span><br><span class="line">            <span class="comment">// 如果线程池关闭了，我们需要做回退动作，也就是撤销刚才放入的任务</span></span><br><span class="line">            <span class="comment">// 如果撤销成功，执行拒绝策略</span></span><br><span class="line">            <span class="keyword">if</span> (! isRunning(recheck) &amp;&amp; remove(command))</span><br><span class="line">                reject(command);</span><br><span class="line">            <span class="comment">// 如果撤销失败，并且没有工作线程不管他</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (workerCountOf(recheck) == <span class="number">0</span>)</span><br><span class="line">                addWorker(<span class="keyword">null</span>, <span class="keyword">false</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 线程池已经关闭了 添加线程也失败则执行拒绝</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (!addWorker(command, <span class="keyword">false</span>))</span><br><span class="line">            reject(command);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-addWorker"><a href="#3-addWorker" class="headerlink" title="3. addWorker"></a>3. addWorker</h4><p>&emsp;&emsp;  这个方法的功能是：根据当前线程池的状态和给的边界条件来检测是否需要添加新的线程，如果是，则添加到线程队列中并调整工作线程数并启动线程执行第一个任务。 如果该方法检测到线程池处于STOP状态或者是察觉到将要停止，则返回false。 如果线程工厂创建线程失败(可能是由于发生了OOM异常)则也返回false。</p>
<p>今天有点累  ，先写到这里！明天继续，清明小长假还要继续学习  ：( </p>
]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>CountDownLatch 源码分析</title>
    <url>/2018/04/03/Java%20SourceCode/CountDownLatch%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="CountDownLatch-源码分析"><a href="#CountDownLatch-源码分析" class="headerlink" title="CountDownLatch 源码分析"></a>CountDownLatch 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-基本介绍"><a href="#1-基本介绍" class="headerlink" title="1. 基本介绍"></a>1. 基本介绍</h2><p>&emsp;&emsp;  <code>Latch</code> 这个单词的意思就是 “闭锁” ，这也是 jdk1.5 引入的一个并发组件，名字听上去并没有让我们对他的功能并没有什么直观的感受。先粗略解释一下他的功能：我们定义了一个带有数值 n 的 Condition 对象，然后我们调用这个对象上的 <code>await</code> 方法，如果说我们要这个线程继续执行，我们不是调用 <code>signal</code> 而是调用 n 次 <code>countDown</code> 方法。这样等待在这个 Condition 上的所有线程都将被唤醒。</p>
<a id="more"></a>
<p>&emsp;&emsp;  好先来举个例子理解一下上面的文字。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CountDownLatchTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        CountDownLatch latch1 = <span class="keyword">new</span> CountDownLatch(<span class="number">1</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">new</span> Thread(()-&gt;&#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    latch1.await();</span><br><span class="line">                    System.out.println(Thread.currentThread().getName());</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).start();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"main must done something !"</span>);</span><br><span class="line">        Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">        latch1.countDown();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>先打印了</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">main must done something !</span><br></pre></td></tr></table></figure>

<p>隔了一秒钟打印了</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Thread-0</span><br><span class="line">Thread-1</span><br><span class="line">Thread-2</span><br><span class="line">Thread-3</span><br><span class="line">Thread-4</span><br><span class="line">Thread-5</span><br><span class="line">Thread-8</span><br><span class="line">Thread-7</span><br><span class="line">Thread-6</span><br><span class="line">Thread-9</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  很明显，这个组件非常适合我们在一些线程开始之前需要完成 n 件其他的准备工作，每完成一件我们就执行一次 <code>countDown()</code> 也就是计数值减一，唯有在计数值为 0 时我们的线程才会被唤醒继续执行。</p>
<h2 id="2-结构"><a href="#2-结构" class="headerlink" title="2. 结构"></a>2. 结构</h2><p>&emsp;&emsp;  好的，先给一张类图看看他的结构。<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-4-3/35332474.jpg" alt></p>
<p>&emsp;&emsp;  毫无疑问，底层还是采用了 AQS 作为基础并发组件。并且这个类要比我们想像中的简单的多。这个类没有继承和实现任何的接口及父类，他但一个独立的组件，里面就封装了 AQS。</p>
<h3 id="3-主要方法分析"><a href="#3-主要方法分析" class="headerlink" title="3. 主要方法分析"></a>3. 主要方法分析</h3><p>&emsp;&emsp;  其实这这个类里面只有两个有价值的方法，就是 await 和 countDown ，但是这两个方法全都是委托给了 AQS 的 acquireSharedInterruptibly 和 releaseShared 。好了又回到了 AQS ，那么上面的这个 Sync 肯定需要对 tryAcquireShared 和 tryReleaseShared 进行重写。所以说我们只需要分析两个方法。</p>
<h4 id="1-tryAcquireShared"><a href="#1-tryAcquireShared" class="headerlink" title="1. tryAcquireShared"></a>1. tryAcquireShared</h4><p>&emsp;&emsp;  逻辑超简单，当 state 变量为 0 就返回 1 ，否则返回 -1 也就是失败。很明显我们调用了 await 肯定是当时的 state 值不为 0，自然会阻塞，并尝试唤醒后面的线程。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">int</span> <span class="title">tryAcquireShared</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">           <span class="keyword">return</span> (getState() == <span class="number">0</span>) ? <span class="number">1</span> : -<span class="number">1</span>;</span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-tryReleaseShared"><a href="#2-tryReleaseShared" class="headerlink" title="2. tryReleaseShared"></a>2. tryReleaseShared</h4><p>countDown 就是让信号量减一操作，所以说他就是进行了减一操作，当为 0 的时候，就会触发唤醒后继节点的操作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">tryReleaseShared</span><span class="params">(<span class="keyword">int</span> releases)</span> </span>&#123;</span><br><span class="line">            <span class="comment">// Decrement count; signal when transition to zero</span></span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="keyword">int</span> c = getState();</span><br><span class="line">                <span class="keyword">if</span> (c == <span class="number">0</span>)</span><br><span class="line">                    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">                <span class="keyword">int</span> nextc = c-<span class="number">1</span>;</span><br><span class="line">                <span class="keyword">if</span> (compareAndSetState(c, nextc))</span><br><span class="line">                    <span class="keyword">return</span> nextc == <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>AQS 与 Sync 源码分析</title>
    <url>/2018/04/01/Java%20SourceCode/AQS%20%E4%B8%8E%20Sync%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="ReentrantReadWriteLock-源码分析"><a href="#ReentrantReadWriteLock-源码分析" class="headerlink" title="ReentrantReadWriteLock 源码分析"></a>ReentrantReadWriteLock 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<p>&emsp;&emsp;  虽然前面几篇文章，已经分析过很多次 AQS 但是有时候分析分析着就会陷入一种调用链分不清楚的情况。为了更好的理解 AQS 中的锁机制，专门用一篇文章分析 AQS 结构，理清调用关系！</p>
<a id="more"></a>
<h2 id="1-AQS-结构分析"><a href="#1-AQS-结构分析" class="headerlink" title="1. AQS 结构分析"></a>1. AQS 结构分析</h2><p><img src="http://ojrw3x8j2.bkt.clouddn.com/18-4-1/98760195.jpg" alt></p>
<p>&emsp;&emsp;好了，一开始我们就能明显的看到有两个内部类 Node 和 ConditionObject 。这是 AQS 中的重点，其中 Node 是 AQS 中的线程表示，而 ConditionObject 则是等待唤醒机制的核心。</p>
<p>&emsp;&emsp;  接着就是一些重要方法：</p>
<ul>
<li>enq</li>
<li>addWaiter</li>
<li>setHead</li>
<li>unparkSuccessor</li>
<li>doReleaseShared</li>
<li>setHeadAndPropagate</li>
<li>cancelAcquire</li>
<li>acquireQueued</li>
<li>doAcquireInterruptibly</li>
<li>doAcquireShared</li>
<li>doAcquireSharedInterruptibly</li>
<li>tryAcquire</li>
<li>tryRelease</li>
<li>tryAcquireShared</li>
<li>tryReleaseShared</li>
<li>acquire</li>
<li>acquireInterruptibly</li>
<li>acquireShared</li>
<li>acquireSharedInterruptibly</li>
<li>release</li>
<li>releaseShared</li>
<li>hasQueuedThreads</li>
<li>hasQueuedPredecessors</li>
<li>transferForSignal</li>
<li>fullyRelease</li>
</ul>
<h2 id="2-Node-节点和-ConditionObject"><a href="#2-Node-节点和-ConditionObject" class="headerlink" title="2. Node 节点和 ConditionObject"></a>2. Node 节点和 ConditionObject</h2><h3 id="1-Node-节点"><a href="#1-Node-节点" class="headerlink" title="1. Node 节点"></a>1. Node 节点</h3><p>&emsp;&emsp;  Node 节点其实就是我们对线程的一个封装，让线程能够被加入到我们设定的队列中进行等待或唤醒，其中里面有几个特别重要的属性：</p>
<ol>
<li>next，pre 这是对于同步队列中的线程来说的，构建的双向链表</li>
<li>nextWaiter 则是给等待队列使用的单向链表</li>
<li>thread 线程引用</li>
<li>waitStatus 等待状态 </li>
</ol>
<h3 id="2-ConditionObject"><a href="#2-ConditionObject" class="headerlink" title="2. ConditionObject"></a>2. ConditionObject</h3><p>&emsp;&emsp;  这个对象就是一个等待队列的管理者，里面有对线程进行等待唤醒机制的操作。</p>
<p>&emsp;&emsp;  重要属性：first/lastWaiter 就是用来指示链表的首尾节点。</p>
<p>&emsp;&emsp;  重要方法：</p>
<ol>
<li>await 首先需要判断是否被中断，然后加入等待队列(addConditionWaiter)，释放他具有的锁 (fullyRelease).然后用死循环检测自己是否在同步队列，并将自己 park。清理等待队列中的非 Condition 的任务。</li>
<li>signal-&gt;doSignal ，用 while 循环将线程从等待队列中移除，然后调用尝试放入同步队列(transferForSignal)并unpark。否则的话继续在等待队列中找。</li>
</ol>
<h2 id="3-重要方法调用过程"><a href="#3-重要方法调用过程" class="headerlink" title="3. 重要方法调用过程"></a>3. 重要方法调用过程</h2><h3 id="1-acquire"><a href="#1-acquire" class="headerlink" title="1. acquire"></a>1. acquire</h3><p>&emsp;&emsp;  获取独占锁，首先使用尝试获取锁(tryAcquire)，失败则加入同步队列(addWaiter),然后如果是队列中的第一个等待者自旋获取锁。<br>&emsp;&emsp;  一般来说 tryAcquire 是一个模板方法，在 AQS 中没有实现，然后其他的并发组件中使用了 AQS 后必然实现自己的 tryAcquire 因为不同的锁类型实现方式不同。使用图例解释一下执行过程。<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-4-3/62101431.jpg" alt></p>
<h3 id="2-releas"><a href="#2-releas" class="headerlink" title="2. releas"></a>2. releas</h3><p>&emsp;&emsp;  这个逻辑更加简单，首先尝试释放锁(tryRelease)，成功则唤醒后继节点(unparkSuccessor)。</p>
<h3 id="3-acquireShared"><a href="#3-acquireShared" class="headerlink" title="3. acquireShared"></a>3. acquireShared</h3><p>&emsp;&emsp;  先尝试获取共享锁(tryAcquireShared),失败则加入同步队列如果是头结点自旋获取锁，并尝试唤醒后继节点(doAcquireSharedInterruptibly)。基本是 acquire 中的 addWaiter 和 acquireQueued 的组合。</p>
<h3 id="4-releaseShared"><a href="#4-releaseShared" class="headerlink" title="4. releaseShared"></a>4. releaseShared</h3><p>&emsp;&emsp;  这个和 release 有点类似，但是里面方法不太一样，首先还是调用了 tryReleaseShared 成功后，传播释放后继节点 doReleaseShared。这里面主要还是调用了 unparkSuccessor，但是还有标记传播的阶段。</p>
]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>ReentrantReadWriteLock 源码分析</title>
    <url>/2018/04/01/Java%20SourceCode/ReentrantReadWriteLock%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="ReentrantReadWriteLock-源码分析"><a href="#ReentrantReadWriteLock-源码分析" class="headerlink" title="ReentrantReadWriteLock 源码分析"></a>ReentrantReadWriteLock 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-概述"><a href="#1-概述" class="headerlink" title="1. 概述"></a>1. 概述</h2><p>&emsp;&emsp;  这个类听名字好像是和 ReentrantLock 差不多，但是实际上他们两没有任何关系，他并没有直接或间接的继承 ReentrantLock。ReentrantLock 属于独占锁，也就是我们前面所说的在临界区之内只能有一个线程运行。比如说我们的 Hashtable 采用的就是这种方式，哪怕在 get 元素的时候都对表加了锁，其他线程希望读取都没办法，但事实上我们知道多个线程同时读不会引起安全问题。至于什么时候会出现安全问题，这里介绍一个操作系统中常提到的 <code>Bernstein条件</code> ，概括的来说就是两个线程对同一个资源不能同时进行如下操作： <code>读写</code>、<code>写读</code>、<code>写写</code> 。所以我们对数据进行并发访问是不会有问题的，于是诞生了 读锁 和 写锁的概念，在 Java 中提供的 <code>ReentrantReadWriteLock</code> 就是一个具体实现。</p>
<a id="more"></a>
<p>&emsp;&emsp;  对于 ReentrantReadWriteLock，当写操作时，其它线程无法读取或写入数据，而当读操作时，其它线程无法写数据，但却可以读取数据。</p>
<p>&emsp;&emsp;  介绍一下线程进入读写锁的条件。</p>
<ol>
<li>读锁：没有其他线程的写锁，没有其他线程的写请求。</li>
<li>写锁：没有其他线程的读锁，没有其他线程的写锁。</li>
</ol>
<p>&emsp;&emsp;  这个锁有以下的特性：</p>
<ol>
<li>WriteLock 中可以加 ReadLock。反之不可！</li>
<li>WriteLock 可以降级为 ReadLock，反之不可！</li>
<li>获取锁可被中断。</li>
<li>锁数量有限制。</li>
</ol>
<h3 id="1-实现"><a href="#1-实现" class="headerlink" title="1. 实现"></a>1. 实现</h3><p>实现了 ReadWriteLock 接口，里面就两个方法让返回读写锁。</p>
<h3 id="2-字段"><a href="#2-字段" class="headerlink" title="2. 字段"></a>2. 字段</h3><p>三个字段，一个读锁，一个写锁，一个锁的实现核心 sync。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 维护两个锁，这两个锁里面的实现就是 sync</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ReentrantReadWriteLock.ReadLock readerLock;</span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> ReentrantReadWriteLock.WriteLock writerLock;</span><br><span class="line"><span class="keyword">final</span> Sync sync;</span><br></pre></td></tr></table></figure>

<h3 id="3-结构"><a href="#3-结构" class="headerlink" title="3. 结构"></a>3. 结构</h3><p><img src="http://ojrw3x8j2.bkt.clouddn.com/18-4-1/29922994.jpg" alt></p>
<p>&emsp;&emsp;  还是似曾相识的结构，里面采用了 AQS 衍生出来的 Sync 以及两个公平锁和非公平锁。接着就是两个新的内部类，分别是读锁，和写锁。里面引用了 sync 核心组件。现在可以说明的是，读锁采用的是共享锁，而写锁使用独占锁。也就是把 AQS 中的两类方法都用上了。</p>
<h2 id="2-ReadLock-实现"><a href="#2-ReadLock-实现" class="headerlink" title="2. ReadLock 实现"></a>2. ReadLock 实现</h2><h3 id="1-lock"><a href="#1-lock" class="headerlink" title="1. lock"></a>1. lock</h3><p>&emsp;&emsp;  lock 方法直接调用了 acquireShared ，在前面我们已经分析过好多次 acquireShared 方法，这里再大概说一下逻辑：先调用 tryAcquireShared 尝试获取锁，如果获取失败，则调用 doAcquireShared 加入等待队列，尝试自旋获取锁，并且唤醒同步队列中的线程。</p>
<p>&emsp;&emsp;  这里重点说一下 tryAcquireShared 方法，因为在公平锁和非公平锁中实现不同，所以放到了子类中实现，但是这里公平和非公平是一样的，都在 Sync 中实现，在 <code>FairSync/NonfairSync</code> 子类中只是实现了是否需要阻塞读写线程的判断条件。在 tryAcquireShared 中核心思想是这样的：</p>
<ol>
<li>如果没有非当前线程的写锁，则可以继续开始获取，否则返回失败。但是当前线程的写锁和要加读锁不冲突，这也就解释了上面提到的读锁中可重入写锁，反之不可以，不可以的情况待会再解释。</li>
<li>如果当前线程不用等待，并且未达到读上限，读数量更新成功，没有并发抢占此方法则可以开始获取锁。</li>
<li>更新读锁的重入次数。里面采用了缓存机制。</li>
<li>出现等待，达到读上限，有并发抢占，再接着重试获取锁，重试获取锁和这里的逻辑一样，只是做了更加详细的判断在并发情况下更适用。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 获取读锁</span></span><br><span class="line">        <span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">tryAcquireShared</span><span class="params">(<span class="keyword">int</span> unused)</span> </span>&#123;</span><br><span class="line">            Thread current = Thread.currentThread();</span><br><span class="line">            <span class="keyword">int</span> c = getState();</span><br><span class="line">            <span class="comment">// 有其他线程的写锁，直接失败   自己线程的写锁可以允许</span></span><br><span class="line">            <span class="keyword">if</span> (exclusiveCount(c) != <span class="number">0</span> &amp;&amp; getExclusiveOwnerThread() != current)</span><br><span class="line">                <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">            <span class="keyword">int</span> r = sharedCount(c);</span><br><span class="line">            <span class="comment">// 如果当前线程不用等待，并且未达到读上限，没有并发抢占此方法     可以获取读锁</span></span><br><span class="line">            <span class="keyword">if</span> (!readerShouldBlock() &amp;&amp; r &lt; MAX_COUNT &amp;&amp; compareAndSetState(c, c + SHARED_UNIT)) &#123;</span><br><span class="line">                <span class="comment">// 唯一一个读线程</span></span><br><span class="line">                <span class="keyword">if</span> (r == <span class="number">0</span>) &#123;</span><br><span class="line">                    firstReader = current;</span><br><span class="line">                    firstReaderHoldCount = <span class="number">1</span>;</span><br><span class="line">                <span class="comment">// 可重入读线程</span></span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (firstReader == current) &#123;</span><br><span class="line">                    firstReaderHoldCount++;</span><br><span class="line">                <span class="comment">//  加入读线程，更新重入次数</span></span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    <span class="comment">// 这一系列操作只是为了获取到当前线程的重入次数，本来直接用 readHolds.get() 就能搞定的，但是这里写了一大堆</span></span><br><span class="line">                    <span class="comment">// 是为了缓存，减少 readHolds.get() 开销</span></span><br><span class="line">                    HoldCounter rh = cachedHoldCounter;</span><br><span class="line">                    <span class="keyword">if</span> (rh == <span class="keyword">null</span> || rh.tid != getThreadId(current))</span><br><span class="line">                        cachedHoldCounter = rh = readHolds.get();</span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (rh.count == <span class="number">0</span>)</span><br><span class="line">                        readHolds.set(rh);</span><br><span class="line">                    rh.count++;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 出现等待，达到读上限，有并发抢占，再接着重试获取锁</span></span><br><span class="line">            <span class="keyword">return</span> fullTryAcquireShared(current);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 这个代码就是上面的代码的重复，但是他在非并发情况下会更简单 条件判断的更加详细，其余真的没什么了</span></span><br><span class="line">        <span class="function"><span class="keyword">final</span> <span class="keyword">int</span> <span class="title">fullTryAcquireShared</span><span class="params">(Thread current)</span> </span>&#123;</span><br><span class="line">            HoldCounter rh = <span class="keyword">null</span>;</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="keyword">int</span> c = getState();</span><br><span class="line">                <span class="comment">// 与上面等价  不能有其他线程的写</span></span><br><span class="line">                <span class="keyword">if</span> (exclusiveCount(c) != <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (getExclusiveOwnerThread() != current)</span><br><span class="line">                        <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">                <span class="comment">// 准备获取锁</span></span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (readerShouldBlock()) &#123;</span><br><span class="line">                    <span class="comment">// 不请求重入锁，只是为了判断 firstReaderHoldCount &gt; 0  ？？为啥</span></span><br><span class="line">                    <span class="keyword">if</span> (firstReader == current) &#123;</span><br><span class="line">                        <span class="comment">// assert firstReaderHoldCount &gt; 0;</span></span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="comment">// 这么大一段就是为了删除重入数为 0 的线程</span></span><br><span class="line">                        <span class="keyword">if</span> (rh == <span class="keyword">null</span>) &#123;</span><br><span class="line">                            rh = cachedHoldCounter;</span><br><span class="line">                            <span class="keyword">if</span> (rh == <span class="keyword">null</span> || rh.tid != getThreadId(current)) &#123;</span><br><span class="line">                                rh = readHolds.get();</span><br><span class="line">                                <span class="keyword">if</span> (rh.count == <span class="number">0</span>)</span><br><span class="line">                                    readHolds.remove();</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="keyword">if</span> (rh.count == <span class="number">0</span>)</span><br><span class="line">                            <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 不能超限</span></span><br><span class="line">                <span class="keyword">if</span> (sharedCount(c) == MAX_COUNT)</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">                <span class="comment">// 重复代码</span></span><br><span class="line">                <span class="keyword">if</span> (compareAndSetState(c, c + SHARED_UNIT)) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (sharedCount(c) == <span class="number">0</span>) &#123;</span><br><span class="line">                        firstReader = current;</span><br><span class="line">                        firstReaderHoldCount = <span class="number">1</span>;</span><br><span class="line">                    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (firstReader == current) &#123;</span><br><span class="line">                        firstReaderHoldCount++;</span><br><span class="line">                    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="keyword">if</span> (rh == <span class="keyword">null</span>)</span><br><span class="line">                            rh = cachedHoldCounter;</span><br><span class="line">                        <span class="keyword">if</span> (rh == <span class="keyword">null</span> || rh.tid != getThreadId(current))</span><br><span class="line">                            rh = readHolds.get();</span><br><span class="line">                        <span class="keyword">else</span> <span class="keyword">if</span> (rh.count == <span class="number">0</span>)</span><br><span class="line">                            readHolds.set(rh);</span><br><span class="line">                        rh.count++;</span><br><span class="line">                        cachedHoldCounter = rh; <span class="comment">// cache for release</span></span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-unlock"><a href="#2-unlock" class="headerlink" title="2. unlock"></a>2. unlock</h3><p>&emsp;&emsp;  unlock 也是调用了 <code>releaseShared(1)</code> ,然后里面的逻辑也是说过的，首先尝试释放当前线程执行 <code>tryReleaseShared</code> ，如果成功 <code>doReleaseShared</code> 唤醒后继线程。</p>
<p>&emsp;&emsp;  还是 <code>tryReleaseShared</code> 方法。就是对读锁的重入次数进行减，删除那些计数值为0 的线程。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 释放读锁</span></span><br><span class="line">        <span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryReleaseShared</span><span class="params">(<span class="keyword">int</span> unused)</span> </span>&#123;</span><br><span class="line">            Thread current = Thread.currentThread();</span><br><span class="line">            <span class="comment">// 第一个读锁</span></span><br><span class="line">            <span class="keyword">if</span> (firstReader == current) &#123;</span><br><span class="line">                <span class="comment">// assert firstReaderHoldCount &gt; 0;</span></span><br><span class="line">                <span class="keyword">if</span> (firstReaderHoldCount == <span class="number">1</span>)</span><br><span class="line">                    firstReader = <span class="keyword">null</span>;</span><br><span class="line">                <span class="keyword">else</span></span><br><span class="line">                    firstReaderHoldCount--;</span><br><span class="line">            <span class="comment">// 查找锁</span></span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                HoldCounter rh = cachedHoldCounter;</span><br><span class="line">                <span class="keyword">if</span> (rh == <span class="keyword">null</span> || rh.tid != getThreadId(current))</span><br><span class="line">                    rh = readHolds.get();</span><br><span class="line">                <span class="keyword">int</span> count = rh.count;</span><br><span class="line">                <span class="keyword">if</span> (count &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">                    readHolds.remove();</span><br><span class="line">                    <span class="keyword">if</span> (count &lt;= <span class="number">0</span>)</span><br><span class="line">                        <span class="keyword">throw</span> unmatchedUnlockException();</span><br><span class="line">                &#125;</span><br><span class="line">                --rh.count;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="keyword">int</span> c = getState();</span><br><span class="line">                <span class="keyword">int</span> nextc = c - SHARED_UNIT;</span><br><span class="line">                <span class="comment">// 没有读锁对读线程没有影响，但是对写线程有影响的</span></span><br><span class="line">                <span class="keyword">if</span> (compareAndSetState(c, nextc))</span><br><span class="line">                    <span class="keyword">return</span> nextc == <span class="number">0</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-WriteLock-实现"><a href="#3-WriteLock-实现" class="headerlink" title="3. WriteLock 实现"></a>3. WriteLock 实现</h2><h3 id="1-lock-1"><a href="#1-lock-1" class="headerlink" title="1. lock"></a>1. lock</h3><p>&emsp;&emsp;  调用 <code>acquire(1)</code> ，这个方法逻辑我们已经清楚了，现在就看一下 <code>tryAcquire()</code> 。先介绍一下基本思路：</p>
<ol>
<li>如果没有任何读写线程直接获取。</li>
<li>要等待或者有竞争更新则失败</li>
<li>有读线程或者非当前写、超过写重入限失败，否则更新重入次数，也就是设置 state 的值。这里也就解释了写锁中可以重入读锁（必须为当前线程的写锁），但是写锁中不允许有任何的读锁。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 读锁获取</span></span><br><span class="line">       <span class="comment">/*</span></span><br><span class="line"><span class="comment">       1. 没任何锁直接获取</span></span><br><span class="line"><span class="comment">       2. 有读锁或非当前写，失败（也就是为什前面提到的在读锁中不能重入写锁的原因，但是反过来可以必须是当前写）</span></span><br><span class="line"><span class="comment">       3. 如果有等待条件失败</span></span><br><span class="line"><span class="comment">        */</span></span><br><span class="line">       <span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">           Thread current = Thread.currentThread();</span><br><span class="line">           <span class="keyword">int</span> c = getState();</span><br><span class="line">           <span class="keyword">int</span> w = exclusiveCount(c);</span><br><span class="line">           <span class="comment">// 可能有读写线程</span></span><br><span class="line">           <span class="keyword">if</span> (c != <span class="number">0</span>) &#123;</span><br><span class="line">               <span class="comment">// (Note: if c != 0 and w == 0 then shared count != 0)</span></span><br><span class="line">               <span class="comment">// 有读线程或者非当前写线程不可获取写锁 巧妙！！！</span></span><br><span class="line">               <span class="keyword">if</span> (w == <span class="number">0</span> || current != getExclusiveOwnerThread())</span><br><span class="line">                   <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">               <span class="comment">// 超过锁容量</span></span><br><span class="line">               <span class="keyword">if</span> (w + exclusiveCount(acquires) &gt; MAX_COUNT)</span><br><span class="line">                   <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">               <span class="comment">// 重入写锁，获取成功</span></span><br><span class="line">               setState(c + acquires);</span><br><span class="line">               <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="comment">// 是否要等待</span></span><br><span class="line">           <span class="keyword">if</span> (writerShouldBlock() || !compareAndSetState(c, c + acquires))</span><br><span class="line">               <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">           <span class="comment">// 没有读写线程 直接获取</span></span><br><span class="line">           setExclusiveOwnerThread(current);</span><br><span class="line">           <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-unlock-1"><a href="#2-unlock-1" class="headerlink" title="2. unlock"></a>2. unlock</h3><p>&emsp;&emsp;  调用 <code>release(1)</code> ，还是看 <code>tryRelease</code> ，减去那个值如果为0说明释放成功。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//读锁释放</span></span><br><span class="line">       <span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryRelease</span><span class="params">(<span class="keyword">int</span> releases)</span> </span>&#123;</span><br><span class="line">           <span class="keyword">if</span> (!isHeldExclusively())</span><br><span class="line">               <span class="keyword">throw</span> <span class="keyword">new</span> IllegalMonitorStateException();</span><br><span class="line">           <span class="comment">// 低位为写锁，可以直接减</span></span><br><span class="line">           <span class="keyword">int</span> nextc = getState() - releases;</span><br><span class="line">           <span class="keyword">boolean</span> free = exclusiveCount(nextc) == <span class="number">0</span>;</span><br><span class="line">           <span class="comment">// 写锁个数为 0 释放线程</span></span><br><span class="line">           <span class="keyword">if</span> (free)</span><br><span class="line">               setExclusiveOwnerThread(<span class="keyword">null</span>);</span><br><span class="line">           <span class="comment">// state 设置为 0</span></span><br><span class="line">           setState(nextc);</span><br><span class="line">           <span class="keyword">return</span> free;</span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>

<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4. 总结"></a>4. 总结</h2><p>&emsp;&emsp;  好了现在算是把独占锁和共享锁来了一个大整合。说到底四个重要的方法，然后里面的调用链必须要清楚，一会我会再写一篇文章分析调用链。不然很容易就蒙了，方法有点多。<br>&emsp;&emsp;  对于这个类需要明白以下几点：</p>
<ol>
<li><p>也是采用了 state 变量来维护锁，高位读，低位写</p>
</li>
<li><p>读锁全是共享锁，写锁全是独占锁了一下。</p>
</li>
<li><p>锁重入问题，读不可重入写，反之可以。 </p>
</li>
</ol>
]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>Semaphore 源码分析</title>
    <url>/2018/03/31/Java%20SourceCode/Semaphore%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="Semaphore-源码分析"><a href="#Semaphore-源码分析" class="headerlink" title="Semaphore 源码分析"></a>Semaphore 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-Semephore-简单介绍"><a href="#1-Semephore-简单介绍" class="headerlink" title="1. Semephore 简单介绍"></a>1. Semephore 简单介绍</h2><p>&emsp;&emsp;  一般来说，在 Java 中比较常用的同步工具就是 Lock 和 Synchronized 但是 Java 也加入了很多新的机制比如这里提到的信号量。他其实给人的感觉就是操作系统中的信号量，如果一个线程要运行需要获取一个资源进行一次 P 操作，在这里就是调用 acquire 方法，然后运行结束后调用 V 操作，释放这个资源以供其他线程使用，这里的 release 方法。那么如果资源都被其他线程抢光了，那么这个线程只能处于等待状态。也就是 P 操作返回的 -1 。</p>
<p>&emsp;&emsp;  上面我们所说的信号量是多值信号量，主要用于进程之间的同步。还有一种称之为二值信号量，也就是操作系统中常提到的 mutex 。对，此时他就是锁！因为做了一个 P 操作后，只有进行 V 操作其他线程才能进入临界区。此时和 Lock 作用一样了。</p>
<a id="more"></a>
<p>&emsp;&emsp;  下面写个简单的程序来感受一下这个同步工具。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SemaphoreTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Semaphore semaphore = <span class="keyword">new</span> Semaphore(<span class="number">2</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">4</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">new</span> Thread(()-&gt;&#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    semaphore.acquire();</span><br><span class="line">                    System.out.println(Thread.currentThread().getName());</span><br><span class="line">                    Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">                    semaphore.release();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).start();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后输出的如下：<br>先打印了</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Thread-0</span><br><span class="line">Thread-1</span><br></pre></td></tr></table></figure>

<p>隔了一秒打印了</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Thread-2</span><br><span class="line">Thread-3</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  可以看到临界区内只有两个线程同时执行。其实当我们理解了上一篇文章中的 AQS 我们大概也能猜到 Semaphore 的实现原理，也是借助于 AQS 中的 state 属性，下面具体分析一下 Semaphore 的结构及原理。</p>
<h2 id="2-基本结构"><a href="#2-基本结构" class="headerlink" title="2. 基本结构"></a>2. 基本结构</h2><h3 id="1-继承"><a href="#1-继承" class="headerlink" title="1. 继承"></a>1. 继承</h3><pre><code>null</code></pre><h3 id="2-实现"><a href="#2-实现" class="headerlink" title="2. 实现"></a>2. 实现</h3><pre><code>Serializable  感觉好多类都能序列化啊！！有没有觉得。</code></pre><h3 id="3-主要字段"><a href="#3-主要字段" class="headerlink" title="3. 主要字段"></a>3. 主要字段</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Sync sync;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  好的，就一个锁字段，真干脆，那么证明前面的猜测也是对的，这个 Sync 肯定是一个内部类，继承了 AQS。在上面一篇文章中我们看到了一些 AQS 中的共享锁我没有分析，其实共享锁就是放在这使用的，所以在这篇文章中主要就是介绍 AQS 中剩下的共享锁部分。看一下整体结构。<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-31/98562096.jpg" alt><br>&emsp;&emsp;  有没有觉得很熟悉，简直和 ReentrantLock 结构极其相似。Semaphore的内部类公平锁(FairSync)和非公平锁(NoFairSync)各自实现不同的获取锁方法即tryAcquireShared(int arg)，毕竟公平锁和非公平锁的获取稍后不同，而释放锁tryReleaseShared(int arg)的操作交由Sync实现，因为释放操作都是相同的，因此放在父类Sync中实现。</p>
<h3 id="4-方法概览"><a href="#4-方法概览" class="headerlink" title="4. 方法概览"></a>4. 方法概览</h3><ol>
<li>acquire()</li>
<li>release()</li>
<li>ctor-2</li>
<li>hasQueuedThreads() </li>
<li>tryAcquire() </li>
</ol>
<h2 id="3-主要方法分析"><a href="#3-主要方法分析" class="headerlink" title="3. 主要方法分析"></a>3. 主要方法分析</h2><h3 id="1-ctor-2"><a href="#1-ctor-2" class="headerlink" title="1. ctor-2"></a>1. ctor-2</h3><p>&emsp;&emsp;  两个构造方法，分别指定了底层采用公平锁还是非公平锁！其中 permits 的值直接传给了 AQS 父类，也就是设置了 AQS 的 state 属性。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//默认创建公平锁，permits指定同一时间访问共享资源的线程数</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">Semaphore</span><span class="params">(<span class="keyword">int</span> permits)</span> </span>&#123;</span><br><span class="line">        sync = <span class="keyword">new</span> NonfairSync(permits);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">Semaphore</span><span class="params">(<span class="keyword">int</span> permits, <span class="keyword">boolean</span> fair)</span> </span>&#123;</span><br><span class="line">     sync = fair ? <span class="keyword">new</span> FairSync(permits) : <span class="keyword">new</span> NonfairSync(permits);</span><br><span class="line"> &#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-acquire"><a href="#2-acquire" class="headerlink" title="2. acquire"></a>2. acquire</h3><p>&emsp;&emsp;  这个方法里面就直接调用了，acquireSharedInterruptibly(1) 。先不看代码，介绍一下过程：当线程调用了 acquire ， state 值代表的资源数足够使用，那么请求线程将会获得同步状态即对共享资源的访问权，并更新 state 的值(一般是对state值减1)，但如果state值代表的许可数已为0，则请求线程将无法获取同步状态，线程将被加入到同步队列并阻塞，直到其他线程释放同步状态(一般是对state值加1)才可能获取对共享资源的访问权。</p>
<p>&emsp;&emsp;  那么现在分析一下 acquireSharedInterruptibly 这个方法显然是可以被中断的，也就是说我们在 acquire 时也可以被中断。进入方法的第一件事就是判断是否有中断事件发生。然后调用了 tryAcquireShared 来获取共享锁。如果失败调用 doAcquireSharedInterruptibly 加入等待队列。 doAcquireSharedInterruptibly 这个方法真的和 acquireQueued 方法如出一辙，大部分代码是一样的！！！仅仅小部分不一样我已经注释出来了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这个方法真的是似曾相识，感觉就是 acquireQueued 的方法稍稍修改了一下</span></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">        1. 那个方法不抛异常</span></span><br><span class="line"><span class="comment">        2. 那个方法在调用前把节点加入了等待队列，封装了独占锁</span></span><br><span class="line"><span class="comment">        3. 那个方法设置标志位，这里直接抛异常</span></span><br><span class="line"><span class="comment"></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">doAcquireSharedInterruptibly</span><span class="params">(<span class="keyword">int</span> arg)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        <span class="comment">// 加入等待队列</span></span><br><span class="line">        <span class="keyword">final</span> Node node = addWaiter(Node.SHARED);</span><br><span class="line">        <span class="keyword">boolean</span> failed = <span class="keyword">true</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="comment">// 前驱</span></span><br><span class="line">                <span class="keyword">final</span> Node p = node.predecessor();</span><br><span class="line">                <span class="comment">// 如果前驱是 head 。自旋获取锁  简直一样！！！</span></span><br><span class="line">                <span class="comment">// 就是写法不一样，意思一模一样</span></span><br><span class="line">                <span class="keyword">if</span> (p == head) &#123;</span><br><span class="line">                    <span class="keyword">int</span> r = tryAcquireShared(arg);</span><br><span class="line">                    <span class="keyword">if</span> (r &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                        setHeadAndPropagate(node, r);</span><br><span class="line">                        p.next = <span class="keyword">null</span>; <span class="comment">// help GC</span></span><br><span class="line">                        failed = <span class="keyword">false</span>;</span><br><span class="line">                        <span class="keyword">return</span>;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 那个方法设置标志位，这里直接抛异常</span></span><br><span class="line">                <span class="keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp; parkAndCheckInterrupt())</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> InterruptedException();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="keyword">if</span> (failed)</span><br><span class="line">                cancelAcquire(node);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  接着又是那个模板方法的问题了， tryAcquireShared ，在公平锁和非公平锁中的实现也不同。下面详细介绍一下两个的区别。在公平锁中还是和前面的 ReentrantLock 中的操作一样，先判断一下同步队列中是不是还有其他的等待线程，有则直接返回失败。否则对 state 值进行减操作并返回剩下的信号量。注意一点的是，如果我们请求的信号量大于可用信号量，返回的值是负值，而在 doAcquireSharedInterruptibly 中要求我们的方法返回正值才会继续执行。举个例子：当我们一开始有 2 信号量，而第一个线程直接请求了 10 个，这个线程就会被挂起，让其他线程来抢资源。并且如果发现请求后的资源数为负，他是不会对 state 进行更新的。</p>
<p>&emsp;&emsp;  然后再分析非公平锁，非公平锁直接调用了父类中的 nonfairTryAcquireShared 和 ReentrantLock 一样，调用父类的 nonfairTryAcquire ，这个方法比较简单，就是把上文中的 tryAcquireShared 里面的同步队列的判断删除了。注意一下，上面的这些方法都是死循环直到有条件对应时才会跳出循环。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 非公平锁的获取方式</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">int</span> <span class="title">tryAcquireShared</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> nonfairTryAcquireShared(acquires);</span><br><span class="line">        &#125;</span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">int</span> <span class="title">nonfairTryAcquireShared</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="keyword">int</span> available = getState();</span><br><span class="line">                <span class="keyword">int</span> remaining = available - acquires;</span><br><span class="line">                <span class="keyword">if</span> (remaining &lt; <span class="number">0</span> ||</span><br><span class="line">                    compareAndSetState(available, remaining))</span><br><span class="line">                    <span class="keyword">return</span> remaining;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line"><span class="comment">// 公平锁获取</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">int</span> <span class="title">tryAcquireShared</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="keyword">if</span> (hasQueuedPredecessors())</span><br><span class="line">                    <span class="keyword">return</span> -<span class="number">1</span>;   <span class="comment">// 仅仅多了这一行代码而已。</span></span><br><span class="line">                <span class="keyword">int</span> available = getState();</span><br><span class="line">                <span class="keyword">int</span> remaining = available - acquires;</span><br><span class="line">                <span class="keyword">if</span> (remaining &lt; <span class="number">0</span> || compareAndSetState(available, remaining))</span><br><span class="line">                    <span class="keyword">return</span> remaining;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-release"><a href="#3-release" class="headerlink" title="3. release"></a>3. release</h3><p>&emsp;&emsp;  release 方法，主要作用就是释放资源，提醒一下一定要保证 release 的执行，否则线程退出但是资源没有释放。所以一般这个代码写在 finally 中是最好的。并且获取多少资源就要释放多少资源，否则还是资源没被争确释放，举个例子 一开始执行了 acquire(10) 最后释放的时候不能只写一个 release() 而是 release(10) 才对。</p>
<p>&emsp;&emsp;  前面我们也看到了，其实 release 方法中调用的释放锁的操作肯定是一个公共操作，毕竟释放锁就不涉及公平和非公平一说了。在 release 中调用了 AQS 中的releaseShared 。这个方法毫无疑问首先会调用 tryReleaseShared 。这个又是模板方法等子类来实现，事实上就是在 Sync 中实现的。逻辑也很简单就是对 state 做增量操作。</p>
<p>&emsp;&emsp;  如果 tryReleaseShared 成功，执行 doReleaseShared ，就是唤醒后继线程。注意到前面的 ReentrantLock 中有 release 方法，他是直接尝试释放锁，如果成功就唤醒后继节点，两者逻辑基本一样。实际上 release 和 releaseShared 方法代码结构及逻辑完全一致，不同的就是在 tryAcquire 后的唤醒同步队列中的节点的操作不同。不然也就没必要弄两个方法了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//  尝试释放锁</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">release</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 如果释放锁成功 唤醒同步队列中的后继节点</span></span><br><span class="line">    <span class="keyword">if</span> (tryRelease(arg)) &#123;</span><br><span class="line">        Node h = head;</span><br><span class="line">        <span class="keyword">if</span> (h != <span class="keyword">null</span> &amp;&amp; h.waitStatus != <span class="number">0</span>)</span><br><span class="line">            unparkSuccessor(h);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 为了方便对比把两个代码放在一块 可以看到 release 中的结构完全一样</span></span><br><span class="line"><span class="comment">// 区别就在于 doReleaseShared 中有更多的判断操作</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">releaseShared</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (tryReleaseShared(arg)) &#123;</span><br><span class="line">        doReleaseShared();  <span class="comment">//在里面执行的 unparkSuccessor(h)</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  重点看一下 doReleaseShared ，一个比较特殊的方法，由于共享的特性，在获取锁和释放锁的过程都需要唤醒后继节点，因为可以有多个线程同时进入临界区。这个方法的主要作用是：</p>
<ol>
<li><p>在执行 acquireShared 申请资源时，执行 tryAcquireShared 失败则执行 doAcquireShared 方法将线程加入同步队列，然后判断同步队列中是否只有当前这一个等待线程，是则自旋获取锁，如果获取到了锁，就把当前节点设为头结点。然后 setHeadAndPropagate 继续唤醒后继节点，因为上次释放的资源可能特别多，现在的资源支持较多的线程同时执行，所以要唤醒后继节点。唤醒后继节点的条件是 <code>资源数 &gt; 0</code> 或者 当前节点的后继节点的 <code>waitStatus &lt; 0</code> 在这里只能是 <code>PROPAGATE</code> 或者 <code>SIGNAL</code>。这里也就解释了为什么在 doReleaseShared 中需要设置头为 PROPAGATE 状态，就是为了后续的唤醒。</p>
</li>
<li><p>而在执行 releaseShared 释放资源时，首先执行 tryReleaseShared ，如果成功则执行 doReleaseShared 释放后继节点，因为释放了资源让更多的线程来运行。如果说现在队列中没有任何节点在等待就把头结点设置为 PROPAGATE ，说明有过剩的资源可用。如果此时刚好遇到上面执行 acquireShared 需要唤醒后继节点的时候先要判断头结点的 <code>waitStatus</code> 的值，如果是 <code>PROPAGATE</code> 肯定可以唤醒后面的。如果说等待队列中有等待线程那么就唤醒他们就行。</p>
</li>
<li><p>好了这个 doReleaseShared 方法需要在以上两种情况下分析，否则始终不清楚为什么要设置 <code>PROPAGATE</code> 。</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这个方法中与 release 中的唤醒不同点在于他保证了释放动作的传递</span></span><br><span class="line">    <span class="comment">// 如果后继节点需要唤醒，则执行唤醒操作，如果没有后继节点则把头设置为 PROPAGATE</span></span><br><span class="line">    <span class="comment">// 这里的死循环和其他的操作中的死循环一样，为了检测新的节点进入队列</span></span><br><span class="line">    <span class="comment">// 其实这个方法比较特殊，在 acquireShared 和 releaseShared 中都被执行了，主要就是共享模式允许多个线程进入临界区</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">doReleaseShared</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">            Node h = head;</span><br><span class="line">            <span class="comment">// 等待队列中有正在等待的线程</span></span><br><span class="line">            <span class="keyword">if</span> (h != <span class="keyword">null</span> &amp;&amp; h != tail) &#123;</span><br><span class="line">                <span class="comment">// 获取头节点对应的线程的状态</span></span><br><span class="line">                <span class="keyword">int</span> ws = h.waitStatus;</span><br><span class="line">                <span class="comment">// 如果头节点对应的线程是SIGNAL状态，则意味着头结点正在运行，后继结点所对应的线程需要被唤醒。</span></span><br><span class="line">                <span class="keyword">if</span> (ws == Node.SIGNAL) &#123;</span><br><span class="line">                    <span class="comment">// 修改头结点，现在后继节点要成为头结点了，状态设置初始值</span></span><br><span class="line">                    <span class="keyword">if</span> (!compareAndSetWaitStatus(h, Node.SIGNAL, <span class="number">0</span>))</span><br><span class="line">                        <span class="keyword">continue</span>;</span><br><span class="line">                    <span class="comment">// 唤醒头结点h的后继结点所对应的线程</span></span><br><span class="line">                    unparkSuccessor(h);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 队列中没有等待线程，只有一个正在运行的线程。</span></span><br><span class="line">                <span class="comment">// 将头结点设置为 PROPAGATE 标志进行传递唤醒</span></span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span> (ws == <span class="number">0</span> &amp;&amp; !compareAndSetWaitStatus(h, <span class="number">0</span>, Node.PROPAGATE))</span><br><span class="line">                    <span class="keyword">continue</span>;                <span class="comment">// loop on failed CAS</span></span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 如果头结点发生变化有一种可能就是在 acquireShared 的时候会调用 setHeadAndPropagate 导致头结点变化，则继续循环。</span></span><br><span class="line">            <span class="comment">// 从新的头结点开始唤醒后继节点。</span></span><br><span class="line">            <span class="keyword">if</span> (h == head)                   <span class="comment">// loop if head changed</span></span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//  被 acquireShard 调用</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">setHeadAndPropagate</span><span class="params">(Node node, <span class="keyword">int</span> propagate)</span> </span>&#123;</span><br><span class="line">        Node h = head; <span class="comment">// Record old head for check below</span></span><br><span class="line">        <span class="comment">// 当前线程设为头结点</span></span><br><span class="line">        setHead(node);</span><br><span class="line">        <span class="comment">// propagate 代表的是当前剩余的资源数，如果还有资源就唤醒后面的共享线程，允许多个线程获取锁，</span></span><br><span class="line">        <span class="comment">// 或者还有一个比较有意思的条件就是 h.waitStatus &lt; 0 他其实是说 h.waitStatus 要么是 signal 要么是 propagate</span></span><br><span class="line">        <span class="comment">// 从而唤醒后继节点</span></span><br><span class="line">        <span class="keyword">if</span> (propagate &gt; <span class="number">0</span> || h == <span class="keyword">null</span> || h.waitStatus &lt; <span class="number">0</span> || (h = head) == <span class="keyword">null</span> || h.waitStatus &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            Node s = node.next;</span><br><span class="line">            <span class="keyword">if</span> (s == <span class="keyword">null</span> || s.isShared())</span><br><span class="line">                doReleaseShared();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4. 总结"></a>4. 总结</h2><p>&emsp;&emsp;  好了，其实到了这里基本就分析的差不多了，其他的一些很少用的方法，其实里面的写法类似，比如说不进行中断的判断等等。<br>&emsp;&emsp;  小结一下前面所说的，Semaphore 是一个信号同步工具，线程通过向 Semaphore 申请资源和释放资源，来进入临界区。一般情况下资源的个数比较多，所以我们允许多个线程同时进入临界区，而当我们资源个数只有一个的时候他就退化成锁也就是只允许一个线程进入临界区。但我们用的比较多的还是多个线程进入，毕竟要使用锁的话 Lock 可能做得更好。<br>&emsp;&emsp;  好，Semaphore 一般被认为是多个线程可同时进入临界区，那么我们底层的实现就不能采用独占锁，而是使用了共享锁，也就是 AQS 中的另外一部分 “江山” 共享锁的实现。资源数就直接采用了 AQS 中的 state 变量来维护。</p>
<p>&emsp;&emsp;  下面完整的跑一遍流程：</p>
<ol>
<li><p>使用构造方法创建一个 Semaphore 对象，默认底层 new 了一个非公平锁，传入的资源数被父类(AQS)的构造方法使用，初始化了 <code>state</code> 变量。</p>
</li>
<li><p>调用 acquire 方法，这个方法直接调用了 <code>acquireSharedInterruptibly()</code> 他首先调用了 Sync 子类中的 tryAcquireShared（因为公平锁和非公平锁的缘故） 如果获取资源失败需要调用 <code>doAcquireSharedInterruptibly()</code> 这个方法和独占锁中的 <code>acquireQueued</code> 方法如出一辙，首先加入等待队列，然后如果等待队列中只有这一个等待的线程则自旋获取锁。如果获取到了锁并且还有可用就尝试唤醒他之后等待的线程，也就是调用了<code>setHeadAndPropagate()</code> ，否则的话直接等待。</p>
</li>
<li><p>这里很巧妙的一点，当我们自旋获取到了锁就说明在自旋的这段时间中有线程释放了资源，然后我们发现资源数还是大于 0 ，那么可以让更多的线程运行（这个时候肯定是新的线程加入了同步队列，也就解释了为什么要使用死循环来执行这段代码），所以才调用了 <code>setHeadAndPropagate()</code> 这个方法里面不仅仅设置了头结点，还调用了 <code>doReleaseShared()</code> 这看起来是需要在 release 方法中调用的在 acquire 中也调用了。</p>
</li>
<li><p>好了，现在如果调用 <code>release</code> 方法 ，他首先调用 <code>releaseShared</code> 之后，里面的逻辑是：先释放锁调用 <code>tryReleaseShared()</code> ，如果成功需要唤醒同步队列中等待的线程。所以会调用 <code>doReleaseShared</code> 。这里面的代码我们已经说过好几次了，因为在很多地方都调用了他。</p>
</li>
</ol>
<p>好，看懂了这些基本对于 Semaphore 了解的差不多了，对于 AQS 中的共享锁部分也算是有了一个完整的介绍。</p>
]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>ReentrantLock 与 AQS 源码分析</title>
    <url>/2018/03/29/Java%20SourceCode/ReentrantLock%20%E4%B8%8E%20AQS%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="ReentrantLock-与-AQS-源码分析"><a href="#ReentrantLock-与-AQS-源码分析" class="headerlink" title="ReentrantLock 与 AQS 源码分析"></a>ReentrantLock 与 AQS 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-基本结构"><a href="#1-基本结构" class="headerlink" title="1. 基本结构"></a>1. 基本结构</h2><p>&emsp;&emsp;  重入锁 ReetrantLock，JDK 1.5新增的类，作用与synchronized关键字相当，但比synchronized更加灵活。ReetrantLock本身也是一种支持重进入的锁，即该锁可以支持一个线程对资源重复加锁，但是加锁多少次，就必须解锁多少次，这样才可以成功释放锁。</p>
<a id="more"></a>
<h3 id="1-继承"><a href="#1-继承" class="headerlink" title="1. 继承"></a>1. 继承</h3><p>没有继承任何类，因为很多操作都使用了组合完成。</p>
<h3 id="2-实现"><a href="#2-实现" class="headerlink" title="2. 实现"></a>2. 实现</h3><p>Lock, java.io.Serializable<br>&emsp;&emsp;这里着重介绍一下 Lock 接口，接口定义了几个必要的方法，也是在 ReentrantLock 中的重点需要分析的方法。<br>&emsp;&emsp;  三类方法：获取锁、释放锁、获取条件。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Lock</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 阻塞获取锁，如果获取不到锁就一直等待</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// 可中断获取锁，在获取锁的过程可以被中断，但是 Synchronized 是不可以</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">lockInterruptibly</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException</span>;</span><br><span class="line">    <span class="comment">// 非阻塞获取锁，没有获取到锁立即返回</span></span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">tryLock</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// 超时获取锁，没获取到锁等待一段时间</span></span><br><span class="line">    <span class="function"><span class="keyword">boolean</span> <span class="title">tryLock</span><span class="params">(<span class="keyword">long</span> time, TimeUnit unit)</span> <span class="keyword">throws</span> InterruptedException</span>;</span><br><span class="line">    <span class="comment">// 解锁</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">unlock</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="comment">// 等待唤醒机制的条件</span></span><br><span class="line">    <span class="function">Condition <span class="title">newCondition</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>从上面可以看到 Synchronized 和 Lock 的一些重要区别：</p>
<ol>
<li><p>Lock 的获取锁的过程是可以中断的，Synchronized 不可以，Synchronized 只能在 wait或同步代码块执行过程中才可以被中断。</p>
</li>
<li><p>由于 Lock 显示的加锁，锁可以横跨几个方法，也就是临界区的位置可以更加自由。</p>
</li>
<li><p>Lock 支持超时获取锁。</p>
</li>
<li><p>后面会看到 Lock 还支持公平及非公平锁。</p>
</li>
<li><p>绑定多个 Condition 条件 </p>
</li>
</ol>
<h3 id="3-主要字段"><a href="#3-主要字段" class="headerlink" title="3. 主要字段"></a>3. 主要字段</h3><p>&emsp;&emsp;很好，这个类的字段非常的少，真正起作用的字段只有一个 “锁” 字段。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 同步锁</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Sync sync;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  这个锁（Sync）是一个继承自 AQS 的抽象内部类，说明一下 AQS (AbstractQueuedSynchronizer) 一般被称为队列同步器，他是并发包中的核心组件，绝大多数锁机制都是采用的这个类来实现的。虽然看到他是一个抽象类，但是你会发现里面没有一个方法是抽象方法，他实现了锁机制中的必要的通用的方法，待会会专门讲这个类。不然 ReentrantLock 没办法说，ReentrantLock 里面的锁操作都是依赖于 AQS。</p>
<p>&emsp;&emsp;  然后这个锁是有两个子类，分别是 <code>NonfairSync</code> 和 <code>FairSync</code> 从名字上也可以看出这两个类分别代表了 <code>公平锁</code> 和 <code>非公平锁</code> 。何为锁的公平性？ 实际上就是新来的线程需要征用锁必须要要等到先于他到达的线程获取并释放锁。也就是获取锁的过程是按照下来后到的顺序进行的，反之就称为非公平锁。后面我们会看到其实这两种锁不同就在于非公平锁在新线程创建后首先会直接进行锁的获取，如果没有获取到会进行一段时间的自旋，始终没获取到锁才进行等待状态。</p>
<p>&emsp;&emsp;  一般而言，公平锁开销比非公平锁大，这也是比较符合我们的直观感受。公平锁是需要进行排队的，但在某些场景下，可能更注重时间先后顺序，那么公平锁自然是很好的选择。</p>
<p>&emsp;&emsp;  好总结一下，在 ReentrantLock 中只维护了一个 “锁” 变量，这个锁是继承了 AQS 同步器，然后这个锁又有两种派生的锁：公平锁，非公平锁。那么 ReentrantLock 实现其实就有两种方式：公平锁，非公平锁。</p>
<h3 id="4-主要方法概览"><a href="#4-主要方法概览" class="headerlink" title="4. 主要方法概览"></a>4. 主要方法概览</h3><ol>
<li>ctor-2</li>
<li>lock</li>
<li>lockInterruptibly</li>
<li>tryLock</li>
<li>tryLock(time)</li>
<li>unlock</li>
<li>newCondition</li>
</ol>
<h2 id="2-基础并发组件-AQS"><a href="#2-基础并发组件-AQS" class="headerlink" title="2. 基础并发组件 AQS"></a>2. 基础并发组件 AQS</h2><h3 id="1-基本字段"><a href="#1-基本字段" class="headerlink" title="1. 基本字段"></a>1. 基本字段</h3><h4 id="1-重要字段"><a href="#1-重要字段" class="headerlink" title="1. 重要字段"></a>1. 重要字段</h4><p>&emsp;&emsp;  AQS 是维护了一个同步队列（双向链表），这个队列里面线程都是需要竞争锁的，没有竞争到的就在同步队列中等待。<code>head</code> 和 <code>tail</code> 就指向队列的首尾。<code>state</code> 是一个标志字段，表示当前有多少线程在临界区。一般来说 <code>state</code> 只能是 0 或 1 但是由于锁是可重入的，所以也有大于 1 的情况。</p>
<p>&emsp;&emsp;  除了一个同步队列还有 0~n 个等待队列，等待队列就是调用了 <code>await</code> 方法的线程，会被挂到调用了 <code>await</code> 的 <code>condition</code> 上面的等待队列，所以有多少个 <code>condition</code> 就有多少等待队列。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//同步队列头指针</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node head;</span><br><span class="line"><span class="comment">// 同步队列尾指针</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node tail;</span><br><span class="line"><span class="comment">// 状态标志，0 则没有线程在临界区，非零表示有 state 个线程在临界区（由于锁可重入）</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">int</span> state;</span><br></pre></td></tr></table></figure>

<h4 id="2-Node-节点"><a href="#2-Node-节点" class="headerlink" title="2. Node 节点"></a>2. Node 节点</h4><p>&emsp;&emsp;Node 节点也就是上文所提到的 <code>同步队列</code> 和 <code>等待队列</code> 中的元素，注意两个队列之间的元素类型是一样的因为他们之间会有相互移动转换的动作，这两个队列中的元素自然是线程，为了方便查找和表示 AQS 将线程封装到了 Node 节点中，构成双向队列。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 共享非 null/独占为 null  </span></span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> Node SHARED = <span class="keyword">new</span> Node();</span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> Node EXCLUSIVE = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 线程状态</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> CANCELLED =  <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> SIGNAL    = -<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> CONDITION = -<span class="number">2</span>;</span><br><span class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> PROPAGATE = -<span class="number">3</span>;</span><br><span class="line">        <span class="keyword">volatile</span> <span class="keyword">int</span> waitStatus;</span><br><span class="line">       <span class="comment">// 双向链表  这两个指针用于同步队列构建链表使用的   下面还有一个 nextWaiter 是用来构建等待单链表队列</span></span><br><span class="line">        <span class="keyword">volatile</span> Node prev;</span><br><span class="line">        <span class="keyword">volatile</span> Node next;</span><br><span class="line">        <span class="comment">// 线程</span></span><br><span class="line">        <span class="keyword">volatile</span> Thread thread;</span><br><span class="line">        <span class="comment">// 等待队列单链表</span></span><br><span class="line">        Node nextWaiter;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * Returns true if node is waiting in shared mode.</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">isShared</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> nextWaiter == SHARED;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  可以看到上面有一个 <code>waitStatus</code> 属性，代表了线程当前的状态，状态标识就是那些常量。具体如下：</p>
<ol>
<li><p>SIGNAL:     正在执行的线程结束释放锁或者被取消执行，他必须唤醒后续的状态为 SIGNAL 节点</p>
</li>
<li><p>CANCELLED:  在同步队列中等待的线程等待超时或被中断，需要从同步队列中取消该Node的结点， 其结点的waitStatus为CANCELLED，即结束状态，进入该状态后的结点将不会再变化。</p>
</li>
<li><p>CONDITION:  该标识的结点处于等待队列中（不是同步队列），结点的线程等待在Condition上，当其他线程调用了Condition的signal()方法后，CONDITION状态的结点将从等待队列转移到同步队列中，等待获取同步锁。</p>
</li>
<li><p>PROPAGATE:在共享模式中，该状态标识结点的线程处于可运行状态。</p>
</li>
<li><p>0:代表初始化状态。</p>
</li>
</ol>
<p>&emsp;&emsp;  可以看到，Node 里面的主要字段就是一个状态标志位、一个线程的引用、用于构建链表的指针。注意，有三个指针，其中前两个 <code>next</code> 和 <code>pre</code> 是用来构建同步队列的（双向链表），后面 <code>nextWaiter</code> 是用来构建等待队列。所以说虽然同步队列和等待队列使用的同一个数据类型，数据结构是不同的，并且在后面我们会看到等待队列中的节点只有两种状态 <code>Condition</code> 和 <code>CANCELLED</code> 前者表示线程已结束需要从等待队列中移除，后者表示条件结点等待被唤醒。</p>
<p>&emsp;&emsp;下面画图说明一下同步队列和等待队列的情况。<br>等待队列<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-30/81487339.jpg" alt><br>同步队列<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-30/91260714.jpg" alt></p>
<h4 id="3-ConditionObject"><a href="#3-ConditionObject" class="headerlink" title="3. ConditionObject"></a>3. ConditionObject</h4><p>&emsp;&emsp;  这个内部类是等待唤醒机制的核心，在他上面绑定了一个等待队列。在这个类中使用了两个指针（ <code>firstWaiter/lastWaiter</code> ）指向队列的首尾。这里主要看一下 <code>await</code> 、<code>signal</code> 和 <code>signalAll</code> 方法。</p>
<ol>
<li>await<br>&emsp;&emsp;  当一个线程调用了await()相关的方法，那么首先构建一个Node节点封装当前线程的相关信息加入到等待队列中进行等待，并释放锁直到被唤醒（移动到同步队列）、中断、超时才被队列中移出。被唤醒后的第一件事是抢锁和检查是否被中断，然后才是移除队列。被唤醒时候的状态应该为 SIGNAL ，而在方法中执行的移除队列的操作就是移除状态非 Condition 的节点。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">await</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">            <span class="comment">// 等待可中断</span></span><br><span class="line">            <span class="keyword">if</span> (Thread.interrupted())</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> InterruptedException();</span><br><span class="line">            <span class="comment">// 加入等待队列， new 新的 Node 做一个尾插入</span></span><br><span class="line">            Node node = addConditionWaiter();</span><br><span class="line">            <span class="comment">// 释放当前线程的锁，失败则将当前线程设置为取消状态</span></span><br><span class="line">            <span class="keyword">int</span> savedState = fullyRelease(node);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">int</span> interruptMode = <span class="number">0</span>;</span><br><span class="line">            <span class="comment">// 如果没在同步队列就让线程等待也就是看是否被唤醒</span></span><br><span class="line">            <span class="comment">// 如果有中断或者被唤醒那么退出循环</span></span><br><span class="line">            <span class="keyword">while</span> (!isOnSyncQueue(node)) &#123;</span><br><span class="line">                LockSupport.park(<span class="keyword">this</span>);</span><br><span class="line">                <span class="keyword">if</span> ((interruptMode = checkInterruptWhileWaiting(node)) != <span class="number">0</span>)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 运行到此处说明已经被唤醒了，因为结束了循环</span></span><br><span class="line">            <span class="comment">// 唤醒后，首先自旋一下获取锁，同时判断是否中断</span></span><br><span class="line">            <span class="keyword">if</span> (acquireQueued(node, savedState) &amp;&amp; interruptMode != THROW_IE)</span><br><span class="line">                interruptMode = REINTERRUPT;</span><br><span class="line">            <span class="comment">// 清理队列中状态不是 Condition 的的任务，包括被唤醒的 SIGNAL 和 被取消的 CANCELLED</span></span><br><span class="line">            <span class="keyword">if</span> (node.nextWaiter != <span class="keyword">null</span>)</span><br><span class="line">                unlinkCancelledWaiters();</span><br><span class="line">            <span class="comment">//被中断 抛异常</span></span><br><span class="line">            <span class="keyword">if</span> (interruptMode != <span class="number">0</span>)</span><br><span class="line">                reportInterruptAfterWait(interruptMode);</span><br><span class="line">        &#125;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>signal/doSignal/signalAll<br>&emsp;&emsp;  执行 signal 首先进行锁的判断，如果没有获取到独占锁就直接抛出异常。这也就是为什么只有拥有锁的线程才能执行 signal ，然后获取等待队列中的第一个节点执行 doSignal。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">signal</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 获取独占锁</span></span><br><span class="line">    <span class="keyword">if</span> (!isHeldExclusively())</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalMonitorStateException();</span><br><span class="line">    <span class="comment">// 唤醒等待队里中的第一个线程</span></span><br><span class="line">    Node first = firstWaiter;</span><br><span class="line">    <span class="keyword">if</span> (first != <span class="keyword">null</span>)</span><br><span class="line">        doSignal(first);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  doSignal 方法主要就干了三个事 ：</p>
<ol>
<li>将被唤醒的节点从等待队列中移除（while 循环体），如果被唤醒的节点被取消了就继续唤醒后面的节点（transferForSignal 返回 false）</li>
<li>否则把这个节点加入到同步队列 （ enq 方法 ）</li>
<li>当同步队列中当前节点的前驱被取消或者没办法唤醒时则唤醒这个线程 （ unpark ），这时候调用了 unpark 正好和 await 中的 park 相对应使得 await 的线程被唤醒，接着执行循环体判断自己已经被移入到同步队列了，接着就可以执行后面的获取锁的操作。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">doSignal</span><span class="params">(Node first)</span> </span>&#123;</span><br><span class="line">           <span class="keyword">do</span> &#123;</span><br><span class="line">               <span class="comment">// 头指针指向唤醒节点的下一个节点，并顺便判断等待队列是否空</span></span><br><span class="line">               <span class="keyword">if</span> ( (firstWaiter = first.nextWaiter) == <span class="keyword">null</span>)</span><br><span class="line">                   lastWaiter = <span class="keyword">null</span>;</span><br><span class="line">               <span class="comment">// 解除引用</span></span><br><span class="line">               first.nextWaiter = <span class="keyword">null</span>;</span><br><span class="line">           &#125; <span class="keyword">while</span> (!transferForSignal(first) &amp;&amp; (first = firstWaiter) != <span class="keyword">null</span>); <span class="comment">//移入同步队列失败则继续唤醒下一个线程，否则唤醒成功</span></span><br><span class="line">           <span class="comment">// 唤醒成功的线程不一定马上能开始执行，只有在前驱节点被取消或者没办法被唤醒时</span></span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  </span><br><span class="line">  <span class="comment">//  将节点从等待队列移动到同步队列   成功返回 true 失败 false</span></span><br><span class="line">   <span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">transferForSignal</span><span class="params">(Node node)</span> </span>&#123;</span><br><span class="line">       <span class="comment">// 在等待队列中的节点只有 condition 和 cancelled 两种状态，如果状态更新失败说明任务被取消</span></span><br><span class="line">       <span class="comment">// 否则更新为初始状态   直接返回的话上面的 doSignal 就会继续唤醒后面的线程</span></span><br><span class="line">       <span class="keyword">if</span> (!compareAndSetWaitStatus(node, Node.CONDITION, <span class="number">0</span>))</span><br><span class="line">           <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">       <span class="comment">// 把当前节点加入同步队列</span></span><br><span class="line">       Node p = enq(node);</span><br><span class="line">       <span class="comment">// 获取同步队列中倒数第二个节点的状态，当前节点的前驱</span></span><br><span class="line">       <span class="keyword">int</span> ws = p.waitStatus;</span><br><span class="line">       <span class="comment">// 如果前驱节点被取消或者在设置前驱节点状态为Node.SIGNAL状态失败时，唤醒被通知节点代表的线程</span></span><br><span class="line">       <span class="keyword">if</span> (ws &gt; <span class="number">0</span> || !compareAndSetWaitStatus(p, ws, Node.SIGNAL))</span><br><span class="line">           LockSupport.unpark(node.thread);</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">   &#125;</span><br><span class="line">   </span><br><span class="line">   </span><br><span class="line">   <span class="comment">// 插入一个节点到同步队列，如果同步队列是空的则加入一个空节点做为头结点</span></span><br><span class="line">   <span class="comment">// 死循环保证肯定能插入    返回插入节点的前驱</span></span><br><span class="line">   <span class="function"><span class="keyword">private</span> Node <span class="title">enq</span><span class="params">(<span class="keyword">final</span> Node node)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">           Node t = tail;</span><br><span class="line">           <span class="keyword">if</span> (t == <span class="keyword">null</span>) &#123; <span class="comment">// Must initialize</span></span><br><span class="line">               <span class="keyword">if</span> (compareAndSetHead(<span class="keyword">new</span> Node()))</span><br><span class="line">                   tail = head;</span><br><span class="line">           &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">               <span class="comment">// 这一步不需要 cas 是因为并发没关系，只是指向链表结尾，不会多线程更新问题</span></span><br><span class="line">               node.prev = t;</span><br><span class="line">               <span class="comment">// 可能有多个线程抢</span></span><br><span class="line">               <span class="keyword">if</span> (compareAndSetTail(t, node)) &#123;</span><br><span class="line">                   t.next = node;</span><br><span class="line">                   <span class="keyword">return</span> t;</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  有一个小问题,就是在某个线程中执行了别人的 signal 不会导致当前线程立即放弃锁，之所以会这样正是由于 <code>ws &gt; 0 || !compareAndSetWaitStatus(p, ws, Node.SIGNAL)</code> 这个判断，即前驱线程都结束了。比如下面的例子：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> util.AQSTest;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.TimeUnit;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Condition;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Lock;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.ReentrantLock;</span><br><span class="line"></span><br><span class="line"><span class="comment">// test signal 执行后不会导致当前线程立即释放锁</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AQSTest</span> </span>&#123;</span><br><span class="line">    <span class="keyword">static</span> Lock lock = <span class="keyword">new</span> ReentrantLock();</span><br><span class="line">   <span class="keyword">static</span> Condition run1Cond = lock.newCondition();</span><br><span class="line">    <span class="keyword">static</span> Condition run2Cond = lock.newCondition();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Runner1</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            lock.lock();</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">"runner 1 start"</span>);</span><br><span class="line">                run1Cond.await(<span class="number">1</span>, TimeUnit.SECONDS);</span><br><span class="line">                run2Cond.signal();</span><br><span class="line">                System.out.println(<span class="string">"runner 1 exit"</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                lock.unlock();</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Runner2</span> <span class="keyword">implements</span> <span class="title">Runnable</span> </span>&#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            lock.lock();</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">"runner 2 start"</span>);</span><br><span class="line">                run2Cond.await();</span><br><span class="line">                System.out.println(<span class="string">"runner 2 exit"</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">                lock.unlock();</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">new</span> Thread(<span class="keyword">new</span> Runner1(),<span class="string">"runner1"</span>).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(<span class="keyword">new</span> Runner2(),<span class="string">"runner2"</span>).start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>输出的结果始终是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">runner 1 start</span><br><span class="line">runner 2 start</span><br><span class="line">runner 1 exit</span><br><span class="line">runner 2 exit</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  我使用了工具对上面的代码进行了调试，大致说一下流程，顺便用来捋一捋等待唤醒机制。</p>
<p>&emsp;&emsp;  首先 runner1 启动，获取到锁，打印出 “runner1 start” ，然后调用了 await 方法，此时 runner1 线程就执行了 AQS 中的 ConditionObject 中的 await 方法，该方法首先 new 了一个新的节点，把 runner1 封装到这个节点里面。挂在了 run1Con 的等待队列上，然后执行了释放锁并判断中断。紧接着 runner1 线程执行循环体判断是否被唤醒也就是是否在同步队列，显然这时候不在，就直接调用了 park 方法，执行休眠 1 秒钟操作， park 方法是 native 方法由操作系统实现。在上面线程释放锁的时候执行的操作是 <code>fullyRelease</code> 这个方法调用了 <code>release</code> 方法，而 <code>release</code> 方法中释放了锁之后，会检查同步队列中是否还有以前因为没抢到锁而等待的线程，如果有执行 <code>unparkSuccessor</code> 也就是唤醒同步队列中的后继线程。那么此时 runner2 会被唤醒，唤醒后就去抢锁，获取到 lock 锁后输出了 “runner2 start” ，然后 runner2 线程又会因为调用 <code>await</code> 处于和 runner1 同样的境地，也就是被放入 run2Con 的等待队列。好！此时 runner1 的超时时间到了，就会被 unpark 这个 unpark 是被操作系统调用的，之后继续执行循环体发现超时时间小于等于 0 ，则调用 <code>transferAfterCancelledWait</code> 里面调用了 <code>enq</code> 就是加入同步队列，接着开始竞争锁，开始执行 run2Con 上的 signal 此时 signal 调用 doSignal 先执行 do while 中的循环体，runner2 从 run2Con 的等待队列上移除，然后执行 <code>transferForSignal</code> 里面又调用了 <code>enq</code> 将他加入同步队列，并返回同步队列中的前驱，前驱节点状态不是 Cancelled 或者 可以被置为 SIGNAL 则 signal 方法结束。接着打印了 “runner1 exit” 。接着需要执行 finally 里面的释放锁的操作了，显然 unlock 肯定调用了 release ，而 release 会唤醒同步队列中的后继的线程，那么位于同步队列中的 runner2 之前的 park 状态就会被打断，从而跳出 while 循环，执行获取锁的操作。打印出 “runner2 exit” ，最后释放锁整个程序结束。</p>
<p>&emsp;&emsp;  现在总算是吧 Condition 的等待唤醒机制弄清楚了。也把 AQS 中的两个内部类的功能都解释完了。接下来就看 AQS 中的方法。</p>
<h3 id="2-重要方法"><a href="#2-重要方法" class="headerlink" title="2. 重要方法"></a>2. 重要方法</h3><ol>
<li>get/setState</li>
<li>release/tryRelease/unparkSuccessor/fullyRelease</li>
<li>acquire/tryAcquire/addWaiter/tryQueued</li>
<li>acquireShared</li>
<li>releaseShared</li>
</ol>
<p>&emsp;&emsp;  这些属于 AQS 中常用的方法，但是里面的核心方法都是模板方法，也就是说由继承他的子类来实现，所以只能看个大概的逻辑。一会等到讲 ReentrantLock 时再详细说这里面的方法。</p>
<h2 id="3-ReentrantLock-内部类-Sync-fairSync-noFairSync"><a href="#3-ReentrantLock-内部类-Sync-fairSync-noFairSync" class="headerlink" title="3. ReentrantLock 内部类 Sync/fairSync/noFairSync"></a>3. ReentrantLock 内部类 Sync/fairSync/noFairSync</h2><h3 id="1-Sync"><a href="#1-Sync" class="headerlink" title="1. Sync"></a>1. Sync</h3><p>&emsp;&emsp;  这三个内部类实际上是继承自 AQS ，也就是说 ReentrantLock 是采用了 AQS 作为自己的核心并发控制组件完成的一系列的锁操作，及等待唤醒机制。</p>
<p>&emsp;&emsp;  首先看一下 Sync 他是后面两个的父类，他直接继承自 AQS 。AQS 中留了几个比较重要的模板方法 tryAcquire 、tryRelease 。这个方法直接实现了一些在公平锁和非公平锁中的通用操作，也就是释放锁的操作 tryRelease 。</p>
<p>&emsp;&emsp;  tryRelease 的实现很简单，主要就是依赖于 AQS 中的 state 属性，如果state 值减去要释放的信号量为 0 则释放成功，否则失败。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 释放锁的公共操作</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryRelease</span><span class="params">(<span class="keyword">int</span> releases)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 释放锁首先就是使用 AQS 中的 state 的值减去信号量 判断是否为0</span></span><br><span class="line">    <span class="comment">// 如果是 0 则表明成功释放锁，独占线程设为 null，否则说明还占用锁</span></span><br><span class="line">    <span class="keyword">int</span> c = getState() - releases;</span><br><span class="line">    <span class="comment">// 必须获取到锁才能解锁，否则抛异常</span></span><br><span class="line">    <span class="keyword">if</span> (Thread.currentThread() != getExclusiveOwnerThread())</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> IllegalMonitorStateException();</span><br><span class="line">    <span class="keyword">boolean</span> free = <span class="keyword">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">        free = <span class="keyword">true</span>;</span><br><span class="line">        setExclusiveOwnerThread(<span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    setState(c);</span><br><span class="line">    <span class="keyword">return</span> free;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-fairSync"><a href="#2-fairSync" class="headerlink" title="2. fairSync"></a>2. fairSync</h3><pre><code>&amp;emsp;&amp;emsp;  公平锁执行 lock 操作就是执行了 AQS 中的 acquire(1) 也就是请求一个锁资源。但是注意，在 AQS 中的 acquire 中的 tryAcquire 方法没有实现，所以必须由当前类实现。

&amp;emsp;&amp;emsp;  在 tryAcquire 中做的事情就是看是否有代码在临界区。没有则还要看同步队列中是否有线程等待，当只有这一个线程在获取锁的时候才能正常的获取锁，其他情况都失败。</code></pre><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 公平锁</span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">FairSync</span> <span class="keyword">extends</span> <span class="title">Sync</span> </span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            acquire(<span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 没有代码在临界区或者是当前线程的重入 则获取成功，否则失败</span></span><br><span class="line">        <span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">final</span> Thread current = Thread.currentThread();</span><br><span class="line">            <span class="keyword">int</span> c = getState();</span><br><span class="line">            <span class="comment">// 如果当前线程在获取锁的过程没有其他线程在临界区</span></span><br><span class="line">            <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">// 如果同步队列中没有等待的线程，就设置 state ，并且当前线程设为独占线程</span></span><br><span class="line">                <span class="keyword">if</span> (!hasQueuedPredecessors() &amp;&amp; compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">                    setExclusiveOwnerThread(current);</span><br><span class="line">                    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 有程序在临界区，如果是当前线程可重入，加上请求的资源数</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">                <span class="keyword">int</span> nextc = c + acquires;</span><br><span class="line">                <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>)</span><br><span class="line">                    <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">                setState(nextc);</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 竞争锁失败，因为他是公平的锁竞争</span></span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-noFairSync"><a href="#3-noFairSync" class="headerlink" title="3. noFairSync"></a>3. noFairSync</h3><p>同理，这个方法也需要实现 lock 和 tryAcquire 操作。在 lock 中直接判断是否有代码在临界区，没有则直接获取到锁，与公平锁不同的是：公平锁还判断了等待队列中是否有等待的线程。有在临界区的情况时执行 acquire 操作。同样的，首先要执行 tryAcquire 如果失败，加入同步队列并自旋获取锁。还是 tryAcquire 的实现，这里又调用了 nonfairTryAcquire。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 非公平锁</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">NonfairSync</span> <span class="keyword">extends</span> <span class="title">Sync</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">lock</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 如果没有代码在临界区 直接获取锁，独占</span></span><br><span class="line">        <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, <span class="number">1</span>))</span><br><span class="line">            setExclusiveOwnerThread(Thread.currentThread());</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        <span class="comment">// 有代码在临界区则执行尝试获取锁</span></span><br><span class="line">            acquire(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 和公平锁中的 tryAcquire 一模一样只是少了关于同步队列中是否有等待线程的判断</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> nonfairTryAcquire(acquires);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">nonfairTryAcquire</span><span class="params">(<span class="keyword">int</span> acquires)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> Thread current = Thread.currentThread();</span><br><span class="line">        <span class="keyword">int</span> c = getState();</span><br><span class="line">        <span class="comment">// 没有线程获取锁 直接获取到锁  和公平锁中的 tryAcquire 一模一样只是少了关于同步队列的判断</span></span><br><span class="line">        <span class="keyword">if</span> (c == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (compareAndSetState(<span class="number">0</span>, acquires)) &#123;</span><br><span class="line">                setExclusiveOwnerThread(current);</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 重入锁</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (current == getExclusiveOwnerThread()) &#123;</span><br><span class="line">            <span class="keyword">int</span> nextc = c + acquires;</span><br><span class="line">            <span class="keyword">if</span> (nextc &lt; <span class="number">0</span>) <span class="comment">// overflow</span></span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> Error(<span class="string">"Maximum lock count exceeded"</span>);</span><br><span class="line">            setState(nextc);</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  好了，现在我们 AQS 中的空的核心方法也被子类实现了，那么现在 fairSync 和 noFairSync 就算是一个完整的 AQS 了。此时看一下加解锁的流程。</p>
<p>只说公平锁，因为非公平锁就只是少了一个判断。</p>
<ol>
<li><p>首先 sync 调用 lock 方法，让后 lock 调用了 AQS 的 acquire(1) 也就是获取一个锁资源。</p>
</li>
<li><p>acquire 就先调用 tryAcquire(1) 尝试获取锁，这时候代码又回调到 sync 中的实现的 tryAcquire 方法，这个方法先判断锁是否已经被别的线程使用，然后需要确定没有更早的线程在同步队列等待获取锁，才把当前线程设置为独占线程，并设置 state 值获取锁。但是如果有代码在临界区需要判断是否为当前线程，因为锁是可重入的。如果是当前线程则 state 加上请求锁的个数，返回。</p>
</li>
<li><p>这时候又回到 AQS 中，如果上面尝试获取锁的过程失败，就需要调用 addWaiter 将当前线程封装成一个独占节点，等待状态默认为 0，并且返回当前节点。</p>
</li>
<li><p>加入同步队列后，再调用 acquireQueued 方法，当此线程是同步队列中等待的第一个线程则自旋尝试获取锁，毕竟很可能正在执行的线程马上就会释放锁了，再进行休眠不合适。如果自旋获取锁失败则判断节点状态是否为 SIGNAL 然后执行等待操作。</p>
</li>
<li><p>锁获取成功则把当前节点设置为头结点，把 thread = null</p>
</li>
<li><p>至此，Acquire 方法执行结束。</p>
</li>
</ol>
<p>然后调用 unlock 方法解锁操作。</p>
<ol>
<li><p>解锁操作就没那么麻烦，首先还是调用到了 AQS 中的 release 方法，这个方法首先尝试解锁当前线程，又回调到了 sync 中的 tryRelease 。</p>
</li>
<li><p>tryRelease 逻辑比较简单，使用 AQS 中的 state 减去释放的资源数，等于 0 代表完全释放，否则释放失败。</p>
</li>
<li><p>如果 tryRelease 成功执行就要去唤醒同步队列中的后继节点，继续执行。</p>
</li>
<li><p>至此，release 方法执行完毕。</p>
</li>
</ol>
<h2 id="4-AQS-中的要方法"><a href="#4-AQS-中的要方法" class="headerlink" title="4. AQS 中的要方法"></a>4. AQS 中的要方法</h2><h3 id="1-get-setState"><a href="#1-get-setState" class="headerlink" title="1. get/setState"></a>1. get/setState</h3><p>&emsp;&emsp;这两个方法主要是对 state 变量的 volatile 的读写，其实里面就就是普通的 get/set 方法。但是注意的一点就是 state 是 volatile 的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 对状态变量的 volatile 读写</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">getState</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> state;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">setState</span><span class="params">(<span class="keyword">int</span> newState)</span> </span>&#123;</span><br><span class="line">    state = newState;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-release-tryRelease-unparkSuccessor-fullyRelease"><a href="#2-release-tryRelease-unparkSuccessor-fullyRelease" class="headerlink" title="2. release/tryRelease/unparkSuccessor/fullyRelease"></a>2. release/tryRelease/unparkSuccessor/fullyRelease</h3><p>&emsp;&emsp;  这几个方法在一起说主要是因为他们之间存在调用链，首先来看 release 这个方法我们在上面也分析了，里面调用了 tryRelease 、unparkSuccessor。 也就是首先调用 tryRelease 来释放当前线程的锁，如果释放成功就调用 unparkSuccessor 来唤醒同步队列中后继节点。其中 tryRelease 是由子类来实现，里面的主要逻辑就是看当前的 state 变量的值在修改过后是否为0 。这里还有一个 fullRelease 主要是在 ConditionObject 中调用的，当执行 await 的操作的时会执行此方法释放锁。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//  尝试释放锁</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">release</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">       <span class="comment">// 如果释放锁成功 唤醒同步队列中的后继节点</span></span><br><span class="line">       <span class="keyword">if</span> (tryRelease(arg)) &#123;</span><br><span class="line">           Node h = head;</span><br><span class="line">           <span class="keyword">if</span> (h != <span class="keyword">null</span> &amp;&amp; h.waitStatus != <span class="number">0</span>)</span><br><span class="line">               unparkSuccessor(h);</span><br><span class="line">           <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">   &#125;</span><br><span class="line">   </span><br><span class="line">   <span class="comment">// 唤醒同步队列中的后继节点</span></span><br><span class="line">   <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">unparkSuccessor</span><span class="params">(Node node)</span> </span>&#123;</span><br><span class="line">       <span class="comment">// node 一般就是当前正在运行的线程</span></span><br><span class="line">       <span class="keyword">int</span> ws = node.waitStatus;</span><br><span class="line">       <span class="comment">// 当前线程置为初始状态   可以失败</span></span><br><span class="line">       <span class="keyword">if</span> (ws &lt; <span class="number">0</span>)</span><br><span class="line">           compareAndSetWaitStatus(node, ws, <span class="number">0</span>);</span><br><span class="line">       <span class="comment">// 找到同步队列中的下一个节点</span></span><br><span class="line">       Node s = node.next;</span><br><span class="line">       <span class="keyword">if</span> (s == <span class="keyword">null</span> || s.waitStatus &gt; <span class="number">0</span>) &#123;  <span class="comment">//没有下一个节点或者被取消</span></span><br><span class="line">           s = <span class="keyword">null</span>;</span><br><span class="line">           <span class="comment">// 从后往前找第一个没有被取消的线程</span></span><br><span class="line">           <span class="keyword">for</span> (Node t = tail; t != <span class="keyword">null</span> &amp;&amp; t != node; t = t.prev)</span><br><span class="line">               <span class="keyword">if</span> (t.waitStatus &lt;= <span class="number">0</span>)</span><br><span class="line">                   s = t;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="comment">// 唤醒那个线程</span></span><br><span class="line">       <span class="keyword">if</span> (s != <span class="keyword">null</span>)</span><br><span class="line">           LockSupport.unpark(s.thread);</span><br><span class="line">   &#125;</span><br><span class="line">   </span><br><span class="line">   <span class="function"><span class="keyword">final</span> <span class="keyword">int</span> <span class="title">fullyRelease</span><span class="params">(Node node)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">boolean</span> failed = <span class="keyword">true</span>;</span><br><span class="line">       <span class="keyword">try</span> &#123;</span><br><span class="line">           <span class="keyword">int</span> savedState = getState();</span><br><span class="line">           <span class="keyword">if</span> (release(savedState)) &#123;</span><br><span class="line">               failed = <span class="keyword">false</span>;</span><br><span class="line">               <span class="keyword">return</span> savedState;</span><br><span class="line">           &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">               <span class="keyword">throw</span> <span class="keyword">new</span> IllegalMonitorStateException();</span><br><span class="line">           &#125;</span><br><span class="line">       &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">           <span class="keyword">if</span> (failed)</span><br><span class="line">               node.waitStatus = Node.CANCELLED;</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-acquire-tryAcquire-addWaiter-acquireQueued"><a href="#3-acquire-tryAcquire-addWaiter-acquireQueued" class="headerlink" title="3. acquire/tryAcquire/addWaiter/acquireQueued"></a>3. acquire/tryAcquire/addWaiter/acquireQueued</h3><p>这个和上面的一样，在执行了 acquire 后，会去调用子类复写的 tryAcquire 方法，这个方法就是看有否有代码块在临界区，没有的话直接获取锁（非公平锁），设置 state，有的话要判断是不是当前线程能否进行重入操作，否则就获取失败。失败后会调用 addWaiter ，new 一个新的节点加入到同步队列，接着调用了 acquireQueued 如果这个节点是同步队列中的第一个等待的线程（但不是第一个节点，因为第一个节点是 thread=null 的运行中的线程）就自旋一段时间看能否获取到锁。不能则 park 等待。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 获取锁</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">acquire</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 尝试获取锁 失败则加入同步队列 如果是同步队列中的第一个线程就自旋获取锁</span></span><br><span class="line">        <span class="comment">// 上面的步骤的自旋获取锁阶段，返回的是是否需要中断，所以下面就进行 selfInterrupt</span></span><br><span class="line">        <span class="comment">// tryAcquire 是模板方法，因为对于公平锁和非公平锁获取锁方式不同</span></span><br><span class="line">        <span class="keyword">if</span> (!tryAcquire(arg) &amp;&amp; acquireQueued(addWaiter(Node.EXCLUSIVE), arg))</span><br><span class="line">            selfInterrupt();</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 创建一个节点放入到同步对列中   可传入是否为独占锁   返回当前节点</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> Node <span class="title">addWaiter</span><span class="params">(Node mode)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 默认的 status 是 0</span></span><br><span class="line">        Node node = <span class="keyword">new</span> Node(Thread.currentThread(), mode);</span><br><span class="line">        <span class="comment">// Try the fast path of enq; backup to full enq on failure</span></span><br><span class="line">        Node pred = tail;</span><br><span class="line">        <span class="keyword">if</span> (pred != <span class="keyword">null</span>) &#123;</span><br><span class="line">            node.prev = pred;</span><br><span class="line">            <span class="comment">// 把 tail 设置为 node 成功说明没有竞争</span></span><br><span class="line">            <span class="keyword">if</span> (compareAndSetTail(pred, node)) &#123;</span><br><span class="line">                pred.next = node;</span><br><span class="line">                <span class="keyword">return</span> node;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 失败则就说明空队列   创建头结点</span></span><br><span class="line">        enq(node);</span><br><span class="line">        <span class="keyword">return</span> node;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">     <span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">acquireQueued</span><span class="params">(<span class="keyword">final</span> Node node, <span class="keyword">int</span> arg)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">boolean</span> failed = <span class="keyword">true</span>;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">boolean</span> interrupted = <span class="keyword">false</span>;</span><br><span class="line">            <span class="comment">// 自旋获取锁</span></span><br><span class="line">            <span class="keyword">for</span> (;;) &#123;</span><br><span class="line">                <span class="comment">// 获取前驱节点</span></span><br><span class="line">                <span class="keyword">final</span> Node p = node.predecessor();</span><br><span class="line">                <span class="comment">// 如果前驱是空的头结点，那么也就是说当前线程就是队列中的第一个线程 并尝试获取锁  成功的话方法返回中断情况</span></span><br><span class="line">                <span class="keyword">if</span> (p == head &amp;&amp; tryAcquire(arg)) &#123;</span><br><span class="line">                    <span class="comment">// 把当前节点设置为头结点  thread=null 也就可以看做当前线程在运行，所以就不在同步队列</span></span><br><span class="line">                    setHead(node);</span><br><span class="line">                    <span class="comment">// gc</span></span><br><span class="line">                    p.next = <span class="keyword">null</span>; <span class="comment">// help GC</span></span><br><span class="line">                    failed = <span class="keyword">false</span>;</span><br><span class="line">                    <span class="keyword">return</span> interrupted;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 如果获取锁失败，检测为 SIGNAL 或者设置为 SIGNAL 然后让此线程等待 等待操作在 parkAndCheckInterrupt 中完成</span></span><br><span class="line">                <span class="keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp; parkAndCheckInterrupt())</span><br><span class="line">                    interrupted = <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            <span class="comment">// 失败 取消</span></span><br><span class="line">            <span class="keyword">if</span> (failed)</span><br><span class="line">                cancelAcquire(node);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="5-总结"><a href="#5-总结" class="headerlink" title="5. 总结"></a>5. 总结</h2><p>&emsp;&emsp;  其实到这里 ReentrantLock 已经讲完了，因为他底层全部调用的是 Sync 中的方法，也就是全都是调用了 AQS 中的方法。而 AQS 中的大部分重要的方法都已经看过了。</p>
]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>synchronized 原理分析</title>
    <url>/2018/03/29/Java%20SourceCode/synchronized%20%E5%8E%9F%E7%90%86%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="synchronized-原理分析"><a href="#synchronized-原理分析" class="headerlink" title="synchronized 原理分析"></a>synchronized 原理分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-synchronized-介绍"><a href="#1-synchronized-介绍" class="headerlink" title="1. synchronized 介绍"></a>1. synchronized 介绍</h2><p>&emsp;&emsp;  在并发程序中，这个关键字可能是出现频率最高的一个字段，他可以避免多线程中的安全问题，对代码进行同步。同步的方式其实就是隐式的加锁，加锁过程是有 jvm 帮我们完成的，再生成的字节码中会有体现，如果反编译带有不可消除的 <code>synchronized</code> 关键字的代码块的 class 文件我们会发现有两个特殊的指令 <code>monitorenter</code> 和 <code>monitorexit</code> ，这两个就是进入管程和退出管程。为什么说不可消除的 <code>synchronized</code> ，这是由于在编译时期会进行锁优化，比如说在 <code>StringBuffer</code> 中是加了锁的，也就是锁对象就是他自己，然而我们编译以后会发现根本没有上面的两条指令就是因为，锁消除技术。</p>
<a id="more"></a>
<p>&emsp;&emsp;  <code>Synchronized</code> 使用的一般场景，在对象方法和类方法上使用，以及自定义同步代码块。但是在方法上使用 <code>Synchronized</code> 关键字和使用同步代码块是不一样的，方法上采用同步是采用的字节码中的标志位  <code>ACC_SYNCHRONIZED</code> 来进行同步的。而同步代码块则是采用了对象头中的锁指针指向一个监视器（锁），来完成同步。</p>
<p>&emsp;&emsp;  当方法调用时，调用指令将会检查方法的 ACC_SYNCHRONIZED 访问标志是否被设置，如果设置了，执行线程将先获取 monitor ，获取成功之后才能执行方法体，方法执行完后再释放 monitor 。在方法执行期间，其他任何线程都无法再获得同一个 monitor 对象。 其实本质上没有区别，只是方法的同步是一种隐式的方式来实现，无需通过字节码来完成。</p>
<h2 id="2-对象头和锁"><a href="#2-对象头和锁" class="headerlink" title="2. 对象头和锁"></a>2. 对象头和锁</h2><p>&emsp;&emsp;  一个对象在内存中分为三部分：对象头、实例数据、对齐填充。</p>
<ol>
<li><p>对象头中主要存放了 GC 分代年龄、偏向锁、偏向 id、锁类型、hash 值等。jvm 一般会用两个字来存放对象头，(如果对象是数组则会分配3个字，多出来的1个字记录的是数组长度)，其主要结构是由Mark Word 和 Class Metadata Address 组成。MarkWord里默认数据是存储对象的HashCode等信息，但是会随着对象的运行改变而发生变化，不同的锁状态对应着不同的记录存储方式<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-29/12286618.jpg" alt></p>
</li>
<li><p>实例数据就包括对象字段的值，不仅有自己的值还有继承自父类的字段的值。一般字段的顺序是同类型的字段放在一起，空间比较大的字段放在前面。在满足上面的规则下父类的放在子类的前面。</p>
</li>
<li><p>对其填充并非必要的，整个对象需要是 8 字节的整数倍，当不足的时候会进行填充以达到 8 字节整数倍，主要还是为了方便存取。</p>
</li>
</ol>
<p>&emsp;&emsp;  这里我们主要分析一下重量级锁也就是通常说synchronized的对象锁，锁标识位为10，其中指针指向的是monitor对象（在 Synchronized 代码块中的监视器 ）的起始地址。每个对象都存在着一个 monitor 与之关联，对象与其 monitor 之间的关系有存在多种实现方式，如 monitor 可以与对象一起创建销毁或当线程试图获取对象锁时自动生成，但当一个 monitor 被某个线程持有后，它便处于锁定状态。。在Java虚拟机(HotSpot)中，monitor是由ObjectMonitor实现的，其主要数据结构如下。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">ObjectMonitor() &#123;</span><br><span class="line">    _count        = <span class="number">0</span>; <span class="comment">//记录个数</span></span><br><span class="line">    _owner        = <span class="literal">NULL</span>; <span class="comment">// 运行的线程</span></span><br><span class="line">    <span class="comment">//两个队列</span></span><br><span class="line">    _WaitSet      = <span class="literal">NULL</span>; <span class="comment">//调用 wait 方法会被加入到_WaitSet</span></span><br><span class="line">   _EntryList    = <span class="literal">NULL</span> ; <span class="comment">//锁竞争失败，会被加入到该列表</span></span><br><span class="line">  &#125;</span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;  ObjectMonitor中有两个队列，_WaitSet 和 _EntryList，用来保存ObjectWaiter对象列表( 每个等待锁的线程都会被封装成ObjectWaiter对象)，_owner指向持有ObjectMonitor对象的线程，当多个线程同时访问一段同步代码时，首先会进入 _EntryList 集合，当线程获取到对象的monitor 后进入 _Owner 区域并把monitor中的owner变量设置为当前线程同时monitor中的计数器count加1，若线程调用 wait() 方法，将释放当前持有的monitor，owner变量恢复为null，count自减1，同时该线程进入 WaitSe t集合中等待被唤醒。若当前线程执行完毕也将释放monitor(锁)并复位变量的值，以便其他线程进入获取monitor(锁)。<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-29/39529455.jpg" alt></p>
<h2 id="3-Synchronized-代码块原理"><a href="#3-Synchronized-代码块原理" class="headerlink" title="3. Synchronized 代码块原理"></a>3. Synchronized 代码块原理</h2><p>反编译下面的代码得到的字节码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SynchronizedTest</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">synchronized</span> (SynchronizedTest.class) &#123;</span><br><span class="line">            System.out.println(<span class="string">"hello"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-29/71639364.jpg" alt></p>
<p>&emsp;&emsp;  当执行monitorenter指令时，当前线程将试图获取 objectref(即对象锁) 所对应的 monitor 的持有权，当 objectref 的 monitor 的进入计数器为 0，那线程可以成功取得 monitor，并将计数器值设置为 1，取锁成功。如果当前线程已经拥有 objectref 的 monitor 的持有权，那它可以重入这个 monitor ，重入时计数器的值也会加 1。倘若其他线程已经拥有 objectref 的 monitor 的所有权，那当前线程将被阻塞，直到正在执行线程执行完毕，即monitorexit指令被执行，执行线程将释放 monitor(锁)并设置计数器值为0 ，其他线程将有机会持有 monitor 。值得注意的是编译器将会确保无论方法通过何种方式完成，方法中调用过的每条 monitorenter 指令都有执行其对应 monitorexit 指令，而无论这个方法是正常结束还是异常结束。为了保证在方法异常完成时 monitorenter 和 monitorexit 指令依然可以正确配对执行，编译器会自动产生一个异常处理器，这个异常处理器声明可处理所有的异常，它的目的就是用来执行 monitorexit 指令。所以看到上面有两条 monitorexit ！</p>
<h2 id="4-Synchronized-方法原理"><a href="#4-Synchronized-方法原理" class="headerlink" title="4. Synchronized 方法原理"></a>4. Synchronized 方法原理</h2><p>&emsp;&emsp;  先看一个反编译的实例方法的结果，确实比普通的方法多了一个标志字段。方法级的同步是隐式，即无需通过字节码指令来控制的，它实现在方法调用和返回操作之中。当方法调用时，调用指令将会 检查方法的 ACC_SYNCHRONIZED 访问标志是否被设置，如果设置了，执行线程将先持有 monitor ， 然后再执行方法，最后再方法完成(无论是正常完成还是非正常完成)时释放monitor。在方法执行期间，执行线程持有了monitor，其他任何线程都无法再获得同一个monitor。如果一个同步方法执行期间抛 出了异常，并且在方法内部无法处理此异常，那这个同步方法所持有的monitor将在异常抛到同步方法之外时自动释放。<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-29/58063890.jpg" alt></p>
<h2 id="5-偏向锁"><a href="#5-偏向锁" class="headerlink" title="5. 偏向锁"></a>5. 偏向锁</h2><p>&emsp;&emsp;  偏向锁是 Java 为了提高程序的性能而设计的一个比较优雅的加锁方式。偏向锁的核心思想是，如果一个线程获得了锁，那么锁就进入偏向模式，此时Mark Word 的结构也变为偏向锁结构，当这个线程再次请求锁时，无需再做获取锁的过程。如果有其他线程竞争锁的时候就需要膨胀为轻量级锁。这样就省去了大量有关锁申请的操作，从而也就提供程序的性能。</p>
<p>&emsp;&emsp;  所以，对于没有锁竞争的场合，偏向锁有很好的优化效果，毕竟极有可能连续多次是同一个线程申请相同的锁。但是对于锁竞争比较激烈的场合，偏向锁就失效了，因为这样场合极有可能每次申请锁的线程都是不相同的，因此这种场合下不应该使用偏向锁，否则会得不偿失，需要注意的是，偏向锁失败后，并不会立即膨胀为重量级锁，而是先升级为轻量级锁。</p>
<p>&emsp;&emsp;  偏向锁获取的过程如下，当锁对象第一次被线程获取的时候，虚拟机把对象头中的标志位设为“01”，即偏向模式。同时使用CAS操作把获取到这个锁的线程的ID记录在对象的Mark Word之中的偏向线程ID，并将是否偏向锁的状态位置置为1。如果CAS操作成功，持有偏向锁的线程以后每次进入这个锁相关的同步块时，直接检查ThreadId是否和自身线程Id一致，<br>如果一致，则认为当前线程已经获取了锁，虚拟机就可以不再进行任何同步操作（例如Locking、Unlocking及对Mark Word的Update等）。</p>
<p>&emsp;&emsp;  其实一般来说偏向锁很少又说去主动释放的，因为只有在其他线程需要获取锁的时候，也就是这个锁不仅仅被一个线程使用，可能有两个线程交替使用，根据对象是否被锁定来决定释放锁（恢复到未锁定状态）还是升级到轻量锁状态。</p>
<h2 id="6-轻量级锁"><a href="#6-轻量级锁" class="headerlink" title="6.轻量级锁"></a>6.轻量级锁</h2><p>&emsp;&emsp;  轻量级锁,一般指的是在有两个线程在交替使用锁的时候由于没有同时抢锁属于一种比较和谐的状态，就可以使用轻量级锁。他的基本思想是，当线程要获取锁时把锁对象的 Mark Word 复制一份到当前线程的栈顶，然后执行一个 CAS 操作把锁对象的 Mark Word 更新为指向栈顶的副本的指针，如果成功则当前线程拥有了锁。可以进行同步代码块的执行，而失败则有两种可能，要么是当前线程已经拥有了锁对象的指针，这时可以继续执行。要么是被其他线程抢占了锁对象，这时候说明了在同一时间有两个线程同时需要竞争锁，那么就打破了这种和谐的局面需要膨胀到重量级锁，锁对象的标志修改，获取线程的锁等待。<br>&emsp;&emsp;  在轻量级锁释放的过程就采用 CAS 把栈上的赋值的 Mark Word 替换到锁对象上，如果失败说明有其他线程执抢占过锁，锁对象的 Mark Word 的标志被修改过，在释放的同时唤醒等待的线程。</p>
]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>ConcurrentHashMap 源码分析</title>
    <url>/2018/03/27/Java%20SourceCode/ConcurrentHashMap%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="ConcurrentHashMap-源码分析"><a href="#ConcurrentHashMap-源码分析" class="headerlink" title="ConcurrentHashMap 源码分析"></a>ConcurrentHashMap 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-前言"><a href="#1-前言" class="headerlink" title="1. 前言"></a>1. 前言</h2><p>&emsp;&emsp;  终于到这个类了，其实在前面很过很多次这个类，因为这个类代码量比较大，并且涉及到并发的问题，还有一点就是这个代码有些真的晦涩，不好懂。前前后后大概花了三天的时间看完的一些重要操作，接着今天来整理一下。</p>
<a id="more"></a>
<p>&emsp;&emsp;  好了首先介绍一个个人的感受：</p>
<ol>
<li>首先这个类很多操作和 <code>HashMap</code> 是类似的，但是麻烦就麻烦在 <code>锁分离技术</code> 和 <code>并发处理</code> </li>
<li>底层还是采用的 <code>数组</code> + <code>链表</code> + <code>红黑树</code> 来实现的，但是红黑树的 <code>TreeNode</code> 改成了 <code>TreeBin</code></li>
<li>里面有很多 CAS （Compare And Swap）操作，比如说 <code>unsafe.compareAndSwapInt(this, valueOffset, expect, update)</code>意思是如果 <code>valueOffset</code> 位置包含的值与 <code>expect</code> 值相同，则更新 <code>valueOffset</code> 位置的值为update，并返回true，否则不更新，返回false。</li>
<li>不仅仅是 CAS 还有一些重量级的锁。也就是 <code>synchronized代码块</code> 用来保证操作同一数组元素下的节点的一致性，后面会看到。 </li>
</ol>
<h2 id="2-基本结构"><a href="#2-基本结构" class="headerlink" title="2. 基本结构"></a>2. 基本结构</h2><h3 id="1-继承"><a href="#1-继承" class="headerlink" title="1. 继承"></a>1. 继承</h3><p>AbstractMap</p>
<h3 id="2-实现"><a href="#2-实现" class="headerlink" title="2. 实现"></a>2. 实现</h3><p>ConcurrentMap&lt;K, V&gt; 和 Serializable 没有克隆。应该也是出于安全考虑！</p>
<h3 id="3-属性"><a href="#3-属性" class="headerlink" title="3. 属性"></a>3. 属性</h3><h4 id="1-基本属性"><a href="#1-基本属性" class="headerlink" title="1. 基本属性"></a>1. 基本属性</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 最大容量，同 hashmap</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MAXIMUM_CAPACITY = <span class="number">1</span> &lt;&lt; <span class="number">30</span>;</span><br><span class="line"><span class="comment">// 默认大小  16</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> DEFAULT_CAPACITY = <span class="number">16</span>;</span><br><span class="line"><span class="comment">// 数组的最大值</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MAX_ARRAY_SIZE = Integer.MAX_VALUE - <span class="number">8</span>;</span><br><span class="line"><span class="comment">// 并发数，不可修改   默认 16</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> DEFAULT_CONCURRENCY_LEVEL = <span class="number">16</span>;</span><br><span class="line"><span class="comment">// 负载因子 0.75</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">float</span> LOAD_FACTOR = <span class="number">0.75f</span>;</span><br><span class="line"><span class="comment">// 转成树链表最大长度 8</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> TREEIFY_THRESHOLD = <span class="number">8</span>;</span><br><span class="line"><span class="comment">// 转链表的节点数</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> UNTREEIFY_THRESHOLD = <span class="number">6</span>;</span><br><span class="line"><span class="comment">// 最小转换步长 16 常量不可修改</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MIN_TRANSFER_STRIDE = <span class="number">16</span>;</span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">int</span> RESIZE_STAMP_BITS = <span class="number">16</span>;</span><br><span class="line"><span class="comment">// 最大  transfer helper 数 默认 2^16-1</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MAX_RESIZERS = (<span class="number">1</span> &lt;&lt; (<span class="number">32</span> - RESIZE_STAMP_BITS)) - <span class="number">1</span>;</span><br><span class="line"><span class="comment">//</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> RESIZE_STAMP_SHIFT = <span class="number">32</span> - RESIZE_STAMP_BITS;</span><br><span class="line"><span class="comment">// 标志位</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MOVED = -<span class="number">1</span>; <span class="comment">// 正在 transfer 的节点的 hash 值</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> TREEBIN = -<span class="number">2</span>; <span class="comment">//  树根的 hash 值</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> RESERVED = -<span class="number">3</span>; <span class="comment">// 不进行序列化的 hash 值</span></span><br><span class="line"><span class="comment">// 可用 cpu 数</span></span><br><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> NCPU = Runtime.getRuntime().availableProcessors();</span><br><span class="line"><span class="comment">// 底层的数组 注意是 volatile 的</span></span><br><span class="line"><span class="keyword">transient</span> <span class="keyword">volatile</span> Node&lt;K, V&gt;[] table;</span><br><span class="line"><span class="comment">// 在 resize 的时候使用的数组，只有在 resize 的时候才不是 null  一样volatile</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node&lt;K, V&gt;[] nextTable;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 在没有竞争的时候使用，或者在初始化的时候作为一个反馈</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> <span class="keyword">long</span> baseCount;</span><br><span class="line"></span><br><span class="line"><span class="comment">// sizeCtl 是一个控制标志</span></span><br><span class="line"><span class="comment">// -1 表示初始化</span></span><br><span class="line"><span class="comment">// -N 表示有 N-1 个线程在 resize</span></span><br><span class="line"><span class="comment">// 正数或0代表hash表还没有被初始化，这个数值表示初始化</span></span><br><span class="line"><span class="comment">// 大于 0 表示下一次进行扩容的时候的阈值</span></span><br><span class="line"><span class="comment">// 它的值始终是当前ConcurrentHashMap容量的0.75倍，这与loadfactor是对应的。</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> <span class="keyword">int</span> sizeCtl;</span><br></pre></td></tr></table></figure>

<p>其中尤其要注意那个 <code>sizeCtl</code> 现在不知道他的意思后面可能就看不懂了！！</p>
<h4 id="2-Node"><a href="#2-Node" class="headerlink" title="2. Node"></a>2. Node</h4><p>和 <code>HashMap</code> 一样的。但是注意这个地方采用了 <code>volatile</code> 关键字，其他的真的一样，只有两个是 <code>volatile</code> 原因在于只有这两个是可变的，会变的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">K</span>, <span class="title">V</span>&gt; <span class="keyword">implements</span> <span class="title">Map</span>.<span class="title">Entry</span>&lt;<span class="title">K</span>, <span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> hash;</span><br><span class="line">    <span class="keyword">final</span> K key;</span><br><span class="line">    <span class="keyword">volatile</span> V val;</span><br><span class="line">    <span class="keyword">volatile</span> Node&lt;K, V&gt; next;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// hashcode 比较有意思 HashMap 中使用了 Objects.hashCode(key) ^ Objects.hashCode(value)</span></span><br><span class="line">    <span class="comment">// 但是 Objects 里面还是调用了 key 和 val 的 hashCode 所以原理一样</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">int</span> <span class="title">hashCode</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> key.hashCode() ^ val.hashCode();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 虽然 val 是 volatile 的变量但是不提供修改方法，否则抛异常  </span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">final</span> V <span class="title">setValue</span><span class="params">(V value)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> UnsupportedOperationException();</span><br><span class="line">    &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-Segment"><a href="#3-Segment" class="headerlink" title="3. Segment"></a>3. Segment</h4><p>&emsp;&emsp;  这个以前是特别重要的一个数据结构，基本所有的查删改都是依靠他的，但是在 <code>1.8</code> 中他的作用被削弱了，里面什么方法都没有。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Segment</span>&lt;<span class="title">K</span>, <span class="title">V</span>&gt; <span class="keyword">extends</span> <span class="title">ReentrantLock</span> <span class="keyword">implements</span> <span class="title">Serializable</span> </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">long</span> serialVersionUID = <span class="number">2249069246763182397L</span>;</span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">float</span> loadFactor;</span><br><span class="line"></span><br><span class="line">        Segment(<span class="keyword">float</span> lf) &#123;</span><br><span class="line">            <span class="keyword">this</span>.loadFactor = lf;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="4-TreeNode"><a href="#4-TreeNode" class="headerlink" title="4. TreeNode"></a>4. TreeNode</h4><p>由于直接继承了 <code>Node 节点</code> 所以相比 <code>HashMap</code> 中的 <code>TreeNode</code> 少了 before 和 after 属性。他的方法比较少主要是因为红黑树已经不用这个数据结构了而是采用的 <code>TreeBin</code> ，但是它存在是因为在转成红黑树的时候是先把 <code>Node</code> 封装成 <code>TreeNode</code> 然后再封装到 <code>TreeBin</code> 中的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">TreeNode</span>&lt;<span class="title">K</span>, <span class="title">V</span>&gt; <span class="keyword">extends</span> <span class="title">Node</span>&lt;<span class="title">K</span>, <span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">       TreeNode&lt;K, V&gt; parent;  <span class="comment">// red-black tree links</span></span><br><span class="line">       TreeNode&lt;K, V&gt; left;</span><br><span class="line">       TreeNode&lt;K, V&gt; right;</span><br><span class="line">       TreeNode&lt;K, V&gt; prev;    <span class="comment">// needed to unlink next upon deletion</span></span><br><span class="line">       <span class="keyword">boolean</span> red;</span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>

<h4 id="5-TreeBin"><a href="#5-TreeBin" class="headerlink" title="5. TreeBin"></a>5. TreeBin</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">TreeBin</span>&lt;<span class="title">K</span>, <span class="title">V</span>&gt; <span class="keyword">extends</span> <span class="title">Node</span>&lt;<span class="title">K</span>, <span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">       TreeNode&lt;K, V&gt; root;  <span class="comment">//用了上文中的 TreeNode</span></span><br><span class="line">       <span class="keyword">volatile</span> TreeNode&lt;K, V&gt; first;</span><br><span class="line">       <span class="keyword">volatile</span> Thread waiter;</span><br><span class="line">       <span class="keyword">volatile</span> <span class="keyword">int</span> lockState;</span><br><span class="line">       <span class="comment">// values for lockState</span></span><br><span class="line">       <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> WRITER = <span class="number">1</span>; <span class="comment">// set while holding write lock</span></span><br><span class="line">       <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> WAITER = <span class="number">2</span>; <span class="comment">// set when waiting for write lock</span></span><br><span class="line">       <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> READER = <span class="number">4</span>; <span class="comment">// increment value for setting read lock</span></span><br><span class="line">       </span><br><span class="line">       <span class="comment">//  大量的 rb-tree 的方法  不分析了</span></span><br><span class="line">       &#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-主要方法概览"><a href="#4-主要方法概览" class="headerlink" title="4. 主要方法概览"></a>4. 主要方法概览</h3><h4 id="1-ctor-4"><a href="#1-ctor-4" class="headerlink" title="1. ctor-4"></a>1. ctor-4</h4><p>&emsp;&emsp;  在构造方法中类似于 <code>HashMap</code> 的做法，在构造方法里面只进行一下参数的判断以及对一些属性进行赋值，但是没有对数组进行初始化。还是那个原理：延时加载，在 <code>put</code> 方法中肯定会对数组进行初始化。<br>&emsp;&emsp;  在这里主要设置的值肯定就是我们前面提到的 <code>sizeCtl</code> 属性，当他为整数的时候就是阈值，我们也看到阈值字段但是没有见到那个字段被使用，主要就是被 <code>sizeCtl</code> 实现了！还有需要设置的属性就是负载因子和初始的数组大小，默认是 <code>0.75</code> 和 <code>16</code> 。<br>&emsp;&emsp;  好具体说一下每一个方法的实现:</p>
<ol>
<li>第一个无参的就是空方法，所有的值采用默认</li>
<li>有初始大小的，就按照 1.5*n 转成 2^n 这个格式。</li>
<li>如果传入一个 Map 就和 HashMap 一样，调用 <code>putAll</code> 方法，然后 <code>putAll</code> 就是循环原来的数组，然后 <code>put</code> 到新的数组中。</li>
<li>其他的就是手动设置大小和阈值。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 空方法，注释说会创建大小为 16 的数组，估计是延时加载 在 put 里面</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ConcurrentHashMap</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 设置了 sizeCtl 就是下一次扩容的容量</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ConcurrentHashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (initialCapacity &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException();</span><br><span class="line">        <span class="comment">// 如果要开的数组比最大的一半还大，那就直接分配最大容量</span></span><br><span class="line">        <span class="comment">// 否则分配 1.5n+1 向上取 2^n</span></span><br><span class="line">        <span class="keyword">int</span> cap = ((initialCapacity &gt;= (MAXIMUM_CAPACITY &gt;&gt;&gt; <span class="number">1</span>)) ? MAXIMUM_CAPACITY : tableSizeFor(initialCapacity + (initialCapacity &gt;&gt;&gt; <span class="number">1</span>) + <span class="number">1</span>));</span><br><span class="line">        <span class="keyword">this</span>.sizeCtl = cap;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 同 HashMap</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ConcurrentHashMap</span><span class="params">(Map&lt;? extends K, ? extends V&gt; m)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.sizeCtl = DEFAULT_CAPACITY;</span><br><span class="line">        putAll(m);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ConcurrentHashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity, <span class="keyword">float</span> loadFactor)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(initialCapacity, loadFactor, <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 设置容量和负载因子</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ConcurrentHashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity,</span></span></span><br><span class="line"><span class="function"><span class="params">                             <span class="keyword">float</span> loadFactor, <span class="keyword">int</span> concurrencyLevel)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!(loadFactor &gt; <span class="number">0.0f</span>) || initialCapacity &lt; <span class="number">0</span> || concurrencyLevel &lt;= <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException();</span><br><span class="line">        <span class="comment">// 容量不能小于并发数</span></span><br><span class="line">        <span class="keyword">if</span> (initialCapacity &lt; concurrencyLevel)   <span class="comment">// Use at least as many bins</span></span><br><span class="line">            initialCapacity = concurrencyLevel;   <span class="comment">// as estimated threads</span></span><br><span class="line">        <span class="keyword">long</span> size = (<span class="keyword">long</span>) (<span class="number">1.0</span> + (<span class="keyword">long</span>) initialCapacity / loadFactor);</span><br><span class="line">        <span class="keyword">int</span> cap = (size &gt;= (<span class="keyword">long</span>) MAXIMUM_CAPACITY) ?</span><br><span class="line">                MAXIMUM_CAPACITY : tableSizeFor((<span class="keyword">int</span>) size);</span><br><span class="line">        <span class="keyword">this</span>.sizeCtl = cap;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-size-sumCount"><a href="#2-size-sumCount" class="headerlink" title="2. size/sumCount"></a>2. size/sumCount</h4><p>&emsp;&emsp;  <code>size</code> 方法直接调用了 <code>sumCount</code> ，然后 <code>sumCount</code> 作用就是统计一下 <code>cellCount</code> 数组中的值的和，这时候我们会发现 <code>cellCount</code> 是一个类，自己定义了一个静态内部类，作用就是专门来统计节点的数量。可见统计节点在并发情况是一件很困难的事，这里还专门设计了一个类来进行统计。里面就一个字段 volatile 的 long 。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">size</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">long</span> n = sumCount();</span><br><span class="line">        <span class="comment">// 如果有一些奇怪的值，比如大于最大小于最小就设置为最大，或者最小</span></span><br><span class="line">        <span class="comment">// 否则就是正常的值</span></span><br><span class="line">        <span class="keyword">return</span> ((n &lt; <span class="number">0L</span>) ? <span class="number">0</span> : (n &gt; (<span class="keyword">long</span>) Integer.MAX_VALUE) ? Integer.MAX_VALUE : (<span class="keyword">int</span>) n);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">CounterCell</span> </span>&#123;</span><br><span class="line">        <span class="keyword">volatile</span> <span class="keyword">long</span> value;</span><br><span class="line"></span><br><span class="line">        CounterCell(<span class="keyword">long</span> x) &#123;</span><br><span class="line">            value = x;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">final</span> <span class="keyword">long</span> <span class="title">sumCount</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        CounterCell[] as = counterCells;</span><br><span class="line">        CounterCell a;</span><br><span class="line">        <span class="keyword">long</span> sum = baseCount;</span><br><span class="line">        <span class="keyword">if</span> (as != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; as.length; ++i) &#123;</span><br><span class="line">                <span class="keyword">if</span> ((a = as[i]) != <span class="keyword">null</span>)</span><br><span class="line">                    sum += a.value;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> sum;</span><br><span class="line">    &#125;</span><br><span class="line">```     </span><br><span class="line">#### 3. get</span><br><span class="line">&amp;emsp;&amp;emsp;  get 方法就和 HashMap 差不多，因为我们会发现他连锁都没有加，直接就以前的那个套路，首先计算 hash 然后找到数组的节点，看第一个是不是，然后看是否是红黑树或者是链表，它们采用了顺序查找，就 <span class="keyword">while</span> 循环。</span><br><span class="line">&amp;emsp;&amp;emsp;  那么我们需要考虑一下为什么不需要加锁，难道我们在读元素的时候同时有别的线程在写不会出现安全问题？举个例子，当我们在遍历一个链表寻找元素的时候刚好有线程在链表的结尾做插入操作，要么我们读到链表结尾的时候，写线程没有更新链表的结尾元素那么我们就认为读先于写也就没有安全问题，因为我们在读的时候不会发现尾节点指针正好发生变化，简单来说写线程的节点指针操作是原子的，对其他线程也是可见的，这时你应该清楚为什么链表的 `next` 是 `<span class="keyword">volatile</span>` 。</span><br><span class="line"></span><br><span class="line">```java</span><br><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">get</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">        Node&lt;K, V&gt;[] tab;</span><br><span class="line">        Node&lt;K, V&gt; e, p;</span><br><span class="line">        <span class="keyword">int</span> n, eh;</span><br><span class="line">        K ek;</span><br><span class="line">        <span class="keyword">int</span> h = spread(key.hashCode());</span><br><span class="line">        <span class="keyword">if</span> ((tab = table) != <span class="keyword">null</span> &amp;&amp; (n = tab.length) &gt; <span class="number">0</span> &amp;&amp; (e = tabAt(tab, (n - <span class="number">1</span>) &amp; h)) != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 判断头结点是不是</span></span><br><span class="line">            <span class="keyword">if</span> ((eh = e.hash) == h) &#123;</span><br><span class="line">                <span class="keyword">if</span> ((ek = e.key) == key || (ek != <span class="keyword">null</span> &amp;&amp; key.equals(ek)))</span><br><span class="line">                    <span class="keyword">return</span> e.val;</span><br><span class="line">            <span class="comment">// 红黑树查找</span></span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (eh &lt; <span class="number">0</span>)</span><br><span class="line">                <span class="keyword">return</span> (p = e.find(h, key)) != <span class="keyword">null</span> ? p.val : <span class="keyword">null</span>;</span><br><span class="line">            <span class="comment">// 顺序查找</span></span><br><span class="line">            <span class="keyword">while</span> ((e = e.next) != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> (e.hash == h &amp;&amp; ((ek = e.key) == key || (ek != <span class="keyword">null</span> &amp;&amp; key.equals(ek))))</span><br><span class="line">                    <span class="keyword">return</span> e.val;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="4-containsKey-Value（Traverser对象）"><a href="#4-containsKey-Value（Traverser对象）" class="headerlink" title="4. containsKey/Value（Traverser对象）"></a>4. containsKey/Value（Traverser对象）</h4><p>&emsp;&emsp;  <code>containsKey</code> 这个方法比较简单，因为我们直接可以调用 <code>get</code> 方法就可以判断。因此它的实现就是调用了 <code>get</code> 方法。<br>&emsp;&emsp;  <code>containsValue</code> 方法必然是需要遍历节点的，因为他需要一个个的比较元素的值。里面并没有像 <code>get</code> 一样遍历，而是采用了一个遍历器 <code>Traverser</code> 这个遍历器的主要作用是如果发现正在 transfer ，就切换到新的数组里面，获取最新插入的值。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">containsKey</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">return</span> get(key) != <span class="keyword">null</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">containsValue</span><span class="params">(Object value)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">if</span> (value == <span class="keyword">null</span>)</span><br><span class="line">           <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">       Node&lt;K, V&gt;[] t;</span><br><span class="line">       <span class="keyword">if</span> ((t = table) != <span class="keyword">null</span>) &#123;</span><br><span class="line">           Traverser&lt;K, V&gt; it = <span class="keyword">new</span> Traverser&lt;K, V&gt;(t, t.length, <span class="number">0</span>, t.length);</span><br><span class="line">           <span class="keyword">for</span> (Node&lt;K, V&gt; p; (p = it.advance()) != <span class="keyword">null</span>; ) &#123;</span><br><span class="line">               V v;</span><br><span class="line">               <span class="keyword">if</span> ((v = p.val) == value || (v != <span class="keyword">null</span> &amp;&amp; value.equals(v)))</span><br><span class="line">                   <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h4 id="5-put-putVal"><a href="#5-put-putVal" class="headerlink" title="5. put/putVal"></a>5. put/putVal</h4><p>&emsp;&emsp;  好了，现在就准备开始介绍最重要的，put 方法了。这个方法里面直接调用了 putVal ，其实 putVal 的逻辑有点复杂，就单看代码来说，代码量还是比较大的。<br>&emsp;&emsp;  那么我们先介绍一下 putVal 的大概的逻辑然后再看代码。</p>
<ol>
<li><p>首先就判断了 key 和 value 不能为空的情况，一开始觉得还挺奇怪的 HashMap 就允许 null 作为键值为什么 ConcurrentHashMap 却不行，这里我查了一下这段源码作者 Doug Lea 的解释是 “ 在普通的非并发的 HashMap 中我们可以存放 null 然后至于这个地方是否是真的为 null 还是由于键不存在呢，我们可以采用 <code>containsKey</code> 来判断。但是在并发情况下在调用方法时可能会发生引用关系的变化导致歧义 ”。感觉还是有点抽象难懂，我自己的理解就是：当我们调用 <code>map.get(key)</code> 时候返回 <code>null</code> 的时候假如根本不存在这个 <code>key</code> ，但是我们由于不清楚这个地方的值到底是 <code>null</code> 还是这个 <code>key</code> 根本不存在，我们就会调用 <code>containsKey</code> 来检查，但是在这个时间间隙中有其他线程往里面放了一个 <code>key -&gt; null</code> 导致我们检测的结果是有这个键值对，从而误判！</p>
</li>
<li><p>然后我们前面也提到了，在构造方法中没有任何初始化表的操作，所以说我们在 putVal 中如果判断到表空，就需要进行初始化工作。这个初始化调用了一个 <code>initTable()</code> 这个方法稍后专门分析。</p>
</li>
<li><p>接着就是利用 hash 值来获取数组的索引了，首先还是判断那个对应的位置有没有元素，如果没有的话就简单了，采用 CAS 操作添加一个新节点此时添加工作就完成了可以返回了。如果不是这种情况就继续往下看。</p>
</li>
<li><p>我们看头结点的 Hash 值是否是 <code>MOVED</code> ，如果是就说明当前的表正在进行 <code>transfer</code> 我们就让当前线程去帮助 <code>transfer</code> 。现在没看懂没关系等看到 <code>transfer</code> 方法的时候就知道为什么了。</p>
</li>
<li><p>否则的话就是一个正常的链表或者红黑树了，这时候我们就和 HashMap 一样，如果是一个链表我们就遍历链表，然后遇到相同的 key 进行 value 的替换，否则插入到链表的结尾。 如果是一个红黑树就执行红黑树的插入操作。 注意这个操作是在同步代码块中进行的，因为我们不能保证多线程的修改安全，但是这个代码块的锁是头结点，也就是数组有多少元素我们就可以同时操作多少把锁，这样并发数就是数组的长度。而前面定义的并发数没有实质的作用。</p>
</li>
<li><p>最后由于我们插入了一个节点需要判断一下当前的节点数是不是大于转红黑树的阈值(默认为8)。是则调用 <code>treeifyBin</code> ，这个方法也比较复杂，待会专门来说。 </p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 直接调用</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">put</span><span class="params">(K key, V value)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> putVal(key, value, <span class="keyword">false</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> V <span class="title">putVal</span><span class="params">(K key, V value, <span class="keyword">boolean</span> onlyIfAbsent)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 不允许 null 键值 解释是在我们没办法判断是没有对应的值还是值为 null 。</span></span><br><span class="line">    <span class="comment">// 在非并发的情况下我们可以使用 containsKey/Value(null) 来明确知道 是不是有 null key/val</span></span><br><span class="line">    <span class="comment">// 这也就解释了为什么 hashmap 在查找的时候采用了先使用 null 来查找的策略</span></span><br><span class="line">    <span class="comment">// 但是并发的话，他底层调用了 get 方法，而 get 方法不是同步的，有可能会发生改变产生歧义</span></span><br><span class="line">    <span class="keyword">if</span> (key == <span class="keyword">null</span> || value == <span class="keyword">null</span>) <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">    <span class="comment">// 得到 hash 值</span></span><br><span class="line">    <span class="keyword">int</span> hash = spread(key.hashCode());</span><br><span class="line">    <span class="comment">// 用于记录相应链表的长度</span></span><br><span class="line">    <span class="keyword">int</span> binCount = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (Node&lt;K, V&gt;[] tab = table; ; ) &#123;</span><br><span class="line">        Node&lt;K, V&gt; f;</span><br><span class="line">        <span class="keyword">int</span> n, i, fh;</span><br><span class="line">        <span class="comment">// 如果数组为 null 也就是没有初始化(延时加载)或者数组没有元素，进行数组初始化</span></span><br><span class="line">        <span class="keyword">if</span> (tab == <span class="keyword">null</span> || (n = tab.length) == <span class="number">0</span>)</span><br><span class="line">            <span class="comment">// 初始化数组，后面会详细介绍</span></span><br><span class="line">            tab = initTable();</span><br><span class="line">        <span class="comment">// 找该 hash 值对应的数组下标，得到第一个节点 f</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((f = tabAt(tab, i = (n - <span class="number">1</span>) &amp; hash)) == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 如果数组该位置为空， 用一次 CAS 操作将这个新值放入其中即可，这个 put 操作差不多就结束了，可以拉到方法的最后面了</span></span><br><span class="line">            <span class="comment">// 如果 CAS 成功，那就是没有并发操作 方法可以结束了  有并发操作就进行下一次循环，注意外面的循环是死循环</span></span><br><span class="line">            <span class="keyword">if</span> (casTabAt(tab, i, <span class="keyword">null</span>, <span class="keyword">new</span> Node&lt;K, V&gt;(hash, key, value, <span class="keyword">null</span>)))</span><br><span class="line">                <span class="keyword">break</span>;                   <span class="comment">// no lock when adding to empty bin</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// hash 居然可以等于 MOVED，这个需要到后面才能看明白，不过从名字上也能猜到，肯定是因为在扩容</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> ((fh = f.hash) == MOVED)</span><br><span class="line">            <span class="comment">// 帮助数据迁移，这个等到看完数据迁移部分的介绍后，再理解这个就很简单了</span></span><br><span class="line">            tab = helpTransfer(tab, f);</span><br><span class="line">        <span class="comment">// 到这里就是说，f 是该位置的头结点，而且不为空  也就是一般情况</span></span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            V oldVal = <span class="keyword">null</span>;</span><br><span class="line">            <span class="comment">// 获取数组该位置的头结点的监视器锁</span></span><br><span class="line">            <span class="keyword">synchronized</span> (f) &#123;</span><br><span class="line">                <span class="comment">// 判断是否有新的节点插入到头部，或者删除头部节点造成不匹配  进行下一次循环</span></span><br><span class="line">                <span class="keyword">if</span> (tabAt(tab, i) == f) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (fh &gt;= <span class="number">0</span>) &#123; <span class="comment">// 头结点的 hash 值大于等于 0，说明是链表</span></span><br><span class="line">                        binCount = <span class="number">1</span>;</span><br><span class="line">                        <span class="comment">// 遍历链表</span></span><br><span class="line">                        <span class="keyword">for</span> (Node&lt;K, V&gt; e = f; ; ++binCount) &#123;</span><br><span class="line">                            K ek;</span><br><span class="line">                            <span class="comment">// 如果发现了"相等"的 key，直接覆盖他的 value ，方法结束</span></span><br><span class="line">                            <span class="keyword">if</span> (e.hash == hash &amp;&amp; ((ek = e.key) == key || (ek != <span class="keyword">null</span> &amp;&amp; key.equals(ek)))) &#123;</span><br><span class="line">                                oldVal = e.val;</span><br><span class="line">                                <span class="keyword">if</span> (!onlyIfAbsent)</span><br><span class="line">                                    e.val = value;</span><br><span class="line">                                <span class="keyword">break</span>;</span><br><span class="line">                            &#125;</span><br><span class="line">                            <span class="comment">// 到了链表的最末端，将这个新值放到链表的最后面</span></span><br><span class="line">                            Node&lt;K, V&gt; pred = e;</span><br><span class="line">                            <span class="keyword">if</span> ((e = e.next) == <span class="keyword">null</span>) &#123;</span><br><span class="line">                                pred.next = <span class="keyword">new</span> Node&lt;K, V&gt;(hash, key,</span><br><span class="line">                                        value, <span class="keyword">null</span>);</span><br><span class="line">                                <span class="keyword">break</span>;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (f <span class="keyword">instanceof</span> TreeBin) &#123; <span class="comment">// 红黑树</span></span><br><span class="line">                        Node&lt;K, V&gt; p;</span><br><span class="line">                        binCount = <span class="number">2</span>;</span><br><span class="line">                        <span class="comment">// 调用红黑树的插值方法插入新节点</span></span><br><span class="line">                        <span class="keyword">if</span> ((p = ((TreeBin&lt;K, V&gt;) f).putTreeVal(hash, key,</span><br><span class="line">                                value)) != <span class="keyword">null</span>) &#123;</span><br><span class="line">                            oldVal = p.val;</span><br><span class="line">                            <span class="keyword">if</span> (!onlyIfAbsent)</span><br><span class="line">                                p.val = value;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 退出链表/红黑树的操作  的同步代码块</span></span><br><span class="line">            <span class="comment">// binCount != 0 说明上面在做链表操作</span></span><br><span class="line">            <span class="keyword">if</span> (binCount != <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">// 判断是否要将链表转换为红黑树，临界值和 HashMap 一样，也是 8</span></span><br><span class="line">                <span class="keyword">if</span> (binCount &gt;= TREEIFY_THRESHOLD)</span><br><span class="line">                    <span class="comment">// 这个方法和 HashMap 中稍微有一点点不同，那就是它不是一定会进行红黑树转换，</span></span><br><span class="line">                    <span class="comment">// 如果当前数组的长度小于 64，那么会选择进行数组扩容，而不是转换为红黑树</span></span><br><span class="line">                    <span class="comment">// 具体源码我们就不看了，扩容部分后面说</span></span><br><span class="line">                    treeifyBin(tab, i);</span><br><span class="line">                <span class="keyword">if</span> (oldVal != <span class="keyword">null</span>)</span><br><span class="line">                    <span class="keyword">return</span> oldVal;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    addCount(<span class="number">1L</span>, binCount);</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="6-initTable"><a href="#6-initTable" class="headerlink" title="6. initTable"></a>6. initTable</h4><p>&emsp;&emsp;  好，上面已经分析了关于 put 方法，我们还有两个问题没有解决，一个是初始化表，另外一个是转成红黑树，先分析表的初始化。那先回顾一下 HashMap 他是采用同样的延时加载然后在 <code>resize</code> 方法中进行了表的初始化工作。也就是 HashMap 的 <code>resize</code> 方法同时进行了初始化和扩容以及迁移的工作，在 ConcurrentHashMap 中划分的更细致，初始化就只进行初始化，因为他是并发的要考虑到更多。<br>&emsp;&emsp; 一样的，我们先分析一下大致的思路。</p>
<ol>
<li><p>首选先需要看是不是有别的线程在进行初始化，如果是我们就不要进行初始化了让出 cpu 资源让别的线程继续初始化。这个如何判断别的线程是否在初始化？就涉及到了前面的 <code>sizeCtl</code> 属性，当他是 -1 的时候就说明在进行表的初始化工作。</p>
</li>
<li><p>显然当别的线程没有初始化，当前线程就要初始化。并且不让别的线程进行争夺，就把 <code>sizeCtl</code> 用 CAS 置为 -1，并开始初始化。</p>
</li>
<li><p>初始化的工作有两个，一是 new 一个容量为 16 的新数组，其次设置扩容的阈值也就是 <code>sizeCtl</code> 的值，设置好了也说明初始化完毕。</p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">final</span> Node&lt;K, V&gt;[] initTable() &#123;</span><br><span class="line">    Node&lt;K, V&gt;[] tab;</span><br><span class="line">    <span class="keyword">int</span> sc;</span><br><span class="line">    <span class="keyword">while</span> ((tab = table) == <span class="keyword">null</span> || tab.length == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="comment">// 别的线程已经初始化好了或者正在初始化 sizeCtl 为 -1</span></span><br><span class="line">        <span class="keyword">if</span> ((sc = sizeCtl) &lt; <span class="number">0</span>)</span><br><span class="line">            Thread.yield(); <span class="comment">// 让出线程的执行权</span></span><br><span class="line">        <span class="comment">// CAS 一下，将 sizeCtl 设置为 -1，代表抢到了锁 基本在这就相当于同步代码块</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc, -<span class="number">1</span>)) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> ((tab = table) == <span class="keyword">null</span> || tab.length == <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="comment">// DEFAULT_CAPACITY 默认初始容量是 16</span></span><br><span class="line">                    <span class="keyword">int</span> n = (sc &gt; <span class="number">0</span>) ? sc : DEFAULT_CAPACITY;</span><br><span class="line">                    <span class="comment">// 初始化数组，长度为 16 或初始化时提供的长度</span></span><br><span class="line">                    Node&lt;K, V&gt;[] nt = (Node&lt;K, V&gt;[]) <span class="keyword">new</span> Node&lt;?, ?&gt;[n];</span><br><span class="line">                    <span class="comment">// 将这个数组赋值给 table，table 是 volatile 的  他的写发生在别人的读之前</span></span><br><span class="line">                    table = tab = nt;</span><br><span class="line">                    <span class="comment">// 如果 n 为 16 的话，那么这里 sc = 12 其实就是 0.75 * n</span></span><br><span class="line">                    sc = n - (n &gt;&gt;&gt; <span class="number">2</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                <span class="comment">// 设置下次扩容的时候的阈值</span></span><br><span class="line">                sizeCtl = sc;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> tab;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="7-treeifyBin"><a href="#7-treeifyBin" class="headerlink" title="7. treeifyBin"></a>7. treeifyBin</h4><p>&emsp;&emsp;  行，解决了初始化接下来就要看看链表转红黑树的操作了。这个操作没有像我想的那样直接进行转树的操作，而是做了一系列的判断。<br>&emsp;&emsp;  当链表长度大于等于 16  但数组长度小于 64 时，需要进行一次扩容操作，扩容操作又委托给了 <code>tryPresize</code> 扩容是预计扩容到原来的两倍。注意区分链表长度和数组长度不要弄混了！！！<br>&emsp;&emsp;  接下来就是真正的链表转成树的操作了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">treeifyBin</span><span class="params">(Node&lt;K, V&gt;[] tab, <span class="keyword">int</span> index)</span> </span>&#123;</span><br><span class="line">        Node&lt;K, V&gt; b;</span><br><span class="line">        <span class="keyword">int</span> n, sc;</span><br><span class="line">        <span class="keyword">if</span> (tab != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">// MIN_TREEIFY_CAPACITY 为 64</span></span><br><span class="line">            <span class="comment">// 所以，如果数组长度小于 64 的时候，其实也就是 32 或者 16 或者更小的时候，会进行数组扩容</span></span><br><span class="line">            <span class="keyword">if</span> ((n = tab.length) &lt; MIN_TREEIFY_CAPACITY)</span><br><span class="line">                <span class="comment">// 扩容操作 大小扩容为 (3*n+1)--&gt; 2^n</span></span><br><span class="line">                tryPresize(n &lt;&lt; <span class="number">1</span>);</span><br><span class="line">                <span class="comment">// b 是头结点</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((b = tabAt(tab, index)) != <span class="keyword">null</span> &amp;&amp; b.hash &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="comment">// 加锁</span></span><br><span class="line">                <span class="keyword">synchronized</span> (b) &#123;</span><br><span class="line"></span><br><span class="line">                    <span class="keyword">if</span> (tabAt(tab, index) == b) &#123;</span><br><span class="line">                        <span class="comment">// 下面就是遍历链表，建立一颗红黑树</span></span><br><span class="line">                        TreeNode&lt;K, V&gt; hd = <span class="keyword">null</span>, tl = <span class="keyword">null</span>;</span><br><span class="line">                        <span class="keyword">for</span> (Node&lt;K, V&gt; e = b; e != <span class="keyword">null</span>; e = e.next) &#123;</span><br><span class="line">                            TreeNode&lt;K, V&gt; p =</span><br><span class="line">                                    <span class="keyword">new</span> TreeNode&lt;K, V&gt;(e.hash, e.key, e.val,</span><br><span class="line">                                            <span class="keyword">null</span>, <span class="keyword">null</span>);</span><br><span class="line">                            <span class="keyword">if</span> ((p.prev = tl) == <span class="keyword">null</span>)</span><br><span class="line">                                hd = p;</span><br><span class="line">                            <span class="keyword">else</span></span><br><span class="line">                                tl.next = p;</span><br><span class="line">                            tl = p;</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="comment">// 将红黑树设置到数组相应位置中</span></span><br><span class="line">                        setTabAt(tab, index, <span class="keyword">new</span> TreeBin&lt;K, V&gt;(hd));</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="8-tryPresize"><a href="#8-tryPresize" class="headerlink" title="8. tryPresize"></a>8. tryPresize</h4><p>&emsp;&emsp;  继续分析上面遗留下来的问题，扩容操作。</p>
<ol>
<li><p>首先判断表是否为空，空则进行初始化，代码同 <code>initTable</code> </p>
</li>
<li><p>如果 <code>3*tableSize+1 &lt; 扩容阈值</code> 就不扩容，这个情况基本不会发生。</p>
</li>
<li><p>将 <code>sizeCtl</code> 设置为下次要进行扩容的阈值，然后进行 <code>transfer</code> ，这个方法里面才是真正的将数组大小扩充为原来的两倍并且进行数据迁移。</p>
</li>
</ol>
<p>&emsp;&emsp;  整体看起来还是以前的 HashMap 的套路，他的 <code>resize</code> 进行初始化、扩容、数据迁移。而这里把这三步拆成了三个方法分别来做，以保证高并发。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">tryPresize</span><span class="params">(<span class="keyword">int</span> size)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// c：size*1.5+1 ，再往上取最近的 2 的 n 次方。</span></span><br><span class="line">        <span class="keyword">int</span> c = (size &gt;= (MAXIMUM_CAPACITY &gt;&gt;&gt; <span class="number">1</span>)) ? MAXIMUM_CAPACITY : tableSizeFor(size + (size &gt;&gt;&gt; <span class="number">1</span>) + <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">int</span> sc;</span><br><span class="line">        <span class="comment">// sizeCtl &gt; 0 表示下次扩容的阈值</span></span><br><span class="line">        <span class="keyword">while</span> ((sc = sizeCtl) &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">            Node&lt;K, V&gt;[] tab = table;</span><br><span class="line">            <span class="keyword">int</span> n;</span><br><span class="line">            <span class="comment">// 数组初始化</span></span><br><span class="line">            <span class="comment">// 这个 if 分支和之前说的初始化数组的代码基本上是一样的，在这里，我们可以不用管这块代码</span></span><br><span class="line">            <span class="keyword">if</span> (tab == <span class="keyword">null</span> || (n = tab.length) == <span class="number">0</span>) &#123;</span><br><span class="line">                n = (sc &gt; c) ? sc : c;</span><br><span class="line">                <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc, -<span class="number">1</span>)) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        <span class="keyword">if</span> (table == tab) &#123;</span><br><span class="line">                            <span class="meta">@SuppressWarnings</span>(<span class="string">"unchecked"</span>)</span><br><span class="line">                            Node&lt;K, V&gt;[] nt = (Node&lt;K, V&gt;[]) <span class="keyword">new</span> Node&lt;?, ?&gt;[n];</span><br><span class="line">                            table = nt;</span><br><span class="line">                            sc = n - (n &gt;&gt;&gt; <span class="number">2</span>); <span class="comment">// 0.75 * n</span></span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                        sizeCtl = sc;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            <span class="comment">// 如果扩容后的值还小于等于阈值或者，当前数组长度已经达到最大了 不进行扩容</span></span><br><span class="line">            &#125; <span class="keyword">else</span> <span class="keyword">if</span> (c &lt;= sc || n &gt;= MAXIMUM_CAPACITY)</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="comment">// 进行扩容操作</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (tab == table) &#123;</span><br><span class="line">                <span class="keyword">int</span> rs = resizeStamp(n);</span><br><span class="line">                <span class="comment">// 不可能发生</span></span><br><span class="line">                <span class="keyword">if</span> (sc &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                    Node&lt;K, V&gt;[] nt;</span><br><span class="line">                    <span class="keyword">if</span> ((sc &gt;&gt;&gt; RESIZE_STAMP_SHIFT) != rs ||</span><br><span class="line">                            sc == rs + <span class="number">1</span> ||</span><br><span class="line">                            sc == rs + MAX_RESIZERS ||</span><br><span class="line">                            (nt = nextTable) == <span class="keyword">null</span> ||</span><br><span class="line">                            transferIndex &lt;= <span class="number">0</span></span><br><span class="line">                            )</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc, sc + <span class="number">1</span>))</span><br><span class="line">                        transfer(tab, nt);</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 1. 将 SIZECTL 设置为 (rs &lt;&lt; RESIZE_STAMP_SHIFT) + 2  这个值等于</span></span><br><span class="line">                <span class="comment">//  (n的32位二进制中空位数 &lt;&lt; 16 ) + 2 肯定是正数 也就是下次扩容的阈值</span></span><br><span class="line">                <span class="comment">//  调用 transfer 方法，此时 nextTab 参数为 null</span></span><br><span class="line"></span><br><span class="line">                <span class="comment">// 2. 这个 cas 操作保证了只有一个线程会最先进行 transfer 操作</span></span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc, (rs &lt;&lt; RESIZE_STAMP_SHIFT) + <span class="number">2</span>))</span><br><span class="line">                    transfer(tab, <span class="keyword">null</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="9-transfer"><a href="#9-transfer" class="headerlink" title="9.  transfer"></a>9.  transfer</h4><p>&emsp;&emsp;  好了，该进入正题了，<code>transfer</code> 是这个类中最麻烦也是最精巧的一个方法，还是先说思路再阅读代码。</p>
<ol>
<li><p>如果当前的 <code>nextTab</code> 是空，也就是说需要进行扩容的数组还没有初始化，那么初始化一个大小是原来两倍的数组出来，作为扩容后的新数组。</p>
</li>
<li><p>我们分配几个变量，来把原来的数组拆分成几个完全相同的段，你可以把他们想象成一个个大小相同的短数组，每个短数组的长度是 <code>stride</code> 。</p>
</li>
<li><p>我们先取最后一个短数组，用 <code>i</code> 表示一个可变的指针，可指向短数组的任意一个位置，最开始指向的是短数组的结尾。<code>bound</code> 表示短数组的下界，也就是开始的位置。也就是我们在短数组选择的时候是采用从后往前进行的。</p>
</li>
<li><p>然后使用了一个全局的属性 <code>transferIndex</code>（线程共享），来记录当前已经选择过的短数组和还没有被选择的短数组之间的分隔。</p>
</li>
<li><p>那么当前的线程选择的这个短数组其实就是当前线程应该进行的数据迁移任务，也就是说当前线程就负责完成这一个小数组的迁移任务就行了。那么很显然在 <code>transferIndex</code> 之前的，没有被线程处理过的短数组就需要其他线程来帮忙进行数据迁移，其他线程来的时候看到的是 <code>transferIndex</code> 那么他们就会从 <code>transferIndex</code> 往前数 <code>stride</code> 个元素作为一个小数组当做自己的迁移任务。</p>
</li>
</ol>
<p>&emsp;&emsp;  好，现在可能感觉有点乱，来总结一下。当前的数组的迁移被分为很多的任务包，每一个任务包中有 <code>stride</code> 个元素，然后这些任务包需要被从后往前的分配给不同的线程。分配过程依赖于共享的全局变量 <code>transferIndex</code> 。这样做的原因就是为了高并发，不得不佩服写这个类源码的大师！ 下面用一张图在来总结一下上面所说的内容。<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-29/28767841.jpg" alt></p>
<p>&emsp;&emsp;  现在线程收到自己的任务包了，肯定就需要进行数据迁移的工作了。迁移工作就比较简单了，由于是需要对链表或红黑树节点进行操作，必须要对过程同步，锁还是头结点。进行节点迁移的时候，就是和 HashMap 一样，把原来的链表和树拆成两部分，分别放到 i 和 i+n 上。<br>&emsp;&emsp;  我们还是主要看链表的拆分，采用的看 hash 值的某一特定位是 0 还是 1 来决定放在哪个位置，节点采用的头插法，也就是部分的节点的顺序是反的。为什么说部分是反的？那是因为他在里面一大段代码都在干一件事，去找原链表的最后一段特定位相同的完整序列保持顺序不变。这个比较难说清楚还是用一张图来说明一下。<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-29/4080400.jpg" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 把节点拷贝到新数组里面</span></span><br><span class="line">    <span class="comment">// 调用情况： 1.在 put 中的转红黑树的时候，如果大小不足 64 试着扩容 时候调用了 transfer</span></span><br><span class="line">    <span class="comment">//           2.在调用 put 完成以后，有一个 addCount 操作里面也调用了 transfer</span></span><br><span class="line">    <span class="comment">//           3.helpTransfer 中</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">transfer</span><span class="params">(Node&lt;K, V&gt;[] tab, Node&lt;K, V&gt;[] nextTab)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> n = tab.length, stride;</span><br><span class="line">        <span class="comment">// 将这些节点分成若干个任务迁移，每一个任务里面的节点数就是 stride</span></span><br><span class="line">        <span class="comment">// 所以当只有一个 cpu 的时候就是一个任务，多个 cpu 按下面的规则</span></span><br><span class="line">        <span class="keyword">if</span> ((stride = (NCPU &gt; <span class="number">1</span>) ? (n &gt;&gt;&gt; <span class="number">3</span>) / NCPU : n) &lt; MIN_TRANSFER_STRIDE)</span><br><span class="line">            stride = MIN_TRANSFER_STRIDE; <span class="comment">// subdivide range</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 如果 nextTab 为 null，先进行一次初始化，在属性字段里面我们就看到了这个字段是只有在迁移的时候不是 null</span></span><br><span class="line">        <span class="comment">// 其他时间都是 null</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//由于前面的 cas 操作已经保证了只有一个线程进行 transfer 所以不担心每个线程都会 new 出自己的新数组</span></span><br><span class="line">        <span class="keyword">if</span> (nextTab == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="comment">// 容量翻倍，所以说前面那个 tryPresize 方法里面计算的希望要扩容的大小只是为了与阈值比较一下</span></span><br><span class="line">                <span class="comment">// 真正在扩容的时候只扩充为原来的 2 倍</span></span><br><span class="line">                Node&lt;K, V&gt;[] nt = (Node&lt;K, V&gt;[]) <span class="keyword">new</span> Node&lt;?, ?&gt;[n &lt;&lt; <span class="number">1</span>];</span><br><span class="line">                nextTab = nt;</span><br><span class="line">            &#125; <span class="keyword">catch</span> (Throwable ex) &#123;      <span class="comment">// try to cope with OOME</span></span><br><span class="line">                sizeCtl = Integer.MAX_VALUE;</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// volatile 赋值</span></span><br><span class="line">            nextTable = nextTab;</span><br><span class="line">            <span class="comment">// 用于控制迁移的位置</span></span><br><span class="line">            transferIndex = n;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 新数组长度</span></span><br><span class="line">        <span class="keyword">int</span> nextn = nextTab.length;</span><br><span class="line">        <span class="comment">// 标志节点</span></span><br><span class="line">        <span class="comment">// 这个构造方法会生成一个Node，key、value 和 next 都为 null，关键是 hash 为 MOVED</span></span><br><span class="line">        <span class="comment">// 后面我们会看到，原数组中位置 i 处的节点完成迁移工作后，</span></span><br><span class="line">        <span class="comment">// 就会将位置 i 处设置为这个 ForwardingNode，用来告诉其他线程该位置已经处理过了</span></span><br><span class="line">        ForwardingNode&lt;K, V&gt; fwd = <span class="keyword">new</span> ForwardingNode&lt;K, V&gt;(nextTab);</span><br><span class="line">        <span class="comment">// advance 指的是做完了一个位置的迁移工作，可以准备做下一个位置的了</span></span><br><span class="line">        <span class="keyword">boolean</span> advance = <span class="keyword">true</span>;</span><br><span class="line">        <span class="comment">// 在对 nextTable 赋值后记得清理节点  待会说</span></span><br><span class="line">        <span class="keyword">boolean</span> finishing = <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 死循环</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>, bound = <span class="number">0</span>; ; ) &#123;</span><br><span class="line">            Node&lt;K, V&gt; f;</span><br><span class="line">            <span class="keyword">int</span> fh;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 说了这么多 很多都是废话，其实就一句话就是：我们从数组的结尾开始把元素分成任务，其中每一个任务的节点数就是 stride</span></span><br><span class="line">            <span class="comment">// 任务是从后往前分配的，也就是最先分出去的是数组结尾的那一段。任务开始的元素下标是 i 结束的下标是 bound 注意 bound &lt; i</span></span><br><span class="line">            <span class="comment">// 因为是从后往前来的</span></span><br><span class="line">            <span class="keyword">while</span> (advance) &#123;</span><br><span class="line">                <span class="keyword">int</span> nextIndex, nextBound;</span><br><span class="line">                <span class="comment">// 刚领取的任务完成了 也就是 i&gt;=bound  一开始我找了半天的 --i 竟然藏在这里</span></span><br><span class="line">                <span class="keyword">if</span> (--i &gt;= bound || finishing)</span><br><span class="line">                    advance = <span class="keyword">false</span>;</span><br><span class="line">                <span class="comment">// 任务领完了 transferIndex 本来是数组的长度，现在都成0 了说明任务都分派完了</span></span><br><span class="line">                <span class="keyword">else</span> <span class="keyword">if</span> ((nextIndex = transferIndex) &lt;= <span class="number">0</span>) &#123;</span><br><span class="line">                    i = -<span class="number">1</span>;</span><br><span class="line">                    advance = <span class="keyword">false</span>;</span><br><span class="line">                <span class="comment">// 实施任务分派，更新了 TRANSFERINDEX 其他线程能看到</span></span><br><span class="line">                &#125; <span class="keyword">else</span> <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, TRANSFERINDEX, nextIndex, nextBound = (nextIndex &gt; stride ? nextIndex - stride : <span class="number">0</span>))) &#123;</span><br><span class="line">                    bound = nextBound;</span><br><span class="line">                    i = nextIndex - <span class="number">1</span>;</span><br><span class="line">                    advance = <span class="keyword">false</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">// 主要就是判断 i&lt;0 ， 也就是是不是都迁移完了  至于 i&gt;=n 是 i = n 导致的</span></span><br><span class="line">            <span class="keyword">if</span> (i &lt; <span class="number">0</span> || i &gt;= n || i + n &gt;= nextn) &#123;</span><br><span class="line">                <span class="keyword">int</span> sc;</span><br><span class="line">                <span class="comment">// 所有的迁移操作已经完成</span></span><br><span class="line">                <span class="comment">// 将新的 nextTab 赋值给 table 属性，完成迁移</span></span><br><span class="line">                <span class="comment">// 重新计算 sizeCtl = 1.5*n 下次的阈值 结束迁移方法</span></span><br><span class="line">                <span class="keyword">if</span> (finishing) &#123;</span><br><span class="line">                    nextTable = <span class="keyword">null</span>;</span><br><span class="line">                    table = nextTab;</span><br><span class="line">                    sizeCtl = (n &lt;&lt; <span class="number">1</span>) - (n &gt;&gt;&gt; <span class="number">1</span>);</span><br><span class="line">                    <span class="keyword">return</span>;</span><br><span class="line">                &#125;</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 之前我们说过，SIZECTL 在迁移前会设置为 (rs &lt;&lt; RESIZE_STAMP_SHIFT) + 2</span></span><br><span class="line">                <span class="comment">// d但是怎么会出现这种情况？？？？？</span></span><br><span class="line">                <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc = sizeCtl, sc - <span class="number">1</span>)) &#123;</span><br><span class="line">                    <span class="comment">// 任务结束，方法退出</span></span><br><span class="line">                    <span class="keyword">if</span> ((sc - <span class="number">2</span>) != resizeStamp(n) &lt;&lt; RESIZE_STAMP_SHIFT)</span><br><span class="line">                        <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">                    <span class="comment">// 到这里，说明 (sizeCtl - 2) == resizeStamp(n) &lt;&lt; RESIZE_STAMP_SHIFT，</span></span><br><span class="line">                    <span class="comment">// 也就是说，所有的迁移任务都做完了，也就会进入到上面的 if(finishing)&#123;&#125; 分支了</span></span><br><span class="line">                    finishing = advance = <span class="keyword">true</span>;</span><br><span class="line">                    i = n; <span class="comment">// recheck before commit</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 如果原数组 i 处是空的，没有任何节点，那么放入 ForwardingNode 作为标志</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((f = tabAt(tab, i)) == <span class="keyword">null</span>)</span><br><span class="line">                advance = casTabAt(tab, i, <span class="keyword">null</span>, fwd);</span><br><span class="line">            <span class="comment">// 该位置处是一个 ForwardingNode，代表该位置已经迁移过了</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((fh = f.hash) == MOVED)</span><br><span class="line">                advance = <span class="keyword">true</span>; <span class="comment">// already processed</span></span><br><span class="line">            <span class="comment">// 原数组处有值</span></span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="comment">// 对数组该位置处的结点加锁，开始处理数组该位置处的迁移工作</span></span><br><span class="line">                <span class="comment">// 迁移干的事同样的事 就是把链表拆成两部分，树也分裂成两部分  和 HashMap 真的几乎一样</span></span><br><span class="line">                <span class="keyword">synchronized</span> (f) &#123;</span><br><span class="line">                    <span class="comment">// 重复判断防止多线程修改</span></span><br><span class="line">                    <span class="keyword">if</span> (tabAt(tab, i) == f) &#123;</span><br><span class="line">                        Node&lt;K, V&gt; ln, hn;</span><br><span class="line">                        <span class="comment">// 头结点的 hash 大于 0，说明是链表的 Node 节点</span></span><br><span class="line">                        <span class="keyword">if</span> (fh &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                            <span class="keyword">int</span> runBit = fh &amp; n;</span><br><span class="line">                            Node&lt;K, V&gt; lastRun = f;</span><br><span class="line">                            <span class="comment">// p.hash &amp; n 其实是取某一特定的位，只能是 0/1 所以这个做法是把原来一个链表分成两个</span></span><br><span class="line">                            <span class="comment">// lastRun 指向最后一段 runBit 相同的连续的节点的开始</span></span><br><span class="line">                            <span class="comment">// 感觉这一段代码写得有点蠢，就是为了找到最后一段完整的同类型的节点遍历了整个链表 ？ 还是我理解错了？</span></span><br><span class="line">                            <span class="keyword">for</span> (Node&lt;K, V&gt; p = f.next; p != <span class="keyword">null</span>; p = p.next) &#123;</span><br><span class="line">                                <span class="keyword">int</span> b = p.hash &amp; n;</span><br><span class="line">                                <span class="keyword">if</span> (b != runBit) &#123;</span><br><span class="line">                                    runBit = b;</span><br><span class="line">                                    lastRun = p;</span><br><span class="line">                                &#125;</span><br><span class="line">                            &#125;</span><br><span class="line"></span><br><span class="line">                            <span class="comment">// 分链表的准备工作</span></span><br><span class="line">                            <span class="keyword">if</span> (runBit == <span class="number">0</span>) &#123;</span><br><span class="line">                                ln = lastRun;</span><br><span class="line">                                hn = <span class="keyword">null</span>;</span><br><span class="line">                            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                                hn = lastRun;</span><br><span class="line">                                ln = <span class="keyword">null</span>;</span><br><span class="line">                            &#125;</span><br><span class="line">                            <span class="comment">// 最后还是按照那个特定的位是 0/1 分成两个链表</span></span><br><span class="line">                            <span class="comment">// 链表的顺序翻转了，采用的头插</span></span><br><span class="line">                            <span class="keyword">for</span> (Node&lt;K, V&gt; p = f; p != lastRun; p = p.next) &#123;</span><br><span class="line">                                <span class="keyword">int</span> ph = p.hash;</span><br><span class="line">                                K pk = p.key;</span><br><span class="line">                                V pv = p.val;</span><br><span class="line">                                <span class="keyword">if</span> ((ph &amp; n) == <span class="number">0</span>)</span><br><span class="line">                                    ln = <span class="keyword">new</span> Node&lt;K, V&gt;(ph, pk, pv, ln);</span><br><span class="line">                                <span class="keyword">else</span></span><br><span class="line">                                    hn = <span class="keyword">new</span> Node&lt;K, V&gt;(ph, pk, pv, hn);</span><br><span class="line">                            &#125;</span><br><span class="line">                            <span class="comment">// 链表放在新数组的位置 i 和 i+n</span></span><br><span class="line">                            setTabAt(nextTab, i, ln);</span><br><span class="line">                            setTabAt(nextTab, i + n, hn);</span><br><span class="line">                            <span class="comment">// 将原数组该位置处设置为 fwd，代表该位置已经处理完毕，</span></span><br><span class="line">                            <span class="comment">// 其他线程一旦看到该位置的 hash 值为 MOVED，就不会进行迁移了</span></span><br><span class="line">                            setTabAt(tab, i, fwd);</span><br><span class="line">                            <span class="comment">// advance 设置为 true，代表该位置已经迁移完毕</span></span><br><span class="line">                            advance = <span class="keyword">true</span>;</span><br><span class="line">                        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (f <span class="keyword">instanceof</span> TreeBin) &#123;</span><br><span class="line">                            <span class="comment">// 红黑树的迁移</span></span><br><span class="line">                            TreeBin&lt;K, V&gt; t = (TreeBin&lt;K, V&gt;) f;</span><br><span class="line">                            TreeNode&lt;K, V&gt; lo = <span class="keyword">null</span>, loTail = <span class="keyword">null</span>;</span><br><span class="line">                            TreeNode&lt;K, V&gt; hi = <span class="keyword">null</span>, hiTail = <span class="keyword">null</span>;</span><br><span class="line">                            <span class="keyword">int</span> lc = <span class="number">0</span>, hc = <span class="number">0</span>;</span><br><span class="line">                            <span class="keyword">for</span> (Node&lt;K, V&gt; e = t.first; e != <span class="keyword">null</span>; e = e.next) &#123;</span><br><span class="line">                                <span class="keyword">int</span> h = e.hash;</span><br><span class="line">                                TreeNode&lt;K, V&gt; p = <span class="keyword">new</span> TreeNode&lt;K, V&gt;</span><br><span class="line">                                        (h, e.key, e.val, <span class="keyword">null</span>, <span class="keyword">null</span>);</span><br><span class="line">                                <span class="keyword">if</span> ((h &amp; n) == <span class="number">0</span>) &#123;</span><br><span class="line">                                    <span class="keyword">if</span> ((p.prev = loTail) == <span class="keyword">null</span>)</span><br><span class="line">                                        lo = p;</span><br><span class="line">                                    <span class="keyword">else</span></span><br><span class="line">                                        loTail.next = p;</span><br><span class="line">                                    loTail = p;</span><br><span class="line">                                    ++lc;</span><br><span class="line">                                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                                    <span class="keyword">if</span> ((p.prev = hiTail) == <span class="keyword">null</span>)</span><br><span class="line">                                        hi = p;</span><br><span class="line">                                    <span class="keyword">else</span></span><br><span class="line">                                        hiTail.next = p;</span><br><span class="line">                                    hiTail = p;</span><br><span class="line">                                    ++hc;</span><br><span class="line">                                &#125;</span><br><span class="line">                            &#125;</span><br><span class="line"></span><br><span class="line">                            <span class="comment">// 如果一分为二后，节点数少于 8，那么将红黑树转换回链表</span></span><br><span class="line">                            ln = (lc &lt;= UNTREEIFY_THRESHOLD) ? untreeify(lo) :</span><br><span class="line">                                    (hc != <span class="number">0</span>) ? <span class="keyword">new</span> TreeBin&lt;K, V&gt;(lo) : t;</span><br><span class="line">                            hn = (hc &lt;= UNTREEIFY_THRESHOLD) ? untreeify(hi) :</span><br><span class="line">                                    (lc != <span class="number">0</span>) ? <span class="keyword">new</span> TreeBin&lt;K, V&gt;(hi) : t;</span><br><span class="line">                            <span class="comment">// 处理同链表</span></span><br><span class="line">                            setTabAt(nextTab, i, ln);</span><br><span class="line">                            setTabAt(nextTab, i + n, hn);</span><br><span class="line">                            setTabAt(tab, i, fwd);</span><br><span class="line">                            advance = <span class="keyword">true</span>;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="10-helpTransfer"><a href="#10-helpTransfer" class="headerlink" title="10. helpTransfer"></a>10. helpTransfer</h4><p>&emsp;&emsp;这个方法主要是让其他线程在对表做操作的时候刚好遇到了，表在扩容，数据迁移。就让这些线程帮助完成这个数据迁移，也就是去领取 <code>transfer</code> 中的数据迁移的任务包。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//1. putVal 当有 MOVED 节点的时候  2.replaceNode 也就是在 remove 中 当有 MOVED 节点的时候</span></span><br><span class="line">    <span class="comment">//3. clear 4.conpute/IfPresent 干嘛的？ 5.merge 干嘛的？</span></span><br><span class="line">    <span class="comment">// 好了发现规律了就是当我们在有遍历节点的操作的时候遇到了 MOVED 就去 helpTransfer 也就是 transfer</span></span><br><span class="line">    <span class="keyword">final</span> Node&lt;K, V&gt;[] helpTransfer(Node&lt;K, V&gt;[] tab, Node&lt;K, V&gt; f) &#123;</span><br><span class="line">        Node&lt;K, V&gt;[] nextTab;</span><br><span class="line">        <span class="keyword">int</span> sc;</span><br><span class="line">        <span class="comment">// 条件判断 + 把正在 transfer 的 nextTab 赋值给 nextTab</span></span><br><span class="line">        <span class="keyword">if</span> (tab != <span class="keyword">null</span> &amp;&amp; (f <span class="keyword">instanceof</span> ForwardingNode) &amp;&amp; (nextTab = ((ForwardingNode&lt;K, V&gt;) f).nextTable) != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">int</span> rs = resizeStamp(tab.length);</span><br><span class="line">            <span class="comment">// 当 transfer 还没有结束  sizeCtl &lt; 0 表示在迁移</span></span><br><span class="line">            <span class="keyword">while</span> (nextTab == nextTable &amp;&amp; table == tab &amp;&amp; (sc = sizeCtl) &lt; <span class="number">0</span>) &#123;</span><br><span class="line">                <span class="keyword">if</span> ((sc &gt;&gt;&gt; RESIZE_STAMP_SHIFT) != rs || sc == rs + <span class="number">1</span> ||</span><br><span class="line">                        sc == rs + MAX_RESIZERS || transferIndex &lt;= <span class="number">0</span>)</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                <span class="comment">// 把 SIZECTL 加一 然后 transfer</span></span><br><span class="line">                <span class="keyword">if</span> (U.compareAndSwapInt(<span class="keyword">this</span>, SIZECTL, sc, sc + <span class="number">1</span>)) &#123;</span><br><span class="line">                    transfer(tab, nextTab);</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> nextTab;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> table;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="11-tableAt-casTableAt-setTableAt"><a href="#11-tableAt-casTableAt-setTableAt" class="headerlink" title="11. tableAt/casTableAt/setTableAt"></a>11. tableAt/casTableAt/setTableAt</h4><p>&emsp;&emsp;  这些方法,不细看，里面主要就是封装了一些对表的 CAS 操作。只是用的比较多。</p>
<h4 id="11-remove-replaceNode、"><a href="#11-remove-replaceNode、" class="headerlink" title="11. remove/replaceNode、"></a>11. remove/replaceNode、</h4><p>&emsp;&emsp;  <code>remove</code> 方法也是调用了 <code>replaceNode</code> 方法，这个方法里面就是常规思路。如果看到有节点在 <code>transfer</code> 就调用 <code>helpTransfer</code>， 由于需要操作节点需要进行同步。对树和链表进行删除操作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">final</span> V <span class="title">replaceNode</span><span class="params">(Object key, V value, Object cv)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> hash = spread(key.hashCode());</span><br><span class="line">        <span class="keyword">for</span> (Node&lt;K, V&gt;[] tab = table; ; ) &#123;</span><br><span class="line">            Node&lt;K, V&gt; f;</span><br><span class="line">            <span class="keyword">int</span> n, i, fh;</span><br><span class="line">            <span class="keyword">if</span> (tab == <span class="keyword">null</span> || (n = tab.length) == <span class="number">0</span> || (f = tabAt(tab, i = (n - <span class="number">1</span>) &amp; hash)) == <span class="keyword">null</span>)</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="comment">// 有正在移动的节点就帮助移动</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((fh = f.hash) == MOVED)</span><br><span class="line">                tab = helpTransfer(tab, f);</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                V oldVal = <span class="keyword">null</span>;</span><br><span class="line">                <span class="keyword">boolean</span> validated = <span class="keyword">false</span>;</span><br><span class="line">                <span class="keyword">synchronized</span> (f) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (tabAt(tab, i) == f) &#123;</span><br><span class="line">                        <span class="comment">// 链表节点</span></span><br><span class="line">                        <span class="keyword">if</span> (fh &gt;= <span class="number">0</span>) &#123;</span><br><span class="line">                            validated = <span class="keyword">true</span>;</span><br><span class="line">                            <span class="keyword">for</span> (Node&lt;K, V&gt; e = f, pred = <span class="keyword">null</span>; ; ) &#123;</span><br><span class="line">                                K ek;</span><br><span class="line">                                <span class="keyword">if</span> (e.hash == hash &amp;&amp; ((ek = e.key) == key || (ek != <span class="keyword">null</span> &amp;&amp; key.equals(ek)))) &#123;</span><br><span class="line">                                    V ev = e.val;</span><br><span class="line">                                    <span class="keyword">if</span> (cv == <span class="keyword">null</span> || cv == ev || (ev != <span class="keyword">null</span> &amp;&amp; cv.equals(ev))) &#123;</span><br><span class="line">                                        oldVal = ev;</span><br><span class="line">                                        <span class="comment">//value不为空，则更新值</span></span><br><span class="line">                                        <span class="keyword">if</span> (value != <span class="keyword">null</span>)</span><br><span class="line">                                            e.val = value;</span><br><span class="line">                                        <span class="comment">//value为空，则删除此节点</span></span><br><span class="line">                                        <span class="keyword">else</span> <span class="keyword">if</span> (pred != <span class="keyword">null</span>)</span><br><span class="line">                                            pred.next = e.next;</span><br><span class="line">                                        <span class="comment">//符合条件的节点e为头结点的情况</span></span><br><span class="line">                                        <span class="keyword">else</span></span><br><span class="line">                                            setTabAt(tab, i, e.next);</span><br><span class="line">                                    &#125;</span><br><span class="line">                                    <span class="keyword">break</span>;</span><br><span class="line">                                &#125;</span><br><span class="line">                                pred = e;</span><br><span class="line">                                <span class="keyword">if</span> ((e = e.next) == <span class="keyword">null</span>)</span><br><span class="line">                                    <span class="keyword">break</span>;</span><br><span class="line">                            &#125;</span><br><span class="line">                        <span class="comment">//  红黑树</span></span><br><span class="line">                        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (f <span class="keyword">instanceof</span> TreeBin) &#123;</span><br><span class="line">                            validated = <span class="keyword">true</span>;</span><br><span class="line">                            TreeBin&lt;K, V&gt; t = (TreeBin&lt;K, V&gt;) f;</span><br><span class="line">                            TreeNode&lt;K, V&gt; r, p;</span><br><span class="line">                            <span class="keyword">if</span> ((r = t.root) != <span class="keyword">null</span> &amp;&amp;</span><br><span class="line">                                    (p = r.findTreeNode(hash, key, <span class="keyword">null</span>)) != <span class="keyword">null</span>) &#123;</span><br><span class="line">                                V pv = p.val;</span><br><span class="line">                                <span class="keyword">if</span> (cv == <span class="keyword">null</span> || cv == pv ||</span><br><span class="line">                                        (pv != <span class="keyword">null</span> &amp;&amp; cv.equals(pv))) &#123;</span><br><span class="line">                                    oldVal = pv;</span><br><span class="line">                                    <span class="keyword">if</span> (value != <span class="keyword">null</span>)</span><br><span class="line">                                        p.val = value;</span><br><span class="line">                                    <span class="keyword">else</span> <span class="keyword">if</span> (t.removeTreeNode(p))</span><br><span class="line">                                        setTabAt(tab, i, untreeify(t.first));</span><br><span class="line">                                &#125;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">                <span class="comment">// 进行了节点的删除  cas 更新 Count</span></span><br><span class="line">                <span class="keyword">if</span> (validated) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (oldVal != <span class="keyword">null</span>) &#123;</span><br><span class="line">                        <span class="keyword">if</span> (value == <span class="keyword">null</span>)</span><br><span class="line">                            addCount(-<span class="number">1L</span>, -<span class="number">1</span>);</span><br><span class="line">                        <span class="keyword">return</span> oldVal;</span><br><span class="line">                    &#125;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="12-clear"><a href="#12-clear" class="headerlink" title="12. clear"></a>12. clear</h4><p>清空操作也要说一下，因为不再和以前一样就直接把所有元素置为 null 等待垃圾回收。因为这里涉及到表的大小统计，以及其他的线程同时在操作表可能会导致混乱。他是一个元素下的所有节点统计一遍然后修改容器的大小，也就是他是对每一个数组下的每一个节点进行回收的。显然回收节点需要同步。当然由于涉及到了修改表如果遇到了在扩容的情况也是需要帮助数据迁移。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 这个变量记录了要删除的节点数的负数</span></span><br><span class="line">        <span class="keyword">long</span> delta = <span class="number">0L</span>;</span><br><span class="line">        <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">        Node&lt;K, V&gt;[] tab = table;</span><br><span class="line">        <span class="keyword">while</span> (tab != <span class="keyword">null</span> &amp;&amp; i &lt; tab.length) &#123;</span><br><span class="line">            <span class="keyword">int</span> fh;</span><br><span class="line">            Node&lt;K, V&gt; f = tabAt(tab, i);</span><br><span class="line">            <span class="comment">// 空节点 进行下一趟</span></span><br><span class="line">            <span class="keyword">if</span> (f == <span class="keyword">null</span>)</span><br><span class="line">                ++i;</span><br><span class="line">            <span class="comment">// 帮助移动</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((fh = f.hash) == MOVED) &#123;</span><br><span class="line">                tab = helpTransfer(tab, f);</span><br><span class="line">                i = <span class="number">0</span>; <span class="comment">// restart</span></span><br><span class="line">            <span class="comment">//  删除节点</span></span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">synchronized</span> (f) &#123;</span><br><span class="line">                    <span class="keyword">if</span> (tabAt(tab, i) == f) &#123;</span><br><span class="line">                        <span class="comment">// 如果是链表节点就返回链表头</span></span><br><span class="line">                        <span class="comment">// 如果是树节点就返回他的下一个 否则返回 null</span></span><br><span class="line">                        Node&lt;K, V&gt; p = (fh &gt;= <span class="number">0</span> ? f : (f <span class="keyword">instanceof</span> TreeBin) ? ((TreeBin&lt;K, V&gt;) f).first : <span class="keyword">null</span>);</span><br><span class="line">                        <span class="comment">// 遍历链表或者树</span></span><br><span class="line">                        <span class="keyword">while</span> (p != <span class="keyword">null</span>) &#123;</span><br><span class="line">                            --delta;</span><br><span class="line">                            p = p.next;</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="comment">// 直接把数组对应的位置设置为 null</span></span><br><span class="line">                        setTabAt(tab, i++, <span class="keyword">null</span>);</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (delta != <span class="number">0L</span>)</span><br><span class="line">            addCount(delta, -<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="13-read-writeObject"><a href="#13-read-writeObject" class="headerlink" title="13. read/writeObject"></a>13. read/writeObject</h4><p>&emsp;&emsp;他的序列化也是自己实现的，只存放 key 和 value，在反序列化的时候进行整个表的重建。</p>
<h2 id="3-总结"><a href="#3-总结" class="headerlink" title="3.总结"></a>3.总结</h2><p>&emsp;&emsp;  这个类确实比较麻烦，但是整体思路确实和 HashMap 差不多，但是由于需要做到高并发，访问安全做了很多的更细节的操作，把原来 HashMap 中一步能做完的拆成好几步，并且并发度感觉比 1.7 版本的更高，因为在加锁的时候只加了一个元素，而 1.7 是加一个 Segment 的锁，锁粒度更小，并发度更高。以及在 <code>transfer</code> 中的任务分包真的很巧妙，加速扩容。其实写文字说明只是一个大概的流程，我对源码写了大量的注释，看完说明在看源码就会轻松很多，并且很多源码里面的细节在说明里面没有提到，所以了解大致思路后一定要仔细阅读源码。</p>
]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>HashSet 源码分析</title>
    <url>/2018/03/27/Java%20SourceCode/HashSet%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="HashSet-源码分析"><a href="#HashSet-源码分析" class="headerlink" title="HashSet 源码分析"></a>HashSet 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-基本结构"><a href="#1-基本结构" class="headerlink" title="1. 基本结构"></a>1. 基本结构</h2><h3 id="1-继承"><a href="#1-继承" class="headerlink" title="1. 继承"></a>1. 继承</h3><p>这个类简直适合 <code>HashMap</code> 如出一辙，他们继承的类都极其相似。继承的是 <code>AbstractSet</code> 。</p>
<h3 id="2-实现"><a href="#2-实现" class="headerlink" title="2. 实现"></a>2. 实现</h3><p>实现了 <code>Set</code> 接口，之后还有两个普遍的接口 Cloneable, java.io.Serializable 。</p>
<h3 id="3-主要字段"><a href="#3-主要字段" class="headerlink" title="3. 主要字段"></a>3. 主要字段</h3><p>&emsp;&emsp;  这个字段非常的少，只有一个 <code>HashMap</code> 和一个常量。估计你已经猜到这个容器的底层是采用<code>HashMap</code> 实现的了，但是具体又是怎么实现的呢？</p>
<a id="more"></a>
<p>&emsp;&emsp;  我们都知道，<code>set 集合</code> 是不允许有重复元素的，那么他怎么让他没有重复元素呢，答案就是把元素存放在 <code>HashMap</code> 的键中，因为 <code>HashMap</code> 的键是用来唯一区别两个 <code>Node</code> 节点是否相同的，而 <code>value</code> 就存放那个常量。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">transient</span> HashMap&lt;E,Object&gt; map;</span><br><span class="line"><span class="comment">// Dummy value to associate with an Object in the backing Map</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Object PRESENT = <span class="keyword">new</span> Object();</span><br></pre></td></tr></table></figure>

<h3 id="4-主要方法"><a href="#4-主要方法" class="headerlink" title="4. 主要方法"></a>4. 主要方法</h3><ol>
<li>ctor-5</li>
<li>add</li>
<li>remove</li>
<li>read/writeObject</li>
</ol>
<h2 id="2-主要方法分析"><a href="#2-主要方法分析" class="headerlink" title="2.主要方法分析"></a>2.主要方法分析</h2><h3 id="1-构造方法"><a href="#1-构造方法" class="headerlink" title="1. 构造方法"></a>1. 构造方法</h3><p>&emsp;&emsp;  这里提供了五个构造方法，其实有一个构造方法是比较特殊的，他在方法里面 new 了一个 <code>LinkedHashMap</code> ，也就是底层的数据结构是双链表。你可能会注意到上面我们声明的是一个 <code>HashMap</code> 的引用，new 的 <code>LinkedHashMap</code> 不会有问题吗？别忘了 后者继承了前者，并在他的基础上添加了一个双向指针。其他的四个方法其实都没什么好说的，全是调用了 <code>HashMap</code> 的构造，然后默认大小还是 16.</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HashSet</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HashSet</span><span class="params">(Collection&lt;? extends E&gt; c)</span> </span>&#123;</span><br><span class="line">    map = <span class="keyword">new</span> HashMap&lt;&gt;(Math.max((<span class="keyword">int</span>) (c.size()/.<span class="number">75f</span>) + <span class="number">1</span>, <span class="number">16</span>));</span><br><span class="line">    addAll(c);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HashSet</span><span class="params">(<span class="keyword">int</span> initialCapacity, <span class="keyword">float</span> loadFactor)</span> </span>&#123;</span><br><span class="line">    map = <span class="keyword">new</span> HashMap&lt;&gt;(initialCapacity, loadFactor);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">HashSet</span><span class="params">(<span class="keyword">int</span> initialCapacity)</span> </span>&#123;</span><br><span class="line">    map = <span class="keyword">new</span> HashMap&lt;&gt;(initialCapacity);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 包内可用！ 底层采用 LinkedHashMap</span></span><br><span class="line">HashSet(<span class="keyword">int</span> initialCapacity, <span class="keyword">float</span> loadFactor, <span class="keyword">boolean</span> dummy) &#123;</span><br><span class="line">    map = <span class="keyword">new</span> LinkedHashMap&lt;&gt;(initialCapacity, loadFactor);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-add"><a href="#2-add" class="headerlink" title="2. add"></a>2. add</h3><p>&emsp;&emsp;  就是由于上面也提到了底层采用的 <code>HashMap</code> 结构，所以 add 就调用了 put 方法，然后看返回的值是不是 <code>null</code> 从而判断是否是重复元素。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">add</span><span class="params">(E e)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> map.put(e, PRESENT)==<span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-remove"><a href="#3-remove" class="headerlink" title="3. remove"></a>3. remove</h3><p>&emsp;&emsp;  remove 和上面的add 一样的操作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">remove</span><span class="params">(Object o)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> map.remove(o)==PRESENT;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-read-writeObject"><a href="#4-read-writeObject" class="headerlink" title="4. read/writeObject"></a>4. read/writeObject</h3><p>&emsp;&emsp;  和其他的容器一样重写了序列化反序列化的操作。</p>
<h3 id="5-其他方法"><a href="#5-其他方法" class="headerlink" title="5.其他方法"></a>5.其他方法</h3><p>&emsp;&emsp;  其他方法都是直接调用了 <code>HashMap</code> 的方法，所以比较简单。总共代码才三百多行，只要我们注意他的底层实现是 <code>HashMap</code> 就够了。</p>
]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>LinkedHashSet 源码分析</title>
    <url>/2018/03/27/Java%20SourceCode/LinkedHashSet%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="LinkedHashSet-源码分析"><a href="#LinkedHashSet-源码分析" class="headerlink" title="LinkedHashSet 源码分析"></a>LinkedHashSet 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-基本结构"><a href="#1-基本结构" class="headerlink" title="1. 基本结构"></a>1. 基本结构</h2><p>&emsp;&emsp;  如果你看了 <code>HashSet</code> 现在基本一分钟就能弄明白 <code>LinkedHashSet</code> 的底层原理。里面没有任何字段，只有一个序列化 id，这个我们不说。然后他继承的是 <code>HashSet</code> ，有没有注意到简直就和 <code>LinkedHashMap</code> 一个套路，然后就调用一下父类的构造方法，但是调用的父类的构造方法都是同一个，很明显肯定是那个比较特殊的构造，也就是只能在包内访问，并且比其他的方法多一个 bool 参数的那个，因为它能指定底层的数据结构是 <code>LinkedHashMap</code> ，这也就解释了为什么 <code>LinkedHashSet</code> 中的四个方法都是 <code>public</code> 的，而最后一个是 <code>default</code>。</p>
<a id="more"></a>
<h2 id="好的介绍完了！！！-：）"><a href="#好的介绍完了！！！-：）" class="headerlink" title="好的介绍完了！！！  ：）"></a>好的介绍完了！！！  ：）</h2>]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>LinkedHashMap 源码分析</title>
    <url>/2018/03/26/Java%20SourceCode/LinkedHashMap%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="LinkedHashMap-源码分析"><a href="#LinkedHashMap-源码分析" class="headerlink" title="LinkedHashMap 源码分析"></a>LinkedHashMap 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-基本结构"><a href="#1-基本结构" class="headerlink" title="1. 基本结构"></a>1. 基本结构</h2><h3 id="1-实现"><a href="#1-实现" class="headerlink" title="1. 实现"></a>1. 实现</h3><p>实现的接口是 <code>Map</code> </p>
<h3 id="2-继承"><a href="#2-继承" class="headerlink" title="2. 继承"></a>2. 继承</h3><p>&emsp;&emsp;  继承的是 <code>HashMap</code> 这个就比较熟悉了，事实上我们会看到 <code>LinkedHashMap</code> 代码量非常的少，主要就是因为他继承的 <code>HashMap</code> ，继承了大多数的操作。 仔细一点的都会发现 <code>HashMap</code> 里面有非常多的空白方法，这些方法其实是模板方法，为了让继承 <code>HashMap</code> 的类重写一些自己的特性。而不破坏代码结构。</p>
<a id="more"></a>
<h3 id="3-数据域"><a href="#3-数据域" class="headerlink" title="3. 数据域"></a>3. 数据域</h3><h4 id="1-基本字段"><a href="#1-基本字段" class="headerlink" title="1. 基本字段"></a>1. 基本字段</h4><p>&emsp;&emsp;  在 <code>HashMap</code> 的基础上他添加了三个字段，这三个字段都非常重要，首先就是关于双向链表的两个字段 以及决定是否进行 LRU 的标志位。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 双向链表的头结点</span></span><br><span class="line"><span class="keyword">transient</span> LinkedHashMap.Entry&lt;K,V&gt; head;</span><br><span class="line"><span class="comment">// 双向链表的尾节点</span></span><br><span class="line"><span class="keyword">transient</span> LinkedHashMap.Entry&lt;K,V&gt; tail;</span><br><span class="line"><span class="comment">//  决定是否进行 LRU 算法</span></span><br><span class="line"><span class="keyword">final</span> <span class="keyword">boolean</span> accessOrder;</span><br></pre></td></tr></table></figure>

<h4 id="2-Entry-节点"><a href="#2-Entry-节点" class="headerlink" title="2. Entry 节点"></a>2. Entry 节点</h4><p>&emsp;&emsp;  可能看过前面关于 <code>HashMap</code> 源码分析的都清楚，里面有一个 <code>TreeNode</code> 节点，他继承的就是 <code>LinkedHashMap</code> 中的 <code>Entry</code> 节点。这个节点里是在 <code>HashMap</code> 的  <code>Node</code> 节点上添加了 <code>before 和 after</code> ,也就是用来构造双向链表的指针。</p>
<h3 id="4-重点方法概览"><a href="#4-重点方法概览" class="headerlink" title="4. 重点方法概览"></a>4. 重点方法概览</h3><ol>
<li>ctor-5  最重要的能实现 LRU 的是设置 accessOrder 的那个</li>
<li>afterNodeRemoval</li>
<li>afterNodeInsertion</li>
<li>afterNodeAccess</li>
<li>containsValue</li>
<li>get</li>
<li>removeEldestEntry</li>
</ol>
<h2 id="2-重要方法分析"><a href="#2-重要方法分析" class="headerlink" title="2. 重要方法分析"></a>2. 重要方法分析</h2><h3 id="1-构造方法"><a href="#1-构造方法" class="headerlink" title="1. 构造方法"></a>1. 构造方法</h3><p>&emsp;&emsp;  这个类有 5 个构造方法，一开始我以为和 <code>HashMap</code> 一样只有四个，后来又找到一个隐藏的比较深的方法，也是实现 LRU 最重要的一个方法。</p>
<p>&emsp;&emsp;  那么我们重点分析最后一个特殊的方法，前面几个方法我们都见过和 <code>HashMap</code> 中的差不多，就是多了一个设置 <code>accessOrder=false</code> 的操作。最后那个方法如果我们设置了 <code>accessOrder=true</code> 那么我们在访问一个元素的时候，这个元素会自动的排到双向链表的结尾。也就是所谓的 LRU。</p>
<p>&emsp;&emsp;  既然提到构造方法我们顺带说一下 <code>reinitialize</code> 方法这个方法是设置了头结点和尾节点。这些方法是在 <code>clone</code> 和 <code>反序列化</code> 的时候使用的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="function"><span class="keyword">public</span> <span class="title">LinkedHashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity,</span></span></span><br><span class="line"><span class="function"><span class="params">                         <span class="keyword">float</span> loadFactor,</span></span></span><br><span class="line"><span class="function"><span class="params">                         <span class="keyword">boolean</span> accessOrder)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>(initialCapacity, loadFactor);</span><br><span class="line">        <span class="keyword">this</span>.accessOrder = accessOrder;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line"><span class="comment">// 初始化双链表</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">reinitialize</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">super</span>.reinitialize();</span><br><span class="line">        head = tail = <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-afterNodeRemoval"><a href="#2-afterNodeRemoval" class="headerlink" title="2. afterNodeRemoval"></a>2. afterNodeRemoval</h3><p>这个方法比较简单，也是模板方法，里面的主要作用就是修改一下双向链表，从而达到删除一个节点的作用。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这个方法还 ok 就是修改一下双向指针</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">afterNodeRemoval</span><span class="params">(Node&lt;K,V&gt; e)</span> </span>&#123; <span class="comment">// unlink</span></span><br><span class="line">    LinkedHashMap.Entry&lt;K,V&gt; p =</span><br><span class="line">        (LinkedHashMap.Entry&lt;K,V&gt;)e, b = p.before, a = p.after;</span><br><span class="line">    p.before = p.after = <span class="keyword">null</span>;</span><br><span class="line">    <span class="keyword">if</span> (b == <span class="keyword">null</span>)</span><br><span class="line">        head = a;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        b.after = a;</span><br><span class="line">    <span class="keyword">if</span> (a == <span class="keyword">null</span>)</span><br><span class="line">        tail = b;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        a.before = b;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-afterNodeInsertion"><a href="#3-afterNodeInsertion" class="headerlink" title="3. afterNodeInsertion"></a>3. afterNodeInsertion</h3><p>&emsp;&emsp;  在插入节点以后，如果说我们实现的 LRU 算法是需要删除一些旧的节点时候，这个方法就会在插入节点完成之后自动删除旧节点。删除的逻辑是 <code>removeEldestEntry</code> 方法决定的，如果要启用删除这个方法里面做删除逻辑，并且返回 <code>true</code> 。这里没有任何实现！</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 插入新的节点以后，如果说定义了删除老元素的方法，这个方法返回 true 的话就直接删除原来的旧元素 注意老元素是在头部  所以删除头部的元素即可</span></span><br><span class="line"><span class="comment">// 在这个地方是不做人事事情的 removeEldestEntry  返回了 false</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">afterNodeInsertion</span><span class="params">(<span class="keyword">boolean</span> evict)</span> </span>&#123; <span class="comment">// possibly remove eldest</span></span><br><span class="line">    LinkedHashMap.Entry&lt;K,V&gt; first;</span><br><span class="line">    <span class="keyword">if</span> (evict &amp;&amp; (first = head) != <span class="keyword">null</span> &amp;&amp; removeEldestEntry(first)) &#123;</span><br><span class="line">        <span class="comment">// 删除头部元素</span></span><br><span class="line">        K key = first.key;</span><br><span class="line">        removeNode(hash(key), key, <span class="keyword">null</span>, <span class="keyword">false</span>, <span class="keyword">true</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-afterNodeAccess"><a href="#4-afterNodeAccess" class="headerlink" title="4. afterNodeAccess"></a>4. afterNodeAccess</h3><p>&emsp;&emsp;  在节点访问以后，如果说我们没有开启 LRU 算法，那没什么关系，访问了以后顺序不变，而如果<code>accessOrder=true</code> 那么访问的元素必须要放到双向链表的结尾。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 如果accessOrder 为 true，也就是支持 LRU 算法，那么就把这个元素先从双向链表中删除（在数组中的位置不变），然后插到链表的头部作为最新的元素</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">afterNodeAccess</span><span class="params">(Node&lt;K,V&gt; e)</span> </span>&#123; <span class="comment">// move node to last</span></span><br><span class="line">        LinkedHashMap.Entry&lt;K,V&gt; last;</span><br><span class="line">        <span class="keyword">if</span> (accessOrder &amp;&amp; (last = tail) != e) &#123;</span><br><span class="line">            LinkedHashMap.Entry&lt;K,V&gt; p =</span><br><span class="line">                (LinkedHashMap.Entry&lt;K,V&gt;)e, b = p.before, a = p.after;</span><br><span class="line">            p.after = <span class="keyword">null</span>;</span><br><span class="line">            <span class="keyword">if</span> (b == <span class="keyword">null</span>)</span><br><span class="line">                head = a;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                b.after = a;</span><br><span class="line">            <span class="keyword">if</span> (a != <span class="keyword">null</span>)</span><br><span class="line">                a.before = b;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                last = b;</span><br><span class="line">            <span class="keyword">if</span> (last == <span class="keyword">null</span>)</span><br><span class="line">                head = p;</span><br><span class="line">            <span class="keyword">else</span> &#123;</span><br><span class="line">                p.before = last;</span><br><span class="line">                last.after = p;</span><br><span class="line">            &#125;</span><br><span class="line">            tail = p;</span><br><span class="line">            ++modCount;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-containsValue"><a href="#5-containsValue" class="headerlink" title="5. containsValue"></a>5. containsValue</h3><p>重写了父类的方法，主要是因为有了 双向链表的支持，遍历会更快。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 重写了 containsValue 因为有了双链表了遍历起来更方便</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">containsValue</span><span class="params">(Object value)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// after 指针</span></span><br><span class="line">        <span class="keyword">for</span> (LinkedHashMap.Entry&lt;K,V&gt; e = head; e != <span class="keyword">null</span>; e = e.after) &#123;</span><br><span class="line">            V v = e.value;</span><br><span class="line">            <span class="keyword">if</span> (v == value || (value != <span class="keyword">null</span> &amp;&amp; value.equals(v)))</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="6-get"><a href="#6-get" class="headerlink" title="6. get"></a>6. get</h3><p>get 方法属于访问元素，还是 LRU 的问题、</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 重写了，获取元素以后，如果用了 LRU 需要重新排列该元素的位置</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> V <span class="title">get</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">        Node&lt;K,V&gt; e;</span><br><span class="line">        <span class="keyword">if</span> ((e = getNode(hash(key), key)) == <span class="keyword">null</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">        <span class="comment">// 重新排列该元素位置</span></span><br><span class="line">        <span class="keyword">if</span> (accessOrder)</span><br><span class="line">            afterNodeAccess(e);</span><br><span class="line">        <span class="keyword">return</span> e.value;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="7-removeEldestEntry"><a href="#7-removeEldestEntry" class="headerlink" title="7. removeEldestEntry"></a>7. removeEldestEntry</h3><p>空实现。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这个方法是用来被覆盖的，也就是子类来用，本类用不着。</span></span><br><span class="line"><span class="comment">// 如果有必要，则删除掉该近期最少使用的节点，</span></span><br><span class="line"><span class="comment">// 这要看对removeEldestEntry的覆写,由于默认为false，因此默认是不做任何处理的。</span></span><br><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">removeEldestEntry</span><span class="params">(Map.Entry&lt;K,V&gt; eldest)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-总结"><a href="#3-总结" class="headerlink" title="3. 总结"></a>3. 总结</h2><p>其实在理解这个容器的时候我们就把他当做 <code>HashMap</code> 加上一个无关的双向指针即可。 然后注意一下他的 LRU 算法的利用！</p>
]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>HashMap 源码分析</title>
    <url>/2018/03/26/Java%20SourceCode/HashMap%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="HashMap-源码分析"><a href="#HashMap-源码分析" class="headerlink" title="HashMap 源码分析"></a>HashMap 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-结构"><a href="#1-结构" class="headerlink" title="1.结构"></a>1.结构</h2><h3 id="1-继承"><a href="#1-继承" class="headerlink" title="1. 继承"></a>1. 继承</h3><p>&emsp;&emsp;该类继承自 <code>AbstractMap</code> 这个类似于 <code>ArrayList</code></p>
<h3 id="2-实现"><a href="#2-实现" class="headerlink" title="2. 实现"></a>2. 实现</h3><p>具体如下：</p>
<ol>
<li>首先这个类是一个 Map 自然有 Map 接口</li>
<li>然后就是两个集合框架肯定会实现的两个接口 Cloneable, Serializable 。<a id="more"></a>

</li>
</ol>
<h3 id="3-主要字段"><a href="#3-主要字段" class="headerlink" title="3. 主要字段"></a>3. 主要字段</h3><h4 id="1-属性字段"><a href="#1-属性字段" class="headerlink" title="1. 属性字段"></a>1. 属性字段</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 默认大小 16</span></span><br><span class="line"> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> DEFAULT_INITIAL_CAPACITY = <span class="number">1</span> &lt;&lt; <span class="number">4</span>; </span><br><span class="line"> <span class="comment">// 最大容量 2^30</span></span><br><span class="line"> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> MAXIMUM_CAPACITY = <span class="number">1</span> &lt;&lt; <span class="number">30</span>;</span><br><span class="line"> <span class="comment">// 负载因子</span></span><br><span class="line"> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">float</span> DEFAULT_LOAD_FACTOR = <span class="number">0.75f</span>;</span><br><span class="line"> <span class="comment">// 链表转成树，链表的长度 8</span></span><br><span class="line"> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> TREEIFY_THRESHOLD = <span class="number">8</span>;</span><br><span class="line"> <span class="comment">// 树转成链表时树大节点数 6</span></span><br><span class="line"> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> UNTREEIFY_THRESHOLD = <span class="number">6</span>;</span><br><span class="line"> <span class="comment">// Node 数组</span></span><br><span class="line"> <span class="keyword">transient</span> Node&lt;K,V&gt;[] table;</span><br><span class="line"> <span class="comment">// 大小</span></span><br><span class="line"> <span class="keyword">transient</span> <span class="keyword">int</span> size;</span><br><span class="line"> <span class="comment">// 阈值 他等于容量乘以负载因子</span></span><br><span class="line"> <span class="keyword">int</span> threshold;</span><br></pre></td></tr></table></figure>

<h4 id="2-Node"><a href="#2-Node" class="headerlink" title="2. Node"></a>2. Node</h4><p>​&emsp;&emsp; 这个其实就是在 JDK1.7 中我们常说的 Entry ，但是在 Java8 把 Entry 更进一步抽象了，放到了 Map 接口里面，那里面的内部接口。里面并没有定义任何的字段，只有一些公共的方法。</p>
<p>​&emsp;&emsp; 然后这个  <code>Node</code> 是实现了  <code>Entry</code> 接口，里面定义了四个属性，这几个属性也就是 <code>HashMap</code> 的关键了，分别就是 <code>hash</code> 、<code>key</code>、<code>value</code> 、<code>next</code> 。下面具体的看下代码。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">implements</span> <span class="title">Map</span>.<span class="title">Entry</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">int</span> hash;</span><br><span class="line">        <span class="keyword">final</span> K key;</span><br><span class="line">        V value;</span><br><span class="line">        Node&lt;K,V&gt; next;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-TreeNode"><a href="#3-TreeNode" class="headerlink" title="3. TreeNode"></a>3. TreeNode</h4><p>&emsp;&emsp;  <code>TreeNode</code> 着很明显，我们在上面的属性字段提到了关于链表转成树的操作，那么我们就需要把 <code>Node</code> 节点包装成 <code>TreeNode</code> 。这里有一个比较有意思的事情就是这个 <code>TreeNode</code> 是继承自 <code>LinkedHashMap</code> 的 <code>Entry</code> 但是他又继承自 <code>HashMap</code> 的 <code>Node</code> ，而 那个 <code>Entery</code> 在 <code>Node</code> 基础上添加了属性就是 <code>before</code> 和 <code>after</code> 。有点绕，那么简单来说就是 <code>TreeNode</code>  在 <code>Node</code> 里面添加了 <code>before</code> 、<code>after</code>  还有其他的红黑树的信息。来具体看一下结构。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">TreeNode</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; <span class="keyword">extends</span> <span class="title">LinkedHashMap</span>.<span class="title">Entry</span>&lt;<span class="title">K</span>,<span class="title">V</span>&gt; </span>&#123;</span><br><span class="line">        TreeNode&lt;K,V&gt; parent;  <span class="comment">// red-black tree links</span></span><br><span class="line">        TreeNode&lt;K,V&gt; left;</span><br><span class="line">        TreeNode&lt;K,V&gt; right;</span><br><span class="line">        TreeNode&lt;K,V&gt; prev;    <span class="comment">// needed to unlink next upon deletion</span></span><br><span class="line">        <span class="keyword">boolean</span> red;</span><br><span class="line">    	<span class="comment">//before after inhert from Entry</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-主要方法概览"><a href="#4-主要方法概览" class="headerlink" title="4. 主要方法概览"></a>4. 主要方法概览</h3><ol>
<li>cotr-4</li>
<li>put/putVal</li>
<li>resize</li>
<li>putAll/putMapEntries</li>
<li>get/getNode/containsKey </li>
<li>remove/removeNode/clear</li>
<li>containsValue</li>
<li>read/writeObject</li>
</ol>
<h2 id="2-主要方法分析"><a href="#2-主要方法分析" class="headerlink" title="2. 主要方法分析"></a>2. 主要方法分析</h2><p>###1. cotr-4    </p>
<p>&emsp;&emsp;  首先介绍一下构造方法，这里我们会看到四个构造方法，他们在里面做的事情都差不多，主要是设置容器的 <code>容量</code> 和<code>阈值</code> 。其中在上面的字段中我们看到了一些常量，其中就有说明初始大小就是 16 ，然后负载因子是 0.75 ，还有提到最大容量 2^30 。</p>
<p>&emsp;&emsp;  在进行数组大小设置的时候有一个比较有意思的方法，<code>tableSizeFor(int size)</code> 这个方法能够保证最后返回出来的值是一个比 <code>size</code> 大的最小的 2^n 这样一个数。这样说可能有点不好理解，举个例子吧。 假如我们传入一个 18 那么返回的就是 32 ，因为 32 是 <code>2 的 n 次方</code> 这类的数，然后他是最接近 18 的  <code>2 的 n 次方</code>  。</p>
<p>&emsp;&emsp;  然后你可能会发现为什么没有初始化  <code>Node</code>  数组，  这是因为在 <code>jdk1.8</code> 里面 <code>HashMap</code> 的实现他的空间是延时分配的，也就是在我们真正往里面 <code>put</code> 值的时候才会真的创建 <code>Node数组</code> ，这个到我们分析 <code>put方法</code> 的时候我们会看到这一机制。</p>
<p>   ​    </p>
   <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 设置负载因子和初始大小</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">HashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity, <span class="keyword">float</span> loadFactor)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// 参数判断</span></span><br><span class="line">        <span class="keyword">if</span> (initialCapacity &lt; <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">"Illegal initial capacity: "</span> +</span><br><span class="line">                                               initialCapacity);</span><br><span class="line">        <span class="keyword">if</span> (initialCapacity &gt; MAXIMUM_CAPACITY)</span><br><span class="line">            initialCapacity = MAXIMUM_CAPACITY;</span><br><span class="line">        <span class="keyword">if</span> (loadFactor &lt;= <span class="number">0</span> || Float.isNaN(loadFactor))</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">"Illegal load factor: "</span> +</span><br><span class="line">                                               loadFactor);</span><br><span class="line">        <span class="keyword">this</span>.loadFactor = loadFactor;</span><br><span class="line">        <span class="comment">// 这个方法就是把一个不是 2 的次幂的数转换成一个大于当前数的最小的 2 的次幂的数</span></span><br><span class="line">        <span class="keyword">this</span>.threshold = tableSizeFor(initialCapacity);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 大小设置一下，负载因子默认</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">HashMap</span><span class="params">(<span class="keyword">int</span> initialCapacity)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>(initialCapacity, DEFAULT_LOAD_FACTOR);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 设置负载因子为默认的 0.75</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">HashMap</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.loadFactor = DEFAULT_LOAD_FACTOR; <span class="comment">// all other fields defaulted</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// putMapEntries 这个方法就是 new 新数组然后 foreach 拷贝</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">HashMap</span><span class="params">(Map&lt;? extends K, ? extends V&gt; m)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.loadFactor = DEFAULT_LOAD_FACTOR;</span><br><span class="line">        putMapEntries(m, <span class="keyword">false</span>);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>   ​</p>
<h3 id="2-put-putVal"><a href="#2-put-putVal" class="headerlink" title="2. put/putVal"></a>2. put/putVal</h3><p>   ​    <code>put</code> 方法是今天的重头戏，因为大部分的代码其实都集中在这里，put 方法直接调用了 <code>putVal</code> 这个方法里面就进行了真正的存放元素的操作。 </p>
<p>   ​    我先大概说一下 <code>putVal</code> 的逻辑，然后再看代码就不会那么头疼了。</p>
<ol>
<li><p>一开始判断当前的 <code>Node 数组</code> 是否是空，如果是空则进行初始化，也就是分配空间（这里就是啊前面提到的延时分配空间）</p>
</li>
<li><p>接着需要计算这个插入的值在数组中的位置，计算的方法就是  <code>hash % capacity</code> ，但是你可能看到的代码不是这样而是采用的 <code>hash &amp; (capacity-1)</code> ，但是他们是等价的！！！不过这个等价是有条件的，那就是 <code>capacity</code>  的值必须是 <code>2 ^ n</code> 。所以你现在可能理解为什么 <code>HashMap</code> 的大小一直需要为  <code>2 ^ n</code>  以及 <code>tableSizeFor</code> 的作用。这个等价是可以证明的，比较简单不再赘述。</p>
</li>
<li><p>找到需要插入元素的位置以后，如果说这个位置没有元素那好，我们直接把这个元素插入即可。</p>
</li>
<li><p>但是如果这个地方的元素并不是空的，那么我们要么就是插入了完全一样的 <code>key</code> 要么就是 <code>key</code> 不一样但是 <code>hash</code>  函数发生了冲突。</p>
</li>
<li><p>如果是完全一样的 <code>key</code> 那我们就用新的 <code>value</code> 替换掉原来的 <code>value</code> 返回老值即可。</p>
</li>
<li><p>但是如果是发生了 hash 冲突我们就需要解决冲突。在 <code>jdk1.8</code> 里面采用的解决冲突的方法就是在这个节点上生成一个链表或红黑树。至于具体生成哪种据坎节点的数量了，节点数量少链表就很快多了的话我们肯定采用平衡二叉树（红黑树）。这个分水岭的节点数是 8 ，在上面的数据域可以看到他是一个常量。</p>
</li>
<li><p>对红黑树直接调用红黑树的 <code>putTreeVal</code> 方法插入，而链表的话我们直接插入到链表的尾部即可。对链表插入完成以后需要检测一下是不是需要转成红黑树。</p>
</li>
<li><p>最后进行一下扩容判断，毕竟有新的节点加入。</p>
</li>
</ol>
<p>​        以上就是 <code>putVal</code> 的全部过程， 其中有一个扩容操作没有说，一会会单独讲这个方法。下面看看这个方法的源码。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">put</span><span class="params">(K key, V value)</span> </span>&#123;</span><br><span class="line">       <span class="keyword">return</span> putVal(hash(key), key, value, <span class="keyword">false</span>, <span class="keyword">true</span>);</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="comment">// 第三个参数 onlyIfAbsent 如果是 true，那么只有在不存在该 key 时才会进行 put 操作</span></span><br><span class="line"><span class="comment">// 第四个参数 evict 我们这里不关心</span></span><br><span class="line">   <span class="function"><span class="keyword">final</span> V <span class="title">putVal</span><span class="params">(<span class="keyword">int</span> hash, K key, V value, <span class="keyword">boolean</span> onlyIfAbsent,<span class="keyword">boolean</span> evict)</span> </span>&#123;</span><br><span class="line">       Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; <span class="keyword">int</span> n, i;</span><br><span class="line">       <span class="comment">// 第一次 put 值的时候，会触发下面的 resize()。这是因为我们调用了默认的无参的构造方法导致的，无参的里面只设置了负载因子</span></span><br><span class="line">       <span class="comment">// 第一次 resize 和后续的扩容有些不一样，因为这次是数组从 null 初始化到默认的 16 或自定义的初始容量</span></span><br><span class="line">       <span class="keyword">if</span> ((tab = table) == <span class="keyword">null</span> || (n = tab.length) == <span class="number">0</span>)</span><br><span class="line">           n = (tab = resize()).length;</span><br><span class="line">       <span class="comment">// 找到具体的数组下标，如果此位置没有值，那么直接初始化一下 Node 并放置在这个位置就可以了</span></span><br><span class="line">       <span class="comment">// 这个地方采用的 (n - 1) &amp; hash 来寻找数组的下标，他和 hash%n 的效果一样的 但是仅仅限制在 n 是 2 的次幂</span></span><br><span class="line">       <span class="keyword">if</span> ((p = tab[i = (n - <span class="number">1</span>) &amp; hash]) == <span class="keyword">null</span>)</span><br><span class="line">           tab[i] = newNode(hash, key, value, <span class="keyword">null</span>);<span class="comment">//newNode 方法不用看  就直接 new</span></span><br><span class="line"></span><br><span class="line">       <span class="keyword">else</span> &#123;<span class="comment">// 数组该位置有数据</span></span><br><span class="line">           Node&lt;K,V&gt; e; K k;</span><br><span class="line">           <span class="comment">// 首先，判断该位置的第一个数据和我们要插入的数据，key 是不是"相等"，如果是，把这个节点放到 e 里面</span></span><br><span class="line">           <span class="comment">// 最后还有一个判断 e 是不是 null 然后对这个节点的 value 进行替换也就是说，如果 key 一样的话直接替换 vaule key 不一样说明是碰撞</span></span><br><span class="line">           <span class="keyword">if</span> (p.hash == hash &amp;&amp;</span><br><span class="line">                   ((k = p.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">               e = p;</span><br><span class="line">           <span class="comment">// 下面两种情况都是碰撞处理</span></span><br><span class="line">           <span class="comment">// 如果该节点是代表红黑树的节点，调用红黑树的插值方法，本文不展开说红黑树</span></span><br><span class="line">           <span class="keyword">else</span> <span class="keyword">if</span> (p <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">               e = ((TreeNode&lt;K,V&gt;)p).putTreeVal(<span class="keyword">this</span>, tab, hash, key, value);</span><br><span class="line">           <span class="comment">// 数组该位置上是一个链表</span></span><br><span class="line">           <span class="keyword">else</span> &#123;</span><br><span class="line">               <span class="comment">// 循环找链表的最后一个节点</span></span><br><span class="line">               <span class="keyword">for</span> (<span class="keyword">int</span> binCount = <span class="number">0</span>; ; ++binCount) &#123;</span><br><span class="line">                   <span class="comment">// 找到尾部就插入尾部 (Java7 是插入到链表的最前面)</span></span><br><span class="line">                   <span class="keyword">if</span> ((e = p.next) == <span class="keyword">null</span>) &#123;</span><br><span class="line">                       p.next = newNode(hash, key, value, <span class="keyword">null</span>);</span><br><span class="line">                       <span class="comment">// TREEIFY_THRESHOLD 为 8，所以，如果新插入的值是链表中的第 9 个，会触发下面的 treeifyBin，也就是将链表转换为红黑树</span></span><br><span class="line">                       <span class="keyword">if</span> (binCount &gt;= TREEIFY_THRESHOLD - <span class="number">1</span>) <span class="comment">// -1 for 1st</span></span><br><span class="line">                           treeifyBin(tab, hash);</span><br><span class="line">                       <span class="keyword">break</span>;</span><br><span class="line">                   &#125;</span><br><span class="line">                   <span class="comment">// 不可能发生，所以直接 break 了 ，这个条件在前面就过滤掉了，也就是 key 相同的情况应该进行 value 的替换</span></span><br><span class="line">                   <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                           ((k = e.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                       <span class="keyword">break</span>;</span><br><span class="line">                   p = e;</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="comment">// e!=null 说明存在旧值的key与要插入的key"相等"，不是碰撞情况而是一致的 key 需替换返回老值</span></span><br><span class="line">           <span class="keyword">if</span> (e != <span class="keyword">null</span>) &#123;</span><br><span class="line">               V oldValue = e.value;</span><br><span class="line">               <span class="keyword">if</span> (!onlyIfAbsent || oldValue == <span class="keyword">null</span>)</span><br><span class="line">                   e.value = value;</span><br><span class="line">               afterNodeAccess(e);  <span class="comment">//这个操作是空操作  模板设计模式   是给 LinkedHashMap 使用的</span></span><br><span class="line">               <span class="keyword">return</span> oldValue;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">       ++modCount;</span><br><span class="line">       <span class="comment">// 如果 HashMap 由于新插入这个值导致 size 已经超过了阈值，需要进行扩容</span></span><br><span class="line">       <span class="keyword">if</span> (++size &gt; threshold)</span><br><span class="line">           resize();</span><br><span class="line">       afterNodeInsertion(evict); <span class="comment">// 同 afterNodeAccess</span></span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-resize"><a href="#3-resize" class="headerlink" title="3.  resize"></a>3.  resize</h3><p>​    扩容方法也有点复杂，方法体有点长，没关系我们慢慢分析，先了解思路在看源码。</p>
<ol>
<li><p>虽然这个方法是扩容方法，但是他也承担着初始化的任务。前面我们提到在 <code>putVal方法</code> 中有为 <code>Node 数组</code> 分配空间的事情，但是这个分配空间是委托给了 这个方法进行的。</p>
</li>
<li><p>所以开始确认当前是分配空间还是在扩容，如果是扩容我们要判断当前的容量是不是已经到达极限了也就是最大容量 <code>2^3</code> ，如果大于等于这个值我们不进行扩容把阈值设置为最大的整数，防止下次再进行扩容操作。否则的话我们正常扩容把容量调整为原来的二倍，这样做的原因很明显容量要是 <code>2 ^ n</code> 。</p>
</li>
<li><p>接下来我们就可以 new 一个新数组了，当然如果是这个操作是初始化那么我们的工作就完成了，但是如果是扩容操作我们还需要把原来的数组中的元素迁移到新的数组中。</p>
</li>
<li><p>接下来的操作就是数据迁移工作。迁移就是遍历原来的数组，然后如果这个位置只有一个元素那直接迁移，如果不是的话就只能是红黑树或者单链表了。</p>
</li>
<li><p>遇到红黑树我们就调用红黑树的迁移方法，单链表就把原来的链表拆成两部分。挂在新的数组的位置，拆分的方法也很巧妙源码中会看到。</p>
</li>
</ol>
<p>&emsp;&emsp;  所以流程大概清楚了就看源码，源码注释的比较清楚。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">final</span> Node&lt;K,V&gt;[] resize() &#123;</span><br><span class="line">        <span class="comment">//前面这一大堆都是关于计算扩容的操作   不管他</span></span><br><span class="line">        Node&lt;K,V&gt;[] oldTab = table;</span><br><span class="line">        <span class="keyword">int</span> oldCap = (oldTab == <span class="keyword">null</span>) ? <span class="number">0</span> : oldTab.length;</span><br><span class="line">        <span class="keyword">int</span> oldThr = threshold;</span><br><span class="line">        <span class="keyword">int</span> newCap, newThr = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">// 对应数组扩容</span></span><br><span class="line">        <span class="keyword">if</span> (oldCap &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">//到极限了不扩容 修改阈值防止下一次又进入扩容操作</span></span><br><span class="line">            <span class="keyword">if</span> (oldCap &gt;= MAXIMUM_CAPACITY) &#123;</span><br><span class="line">                threshold = Integer.MAX_VALUE;</span><br><span class="line">                <span class="keyword">return</span> oldTab;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 将数组大小扩大一倍 将阈值扩大一倍</span></span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> ((newCap = oldCap &lt;&lt; <span class="number">1</span>) &lt; MAXIMUM_CAPACITY &amp;&amp; oldCap &gt;= DEFAULT_INITIAL_CAPACITY)</span><br><span class="line">                newThr = oldThr &lt;&lt; <span class="number">1</span>; <span class="comment">// double threshold</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// 对应使用两个有参的构造方法初始化后，第一次 put 的时候  也就是说 HashMap 在初始化的时候没有分配数组，空间是延时分配的</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (oldThr &gt; <span class="number">0</span>)</span><br><span class="line">            newCap = oldThr;</span><br><span class="line">        <span class="comment">// 对应使用 new HashMap() 初始化后，第一次 put 的时候</span></span><br><span class="line">        <span class="keyword">else</span> &#123;</span><br><span class="line">            newCap = DEFAULT_INITIAL_CAPACITY;</span><br><span class="line">            newThr = (<span class="keyword">int</span>)(DEFAULT_LOAD_FACTOR * DEFAULT_INITIAL_CAPACITY);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (newThr == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">float</span> ft = (<span class="keyword">float</span>)newCap * loadFactor;</span><br><span class="line">            newThr = (newCap &lt; MAXIMUM_CAPACITY &amp;&amp; ft &lt; (<span class="keyword">float</span>)MAXIMUM_CAPACITY ?</span><br><span class="line">                    (<span class="keyword">int</span>)ft : Integer.MAX_VALUE);</span><br><span class="line">        &#125;</span><br><span class="line">        threshold = newThr;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="comment">// 用新的数组大小初始化新的数组</span></span><br><span class="line">        Node&lt;K,V&gt;[] newTab = (Node&lt;K,V&gt;[])<span class="keyword">new</span> Node[newCap];</span><br><span class="line">        <span class="comment">// 如果是初始化数组，到这里就结束了，返回 newTab 即可  接下来的操作就是数据迁移</span></span><br><span class="line">        table = newTab;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (oldTab != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 开始遍历原数组，进行数据迁移。</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; oldCap; ++j) &#123;</span><br><span class="line">                Node&lt;K,V&gt; e;</span><br><span class="line">                <span class="keyword">if</span> ((e = oldTab[j]) != <span class="keyword">null</span>) &#123;</span><br><span class="line">                    oldTab[j] = <span class="keyword">null</span>;</span><br><span class="line">                    <span class="comment">// 如果该数组位置上只有单个元素，那就简单了，简单迁移这个元素就可以了</span></span><br><span class="line">                    <span class="keyword">if</span> (e.next == <span class="keyword">null</span>)</span><br><span class="line">                        newTab[e.hash &amp; (newCap - <span class="number">1</span>)] = e;</span><br><span class="line">                    <span class="comment">// 如果是红黑树，就进行分裂操作</span></span><br><span class="line">                    <span class="keyword">else</span> <span class="keyword">if</span> (e <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">                        ((TreeNode&lt;K,V&gt;)e).split(<span class="keyword">this</span>, newTab, j, oldCap);</span><br><span class="line">                    <span class="comment">// 链表的话就要把这些数据迁移到对应的位置 ，注意不是直接把整个链表放到数组的新位置  而是拆成两个链表</span></span><br><span class="line">                    <span class="keyword">else</span> &#123;</span><br><span class="line">                        <span class="comment">// 这块是处理链表的情况，需要将此链表拆成两个链表，放到新的数组中，并且保留原来的先后顺序</span></span><br><span class="line">                        <span class="comment">// loHead、loTail 对应一条链表，hiHead、hiTail 对应另一条链表，代码还是比较简单的</span></span><br><span class="line">                        Node&lt;K,V&gt; loHead = <span class="keyword">null</span>, loTail = <span class="keyword">null</span>;</span><br><span class="line">                        Node&lt;K,V&gt; hiHead = <span class="keyword">null</span>, hiTail = <span class="keyword">null</span>;</span><br><span class="line">                        Node&lt;K,V&gt; next;</span><br><span class="line"></span><br><span class="line">                        <span class="comment">//一条链表拆成两条</span></span><br><span class="line">                        <span class="keyword">do</span> &#123;</span><br><span class="line">                            next = e.next;</span><br><span class="line">                            <span class="comment">//这里就是用了一个条件拆分成了两条链表</span></span><br><span class="line">                            <span class="comment">//他代码这样写的原因在于：oldCap 是一个2的次幂，那么也就是说 他是形如 "100000000...000" 这个格式的</span></span><br><span class="line">                            <span class="comment">//那么任何一个数和他相与必然只有两种结果 0 / 非0 就看最高位，其他位出来肯定是0  这样就区分了两个链表  巧妙！</span></span><br><span class="line">                            <span class="keyword">if</span> ((e.hash &amp; oldCap) == <span class="number">0</span>) &#123;</span><br><span class="line">                                <span class="comment">//这里面的逻辑其实就是链表按照原来的顺序连接  也就是说原来 a 在 c 前面只要 ac 在同一条链表上 a 就在 c 前面</span></span><br><span class="line">                                <span class="keyword">if</span> (loTail == <span class="keyword">null</span>)</span><br><span class="line">                                    loHead = e;</span><br><span class="line">                                <span class="keyword">else</span></span><br><span class="line">                                    loTail.next = e;</span><br><span class="line">                                loTail = e;</span><br><span class="line">                            &#125;</span><br><span class="line">                            <span class="keyword">else</span> &#123;</span><br><span class="line">                                <span class="comment">// 同上</span></span><br><span class="line">                                <span class="keyword">if</span> (hiTail == <span class="keyword">null</span>)</span><br><span class="line">                                    hiHead = e;</span><br><span class="line">                                <span class="keyword">else</span></span><br><span class="line">                                    hiTail.next = e;</span><br><span class="line">                                hiTail = e;</span><br><span class="line">                            &#125;</span><br><span class="line">                        &#125; <span class="keyword">while</span> ((e = next) != <span class="keyword">null</span>);</span><br><span class="line"></span><br><span class="line">                        <span class="comment">//用来把两条链表分别挂在正确的数组位置</span></span><br><span class="line">                        <span class="keyword">if</span> (loTail != <span class="keyword">null</span>) &#123;</span><br><span class="line">                            loTail.next = <span class="keyword">null</span>;</span><br><span class="line">                            <span class="comment">// 第一条链表新位置就是原来的位置</span></span><br><span class="line">                            newTab[j] = loHead;</span><br><span class="line">                        &#125;</span><br><span class="line">                        <span class="keyword">if</span> (hiTail != <span class="keyword">null</span>) &#123;</span><br><span class="line">                            hiTail.next = <span class="keyword">null</span>;</span><br><span class="line">                            <span class="comment">// 第二条链表的新的位置是 j + oldCap，这个很好理解</span></span><br><span class="line">                            newTab[j + oldCap] = hiHead;</span><br><span class="line">                        &#125;</span><br><span class="line"></span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> newTab;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-putAll-putMapEntries"><a href="#4-putAll-putMapEntries" class="headerlink" title="4. putAll/putMapEntries"></a>4. putAll/putMapEntries</h3><p>&emsp;&emsp;  前面分析完比较困难的 <code>putVal</code> 和 <code>resize</code> 方法后接下来的方法都很轻松了。</p>
<p>&emsp;&emsp;    这个 <code>putAll</code> 方法调用了 <code>putMapEntries</code> ，在构造函数中也调用了这个方法的。其具体的就是 <code>foreach</code> 拷贝元素。</p>
<h3 id="5-get-getNode-containsKey"><a href="#5-get-getNode-containsKey" class="headerlink" title="5. get/getNode/containsKey"></a>5. get/getNode/containsKey</h3><p>​    这几个方法底层调用的都是 <code>getNode</code> 方法，它的原理就是判断第一个元素是不是，然后看是红黑树还是单链表再遍历得到结果。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">get</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">        Node&lt;K,V&gt; e;</span><br><span class="line">        <span class="keyword">return</span> (e = getNode(hash(key), key)) == <span class="keyword">null</span> ? <span class="keyword">null</span> : e.value;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">final</span> Node&lt;K,V&gt; <span class="title">getNode</span><span class="params">(<span class="keyword">int</span> hash, Object key)</span> </span>&#123;</span><br><span class="line">        Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; first, e; <span class="keyword">int</span> n; K k;</span><br><span class="line">        <span class="keyword">if</span> ((tab = table) != <span class="keyword">null</span> &amp;&amp; (n = tab.length) &gt; <span class="number">0</span> &amp;&amp;</span><br><span class="line">                (first = tab[(n - <span class="number">1</span>) &amp; hash]) != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="comment">// 判断第一个节点是不是就是需要的。</span></span><br><span class="line">            <span class="comment">// 至于为什么要判断第一个元素我认为可能是作者觉得虽然说我们可能会碰到冲突，但是元素不多的情况下真的不会冲突.</span></span><br><span class="line">            <span class="comment">//也就是最理想的情况就是一个元素。</span></span><br><span class="line">            <span class="keyword">if</span> (first.hash == hash &amp;&amp; <span class="comment">// always check first node</span></span><br><span class="line">                    ((k = first.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                <span class="keyword">return</span> first;</span><br><span class="line">            <span class="keyword">if</span> ((e = first.next) != <span class="keyword">null</span>) &#123;</span><br><span class="line">                <span class="comment">// 判断是否是红黑树</span></span><br><span class="line">                <span class="keyword">if</span> (first <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">                    <span class="keyword">return</span> ((TreeNode&lt;K,V&gt;)first).getTreeNode(hash, key);</span><br><span class="line"></span><br><span class="line">                <span class="comment">// 链表遍历</span></span><br><span class="line">                <span class="keyword">do</span> &#123;</span><br><span class="line">                    <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                            ((k = e.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">                        <span class="keyword">return</span> e;</span><br><span class="line">                &#125; <span class="keyword">while</span> ((e = e.next) != <span class="keyword">null</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-remove-removeNode-clear"><a href="#5-remove-removeNode-clear" class="headerlink" title="5. remove/removeNode/clear"></a>5. remove/removeNode/clear</h3><p>这几个方法也很简单，<code>remove</code> 就是底层依赖的 <code>removeNode</code> 就是先遍历找到对应的节点，然后在遍历去删除。</p>
<p>​        <code>clear</code> 方法和前面介绍的 <code>ArrayList</code>  一样就是把数组元素设置为 <code>null</code> 让他去 gc</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> V <span class="title">remove</span><span class="params">(Object key)</span> </span>&#123;</span><br><span class="line">       Node&lt;K,V&gt; e;</span><br><span class="line">       <span class="keyword">return</span> (e = removeNode(hash(key), key, <span class="keyword">null</span>, <span class="keyword">false</span>, <span class="keyword">true</span>)) == <span class="keyword">null</span> ?</span><br><span class="line">           <span class="keyword">null</span> : e.value;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line">   <span class="function"><span class="keyword">final</span> Node&lt;K,V&gt; <span class="title">removeNode</span><span class="params">(<span class="keyword">int</span> hash, Object key, Object value,</span></span></span><br><span class="line"><span class="function"><span class="params">                              <span class="keyword">boolean</span> matchValue, <span class="keyword">boolean</span> movable)</span> </span>&#123;</span><br><span class="line">       Node&lt;K,V&gt;[] tab; Node&lt;K,V&gt; p; <span class="keyword">int</span> n, index;</span><br><span class="line">       <span class="comment">//首先 hash 对应的位置是有东西的 否则直接返回 null</span></span><br><span class="line">       <span class="keyword">if</span> ((tab = table) != <span class="keyword">null</span> &amp;&amp; (n = tab.length) &gt; <span class="number">0</span> &amp;&amp;</span><br><span class="line">           (p = tab[index = (n - <span class="number">1</span>) &amp; hash]) != <span class="keyword">null</span>) &#123;</span><br><span class="line">           Node&lt;K,V&gt; node = <span class="keyword">null</span>, e; K k; V v;</span><br><span class="line">           <span class="comment">//这个 if else 是用来寻找那个要删除的节点的</span></span><br><span class="line">           <span class="keyword">if</span> (p.hash == hash &amp;&amp;</span><br><span class="line">               ((k = p.key) == key || (key != <span class="keyword">null</span> &amp;&amp; key.equals(k))))</span><br><span class="line">               node = p;</span><br><span class="line">           <span class="keyword">else</span> <span class="keyword">if</span> ((e = p.next) != <span class="keyword">null</span>) &#123;</span><br><span class="line">               <span class="keyword">if</span> (p <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">                   node = ((TreeNode&lt;K,V&gt;)p).getTreeNode(hash, key);</span><br><span class="line">               <span class="keyword">else</span> &#123;</span><br><span class="line">                   <span class="keyword">do</span> &#123;</span><br><span class="line">                       <span class="keyword">if</span> (e.hash == hash &amp;&amp;</span><br><span class="line">                           ((k = e.key) == key ||</span><br><span class="line">                            (key != <span class="keyword">null</span> &amp;&amp; key.equals(k)))) &#123;</span><br><span class="line">                           node = e;</span><br><span class="line">                           <span class="keyword">break</span>;</span><br><span class="line">                       &#125;</span><br><span class="line">                       p = e;</span><br><span class="line">                   &#125; <span class="keyword">while</span> ((e = e.next) != <span class="keyword">null</span>);</span><br><span class="line">               &#125;</span><br><span class="line">           &#125;</span><br><span class="line">           <span class="comment">//这个 if 是用来删除上面找到的那个节点</span></span><br><span class="line">           <span class="keyword">if</span> (node != <span class="keyword">null</span> &amp;&amp; (!matchValue || (v = node.value) == value ||</span><br><span class="line">                                (value != <span class="keyword">null</span> &amp;&amp; value.equals(v)))) &#123;</span><br><span class="line">               <span class="keyword">if</span> (node <span class="keyword">instanceof</span> TreeNode)</span><br><span class="line">                   ((TreeNode&lt;K,V&gt;)node).removeTreeNode(<span class="keyword">this</span>, tab, movable);</span><br><span class="line">               <span class="keyword">else</span> <span class="keyword">if</span> (node == p)</span><br><span class="line">                   tab[index] = node.next;</span><br><span class="line">               <span class="keyword">else</span></span><br><span class="line">                   p.next = node.next;</span><br><span class="line">               ++modCount;</span><br><span class="line">               --size;</span><br><span class="line">               afterNodeRemoval(node);</span><br><span class="line">               <span class="keyword">return</span> node;</span><br><span class="line">           &#125;</span><br><span class="line">       &#125;</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">   &#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span> </span>&#123;</span><br><span class="line">       Node&lt;K,V&gt;[] tab;</span><br><span class="line">       modCount++;</span><br><span class="line">       <span class="keyword">if</span> ((tab = table) != <span class="keyword">null</span> &amp;&amp; size &gt; <span class="number">0</span>) &#123;</span><br><span class="line">           size = <span class="number">0</span>;</span><br><span class="line">           <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; tab.length; ++i)</span><br><span class="line">               tab[i] = <span class="keyword">null</span>;</span><br><span class="line">       &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h3 id="6-containsValue"><a href="#6-containsValue" class="headerlink" title="6. containsValue"></a>6. containsValue</h3><p>​    这个方法还是采用遍历的方法，他没有区分是树还是链表统一的采用了 <code>next</code> 指针，这是因为 <code>key</code> 是作为红黑树的索引条件但是 <code>value</code> 并不是，并且在 <code>TreeNode</code> 中是有 <code>next</code> 的因为他间接继承了 <code>Node</code>。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">containsValue</span><span class="params">(Object value)</span> </span>&#123;</span><br><span class="line">        Node&lt;K,V&gt;[] tab; V v;</span><br><span class="line">        <span class="keyword">if</span> ((tab = table) != <span class="keyword">null</span> &amp;&amp; size &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; tab.length; ++i) &#123;</span><br><span class="line">                <span class="keyword">for</span> (Node&lt;K,V&gt; e = tab[i]; e != <span class="keyword">null</span>; e = e.next) &#123;</span><br><span class="line">                    <span class="keyword">if</span> ((v = e.value) == value ||</span><br><span class="line">                        (value != <span class="keyword">null</span> &amp;&amp; value.equals(v)))</span><br><span class="line">                        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>



<h3 id="7-read-writeObject"><a href="#7-read-writeObject" class="headerlink" title="7. read/writeObject"></a>7. read/writeObject</h3><p>​    最后还是序列化的问题，<code>Node数组</code>  并没有采用默认的序列化可以看到他加了 <code>transient</code> 关键字。这里手动序列化只是序列化了 <code>key</code> <code>value</code> 其他的一概不存储。原因还是节省空间。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">writeObject</span><span class="params">(java.io.ObjectOutputStream s)</span></span></span><br><span class="line"><span class="function">        <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> buckets = capacity();</span><br><span class="line">        <span class="comment">// Write out the threshold, loadfactor, and any hidden stuff</span></span><br><span class="line">        s.defaultWriteObject();</span><br><span class="line">        s.writeInt(buckets);</span><br><span class="line">        s.writeInt(size);</span><br><span class="line">        internalWriteEntries(s);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"> <span class="function"><span class="keyword">void</span> <span class="title">internalWriteEntries</span><span class="params">(java.io.ObjectOutputStream s)</span> <span class="keyword">throws</span> IOException </span>&#123;</span><br><span class="line">        Node&lt;K,V&gt;[] tab;</span><br><span class="line">        <span class="keyword">if</span> (size &gt; <span class="number">0</span> &amp;&amp; (tab = table) != <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; tab.length; ++i) &#123;</span><br><span class="line">                <span class="keyword">for</span> (Node&lt;K,V&gt; e = tab[i]; e != <span class="keyword">null</span>; e = e.next) &#123;</span><br><span class="line">                    s.writeObject(e.key);</span><br><span class="line">                    s.writeObject(e.value);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-Hashtable"><a href="#3-Hashtable" class="headerlink" title="3. Hashtable"></a>3. Hashtable</h2><p>​    &emsp;&emsp;    就像是 <code>ArrayList</code> 和 <code>Vector</code> 一样我们需要讨论一下 <code>Hashtable</code> 和 <code>HashMap</code> 之间的异同。</p>
<ol>
<li><p>继承结构他们的实现接口一致，继承的类却不同，<code>Hashtable</code> 继承的是 <code>Dictionary</code> </p>
</li>
<li><p>里面采用的结构是 <code>Entry 数组</code> ,没有采用延时空间分配，默认大小是 11 ，容量也不是 2^n 扩容是 <code>2n+1</code> 的增长 。他的扩容叫做 <code>rehash</code> ,但是注意在扩容的时候把元素的位置颠倒了，也就是链表反插。 迭代接口含有比较古老的 <code>Enumeration</code> </p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">rehash</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> oldCapacity = table.length;</span><br><span class="line">        Entry&lt;?,?&gt;[] oldMap = table;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// overflow-conscious code</span></span><br><span class="line">        <span class="keyword">int</span> newCapacity = (oldCapacity &lt;&lt; <span class="number">1</span>) + <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">if</span> (newCapacity - MAX_ARRAY_SIZE &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (oldCapacity == MAX_ARRAY_SIZE)</span><br><span class="line">                <span class="comment">// Keep running with MAX_ARRAY_SIZE buckets</span></span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            newCapacity = MAX_ARRAY_SIZE;</span><br><span class="line">        &#125;</span><br><span class="line">        Entry&lt;?,?&gt;[] newMap = <span class="keyword">new</span> Entry&lt;?,?&gt;[newCapacity];</span><br><span class="line"></span><br><span class="line">        modCount++;</span><br><span class="line">        threshold = (<span class="keyword">int</span>)Math.min(newCapacity * loadFactor, MAX_ARRAY_SIZE + <span class="number">1</span>);</span><br><span class="line">        table = newMap;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = oldCapacity ; i-- &gt; <span class="number">0</span> ;) &#123;</span><br><span class="line">            <span class="comment">// 倒序插入到新的数组中，也就是老链表从头到尾遍历的节点头插到新的链表的头。</span></span><br><span class="line">            <span class="keyword">for</span> (Entry&lt;K,V&gt; old = (Entry&lt;K,V&gt;)oldMap[i] ; old != <span class="keyword">null</span> ; ) &#123;</span><br><span class="line">                Entry&lt;K,V&gt; e = old;</span><br><span class="line">                old = old.next;</span><br><span class="line"></span><br><span class="line">                <span class="keyword">int</span> index = (e.hash &amp; <span class="number">0x7FFFFFFF</span>) % newCapacity;</span><br><span class="line">                e.next = (Entry&lt;K,V&gt;)newMap[index];</span><br><span class="line">                newMap[index] = e;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>确定数组的下标采用的直接 <code>(hash &amp; 0x7FFFFFFF) % tab.length</code> ，并且在计算 <code>hash</code> 的时候是直接采用了 <code>key.hashCode()</code> 而在 <code>HashMap</code> 中还使用了扰动计算用来降低冲突的概率。 简单来说就是结合高低位来降低 <code>Hash冲突</code></li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">h ^= k.hashCode();</span><br><span class="line">h ^= (h &gt;&gt;&gt; <span class="number">20</span>) ^ (h &gt;&gt;&gt; <span class="number">12</span>);</span><br><span class="line"><span class="keyword">return</span> h ^ (h &gt;&gt;&gt; <span class="number">7</span>) ^ (h &gt;&gt;&gt; <span class="number">4</span>);</span><br></pre></td></tr></table></figure>

<ol start="4">
<li>不允许 null 的键值，他在源码中的做法就是先判断 <code>value</code> 是不是 <code>null</code> ，如果是直接抛异常。至于为什么没有判断 <code>key</code> 这是由于一会我们进行 <code>put</code> 操作的时候自然会调用 <code>key.hashCode()</code> 异常妥妥的抛出来了。</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> V <span class="title">put</span><span class="params">(K key, V value)</span> </span>&#123;</span><br><span class="line">        <span class="comment">// Make sure the value is not null</span></span><br><span class="line">        <span class="keyword">if</span> (value == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> NullPointerException();</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Makes sure the key is not already in the hashtable.</span></span><br><span class="line">        Entry&lt;?,?&gt; tab[] = table;</span><br><span class="line">        <span class="keyword">int</span> hash = key.hashCode();</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        关键在于一个对象的 HashCode可以为负数，这样操作后可以保证它为一个正整数</span></span><br><span class="line"><span class="comment">        0x7FFFFFFF is 0111 1111 1111 1111 1111 1111 1111 1111</span></span><br><span class="line"><span class="comment">        (hash &amp; 0x7FFFFFFF) 将会得到一个正整数</span></span><br><span class="line"><span class="comment">        因为hash是要作为数组的index的，这样可以避免出现下标为负数而出现异常</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">int</span> index = (hash &amp; <span class="number">0x7FFFFFFF</span>) % tab.length;</span><br><span class="line">        <span class="meta">@SuppressWarnings</span>(<span class="string">"unchecked"</span>)</span><br><span class="line">        Entry&lt;K,V&gt; entry = (Entry&lt;K,V&gt;)tab[index];</span><br><span class="line">        <span class="keyword">for</span>(; entry != <span class="keyword">null</span> ; entry = entry.next) &#123;</span><br><span class="line">            <span class="keyword">if</span> ((entry.hash == hash) &amp;&amp; entry.key.equals(key)) &#123;</span><br><span class="line">                V old = entry.value;</span><br><span class="line">                entry.value = value;</span><br><span class="line">                <span class="keyword">return</span> old;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        addEntry(hash, key, value, index);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<ol start="5">
<li><p>扩容迁移采用链表倒序插入，只有链表没有红黑树。</p>
</li>
<li><p>全部的方法都是 <code>synchronized</code> 的方法。</p>
</li>
</ol>
<p>&emsp;&emsp;   关于为什么 <code>&amp;0x7ffffffff</code> 这里提一下，关键在于一个对象的 HashCode可以为负数，这样操作后可以保证它为一个正整数0x7FFFFFFF is 0111 1111 1111 1111 1111 1111 1111 1111(hash &amp; 0x7FFFFFFF) 将会得到一个正整数。因为hash是要作为数组的index的，这样可以避免出现下标为负数而出现异常。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>LinkedList 源码分析</title>
    <url>/2018/03/26/Java%20SourceCode/LinkedList%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="LinkedList-源码分析"><a href="#LinkedList-源码分析" class="headerlink" title="LinkedList 源码分析"></a>LinkedList 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-结构"><a href="#1-结构" class="headerlink" title="1.结构"></a>1.结构</h2><h3 id="1-继承"><a href="#1-继承" class="headerlink" title="1. 继承"></a>1. 继承</h3><p>&emsp;&emsp;该类继承自 AbstractSequentialList 这个是由于他是一个顺序的列表，所以说继承的是一个顺序的 List</p>
<h3 id="2-实现"><a href="#2-实现" class="headerlink" title="2. 实现"></a>2. 实现</h3><p>这个类实现的接口比较多，具体如下：</p>
<ol>
<li>首先这个类是一个 List 自然有 List 接口</li>
<li>然后由于这个类是实现了 Deque 这个接口是双端队列的接口，所以说它是具有双端队列的特性的。后面我们会看到很多关于双端队列的方法。</li>
<li>然后就是两个集合框架肯定会实现的两个接口 Cloneable, Serializable 。<a id="more"></a>

</li>
</ol>
<h3 id="3-主要字段"><a href="#3-主要字段" class="headerlink" title="3. 主要字段"></a>3. 主要字段</h3><h4 id="1-属性字段"><a href="#1-属性字段" class="headerlink" title="1. 属性字段"></a>1. 属性字段</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">transient</span> <span class="keyword">int</span> size = <span class="number">0</span>;</span><br><span class="line"><span class="comment">//指向链表的头指针和尾指针</span></span><br><span class="line"><span class="keyword">transient</span> Node&lt;E&gt; first;</span><br><span class="line"><span class="keyword">transient</span> Node&lt;E&gt; last;</span><br></pre></td></tr></table></figure>

<h4 id="2-Node-节点"><a href="#2-Node-节点" class="headerlink" title="2. Node 节点"></a>2. Node 节点</h4><p>Node 节点是主要存放数据的地方这个节点数据结构也比较简单就是一个泛型加上前驱后继指针。也就是一个双向链表。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span>&lt;<span class="title">E</span>&gt; </span>&#123;</span><br><span class="line">  E item;</span><br><span class="line">  Node&lt;E&gt; next;</span><br><span class="line">  Node&lt;E&gt; prev;</span><br><span class="line"></span><br><span class="line">  Node(Node&lt;E&gt; prev, E element, Node&lt;E&gt; next) &#123;</span><br><span class="line">      <span class="keyword">this</span>.item = element;</span><br><span class="line">      <span class="keyword">this</span>.next = next;</span><br><span class="line">      <span class="keyword">this</span>.prev = prev;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>


<h3 id="4-主要方法概览"><a href="#4-主要方法概览" class="headerlink" title="4. 主要方法概览"></a>4. 主要方法概览</h3><ol>
<li>ctor-2</li>
<li>addFirst</li>
<li>addLast</li>
<li>addAll</li>
<li>add</li>
<li>indexOf</li>
<li>lastIndexOf</li>
<li>peek 获取第一个元素，是 null 就返回 null</li>
<li>peekFirst/Last  获取第一个最后一个元素</li>
<li>poll 删除第一个元素并返回 没有返回 null</li>
<li>pollFirst/Last </li>
<li>offer 调用了 add</li>
<li>offerFirst/Last</li>
<li>push</li>
<li>pop</li>
<li>set</li>
<li>remove(noArgs) == removeFirst  继承自 deque</li>
<li>remove(E e) 查找删除</li>
<li>read/writeObject  还是手动的序列化，原因一样，直接序列化元素而没有 pre/next</li>
</ol>
<h2 id="2-构造方法分析"><a href="#2-构造方法分析" class="headerlink" title="2. 构造方法分析"></a>2. 构造方法分析</h2><p>只有两个构造方法。其中一个是默认的空构造也就是生成一个空的 LinkedList 另外一个就是接受一个 Collection 接口。里面调用了 PutAll 方法。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">LinkedList</span><span class="params">()</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">LinkedList</span><span class="params">(Collection&lt;? extends E&gt; c)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">this</span>();</span><br><span class="line">    addAll(c);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-主要方法分析"><a href="#3-主要方法分析" class="headerlink" title="3. 主要方法分析"></a>3. 主要方法分析</h2><h3 id="1-add"><a href="#1-add" class="headerlink" title="1. add"></a>1. add</h3><p>这个方法就直接调用了 <code>linkLast</code> 而 <code>linkLast</code> 里面就是直接把元素添加到元素的结尾。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"> <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">add</span><span class="params">(E e)</span> </span>&#123;</span><br><span class="line">        linkLast(e);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">linkLast</span><span class="params">(E e)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; l = last;</span><br><span class="line">        <span class="keyword">final</span> Node&lt;E&gt; newNode = <span class="keyword">new</span> Node&lt;&gt;(l, e, <span class="keyword">null</span>);</span><br><span class="line">        last = newNode;</span><br><span class="line">        <span class="keyword">if</span> (l == <span class="keyword">null</span>)</span><br><span class="line">            first = newNode;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            l.next = newNode;</span><br><span class="line">        size++;</span><br><span class="line">        modCount++;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-addFrist-Last"><a href="#2-addFrist-Last" class="headerlink" title="2. addFrist/Last"></a>2. addFrist/Last</h3><p>这两个方法同上还是调用了 <code>linkFirst</code> 和 <code>linkLast</code> 所以说这几个添加修改的方法基本都是靠底层的同样的方法实现的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addFirst</span><span class="params">(E e)</span> </span>&#123;</span><br><span class="line">   linkFirst(e);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">addLast</span><span class="params">(E e)</span> </span>&#123;</span><br><span class="line">   linkLast(e);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-addAll"><a href="#3-addAll" class="headerlink" title="3. addAll"></a>3. addAll</h3><p> 该方法我们在构造方法中也看到了，在它里面实现的时候和 ArrayList 一样是直接把集合转成数组，然后进行创建新的节点插入进来。</p>
 <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">addAll</span><span class="params">(<span class="keyword">int</span> index, Collection&lt;? extends E&gt; c)</span> </span>&#123;</span><br><span class="line">       checkPositionIndex(index);</span><br><span class="line"></span><br><span class="line">       Object[] a = c.toArray();</span><br><span class="line">       <span class="keyword">int</span> numNew = a.length;</span><br><span class="line">       <span class="keyword">if</span> (numNew == <span class="number">0</span>)</span><br><span class="line">           <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line"></span><br><span class="line">       Node&lt;E&gt; pred, succ;</span><br><span class="line">       <span class="keyword">if</span> (index == size) &#123;</span><br><span class="line">           succ = <span class="keyword">null</span>;</span><br><span class="line">           pred = last;</span><br><span class="line">       &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">           succ = node(index);</span><br><span class="line">           pred = succ.prev;</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">for</span> (Object o : a) &#123;</span><br><span class="line">           <span class="meta">@SuppressWarnings</span>(<span class="string">"unchecked"</span>) E e = (E) o;</span><br><span class="line">           Node&lt;E&gt; newNode = <span class="keyword">new</span> Node&lt;&gt;(pred, e, <span class="keyword">null</span>);</span><br><span class="line">           <span class="keyword">if</span> (pred == <span class="keyword">null</span>)</span><br><span class="line">               first = newNode;</span><br><span class="line">           <span class="keyword">else</span></span><br><span class="line">               pred.next = newNode;</span><br><span class="line">           pred = newNode;</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       <span class="keyword">if</span> (succ == <span class="keyword">null</span>) &#123;</span><br><span class="line">           last = pred;</span><br><span class="line">       &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">           pred.next = succ;</span><br><span class="line">           succ.prev = pred;</span><br><span class="line">       &#125;</span><br><span class="line"></span><br><span class="line">       size += numNew;</span><br><span class="line">       modCount++;</span><br><span class="line">       <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-indexOf"><a href="#4-indexOf" class="headerlink" title="4. indexOf"></a>4. indexOf</h3><p>这个方法里面采用 for 循环遍历，遍历的时候是从头结点开始遍历，只要找到那个元素立即返回，而不继续进行下去。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">indexOf</span><span class="params">(Object o)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> index = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">if</span> (o == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">for</span> (Node&lt;E&gt; x = first; x != <span class="keyword">null</span>; x = x.next) &#123;</span><br><span class="line">                <span class="keyword">if</span> (x.item == <span class="keyword">null</span>)</span><br><span class="line">                    <span class="keyword">return</span> index;</span><br><span class="line">                index++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (Node&lt;E&gt; x = first; x != <span class="keyword">null</span>; x = x.next) &#123;</span><br><span class="line">                <span class="keyword">if</span> (o.equals(x.item))</span><br><span class="line">                    <span class="keyword">return</span> index;</span><br><span class="line">                index++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-lastIndexOf"><a href="#5-lastIndexOf" class="headerlink" title="5. lastIndexOf"></a>5. lastIndexOf</h3><p>这个方法和上面的方法实现的方式一样的，但是注意这个方法的意思是找到最后一个与之匹配的元素，他并不是从头开始找，而是直接从尾节点开始遍历。做法同理找到即停止。</p>
<h3 id="6-peek-peekFirst-peekLast"><a href="#6-peek-peekFirst-peekLast" class="headerlink" title="6. peek/peekFirst/peekLast"></a>6. peek/peekFirst/peekLast</h3><p><code>peek</code> 方法的意思就是返回最顶端的元素，如果这个元素不存在，那么直接返回 <code>null</code> 。之后还有 <code>peekFirst</code> 这类的就是返回第一个的意思。底层调用的就是头结点的属性。这些方法其实在 Collection 接口中是不存在的，主要就是因为他实现了 Deque 所带来的的新特性。</p>
<h3 id="7-poll-pollFirst-pollLast"><a href="#7-poll-pollFirst-pollLast" class="headerlink" title="7. poll/pollFirst/pollLast"></a>7. poll/pollFirst/pollLast</h3><p><code>poll</code> 用来删除头结点并返回，如果不存在就返回 <code>null</code><br>剩下的两个方法同理。 </p>
<h3 id="8-offer-offerFirst-offerLast"><a href="#8-offer-offerFirst-offerLast" class="headerlink" title="8. offer/offerFirst/offerLast"></a>8. offer/offerFirst/offerLast</h3><p>插入头结点。</p>
<h3 id="9-push-pop"><a href="#9-push-pop" class="headerlink" title="9. push/pop"></a>9. push/pop</h3><p>底层的方法就是 addFirst 和 removeFirst </p>
<h3 id="10-remove-noargs-remove-E-e"><a href="#10-remove-noargs-remove-E-e" class="headerlink" title="10. remove(noargs)/remove(E e)"></a>10. remove(noargs)/remove(E e)</h3><p>无参的调用 removeFirst 有参数的就是去查找然后删除。</p>
<h3 id="11-read-writeObject"><a href="#11-read-writeObject" class="headerlink" title="11. read/writeObject"></a>11. read/writeObject</h3><p>这里同 <code>ArrayList</code> 自己手动的进行了序列化。序列化的时候只是对 Node 节点里面的元素进行序列化，而前驱后继直接省略，也是节约空间的想法。</p>
<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4.总结"></a>4.总结</h2><p>好，其实在完全理解 <code>ArrayList</code> 的基础之上看这篇文章就比较好理解，里面的操作更加简单。只是注意一下两者的区别，实现了 <code>Deque</code> 带来的不少新的方法。</p>
]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>ArrayList 源码分析</title>
    <url>/2018/03/25/Java%20SourceCode/ArrayList%20%E6%BA%90%E7%A0%81%E5%88%86%E6%9E%90/</url>
    <content><![CDATA[<h1 id="ArrayList-源码分析"><a href="#ArrayList-源码分析" class="headerlink" title="ArrayList 源码分析"></a>ArrayList 源码分析</h1><blockquote>
<h3 id="1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！"><a href="#1-在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有-IDE-方便，所以在-github-上提供JDK1-8-的源码、详细的注释及测试用例。欢迎大家-star、fork-！" class="headerlink" title="1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 github 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！"></a>1. 在阅读源码时做了大量的注释，并且做了一些测试分析源码内的执行流程，由于博客篇幅有限，并且代码阅读起来没有 IDE 方便，所以在 <a href="https://github.com/lwenxu/JDKSourceCode" target="_blank" rel="noopener">github</a> 上提供JDK1.8 的源码、详细的注释及测试用例。欢迎大家 star、fork ！</h3><h3 id="2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"><a href="#2-由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！" class="headerlink" title="2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！"></a>2. 由于个人水平有限，对源码的分析理解可能存在偏差或不透彻的地方还请大家在评论区指出，谢谢！</h3></blockquote>
<h2 id="1-结构"><a href="#1-结构" class="headerlink" title="1. 结构"></a>1. 结构</h2><p>&emsp;&emsp;首先我们需要对 ArrayList 有一个大致的了解就从结构来看看吧.</p>
<h3 id="1-继承"><a href="#1-继承" class="headerlink" title="1. 继承"></a>1. 继承</h3><p>&emsp;&emsp;该类继承自 AbstractList 这个比较好说</p>
<h3 id="2-实现"><a href="#2-实现" class="headerlink" title="2. 实现"></a>2. 实现</h3><p>这个类实现的接口比较多，具体如下：</p>
<ol>
<li>首先这个类是一个 List 自然有 List 接口</li>
<li>然后由于这个类需要进行随机访问，所谓随机访问就是用下标任一访问，所以实现了RandomAccess</li>
<li>然后就是两个集合框架肯定会实现的两个接口 Cloneable, Serializable 前面这个好说序列化一会我们具体再说说<a id="more"></a>

</li>
</ol>
<h3 id="3-主要字段"><a href="#3-主要字段" class="headerlink" title="3. 主要字段"></a>3. 主要字段</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 默认大小为10</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> DEFAULT_CAPACITY = <span class="number">10</span>;</span><br><span class="line"><span class="comment">// 空数组  </span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Object[] EMPTY_ELEMENTDATA = &#123;&#125;;</span><br><span class="line"><span class="comment">// 默认的空数组  这个是在传入无参的是构造函数会调用的待会再 add 方法中会看到</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> Object[] DEFAULTCAPACITY_EMPTY_ELEMENTDATA = &#123;&#125;;</span><br><span class="line"><span class="comment">// 用来存放 ArrayList 中的元素 注意他的修饰符是一个 transient 也就是不会自动序列化</span></span><br><span class="line"><span class="keyword">transient</span> Object[] elementData; </span><br><span class="line"><span class="comment">// 大小</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">int</span> size;</span><br></pre></td></tr></table></figure>

<h3 id="4-主要方法"><a href="#4-主要方法" class="headerlink" title="4. 主要方法"></a>4. 主要方法</h3><p>下面的方法后面标有数字的就是表示重载方法</p>
<ol>
<li>ctor-3 </li>
<li>get</li>
<li>set</li>
<li>add-2</li>
<li>remove-2</li>
<li>clear </li>
<li>addAll</li>
<li>write/readObject</li>
<li>fast-fail 机制</li>
<li>subList</li>
<li>iterator</li>
<li>forEach</li>
<li>sort</li>
<li>removeIf</li>
</ol>
<h2 id="2-构造方法分析"><a href="#2-构造方法分析" class="headerlink" title="2. 构造方法分析"></a>2. 构造方法分析</h2><h3 id="1-无参的构造方法"><a href="#1-无参的构造方法" class="headerlink" title="1. 无参的构造方法"></a>1. 无参的构造方法</h3><p>&emsp;&emsp;  里面只有一个操作就是把 <code>elementData</code> 设置为 <code>DEFAULTCAPACITY_EMPTY_ELEMENTDATA</code> 这个空数组。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 无参的构造函数，传入一个空数组  这时候会创建一个大小为10的数组，具体操作在 add 中</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ArrayList</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.elementData = DEFAULTCAPACITY_EMPTY_ELEMENTDATA;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-传入数组大小的构造"><a href="#2-传入数组大小的构造" class="headerlink" title="2. 传入数组大小的构造"></a>2. 传入数组大小的构造</h3><p>&emsp;&emsp;  这个就是 new 一个数组，如果数组大小为0就 赋值为 <code>EMPTY_ELEMENTDATA</code> </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 按传入的参数创建新的底层数组</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">ArrayList</span><span class="params">(<span class="keyword">int</span> initialCapacity)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (initialCapacity &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">this</span>.elementData = <span class="keyword">new</span> Object[initialCapacity];</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (initialCapacity == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">this</span>.elementData = EMPTY_ELEMENTDATA;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> IllegalArgumentException(<span class="string">"Illegal Capacity: "</span>+</span><br><span class="line">                                               initialCapacity);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-传入-Collection-接口"><a href="#3-传入-Collection-接口" class="headerlink" title="3. 传入 Collection 接口"></a>3. 传入 Collection 接口</h3><p>&emsp;&emsp;  在这个方法里面主要就是把这个 Collection 转成一个数组，然后把这个数组 copy 一下，如果这个接口的 size 为0 和上面那个方法一样传入 <code>EMPTY_ELEMENTDATA</code> </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="title">ArrayList</span><span class="params">(Collection&lt;? extends E&gt; c)</span> </span>&#123;</span><br><span class="line">        elementData = c.toArray();</span><br><span class="line">        <span class="keyword">if</span> ((size = elementData.length) != <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">// c.toArray might (incorrectly) not return Object[] (see 6260652)</span></span><br><span class="line">            <span class="comment">// 上面的注释的意思是说 jdk 有一个 bug 具体来说就是一个 Object 类型的数组不一定能够存放 Object类型的对象，有可能抛异常</span></span><br><span class="line">            <span class="comment">// 主要是因为 Object 类型的数组可能指向的是他的子类的数组，存 Object 类型的东西会报错</span></span><br><span class="line">            <span class="keyword">if</span> (elementData.getClass() != Object[].class)</span><br><span class="line">                <span class="comment">// 这个操作是首先new 了新的数组，然后再调用 System.arraycopy 拷贝值。也就是产生新的数组</span></span><br><span class="line">                elementData = Arrays.copyOf(elementData, size, Object[].class);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="comment">// 传入的是空的就直接使用空数组初始化</span></span><br><span class="line">            <span class="keyword">this</span>.elementData = EMPTY_ELEMENTDATA;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>


<p>&emsp;&emsp;  但是注意一点这里有一个 jdk 的 bug 也就是一个 Object 类型的数组不一定能够存放 Object类型的对象，有可能抛异常，主要是因为 Object 类型的数组可能指向的是他的子类的数组，存 Object 类型的东西会报错。 为了测试这个 bug 写了几行代码测试一下。这个测试是通不过的，就是存在上面的原因。</p>
<p>&emsp;&emsp;  一个典型的例子就是 我们创建一个 string 类型的 list 然后调用 toArray 方法发现返回的是一个 string[] 这时候自然就不能随便存放元素了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JDKBug</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        B[] arrB = <span class="keyword">new</span> B[<span class="number">10</span>];</span><br><span class="line">        A[] arrA = arrB;</span><br><span class="line">        arrA[<span class="number">0</span>]=<span class="keyword">new</span> A();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="3-修改方法分析"><a href="#3-修改方法分析" class="headerlink" title="3. 修改方法分析"></a>3. 修改方法分析</h2><h3 id="1-Set-方法"><a href="#1-Set-方法" class="headerlink" title="1. Set 方法"></a>1. Set 方法</h3><p>&emsp;&emsp;  这个方法也很简单 ，首先进行范围判断，然后就是直接更新下标即可。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 也没啥好说的就是，设置新值返回老值</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> E <span class="title">set</span><span class="params">(<span class="keyword">int</span> index, E element)</span> </span>&#123;</span><br><span class="line">    rangeCheck(index);</span><br><span class="line"></span><br><span class="line">    E oldValue = elementData(index);</span><br><span class="line">    elementData[index] = element;</span><br><span class="line">    <span class="keyword">return</span> oldValue;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-Add-E-e-方法"><a href="#2-Add-E-e-方法" class="headerlink" title="2. Add(E e) 方法"></a>2. Add(E e) 方法</h3><p>&emsp;&emsp;这个方法首先调用了 <code>ensureCapacityInternal()</code> 这个方法里面就判断了当前的 <code>elementData</code> 是否等于 <code>DEFAULTCAPACITY_EMPTY_ELEMENTDATA</code> 如果是的话，就把数组的大小设置为 10 然后进行扩容操作,这里刚好解释了为什么采用无参构造的List 的大小是 10 ，这里扩容操作调用的方法是 <code>ensureExplicitCapacity</code> 里面就干了一件事如果用户指定的大小 大于当前长度就扩容，扩容的方法采用了 <code>Arrays.copy</code> 方法，这个方法实现原理是 new 出一个新的数组，然后调用 <code>System.arraycopy</code> 拷贝数组，最后返回新的数组。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">add</span><span class="params">(E e)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 当调用了无参构造，设置大小为10</span></span><br><span class="line">    ensureCapacityInternal(size + <span class="number">1</span>);  <span class="comment">// Increments modCount        </span></span><br><span class="line">    elementData[size++] = e;</span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">ensureCapacityInternal</span><span class="params">(<span class="keyword">int</span> minCapacity)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 如果当前数组是默认空数组就设置为 10和 size+1中的最小值</span></span><br><span class="line">    <span class="comment">// 这也就是说为什么说无参构造 new 的数组大小是 10</span></span><br><span class="line">    <span class="keyword">if</span> (elementData == DEFAULTCAPACITY_EMPTY_ELEMENTDATA) &#123;</span><br><span class="line">        minCapacity = Math.max(DEFAULT_CAPACITY, minCapacity);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    ensureExplicitCapacity(minCapacity);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">ensureExplicitCapacity</span><span class="params">(<span class="keyword">int</span> minCapacity)</span> </span>&#123;</span><br><span class="line">    modCount++;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 若用户指定的最小容量 &gt; 最小扩充容量，则以用户指定的为准，否则还是 10</span></span><br><span class="line">    <span class="keyword">if</span> (minCapacity - elementData.length &gt; <span class="number">0</span>)</span><br><span class="line">        grow(minCapacity);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">grow</span><span class="params">(<span class="keyword">int</span> minCapacity)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// overflow-conscious code</span></span><br><span class="line">    <span class="keyword">int</span> oldCapacity = elementData.length;</span><br><span class="line">    <span class="comment">// 1.5倍增长</span></span><br><span class="line">    <span class="keyword">int</span> newCapacity = oldCapacity + (oldCapacity &gt;&gt; <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">if</span> (newCapacity - minCapacity &lt; <span class="number">0</span>)</span><br><span class="line">        newCapacity = minCapacity;</span><br><span class="line">    <span class="keyword">if</span> (newCapacity - MAX_ARRAY_SIZE &gt; <span class="number">0</span>)</span><br><span class="line">        newCapacity = hugeCapacity(minCapacity);</span><br><span class="line">    <span class="comment">// minCapacity is usually close to size, so this is a win:</span></span><br><span class="line">    elementData = Arrays.copyOf(elementData, newCapacity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-Add-int-index-E-e-方法"><a href="#3-Add-int-index-E-e-方法" class="headerlink" title="3. Add(int index, E e) 方法"></a>3. Add(int index, E e) 方法</h3><p>&emsp;&emsp;  这个方法比较简单和上面基本一样，然后只是最后放元素的时候的操作不一样，他是采用了 System.arraycopy 从自己向自己拷贝，目的就在于覆盖元素。 注意一个规律这里面只要涉及下标的操作的很多不是自己手写 for 循环而是采用类似的拷贝覆盖的方法。算是一个小技巧。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(<span class="keyword">int</span> index, E element)</span> </span>&#123;</span><br><span class="line">        rangeCheckForAdd(index);</span><br><span class="line">        ensureCapacityInternal(size + <span class="number">1</span>);  <span class="comment">// Increments modCount</span></span><br><span class="line">        <span class="comment">// 覆盖</span></span><br><span class="line">        System.arraycopy(elementData, index, elementData, index + <span class="number">1</span>,</span><br><span class="line">                         size - index);</span><br><span class="line">        elementData[index] = element;</span><br><span class="line">        size++;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-remove-int-index"><a href="#4-remove-int-index" class="headerlink" title="4. remove(int index)"></a>4. remove(int index)</h3><p>&emsp;&emsp;同理这里面还是用了拷贝覆盖的技巧。 但是有一点注意的就是不用的节点需要手动的触发 gc ，这也是在 Efftive Java 中作者举的一个例子。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> E <span class="title">remove</span><span class="params">(<span class="keyword">int</span> index)</span> </span>&#123;</span><br><span class="line">        rangeCheck(index);</span><br><span class="line">        modCount++;</span><br><span class="line">        E oldValue = elementData(index);</span><br><span class="line">        <span class="keyword">int</span> numMoved = size - index - <span class="number">1</span>;</span><br><span class="line">        <span class="comment">//覆盖</span></span><br><span class="line">        <span class="keyword">if</span> (numMoved &gt; <span class="number">0</span>)</span><br><span class="line">            System.arraycopy(elementData, index+<span class="number">1</span>, elementData, index,</span><br><span class="line">                             numMoved);</span><br><span class="line">        elementData[--size] = <span class="keyword">null</span>; <span class="comment">// clear to let GC do its work</span></span><br><span class="line">        <span class="keyword">return</span> oldValue;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-remove-E-e"><a href="#5-remove-E-e" class="headerlink" title="5. remove(E e)"></a>5. remove(E e)</h3><p>&emsp;&emsp;  这个方法操作很显然会判断 e 是不是 null 如果是 null 的话直接采用 <code>==</code> 比较，否则的话就直接调用 <code>equals</code> 方法然后执行拷贝覆盖。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">remove</span><span class="params">(Object o)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (o == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> index = <span class="number">0</span>; index &lt; size; index++)</span><br><span class="line">                <span class="keyword">if</span> (elementData[index] == <span class="keyword">null</span>) &#123;</span><br><span class="line">                    <span class="comment">// 覆盖</span></span><br><span class="line">                    fastRemove(index);</span><br><span class="line">                    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> index = <span class="number">0</span>; index &lt; size; index++)</span><br><span class="line">                <span class="comment">// 调用 equals 方法</span></span><br><span class="line">                <span class="keyword">if</span> (o.equals(elementData[index])) &#123;</span><br><span class="line">                    fastRemove(index);</span><br><span class="line">                    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="6-clear"><a href="#6-clear" class="headerlink" title="6. clear()"></a>6. clear()</h3><p>&emsp;&emsp;  这个方法就干了一件事，把数组中的引用全都设置为 null 以便 gc 。而不是仅仅把 size 设置为 0 。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// gc 所有节点</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">clear</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        modCount++;</span><br><span class="line">        <span class="comment">// clear to let GC do its work</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; size; i++)</span><br><span class="line">            elementData[i] = <span class="keyword">null</span>;</span><br><span class="line">        size = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">``` </span><br><span class="line"></span><br><span class="line">### 7. addAll(Collection e)</span><br><span class="line">&amp;emsp;&amp;emsp;  这个没啥好说的就是，采用转数组然后 copy</span><br><span class="line"></span><br><span class="line">```java</span><br><span class="line">    <span class="comment">// 一个套路 只要涉及到 Collection接口的方法都是把这个接口转成一个数组然后对数组操作</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">addAll</span><span class="params">(Collection&lt;? extends E&gt; c)</span> </span>&#123;</span><br><span class="line">        Object[] a = c.toArray();</span><br><span class="line">        <span class="keyword">int</span> numNew = a.length;</span><br><span class="line">        ensureCapacityInternal(size + numNew);  <span class="comment">// Increments modCount</span></span><br><span class="line">        System.arraycopy(a, <span class="number">0</span>, elementData, size, numNew);</span><br><span class="line">        size += numNew;</span><br><span class="line">        <span class="keyword">return</span> numNew != <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h2 id="4-访问方法分析"><a href="#4-访问方法分析" class="headerlink" title="4. 访问方法分析"></a>4. 访问方法分析</h2><h3 id="1-get"><a href="#1-get" class="headerlink" title="1. get"></a>1. get</h3><p>&emsp;&emsp;  直接访问数组下标。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 没啥好说的直接去找数组下标</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> E <span class="title">get</span><span class="params">(<span class="keyword">int</span> index)</span> </span>&#123;</span><br><span class="line">    rangeCheck(index);</span><br><span class="line">    <span class="keyword">return</span> elementData(index);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-subList"><a href="#2-subList" class="headerlink" title="2. subList"></a>2. subList</h3><p>&emsp;&emsp;  这个方法的实现比较有意思，他不是直接截取一个新的 List 返回，而是在这个类的内部还有一个 subList 的内部类，然后这个类就记录了 subList 的开始结束下标，然后返回的是这个 subList 对象。你可能会想返回的 subList 他不是 List 不会有问题吗，这里这个 subList 是继承的 AbstractList 所以还是正确的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> List&lt;E&gt; <span class="title">subList</span><span class="params">(<span class="keyword">int</span> fromIndex, <span class="keyword">int</span> toIndex)</span> </span>&#123;</span><br><span class="line">        subListRangeCheck(fromIndex, toIndex, size);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> SubList(<span class="keyword">this</span>, <span class="number">0</span>, fromIndex, toIndex);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// subList 返回的是一个位置标记实例，就是在原来的数组上放了一些标志，没有修改或者拷贝新的空间</span></span><br><span class="line"><span class="keyword">private</span> <span class="class"><span class="keyword">class</span> <span class="title">SubList</span> <span class="keyword">extends</span> <span class="title">AbstractList</span>&lt;<span class="title">E</span>&gt; <span class="keyword">implements</span> <span class="title">RandomAccess</span> </span>&#123;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> AbstractList&lt;E&gt; parent;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> parentOffset;</span><br><span class="line">        <span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">int</span> offset;</span><br><span class="line">        <span class="keyword">int</span> size;</span><br><span class="line">        <span class="comment">// other functions .....</span></span><br><span class="line">     &#125;</span><br></pre></td></tr></table></figure>

<h2 id="5-其他功能方法"><a href="#5-其他功能方法" class="headerlink" title="5. 其他功能方法"></a>5. 其他功能方法</h2><h3 id="1-write-readObject"><a href="#1-write-readObject" class="headerlink" title="1. write/readObject"></a>1. write/readObject</h3><p>&emsp;&emsp;前面在介绍数据域的时候我就有标注 elementData 是一个 transition 的变量也就是在自动序列化的时候会忽略这个字段。</p>
<p>&emsp;&emsp;  然后我们又在源码中找到到了 <code>write/readObject</code> 方法，这两个方法是用来序列化 <code>elementData</code> 中的每一个元素，也就是手动的对这个字段进行序列化和反序列化。这不是多此一举吗？</p>
<p>&emsp;&emsp;  既然要将ArrayList的字段序列化（即将elementData序列化），那为什么又要用transient修饰elementData呢？</p>
<p>&emsp;&emsp; 回想ArrayList的自动扩容机制，elementData数组相当于容器，当容器不足时就会再扩充容量，但是容器的容量往往都是大于或者等于ArrayList所存元素的个数。</p>
<p>&emsp;&emsp; 比如，现在实际有了8个元素，那么elementData数组的容量可能是8x1.5=12，如果直接序列化elementData数组，那么就会浪费4个元素的空间，特别是当元素个数非常多时，这种浪费是非常不合算的。</p>
<p>&emsp;&emsp; 所以ArrayList的设计者将elementData设计为transient，然后在writeObject方法中手动将其序列化，并且只序列化了实际存储的那些元素，而不是整个数组。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">writeObject</span><span class="params">(java.io.ObjectOutputStream s)</span></span></span><br><span class="line"><span class="function">        <span class="keyword">throws</span> java.io.IOException</span>&#123;</span><br><span class="line">        <span class="comment">// Write out element count, and any hidden stuff</span></span><br><span class="line">        <span class="keyword">int</span> expectedModCount = modCount;</span><br><span class="line">        s.defaultWriteObject();</span><br><span class="line">        <span class="comment">// Write out size as capacity for behavioural compatibility with clone()</span></span><br><span class="line">        s.writeInt(size);</span><br><span class="line">        <span class="comment">// Write out all elements in the proper order.</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>; i&lt;size; i++) &#123;</span><br><span class="line">            s.writeObject(elementData[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (modCount != expectedModCount) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> ConcurrentModificationException();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-fast-fail"><a href="#2-fast-fail" class="headerlink" title="2. fast-fail"></a>2. fast-fail</h3><p>&emsp;&emsp;  所谓的 <code>fast-fail</code> 就是在我们进行 <code>iterator</code> 遍历的时候不允许调用 <code>Collection</code> 接口的方法进行对容器修改，否则就会抛异常。这个实现的机制是在 <code>iterator</code> 中维护了两个变量，分别是 <code>modCount</code> 和 <code>expectedModCount</code> 由于 <code>Collection</code> 接口的方法在每次修改操作的时候都会对 <code>modCount++</code> 所以如果在 <code>iterator</code> 中检测到他们不相等的时候就抛异常。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="class"><span class="keyword">class</span> <span class="title">Itr</span> <span class="keyword">implements</span> <span class="title">Iterator</span>&lt;<span class="title">E</span>&gt; </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> cursor;       <span class="comment">// index of next element to return</span></span><br><span class="line">        <span class="keyword">int</span> lastRet = -<span class="number">1</span>; <span class="comment">// index of last element returned; -1 if no such</span></span><br><span class="line">        <span class="keyword">int</span> expectedModCount = modCount;</span><br><span class="line">        </span><br><span class="line">        <span class="function"><span class="keyword">final</span> <span class="keyword">void</span> <span class="title">checkForComodification</span><span class="params">()</span> </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (modCount != expectedModCount)</span><br><span class="line">                <span class="keyword">throw</span> <span class="keyword">new</span> ConcurrentModificationException();</span><br><span class="line">        &#125;</span><br><span class="line">   &#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-forEach"><a href="#3-forEach" class="headerlink" title="3. forEach"></a>3. forEach</h3><p>&emsp;&emsp;  这个是一个函数式编程的方法，看看他的参数 <code>forEach(Consumer&lt;? super E&gt; action)</code> 很有意思里面接受是一个函数式的接口，我们里面回调了 <code>Consumer</code> 的 <code>accept</code> 所以我们只需要传入一个函数接口就能对每一个元素处理。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Override</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">forEach</span><span class="params">(Consumer&lt;? <span class="keyword">super</span> E&gt; action)</span> </span>&#123;</span><br><span class="line">    Objects.requireNonNull(action);</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> expectedModCount = modCount;</span><br><span class="line">    <span class="meta">@SuppressWarnings</span>(<span class="string">"unchecked"</span>)</span><br><span class="line">    <span class="keyword">final</span> E[] elementData = (E[]) <span class="keyword">this</span>.elementData;</span><br><span class="line">    <span class="keyword">final</span> <span class="keyword">int</span> size = <span class="keyword">this</span>.size;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>; modCount == expectedModCount &amp;&amp; i &lt; size; i++) &#123;</span><br><span class="line">        <span class="comment">//回调</span></span><br><span class="line">        action.accept(elementData[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (modCount != expectedModCount) &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> ConcurrentModificationException();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>写了一段测试代码，但是这个方法不常用，主要是 Collection 是可以自己生成 Stream 对象，然后调用上面的方法即可。这里提一下。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ArrayListTest</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">foreach</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        ArrayList&lt;Integer&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        list.add(<span class="number">2</span>);</span><br><span class="line">        list.add(<span class="number">1</span>);</span><br><span class="line">        list.add(<span class="number">4</span>);</span><br><span class="line">        list.add(<span class="number">6</span>);</span><br><span class="line">        list.forEach(System.out::print);  <span class="comment">//打印每一次元素。</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-sort"><a href="#4-sort" class="headerlink" title="4. sort"></a>4. sort</h3><p>底层调用了 Arrays.sort 方法没什么好说的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">(Comparator&lt;? <span class="keyword">super</span> E&gt; c)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">final</span> <span class="keyword">int</span> expectedModCount = modCount;</span><br><span class="line">        Arrays.sort((E[]) elementData, <span class="number">0</span>, size, c);</span><br><span class="line">        <span class="keyword">if</span> (modCount != expectedModCount) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> ConcurrentModificationException();</span><br><span class="line">        &#125;</span><br><span class="line">        modCount++;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-removeIf"><a href="#5-removeIf" class="headerlink" title="5. removeIf"></a>5. removeIf</h3><p>&emsp;&emsp;  这个和 forEach 差不多，就是回调写好了。</p>
<h2 id="6-Vector"><a href="#6-Vector" class="headerlink" title="6. Vector"></a>6. Vector</h2><p>以上基本是把 <code>ArrayList</code> 的重要的方法和属性介绍完了，我们已经比较清楚他底层的实现和数据结构了。然后提到 <code>ArrayList</code> 自然也少不了一个比较古老的容器 <code>Vector</code> 这个容器真的和 <code>ArrayList</code> 太像了。因为你会发现他们连继承和实现的接口都是一样的。但是也会有一些不同的地方，下面分条介绍一下。</p>
<ol>
<li><p>在 <code>Vector</code> 中基本所有的方法都是 <code>synchronized</code> 的方法，所以说他是线程安全的 <code>ArrayList</code> </p>
</li>
<li><p>构造方法不一样，在属性中没有两个比较特殊的常量，所以说他的构造方法直接初始化一个容量为 10 的数组。然后他有四个构造方法。</p>
</li>
<li><p>遍历的接口不一样。他还是有 <code>iterator</code> 的但是他以前的遍历的方法是 <code>Enumeration</code> 接口，通过 <code>elements</code> 获取 <code>Enumeration</code> 然后使用 <code>hasMoreElements</code> 和 <code>nextElement</code> 获取元素。</p>
</li>
<li><p>缺少一些函数式编程的方法。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>JDK 源码分析</category>
      </categories>
      <tags>
        <tag>JDK 源码分析</tag>
      </tags>
  </entry>
  <entry>
    <title>P2P 浅析</title>
    <url>/2018/03/25/NetWork/P2P%20%E6%B5%85%E6%9E%90/</url>
    <content><![CDATA[<h1 id="P2P-浅析"><a href="#P2P-浅析" class="headerlink" title="P2P 浅析"></a>P2P 浅析</h1><p>&emsp;&emsp;  对等网络(P2P)技术,是在传统的客户机和服务器模式来说的一种方式。在C/S模式中，数据的分发采用专门的服务器，多个客户端都从此服务器获取数据。这种模式的优点是：数据的一致性容易控制，系统也容易管理。但是此种模式的缺点是：因为服务器的个数只有一个(即便有多个也非常有限)，系统容易出现单一失效点；单一服务器面对众多的客户端，由于CPU能力、内存大小、网络带宽的限制，可同时服务的客户端非常有限，可扩展性差。</p>
<a id="more"></a>
<p>&emsp;&emsp;  P2P技术正是为了解决这些问题而提出来的一种对等网络结构。在P2P网络中，每个节点既可以从其他节点得到服务，也可以向其他节点提供服务,他是一种是一种无中心服务器的对等网络泛型。这样，庞大的终端资源被利用起来，一举解决了C/S模式中的两个弊端。</p>
<p>&emsp;&emsp;  但是一说到 P2P 肯定哟啊说关于 NAT 穿透的问题，因为这是干扰 P2P 最重要的一个因素。</p>
<p>待续。。</p>
<h2 id="1-NAT-类型"><a href="#1-NAT-类型" class="headerlink" title="1. NAT 类型"></a>1. NAT 类型</h2><h2 id="2-STUN-鉴别-NAT-类型"><a href="#2-STUN-鉴别-NAT-类型" class="headerlink" title="2. STUN 鉴别 NAT 类型"></a>2. STUN 鉴别 NAT 类型</h2><h2 id="3-P2P-结构"><a href="#3-P2P-结构" class="headerlink" title="3. P2P 结构"></a>3. P2P 结构</h2><h2 id="4-BitTorrent-基本原理"><a href="#4-BitTorrent-基本原理" class="headerlink" title="4. BitTorrent 基本原理"></a>4. BitTorrent 基本原理</h2><h2 id="5-DHT-基本原理"><a href="#5-DHT-基本原理" class="headerlink" title="5. DHT 基本原理"></a>5. DHT 基本原理</h2>]]></content>
      <categories>
        <category>-计算机网络</category>
      </categories>
  </entry>
  <entry>
    <title>TCP 总结</title>
    <url>/2018/03/25/NetWork/TCP%20%E8%AF%A6%E8%A7%A3/</url>
    <content><![CDATA[<h1 id="TCP-总结"><a href="#TCP-总结" class="headerlink" title="TCP 总结"></a>TCP 总结</h1><blockquote>
<p>计算机网络中比较中要的无非就是 TCP/IP 协议栈，以及应用层的 HTTP 和 HTTPS 。<br>前几天一直炒的的比较火的就是 HTTP/2.0 了，但是其实 HTTP/2.0 早在2015年的时候就已经出来了，并且这个版本是基于 Google 公司的 SPDY 协议发布的，其实说白了就是用的 SPDY 做了一点修改。<br>好了今天的主题是 TCP 就不过多的介绍 HTTP/2.0 了,以后会专门写一篇关于 HTTP/2.0 的文章，介绍一下他的新特性。</p>
</blockquote>
<a id="more"></a>
<h2 id="1-引言"><a href="#1-引言" class="headerlink" title="1.引言"></a>1.引言</h2><p>&emsp;&emsp;我们都知道 TCP 是位于传输层的协议，他还有一个兄弟就是 UDP ，他们两共同构成了传输层。显然他们之间有很大的区别要不然的话在传输层只需要一个就好了。</p>
<p>&emsp;&emsp;其中最重要的区别就是一个面向连接另外一个不是，这个区别就导致了他们是否能够保证稳定传输，显然不面向连接的 UDP 是没办法保证可靠传输的，他只能靠底层的网络层和链路层来保证。我们都知道网络层采用的是不可靠的 IP 协议。好吧，网络层也保证不了可靠传输，所以 UDP 保证可靠传输只能依靠链路层了。</p>
<p>&emsp;&emsp;而 TCP 就好说了他不仅仅有底层的链路层的支持，还有自己的面向链接服务来保证可靠传输。当然 TCP也不仅仅就是比 UDP 多了一个可靠传输，前面也说到了这只是他们之间一个重要的区别。其实他的三个重要特性就是它们之间的区别。</p>
<p>&emsp;&emsp;* 可靠传输<br>&emsp;&emsp;* 流量控制<br>&emsp;&emsp;* 拥塞控制</p>
<h2 id="2-可靠传输"><a href="#2-可靠传输" class="headerlink" title="2.可靠传输"></a>2.可靠传输</h2><p>TCP 主要是<code>确认重传机制</code> <code>数据校验</code> <code>数据合理分片和排序</code> <code>流量控制</code> <code>拥塞控制</code>依靠来完成可靠传输的 , 下面详细介绍这几种保证可靠传输的方式。</p>
<h3 id="1-确认和重传"><a href="#1-确认和重传" class="headerlink" title="1. 确认和重传"></a>1. 确认和重传</h3><p>确认重传，简单来说就是接收方收到报文以后给发送方一个 ACK 回复，说明自己已经收到了发送方发过来的数据。如果发送方等待了一个特定的时间还没有收到接收方的 ACK 他就认为数据包丢了，接收方没有收到就会重发这个数据包。</p>
<p>好的，上面的机制还是比较好理解的，但是我们会发现一个问题，那就是如果接收方已经收到了数据然后返回的 ACK 丢失，发送方就会误判导致重发。而此时接收方就会收到冗余的数据，但是接收方怎么能判定这个数据是冗余的还是新的数据呢？</p>
<p>这就涉及到了 TCP 的另外一个机制就是采用序号和确认号，也就是每次发送数据的时候这个报文段里面包括了当前报文段的序号和对上面的报文的确认号，这样我们的接收方可以根据自己接受缓存中已经有的数据来确定是否接受到了重复的报文段。这时候如果出现上面所说的 ACK 丢失，导致接受重复的报文段时客户端丢弃这个冗余的报文段。</p>
<p>好现在我们大致了解了确认重传机制，但是还有些东西还没有弄清楚，也就是 TCP 真正的实现究竟是怎样的。</p>
<ol>
<li><p>确认是每发一个报文段就确认一次还是一次确认多个呢？</p>
</li>
<li><p>还有上面所说的发送方等待一个特定的时间，这个时间究竟等多长比较合适？</p>
</li>
<li><p>重传的时候是只重传那个没收到的报文还是重传那个报文段及它以后的报文段？</p>
<h4 id="1-累计确认-单停等协议"><a href="#1-累计确认-单停等协议" class="headerlink" title="1.累计确认/单停等协议"></a>1.累计确认/单停等协议</h4><p>这就是我们要解决的第一个问题就是如何确认。这里涉及到两种确认方式，分别称为<code>累计确认（捎带确认）</code> 和 <code>单停等协议</code> 。</p>
<h6 id="单停等协议"><a href="#单停等协议" class="headerlink" title="单停等协议"></a>单停等协议</h6><p><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-25/9290971.jpg" alt></p>
</li>
</ol>
<p>用一张图来快速理解，就是每发送一次数据，就进行一次确认。等发送方收到了 ACK 才能进行下一次的发送。</p>
<h5 id="累计确认"><a href="#累计确认" class="headerlink" title="累计确认"></a>累计确认</h5><p><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-25/8684982.jpg" alt></p>
<p>一样的也是采用的 ACK 机制，但是注意一点的是，并非对于每一个报文段都进行确认，而仅仅对最后一个报文段确认，捎带的确认了上图中的 203 号及以前的报文。<br><font color="#A52A2A"><br><strong>总结：从上面可以看到累计确认的效率更加高，首先他的确认包少一些那么也就是在网络中出现的大部分是需要传输的数据，而不是一半的数据一半的 ACK ，然后我们在第二张图中可以看到我们是可以连续发送多个报文段的（究竟一次性能发多少这个取决于发送窗口，而发送窗口又是由接受窗口和拥塞窗口一起来决定的。），一次性发多个数据会提高网络的吞吐量以及效率这个可以证明，比较简单这里不再赘述！</strong></font></p>
<p><strong>结论：显然怎么看都是后者比较有优势，TCP 的实现者自然也是采用的累计确认的方式！</strong><br></p>
<h4 id="2-超时时间计算"><a href="#2-超时时间计算" class="headerlink" title="2. 超时时间计算"></a>2. 超时时间计算</h4><p>上文中的那个特定的时间就是超时时间，为什么有这个值呢? 其实在发送端发送的时候就为数据启动了一个定时器，这个定时器的初始值就是超时时间。</p>
<p>超时时间的计算其实有点麻烦，主要是我们很难确定一个确定的值，太长则进行了无意义的等待，太短就会导致冗余的包。TCP 的设计者们设计了一个计算超时时间的公式，这个公式概念比较多，有一点点麻烦，不过没关系我们一点点的来。</p>
<p>首先我们自己思考如何设计一个超时时间的计算公式，超时时间一般肯定是和数据的传输时间有关系的，他必然要大于数据的往返时间（数据在发送端接收端往返一趟所用的时间）。好，那么我们就从往返时间下手，可是又有一个问题就是往返时间并不是固定的我们有如何确定这个值呢？自然我们会想到我们可以取一小段时间的往返时间的平均值来代表这一时间点的往返时间，也就是微积分的思想！</p>
<p>好了我们找到了往返时间（RTT），接下来的超时时间应该就是往返时间再加上一个数就能得到超时时间了。这个数也应该是动态的，我们就选定为往返时间的波动差值，也就是相邻两个往返时间的差。</p>
<p>下面给出我们所预估的超时时间（TimeOut）公式：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">TimeOut = AvgRTT2 + | AvgRTT2 - AvgRTT1 |</span><br></pre></td></tr></table></figure>

<p>很好，看到这里其实你已经差不多理解了超时时间的计算方式了，只不过我们这个公式不够完善，但是思路是对的。我们这时候来看看 TCP 的实现者们采用的方式。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">RTT_New = (1-a)RTT_Current + a*Avg_RTT (计算平均 RTT，a 通常取0.125)</span><br><span class="line">DevRTT = (1-b)DevRTT + b|RTT_New - Avg_RTT|  （计算差值，b 通常取0.25）</span><br><span class="line">TimeOut = RTT_New + 4*DevRTT （计算超时时间）</span><br></pre></td></tr></table></figure>

<p>好的，这就是 TCP 实现的超时时间的方式，但是在实际的应用中并不是一直采用的这种方式。假如说我们现在网络状态非常的差，一直在丢包我们根本没必要这样计算，而是采用直接把原来的超时时间加倍作为新的超时时间。</p>
<font color="#A52A2A">
**总结：好的现在我们知道了在两种情况下的超时时间的计算方式，正常的情况下我们采用的上面的比较复杂的计算公式，也就是 `RTT+波动值` 否则直接加倍**
</font>

<h4 id="3-快速重传"><a href="#3-快速重传" class="headerlink" title="3. 快速重传"></a>3. 快速重传</h4><p>上面我们看到在发送方等待一个超时重传时间后会开始重传，但是我们计算的超时重传时间也不定就很准，也就是说我们经常干的一件事就会是等待，而且一般等的时间还挺长。那么可不可以优化一下呢？</p>
<p>当然，在 TCP 实现中是做了优化的，也就是这里说到的快速重传机制。他的原理就是在发送方收到三个冗余的 ACK 的时候，就开始重传那个报文段。那么为什么是三个冗余的 ACK 呢？注意三个冗余的 ACK 其实是四个 ACK 。我们先了解一下发送 ACK 策略，这个是 <code>RFC 5681 文档</code> 规定的。</p>
<ol>
<li><p>第一种情况收到一个期望的有序的数据时，最多延时 500ms 发送一个 ACK 表示该数据及以前的数据都收到了。</p>
</li>
<li><p>第二种情况是收到一个期望的有序的数据时，前面的有序数据等待发送 ACK 的时候立即发送一个 ACK 捎带确认前面那个数据，也就是第一个数据还在延时的时候又来一个那么久两个一起确认。</p>
</li>
<li><p>第三种情况，收到比期望序号大的数据的时候立即发送冗余 ACK ，ACK 确认的值就是中间缺少的第一个序号的值。</p>
</li>
<li><p>收到能部分填充或者完全填充中间缺少的数据的，如果这个报文是起始于缺少的数据的低端就立即发送一个 ACK。</p>
</li>
</ol>
<p>好的，那么现在我们可以看到如果出现了三个冗余的 ACK 他只可能是发生了两次情况三，也就是发送了两个比期望值大的数据。但是注意出现情况三有两种可能，一个是丢包，另外一个是乱序到达。<br>比如说我们现在是数据乱序到达的，我们来看一下。</p>
<p>第一种乱序情况<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-25/14623789.jpg" alt></p>
<p>另外一种乱序<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-25/60926878.jpg" alt></p>
<p>丢包情况<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-25/17661680.jpg" alt></p>
<font color="#A52A2A">
**结论: 很显然我们可以看到，如果发生了乱序有可能会出现三次冗余 ACK，但是如果发现了丢包必然会有三次冗余 ACK 发生，只是 ACK 数量可能更多但是不会比三次少**
</font>

<h4 id="4-数据重传方式"><a href="#4-数据重传方式" class="headerlink" title="4.数据重传方式"></a>4.数据重传方式</h4><p>在我们发现丢包以后我们需要重传，但是我们重传的方式也有两种方式可以选择分别是 <code>GBN</code> 和 <code>SR</code> 翻译过来就是 <code>拉回重传</code> 和 <code>选择重传</code> 。好其实我们已经能从名字上面看出来他们的作用方式了，拉回重传就是哪个地方没收到那么就从那个地方及以后的数据都重新传输，这个实现起来确实很简单，就是把发送窗口和接受窗口移回去，但是同样的我们发现这个方式不实用干了很多重复的事，效率低。</p>
<p>那么选择重传就是你想到的谁丢了，就传谁。不存在做无用功的情况。</p>
<font color="#A52A2A">
**结论:  TCP 实际上使用的是两者的结合，称为选择确认，也就是允许 TCP 接收方有选择的确认失序的报文段，而不是累计确认最后一个正确接受的有序报文段。也就是跳过重传那些已经正确接受的乱序报文段。**
</font>
### 2. 数据校验
![](http://ojrw3x8j2.bkt.clouddn.com/18-3-25/40689669.jpg)

<p>&emsp;&emsp;数据校验，其实这个比较简单就是头部的一个校验，然后进行数据校验的时候计算一遍 checkSum 比对一下。</p>
<h3 id="3-数据合理分片和排序"><a href="#3-数据合理分片和排序" class="headerlink" title="3. 数据合理分片和排序"></a>3. 数据合理分片和排序</h3><p>&emsp;&emsp;在 UDP 中，UDP 是直接把应用层的数据往对方的端口上 “扔” ，他基本没有任何的处理。所以说他发给网络层的数据如果大于1500字节,也就是大于MTU。这个时候发送方 IP 层就需要分片。把数据报分成若干片，使每一片都小于MTU.而接收方IP层则需要进行数据报的重组。这样就会多做许多事情,而更严重的是 ，由于UDP的特性,当某一片数据传送中丢失时 ， 接收方便无法重组数据报，将导致丢弃整个UDP数据报。</p>
<p>&emsp;&emsp;而在 TCP 中会按MTU合理分片，也就是在 TCP 中有一个概念叫做最大报文段长度（MSS）它规定了 TCP 的报文段的最大长度，注意这个不包括 TCP 的头，也就是他的典型值就是 1460 个字节（TCP 和 IP 的头各占用了 20 字节）。并且由于 TCP 是有序号和确认号的，接收方会缓存未按序到达的数据，根据序号重新排序报文段后再交给应用层。</p>
<h3 id="4-流量控制"><a href="#4-流量控制" class="headerlink" title="4. 流量控制"></a>4. 流量控制</h3><p>&emsp;&emsp;流量控制一般指的就是在接收方接受报文段的时候，应用层的上层程序可能在忙于做一些其他的事情，没有时间处理缓存中的数据，如果发送方在发送的时候不控制它的速度很有可能导致接受缓存溢出，导致数据丢失。</p>
<p>&emsp;&emsp;相对的还有一种情况是由于两台主机之间的网络比较拥塞，如果发送方还是以一个比较快的速度发送的话就可能导致大量的丢包，这个时候也需要发送方降低发送的速度。</p>
<p>&emsp;&emsp;虽然看起来上面的两种情况都是由于可能导致数据丢失而让发送主机降低发送速度，但是一定要把这两种情况分开，因为前者是属于<code>流量控制</code> 而后者是 <code>拥塞控制</code> ，那将是我们后面需要讨论的事情。不要把这两个概念混了。</p>
<p>&emsp;&emsp;其实说到流量控制我们就不得不提一下滑动窗口协议，这个是流量控制的基础。由于 TCP 连接是一个全双工的也就是在发送的时候也是可以接受的，所以在发送端和接收端同时维持了发送窗口和接收窗口。这里为了方便讨论我们就按照单方向来讨论。</p>
<p>&emsp;&emsp;接收方维持一个接受窗口，发送方一个发送窗口。发送的时候要知道接受窗口还有多少空间，也就是发送的数据量不能超过接受窗口的大小，否则就溢出了。而当我们收到一个接收方的 ACK 的时候我们就可以移动接受窗口把那些已经确认的数据滑动到窗口之外，发送窗口同理把确认的移出去。这样一直维持两个窗口大小，当接收方不能在接受数据的时候就把自己的窗口大小调整为 0 发送窗口就不会发送数据了。但是有一个问题，这个时候当接收窗口再调大的时候他不会主动通知发送方，这里采用的是发送方主动询问。</p>
<p>&emsp;&emsp;还是画个图看的比较直观：<br><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-25/32650197.jpg" alt></p>
<h3 id="5-拥塞控制"><a href="#5-拥塞控制" class="headerlink" title="5. 拥塞控制"></a>5. 拥塞控制</h3><p>&emsp;&emsp;拥塞控制一般都是由于网络中的主机发送的数据太多导致的拥塞，一般拥塞的都是一些负载比较高的路由，这时候为了获得更好的数据传输稳定性，我们必须采用拥塞控制，当然也为了减轻路由的负载防止崩溃。</p>
<p>&emsp;&emsp;这里主要介绍两个拥塞控制的方法，一个是慢开始，另外一个称为快恢复。</p>
<h4 id="1-慢开始"><a href="#1-慢开始" class="headerlink" title="1.慢开始"></a>1.慢开始</h4><ol>
<li>一开始我们不知道网络中的拥塞情况，我们就发一个数据包</li>
<li>如果没有发生拥塞我们成倍的增加发送的数据的数量。</li>
<li>当然我们也不能到无休止的增加，这里有一个慢开始门限，到达门限则加法增加，每次加一。</li>
<li>这时候如果遇到了拥塞，我们直接跳到第一步，也就是从头开始，并且把慢开始门限调整为拥塞时候的数据量的一半再次开始。</li>
</ol>
<h4 id="2-快恢复"><a href="#2-快恢复" class="headerlink" title="2.快恢复"></a>2.快恢复</h4><ol>
<li>一开始我们不知道网络中的拥塞情况，我们就发一个数据包</li>
<li>如果没有发生拥塞我们成倍的增加发送的数据的数量。</li>
<li>当然我们也不能到无休止的增加，这里有一个慢开始门限，到达门限则加法增加，每次加一。</li>
<li>这时候如果遇到了拥塞，这里就是唯一和慢开始不一样的地方，直接从新的慢开始门限加法增长。</li>
</ol>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-25/14662652.jpg" alt></p>
<h2 id="3-连接管理"><a href="#3-连接管理" class="headerlink" title="3.连接管理"></a>3.连接管理</h2><h3 id="1-建立连接3次握手"><a href="#1-建立连接3次握手" class="headerlink" title="1. 建立连接3次握手"></a>1. 建立连接3次握手</h3><ol>
<li><p>客户端像服务端发起连接，首先向服务端发送一个特殊的报文，这个报文的 SYN 位被置 1 ，然后生成一个随机的序号填入到 TCP 的头部。这个报文段称为 SYN 报文，用于请求连接。</p>
</li>
<li><p>服务器接收到客户端的 SYN 报文以后，也要生成一个特殊的报文段来允许客户端的接入，这个报文是设置一个自己的初始序号，SYN 设置为 1，ACK 设置为 SYN 报文序号加一。这个报文段称之为 SYNACK 报文。并且根据 SYN 报文的参数来分配本地变量，但是也是由于这么早的分配变量就有一种 SYN 洪泛攻击。注意一下，上面的这两个报文段都没有数据部分。</p>
</li>
<li><p>在客户端收到 SYNACK 报文段时候需要对客户端分配变量，然后对服务器的允许进行确认。这时候 SYN 位要置位 0 ，并且可以携带数据，也就是这时候是已经开始了数据传输的。</p>
</li>
</ol>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-25/3147427.jpg" alt></p>
<p>那么问题来了，为什么需要序号呢？为什么又是三次握手而不是两次？以及什么是 SYN 洪泛攻击？</p>
<ol>
<li><p>序号存在的目的是为了能否区分多个 TCP 连接，毕竟是一个服务器，多个客户端，不然各个 TCP 连接就会变得非常混乱。</p>
</li>
<li><p>其实我们单方向来看其实就是两次握手，之所以是三次握手是因为 TCP 是双工的，中间那次的 SYNACK 其实试一次合并。</p>
</li>
<li><p>SYN 洪泛攻击就是让客户端乱遭一些 IP 然后和服务器简历 TCP 连接，由于服务器收不到 ACK 但是他分配了变量，导致一直在消耗服务器资源。这个解决方法就是采用 SYNCookie 这个 Cookie 其实就是服务器在发送 SYNACK 的头部的 seq 序号的值，那么客户端必须返回一个比 Cookie 大一的 ACK 回来才是正确的，否则不分配变量，也就是变量延时分配。</p>
</li>
</ol>
<h3 id="2-释放链接四次挥手"><a href="#2-释放链接四次挥手" class="headerlink" title="2.释放链接四次挥手"></a>2.释放链接四次挥手</h3><ol>
<li>首先客户端发起终止会话的请求，FIN=1</li>
<li>服务器接收到后相应客户端 ACK=1</li>
<li>服务器发送完毕终止会话 FIN=1</li>
<li>客户端回应 ACK=1</li>
</ol>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/18-3-25/59663362.jpg" alt></p>
<p>这里需要说明一下的是最后的那个长长的 TIME_WAIT 状态一般是为了客户端能够发出 ACK 一般他的值是 1分钟 或者2分钟</p>
<h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4.总结"></a>4.总结</h2><p>&emsp;&emsp;好了，今天真的写了不少，主要就是把 TCP 的可靠传输以及连接管理讲清楚了，以及里面的一下细节问题，真的很花时间。然后其他没有涉及到的就是关于 TCP 的头并没有详细的去分析，这个东西其实也不是很难，但是现在篇幅真的已经很大就先这样，头里面的都是固定的不需要太多的理解。</p>
]]></content>
      <categories>
        <category>-计算机网络</category>
      </categories>
  </entry>
  <entry>
    <title>今天是个转折点</title>
    <url>/2018/03/04/Life/%E4%BB%8A%E5%A4%A9%E6%98%AF%E4%B8%AA%E8%BD%AC%E6%8A%98%E7%82%B9/</url>
    <content><![CDATA[<h1 id="今天是个转折点"><a href="#今天是个转折点" class="headerlink" title="今天是个转折点"></a>今天是个转折点</h1><p>今天是一个转折点，人生的转折点。</p>
<p>等待这天好久好久了，上高中到现在我好像没有任何改变，只是一个比较努力的傻子。慢慢的我的生活好像失去了那些本该有的颜色，开心，以及能够让我高兴半个月或者好久好久的事情了。</p>
<p>只是每个周惯例的给爸爸妈妈打电话，我想跟他们多聊聊可是话到了嘴边又咽下去了，好像不知道什么时候起突然就变得这么懂事，乖巧，大家眼里的好孩子。可是啊，我一点也不开心，找不到开心的事，就连收到那么久以来梦寐以求的阿里 offer 的时候我也非常平静，只是高兴了那么一会会，也就一会会。</p>
<p>今天发生了两件事，一个是我从阿里离职了，另外就是我跟父母说了我很想说的这些事。</p>
<p>我爸说 22 岁的我能达到目前的这个程度，我是他的骄傲！听到这句话的时候，怎么猛地眼泪就掉下来了，长时间积郁在胸口的悲伤，失望与痛苦瞬间随着眼泪涌了出来。我也明白了我告诉他们我睡不着的时候爸妈整晚的说我的事，为我想办法，也明白我是真的有多么幸运，一个爱你的人究竟能有多爱你。</p>
<p>所以什么才是对你真的用心的那种好，我想绝对不是那种看心情对你呼之即来挥之即去的爱。五点多的时候因为妈妈的关心，我大声说话时她有多么伤心，我说完她就没说话了，坐在那发呆，弟弟告诉她：“妈妈别担心，哥哥那么优秀怎么会找不到工作”，电话的尾声她还是不住的提醒爸爸关心我明天怎么坐车，有没有买药。电话意外挂断的时候，第一时间去打我的电话。从未觉得我的人生如此的幸运 ：）</p>
<p>另外一个就是从阿里离职，办完离职手续的那一刻其实心里空空的，我不确信我这辈子是否还有机会踏入阿里的大门，我也不清楚明天的路会是怎样的，只是我明白了我不会去选择那条别人眼中的完美的充满鲜花和掌声的路，我想我选择的会是那条能让自己开心，有色彩的路。</p>
<p>是啊，我可以无趣，但不可以不开心。</p>
]]></content>
      <categories>
        <category>生活</category>
      </categories>
      <tags>
        <tag>生活</tag>
      </tags>
  </entry>
  <entry>
    <title>6月8号</title>
    <url>/2018/03/04/Life/6%E6%9C%888%E5%8F%B7/</url>
    <content><![CDATA[<a id="more"></a> 

<h1 id="无论事情怎么糟糕，将来多么不确定，当下多么痛苦，保持一颗让自己有好心情的❤。"><a href="#无论事情怎么糟糕，将来多么不确定，当下多么痛苦，保持一颗让自己有好心情的❤。" class="headerlink" title="无论事情怎么糟糕，将来多么不确定，当下多么痛苦，保持一颗让自己有好心情的❤。"></a>无论事情怎么糟糕，将来多么不确定，当下多么痛苦，保持一颗让自己有好心情的❤。</h1>]]></content>
      <categories>
        <category>生活</category>
      </categories>
      <tags>
        <tag>生活</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉搜索树</title>
    <url>/2018/03/04/Algorithm/%E4%BA%8C%E5%8F%89%E6%90%9C%E7%B4%A2%E6%A0%91/</url>
    <content><![CDATA[<h3 id="一、操作："><a href="#一、操作：" class="headerlink" title="一、操作："></a>一、操作：</h3><ol>
<li>判断元素是否存在：递归的在左右子树中查找</li>
<li>查找最小元素：在左子树中递归或者循环</li>
<li>查找最大元素：在右子树中递归或循环</li>
<li>插入：递归的插入，大于则插入在节点的右子树，小于则左子树，等于则是重复节点不作处理</li>
<li>删除：递归删除( 或者说递归查找需要删除的元素 )，找到该元素后，如果元素有两个子节点那么久找到这个元素的右子树的最小元素代替要删除的元素，然后再删除那个右子树上的最小元素。如果只有一个子节点直接让要被删除的节点赋值上他的子节点。<a id="more"></a>
<h3 id="二、代码"><a href="#二、代码" class="headerlink" title="二、代码"></a>二、代码</h3></li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Tree;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> sun.tools.tree.ThisExpression;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Stack;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyBinaryTree</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BinaryNode</span> <span class="keyword">implements</span> <span class="title">Comparable</span>&lt;<span class="title">Integer</span>&gt; </span>&#123;</span><br><span class="line">        BinaryNode lChild;</span><br><span class="line">        BinaryNode rChild;</span><br><span class="line">        Integer ele;</span><br><span class="line"></span><br><span class="line">        BinaryNode(Integer ele) &#123;</span><br><span class="line">            <span class="keyword">this</span>(ele, <span class="keyword">null</span>, <span class="keyword">null</span>);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        BinaryNode(Integer ele, BinaryNode lChild, BinaryNode rChild) &#123;</span><br><span class="line">            <span class="keyword">this</span>.ele = ele;</span><br><span class="line">            <span class="keyword">this</span>.lChild = lChild;</span><br><span class="line">            <span class="keyword">this</span>.rChild = rChild;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareTo</span><span class="params">(Integer o)</span> </span>&#123;</span><br><span class="line">            <span class="keyword">return</span> ele - o;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> BinaryNode root;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">MyBinaryTree</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        root = <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">MyBinaryTree</span><span class="params">(Integer ele)</span> </span>&#123;</span><br><span class="line">        root = <span class="keyword">new</span> BinaryNode(ele, <span class="keyword">null</span>, <span class="keyword">null</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">makeEmpty</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.root = <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isEmpty</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> root == <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">contains</span><span class="params">(Integer node)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> contains(node, root);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">contains</span><span class="params">(Integer node, BinaryNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> result = root.ele.compareTo(node);</span><br><span class="line">        <span class="keyword">if</span> (result &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> contains(node, root.lChild);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (result &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> contains(node, root.rChild);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">findMin</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (isEmpty()) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> (Integer) findMin(root);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">findMin</span><span class="params">(BinaryNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (root.lChild == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> root.ele;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> findMin(root.lChild);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">findMax</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (isEmpty()) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> (Integer) findMax(root).ele;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> BinaryNode <span class="title">findMax</span><span class="params">(BinaryNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (root.rChild == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> root;</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            <span class="keyword">return</span> findMax(root.rChild);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insert</span><span class="params">(Integer x)</span> </span>&#123;</span><br><span class="line">        root = insert(x, root);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> BinaryNode <span class="title">insert</span><span class="params">(Integer ele, BinaryNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> BinaryNode(ele);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> result = root.compareTo(ele);</span><br><span class="line">        <span class="keyword">if</span> (result &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            root.rChild = insert(ele, root.rChild);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (result &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            root.lChild = insert(ele, root.lChild);</span><br><span class="line">        &#125; <span class="keyword">else</span> ;</span><br><span class="line">        <span class="keyword">return</span> root;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> BinaryNode <span class="title">remove</span><span class="params">(Integer ele)</span> </span>&#123;</span><br><span class="line">        root = remove(ele, root);</span><br><span class="line">        <span class="keyword">return</span> root;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> BinaryNode <span class="title">remove</span><span class="params">(Integer ele, BinaryNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> result = root.compareTo(ele);</span><br><span class="line">        <span class="keyword">if</span> (result &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            root.lChild = remove(ele, root.lChild);</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (result &lt; <span class="number">0</span>) &#123;</span><br><span class="line">            root.rChild = remove(ele, root.rChild);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//这个地方就是递归的结束条件  也就是当我们找到了要删除的节点，这时候要分情况如果两个子节点都不空我们需要把右子树的最小值的替换到当前节点然后删除右子树最小节点</span></span><br><span class="line">        <span class="comment">//当有一个或者0个节点的时候我们只需要把左边或者右边挂上去就行</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (root.lChild != <span class="keyword">null</span> &amp;&amp; root.rChild != <span class="keyword">null</span>) &#123;</span><br><span class="line">            root.ele = findMin(root.rChild);</span><br><span class="line">            root.rChild = remove(root.ele, root.rChild);</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            root = root.lChild == <span class="keyword">null</span> ? root.rChild : root.lChild;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> root;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">printTree</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        preOrder(root);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">preOrder</span><span class="params">(BinaryNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.print(root.ele + <span class="string">" "</span>);</span><br><span class="line">        <span class="keyword">if</span> (root.lChild != <span class="keyword">null</span>) &#123;</span><br><span class="line">            preOrder(root.lChild);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (root.rChild != <span class="keyword">null</span>) &#123;</span><br><span class="line">            preOrder(root.rChild);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">inOrder</span><span class="params">(BinaryNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (root.lChild != <span class="keyword">null</span>) &#123;</span><br><span class="line">            inOrder(root.lChild);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.print(root.ele + <span class="string">" "</span>);</span><br><span class="line">        <span class="keyword">if</span> (root.rChild != <span class="keyword">null</span>) &#123;</span><br><span class="line">            inOrder(root.rChild);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">subOrder</span><span class="params">(BinaryNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (root.lChild != <span class="keyword">null</span>) &#123;</span><br><span class="line">            inOrder(root.lChild);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (root.rChild != <span class="keyword">null</span>) &#123;</span><br><span class="line">            inOrder(root.rChild);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.print(root.ele + <span class="string">" "</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">preOrder_1</span><span class="params">(BinaryNode root)</span> </span>&#123;</span><br><span class="line">        Stack&lt;BinaryNode&gt; stack = <span class="keyword">new</span> Stack&lt;&gt;();</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">// stack.push(root);</span></span><br><span class="line">        BinaryNode p = root;</span><br><span class="line">        System.out.printf(p.ele + <span class="string">" "</span>);</span><br><span class="line">        <span class="keyword">while</span> (p != <span class="keyword">null</span> || !stack.isEmpty()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (p != <span class="keyword">null</span>) &#123;</span><br><span class="line">                System.out.printf(p.ele + <span class="string">" "</span>);</span><br><span class="line">                stack.push(p);</span><br><span class="line">                p = p.lChild;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                p = stack.pop();</span><br><span class="line">                p = p.rChild;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">inOrder_1</span><span class="params">(BinaryNode root)</span> </span>&#123;</span><br><span class="line">        BinaryNode p = root;</span><br><span class="line">        Stack&lt;BinaryNode&gt; stack = <span class="keyword">new</span> Stack&lt;&gt;();</span><br><span class="line">        <span class="keyword">while</span> (p != <span class="keyword">null</span> || !stack.isEmpty()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (p != <span class="keyword">null</span>) &#123;</span><br><span class="line">                stack.push(p);</span><br><span class="line">                p = p.lChild;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                p = stack.pop();</span><br><span class="line">                System.out.printf(p.ele + <span class="string">" "</span>);</span><br><span class="line">                p = p.rChild;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">subOrder_1</span><span class="params">(BinaryNode root)</span> </span>&#123;</span><br><span class="line">        Stack&lt;BinaryNode&gt; stack = <span class="keyword">new</span> Stack&lt;&gt;();</span><br><span class="line">        BinaryNode p = root;</span><br><span class="line">        BinaryNode q = <span class="keyword">null</span>;</span><br><span class="line">        <span class="keyword">while</span> (p != <span class="keyword">null</span> || !stack.isEmpty()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (p != <span class="keyword">null</span>) &#123;</span><br><span class="line">                stack.push(p);</span><br><span class="line">                p = p.lChild;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                p = stack.peek();</span><br><span class="line">                <span class="keyword">if</span> (p.rChild == <span class="keyword">null</span> || p.rChild == q) &#123;</span><br><span class="line">                    q = p;</span><br><span class="line">                    p = stack.pop();</span><br><span class="line">                    System.out.printf(p.ele + <span class="string">" "</span>);</span><br><span class="line">                    p = <span class="keyword">null</span>;</span><br><span class="line">                &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                    p = p.rChild;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> BinaryNode <span class="title">getRoot</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.root;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-数据结构</category>
      </categories>
      <tags>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>八大排序算法</title>
    <url>/2018/03/03/Algorithm/%E5%85%AB%E5%A4%A7%E6%8E%92%E5%BA%8F%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<p>​    八大排序算法是面试经常考到的，尤其是快排，希尔排序和归并也是经常会让写代码的题目，其实只要用一句话说明了他们的原理我们写起代码就没那么困难。</p>
<h3 id="冒泡排序"><a href="#冒泡排序" class="headerlink" title="冒泡排序"></a>冒泡排序</h3><h4 id="思想：有-n-个数我们就进行-n-1-趟排序，每一趟我们都选取最大的一个数放到已经排序的位置即可。"><a href="#思想：有-n-个数我们就进行-n-1-趟排序，每一趟我们都选取最大的一个数放到已经排序的位置即可。" class="headerlink" title="思想：有 n 个数我们就进行 n-1 趟排序，每一趟我们都选取最大的一个数放到已经排序的位置即可。"></a>思想：有 n 个数我们就进行 n-1 趟排序，每一趟我们都选取最大的一个数放到已经排序的位置即可。</h4><h4 id="伪代码：两个-For-循环，外层表示要进行的趟数，内层则是找出最大的数，找最大的数的方法就是比较、交换。"><a href="#伪代码：两个-For-循环，外层表示要进行的趟数，内层则是找出最大的数，找最大的数的方法就是比较、交换。" class="headerlink" title="伪代码：两个 For 循环，外层表示要进行的趟数，内层则是找出最大的数，找最大的数的方法就是比较、交换。"></a>伪代码：两个 For 循环，外层表示要进行的趟数，内层则是找出最大的数，找最大的数的方法就是比较、交换。</h4><a id="more"></a>

<h4 id="时间复杂度：O-n2"><a href="#时间复杂度：O-n2" class="headerlink" title="时间复杂度：O(n2)"></a>时间复杂度：O(n2)</h4><h4 id="空间复杂度：O-n"><a href="#空间复杂度：O-n" class="headerlink" title="空间复杂度：O(n)"></a>空间复杂度：O(n)</h4><h4 id="代码："><a href="#代码：" class="headerlink" title="代码："></a>代码：</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Sorting;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Bubble</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">(<span class="keyword">int</span>[] arr)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">1</span>;i&lt;arr.length-<span class="number">1</span>;++i) &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">0</span>;j&lt;arr.length-i;++j) &#123;</span><br><span class="line">                <span class="keyword">if</span> (arr[j] &gt; arr[j + <span class="number">1</span>]) &#123;</span><br><span class="line">                    swap(arr,j);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> j)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> temp = arr[j];</span><br><span class="line">        arr[j] = arr[j + <span class="number">1</span>];</span><br><span class="line">        arr[j + <span class="number">1</span>] = temp;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] arr = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">2</span>, <span class="number">1</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">3</span>, <span class="number">2</span>&#125;;</span><br><span class="line">        sort(arr);</span><br><span class="line">        System.out.println(Arrays.toString(arr));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="插入排序"><a href="#插入排序" class="headerlink" title="插入排序"></a>插入排序</h3><h4 id="思想：插入排序就是把元素分成两部分-一部分就是有序的，而另外一部分无序。我们每次在无序的集合里面找到一个元素插入到他在有序集合中对应的位置。因此总的来说他就是把一个数据插入到已经排好序的数据中。"><a href="#思想：插入排序就是把元素分成两部分-一部分就是有序的，而另外一部分无序。我们每次在无序的集合里面找到一个元素插入到他在有序集合中对应的位置。因此总的来说他就是把一个数据插入到已经排好序的数据中。" class="headerlink" title="思想：插入排序就是把元素分成两部分,一部分就是有序的，而另外一部分无序。我们每次在无序的集合里面找到一个元素插入到他在有序集合中对应的位置。因此总的来说他就是把一个数据插入到已经排好序的数据中。"></a>思想：插入排序就是把元素分成两部分,一部分就是有序的，而另外一部分无序。我们每次在无序的集合里面找到一个元素插入到他在有序集合中对应的位置。因此总的来说他就是把一个数据插入到已经排好序的数据中。</h4><p>####分类：实际上插入排序是一个类别，它里面有很多具体的排序方式。直接插入排序，折半插入排序，希尔排序 ( 又称缩小增量排序 )。他们都是属于稳定排序。</p>
<h3 id="直接插入排序"><a href="#直接插入排序" class="headerlink" title="直接插入排序"></a>直接插入排序</h3><h4 id="思想：把待排序的记录按其关键码值的大小逐个插入到一个已经排好序的有序序列中，直到所有的记录插入完为止，得到一个新的有序序列。形象的可以理解为打扑克抓拍的过程，通常我们右手抓牌，没抓一张牌，就放到左手，抓下一张牌后，会把这张牌依次与左手上的牌比较，并把它插入到一个合适的位置（按牌面大小）。"><a href="#思想：把待排序的记录按其关键码值的大小逐个插入到一个已经排好序的有序序列中，直到所有的记录插入完为止，得到一个新的有序序列。形象的可以理解为打扑克抓拍的过程，通常我们右手抓牌，没抓一张牌，就放到左手，抓下一张牌后，会把这张牌依次与左手上的牌比较，并把它插入到一个合适的位置（按牌面大小）。" class="headerlink" title="思想：把待排序的记录按其关键码值的大小逐个插入到一个已经排好序的有序序列中，直到所有的记录插入完为止，得到一个新的有序序列。形象的可以理解为打扑克抓拍的过程，通常我们右手抓牌，没抓一张牌，就放到左手，抓下一张牌后，会把这张牌依次与左手上的牌比较，并把它插入到一个合适的位置（按牌面大小）。"></a>思想：把待排序的记录按其关键码值的大小逐个插入到一个已经排好序的有序序列中，直到所有的记录插入完为止，得到一个新的有序序列。形象的可以理解为打扑克抓拍的过程，通常我们右手抓牌，没抓一张牌，就放到左手，抓下一张牌后，会把这张牌依次与左手上的牌比较，并把它插入到一个合适的位置（按牌面大小）。</h4><h4 id="应用优势：插入排序在对几乎已经排好序的数据操作时，效率高，即可以达到线性排序的效率。"><a href="#应用优势：插入排序在对几乎已经排好序的数据操作时，效率高，即可以达到线性排序的效率。" class="headerlink" title="应用优势：插入排序在对几乎已经排好序的数据操作时，效率高，即可以达到线性排序的效率。"></a>应用优势：插入排序在对几乎已经排好序的数据操作时，效率高，即可以达到线性排序的效率。</h4><h4 id="时间复杂度：O-n2-1"><a href="#时间复杂度：O-n2-1" class="headerlink" title="时间复杂度：O(n2)"></a>时间复杂度：O(n2)</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Sorting;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleInsert</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">(<span class="keyword">int</span>[] arr)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; arr.length; i++) &#123;</span><br><span class="line">            <span class="keyword">int</span> temp = arr[i];</span><br><span class="line">            <span class="keyword">int</span> j = i - <span class="number">1</span>;</span><br><span class="line">            <span class="keyword">while</span> (j &gt; -<span class="number">1</span> &amp;&amp; temp &lt; arr[j]) &#123;</span><br><span class="line">                arr[j + <span class="number">1</span>] = arr[j];</span><br><span class="line">                --j;</span><br><span class="line">            &#125;</span><br><span class="line">            arr[j + <span class="number">1</span>] = temp;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] arr = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">5</span>, <span class="number">3</span>, <span class="number">2</span>, <span class="number">6</span>, <span class="number">8</span>, <span class="number">1</span>, <span class="number">3</span>, <span class="number">0</span>&#125;;</span><br><span class="line">        sort(arr);</span><br><span class="line">        System.out.println(Arrays.toString(arr));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="二分插入排序"><a href="#二分插入排序" class="headerlink" title="二分插入排序"></a>二分插入排序</h3><h4 id="思想：也就是和上面的简单插入排序一样，只是简单插入排序做了很多的判断，元素到底应该插入哪个位置，而这里我们是使用这般查找来确定位置，因为那一部分是有序的。"><a href="#思想：也就是和上面的简单插入排序一样，只是简单插入排序做了很多的判断，元素到底应该插入哪个位置，而这里我们是使用这般查找来确定位置，因为那一部分是有序的。" class="headerlink" title="思想：也就是和上面的简单插入排序一样，只是简单插入排序做了很多的判断，元素到底应该插入哪个位置，而这里我们是使用这般查找来确定位置，因为那一部分是有序的。"></a>思想：也就是和上面的简单插入排序一样，只是简单插入排序做了很多的判断，元素到底应该插入哪个位置，而这里我们是使用这般查找来确定位置，因为那一部分是有序的。</h4><h4 id="时间复杂度：n2"><a href="#时间复杂度：n2" class="headerlink" title="时间复杂度：n2"></a>时间复杂度：n2</h4><h3 id="希尔排序"><a href="#希尔排序" class="headerlink" title="希尔排序"></a>希尔排序</h3><h4 id="思想：希尔排序的基本思想就是设置一个步长，首先步长是整个元素的长度的一半，这样我们每一个分组只有两个元素在这个分组中我们进行排序，然后我们减小步长也就是再次除以二，继续上面的步骤，最后我们会得到整个元素的集合成为一个分组。"><a href="#思想：希尔排序的基本思想就是设置一个步长，首先步长是整个元素的长度的一半，这样我们每一个分组只有两个元素在这个分组中我们进行排序，然后我们减小步长也就是再次除以二，继续上面的步骤，最后我们会得到整个元素的集合成为一个分组。" class="headerlink" title="思想：希尔排序的基本思想就是设置一个步长，首先步长是整个元素的长度的一半，这样我们每一个分组只有两个元素在这个分组中我们进行排序，然后我们减小步长也就是再次除以二，继续上面的步骤，最后我们会得到整个元素的集合成为一个分组。"></a>思想：希尔排序的基本思想就是设置一个步长，首先步长是整个元素的长度的一半，这样我们每一个分组只有两个元素在这个分组中我们进行排序，然后我们减小步长也就是再次除以二，继续上面的步骤，最后我们会得到整个元素的集合成为一个分组。</h4><h4 id="稳定性：不稳定"><a href="#稳定性：不稳定" class="headerlink" title="稳定性：不稳定"></a>稳定性：不稳定</h4><h4 id="时间复杂度：n-logn"><a href="#时间复杂度：n-logn" class="headerlink" title="时间复杂度：n(logn)"></a>时间复杂度：n(logn)</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Sorting;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Shell</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">(<span class="keyword">int</span>[] arr)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> step = arr.length / <span class="number">2</span>;</span><br><span class="line">        <span class="comment">//这是循环每一个步长</span></span><br><span class="line">        <span class="keyword">while</span> (step &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="comment">//这是循环每一个分组的第一个元素的下标</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt;= step; i++) &#123;</span><br><span class="line">                <span class="comment">//这是一层简单插入排序</span></span><br><span class="line">                <span class="keyword">for</span> (<span class="keyword">int</span> ptr = i; ptr + step &lt; arr.length; ptr += step) &#123;</span><br><span class="line">                    <span class="keyword">int</span> temp = arr[ptr + step];</span><br><span class="line">                    <span class="keyword">int</span> j = ptr;</span><br><span class="line">                    <span class="keyword">while</span> (j &gt;= <span class="number">0</span> &amp;&amp; arr[j] &gt; temp) &#123;</span><br><span class="line">                        arr[j + step] = arr[j];</span><br><span class="line">                        j -= step;</span><br><span class="line">                    &#125;</span><br><span class="line">                    arr[j+step] = temp;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            step /= <span class="number">2</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] arr = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">5</span>, <span class="number">3</span>, <span class="number">2</span>, <span class="number">6</span>, <span class="number">8</span>, <span class="number">1</span>, <span class="number">3</span>, <span class="number">0</span>&#125;;</span><br><span class="line">        sort(arr);</span><br><span class="line">        System.out.println(Arrays.toString(arr));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="简单选择排序"><a href="#简单选择排序" class="headerlink" title="简单选择排序"></a>简单选择排序</h3><h4 id="思想：始终选取最小的元素往前面的有序序列中插入"><a href="#思想：始终选取最小的元素往前面的有序序列中插入" class="headerlink" title="思想：始终选取最小的元素往前面的有序序列中插入"></a>思想：始终选取最小的元素往前面的有序序列中插入</h4><h4 id="稳定性：不稳定-1"><a href="#稳定性：不稳定-1" class="headerlink" title="稳定性：不稳定"></a>稳定性：不稳定</h4><h4 id="时间复杂度：O-n2-2"><a href="#时间复杂度：O-n2-2" class="headerlink" title="时间复杂度：O(n2)"></a>时间复杂度：O(n2)</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Sorting;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SimpleSorting</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">sort</span><span class="params">(<span class="keyword">int</span>[] arr)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> min;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; arr.length; i++) &#123;</span><br><span class="line">            min = minIndex(arr, i);</span><br><span class="line">            <span class="keyword">int</span> temp = arr[min];</span><br><span class="line">            arr[min] = arr[i];</span><br><span class="line">            arr[i] = temp;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">minIndex</span><span class="params">(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> start)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> min = start;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = start; i &lt; arr.length; i++) &#123;</span><br><span class="line">            min = arr[i] &lt; arr[min] ? i : min;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> min;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] arr = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">6</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">51</span>, <span class="number">32</span>, <span class="number">52</span>, <span class="number">33</span>&#125;;</span><br><span class="line">        sort(arr);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> ele : arr) &#123;</span><br><span class="line">            System.out.printf(ele + <span class="string">" "</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h3><h4 id="思想：寻找一个枢轴元素，然后我们把枢轴元素归位，也就是比他小的放在他的左边比他大的放右边，然后继续在他的左边右边选取枢轴元素继续如此进行。"><a href="#思想：寻找一个枢轴元素，然后我们把枢轴元素归位，也就是比他小的放在他的左边比他大的放右边，然后继续在他的左边右边选取枢轴元素继续如此进行。" class="headerlink" title="思想：寻找一个枢轴元素，然后我们把枢轴元素归位，也就是比他小的放在他的左边比他大的放右边，然后继续在他的左边右边选取枢轴元素继续如此进行。"></a>思想：寻找一个枢轴元素，然后我们把枢轴元素归位，也就是比他小的放在他的左边比他大的放右边，然后继续在他的左边右边选取枢轴元素继续如此进行。</h4><h4 id="时间复杂度："><a href="#时间复杂度：" class="headerlink" title="时间复杂度："></a>时间复杂度：</h4><h4 id="稳定性：这就是在我们输入数据基本有序甚至完全有序的时候，这算法退化为冒泡排序，不再是O-n㏒n-，而是O-n-2-了。"><a href="#稳定性：这就是在我们输入数据基本有序甚至完全有序的时候，这算法退化为冒泡排序，不再是O-n㏒n-，而是O-n-2-了。" class="headerlink" title="稳定性：这就是在我们输入数据基本有序甚至完全有序的时候，这算法退化为冒泡排序，不再是O(n㏒n)，而是O(n^2)了。"></a>稳定性：这就是在我们输入数据基本有序甚至完全有序的时候，这算法退化为冒泡排序，不再是O(n㏒n)，而是O(n^2)了。</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Sorting;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">QuickSorting</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">sort</span><span class="params">(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> start, <span class="keyword">int</span> end)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (start &gt;= end) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> center = getMid(arr, start, end);</span><br><span class="line">        sort(arr, start, center - <span class="number">1</span>);</span><br><span class="line">        sort(arr, center + <span class="number">1</span>, end);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">sort</span><span class="params">(<span class="keyword">int</span>[] arr)</span> </span>&#123;</span><br><span class="line">        sort(arr, <span class="number">0</span>, arr.length - <span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">getMid</span><span class="params">(<span class="keyword">int</span>[] arr, <span class="keyword">int</span> start, <span class="keyword">int</span> end)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> temp = arr[start];</span><br><span class="line">        <span class="keyword">while</span> (start &lt; end) &#123;</span><br><span class="line">            <span class="keyword">while</span> (start &lt; end &amp;&amp; arr[end] &gt;= temp) &#123;</span><br><span class="line">                end--;</span><br><span class="line">            &#125;</span><br><span class="line">            arr[start] = arr[end];</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">            <span class="keyword">while</span> (start &lt; end &amp;&amp; arr[start] &lt;= temp) &#123;</span><br><span class="line">                start++;</span><br><span class="line">            &#125;</span><br><span class="line">            arr[end] = arr[start];</span><br><span class="line">        &#125;</span><br><span class="line">        arr[start] = temp;</span><br><span class="line">        <span class="keyword">return</span> start;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] arr = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">5</span>, <span class="number">3</span>, <span class="number">2</span>, <span class="number">6</span>, <span class="number">8</span>, <span class="number">1</span>, <span class="number">3</span>, <span class="number">0</span>&#125;;</span><br><span class="line">        sort(arr);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> ele : arr) &#123;</span><br><span class="line">            System.out.printf(ele + <span class="string">" "</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="归并排序"><a href="#归并排序" class="headerlink" title="归并排序"></a>归并排序</h3><h4 id="思想：就是先把元素分成两部分，然后对这两部分继续排序，最后进行合并这两部分。这也是一个重要的分治思想归并法。"><a href="#思想：就是先把元素分成两部分，然后对这两部分继续排序，最后进行合并这两部分。这也是一个重要的分治思想归并法。" class="headerlink" title="思想：就是先把元素分成两部分，然后对这两部分继续排序，最后进行合并这两部分。这也是一个重要的分治思想归并法。"></a>思想：就是先把元素分成两部分，然后对这两部分继续排序，最后进行合并这两部分。这也是一个重要的分治思想归并法。</h4><h4 id="分治思想：经典的分治（divide-and-conquer）策略（分治法将问题分-divide-成一些小的问题然后递归求解，而治-conquer-的阶段则将分的阶段得到的各答案”修补”在一起，即分而治之-。"><a href="#分治思想：经典的分治（divide-and-conquer）策略（分治法将问题分-divide-成一些小的问题然后递归求解，而治-conquer-的阶段则将分的阶段得到的各答案”修补”在一起，即分而治之-。" class="headerlink" title="分治思想：经典的分治（divide-and-conquer）策略（分治法将问题分(divide)成一些小的问题然后递归求解，而治(conquer)的阶段则将分的阶段得到的各答案”修补”在一起，即分而治之)。"></a>分治思想：经典的<strong>分治</strong>（divide-and-conquer）策略（分治法将问题<strong>分</strong>(divide)成一些小的问题然后递归求解，而<strong>治(conquer)</strong>的阶段则将分的阶段得到的各答案”修补”在一起，即分而治之)。</h4><h4 id="时间复杂度：O-nlogn"><a href="#时间复杂度：O-nlogn" class="headerlink" title="时间复杂度：O(nlogn)"></a>时间复杂度：O(nlogn)</h4><h4 id="空间复杂度：O-n-1"><a href="#空间复杂度：O-n-1" class="headerlink" title="空间复杂度：O(n)"></a>空间复杂度：O(n)</h4><h4 id="稳定性：稳定"><a href="#稳定性：稳定" class="headerlink" title="稳定性：稳定"></a>稳定性：稳定</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Sorting;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Merge</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">(<span class="keyword">int</span> []arr)</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> []temp = <span class="keyword">new</span> <span class="keyword">int</span>[arr.length];<span class="comment">//在排序前，先建好一个长度等于原数组长度的临时数组，避免递归中频繁开辟空间</span></span><br><span class="line">        sort(arr,<span class="number">0</span>,arr.length-<span class="number">1</span>,temp);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">(<span class="keyword">int</span>[] arr,<span class="keyword">int</span> left,<span class="keyword">int</span> right,<span class="keyword">int</span> []temp)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span>(left&lt;right)&#123;</span><br><span class="line">            <span class="keyword">int</span> mid = (left+right)/<span class="number">2</span>;</span><br><span class="line">            sort(arr,left,mid,temp);<span class="comment">//左边归并排序，使得左子序列有序</span></span><br><span class="line">            sort(arr,mid+<span class="number">1</span>,right,temp);<span class="comment">//右边归并排序，使得右子序列有序</span></span><br><span class="line">            merge(arr,left,mid,right,temp);<span class="comment">//将两个有序子数组合并操作</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">merge</span><span class="params">(<span class="keyword">int</span>[] arr,<span class="keyword">int</span> left,<span class="keyword">int</span> mid,<span class="keyword">int</span> right,<span class="keyword">int</span>[] temp)</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> i = left;<span class="comment">//左序列指针</span></span><br><span class="line">        <span class="keyword">int</span> j = mid+<span class="number">1</span>;<span class="comment">//右序列指针</span></span><br><span class="line">        <span class="keyword">int</span> t = <span class="number">0</span>;<span class="comment">//临时数组指针</span></span><br><span class="line">        <span class="keyword">while</span> (i&lt;=mid &amp;&amp; j&lt;=right)&#123;</span><br><span class="line">            <span class="keyword">if</span>(arr[i]&lt;=arr[j])&#123;</span><br><span class="line">                temp[t++] = arr[i++];</span><br><span class="line">            &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">                temp[t++] = arr[j++];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span>(i&lt;=mid)&#123;<span class="comment">//将左边剩余元素填充进temp中</span></span><br><span class="line">            temp[t++] = arr[i++];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span>(j&lt;=right)&#123;<span class="comment">//将右序列剩余元素填充进temp中</span></span><br><span class="line">            temp[t++] = arr[j++];</span><br><span class="line">        &#125;</span><br><span class="line">        t = <span class="number">0</span>;</span><br><span class="line">        <span class="comment">//将temp中的元素全部拷贝到原数组中</span></span><br><span class="line">        <span class="keyword">while</span>(left &lt;= right)&#123;</span><br><span class="line">            arr[left++] = temp[t++];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] arr = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">5</span>, <span class="number">3</span>, <span class="number">2</span>, <span class="number">6</span>, <span class="number">8</span>, <span class="number">1</span>, <span class="number">3</span>, <span class="number">0</span>, <span class="number">9</span>&#125;;</span><br><span class="line">        <span class="keyword">int</span>[] temp = <span class="keyword">new</span> <span class="keyword">int</span>[arr.length];</span><br><span class="line">        sort(arr, <span class="number">0</span>, arr.length - <span class="number">1</span>, temp);</span><br><span class="line">        System.out.println(Arrays.toString(arr));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="堆排序"><a href="#堆排序" class="headerlink" title="堆排序"></a>堆排序</h3><h4 id="思想：将待排序序列构造成一个大顶堆，此时，整个序列的最大值就是堆顶的根节点。将其与末尾元素进行交换，此时末尾就为最大值。然后将剩余n-1个元素重新构造成一个堆，这样会得到n个元素的次小值。如此反复执行，便能得到一个有序序列了"><a href="#思想：将待排序序列构造成一个大顶堆，此时，整个序列的最大值就是堆顶的根节点。将其与末尾元素进行交换，此时末尾就为最大值。然后将剩余n-1个元素重新构造成一个堆，这样会得到n个元素的次小值。如此反复执行，便能得到一个有序序列了" class="headerlink" title="思想：将待排序序列构造成一个大顶堆，此时，整个序列的最大值就是堆顶的根节点。将其与末尾元素进行交换，此时末尾就为最大值。然后将剩余n-1个元素重新构造成一个堆，这样会得到n个元素的次小值。如此反复执行，便能得到一个有序序列了"></a>思想：将待排序序列构造成一个大顶堆，此时，整个序列的最大值就是堆顶的根节点。将其与末尾元素进行交换，此时末尾就为最大值。然后将剩余n-1个元素重新构造成一个堆，这样会得到n个元素的次小值。如此反复执行，便能得到一个有序序列了</h4><h4 id="稳定性：不稳定-2"><a href="#稳定性：不稳定-2" class="headerlink" title="稳定性：不稳定"></a>稳定性：不稳定</h4><h4 id="时间复杂度：O-nlogn-1"><a href="#时间复杂度：O-nlogn-1" class="headerlink" title="时间复杂度：O(nlogn)"></a>时间复杂度：O(nlogn)</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Sorting;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HeapSort</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">(<span class="keyword">int</span> []arr)</span></span>&#123;</span><br><span class="line">        <span class="comment">//1.构建大顶堆</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=arr.length/<span class="number">2</span>-<span class="number">1</span>;i&gt;=<span class="number">0</span>;i--)&#123;</span><br><span class="line">            <span class="comment">//从第一个非叶子结点从下至上，从右至左调整结构</span></span><br><span class="line">            swim(arr,i,arr.length);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//2.调整堆结构+交换堆顶元素与末尾元素</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j=arr.length-<span class="number">1</span>;j&gt;<span class="number">0</span>;j--)&#123;</span><br><span class="line">            swap(arr,<span class="number">0</span>,j);<span class="comment">//将堆顶元素与末尾元素进行交换</span></span><br><span class="line">            swim(arr,<span class="number">0</span>,j);<span class="comment">//重新对堆进行调整</span></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 调整大顶堆（仅是调整过程，建立在大顶堆已构建的基础上）</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> arr</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> i</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> length</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">swim</span><span class="params">(<span class="keyword">int</span> []arr,<span class="keyword">int</span> i,<span class="keyword">int</span> length)</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> temp = arr[i];<span class="comment">//先取出当前元素i</span></span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> k=i*<span class="number">2</span>+<span class="number">1</span>;k&lt;length;k=k*<span class="number">2</span>+<span class="number">1</span>)&#123;<span class="comment">//从i结点的左子结点开始，也就是2i+1处开始</span></span><br><span class="line">            <span class="keyword">if</span>(k+<span class="number">1</span>&lt;length &amp;&amp; arr[k]&lt;arr[k+<span class="number">1</span>])&#123;<span class="comment">//如果左子结点小于右子结点，k指向右子结点</span></span><br><span class="line">                k++;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span>(arr[k] &gt;temp)&#123;<span class="comment">//如果子节点大于父节点，将子节点值赋给父节点（不用进行交换）</span></span><br><span class="line">                arr[i] = arr[k];</span><br><span class="line">                i = k;</span><br><span class="line">            &#125;<span class="keyword">else</span>&#123;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        arr[i] = temp;<span class="comment">//将temp值放到最终的位置</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 交换元素</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> arr</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> a</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> b</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">swap</span><span class="params">(<span class="keyword">int</span> []arr,<span class="keyword">int</span> a ,<span class="keyword">int</span> b)</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> temp=arr[a];</span><br><span class="line">        arr[a] = arr[b];</span><br><span class="line">        arr[b] = temp;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] arr = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">5</span>, <span class="number">3</span>, <span class="number">2</span>, <span class="number">6</span>, <span class="number">8</span>, <span class="number">1</span>, <span class="number">3</span>, <span class="number">0</span>&#125;;</span><br><span class="line">        sort(arr);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> ele : arr) &#123;</span><br><span class="line">            System.out.printf(ele + <span class="string">" "</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><h4 id="稳定性：堆排序、快速排序、希尔排序、直接选择排序-不是稳定的排序算法，而基数排序、冒泡排序、直接插入排序、折半插入排序、归并排序是稳定的排序算法。"><a href="#稳定性：堆排序、快速排序、希尔排序、直接选择排序-不是稳定的排序算法，而基数排序、冒泡排序、直接插入排序、折半插入排序、归并排序是稳定的排序算法。" class="headerlink" title="稳定性：堆排序、快速排序、希尔排序、直接选择排序 不是稳定的排序算法，而基数排序、冒泡排序、直接插入排序、折半插入排序、归并排序是稳定的排序算法。"></a>稳定性：堆排序、快速排序、希尔排序、直接选择排序 不是稳定的排序算法，而基数排序、冒泡排序、直接插入排序、折半插入排序、归并排序是稳定的排序算法。</h4><h4 id="应用场景："><a href="#应用场景：" class="headerlink" title="应用场景："></a>应用场景：</h4><p>(1)若n较小(如n≤50)，可采用直接插入或直接选择排序。<br>　当记录规模较小时，直接插入排序较好；否则因为直接选择移动的记录数少于直接插人，应选直接选择排序为宜。<br>(2)若文件初始状态基本有序(指正序)，则应选用直接插人、冒泡或随机的快速排序为宜；<br>(3)若n较大，则应采用时间复杂度为O(nlgn)的排序方法：快速排序、堆排序或归并排序。<br>　快速排序是目前基于比较的内部排序中被认为是最好的方法，当待排序的关键字是随机分布时，快速排序的平均时间最短；<br>　堆排序所需的辅助空间少于快速排序，并且不会出现快速排序可能出现的最坏情况。这两种排序都是不稳定的。<br>　若要求排序稳定，则可选用归并排序。但前面介绍的从单个记录起进行两两归并的排序算法并不值得提倡，通常可以将它和直接插入排序结合在一起使用。先利用直接插入排序求得较长的有序子序列，然后再两两归并之。因为直接插入排序是稳定 的，所以改进后的归并排序仍是稳定的。</p>
<h4 id="复杂度："><a href="#复杂度：" class="headerlink" title="复杂度："></a>复杂度：</h4><p><img src="/images/heap06-1.png" alt="heap06"><br><img src="/images/summer_sort-2.png" alt="summer_sort"></p>
]]></content>
      <categories>
        <category>-数据结构</category>
      </categories>
      <tags>
        <tag>算法</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构Generic</title>
    <url>/2018/03/02/Algorithm/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84Generic/</url>
    <content><![CDATA[<p>​    接下来我们要处理的是前面实现里另一个 根本性的缺陷 那些实现只适用于字符串，想要实现其他类型数据的队列和栈怎么办呢？ 这个问题就涉及泛型的话题了。<a id="more"></a></p>
<p>​    有一个广泛采用的捷径是 使用强制类型转换对不同的数据类型重用代码 我们对Object类实现数据结构，Java中所有的类都是Object的 子类，当客户端使用时，就将结果转换为 对应的类型。这个我不想花很多时间来讲 因为我认为这样的解决方案不能令人满意。</p>
<p>​    第二种方法是用的是泛型 这种方法中客户端程序不需要强制类型转换。在编译时就能 发现类型不匹配的错误，而不是在运行时。优秀的模块化编程的指导原则就是我们应当欢迎编译时错误，避免运行时错误。因为如果我们能在编译时 检测到错误，我们给客户交付产品或者部署对一个API的实现时 有把握对于任何客户都是没问题的，然而 直到运行时才会出现的错误可能在某些客户的开发中几年之后出现。</p>
<p>​    基于数组的实现，这种方法不管用。目前很多编程语言 这方面都有问题，而对Java尤其是个难题 我们想做的是用泛型名称item直接声明一个新的数组， 不幸的是，Java不允许创建泛型数组。对于这个问题有各种 技术方面的原因。这里，要行得通我们需要 加入强制类型转换。我们创建Object数组，然后将类型转换为 item数组。我的观点是优秀的代码应该不用强制类型转换。要 尽量避免强制类型转换因为它确实在我们的实现中 留下隐患。但这个情况中我们必须加入这个强制类型转换 我们听到过的教导是蹩脚的强制类型转换让你看你的代码不爽 这样的想法不仅仅你一个人有 我认为像这么简单的代码强制类型转换是讨厌的特性。当我们编译这个程序的 时候，Java会发出警告信息说我们在使用未经检查 或者不安全的操作，详细信息需要使用-Xlint=unchecked参数 重新编译。我们加上这个参数重新编译之后显示 你在代码中加入了一个未经检查的强制类型转换，对此发出 警告，你不应该加入未经检查的强制类型转换。好吧，当你 编译这样的代码的时候看到这个警告信息没事。</p>
<p>​    接下来，是个跟Java有关的 细节问题，关于基本类型。我们用的泛型类型是针对 Object及其子类的。前面讲过，是从Object数组强制类型转换 来的。为了处理基本类型，我们需要使用Java的包装对象类型 如大写的Integer是整型的包装类型等等。</p>
]]></content>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构Queue</title>
    <url>/2018/03/02/Algorithm/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84Queue/</url>
    <content><![CDATA[<p>​    栈和队列其实是相同的，只是名字不一样 入栈换成了入队（enqueue），出栈换成了出队（dequeue）。语义 是不同的。入队操作向队尾添加元素，而出队操作从 队首移除元素。<a id="more"></a></p>
<p>​    现在，队列的链表表示中 我们需要维护两个指针引用。一个是链表中的第一个 元素，另一个是链表最后一个元素。插入的时候我们在 链表末端添加元素，而不是在链表头。移除元素的时候 不变，依然从链表头取出元素。那么这就是出队操作的实现 和栈的出栈操作的代码是一样的。保存元素，前进指针 指向下一个节点，这样就删除了第一个节点，然后返回该元素。一模一样 添加节点，或者入队操作时，向链表添加新节点。我们要把它放在链表末端 这样它就是最后一个出队的元素。首先 要做的是保存指向最后一个节点的指针，因为我们需要 将它指向下一个节点的指针从null变为新的节点。然后给 链表末端创建新的节点并对其属性赋值，将旧的指针 从null变为指向新节点。</p>
<p>​    那么用数组实现呢？用可调大小的数组实现并不难 。我们维护两个指针，分别指向队列中的 第一个元素和队尾，即下一个元素要加入的地方 那么对于入队操作在tail指向的地方加入新元素，出队操作移除 head指向的元素。棘手的地方是一旦指针的位置超过了数组 的容量，必须重置指针回到0，这里需要多写一些代码 而且和栈一样实现数据结构的时候你需要加上调整容量的方法 。下面给出完整的实现。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Queue;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 同 Stack 我们遇到的问题就是调整队列大小的问题，以及处理碎片空间的问题</span></span><br><span class="line"><span class="comment"> * 调整大小我们依然使用倍增法，碎片空间就涉及到了循环队列的处理方法</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">QueueArray</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] arr;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> head = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> tail = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">QueueArray</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        arr = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">20</span>];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">resize</span><span class="params">(<span class="keyword">int</span> size)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] temp = <span class="keyword">new</span> <span class="keyword">int</span>[size];</span><br><span class="line">        System.arraycopy(arr, <span class="number">0</span>, temp, <span class="number">0</span>, arr.length);</span><br><span class="line">        arr = temp;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isEmpty</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> head == tail;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">enqueue</span><span class="params">(<span class="keyword">int</span> num)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (isFull()) &#123;</span><br><span class="line">            resize(arr.length * <span class="number">2</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        tail = (++tail) % arr.length;</span><br><span class="line">        arr[tail] = num;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">dequeue</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> arr[++head % arr.length];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isFull</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> (tail + <span class="number">1</span>) % arr.length == head;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>数据结构Stack</title>
    <url>/2018/03/02/Algorithm/%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84Stack/</url>
    <content><![CDATA[<p>​    在很多应用中，我们需要维护多个对象的集合，这种操作非常简单。我们可能想要向集合中 加入某个元素，去掉某个元素，以及遍历 集合中的元素并对他们执行某种操作，当然还有 检查集合是否为空。对于大多数操作来说，目的都很明确 关键是当需要去掉一个元素时，去掉哪一个元素呢？处理这类问题 有两个经典基础数据结构，栈和队列。<a id="more"></a></p>
<p>​    它们的区别就在于 去除元素的选择方式。在栈中，我们取出 最近加入的元素。插入元素对应的术语是入栈（push） 去掉最近加入的元素叫做出栈（pop）。这也叫做后进先出原则 ( LIFO )。在队列中，我们关注最先加入队列的元素 为了和栈的操作区分，队列加入元素的操作叫做入队（enqueue） 去除元素的操作叫做出队（dequeue）。这也叫做先入先出原则 (FIFO) </p>
<p>​    如何实现这些操作 ,我们今天隐含的主题是模块式编程。这也将是我们需要特别遵守的原则,这一原则主要思想是将接口与实现完全分离,比如我们精确定义了一些如栈、队列等数据结构的时候和数据类型，我们想要的是实现的细节与客户端的 完全分离。客户端可以选择不同的实现 但是客户端代码只能执行基本操作 另一方面，实现部分无法知道客户端需求的细节 它所要做的只是实现这些操作 这样，很多客户端能够共用同一个实现 这使得我们能够用模块式可复用的算法与数据结构库 来构建更复杂的算法和数据结构。在 Java 这门语言中就是使用接口俩统一 API，我们的所有实现必须遵从我们先前的 API。</p>
<p>​    现在，我们来看实现栈的代码 我们要看的第一个实现使用链表。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Stack;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StacksLink</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span></span>&#123;</span><br><span class="line">        String item;</span><br><span class="line">        Node next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> Node first = <span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isEmpty</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> first == <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">push</span><span class="params">(String item)</span> </span>&#123;</span><br><span class="line">        Node oldFirst = first;</span><br><span class="line">        first = <span class="keyword">new</span> Node();</span><br><span class="line">        first.item = item;</span><br><span class="line">        first.next = oldFirst;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">pop</span><span class="params">()</span></span>&#123;</span><br><span class="line">        Node temp = first;</span><br><span class="line">        first = first.next;</span><br><span class="line">        <span class="keyword">return</span> temp.item;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    好，我们来看代码 这门课中所有的链式数据结构中 我们使用Java中内部类来实现，这只是 描述我们要操作的节点对象的一种方法 节点对象由一个字符串和指向另一个节点的引用组成。</p>
<p>​    所以，链表的 出栈操作非常容易实现。首先，我们需要 返回链表中第一个元素，所以先将它存在变量item中 然后，要去掉第一个节点，我们只需要将 链表指向第一个元素的指针指向下一个元素 然后第一个节点就等着被垃圾回收处理 最后，返回保存的元素。</p>
<p>​    入栈操作呢？ 我们要在链表头加入新的节点 首先，将指向链表头的指针存起来，oldfirst = first 然后创建新节点，这是我们要插入链表头 的新节点。first = new Node() 这是个实例变量，它的元素就是我们想要插入链表头 的字符串，这个例子中是“not”，它的next指针指向链表oldfirst元素 现在成了链表第二个元素。在这个操作之后 first指向链表头处的元素，链表中的元素依照 入栈时间降序排列。实现入栈操作 只需要四行代码。</p>
<p>​    这个类中构造函数不做任何操作 也就不用写构造函数。内部类用来构成链表中的元素 将它写成了内部类，这样我们能够直接访问这些实例变量 栈唯一的实例变量是 链表中第一个节点的引用。</p>
<p>​    现在，我们分析实现的性能 这样我们就能提供给客户算法数据结构的 性能信息。这个例子中，很容易就能看出每个操作 最坏情况下只需要常数时间。每个操作没有循环，这显然是我们很想要的特性。那么空间需求呢？这和机器具体 实现有关。这是个典型Java实现，每个对象会有16字节的 额外空间，因为有内部类，所以还有8字节的额外空间 在类Node中有两个引用 一个指向字符串，另一个指向Node类 各需要8字节，每个栈节点需要40字节，如果栈大小为N 需要大约40N字节。</p>
<p>​    另一种实现栈的自然的方式是使用数组来储存栈上的元素。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Stack;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StackArray</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> top;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] arr;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">StackArray</span><span class="params">(<span class="keyword">int</span> size)</span> </span>&#123;</span><br><span class="line">        arr = <span class="keyword">new</span> <span class="keyword">int</span>[size];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isEmpty</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> top == <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">push</span><span class="params">(<span class="keyword">int</span> num)</span> </span>&#123;</span><br><span class="line">        arr[top++] = num;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">pop</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> arr[--top];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​        </p>
<p>使用数组 我们将栈中N个元素保存在数组中，索引为N的 数组位置对应栈顶的位置，即下一个元素加入的地方。好，要入栈 我们只需要将心元素加入s[N]，要出栈则移除s[N-1]处的元素 并将N减1。那么能看到使用数组一个根本性的缺点 必须事先声明数组的大小。将元素入栈，我们将元素放在N索引的位置 并将N增加1。这是现在很多编程语言表示使用索引N 并增加1的简洁表示。将元素出栈，我们将索引N减1并用它 返回数组中的元素。每个操作都只需要一行代码。有一个问题 客户端是否能向数据结构中插入空元素。这种情况中，我们 确实允许客户端插入空元素，但在Java中我们需要操心一个问题 叫做对象游离（loitering）,即在栈的数组 实现中有对象的引用，而我们并没有真正使用它 所以当减小N时，在数组中仍然有我们已经出栈 的对象的指针。尽管我们知道我们不再使用它了，但是Java系统 不知道。所以为了避免这个问题，最有效地使用内存 最好将去除的元素对应的项设为null，这样就不会剩下 旧元素的引用。因为不存在引用了接下来垃圾回收器会收回那些内存。这个问题比较细节，但是很重要 我们必须在实现中要确保充分利用内存。</p>
<p>​    那么，栈的基本数组实现具有需要 客户端事先提供栈的最大容量的缺点。我们没有 严格按照API的要求。API就是要求我们能够建立一个栈 并且能够增长或者缩小到任意大小。首先会想到的也许是当客户端入栈新元素时 将栈的大小增加1，当出栈时 将栈的大小减小1。代码实现不难，但是不值得这么做，因为这么做 开销太大了。因为你必须创建大小大一个元素的 新的数组，然后把所有的元素复制到新的数组中。所以如果栈大小为N-1 插入N个元素需要的时间和N成正比 如果栈大小为N-2，需要正比于N-1的时间。所以前N个元素需要的时间就是对 前N个整数求和，我们知道这大约是N^2 / 2。往栈里插入N个元素 需要平方时间，我们已经看到过很多次，这样的性能对于 巨大的问题是不可接受的。</p>
<p>​    那么，调整大小是个挑战 但要通过某种方式确保它并不会经常发生。处理这个问题 有个著名的方法叫反复倍增，当数组填满时 建立一个大小翻倍的新数组，然后将所有元素复制过去，我们就不会 那么频繁地创建新数组。这就是那个方法的实现。从大小为1的 数组开始。如果我们检测到N即栈中元素的个数与数组 长度相等，则栈满了，那么我们就在插入元素之前 将数组长度调整为两倍。我们如何调整为更大的数组呢？ 我们创建具有目标容量的新的数组，然后把 当前栈复制到新数组的前一半，然后返回 重新设置实例变量，我们的栈就有了更大的数组 这样做导致如果你向一个具有这种 数组表示的栈中插入N个元素，时间复杂度 与N而不是N^2成正比。因为你只有在数组大小翻倍时 才创建新的数组。而当数组翻倍时，你已经往栈里插入了 那么多的元素。所以平均下来就像每次插入只需要一个操作 所以，如果我们计算一下开销，插入前N个元素 你不需要花费从1到N之和的时间，而是 对二的幂从1到N求和 这样，总的开销大约是3N。所以，push 时先要访问数组一次，对于复制 要访问两次。所以，要插入元素，大约需要访问数组三次 这个图标是观察时间开销的另一种方式，表示出了实现 入栈操作需要访问数组的次数。每次遇到2的幂，需要进行那么多次 数组访问，但是从宏观上来看你是将那些元素放在栈上花去了那么多时间 这叫做平摊分析。考虑开销时 将总的开销平均给所有的操作。</p>
<p>​    <img src="/Users/lwen/Blog/source/images/arrstack_time.png" alt="avatar"></p>
<p>​    这是一个很好而且 有用的平摊分析的例子，我们分析出了栈实现的效率 出栈呢？我们需要考虑如何缩小数组 我们也许这么考虑，当数组满了的时候将容量翻倍，那么当它 只有一半满的时候，将容量缩减一半。我们不想这样做，这个办法并不如我们所愿解决问题。因为有一种现象叫做 抖动（thrashing）。如果客户端刚好反复交替入栈出栈入栈出栈 当数组满了就会反复翻倍减半翻倍减半，并且 每个操作都会新建数组，都要花掉正比与N的时间 这样就会导致平方时间，我们不想这样 有效的解决方案是直到数组变为1/4满的时候才将容量减半 实现起来也很容易，我们只要测试数组是否为 1/4满，如果是，则调整大小使其为半满。</p>
<p>​    这是两种 API相同的不同的实现，客户端可以互换使用。哪个更好呢？ 很多情形中，我们会有同一API的多种实现 你需要根据客户端的性质选择 合适的实现。对于链表，每个操作最坏情况下 需要常数时间，这是有保障的。但是为了处理链接 我们需要一些额外的时间和空间。所以链表实现会慢一些 可调大小的数组实现有很好的分摊时间，所以整个过程 总的平均效率不错，浪费更少的空间，对于每个操作 也许有更快的实现，所以对于一些客户端，也许会有区别 以下这样的情形你不会想用可调大小数组实现 你有一架飞机进场等待降落 你不想系统突然间不能高效运转 或者互联网上的一个路由器，数据包以很高的速度涌进来 你不想因为某个操作突然变得很慢而丢失 一些数据。客户端就可以权衡，如果想要 获得保证每个操作能够很快完成，就使用 链表实现，如果不需要，只是关心总的时间 可能就是用可调大小数组实现，因为总的 时间会小得多，单个操作非常快。</p>
]]></content>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 虚拟机对象布局及创建过程</title>
    <url>/2017/12/10/JVM/Java-%E8%99%9A%E6%8B%9F%E6%9C%BA%E5%AF%B9%E8%B1%A1%E5%B8%83%E5%B1%80%E5%8F%8A%E5%88%9B%E5%BB%BA%E8%BF%87%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="一-对象创建过程"><a href="#一-对象创建过程" class="headerlink" title="一. 对象创建过程"></a>一. 对象创建过程</h1><h3 id="一、类加载"><a href="#一、类加载" class="headerlink" title="一、类加载"></a>一、类加载</h3><p>​    这个阶段其实主要做的就是类的加载验证，以及类的准备等等一系列的工作。为后面的类的初始化和解析做铺垫。</p>
<a id="more"></a>

<h3 id="二、类解析"><a href="#二、类解析" class="headerlink" title="二、类解析"></a>二、类解析</h3><p>​    这个阶段主要对上面已经加载入内存的类进行词法语法的解析，形成语法树等操作。</p>
<h3 id="三、类初始化"><a href="#三、类初始化" class="headerlink" title="三、类初始化"></a>三、类初始化</h3><p>​    类初始化执行的就是 &lt;clinit&gt; 方法，以及在类中的各种常量的初始化（放入常量池中的常量）。</p>
<h3 id="四、分配内存"><a href="#四、分配内存" class="headerlink" title="四、分配内存"></a>四、分配内存</h3><p>​    虚拟机对所需要创建的对象分配内存，在类的解析的时候其实已经知道实例变量需要的堆空间了。此时只需要调用操作系统的系统调用为当前的对象分配内存即可。</p>
<p>​    但是为对象分配内存如果是在大型环境下，肯定是并发的也就是可能会出现正当当前指针分配内存的时候另外一个线程把指针指向了别处，这样一来自然就会出现多线程的安全问题。这里 jvm 通常采用两种方式来解决：</p>
<ul>
<li>使用操作系统的CAS算法来分配内存，保证分配内存动作的原子性，CAS 也就是 Compare And Save 其算法的基本思想就是拿线程需要改变的变量写入内存之前从内存中读取的值 A 和该变量的最初从内存读取的值 V 进行比较如果他们相等就可以将当前的修改后的值 B 写入内存，否则则放弃当前的操作重新开始尝试。</li>
<li>另外一种就是使用了线程的临时分配表叫做 TLAB 表，每个线程获得一块这样的内存这样一来每个线程就拿到了相互独立的内存空间，只有当 TLAB 表分配没有剩余的时候才使用 CAS 同步操作。</li>
</ul>
<h3 id="五、内存的初始化"><a href="#五、内存的初始化" class="headerlink" title="五、内存的初始化"></a>五、内存的初始化</h3><p>​    虚拟机对这块刚分配的内存进行初始化的时候是非常暴力的或者说简单的，直接使用了 </p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="built_in">memset</span>( bf , <span class="number">0</span> ) ;</span><br></pre></td></tr></table></figure>

<p>这是真正的 0 值，所以说对象在分配内存以后其实就是可用的，里面的引用类型就是 NULL 而普通变量的内容则是 0 。这个操作其实不一定到现在才进行，如果说我们采用的 TLAB 的方式的话我们只需要在为线程分配 TLAB 表的时候执行那个 memset 函数即可完成内存初始化。</p>
<h3 id="六、对象进行必要的初始化"><a href="#六、对象进行必要的初始化" class="headerlink" title="六、对象进行必要的初始化"></a>六、对象进行必要的初始化</h3><p>​    在经过上面的暴力的 0 值初始化之后对象还是不能使用的，因为对象不是一块充满数据的内存，对象里面有一些特殊的标记信息，标识当前对象的状态，这个就叫做对象头也称作 MarkWord ，这里对对象的必要的初始化说的就是对对象头进行设置。比如说 GC 分代年龄，hashCode，偏向锁，偏向 ID，时间戳，线程持有的锁等等。</p>
<p>​    至此 jvm 对一个对象的创建才算是真正的完成，但是这个对于 java 程序员来说对象的创建才刚刚开始，因为此时才开始执行用户代码，这个要取决于字节码中是否有 invokespecial 指令，这个指令就会去执行实例的 &lt;init&gt;</p>
<p>方法来初始化对象。</p>
<h1 id="二、对象的内存布局"><a href="#二、对象的内存布局" class="headerlink" title="二、对象的内存布局"></a>二、对象的内存布局</h1>]]></content>
      <categories>
        <category>-Java -虚拟机</category>
      </categories>
      <tags>
        <tag>Java 虚拟机</tag>
      </tags>
  </entry>
  <entry>
    <title>Java 虚拟机运行时数据区</title>
    <url>/2017/12/10/JVM/Java-%E8%99%9A%E6%8B%9F%E6%9C%BA%E8%BF%90%E8%A1%8C%E6%97%B6%E6%95%B0%E6%8D%AE%E5%8C%BA/</url>
    <content><![CDATA[<h1 id="运行时数据区："><a href="#运行时数据区：" class="headerlink" title="运行时数据区："></a>运行时数据区：</h1><p>Java 虚拟机的运行时数据区按照大的可以分为线程独立使用的数据区，和所有线程共享的数据区。</p>
<h3 id="一-线程独立使用数据区"><a href="#一-线程独立使用数据区" class="headerlink" title="一.线程独立使用数据区"></a>一.线程独立使用数据区</h3><h4 id="1-程序计数器"><a href="#1-程序计数器" class="headerlink" title="1.程序计数器"></a>1.程序计数器</h4><ol>
<li>程序计数器其实就是 jvm 里面的pc，他指向的都是字节码的偏移量，也就是下一条要执行的字节码</li>
<li>当然这是 jvm 在执行 java 方法的时候，当程序在执行 navtive 方法的时候这时候起作用的其实是我们物理机上的 pc 此时 jvm 的 pc 是空值（undefine）</li>
<li>并且这个地方也是所有的 jvm 内存区完全不会抛出 OutOfMemary 异常的位置</li>
</ol>
<a id="more"></a>

<h4 id="2-虚拟机栈"><a href="#2-虚拟机栈" class="headerlink" title="2.虚拟机栈"></a>2.虚拟机栈</h4><ol>
<li>虚拟机栈其实也就是我们日常所说的堆栈中的栈</li>
<li>他的生命周期是和当前的线程完全一样</li>
<li>当在执行一个新的 java 方法的时候他会在 java 虚拟机栈创建一个栈帧。</li>
<li>栈帧中存放的内容：<ul>
<li>局部变量表</li>
<li>操作数栈</li>
<li>动态链接</li>
<li>方法的出入口</li>
</ul>
</li>
<li>局部变量表是一个非常重要的数据结构这里面存放的主要就是这个 java 方法中的局部变量。而这些变量都是在编译时期确定的。后来会看到其实在 class 中有一个 code 属性，这个属性里面会有一个 Local_max 他的作用就是计算当前的方法需要多少个 Slot ，并且注意这个 Slot 并不是局部变量有多少他就会产生多少，而是根据作用域对这些 Slot 进行复用。（这些都是后面 Class 结构部分需要说的，暂时可以放放）</li>
<li>局部变量表存放的数据类型除了一些常用 int，float，char，long，short，double，byte 等等还有两个比较特殊的分别是 returnValue 类型和引用类型 reference 类型，reference 类型其实就是我们常说的当我们创建一个对象的时候 jvm 会在栈上创建一个对象的引用，然后真正的对象数据放在堆上。这里的引用就是 reference 类型变量。当然这个引用并不是一定指向的堆中的那个对象还可能指向的是堆中的句柄池中的一个句柄。（后面会详细解释）</li>
</ol>
<h4 id="3-本地方法栈"><a href="#3-本地方法栈" class="headerlink" title="3.本地方法栈"></a>3.本地方法栈</h4><ol>
<li>本地方法栈其实就用于执行 Native 方法，也就是本地方法。一般来说 jvm 的规范对这方面没有过多的要求，一般的 jvm 都是直接把本地方法栈和虚拟机栈直接合并了。</li>
<li>本地方法也就是 Native 方法其实这是 Jvm 对外提供的一个接口，这个接口的作用就是使用 java 的接口去调用其他编程语言写的代码，一般一个 Native 方法里面的方法体不是 java 代码，而是去调用其他的编程语言的代码，也就是此时代码就不归 jvm 来管了，此时调用的什么编程语言就由什么编程语言来管，所以说白了这块内存不完全算是 jvm 的内存。例如说 java 调用了 c++代码这时候的方法堆栈就是 c++的堆栈，jvm 这边应该就是用一个指针指向那块内存。java 的这一特性叫做 JNI （Java Native Interface），其实也不是 java 才有这一特性，很多的编程语言都可以这么玩，例如说在 c++中调用 c 代码其实我们用了 extern 这也是类似的。</li>
</ol>
<h3 id="二-线程共享数据区"><a href="#二-线程共享数据区" class="headerlink" title="二.线程共享数据区"></a>二.线程共享数据区</h3><h4 id="1-堆"><a href="#1-堆" class="headerlink" title="1. 堆"></a>1. 堆</h4><ol>
<li>其实一提到堆我们对他都比较熟悉，也就是我们经常说的和栈结构相对的就是堆了。</li>
<li>一般来说我们会认为所有的对象都是在堆上开辟的，但是当我们了解了 jvm 内部的一些原理之后我们就应该转变这种观念，其实并非所有的对象都是在堆上开辟的主要是现在的 JIT（Just In Time 即时编译技术） 以及对象逃逸造成部分对象是在栈上开辟的。</li>
<li>堆目前也是 jvm 进行内存自动管理的部分，GC 堆。</li>
<li>堆上的内存其实在物理机的内存上不必完全就是连续的。</li>
</ol>
<h4 id="2-方法区"><a href="#2-方法区" class="headerlink" title="2.方法区"></a>2.方法区</h4><ol>
<li>方法区其实是一个比较复杂的内存区域，里面存放的内容主要为：<ul>
<li>JIT 即时编译的代码</li>
<li>静态变量</li>
<li>常量池</li>
<li>加载的类和接口的信息</li>
</ul>
</li>
<li>这个区域可以看到里面的东西都不是朝生夕死的一些数据，也就是更换的速率不是很频繁，所以对这个区域的 GC 并不是很对，而且这里的 GC 区域比较困难。部分区域的 GC 条件较为苛刻。</li>
</ol>
<h4 id="3-常量池"><a href="#3-常量池" class="headerlink" title="3.常量池"></a>3.常量池</h4><p>​    常量池其实就是方法区的一部分（当然这是 jdk1.7之前的位置），其实现在（也就是 jdk1.7 以后）java 虚拟机团队把常量池移动到了堆中。这个区域也叫做  “ 永久代 ” （PermGen），但是当前的 jdk1.8 虚拟机团队直接把常量池这个永久代取消了，然后取而代之的叫做元空间（MateSpace）。好吧这里我们还是讨论常量池还存在的情况的吧。之后会专门写关于 jdk1.7 以及1.8 中的做的很大的更改。</p>
<p>​    其实在 jdk1.7 之前这个地方主要存放的就是 class 字节码中的常量池的内容以及在运行过程中动态生成的常量。尤其是使用的比较多的 String.intern() 方法。这个方法的主要作用就是当程序第一个遇到这个字符串的时候会把他放入常量池中。然后返回常量池中的引用。</p>
<h4 id="4-直接内存"><a href="#4-直接内存" class="headerlink" title="4.直接内存"></a>4.直接内存</h4><ol>
<li>直接内存是不同于 Native 堆的，因为这个地方主要作用在于进行 NIO 的时候做数据缓冲用的。</li>
<li>NIO 中采用的是通道缓冲区的方式进行 IO 操作的，这里他们为了减少 Java 堆和 Native 堆来回复制数据而采用的一种方式也就是在 java 堆中使用一个 DirectByteBuffer 对象引用这块直接内存然后直接把数据读取到直接内存避免往 Java 堆里面复制。</li>
</ol>
]]></content>
      <tags>
        <tag>JVM Java 虚拟机</tag>
      </tags>
  </entry>
  <entry>
    <title>今日总结20180304</title>
    <url>/2017/12/06/Life/%E4%BB%8A%E6%97%A5%E6%80%BB%E7%BB%9320180304.md/</url>
    <content><![CDATA[<ol>
<li>八大排序算法</li>
<li>优先队列</li>
<li>红黑树</li>
<li>并查集</li>
<li>左式堆</li>
<li>栈的应用：逆波兰表达式计算、中缀表达式转后缀表达式</li>
<li>队列应用：杨辉三角、</li>
<li>最大子序列的和：分治、贪心</li>
<li>递归的四大原则：<ol>
<li>基准情型：所有的递归都有一个基准，或者说是递归停止的界限，他是无序计算就能够得到的</li>
<li>不断推进：向着基准方向不断的推进，也就是递归的过程。</li>
<li>设计法则：我们假定递归的每一个环节都能运行，也就是我们无需考虑递归甚至调试追踪递归内部的情形</li>
<li>合成效益：不要进行重复的计算.</li>
</ol>
</li>
<li>内部类的作用，静态内部类的特点。</li>
<li>二叉树表示表达式，先序、中序、后序分别代表了对应的表达式</li>
<li>二叉搜索树的定义操作：<ol>
<li>判断元素是否存在：递归的在左右子树中查找</li>
<li>查找最小元素：在左子树中递归或者循环</li>
<li>查找最大元素：在右子树中递归或循环</li>
<li>插入：递归的插入，大于则插入在节点的右子树，小于则左子树，等于则是重复节点不作处理</li>
<li>删除：递归删除( 或者说递归查找需要删除的元素 )，找到该元素后，如果元素有两个子节点那么久找到这个元素的右子树的最小元素代替要删除的元素，然后再删除那个右子树上的最小元素。如果只有一个子节点直接让要被删除的节点赋值上他的子节点。</li>
</ol>
</li>
</ol>
]]></content>
      <categories>
        <category>-生活</category>
      </categories>
      <tags>
        <tag>生活</tag>
      </tags>
  </entry>
  <entry>
    <title>博客迁移到 coding 了</title>
    <url>/2017/12/06/Life/%E5%8D%9A%E5%AE%A2%E8%BF%81%E7%A7%BB%E5%88%B0-coding-%E4%BA%86/</url>
    <content><![CDATA[<p>​    从这个博客搭建成功到现在已经过了两年了，哎！时间真的过的超快。纪念一下  ：）  </p>
<p>​    但是一直以来我的博客都是托管到 github 上面的，每次最讨厌去访问博客了，做一次翻页我的等几秒，用户体验极差（虽然还是我自己写的，要是别人的博客估计直接一个 ctrl+w 伺候着了 ）。  </p>
<p>​    <a id="more"></a></p>
<p>​    后来也是看到简书上有人说使用 coding 也可以部署 hexo 一直想迁移来着，昨天终于是完全的迁移过来了。感觉速度真的没的说，比我用梯子访问 github 不知道快了多少，想着还是要不注册一个域名实名认证一下。那个 fmzero 域名真的没法用，我也就懒得认证了。不知道当时咋想的（让我哭一会，白白浪费一个58的域名）。  </p>
<p>​    好了，以后还是要认认真真的写博客，毕竟买了这么多书，除了看书还有看博客总会有收获的，不仅仅记录自己的成长我想我能把我的一些东西分享出来也是对自己的提升吧  ：） 加油！！</p>
]]></content>
      <categories>
        <category>-杂记</category>
      </categories>
      <tags>
        <tag>杂记</tag>
      </tags>
  </entry>
  <entry>
    <title>SpingBoot笔记一</title>
    <url>/2017/10/23/Java%20Web/SpingBoot%E7%AC%94%E8%AE%B0%E4%B8%80/</url>
    <content><![CDATA[<h2 id="一-目录配置"><a href="#一-目录配置" class="headerlink" title="一.目录配置"></a>一.目录配置</h2><p>1、Application.java 建议放到跟目录下面,主要用于做一些框架配置</p>
<p>2、domain目录主要用于实体（Entity）与数据访问层（Repository）</p>
<p>3、service 层主要是业务类代码</p>
<p>4、controller 负责页面访问控制</p>
<p>##二.pom.xml 配置</p>
<p><strong>引入web模块</strong></p>
<p>1、pom.xml中添加支持web的模块：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.springframework.boot<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>spring-boot-starter-web<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line"> <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>pom.xml文件中默认有两个模块：</p>
<p>spring-boot-starter：核心模块，包括自动配置支持、日志和YAML；</p>
<p>spring-boot-starter-test：测试模块，包括JUnit、Hamcrest、Mockito。</p>
<p>##三.编写controller内容</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HelloWorldController</span> </span>&#123;</span><br><span class="line">    <span class="meta">@RequestMapping</span>(<span class="string">"/hello"</span>)</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">index</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"Hello World"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>@RestController的意思就是controller里面的方法都以json格式输出，不用再写什么jackjson配置的了！</p>
<h2 id="四-单元测试"><a href="#四-单元测试" class="headerlink" title="四.单元测试"></a>四.单元测试</h2><hr>
<p>打开的src/test/下的测试入口，编写简单的http请求来测试；使用mockmvc进行，利用MockMvcResultHandlers.print()打印出执行结果。</p>
]]></content>
      <categories>
        <category>-SpringBoot</category>
      </categories>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>SpingBoot依赖包</title>
    <url>/2017/10/23/Java%20Web/SpingBoot%E4%BE%9D%E8%B5%96%E5%8C%85/</url>
    <content><![CDATA[<h1 id="Spring-Boot的启动器Starter详解"><a href="#Spring-Boot的启动器Starter详解" class="headerlink" title="Spring Boot的启动器Starter详解"></a>Spring Boot的启动器Starter详解</h1><h5 id="作者：chszs，转载博客主页：http-blog-csdn-net-chszs"><a href="#作者：chszs，转载博客主页：http-blog-csdn-net-chszs" class="headerlink" title="作者：chszs，转载博客主页：http://blog.csdn.net/chszs"></a>作者：chszs，转载博客主页：<a href="http://blog.csdn.net/chszs" target="_blank" rel="noopener">http://blog.csdn.net/chszs</a></h5><p>Spring Boot应用启动器基本的一共有44种，具体如下：</p>
<p>1）spring-boot-starter<br>这是Spring Boot的核心启动器，包含了自动配置、日志和YAML。</p>
<p>2）spring-boot-starter-actuator<br>帮助监控和管理应用。</p>
<p>3）spring-boot-starter-amqp<br>通过spring-rabbit来支持AMQP协议（Advanced Message Queuing Protocol）。</p>
<p>4）spring-boot-starter-aop<br>支持面向方面的编程即AOP，包括spring-aop和AspectJ。</p>
<p>5）spring-boot-starter-artemis<br>通过Apache Artemis支持JMS的API（Java Message Service API）。</p>
<p>6）spring-boot-starter-batch<br>支持Spring Batch，包括HSQLDB数据库。</p>
<p>7）spring-boot-starter-cache<br>支持Spring的Cache抽象。</p>
<p>8）spring-boot-starter-cloud-connectors<br>支持Spring Cloud Connectors，简化了在像Cloud Foundry或Heroku这样的云平台上连接服务。</p>
<p>9）spring-boot-starter-data-elasticsearch<br>支持ElasticSearch搜索和分析引擎，包括spring-data-elasticsearch。</p>
<p>10）spring-boot-starter-data-gemfire<br>支持GemFire分布式数据存储，包括spring-data-gemfire。</p>
<p>11）spring-boot-starter-data-jpa<br>支持JPA（Java Persistence API），包括spring-data-jpa、spring-orm、Hibernate。</p>
<p>12）spring-boot-starter-data-mongodb<br>支持MongoDB数据，包括spring-data-mongodb。</p>
<p>13）spring-boot-starter-data-rest<br>通过spring-data-rest-webmvc，支持通过REST暴露Spring Data数据仓库。</p>
<p>14）spring-boot-starter-data-solr<br>支持Apache Solr搜索平台，包括spring-data-solr。</p>
<p>15）spring-boot-starter-freemarker<br>支持FreeMarker模板引擎。</p>
<p>16）spring-boot-starter-groovy-templates<br>支持Groovy模板引擎。</p>
<p>17）spring-boot-starter-hateoas<br>通过spring-hateoas支持基于HATEOAS的RESTful Web服务。</p>
<p>18）spring-boot-starter-hornetq<br>通过HornetQ支持JMS。</p>
<p>19）spring-boot-starter-integration<br>支持通用的spring-integration模块。</p>
<p>20）spring-boot-starter-jdbc<br>支持JDBC数据库。</p>
<p>21）spring-boot-starter-jersey<br>支持Jersey RESTful Web服务框架。</p>
<p>22）spring-boot-starter-jta-atomikos<br>通过Atomikos支持JTA分布式事务处理。</p>
<p>23）spring-boot-starter-jta-bitronix<br>通过Bitronix支持JTA分布式事务处理。</p>
<p>24）spring-boot-starter-mail<br>支持javax.mail模块。</p>
<p>25）spring-boot-starter-mobile<br>支持spring-mobile。</p>
<p>26）spring-boot-starter-mustache<br>支持Mustache模板引擎。</p>
<p>27）spring-boot-starter-redis<br>支持Redis键值存储数据库，包括spring-redis。</p>
<p>28）spring-boot-starter-security<br>支持spring-security。</p>
<p>29）spring-boot-starter-social-facebook<br>支持spring-social-facebook</p>
<p>30）spring-boot-starter-social-linkedin<br>支持pring-social-linkedin</p>
<p>31）spring-boot-starter-social-twitter<br>支持pring-social-twitter</p>
<p>32）spring-boot-starter-test<br>支持常规的测试依赖，包括JUnit、Hamcrest、Mockito以及spring-test模块。</p>
<p>33）spring-boot-starter-thymeleaf<br>支持Thymeleaf模板引擎，包括与Spring的集成。</p>
<p>34）spring-boot-starter-velocity<br>支持Velocity模板引擎。</p>
<p>35）spring-boot-starter-web<br>S支持全栈式Web开发，包括Tomcat和spring-webmvc。</p>
<p>36）spring-boot-starter-websocket<br>支持WebSocket开发。</p>
<p>37）spring-boot-starter-ws<br>支持Spring Web Services。</p>
<p>Spring Boot应用启动器面向生产环境的还有2种，具体如下：</p>
<p>1）spring-boot-starter-actuator<br>增加了面向产品上线相关的功能，比如测量和监控。</p>
<p>2）spring-boot-starter-remote-shell<br>增加了远程ssh shell的支持。</p>
<p>最后，Spring Boot应用启动器还有一些替换技术的启动器，具体如下：</p>
<p>1）spring-boot-starter-jetty<br>引入了Jetty HTTP引擎（用于替换Tomcat）。</p>
<p>2）spring-boot-starter-log4j<br>支持Log4J日志框架。</p>
<p>3）spring-boot-starter-logging<br>引入了Spring Boot默认的日志框架Logback。</p>
<p>4）spring-boot-starter-tomcat<br>引入了Spring Boot默认的HTTP引擎Tomcat。</p>
<p>5）spring-boot-starter-undertow<br>引入了Undertow HTTP引擎（用于替换Tomcat）。</p>
]]></content>
      <tags>
        <tag>Sping</tag>
      </tags>
  </entry>
  <entry>
    <title>PalindromicSubstrings</title>
    <url>/2017/10/12/Leet%20Code/PalindromicSubstrings/</url>
    <content><![CDATA[<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Classify.DP.Medium;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">PalindromicSubstrings</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 基本思路：这里的 dp 方程的每一个元素就代表我要以当前元素作为回文子串的结尾时候的回文子串的数量</span></span><br><span class="line"><span class="comment">     * 那么递推公式就是以上一个元素结尾时候的子串数量加上本次的结尾的子串的数量就能获得总数量了</span></span><br><span class="line"><span class="comment">     * 而判断当前结尾的回文子串就是判断到对称的元素，然后翻转操作做判断即可</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> s</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">countSubstrings</span><span class="params">(String s)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] dp = <span class="keyword">new</span> <span class="keyword">int</span>[s.length()];</span><br><span class="line">        dp[<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; dp.length; i++) &#123;</span><br><span class="line">            dp[i] = dp[i - <span class="number">1</span>] + currentCount(s, i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> dp[s.length() - <span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">currentCount</span><span class="params">(String string, <span class="keyword">int</span> i)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> j = i; j &gt;= <span class="number">0</span>; --j) &#123;</span><br><span class="line">            <span class="keyword">if</span> (string.charAt(i) != string.charAt(j)) &#123;</span><br><span class="line">                <span class="keyword">continue</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (<span class="keyword">new</span> StringBuilder(string.substring(j, i + <span class="number">1</span>)).reverse().toString().equals(string.substring(j, i + <span class="number">1</span>))) &#123;</span><br><span class="line">                ++count;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(countSubstrings(<span class="string">"aaa"</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-leetcode</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
      </tags>
  </entry>
  <entry>
    <title>DP刷题记录Medium</title>
    <url>/2017/10/11/Leet%20Code/DP%E5%88%B7%E9%A2%98%E8%AE%B0%E5%BD%95Medium/</url>
    <content><![CDATA[<ul>
<li><input disabled type="checkbox"> <p>House Robber II</p>
<pre><code><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Classify.DP.Medium;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HouseRobberII</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 思路一：这道题的意思就是成一个环，也就是偷了第一个不能头最后一个。</span></span><br><span class="line"><span class="comment">     * 所以我们的思路就是让他从第一个偷到倒数第二个</span></span><br><span class="line"><span class="comment">     * 从第二个偷到最后一个</span></span><br><span class="line"><span class="comment">     * 算两个的最大值</span></span><br><span class="line"><span class="comment">     * 思路二：这种方法就是去掉头和尾，计算中间的最大值，然后加上第一个与最后一个中的最大值</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> nums</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">rob</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (nums.length == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (nums.length == <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> nums[<span class="number">0</span>];</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (nums.length == <span class="number">2</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> Math.max(nums[<span class="number">0</span>], nums[<span class="number">1</span>]);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> result1;</span><br><span class="line">        <span class="keyword">int</span> result2;</span><br><span class="line">        <span class="keyword">int</span>[] dp=<span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">2</span>];</span><br><span class="line">        dp[<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">        dp[<span class="number">1</span>] = nums[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">int</span> tmp;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; nums.length-<span class="number">1</span>; i++) &#123;</span><br><span class="line">            tmp = dp[<span class="number">0</span>];</span><br><span class="line">            dp[<span class="number">0</span>] = Math.max(dp[<span class="number">0</span>], dp[<span class="number">1</span>]);</span><br><span class="line">            dp[<span class="number">1</span>] = tmp + nums[i];</span><br><span class="line">        &#125;</span><br><span class="line">        result1 = Math.max(dp[<span class="number">0</span>], dp[<span class="number">1</span>]);</span><br><span class="line"></span><br><span class="line">        dp[<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">        dp[<span class="number">1</span>] = nums[<span class="number">1</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">2</span>; i &lt; nums.length; i++) &#123;</span><br><span class="line">            tmp = dp[<span class="number">0</span>];</span><br><span class="line">            dp[<span class="number">0</span>] = Math.max(dp[<span class="number">0</span>], dp[<span class="number">1</span>]);</span><br><span class="line">            dp[<span class="number">1</span>] = tmp + nums[i];</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        result2 = Math.max(dp[<span class="number">0</span>], dp[<span class="number">1</span>]);</span><br><span class="line">        <span class="keyword">return</span> Math.max(result1, result2);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(rob(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">1</span>,<span class="number">1</span>,<span class="number">1</span>&#125;));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></code></pre></li>
<li><input disabled type="checkbox"> <p>2 Keys Keyboard</p>
<pre><code><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Classify.DP.Medium;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment">     Initially on a notepad only one character 'A' is present. You can perform two operations on this notepad for each step:</span></span><br><span class="line"><span class="comment">    </span></span><br><span class="line"><span class="comment">     Copy All: You can copy all the characters present on the notepad (partial copy is not allowed).</span></span><br><span class="line"><span class="comment">     Paste: You can paste the characters which are copied last time.</span></span><br><span class="line"><span class="comment">     Given a number n. You have to get exactly n 'A' on the notepad by performing the minimum number of steps permitted. Output the minimum number of steps to get n 'A'.</span></span><br><span class="line"><span class="comment">    </span></span><br><span class="line"><span class="comment">     Example 1:</span></span><br><span class="line"><span class="comment">     Input: 3</span></span><br><span class="line"><span class="comment">     Output: 3</span></span><br><span class="line"><span class="comment">     Explanation:</span></span><br><span class="line"><span class="comment">     Intitally, we have one character 'A'.</span></span><br><span class="line"><span class="comment">     In step 1, we use Copy All operation.</span></span><br><span class="line"><span class="comment">     In step 2, we use Paste operation to get 'AA'.</span></span><br><span class="line"><span class="comment">     In step 3, we use Paste operation to get 'AAA'.</span></span><br><span class="line"><span class="comment">     Note:</span></span><br><span class="line"><span class="comment">     The n will be in the range [1, 1000].</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">KeysKeyboard</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 这题的思路就是 dp 代表的就是当前的 A 的数量,然后我们优先考虑上一步复制过的情况，因为这样能够最快的填满 n 个 A ，但是使用这种情况要注意的就是</span></span><br><span class="line"><span class="comment">     * 我们可能上部赋值我们最后收不了尾，所以必须有一个判断就是我们能用上一步的复制的内容填满剩下的 A ，也就是 n % dp[i - 1] == 0</span></span><br><span class="line"><span class="comment">     * 里面的内容的意思就是我们使用了上一步的复制的内容那么我们的字符串就直接翻倍，而且我们进行了一次复制，那么我们 count  就得加2</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> n</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">minSteps</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (n == <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span>[] dp = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">1000</span>];</span><br><span class="line">        dp[<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">int</span> paste = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> count = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">while</span> (dp[i] &lt;= n) &#123;</span><br><span class="line">            ++i;</span><br><span class="line">            <span class="keyword">if</span> (n % dp[i - <span class="number">1</span>] == <span class="number">0</span>) &#123;</span><br><span class="line">                paste = dp[i - <span class="number">1</span>];</span><br><span class="line">                dp[i] = dp[i - <span class="number">1</span>] * <span class="number">2</span>;</span><br><span class="line">                ++count;</span><br><span class="line">                <span class="keyword">if</span> (paste != <span class="number">1</span>) &#123;</span><br><span class="line">                    ++count;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                dp[i] = dp[i - <span class="number">1</span>] + paste;</span><br><span class="line">                ++count;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (dp[i] == n) &#123;</span><br><span class="line">                <span class="keyword">return</span> count;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(minSteps(<span class="number">10</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></code></pre></li>
</ul>
]]></content>
      <tags>
        <tag>LeetCode</tag>
      </tags>
  </entry>
  <entry>
    <title>DP 刷题记录 Easy</title>
    <url>/2017/10/11/Leet%20Code/DP%E5%88%B7%E9%A2%98%E8%AE%B0%E5%BD%95Easy/</url>
    <content><![CDATA[<ul>
<li><input disabled type="checkbox"> House robber</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Classify.DP;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HouseRobber</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 解法一：思路就是一个二维的动态规划问题，然后0代表不偷当前的房子，1就是偷</span></span><br><span class="line"><span class="comment">     * 那么就可以写出两个地推公式出来，这次不偷的话就是看上一次的最多的情况，然后如果这次偷那么就上一次肯定没有偷，就可以用上一次的没有偷的加上本次的</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> nums</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">rob</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (nums.length == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span>[][] dp = <span class="keyword">new</span> <span class="keyword">int</span>[nums.length][<span class="number">2</span>];</span><br><span class="line">        dp[<span class="number">0</span>][<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">        dp[<span class="number">0</span>][<span class="number">1</span>] = nums[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">int</span> i;</span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">1</span>; i &lt; nums.length; i++) &#123;</span><br><span class="line">            dp[i][<span class="number">0</span>] = Math.max(dp[i - <span class="number">1</span>][<span class="number">0</span>], dp[i - <span class="number">1</span>][<span class="number">1</span>]);</span><br><span class="line">            dp[i][<span class="number">1</span>] = dp[i - <span class="number">1</span>][<span class="number">0</span>] + nums[i];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> Math.max(dp[i-<span class="number">1</span>][<span class="number">0</span>], dp[i-<span class="number">1</span>][<span class="number">1</span>]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 解法二：也就是使用上面的方法然后进行降为，从二维到一维，空间复杂度成 o one</span></span><br><span class="line"><span class="comment">     * 观察可以发现我们每次进行递推的时候我们只用到了上一步的偷了或者没偷的情况所以我们直接就可以用两个变量来代替</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> nums</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">rob_1</span><span class="params">(<span class="keyword">int</span>[] nums)</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] dp=<span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">2</span>];</span><br><span class="line">        dp[<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">        dp[<span class="number">1</span>] = nums[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">int</span> tmp;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; nums.length; i++) &#123;</span><br><span class="line">            tmp = dp[<span class="number">0</span>];</span><br><span class="line">            dp[<span class="number">0</span>] = Math.max(dp[<span class="number">0</span>], dp[<span class="number">1</span>]);</span><br><span class="line">            dp[<span class="number">1</span>] = tmp + nums[i];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> Math.max(dp[<span class="number">0</span>], dp[<span class="number">1</span>]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(rob_1(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">43</span>&#125;));</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ul>
<li><input disabled type="checkbox"> <p>Maximum Subarray</p>
<pre><code><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Classify.DP;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MaximumSubarray</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">maxSubArray</span><span class="params">(<span class="keyword">int</span>[] A)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (A == <span class="keyword">null</span> || A.length == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> global = A[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">int</span> local = A[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; A.length; i++) &#123;</span><br><span class="line">            local = Math.max(A[i], local + A[i]);</span><br><span class="line">            global = Math.max(local, global);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> global;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(maxSubArray(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;-<span class="number">2</span>, <span class="number">1</span>, -<span class="number">3</span>, <span class="number">4</span>, -<span class="number">1</span>, <span class="number">2</span>, <span class="number">1</span>, -<span class="number">5</span>, <span class="number">4</span>&#125;));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></code></pre></li>
<li><input disabled type="checkbox"> <p>Best Time to Buy and Sell Stock</p>
<pre><code><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Classify.DP;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BestTimeBuyAndSellStock</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 思路：这个动态规划问题就是定义 dp 数组就是当前的最大利润。那么递推式就是上一次的最大利润和今天与昨天的差值利润之间的较大的那个，如果是</span></span><br><span class="line"><span class="comment">     * 昨天收盘加上今天的的利润还不如仅仅今天的利润大就直接拿今天的否则就拿昨天的</span></span><br><span class="line"><span class="comment">     * 也就是这一行！</span></span><br><span class="line"><span class="comment">     * dp[1] = Math.max(dp[0] + prices[i] - prices[i - 1], prices[i] - prices[i - 1]);</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> prices</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">maxProfit</span><span class="params">(<span class="keyword">int</span>[] prices)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (prices.length == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span>[] dp = <span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">2</span>];</span><br><span class="line">        <span class="keyword">int</span> result = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; prices.length; i++) &#123;</span><br><span class="line">            dp[<span class="number">1</span>] = Math.max(dp[<span class="number">0</span>] + prices[i] - prices[i - <span class="number">1</span>], prices[i] - prices[i - <span class="number">1</span>]);</span><br><span class="line">            dp[<span class="number">0</span>] = dp[<span class="number">1</span>];</span><br><span class="line">            result = Math.max(dp[<span class="number">1</span>], result);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(maxProfit(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">7</span>, <span class="number">1</span>, <span class="number">5</span>, <span class="number">3</span>, <span class="number">6</span>, <span class="number">4</span>&#125;));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></code></pre></li>
<li><input disabled type="checkbox"> <p>Climbing Stairs</p>
<pre><code><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> Classify.DP;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ClimbingStairs</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     *  Time Limit Exceeded  暴力破解法</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">climbStairs</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        find(<span class="number">0</span>, n);</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">find</span><span class="params">(<span class="keyword">int</span> n,<span class="keyword">int</span> level)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (n == level) &#123;</span><br><span class="line">            count++;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (n &gt; level) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        find(n + <span class="number">1</span>,level);</span><br><span class="line">        find(n + <span class="number">2</span>, level);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 上面的暴力破解的方式导致超时了，其实一般来说绝大多数的 dp 问题都可以使用暴力破解的方法来解决，也就是上面的递归枚举，但是我们知道 dp 的出现就是</span></span><br><span class="line"><span class="comment">     * 为了记录一些东西防止重复计算的，也就是为枚举剪枝，所以说还是要用枚举，这里我们定义了 dp 的含义就是到达当前位置的方法数，而递推公式写法就是</span></span><br><span class="line"><span class="comment">     * 到达当前位置要么是跳两部要么是跳一步来的。所以递推公式就是</span></span><br><span class="line"><span class="comment">     * dp[i] = dp[i - 2] + dp[i - 1];</span></span><br><span class="line"><span class="comment">     * 最重要的就是首先要确定 dp 数组的含义</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> n</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@return</span></span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">climbStairs_1</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] dp = <span class="keyword">new</span> <span class="keyword">int</span>[n+<span class="number">2</span>];</span><br><span class="line">        dp[<span class="number">0</span>] = <span class="number">0</span>;</span><br><span class="line">        dp[<span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">int</span> i;</span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">2</span>; i &lt;= n+<span class="number">1</span>; i++) &#123;</span><br><span class="line">            dp[i] = dp[i - <span class="number">2</span>] + dp[i - <span class="number">1</span>];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> dp[i-<span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(climbStairs_1(<span class="number">10</span>));</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></code></pre></li>
</ul>
<p>以上的题全部都是 Easy 的题，然后也就是我们发现最后我们都可以转为一维的 dp 问题，我们都没设计二维的问题。下一篇开始就是 Mid 的题，估计主要就是类似背包问题的那一类二维的不可分解的问题。</p>
]]></content>
      <tags>
        <tag>LeetCode</tag>
      </tags>
  </entry>
  <entry>
    <title>刷题记录</title>
    <url>/2017/10/11/Leet%20Code/%E5%88%B7%E9%A2%98%E8%AE%B0%E5%BD%95/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>动态规划</title>
    <url>/2017/10/10/Algorithm/%E5%8A%A8%E6%80%81%E8%A7%84%E5%88%92/</url>
    <content><![CDATA[<p>​    动态规划一般来说和分治有点类似都是让他们去处理相同的子问题，但是在动态规划里面你会遇到更多的相同子问题。然后我们就会导致很多的重复计算，所以一般我们可以使用递归来完成一个动态规划要完成的任务，但是这样一般会重复计算很多东西，所以动态规划一般就增加了一些矩阵来存放上一次计算的结果。</p>
<a id="more"></a>

<p>​    例如斐波那契数列，这个如果我们直接使用地柜计算的话，我们会很多重复计算。然后当我们要使用一个阵列计算的时候我们在每一次计算之前我们都需要进行一次判断看看我们目前的结果是不是已经被计算了，如果计算了就相当于查表直接获取答案，否则我们才开始计算。</p>
<p>​    <strong>状态</strong>：状态就是对于每一个字问题都可以使用一个数字 n 来描述这个子问题，如果状态相同就说明他们的答案相同。</p>
<p>​    状态也可以有很多维度，例如排列组合就可以使用二维的，距离使用三维的，所谓的维度就是影响这个子问题的因素，也就是上面的那个 n 以及其他自定义的 j 等等！</p>
<p>​    <strong>状态转移方程</strong>：就是使用当前的状态来获取其他的状态。</p>
<p>​    一般状态转移方程可以使用 top down  也可使用 bottom up 方法。</p>
<p>​    给一个例子：现在有红 绿 蓝 三种颜色的油漆图一面墙，这面墙 红绿 和 绿蓝不能相邻问共有多少中涂色的方法。</p>
<p>​    状态：定义长度为 n  的时候的涂色的方法数，然后出事值就是当 n 等于 1 的时候涂色的方法就是三种。但是这样我们发现很难写出状态方程，所以我们就再添加一个维度。第二个维度的意思就是颜色。</p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-10-10/13798981.jpg" alt></p>
<p>于是就可以得到这样的状态转移方程。看起来稍微有点复杂。而我们最后求结果就是</p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-10-10/78885912.jpg" alt></p>
<p>另外一个例子就是骨牌的问题，这个问题就是费时数列的问题，但是我们需要自己进行研究开始的几种情况。</p>
]]></content>
      <categories>
        <category>-算法设计</category>
      </categories>
      <tags>
        <tag>算法设计</tag>
      </tags>
  </entry>
  <entry>
    <title>分治算法</title>
    <url>/2017/10/10/Algorithm/%E5%88%86%E6%B2%BB%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<p>​    分治算法分为 “分” 和 “治” 两个部分，所谓的分就是寻找分割点，然后对问题划分成相同的子问题，但是问题的规模变小了，这些步骤其实就是使用递归来完成的。之后就是治的问题，也就是递归里面具体的内容。最后一步就是把所分的结果要合并成为一个完整的结果。</p>
<p>​    一个例子就是求出逆序数对，这个就是分治法来计算，首席我们分就是中间划分，分别求左右的两边的逆序数对，然后还需要加上横跨中间的逆序数对即可完成。重点就是横跨中间的逆序数对，中间的如果直接暴力枚举就会出现 n 平方的，所以我们首先对两边分别排序，然后在进行计算，这样复杂度就变成了 n/2 复杂度。</p>
]]></content>
      <categories>
        <category>-算法设计</category>
      </categories>
      <tags>
        <tag>算法设计</tag>
      </tags>
  </entry>
  <entry>
    <title>贪心</title>
    <url>/2017/10/09/Algorithm/%E8%B4%AA%E5%BF%83/</url>
    <content><![CDATA[<p>​    贪心算法，这个算法就是大胆的猜测，然后小心求证！求证一般就是使用反证法进行证明。假如我们说这个顺序是对的，也就是始终选取大的，或者小的，我们就要证明一下我们的猜测正确性。这里的反正方式就是交换顺序发现不符合预期结果原来的结论就是对的。    </p>
]]></content>
      <categories>
        <category>-算法设计</category>
      </categories>
      <tags>
        <tag>算法设计</tag>
      </tags>
  </entry>
  <entry>
    <title>枚举</title>
    <url>/2017/10/09/Algorithm/%E6%9E%9A%E4%B8%BE/</url>
    <content><![CDATA[<p>​    枚举就是尝试所有的可能性，尤其是当我们在确定一个问题是不是的这一类问题中尤其有用，例如说给一堆数，让我我们判断他们是不是素数，或者素数的数量的时候，这里他们就是判断类问题我们就可以使用枚举。</p>
<p>​    但是注意这里我们需要考虑的就是枚举的方式，也就是枚举的角度。这里有一个小的例子就是最长回文子串的问题。<a id="more"></a></p>
<p>​    首先我们就是用一个最简单的方式就是枚举出所有的字串，然后在这些字串里面找回文串。这样我们首先需要进行枚举就需要 n 平方的复杂度，然后我们还需要 n 的时间去判断这个串是不是回文，那么也就是说我们需要 n 三次方的时间复杂度。</p>
<p>​    然后上面的方式枚举的对象就是所有的字串，但是我们仔细就会发现重点在于回文子串的中心，如果我们枚举的是回文子串的中心以及回文的长度，我们就更简单的找到最长回文子串。中心不仅仅就是每一个字符还包括他们之间的缝隙，这样我们就有了 2n + 1 个中心，然后在对他们进行回文判断，最后的时间复杂度也就是 n 平方。</p>
<p>​    这里是从 n 三次方降到了 n 平方的复杂度，这样的原因在于我们去掉了很多的无用的字串，第一个枚举的方法就是枚举所有的字串，然后第二个就是仅仅找出那些具有回文形式的字串，这样就少了一个 n 。经常这样思考会慢慢锻炼我们优化时间复杂度的能力。</p>
<p>​    并且这里看着是 n 平方其实他根本没有那么多的复杂度，因为每一个中心能找到的会问其实并不多除非我们给的一个串是一个完全相同的字符，这也就是第二种算法的最坏情况。</p>
<p>​    其实在枚举的过程中有的枚举并没有必要，因为这些就是用来占用了时间复杂度但是没有给程序带来多大的帮助。然后我们在计算的时候有时候题目给的条件很少，这时候我们就需要使用枚举来增加我们已知的条件。</p>
<p>​    再看一个例子就是在一个矩阵中填满数，然后这些数有正有负我们需要获取一个和的最大矩阵。这里我们先不考虑 DP 而是使用枚举的方式来解这道题。因为这道题的内容是静态的，也就是矩阵是不变化的，我们可以使用计算存储，然后再使用排容原理解题。首先我们考虑一维的情况，我们可以新开一个矩阵，这个矩阵的目的就在于把原来的矩阵的到当前位置的和记录下来。这样我们仅仅需要一次的扫描就能获取从 0 到当前位置的和，然后我们可以计算出任何两个点的之间的和，使用排容原理，就是后面的下标的和减去前面的下标的和即可。这样一个最大的和我们就可以使用 n 的复杂度完成。</p>
<p>​    然后我们扩展到二维的情况，就是新开一个矩阵，然后每一个位置记录 ( 0 , 0 ) 位置到当前位置的和。至于这个和怎么算还需要一个过程，中间我们还需要构造一个矩阵，这个矩阵就是先计算出每一行的和，也就是我们上面所说的一维的情况，然后再过渡到二维的辅助阵列。</p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-10-9/75118229.jpg" alt></p>
<p>​    那么这个算法的复杂度就是一开始我们需要算出这个辅助矩阵的和需要 n 平方的时间，然后需要进行排容原理，也就是我们要枚举左上角和右下角的位置这里就需要 n 的四次方的时间。</p>
<p>​    但是这样做还是不够好，也就是我们枚举了每一个矩形，我们只是优化了 计算矩形的和。但是我们可以使用另外一种思路就是枚举左右边界，然后我们计算出上下界，这样的话这里的时间复杂度就是 n 平方乘以我们算出上下界的时间。而我们算上下界的时候因为我们已经指定了宽度所以说这个宽度之间的东西我们可以把它影射成一维的解决即可。</p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-10-9/30006571.jpg" alt></p>
]]></content>
      <categories>
        <category>-算法设计</category>
      </categories>
      <tags>
        <tag>算法设计</tag>
      </tags>
  </entry>
  <entry>
    <title>犒劳一下自己</title>
    <url>/2017/09/27/Life/%E7%8A%92%E5%8A%B3%E4%B8%80%E4%B8%8B%E8%87%AA%E5%B7%B1/</url>
    <content><![CDATA[<p>​    心情不好的时候，可能这时候真的已经很疲惫了。</p>
<p>​    不要继续沮丧让自己的心情更糟糕，或许这个时候应该犒劳一下自己，让自己的心情放松，晴朗起来或许一切看起来回事那么情切舒服！</p>
]]></content>
      <categories>
        <category>-杂文</category>
      </categories>
      <tags>
        <tag>杂文</tag>
      </tags>
  </entry>
  <entry>
    <title>HouseRobberIII</title>
    <url>/2017/09/27/Leet%20Code/HouseRobberIII/</url>
    <content><![CDATA[<p>The thief has found himself a new place for his thievery again. There is only one entrance to this area, called the “root.” Besides the root, each house has one and only one parent house. After a tour, the smart thief realized that “all houses in this place forms a binary tree”. It will automatically contact the police if two directly-linked houses were broken into on the same night.</p>
<p>Determine the maximum amount of money the thief can rob tonight without alerting the police.</p>
<p>Example 1:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">  3</span><br><span class="line"> / \</span><br><span class="line">2   3</span><br><span class="line"> \   \ </span><br><span class="line">  3   1</span><br></pre></td></tr></table></figure>

<p>Maximum amount of money the thief can rob = 3 + 3 + 1 = 7.</p>
<p>Example 2:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    3</span><br><span class="line">   / \</span><br><span class="line">  4   5</span><br><span class="line"> / \   \ </span><br><span class="line">1   3   1</span><br></pre></td></tr></table></figure>

<p>Maximum amount of money the thief can rob = 4 + 5 = 9.</p>
<p>所以说纯粹是怎么多怎么来，那么这种问题是很典型的递归问题，我们可以利用回溯法来做，因为当前的计算需要依赖之前的结果，那么我们对于某一个节点，如果其左子节点存在，我们通过递归调用函数，算出不包含左子节点返回的值，同理，如果右子节点存在，算出不包含右子节点返回的值，那么此节点的最大值可能有两种情况，一种是该节点值加上不包含左子节点和右子节点的返回值之和，另一种是左右子节点返回值之和不包含当期节点值，取两者的较大值返回即可：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//直接使用递归</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">rob</span><span class="params">(TreeNode root)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> <span class="keyword">var</span> = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> (root.left != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">var</span> += rob(root.left.left) + rob(root.left.right);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (root.right != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">var</span> += rob(root.right.left) + rob(root.right.right);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> Math.max(root.val + <span class="keyword">var</span>, rob(root.right) + rob(root.left));</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>



<p>由于上面的方法超时了，所以我们必须优化上面的方法，上面的方法重复计算了很多地方，比如要完成一个节点的计算，就得一直找左右子节点计算，我们可以把已经算过的节点用哈希表保存起来，以后递归调用的时候，现在哈希表里找，如果存在直接返回，如果不存在，等计算出来后，保存到哈希表中再返回，这样方便以后再调用，参见代码如下：</p>
 <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//使用hash 表对多余的递归进行优化</span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">rob_1</span><span class="params">(TreeNode root)</span> </span>&#123;</span><br><span class="line">    HashMap&lt;TreeNode, Integer&gt; map = <span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">    <span class="keyword">return</span> dfs(root, map);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">dfs</span><span class="params">(TreeNode root, HashMap&lt;TreeNode, Integer&gt; map)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (map.containsKey(root)) &#123;</span><br><span class="line">        <span class="keyword">return</span> map.get(root);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> <span class="keyword">var</span> = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> (root.left != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">var</span> += dfs(root.left.right, map) + dfs(root.left.left, map);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (root.right != <span class="keyword">null</span>) &#123;</span><br><span class="line">        <span class="keyword">var</span> += dfs(root.right.left, map) + dfs(root.right.right, map);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">int</span> max = Math.max(<span class="keyword">var</span> + root.val, dfs(root.left, map) + dfs(root.right, map));</span><br><span class="line">    map.put(root, max);</span><br><span class="line">    <span class="keyword">return</span> max;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>下面再来看一种方法，这种方法的递归函数返回一个大小为2的一维数组res，其中res[0]表示不包含当前节点值的最大值，res[1]表示包含当前值的最大值，那么我们在遍历某个节点时，首先对其左右子节点调用递归函数，分别得到包含与不包含左子节点值的最大值，和包含于不包含右子节点值的最大值，那么当前节点的res[0]就是左子节点两种情况的较大值加上右子节点两种情况的较大值，res[1]就是不包含左子节点值的最大值加上不包含右子节点值的最大值，和当前节点值之和，返回即可，参见代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">rob_2</span><span class="params">(TreeNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] result = dfs_1(root);</span><br><span class="line">        <span class="keyword">return</span> Math.max(result[<span class="number">0</span>], result[<span class="number">1</span>]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] dfs_1(TreeNode root) &#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">0</span>, <span class="number">0</span>&#125;;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span>[] left = dfs_1(root.left);</span><br><span class="line">        <span class="keyword">int</span>[] right = dfs_1(root.right);</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="keyword">int</span>[]&#123; Math.max(left[<span class="number">0</span>], left[<span class="number">1</span>]) + Math.max(right[<span class="number">0</span>], right[<span class="number">1</span>]),root.val + left[<span class="number">0</span>] + right[<span class="number">0</span>]&#125;;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<p>整理于：<a href="http://www.cnblogs.com/grandyang/p/5275096.html" target="_blank" rel="noopener">http://www.cnblogs.com/grandyang/p/5275096.html</a></p>
]]></content>
      <tags>
        <tag>动态规划</tag>
      </tags>
  </entry>
  <entry>
    <title>Target Sum</title>
    <url>/2017/09/27/Leet%20Code/TargetSum/</url>
    <content><![CDATA[<blockquote>
<p>You are given a list of non-negative integers, a1, a2, …, an, and a target, S. Now you have 2 symbols + and -. For each integer, you should choose one from + and - as its new symbol.</p>
<p>Find out how many ways to assign symbols to make sum of integers equal to target S.</p>
<p>Example 1:<br>Input: nums is [1, 1, 1, 1, 1], S is 3.<br>Output: 5<br>Explanation:</p>
<p>-1+1+1+1+1 = 3<br>+1-1+1+1+1 = 3<br>+1+1-1+1+1 = 3<br>+1+1+1-1+1 = 3<br>+1+1+1+1-1 = 3</p>
<p>There are 5 ways to assign symbols to make the sum of nums be target 3.<br>Note:<br>The length of the given array is positive and will not exceed 20.<br>The sum of elements in the given array will not exceed 1000.<br>Your output answer is guaranteed to be fitted in a 32-bit integer.</p>
</blockquote>
<p>这题主要就是给一个算式，然后让我们填符号，要是最后的算式的结果等于一个特定的数，现在发现这种阶梯方法比较好就使用了动态规划，里面具体的东西就是递归求解了。</p>
<p>与此类似的体还有很多就是台阶问题等等，都可以使用类似的方法求解！</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> LeetCode;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TargetSum</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">findTargetSumWays</span><span class="params">(<span class="keyword">int</span>[] nums, <span class="keyword">int</span> S)</span> </span>&#123;</span><br><span class="line">        calculate(nums, <span class="number">0</span>, <span class="number">0</span>, S);</span><br><span class="line">        <span class="keyword">return</span> count;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">calculate</span><span class="params">(<span class="keyword">int</span>[] nums,<span class="keyword">int</span> i,<span class="keyword">int</span> sum,<span class="keyword">int</span> S)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (i == nums.length) &#123;</span><br><span class="line">            <span class="keyword">if</span> (sum == S) &#123;</span><br><span class="line">                count++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">            calculate(nums, i + <span class="number">1</span>, sum + nums[i], S);</span><br><span class="line">            calculate(nums, i + <span class="number">1</span>, sum - nums[i], S);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <tags>
        <tag>动态规划</tag>
      </tags>
  </entry>
  <entry>
    <title>QueueReconstructionbyHeight</title>
    <url>/2017/09/27/Leet%20Code/QueueReconstructionbyHeight/</url>
    <content><![CDATA[<blockquote>
<p>Suppose you have a random list of people standing in a queue. Each person is described by a pair of integers (h, k), where h is the height of the person and k is the number of people in front of this person who have a height greater than or equal to h. Write an algorithm to reconstruct the queue.</p>
<p>Note:<br>The number of people is less than 1,100.</p>
<p>Example</p>
<p>Input:<br>[[7,0], [4,4], [7,1], [5,0], [6,1], [5,2]]</p>
<p>Output:<br>[[5,0], [7,0], [5,2], [6,1], [4,4], [7,1]]</p>
</blockquote>
<p> 这道题给了我们一个队列，队列中的每个元素是一个pair，分别为身高和前面身高不低于当前身高的人的个数，让我们重新排列队列，使得每个pair的第二个参数都满足题意。</p>
<p> 基本思路：<br> 首先按照高度排序，如果两个人的高度一样的话我们就就按照第二个数字排序<br> 最后把他们插入到一个新的数组里面，就按照他们的第二个数字的下标，插入到对应的下标</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> LeetCode;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"><span class="keyword">import</span> java.util.Comparator;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">QueueReconstructionbyHeight</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span>[][] reconstructQueue(<span class="keyword">int</span>[][] people) &#123;</span><br><span class="line">        <span class="keyword">if</span> (people == <span class="keyword">null</span> || people.length == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> people;</span><br><span class="line">        &#125;</span><br><span class="line">        Arrays.sort(people, <span class="keyword">new</span> Comparator&lt;<span class="keyword">int</span>[]&gt;() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(<span class="keyword">int</span>[] p1, <span class="keyword">int</span>[] p2)</span> </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> p1[<span class="number">0</span>] == p2[<span class="number">0</span>] ? p1[<span class="number">1</span>] - p2[<span class="number">1</span>] : p2[<span class="number">0</span>] - p1[<span class="number">0</span>];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        List&lt;<span class="keyword">int</span>[]&gt; temp = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span>[] aPeople : people) &#123;</span><br><span class="line">            <span class="keyword">if</span> (people.length == aPeople[<span class="number">1</span>]) &#123;</span><br><span class="line">                temp.add(aPeople);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                temp.add(aPeople[<span class="number">1</span>], aPeople);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> ans[][] = <span class="keyword">new</span> <span class="keyword">int</span>[people.length][<span class="number">2</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; temp.size(); i++) &#123;</span><br><span class="line">            ans[i] = temp.get(i);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ans;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>排序</tag>
      </tags>
  </entry>
  <entry>
    <title>Dp经典问题LongestIncreasingSubsequence</title>
    <url>/2017/09/27/Leet%20Code/Dp%E7%BB%8F%E5%85%B8%E9%97%AE%E9%A2%98LongestIncreasingSubsequence/</url>
    <content><![CDATA[<blockquote>
<p>Given an unsorted array of integers, find the length of longest increasing subsequence.</p>
<p> For example,<br> Given [10, 9, 2, 5, 3, 7, 101, 18],<br> The longest increasing subsequence is [2, 3, 7, 101], therefore the length is 4. Note that there may be more than one LIS combination, it is only necessary for you to return the length.</p>
<p> Your algorithm should run in O(n2) complexity.</p>
<p> Follow up: Could you improve it to O(n log n) time complexity?</p>
<p> Credits:<br> Special thanks to @pbrother for adding this problem and creating all test cases.</p>
</blockquote>
<p> 思路：<br> 这道题主要考察的就是动态规划<br> 如果使用暴力破解的方式要么就是超时要么就是空间超出限制<br> 然后动态规划的方法就是：使用一个 dp 数组然后在这个 dp 数组记录的内容就是党目前为止的最长的子序列</p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-9-27/81514595.jpg" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">package</span> LeetCode;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Arrays;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LongestIncreasingSubsequence</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">lengthOfLIS</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (nums.length == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span>[] dp = <span class="keyword">new</span> <span class="keyword">int</span>[nums.length];</span><br><span class="line">        Arrays.fill(dp,<span class="number">0</span>);</span><br><span class="line">        dp[<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">int</span> maxans = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; dp.length; i++) &#123;</span><br><span class="line">            <span class="keyword">int</span> maxval = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; i; j++) &#123;</span><br><span class="line">                <span class="keyword">if</span> (nums[i] &gt; nums[j]) &#123;</span><br><span class="line">                    maxval = Math.max(maxval, dp[j]);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">            dp[i] = maxval + <span class="number">1</span>;</span><br><span class="line">            maxans = Math.max(maxans, dp[i]);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(LeetcodeUtils.integerArrayToString(dp));;</span><br><span class="line">        <span class="keyword">return</span> maxans;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">        lengthOfLIS(<span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">4</span>,<span class="number">10</span>,<span class="number">4</span>,<span class="number">3</span>,<span class="number">8</span>,<span class="number">9</span>&#125;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <tags>
        <tag>动态规划</tag>
      </tags>
  </entry>
  <entry>
    <title>烦躁</title>
    <url>/2017/09/25/Life/%E7%83%A6%E8%BA%81/</url>
    <content><![CDATA[<p>​    下午两点过一点，从宿舍走出来，外面还是淅淅沥沥下着小雨。心里不是很舒服，一直不喜欢下雨，阴冷的天气，总是让人觉得不适，什么都不适合，不适合上课不适合看书，就想让人躺在床上。躺在床上就不经意间会回忆到以前，想到种种。<a id="more"></a></p>
<p>​    算了不管了，还是去实验室一个人清净一会，这里就我一个人。拉上窗帘根本看不到外面在下雨，就好像一个晴天的夜晚。</p>
<p>​    打开电脑准备看看 Coursera 上面的算法，可是静不下心来，真的静不下心来！我尽力让自己平静，关掉了窗口，算了！刷题吧，连续点开了三道中档题，没有一个有思路的，题目太长，看到题目的第一眼我就觉得我没有办法再看下去了….</p>
<p>​    QQ 上好像有消息，成绩和对的表格。排名并不好看，六级没加分，没参加任何比赛，没有任何团体活动。之后随随便便看了几个人的就关掉了。为什么突然心里落差这么大？</p>
<p>​    再次回到 leetcode 这次更加烦躁了，突然想到是不是自己太差劲了，好像没什么值得自己或者能让自己感到骄傲的，反而失败的地方好像更多！</p>
]]></content>
  </entry>
  <entry>
    <title>刷题列表</title>
    <url>/2017/09/25/Leet%20Code/%E5%88%B7%E9%A2%98%E5%88%97%E8%A1%A8/</url>
    <content><![CDATA[<h3 id="动态规划"><a href="#动态规划" class="headerlink" title="动态规划"></a>动态规划</h3><ul>
<li><input disabled type="checkbox"> <a href="https://leetcode.com/problems/longest-increasing-subsequence" target="_blank" rel="noopener">Longest Increasing Subsequence</a> </li>
<li><input disabled type="checkbox"> <a href="https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-cooldown" target="_blank" rel="noopener">Best Time to Buy and Sell Stock with Cooldown</a></li>
<li><input disabled type="checkbox"> ​</li>
</ul>
]]></content>
      <categories>
        <category>-LeetCode</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
      </tags>
  </entry>
  <entry>
    <title>1-d range</title>
    <url>/2017/09/25/Algorithm/1-d-range/</url>
    <content><![CDATA[<p>​    1d range 的问题主要就是讨论，在某两个符号之间存在多少个其他的符号。首先这里用到了二叉排序树，然后我们利用了二叉排序树的 rank 操作就能直接得到两个符号之间的 range 数<a id="more"></a></p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-9-25/81803300.jpg" alt></p>
]]></content>
      <categories>
        <category>-数据结构与算法</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>平衡搜索树</title>
    <url>/2017/09/24/Algorithm/%E5%B9%B3%E8%A1%A1%E6%90%9C%E7%B4%A2%E6%A0%91/</url>
    <content><![CDATA[<h3 id="2-3树"><a href="#2-3树" class="headerlink" title="2-3树"></a>2-3树</h3><p>​    其实仔细来看2-3树好像是 B 树的一个特例，它规定了一个节点要么有一个 key 要么有两个 key。</p>
<ol>
<li>如果有一个 key 那么他就有两个子节点，左边小于这个 key 右边大于这个 key</li>
<li>如果有两个 key 那么他就有三个子节点，左边小于，中间处于两者之间，右边大于<a id="more"></a></li>
</ol>
<p>​        这样一来就会发现他其实也会处在插入的时候出现分裂的情况，当一个节点需要插入的时候我们先让他插入，这时候可能出现一个节点有三个 key 的情况，我们就打出四个分支，然后我们把中间的那个 key 分裂到父节点，然后左右的 key 分别作为左右子节点。</p>
<p>​    这时候我们能够发现当且仅当我们的根节点分裂的时候我们的 2-3 树的高度才会真正的加一。这也是和 B 树的性质相似的。</p>
<p>​    2-3 树最好情况就是当所有的节点都是 3 key 节点的时候，这时候我们的树高度最小，而最坏情况自然也就是一个二叉树的时候。</p>
<h3 id="红黑树"><a href="#红黑树" class="headerlink" title="红黑树"></a>红黑树</h3><p>红黑树我们可以把它看做为 2-3 树的变种，也就是说我们可以在 2-3 上进行一些改造生成对应的红黑树。一般来说我们就是将那些具有三个子节点的节点拆分成两个节点，key 大的那个作为根，小的那个作为左子节点，然后他们之间使用红色连接起来（用红色连接的意思就是父节点是黑的，子节点是红的），也就可以看出红黑树更偏向于左边。下面是一个具体的例子：</p>
<p><img src="/Users/lwen/Blog/source/images/br-tree.tiff" alt="br-tree"></p>
<h4 id="红黑树的三个基本操作："><a href="#红黑树的三个基本操作：" class="headerlink" title="红黑树的三个基本操作："></a>红黑树的三个基本操作：</h4><h5 id="左旋转"><a href="#左旋转" class="headerlink" title="左旋转"></a>左旋转</h5><p>​    从第一个图到第二个，这个在什么情况下需要呢？当我们插入一个新节点的时候我们发现插入后就是图一的情况，这时候我们还原到 2-3 树我们发现因为 s 节点更大，所以 s 节点应该是根节点， e 节点应该是 s 节点的子节点才对。所以产生了这样的操作！</p>
<p>​    所以说，当我们插入一个节点的时候我们发现插入到了右边的节点了我们就需要左旋转进行调整。</p>
<p><img src="/Users/lwen/Blog/source/images/br-tree1.tiff" alt="br-tree1"></p>
<p><img src="/Users/lwen/Blog/source/images/br-tree2.tiff" alt="br-tree2"></p>
<h5 id="右旋转"><a href="#右旋转" class="headerlink" title="右旋转"></a>右旋转</h5><p>右旋转刚和和左旋转相反。</p>
<h5 id="颜色翻转"><a href="#颜色翻转" class="headerlink" title="颜色翻转"></a>颜色翻转</h5><p>颜色翻转就是</p>
<p><img src="/Users/lwen/Blog/source/images/br-tree4.tiff" alt="br-tree4"></p>
<p><img src="/Users/lwen/Blog/source/images/br-tree3.tiff" alt="br-tree3"></p>
<p>当红黑树是这种情况的时候我们会发现他对应的 2-3 树是有问题的，需要进行分裂，所以说这里的颜色翻转就是把他的子节点都表示为黑色，自己变成红色。</p>
<h4 id="红黑树的插入操作"><a href="#红黑树的插入操作" class="headerlink" title="红黑树的插入操作"></a>红黑树的插入操作</h4><p>上面看到了关于红黑树的三个基本操作，这三个操作其实在我们插入的时候都是用的上的，并且重要的是在 AVL 树我们也可以仿照这种思想去完成平衡操作。</p>
<p><img src="/Users/lwen/Blog/source/images/br-tree6.tiff" alt="br-tree6"></p>
<p><img src="/Users/lwen/Blog/source/images/br-tree5.tiff" alt="br-tree5"></p>
<p>上面一般就是我们进行插入的时候会遇到的所有需要处理的情况。</p>
<ul>
<li><p>首先看第一个，就是在插入的时候 C 被插入到了左节点，这时候根据 2-3 树的规定我们是需要进行旋转的，把 C 作为根节点，就类似于把 C 提起来，然后 A 节点随着重力到达左节点。也就是左旋转</p>
</li>
<li><p>第二种情况就是左右节点都是红节点的时候我们就需要进行颜色翻转，让左右子节点都变黑，根节点边城红节点</p>
</li>
<li><p>第三种情况一开始属于有两个连续的红节点在左边，这时候我们仅仅需要进行一次右旋转就变成了第二种情况了</p>
</li>
<li><p>第四种情况我们可以看到首先进行一次左旋转，然后右旋转又再次回到了第二种情况，也就是颜色翻转</p>
<p>所以说无论怎么变化我们只需要这三个基本操作我们就可以保证红黑树的性质不被破坏。</p>
<p>所以最终的代码就是</p>
<p><img src="/Users/lwen/Blog/source/images/br-tree7.tiff" alt="br-tree7"></p>
<p>​</p>
</li>
</ul>
<p>我们的 AVL 树完全可以按照这个套路在接着写。</p>
]]></content>
      <categories>
        <category>-数据结构与算法</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>优先队列</title>
    <url>/2017/09/24/Algorithm/%E4%BC%98%E5%85%88%E9%98%9F%E5%88%97/</url>
    <content><![CDATA[<h3 id="优先队列基本介绍"><a href="#优先队列基本介绍" class="headerlink" title="优先队列基本介绍"></a>优先队列基本介绍</h3><p>​    优先队列又叫做堆，他是一种比较特殊的完全二叉树。所谓完全二叉树就是一层层的堆叠，本层不满就不能堆放下一层。并且优先队列还有一个特点就是如果他是大根堆那么父节点不小于子节点，如果是小根堆父节点不大于子节点。这也是一个递归定义。<a id="more"></a></p>
<p>为什么要是用优先队列？</p>
<ol>
<li>首先如果我们需要查找一个第 k 大的数字，毫无疑问这个是最方便的</li>
<li>他的插入操作和删除操作都是 logn 的复杂度，所以说他是最经济的方式</li>
</ol>
<h3 id="优先队列的常用操作"><a href="#优先队列的常用操作" class="headerlink" title="优先队列的常用操作"></a>优先队列的常用操作</h3><h4 id="插入"><a href="#插入" class="headerlink" title="插入"></a>插入</h4><p>插入的时候我们一般采用的方式就是上滤，也就是把要插入的元素放在最后面，然后比较让这个元素向上冒，知道正确的位置。</p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-9-24/38955717.jpg" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//插入</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">int</span> x)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (size &gt; ele.length - <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//不使用0号元素</span></span><br><span class="line">        ele[++size] = x;</span><br><span class="line">        swim(size);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//上滤操作</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">swim</span><span class="params">(<span class="keyword">int</span> index)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (index / <span class="number">2</span> &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">if</span> (ele[index] &gt; ele[index / <span class="number">2</span>]) &#123;</span><br><span class="line">                <span class="keyword">int</span> tep = ele[index];</span><br><span class="line">                ele[index] = ele[index / <span class="number">2</span>];</span><br><span class="line">                ele[index / <span class="number">2</span>] = tep;</span><br><span class="line">            &#125;</span><br><span class="line">            index = index / <span class="number">2</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h4 id="删除操作"><a href="#删除操作" class="headerlink" title="删除操作"></a>删除操作</h4><p>​    在删除的时候我们再删除了最上面的元素之后我们还需要调整堆的平衡，这个时候我们采取的策略就是下滤，首先用最后一个元素代替要删除的那个元素，然后对该元素进行下滤，直到平衡。</p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-9-24/30979864.jpg" alt></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//删除元素</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">deleteMax</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span> max = ele[<span class="number">1</span>];</span><br><span class="line">        ele[<span class="number">1</span>] = ele[size--];</span><br><span class="line">        sink(<span class="number">1</span>);</span><br><span class="line">        <span class="keyword">return</span> max;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//下滤</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">sink</span><span class="params">(<span class="keyword">int</span> i)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> index = i;</span><br><span class="line">        <span class="keyword">int</span> tmp = ele[i];</span><br><span class="line">        <span class="keyword">while</span> (i * <span class="number">2</span> &lt; size) &#123;</span><br><span class="line">            <span class="keyword">int</span> next = i * <span class="number">2</span>;</span><br><span class="line">            <span class="keyword">if</span> (ele[next + <span class="number">1</span>] &gt; ele[next]) &#123;</span><br><span class="line">                ++next;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (ele[next] &gt; tmp) &#123;</span><br><span class="line">                <span class="comment">// int temp = ele[index];</span></span><br><span class="line">                ele[index] = ele[next];</span><br><span class="line">                <span class="comment">// ele[next] = temp;</span></span><br><span class="line">                index = next;</span><br><span class="line">            &#125;</span><br><span class="line">            i = next;</span><br><span class="line">        &#125;</span><br><span class="line">        ele[index] = tmp;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="堆排序"><a href="#堆排序" class="headerlink" title="堆排序"></a>堆排序</h3><p>​    优先队列的很好的一个使用就是堆排序，他有比较好的性能，和优点。</p>
<p>堆排序分为两个步骤：</p>
<ol>
<li><p>首先我们需要把一个无序的数组构建成一个优先队列，这个过程我们是从下往上进行的，也就是从它有两个孩子的节点开始依次向上上滤操作。</p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-9-24/33562149.jpg" alt></p>
<p>这样我们就建立了一个完整的优先队列了，接下来就是类似于删除最大元素最小元素的问题了。</p>
</li>
<li><p>然后我们只需要把最大或者最小的元素同最后一个元素交换，然后再次下滤就可以了。这一步就类似于删除顶点元素，的步骤。</p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-9-24/33045791.jpg" alt></p>
<p>这样我们就完成了整个的堆排序</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HeapSort</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  	<span class="comment">//下滤操作	</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sink</span><span class="params">(<span class="keyword">int</span>[] arr,<span class="keyword">int</span> i,<span class="keyword">int</span> end)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> index = i;</span><br><span class="line">        <span class="keyword">int</span> tmp = arr[i];</span><br><span class="line">        <span class="keyword">while</span> (i * <span class="number">2</span> &lt;= end) &#123;</span><br><span class="line">            <span class="keyword">int</span> n = i * <span class="number">2</span>;</span><br><span class="line">            <span class="keyword">if</span> (n+<span class="number">1</span>&lt;=end&amp;&amp;arr[n] &gt; arr[n + <span class="number">1</span>]) &#123;</span><br><span class="line">                ++n;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (arr[n] &lt; tmp) &#123;</span><br><span class="line">                arr[index] = arr[n];</span><br><span class="line">                index = n;</span><br><span class="line">            &#125;</span><br><span class="line">            i = n;</span><br><span class="line">        &#125;</span><br><span class="line">        arr[index] = tmp;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sort</span><span class="params">(<span class="keyword">int</span>[] arr)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//构建堆</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = arr.length / <span class="number">2</span>; i &gt; <span class="number">0</span>; i--) &#123;</span><br><span class="line">            sink(arr, i, arr.length - <span class="number">1</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//从堆序生成顺序</span></span><br><span class="line">        <span class="keyword">int</span> size = arr.length - <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">while</span> (size &gt; <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">int</span> tmp = arr[<span class="number">1</span>];</span><br><span class="line">            arr[<span class="number">1</span>] = arr[size];</span><br><span class="line">            arr[size] = tmp;</span><br><span class="line">            --size;</span><br><span class="line">            sink(arr, <span class="number">1</span>, size);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] arr = <span class="keyword">new</span> <span class="keyword">int</span>[]&#123;<span class="number">0</span>, <span class="number">7</span>, <span class="number">2</span>, <span class="number">5</span>, <span class="number">8</span>, <span class="number">3</span>, <span class="number">9</span>&#125;;</span><br><span class="line">        sort(arr);</span><br><span class="line">        System.out.println(arr);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​</p>
</li>
</ol>
<h3 id="三大排序的分析"><a href="#三大排序的分析" class="headerlink" title="三大排序的分析"></a>三大排序的分析</h3><p>三大排序就是<code>快排</code>，<code>堆排序</code>，<code>归并排序</code>。</p>
<ol>
<li>堆排序的优势在于它能够在最坏的情况下使用 NLogN 的复杂度，并且在不借助辅助空间的情况下完成排序</li>
<li>归并当然也是最坏的情况为 NLogN 时间复杂度，但是他需要借助线性的辅助空间</li>
<li>快排虽然不需要借助空间，但是时间复杂度没有让人满意，最坏情况快排的复杂度到了平方级别。</li>
</ol>
<p>看起来貌似堆排序是最完美的排序算法，但是其实不是的，下面就是一些缺点：</p>
<ol>
<li>他的内部循环的次数要高于快排</li>
<li>在现代计算机上我们主存其实也不是很大的问题，这个时候归并其实也没什么不好</li>
<li>他是不稳定的排序算法</li>
</ol>
<h3 id="常见排序算法的复杂度"><a href="#常见排序算法的复杂度" class="headerlink" title="常见排序算法的复杂度"></a>常见排序算法的复杂度</h3><p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-9-24/87997922.jpg" alt></p>
]]></content>
      <categories>
        <category>-数据结构与算法</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>排序算法</title>
    <url>/2017/09/23/Algorithm/%E6%8E%92%E5%BA%8F%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[<h3 id="选择排序："><a href="#选择排序：" class="headerlink" title="选择排序："></a>选择排序：</h3><p>​    选择排序一般来说就是，我们 始终从中右边的序列中找出那些最小的元素，放在左侧，这样我们就能保证左边始终有序。</p>
<p>​    但是这个算法的复杂度比较高，为<br>$$<br>n^2/2<br>$$<br>​    那为什么是这个值，假若我们放一张 n x n 的表格，然后我们在排序的过程中用灰色表示不变的元素，然后用黑色表示变化的元素。这样一来我们会发现这个表格是一个以对角线分隔的一个矩阵。很统一看到我们进行了二分之一的 n^2 扫描。<a id="more"></a></p>
<p>​    这个算法的另外一个问题就是无论我们的序列本来是否有序，我们都需要不停地扫描，所以说这个不存在最好情况和最坏情况，一直都效率不高。</p>
<h3 id="插入排序："><a href="#插入排序：" class="headerlink" title="插入排序："></a>插入排序：</h3><p>​    插入排序的算法过程和选择排序类似，这是这个地方我们始终让左边的序列保持有序，然后把剩下的那些无序的元素一个个插入到有序序列之中。或许你已经明白了整整比较消耗时间的就是插入操作，我们又需要不停地移动着数组。但是这就有一个好处就是当我们序列比较有序的时候我们所做的操作非常少，甚至当序列完全有序的时候我们只需要进行 n 次比较而已。他的算法复杂度比上面的要好一点,为<br>$$<br>n^2/4<br>$$<br>同样的我们可以画一个矩阵，最终我们会发现有黑色全部在对角线下面，而且对角线下面只有一半是黑色的，因此就是 1/4     </p>
<h3 id="希尔排序："><a href="#希尔排序：" class="headerlink" title="希尔排序："></a>希尔排序：</h3><p>​    希尔排序事实上可以看做插入排序的一个变种，他是首先进行了分组，然后在组内进行了插入排序，这样改造后的性能提高了很多。重点就在于如何进行分组的问题，希尔自己提出的方法就是使用 n/3 也就是每一次步长减少 n/3 </p>
<h3 id="归并排序："><a href="#归并排序：" class="headerlink" title="归并排序："></a>归并排序：</h3><p>​    归并排序是比较有名的排序，这个排序的思想就是<strong><u>归</u></strong>和<strong><u>并</u></strong>，所谓的归就是递归，递归的去求左右子序列。而并就是最后需要合并这些子序列。这个算法一般需要一个辅助数组来支撑，也就是把两个子序列合并成一个新的数组的时候，需要这个辅助数组，最后还需要把他拷贝回去。下面是归并的流程图。</p>
<p>​    <img src="http://ojrw3x8j2.bkt.clouddn.com/17-9-23/35016136.jpg" alt></p>
<p>​    除了这种使用递归的方式我们还可以从下到上的进行合并操作，也就是我们合并操作不变，但是把递归改成循环了，这里就用到了双循环，首先两两排序，然后两两合并。有点类似于希尔排序了。但是这种方式的算法复杂度并没有改变依然就是  nlogn</p>
<p>​    对于这种比较高级的排序我们可以做一定的优化让他们支持所中情况下的排序。首先这个归并排序并不适合于少量的数据，少量的数据的时候我们使用分治循环是很不划算的，我们可以在归并排序前面添加一些判断条件，当数组的容量少于多少的时候我们可以采用插入排序，这样算法更具有通用性。</p>
<p>​    </p>
]]></content>
      <categories>
        <category>-数据结构与算法</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>并查集</title>
    <url>/2017/09/23/Algorithm/%E5%B9%B6%E6%9F%A5%E9%9B%86/</url>
    <content><![CDATA[<p>​    在我们需要判断某一些事物之间是否存在一定的关系的时候，我们最好的办法不是使用图而是使用并查集。因为我们关心的是他们之间是否有关系，而不是关心的他们到底存在怎样的关系。</p>
<p>​    并查集，简单来说就是 n 个集合，我们通过 union 操作来建立两个节点之间的关系。通过 connected 来判断两个节点之间的关系。那么现在我们知道了 并查集的基本操作就是 union 和 connected 。<a id="more"></a></p>
<h3 id="逻辑结构："><a href="#逻辑结构：" class="headerlink" title="逻辑结构："></a>逻辑结构：</h3><p>并查集一开始我们初始化都是初始化 n 个不相关的独立集合。然后我们在做 union 操作的时候也就是让两个集合进行合并，所谓的合并操作就是让 connected 操作在应用于这两个元素的时候能返回同一个集合我们就逻辑上认为他们处于同一个集合内。</p>
<h3 id="物理结构"><a href="#物理结构" class="headerlink" title="物理结构:"></a>物理结构:</h3><p>现在我们大致对逻辑结构有了了解，就是集合的合并，以及判断两个元素是否在同一个集合。对于这种问题我们可以使用一个数组来对元素进行存储。这里我们维护一个 id 数组，一开始我们数组存放的就都是自己的下标，这个时候我们认为他们相互独立，当我们需要进行合并操作的时候我们只需要把一个集合的元素 id 值改成另外一个集合的元素的下标即可。这时也许你已经想到了那么 connected 操作就是判断这个 id 内容是否相等就可以了。这里确实也如此！</p>
<h3 id="代码实现的第一个版本："><a href="#代码实现的第一个版本：" class="headerlink" title="代码实现的第一个版本："></a>代码实现的第一个版本：</h3><p>​    此时大概了解他们的物理结构和逻辑结构以后我们就可以开始写代码了。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">QuickFindUF</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] id;</span><br><span class="line"></span><br><span class="line">    QuickFindUF(<span class="keyword">int</span> N) &#123;</span><br><span class="line">        id = <span class="keyword">new</span> <span class="keyword">int</span>[N];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; id.length; i++) &#123;</span><br><span class="line">            id[i] = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">connected</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> id[p] == id[q];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">union</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> pid = id[p];</span><br><span class="line">        <span class="keyword">int</span> qid = id[q];</span><br><span class="line">        <span class="keyword">if</span> (pid == qid) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; id.length; i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (id[i] == pid) &#123;</span><br><span class="line">                id[i] = qid;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    可以看到以上的代码我们就是维护了一个 id 数组，然后我们根据这个 id 数组进行判断是否联通，以及使用它进行合并操作，也许你已经发现了这个代码的 connected 操作的时间复杂度是常数，但是 union 操作的代价却是 n平方（为什么是 n 平方而不是 n，因为每一次合并的代价是 n 而一般我们最坏情况需要进行 n 次的合并所以就是 n 平方数量级），所以说当我们在处理很大数据量的时候这个复杂大是完全不可以接受的，一般10亿数据在 n 平方的复杂度下面需要的时间大概是 30 年，而 n 复杂度可能就是几秒钟的事情，所以我们说 n 平方是不可接受的慢。</p>
<h3 id="代码实现的第二个版本："><a href="#代码实现的第二个版本：" class="headerlink" title="代码实现的第二个版本："></a>代码实现的第二个版本：</h3><p>​    好了上面我们已经分析了第一个版本的代码对于 union 操作的代价太高，所以我们不得不想办法对其进行优化，否则这个算法面对大量的数据是无法使用的。上面的第一个版本我们可以称之为 <code>快速查找</code> 因为他只用了常数的时间完成了操作。</p>
<p>​    接下来需要看一个<code>快速合并</code>的例子，来改进上述的代码。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">QuickUnionUF</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] id;</span><br><span class="line"></span><br><span class="line">    QuickUnionUF(<span class="keyword">int</span> N) &#123;</span><br><span class="line">        id = <span class="keyword">new</span> <span class="keyword">int</span>[N];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; id.length; i++) &#123;</span><br><span class="line">            id[i] = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">root</span><span class="params">(<span class="keyword">int</span> x)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (x != id[x]) &#123;</span><br><span class="line">            x = id[x];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">connected</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> root(p) == root(q);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">union</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (connected(p, q)) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        id[p] = q;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    这里我们改进的思想就是让这个集合成为一棵树，然后我们在合并的时候只需要调整一次根节点就好了，这样复杂度瞬间就降低了，但是有一个问题就是查找的复杂度变高了，因为此时我们存放的不是一个同一个根节点，而是有可能是一些中间节点，我们为了能够查找我们必须去寻找这个节点的根节点。如下图。无偶一我们需要一个 root 方法帮我们找到他们的根节点。<img src="http://ojrw3x8j2.bkt.clouddn.com/17-9-23/23759102.jpg" alt></p>
<p>​    现在合并只需要线性的时间，但是我们查找则需要 n 最坏的情况就是我们要找的节点刚好就一个子树的最下面的节点，而且这个树根只有一个子节点。也就是极左或者极右的那种。</p>
<h3 id="代码实现的第三个版本："><a href="#代码实现的第三个版本：" class="headerlink" title="代码实现的第三个版本："></a>代码实现的第三个版本：</h3><p>​    现在看起来这个数据结构已经比较完美了，合并和查找的时间都比较短，但是仔细想想在上面进行合并的时候我们并没有规定怎么合并，我们只是说了合并就是把一个树的根作为另外一棵树的子树。也许你已经想到了，有时候我们把一颗大的树作为小的树子树会导致树高度变高。这样显然是不明智的，比较好的做法就是把小树作为大树的子树。这里我们采用了权重，我们所说的权重就是<u><em>树的大小</em></u> 注意是树的<strong><u>大小</u></strong>而不是树的<strong><u>高度</u></strong>，这样我们就只需要再维护一个权重的数组就可以了，每次在合并的时候只需要进行一下判断就能避免树高度大幅度增加，导致不必要的查找开销。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">QuickUnionWeightImprove</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] id;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] size;</span><br><span class="line"></span><br><span class="line">    QuickUnionWeightImprove(<span class="keyword">int</span> N) &#123;</span><br><span class="line">        id = <span class="keyword">new</span> <span class="keyword">int</span>[N];</span><br><span class="line">        size = <span class="keyword">new</span> <span class="keyword">int</span>[N];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; id.length; i++) &#123;</span><br><span class="line">            id[i] = i;</span><br><span class="line">            size[i] = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">root</span><span class="params">(<span class="keyword">int</span> x)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (x != id[x]) &#123;</span><br><span class="line">            x = id[x];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">connected</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> root(p) == root(q);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">union</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> rootP = root(p);</span><br><span class="line">        <span class="keyword">int</span> rootQ = root(q);</span><br><span class="line">        <span class="keyword">if</span> (connected(p, q)) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (size[p] &gt; size[q]) &#123;</span><br><span class="line">            id[rootQ] = rootP;</span><br><span class="line">            size[p] += size[q];</span><br><span class="line">        &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">            id[rootP] = rootQ;</span><br><span class="line">            size[q] += size[p];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    <strong><em>好了这里需要注意的是</em></strong>：在进行了树的合并以后不要忘记修改根节点的树的大小（权重）</p>
<h3 id="代码实现的第四个版本："><a href="#代码实现的第四个版本：" class="headerlink" title="代码实现的第四个版本："></a>代码实现的第四个版本：</h3><p>​    上面的代码其实还可以再进行优化，这里提出的优化方案就是路径压缩。简单来说我们进行查找的时候我们可以把这个节点挂到祖父节点下面这样树的高度明显变低，查找起来很方便。然而这一切我们只需要一行代码就能搞定，并且不影响查找的正确性。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">QuickUnionWeightCompressImprove</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] id;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span>[] size;</span><br><span class="line"></span><br><span class="line">    QuickUnionWeightCompressImprove(<span class="keyword">int</span> N) &#123;</span><br><span class="line">        id = <span class="keyword">new</span> <span class="keyword">int</span>[N];</span><br><span class="line">        size = <span class="keyword">new</span> <span class="keyword">int</span>[N];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; id.length; i++) &#123;</span><br><span class="line">            id[i] = i;</span><br><span class="line">            size[i] = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">root</span><span class="params">(<span class="keyword">int</span> x)</span></span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (x!=id[x])&#123;</span><br><span class="line">            <span class="comment">//路径压缩只需要一行,这一行就是把节点向上移动一层</span></span><br><span class="line">            id[x]=id[id[x]];</span><br><span class="line">            x=id[x];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">connected</span><span class="params">(<span class="keyword">int</span> p,<span class="keyword">int</span> q)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> root(p) == root(q);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">union</span><span class="params">(<span class="keyword">int</span> p, <span class="keyword">int</span> q)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> root_p = root(p);</span><br><span class="line">        <span class="keyword">int</span> root_q = root(q);</span><br><span class="line">        <span class="keyword">if</span> (root_p == root_q) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (size[root_p] &gt; size[root_q]) &#123;</span><br><span class="line">            id[root_q] = root_p;</span><br><span class="line">        &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">            id[root_p] = root_q;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>​    好了现在代码看起来会比较完美了，该用的技巧我们都已经用上了，现在合并操作的时间复杂度是常数，而查找操作的复杂度则是 n+nlogn</p>
<h3 id="应用："><a href="#应用：" class="headerlink" title="应用："></a>应用：</h3><p>​    接下来一个并查集的小应用的例子，就是迷宫是否有解，我们就可以使用并查集来找最上面，和最下面一行之间是不是有联通的节点，如果有的话我们就能找到迷宫的解。</p>
<p>​    但是这样一来复杂度又上去了，因为我们最上面一层的 n 个节点和最下面一层 n 个节点都要进行 connected 操作，这样我们时间复杂度毫无疑问的成为了 n 平方 又是那个不可接受的慢，这里有一个技巧就是我们为最上面一行，最下面一行的 n 个节点分别建立一个父节点，这样一来我们只需要进行一次 connected 操作即可。</p>
]]></content>
      <categories>
        <category>-数据结构与算法</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
      </tags>
  </entry>
  <entry>
    <title>吐槽关于许鹏飞</title>
    <url>/2017/09/18/Life/%E5%90%90%E6%A7%BD%E5%85%B3%E4%BA%8E%E8%AE%B8%E9%B9%8F%E9%A3%9E/</url>
    <content><![CDATA[<p>​        今年的微机原理的老师竟然叫许鹏飞，这个我也没什么好吐槽的。本来上学期以来对伟明哥教的计算机组成原理还是蛮感兴趣的，最后成绩也还不错。没想到今年又学一点计算机的基本原理和体系结构能丰富一下自己对计算机的理解增长一点见识。不过到目前为止计算机的知识我都是没增长多少，对这个老师的态度真的不是很好。首先他说他是经管院的考研考到计算机专业的，当时还是挺佩服的。</p>
<a id="more"></a>

<p>​    然而他后来的种种举措导致我对他的影响非常不好：</p>
<ol>
<li><p>文科生+理科生的教学方式，自己理解的不是很透彻的就硬背，程序硬背，cup 总线结构硬背。我觉得既然教的是计算机就应该用like 的方式探讨<code>为什么要这样设计</code>，<code>作用是什么</code>，<code>流程是什么</code> 而不是一味的去背，真没啥用</p>
</li>
<li><p>超级愤青，仇视社会。上课没办法调动大家的积极性，然后就各种抱怨，各种不爽，说什么我们不努力什么的。这些话完全是废话，都是二十多岁的人人家该怎么走他不清楚吗，而且重要的是有时候听你在这废话不如自己看书来的快</p>
</li>
<li><p>最后一点也是让我最不爽的一点就是<strong>每节课都会点名点到我，让我起来回答问题。</strong>就因为我们名字一样吗？我就呵呵了，自己教的一塌糊涂，全靠自己背，还说我们不认真去背。</p>
</li>
</ol>
<p>​         今天又在学汇编，说实话这种东西很少会去在用了，除非你是做硬件的不然学这个真的浪费时间，没有任何学习的价值，首先汇编基本就是记流水账，先背会各个寄存器作用，中断什么的，记这些可能有用对理解操作系统什么的还行。主要是这个就是简单的堆砌，顶多能耍小聪明，<code>没有任何的设计可言</code>，<code>没有设计模式</code>，<code>没有异常</code>，<code>代码不可维护</code>，<code>不可扩展</code> 这样的语言除了在底层硬件上需要，难道我们还真的需要这样的语言去做设计做开发吗。</p>
<p>​    然后就是看到了一个外行人的写得非常烂的代码，虽然说那些教授写得代码可能不多，但是我觉得这个人的代码完全就是一个外行的随便拼凑的代码片段。所以说外行人真的还是和行内有区别的，理论多，背的多，但是你别忘了<strong>计算机就是一个应用性的学科，生来就是为了服务，不是为了背那些条条框框。</strong></p>
]]></content>
      <categories>
        <category>-小记</category>
      </categories>
      <tags>
        <tag>生活</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉树</title>
    <url>/2017/09/11/Algorithm/%E4%BA%8C%E5%8F%89%E6%A0%91/</url>
    <content><![CDATA[<h3 id="1-二叉树的性质"><a href="#1-二叉树的性质" class="headerlink" title="1.二叉树的性质"></a>1.二叉树的性质</h3><h4 id="1-具有-n-个节点的二叉树第-n-层最多2的-n-1-次方个节点"><a href="#1-具有-n-个节点的二叉树第-n-层最多2的-n-1-次方个节点" class="headerlink" title="1.具有 n 个节点的二叉树第 n 层最多2的 n-1 次方个节点"></a>1.具有 n 个节点的二叉树第 n 层最多2的 n-1 次方个节点</h4><h4 id="2-具有-n-个节点的二叉树最多有-2-的-n-次方减-1-个节点"><a href="#2-具有-n-个节点的二叉树最多有-2-的-n-次方减-1-个节点" class="headerlink" title="2.具有 n 个节点的二叉树最多有 2 的 n 次方减 1 个节点"></a>2.具有 n 个节点的二叉树最多有 2 的 n 次方减 1 个节点</h4><h4 id="3-度为-0-的节点数等于度为-2-的节点数加-1"><a href="#3-度为-0-的节点数等于度为-2-的节点数加-1" class="headerlink" title="3.度为 0 的节点数等于度为 2 的节点数加 1"></a>3.度为 0 的节点数等于度为 2 的节点数加 1</h4><a id="more"></a>

<blockquote>
<p>节点度的关系 n=n0+n1+n2<br>边的条数就是 n-1 ,也就是节点的关系的个数 另外一方面就是从父亲的方面来看就是利用度来计算  n0*0+n1*1+n2*2<br>也就是从不同的角度来理解这个东西获得一个等式从而得到的</p>
</blockquote>
<h3 id="2-两个小概念："><a href="#2-两个小概念：" class="headerlink" title="2.两个小概念："></a>2.两个小概念：</h3><p>满二叉树：所有的节点排满了<br>完全二叉树:按顺序从左向右排放可以不满</p>
<h3 id="3-线索化"><a href="#3-线索化" class="headerlink" title="3.线索化"></a>3.线索化</h3><h4 id="1-中序线索化"><a href="#1-中序线索化" class="headerlink" title="1.中序线索化"></a>1.中序线索化</h4><p>rtag=0  右子树最左边的节点<br>rtag=1  rchild 指向</p>
<p>ltag=0  左子树的最右边节点<br>ltag=1  lchild 指向</p>
<h3 id="4-树"><a href="#4-树" class="headerlink" title="4.树"></a>4.树</h3><p>任何一棵树我们如果使用孩子兄弟表示法的话  我们自然就会把这个树转化成一个二叉树</p>
<p>当然也可以使用双亲表示法，就是用map 然后记录双亲的位置<br>也可以使用孩子表示法就是使用一个索引链表<br>也可以把上面两个方法结合起来就是使用双亲孩子表示法</p>
<h3 id="5-层序遍历"><a href="#5-层序遍历" class="headerlink" title="5.层序遍历"></a>5.层序遍历</h3><p>就是先把根节点放在队列里面，然后只要这个队列不空的话，就访问这个节点然后出队，然后分别把这个节点的左右节点分别入队列</p>
<h3 id="6-中序和先序遍历的非递归的算法"><a href="#6-中序和先序遍历的非递归的算法" class="headerlink" title="6.中序和先序遍历的非递归的算法"></a>6.中序和先序遍历的非递归的算法</h3><p>使用一个栈，中序的话就是第二次经过这个节点的时候我们访问，先序就是第一次经过这个节点的时候去访问这个节点<br>因为一般来说我们知道他会进过两次，但是什么时候第三次经过我们并不知道。我们可以每个节点设置一个 mark 如果是第三次访问的时候我们就访问</p>
]]></content>
      <categories>
        <category>-数据结构</category>
      </categories>
      <tags>
        <tag>二叉树</tag>
      </tags>
  </entry>
  <entry>
    <title>HouseRobber</title>
    <url>/2017/09/07/Leet%20Code/HouseRobber/</url>
    <content><![CDATA[<blockquote>
<p>You are a professional robber planning to rob houses along a street. Each house has a certain amount of money stashed, the only constraint stopping you from robbing each of them is that adjacent houses have security system connected and it will automatically contact the police if two adjacent houses were broken into on the same night.<br>Given a list of non-negative integers representing the amount of money of each house, determine the maximum amount of money you can rob tonight without alerting the police.</p>
</blockquote>
<p>题目的意思大概就是有一个数组，数组里面装的就是正整数，你要去访问各个数组，但是这些数组相邻元素不可同时访问，从而获取最大的和</p>
<h3 id="思路"><a href="#思路" class="headerlink" title="思路"></a>思路</h3><p>还是 dp 动态规划 ：我们首先就想当前是头还是不偷，从而推断下一步，不管别的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HouseRobber</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">rob</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> no=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">int</span> yes=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> num : nums) &#123;</span><br><span class="line">            <span class="keyword">int</span> lastNo=no;</span><br><span class="line">            no=Math.max(yes,no);</span><br><span class="line">            yes=lastNo+num;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> Math.max(yes, no);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">rob1</span><span class="params">(<span class="keyword">int</span>[] nums)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[][] dp=<span class="keyword">new</span> <span class="keyword">int</span>[nums.length+<span class="number">1</span>][<span class="number">2</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; nums.length; i++) &#123;</span><br><span class="line">            dp[i][<span class="number">0</span>]=Math.max(dp[i-<span class="number">1</span>][<span class="number">0</span>],dp[i-<span class="number">1</span>][<span class="number">1</span>]);</span><br><span class="line">            dp[i][<span class="number">1</span>]=dp[i-<span class="number">1</span>][<span class="number">0</span>]+nums[i];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> Math.max(dp[nums.length][<span class="number">0</span>],dp[nums.length][<span class="number">1</span>]);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>-LeetCode</category>
      </categories>
      <tags>
        <tag>动态规划</tag>
      </tags>
  </entry>
  <entry>
    <title>IntersectionOfTwoLinkedLists</title>
    <url>/2017/09/07/Leet%20Code/IntersectionOfTwoLinkedLists/</url>
    <content><![CDATA[<pre>
Write a program to find the node at which the intersection of two singly linked lists begins.


For example, the following two linked lists:

A:          a1 → a2
                   ↘
                     c1 → c2 → c3
                   ↗
B:     b1 → b2 → b3
begin to intersect at node c1.


Notes:

If the two linked lists have no intersection at all, return null.
The linked lists must retain their original structure after the function returns.
You may assume there are no cycles anywhere in the entire linked structure.
Your code should preferably run in O(n) time and use only O(1) memory.
Credits:
Special thanks to @stellari for adding this problem and creating all test cases.
</pre>

<h3 id="两个思路"><a href="#两个思路" class="headerlink" title="两个思路:"></a>两个思路:</h3><h5 id="一个就是把链表转化成一个有环链表-这样的话我们就可以当做那个有环的求入点问题了-但是人家说了不能修改链表的结构很抱歉没办法使用"><a href="#一个就是把链表转化成一个有环链表-这样的话我们就可以当做那个有环的求入点问题了-但是人家说了不能修改链表的结构很抱歉没办法使用" class="headerlink" title="一个就是把链表转化成一个有环链表  这样的话我们就可以当做那个有环的求入点问题了  但是人家说了不能修改链表的结构很抱歉没办法使用"></a>一个就是把链表转化成一个有环链表  这样的话我们就可以当做那个有环的求入点问题了  但是人家说了不能修改链表的结构很抱歉没办法使用</h5><h5 id="第二个思路就是既然是这个入点是重复的那么我们就找出重复元素即可-这样自然想到了-hash-表"><a href="#第二个思路就是既然是这个入点是重复的那么我们就找出重复元素即可-这样自然想到了-hash-表" class="headerlink" title="第二个思路就是既然是这个入点是重复的那么我们就找出重复元素即可  这样自然想到了 hash 表"></a>第二个思路就是既然是这个入点是重复的那么我们就找出重复元素即可  这样自然想到了 hash 表</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.HashSet;</span><br><span class="line"><span class="keyword">import</span> java.util.Set;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">IntersectionOfTwoLinkedLists_1</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ListNode <span class="title">getIntersectionNode</span><span class="params">(ListNode headA, ListNode headB)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (headA==<span class="keyword">null</span>||headB==<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        ListNode circle=headA;</span><br><span class="line">        ListNode fast=headB;</span><br><span class="line">        ListNode slow=headB;</span><br><span class="line">        <span class="keyword">while</span> (circle.next!=<span class="keyword">null</span>)&#123;</span><br><span class="line">            circle=circle.next;</span><br><span class="line">        &#125;</span><br><span class="line">        circle.next=headA;</span><br><span class="line">        <span class="keyword">while</span> (fast!=<span class="keyword">null</span>&amp;&amp;slow!=<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span> (fast.next!=<span class="keyword">null</span>&amp;&amp;fast.next.next!=<span class="keyword">null</span>)&#123;</span><br><span class="line">                fast=fast.next.next;</span><br><span class="line">            &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (slow.next!=<span class="keyword">null</span>)&#123;</span><br><span class="line">                slow=slow.next;</span><br><span class="line">            &#125;<span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">if</span> (fast==slow)&#123;</span><br><span class="line">                ListNode x=fast;</span><br><span class="line">                ListNode y=headB;</span><br><span class="line">                <span class="keyword">while</span>(x!=<span class="keyword">null</span>&amp;&amp;y!=<span class="keyword">null</span>)&#123;</span><br><span class="line">                    x=x.next;</span><br><span class="line">                    y=y.next;</span><br><span class="line">                    <span class="keyword">if</span> (x==y)&#123;</span><br><span class="line">                        <span class="keyword">return</span> x;</span><br><span class="line">                    &#125;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">IntersectionOfTwoLinkedLists</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> ListNode <span class="title">getIntersectionNode</span><span class="params">(ListNode headA, ListNode headB)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (headA==<span class="keyword">null</span>||headB==<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        Set&lt;ListNode&gt; nodes=<span class="keyword">new</span> HashSet&lt;&gt;();</span><br><span class="line">        ListNode listA=headA;</span><br><span class="line">        ListNode listB=headB;</span><br><span class="line">        <span class="keyword">while</span> (listA!=<span class="keyword">null</span>)&#123;</span><br><span class="line">            nodes.add(listA);</span><br><span class="line">            listA=listA.next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (listB!=<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">if</span> (!nodes.add(listB))&#123;</span><br><span class="line">                <span class="keyword">return</span> listB;</span><br><span class="line">            &#125;</span><br><span class="line">            listB=listB.next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">null</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-LeetCode</category>
      </categories>
      <tags>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>归并排序</title>
    <url>/2017/09/05/Algorithm/%E5%BD%92%E5%B9%B6%E6%8E%92%E5%BA%8F/</url>
    <content><![CDATA[<p>归并排序是建立在归并操作上的一种有效的排序算法。该算法是采用分治法（Divide and Conquer）的一个非常典型的应用。</p>
<p>首先考虑下如何将将二个有序数列合并。<a id="more"></a>这个非常简单，只要从比较二个数列的第一个数，谁小就先取谁，取了后就在对应数列中删除这个数。然后再进行比较，如果有数列为空，那直接将另一个数列的数据依次取出即可。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//将有序数组a[]和b[]合并到c[]中  </span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">MemeryArray</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> n, <span class="keyword">int</span> b[], <span class="keyword">int</span> m, <span class="keyword">int</span> c[])</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">int</span> i, j, k;  </span><br><span class="line">  </span><br><span class="line">    i = j = k = <span class="number">0</span>;  </span><br><span class="line">    <span class="keyword">while</span> (i &lt; n &amp;&amp; j &lt; m)  </span><br><span class="line">    &#123;  </span><br><span class="line">        <span class="keyword">if</span> (a[i] &lt; b[j])  </span><br><span class="line">            c[k++] = a[i++];  </span><br><span class="line">        <span class="keyword">else</span>  </span><br><span class="line">            c[k++] = b[j++];   </span><br><span class="line">    &#125;  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">while</span> (i &lt; n)  </span><br><span class="line">        c[k++] = a[i++];  </span><br><span class="line">  </span><br><span class="line">    <span class="keyword">while</span> (j &lt; m)  </span><br><span class="line">        c[k++] = b[j++];  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>可以看出合并有序数列的效率是比较高的，可以达到O(n)。</p>
<p>解决了上面的合并有序数列问题，再来看归并排序，其的基本思路就是将数组分成二组A，B，如果这二组组内的数据都是有序的，那么就可以很方便的将这二组数据进行排序。如何让这二组组内数据有序了？</p>
<p>可以将A，B组各自再分成二组。依次类推，当分出来的小组只有一个数据时，可以认为这个小组组内已经达到了有序，然后再合并相邻的二个小组就可以了。这样通过先递归的分解数列，再合并数列就完成了归并排序。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//将有二个有序数列a[first...mid]和a[mid...last]合并。  </span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mergearray</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> first, <span class="keyword">int</span> mid, <span class="keyword">int</span> last, <span class="keyword">int</span> temp[])</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">int</span> i = first, j = mid + <span class="number">1</span>;  </span><br><span class="line">    <span class="keyword">int</span> m = mid,   n = last;  </span><br><span class="line">    <span class="keyword">int</span> k = <span class="number">0</span>;  </span><br><span class="line">      </span><br><span class="line">    <span class="keyword">while</span> (i &lt;= m &amp;&amp; j &lt;= n)  </span><br><span class="line">    &#123;  </span><br><span class="line">        <span class="keyword">if</span> (a[i] &lt;= a[j])  </span><br><span class="line">            temp[k++] = a[i++];  </span><br><span class="line">        <span class="keyword">else</span>  </span><br><span class="line">            temp[k++] = a[j++];  </span><br><span class="line">    &#125;  </span><br><span class="line">      </span><br><span class="line">    <span class="keyword">while</span> (i &lt;= m)  </span><br><span class="line">        temp[k++] = a[i++];  </span><br><span class="line">      </span><br><span class="line">    <span class="keyword">while</span> (j &lt;= n)  </span><br><span class="line">        temp[k++] = a[j++];  </span><br><span class="line">      </span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; k; i++)  </span><br><span class="line">        a[first + i] = temp[i];  </span><br><span class="line">&#125;  </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">mergesort</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> first, <span class="keyword">int</span> last, <span class="keyword">int</span> temp[])</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">if</span> (first &lt; last)  </span><br><span class="line">    &#123;  </span><br><span class="line">        <span class="keyword">int</span> mid = (first + last) / <span class="number">2</span>;  </span><br><span class="line">        mergesort(a, first, mid, temp);    <span class="comment">//左边有序  </span></span><br><span class="line">        mergesort(a, mid + <span class="number">1</span>, last, temp); <span class="comment">//右边有序  </span></span><br><span class="line">        mergearray(a, first, mid, last, temp); <span class="comment">//再将二个有序数列合并  </span></span><br><span class="line">    &#125;  </span><br><span class="line">&#125;  </span><br><span class="line">  </span><br><span class="line"><span class="function">bool <span class="title">MergeSort</span><span class="params">(<span class="keyword">int</span> a[], <span class="keyword">int</span> n)</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="keyword">int</span> *p = <span class="keyword">new</span> <span class="keyword">int</span>[n];  </span><br><span class="line">    <span class="keyword">if</span> (p == NULL)  </span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;  </span><br><span class="line">    mergesort(a, <span class="number">0</span>, n - <span class="number">1</span>, p);  </span><br><span class="line">    delete[] p;  </span><br><span class="line">    <span class="keyword">return</span> <span class="keyword">true</span>;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>归并排序的效率是比较高的，设数列长为N，将数列分开成小数列一共要logN步，每步都是一个合并有序数列的过程，时间复杂度可以记为O(N)，故一共为O(N<em>logN)。因为归并排序每次都是在相邻的数据中进行操作，所以归并排序在O(N</em>logN)的几种排序方法（快速排序，归并排序，希尔排序，堆排序）也是效率比较高的。</p>
]]></content>
      <categories>
        <category>-LeetCode</category>
      </categories>
      <tags>
        <tag>排序</tag>
      </tags>
  </entry>
  <entry>
    <title>LinkedListCycle</title>
    <url>/2017/09/04/Leet%20Code/LinkedListCycle/</url>
    <content><![CDATA[<pre>
Given a linked list, determine if it has a cycle in it.

Follow up:
Can you solve it without using extra space?
</pre>

<h4 id="分析"><a href="#分析" class="headerlink" title="分析"></a>分析</h4><p> 一开始使用了复杂度O(n^2)的方法，使用两个指针a, b。a从表头开始一步一步往前走，遇到null则说明没有环，返回false；a每走一步，b从头开始走，如果遇到b==a.next，则说明有环true，如果遇到b==a，则说明暂时没有环，继续循环。</p>
<p>后来找到了复杂度O(n)的方法，使用两个指针slow,fast。两个指针都从表头开始走，slow每次走一步，fast每次走两步，如果fast遇到null，则说明没有环，返回false；如果slow==fast，说明有环，并且此时fast超了slow一圈，返回true。</p>
<p>为什么有环的情况下二者一定会相遇呢？因为fast先进入环，在slow进入之后，如果把slow看作在前面，fast在后面每次循环都向slow靠近1，所以一定会相遇，而不会出现fast直接跳过slow的情况。</p>
<h4 id="扩展问题"><a href="#扩展问题" class="headerlink" title="扩展问题"></a>扩展问题</h4><p>在网上搜集了一下这个问题相关的一些问题，思路开阔了不少，总结如下：</p>
<ol>
<li><p>环的长度是多少？</p>
</li>
<li><p>如何找到环中第一个节点（即Linked List Cycle II）？</p>
</li>
<li><p>如何将有环的链表变成单链表（解除环）？</p>
</li>
<li><p>如何判断两个单链表是否有交点？如何找到第一个相交的节点？</p>
</li>
</ol>
<p>首先我们看下面这张图：</p>
<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-9-4/88124012.jpg" alt></p>
<p>设：链表头是X，环的第一个节点是Y，slow和fast第一次的交点是Z。各段的长度分别是a,b,c，如图所示。环的长度是L。slow和fast的速度分别是qs,qf。</p>
<p>下面我们来挨个问题分析。</p>
<ol>
<li>方法一（网上都是这个答案）：</li>
</ol>
<p>第一次相遇后，让slow,fast继续走，记录到下次相遇时循环了几次。因为当fast第二次到达Z点时，fast走了一圈，slow走了半圈，而当fast第三次到达Z点时，fast走了两圈，slow走了一圈，正好还在Z点相遇。</p>
<p>方法二：</p>
<p>第一次相遇后，让fast停着不走了，slow继续走，记录到下次相遇时循环了几次。</p>
<p>方法三（最简单）：</p>
<p>第一次相遇时slow走过的距离：a+b，fast走过的距离：a+b+c+b。</p>
<p>因为fast的速度是slow的两倍，所以fast走的距离是slow的两倍，有 2(a+b) = a+b+c+b，可以得到a=c（这个结论很重要！）。</p>
<p>我们发现L=b+c=a+b，也就是说，从一开始到二者第一次相遇，循环的次数就等于环的长度。</p>
<ol start="2">
<li><p>我们已经得到了结论a=c，那么让两个指针分别从X和Z开始走，每次走一步，那么正好会在Y相遇！也就是环的第一个节点。</p>
</li>
<li><p>在上一个问题的最后，将c段中Y点之前的那个节点与Y的链接切断即可。</p>
</li>
<li><p>如何判断两个单链表是否有交点？先判断两个链表是否有环，如果一个有环一个没环，肯定不相交；如果两个都没有环，判断两个列表的尾部是否相等；如果两个都有环，判断一个链表上的Z点是否在另一个链表上。</p>
</li>
</ol>
<p>如何找到第一个相交的节点？求出两个链表的长度L1,L2（如果有环，则将Y点当做尾节点来算），假设L1&lt;L2，用两个指针分别从两个链表的头部开始走，长度为L2的链表先走(L2-L1)步，然后两个一起走，直到二者相遇。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LinkedListCycle</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">hasCycle</span><span class="params">(ListNode head)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (head==<span class="keyword">null</span>||head.next==<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        ListNode outer=head;</span><br><span class="line">        <span class="keyword">int</span> outCount=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> (outer.next!=<span class="keyword">null</span>)&#123;</span><br><span class="line">            outer=outer.next;</span><br><span class="line">            ListNode current=outer;</span><br><span class="line"></span><br><span class="line">            outCount++;</span><br><span class="line">            ListNode inner=head;</span><br><span class="line">            <span class="keyword">int</span> inCount=<span class="number">0</span>;</span><br><span class="line">            <span class="keyword">while</span> (inCount&lt;outCount)&#123;</span><br><span class="line">                <span class="keyword">if</span> (inner==current)&#123;</span><br><span class="line">                    <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                inner=inner.next;</span><br><span class="line">                inCount++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">hasCycle_1</span><span class="params">(ListNode head)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (head==<span class="keyword">null</span>||head.next==<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        ListNode fast=head;</span><br><span class="line">        ListNode slow=head;</span><br><span class="line">        <span class="keyword">while</span> (fast!=<span class="keyword">null</span>||fast.next!=<span class="keyword">null</span>)&#123;</span><br><span class="line">            fast=fast.next;</span><br><span class="line">            <span class="keyword">if</span> (fast==<span class="keyword">null</span>)&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            fast=fast.next;</span><br><span class="line">            <span class="keyword">if</span> (fast==<span class="keyword">null</span>)&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            slow=slow.next;</span><br><span class="line">            <span class="keyword">if</span> (fast==slow)&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        ListNode n1=<span class="keyword">new</span> ListNode(<span class="number">1</span>);</span><br><span class="line">        ListNode n2=<span class="keyword">new</span> ListNode(<span class="number">2</span>);</span><br><span class="line">        n1.next=n2;</span><br><span class="line">        n2.next=<span class="keyword">null</span>;</span><br><span class="line">        System.out.println(hasCycle_1(n1));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-LeetCode</category>
      </categories>
      <tags>
        <tag>链表</tag>
      </tags>
  </entry>
  <entry>
    <title>WordBreak</title>
    <url>/2017/09/04/Leet%20Code/WordBreak/</url>
    <content><![CDATA[<pre>
Given a non-empty string s and a dictionary wordDict containing a list of non-empty words, determine if s can be segmented into a space-separated sequence of one or more dictionary words. You may assume the dictionary does not contain duplicate words.

For example, given
s = "leetcode",
dict = ["leet", "code"].

Return true because "leetcode" can be segmented as "leet code".
</pre>
<p>这道题的思路是这样的，首先设置一个比字符串长一的数组，这个数组里面多的那个一就是存放空格的位置<br>假设我们空格在某个位置，然后这个数组的空格前一个位置表示空格之前的数组是字典里面的内容，然后<br>空格后面的字串被包含在字典里那么就说明在这个字串就是字典里面的内容</p>
<p>这道题说白了也是一个动态规划的问题，这类问题的思想就是存储上一步骤的东西，然后根据上一步骤的东西推算出下一步骤的结论<br>这里的存储的上一步骤的东西就是那个布尔数组，然后根据递推公式也就是 if 判断里面的东西得出本步骤的结论</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WordBreak</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">wordBreak</span><span class="params">(String s, List&lt;String&gt; wordDict)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (s.length() == <span class="number">0</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">boolean</span>[] res = <span class="keyword">new</span> <span class="keyword">boolean</span>[s.length() + <span class="number">1</span>];</span><br><span class="line">        res[<span class="number">0</span>] = <span class="keyword">true</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; s.length(); i++) &#123;</span><br><span class="line">            StringBuilder stringBuilder = <span class="keyword">new</span> StringBuilder(s.substring(<span class="number">0</span>, i + <span class="number">1</span>));</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt;= i; j++) &#123;</span><br><span class="line">                <span class="keyword">if</span> (res[j] &amp;&amp; wordDict.contains(stringBuilder.toString())) &#123;</span><br><span class="line">                    res[i + <span class="number">1</span>] = <span class="keyword">true</span>;</span><br><span class="line">                    <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">                stringBuilder.deleteCharAt(<span class="number">0</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> res[s.length()];</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-LeetCode</category>
      </categories>
      <tags>
        <tag>动态规划</tag>
      </tags>
  </entry>
  <entry>
    <title>BinaryTreeLevelOrderTraversal树的层序遍历</title>
    <url>/2017/09/03/Leet%20Code/BinaryTreeLevelOrderTraversal%E6%A0%91%E7%9A%84%E5%B1%82%E5%BA%8F%E9%81%8D%E5%8E%86/</url>
    <content><![CDATA[<pre>
Given a binary tree, return the level order traversal of its nodes' values. (ie, from left to right, level by level).

For example:
Given binary tree [3,9,20,null,null,15,7],
    3
   / \
  9  20
    /  \
   15   7
return its level order traversal as:
[
  [3],
  [9,20],
  [15,7]
]
</pre>


<p>基本思想就是每一次遍历的时候看当前的层数是不是大于已有的 list 集合数，不大于则需要拓展<br>否则就把获取当前层的 list 集合，然后王进放元素即可。这里需要注意的就是在这个种递归问题中<br>我们最重要的是考虑当前的问题，而不要考虑到下一层，否则会越来越乱。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">BinaryTreeLevelOrderTraversal</span> </span>&#123;</span><br><span class="line">    <span class="keyword">public</span> List&lt;List&lt;Integer&gt;&gt; levelOrder(TreeNode root) &#123;</span><br><span class="line">        List&lt;List&lt;Integer&gt;&gt; result=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line"></span><br><span class="line">        levelOrder(root,result,<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">levelOrder</span><span class="params">(TreeNode root, List&lt;List&lt;Integer&gt;&gt; result, <span class="keyword">int</span> level)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root==<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (level&gt;result.size()-<span class="number">1</span>)&#123;</span><br><span class="line">            result.add(<span class="keyword">new</span> ArrayList&lt;&gt;());</span><br><span class="line">        &#125;</span><br><span class="line">        List&lt;Integer&gt; list=result.get(level);</span><br><span class="line">        <span class="keyword">if</span> (root!=<span class="keyword">null</span>)&#123;</span><br><span class="line">            list.add(root.val);</span><br><span class="line">        &#125;</span><br><span class="line">        levelOrder(root.left,result,level+<span class="number">1</span>);</span><br><span class="line">        levelOrder(root.right,result,level+<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-LeetCode</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
      </tags>
  </entry>
  <entry>
    <title>SymmetricTree</title>
    <url>/2017/09/03/Leet%20Code/SymmetricTree/</url>
    <content><![CDATA[<pre>
Given a binary tree, check whether it is a mirror of itself (ie, symmetric around its center).

For example, this binary tree [1,2,2,3,4,4,3] is symmetric:

1
/ \
2   2
/ \ / \
3  4 4  3
But the following [1,2,2,null,3,null,3] is not:
1
/ \
2   2
\   \
3    3
</pre>

<p>错误的解法，就是遇到多个 null 值的时候就会出问题，也就是在有的时候虽然他不对称，<br>但是终须遍历的结果表明他就是对称的</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> org.junit.jupiter.api.Test;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"><span class="keyword">import</span> java.util.List;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SymmetricTree</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isSymmetric</span><span class="params">(TreeNode root)</span> </span>&#123;</span><br><span class="line">        List&lt;TreeNode&gt; list = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        inOrderTraversal(root, list);</span><br><span class="line">        <span class="keyword">int</span> center = list.size() / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">return</span> compare(list, center);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">compare</span><span class="params">(List&lt;TreeNode&gt; list, <span class="keyword">int</span> center)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>, j = list.size() - <span class="number">1</span>; i &lt; center; i++, j--) &#123;</span><br><span class="line">            <span class="keyword">if</span> (list.get(i).val != list.get(j).val) &#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">inOrderTraversal</span><span class="params">(TreeNode root, List&lt;TreeNode&gt; list)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root == <span class="keyword">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        inOrderTraversal(root.left, list);</span><br><span class="line">        list.add(root);</span><br><span class="line">        inOrderTraversal(root.right, list);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>好的解法就是使用递归，所谓对称简单来说就是 root 的左孩子和右孩子要一样才行</p>
]]></content>
      <categories>
        <category>-LeetCode</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
      </tags>
  </entry>
  <entry>
    <title>ValidateBinarySearchTree</title>
    <url>/2017/09/02/Leet%20Code/ValidateBinarySearchTree/</url>
    <content><![CDATA[<p>import org.junit.jupiter.api.Test;<br>import org.omg.CORBA.INTERNAL;</p>
<pre>
   Given a binary tree, determine if it is a valid binary search tree (BST).

     Assume a BST is defined as follows:

     The left subtree of a node contains only nodes with keys less than the node's key.
     The right subtree of a node contains only nodes with keys greater than the node's key.
     Both the left and right subtrees must also be binary search trees.
     Example 1:
     2
     / \
     1   3
     Binary tree [2,1,3], return true.
     Example 2:
     1
     / \
     2   3
     Binary tree [1,2,3], return false.
</pre>

<p>错误解法，只判断了每一个小的子树是搜索二叉树，而忽略了整个 root 的情况</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ValidateBinarySearchTree</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isValidBST</span><span class="params">(TreeNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root==<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (root.left==<span class="keyword">null</span>&amp;&amp;root.right==<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">int</span> leftValue=(root.left==<span class="keyword">null</span>)?Integer.MIN_VALUE:root.left.val;</span><br><span class="line">        <span class="keyword">int</span> rightValue=(root.right==<span class="keyword">null</span>)? Integer.MAX_VALUE:root.right.val;</span><br><span class="line">        <span class="keyword">if</span>(leftValue&lt;root.val&amp;&amp;rightValue&gt;root.val)&#123;</span><br><span class="line">            <span class="keyword">return</span> isValidBST(root.left)&amp;&amp;isValidBST(root.right);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        TreeNode root=<span class="keyword">new</span> TreeNode(<span class="number">0</span>);</span><br><span class="line">        TreeNode left=<span class="keyword">new</span> TreeNode(-<span class="number">1</span>);</span><br><span class="line">        TreeNode right=<span class="keyword">new</span> TreeNode(<span class="number">3</span>);</span><br><span class="line">        root.left=left;</span><br><span class="line">        <span class="comment">// root.right=right;</span></span><br><span class="line">        System.out.println(isValidBST(root));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>通过的代码就是使用的搜索二叉树的中序遍历，他的终须遍历的话刚好出来结果就是<br>左边的树必须小于右边的数，这样就能很轻松的判断出来是否合法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ValidateBinarySearchTree</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isValidBST</span><span class="params">(TreeNode root)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root==<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (root.left==<span class="keyword">null</span>&amp;&amp;root.right==<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        List&lt;TreeNode&gt; list=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        inOrderTraversal(root,list);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; list.size(); i++) &#123;</span><br><span class="line">            <span class="keyword">if</span> (list.get(i).val &lt;= list.get(i - <span class="number">1</span>).val)&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">inOrderTraversal</span><span class="params">(TreeNode root,List&lt;TreeNode&gt; list)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (root==<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">return</span> ;</span><br><span class="line">        &#125;</span><br><span class="line">        inOrderTraversal(root.left,list);</span><br><span class="line">        list.add(root);</span><br><span class="line">        inOrderTraversal(root.right,list);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        TreeNode root=<span class="keyword">new</span> TreeNode(<span class="number">0</span>);</span><br><span class="line">        TreeNode left=<span class="keyword">new</span> TreeNode(-<span class="number">1</span>);</span><br><span class="line">        TreeNode right=<span class="keyword">new</span> TreeNode(<span class="number">3</span>);</span><br><span class="line">        root.left=left;</span><br><span class="line">        <span class="comment">// root.right=right;</span></span><br><span class="line">        System.out.println(isValidBST(root));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>以及终须遍历衍生出来的新方法，也就是不使用一个 list 集合来存储前后之间的关系</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ValidateBinarySearchTree2</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    TreeNode pre=<span class="keyword">null</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">isValidBST</span><span class="params">(TreeNode root)</span> </span>&#123;</span><br><span class="line">        <span class="comment">//这是一个中序遍历。然后我们在每次遍历的时候都存储了前一个元素，避免了使用一个 list 集合</span></span><br><span class="line">        <span class="keyword">if</span> (root!=<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="comment">//首先遍历左子树，左子树出问题直接 false</span></span><br><span class="line">            <span class="keyword">if</span> (!isValidBST(root.left))&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//核心判断条件，是不是前面的数大于后面的数</span></span><br><span class="line">            <span class="keyword">if</span> (pre!=<span class="keyword">null</span>&amp;&amp;pre.val&gt;=root.val)&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">//最后就是右子树，右子树由于是最后一道所以可以直接返回他的结果</span></span><br><span class="line">            <span class="keyword">return</span> isValidBST(root.right);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span>&#123;</span><br><span class="line">        TreeNode root=<span class="keyword">new</span> TreeNode(<span class="number">0</span>);</span><br><span class="line">        TreeNode left=<span class="keyword">new</span> TreeNode(-<span class="number">1</span>);</span><br><span class="line">        TreeNode right=<span class="keyword">new</span> TreeNode(<span class="number">3</span>);</span><br><span class="line">        root.left=left;</span><br><span class="line">        <span class="comment">// root.right=right;</span></span><br><span class="line">        System.out.println(isValidBST(root));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
  </entry>
  <entry>
    <title>UniqueBinarySearchTreesII</title>
    <url>/2017/09/02/Leet%20Code/UniqueBinarySearchTreesII/</url>
    <content><![CDATA[<p>import org.junit.jupiter.api.Test;</p>
<p>题目的描述如下</p>
<pre>
 * Given n, how many structurally unique BST's (binary search trees) that store values 1...n?

     For example,
     Given n = 3, there are a total of 5 unique BST's.

     1         3     3      2      1
     \       /     /      / \      \
     3     2     1      1   3      2
     /     /       \                 \
     2     1         2                 3
</pre>

<p>也就是让生成搜索二叉树的所有可能情况，用一种遍历方式放在 list 集合中展示出来</p>
<p>这个地方的基本思路就是把这个问题分解成子问题，也就是先找出一个 root 节点，然后分别对左右的子树进行同样的查找操作，<br>这里的代码比较巧妙的一点就是他一开始存放的并不是已经遍历过的树，而是只存放了这个根结点，根结点的左右子树都放在了根结点的<br>内部了，所以最后才会有一个双重循环说白了就是循环的是当选这个 root 节点的时候左右子树的所有情况</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TreeNode</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> val;</span><br><span class="line">    TreeNode left;</span><br><span class="line">    TreeNode right;</span><br><span class="line"></span><br><span class="line">    TreeNode(<span class="keyword">int</span> x) &#123;</span><br><span class="line">        val = x;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UniqueBinarySearchTreesII</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;TreeNode&gt; <span class="title">generateTrees</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (n &lt; <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">new</span> ArrayList&lt;TreeNode&gt;();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> generate(<span class="number">1</span>, n);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> List&lt;TreeNode&gt; <span class="title">generate</span><span class="params">(<span class="keyword">int</span> start, <span class="keyword">int</span> end)</span> </span>&#123;</span><br><span class="line">        List&lt;TreeNode&gt; lists = <span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        <span class="keyword">if</span> (start &gt; end) &#123;</span><br><span class="line">            lists.add(<span class="keyword">null</span>);</span><br><span class="line">            <span class="keyword">return</span> lists;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = start; i &lt;= end; i++) &#123;</span><br><span class="line">            List&lt;TreeNode&gt; leftTree = generate(start, i - <span class="number">1</span>); <span class="comment">//里面的存储结构其实就是只存了一个子树的 root 节点然后，root 节点里面带的有左右子节点的关系</span></span><br><span class="line">            List&lt;TreeNode&gt; rightTree = generate(i+<span class="number">1</span>, end);</span><br><span class="line">            <span class="keyword">for</span> (TreeNode leftNode : leftTree) &#123;</span><br><span class="line">                <span class="keyword">for</span> (TreeNode rightNode : rightTree) &#123;</span><br><span class="line">                    TreeNode root = <span class="keyword">new</span> TreeNode(i);</span><br><span class="line">                    root.left = leftNode;</span><br><span class="line">                    root.right = rightNode;</span><br><span class="line">                    lists.add(root);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> lists;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="comment">// generateTrees(3);</span></span><br><span class="line">        System.out.println(Arrays.toString(generateTrees(<span class="number">3</span>).toArray()));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-LeetCode</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
      </tags>
  </entry>
  <entry>
    <title>卡塔兰数列[UniqueBinarySearchTrees]</title>
    <url>/2017/09/01/Leet%20Code/%E5%8D%A1%E5%A1%94%E5%85%B0%E6%95%B0%E5%88%97%5BUniqueBinarySearchTrees%5D/</url>
    <content><![CDATA[<p>Given n, how many structurally unique BST’s (binary search trees) that store values 1…n?</p>
<p>For example,<br>Given n = 3, there are a total of 5 unique BST’s.</p>
<pre>

   1         3     3      2      1
    \       /     /      / \      \
     3     2     1      1   3      2
    /     /       \                 \
   2     1         2                 3

</pre>

<p>这道题实际上是 Catalan Number卡塔兰数的一个例子，我们先来看当 n = 1的情况，只能形成唯一的一棵二叉搜索树，n分别为1,2,3的情况如下所示：</p>
 <pre>
                    1                        n = 1

                2        1                   n = 2
               /          \
              1            2

   1         3     3      2      1           n = 3
    \       /     /      / \      \
     3     2     1      1   3      2
    /     /       \                 \
   2     1         2                 3
</pre>

<p>就跟斐波那契数列一样，我们把n = 0 时赋为1，因为空树也算一种二叉搜索树，那么n = 1时的情况可以看做是其左子树个数乘以右子树的个数，左右字数都是空树，所以1乘1还是1。那么n = 2时，由于1和2都可以为跟，分别算出来，再把它们加起来即可。n = 2的情况可由下面式子算出：</p>
<p>dp[2] =  dp[0] * dp[1]　　　(1为根的情况)</p>
<p>　　　　+ dp[1] * dp[0]　　  (2为根的情况)</p>
<p>同理可写出 n = 3 的计算方法：</p>
<p>dp[3] =  dp[0] * dp[2]　　　(1为根的情况)</p>
<p>　　　　+ dp[1] * dp[1]　　  (2为根的情况)</p>
<p> 　　　  + dp[2] * dp[0]　　  (3为根的情况)</p>
<p>由此可以得出卡塔兰数列的递推式为：</p>
<p>c[0]=1 ; c[n+1] = sum(c[i]*c[n-i])  </p>
<p>最后代码实现为：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">UniqueBinarySearchTrees</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">numTrees</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] dp=<span class="keyword">new</span> <span class="keyword">int</span>[n+<span class="number">1</span>];</span><br><span class="line">        dp[<span class="number">0</span>]=dp[<span class="number">1</span>]=<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">2</span>; i &lt;=n; i++) &#123;</span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">int</span> j = <span class="number">0</span>; j &lt; i; j++) &#123;</span><br><span class="line">                dp[i]+=dp[j]*dp[i-j-<span class="number">1</span>];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> dp[n];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">fun</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(numTrees(<span class="number">3</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-LeetCode</category>
      </categories>
      <tags>
        <tag>LeetCode</tag>
      </tags>
  </entry>
  <entry>
    <title>命令行常用参数</title>
    <url>/2017/09/01/Linux/%E5%91%BD%E4%BB%A4%E8%A1%8C%E5%B8%B8%E7%94%A8%E5%8F%82%E6%95%B0/</url>
    <content><![CDATA[<h2 id="1-命令行中"><a href="#1-命令行中" class="headerlink" title="1.命令行中"></a>1.命令行中</h2><h4 id="1-上一条命令的最后一个参数-："><a href="#1-上一条命令的最后一个参数-：" class="headerlink" title="1.上一条命令的最后一个参数 ： !$"></a>1.上一条命令的最后一个参数 ： !$</h4><h4 id="2-上一条命令的第-n-个参数-：-n"><a href="#2-上一条命令的第-n-个参数-：-n" class="headerlink" title="2.上一条命令的第 n 个参数 ：  !:n"></a>2.上一条命令的第 n 个参数 ：  !:n</h4><h4 id="3-上一条命令的名称-："><a href="#3-上一条命令的名称-：" class="headerlink" title="3.上一条命令的名称 ： !"></a>3.上一条命令的名称 ： !</h4><h4 id="4-上一条命令的内容-："><a href="#4-上一条命令的内容-：" class="headerlink" title="4.上一条命令的内容 ： !!"></a>4.上一条命令的内容 ： !!</h4><h4 id="5-上n条命令的内容-：-n"><a href="#5-上n条命令的内容-：-n" class="headerlink" title="5.上n条命令的内容 ： n!!"></a>5.上n条命令的内容 ： n!!</h4><h2 id="2-shell-脚本中"><a href="#2-shell-脚本中" class="headerlink" title="2.shell 脚本中"></a>2.shell 脚本中</h2><h4 id="1-上一条命令的名称-：-0"><a href="#1-上一条命令的名称-：-0" class="headerlink" title="1.上一条命令的名称 ： $0"></a>1.上一条命令的名称 ： $0</h4><h4 id="2-上一条命令的第-n-个参数-：-n-1"><a href="#2-上一条命令的第-n-个参数-：-n-1" class="headerlink" title="2.上一条命令的第 n 个参数 ：$n"></a>2.上一条命令的第 n 个参数 ：$n</h4><h4 id="3-上一条命令的执行情况-：-0-正常退出-非0不正常"><a href="#3-上一条命令的执行情况-：-0-正常退出-非0不正常" class="headerlink" title="3.上一条命令的执行情况 ： 0 正常退出 非0不正常"></a>3.上一条命令的执行情况 ： 0 正常退出 非0不正常</h4>]]></content>
      <categories>
        <category>-Linux</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>三大框架整合</title>
    <url>/2017/08/23/Java%20Web/%E4%B8%89%E5%A4%A7%E6%A1%86%E6%9E%B6%E6%95%B4%E5%90%88/</url>
    <content><![CDATA[<h3 id="1-整合思想"><a href="#1-整合思想" class="headerlink" title="1.整合思想"></a>1.整合思想</h3><ul>
<li>web 层 -&gt; struts2</li>
<li>service 层 -&gt; Spring</li>
<li>dao 层 -&gt; Hibernate<br>整合就是两两整合，struts 和 spring 整合，然后 spring 和 hibernate 整合。  </li>
<li>struts 和 spring 整合就是 action 需要的 service 直接通过 spring 实例化并且注入到 action 中</li>
<li>spring 和 hibernate 整合就是把 sessionFactory 放在 spring 中管理，然后把 dao 注入到 service 中<a id="more"></a>
</li>
</ul>
]]></content>
      <categories>
        <category>-JAVA</category>
      </categories>
      <tags>
        <tag>JAVA</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring笔记(三)</title>
    <url>/2017/08/23/Java%20Web/Spring%E7%AC%94%E8%AE%B0-%E4%B8%89/</url>
    <content><![CDATA[<h3 id="1-使用注解实现-aop"><a href="#1-使用注解实现-aop" class="headerlink" title="1.使用注解实现 aop"></a>1.使用注解实现 aop</h3><ul>
<li>配置 xml<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:aop</span>=<span class="string">"http://www.springframework.org/schema/aop"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">                            http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd "</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--开启 aop 操作--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">aop:aspectj-autoproxy</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"user"</span> <span class="attr">class</span>=<span class="string">"aop1.User"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"improve"</span> <span class="attr">class</span>=<span class="string">"aop1.Improve"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br><span class="line">```<span class="comment">&lt;!--more--&gt;</span></span><br><span class="line">* 在增强类中注解</span><br><span class="line">```java</span><br><span class="line">package aop1;</span><br><span class="line"></span><br><span class="line">import org.springframework.aop.aspectj.MethodInvocationProceedingJoinPoint;</span><br><span class="line">import org.springframework.context.annotation.EnableAspectJAutoProxy;</span><br><span class="line"></span><br><span class="line">@Aspect</span><br><span class="line">public class Improve &#123;</span><br><span class="line">    @Before(value="execution(* aop1.User.say(..))")</span><br><span class="line">    public void before_say()&#123;</span><br><span class="line">        System.out.println("before");</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void after_say()&#123;</span><br><span class="line">        System.out.println("after");</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    public void around_say(MethodInvocationProceedingJoinPoint joinPoint) throws Throwable &#123;</span><br><span class="line">        System.out.println("before");</span><br><span class="line">        joinPoint.proceed();</span><br><span class="line">        System.out.println("after");</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="2-JdbcTemplate"><a href="#2-JdbcTemplate" class="headerlink" title="2.JdbcTemplate"></a>2.JdbcTemplate</h3><h4 id="1-增"><a href="#1-增" class="headerlink" title="1.增"></a>1.增</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//配置 dataSource</span></span><br><span class="line">        DriverManagerDataSource dataSource=<span class="keyword">new</span> DriverManagerDataSource();</span><br><span class="line">        dataSource.setDriverClassName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        dataSource.setUrl(<span class="string">"jdbc:mysql:///JAVA"</span>);</span><br><span class="line">        dataSource.setUsername(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">"12345678"</span>);</span><br><span class="line">        <span class="comment">//设置模板</span></span><br><span class="line">        JdbcTemplate jdbcTemplate=<span class="keyword">new</span> JdbcTemplate(dataSource);</span><br><span class="line">        <span class="comment">//操作</span></span><br><span class="line">        String sql=<span class="string">"insert into user values(?,?)"</span>;</span><br><span class="line">        jdbcTemplate.update(sql,<span class="number">1</span>,<span class="string">"lwen"</span>);</span><br></pre></td></tr></table></figure>

<h4 id="2-删"><a href="#2-删" class="headerlink" title="2.删"></a>2.删</h4><p>还是 update 操作只是 sql 不一样</p>
<h4 id="3-改"><a href="#3-改" class="headerlink" title="3.改"></a>3.改</h4><p>同增加</p>
<h4 id="4-查"><a href="#4-查" class="headerlink" title="4.查"></a>4.查</h4><p>类似于 QueryRunner 也是使用接口的实例，不过这里没有准备好的实现类，实现类需要我们自己书写。</p>
<h5 id="1-查询单值"><a href="#1-查询单值" class="headerlink" title="1.查询单值"></a>1.查询单值</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">fun2</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">//配置 dataSource</span></span><br><span class="line">        DriverManagerDataSource dataSource=<span class="keyword">new</span> DriverManagerDataSource();</span><br><span class="line">        dataSource.setDriverClassName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        dataSource.setUrl(<span class="string">"jdbc:mysql:///JAVA"</span>);</span><br><span class="line">        dataSource.setUsername(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">"12345678"</span>);</span><br><span class="line">        <span class="comment">//设置模板</span></span><br><span class="line">        JdbcTemplate jdbcTemplate=<span class="keyword">new</span> JdbcTemplate(dataSource);</span><br><span class="line">        <span class="comment">//操作</span></span><br><span class="line">        String sql=<span class="string">"select count(*) from user"</span>;</span><br><span class="line">        Object object=jdbcTemplate.queryForObject(sql,Integer.class);</span><br><span class="line">        System.out.println(object);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">MyRowMapper</span> <span class="keyword">implements</span> <span class="title">RowMapper</span></span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> Object <span class="title">mapRow</span><span class="params">(ResultSet resultSet, <span class="keyword">int</span> i)</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">            User user=<span class="keyword">new</span> User();</span><br><span class="line">            user.setId(resultSet.getInt(<span class="string">"id"</span>));</span><br><span class="line">            user.setName(resultSet.getString(<span class="string">"name"</span>));</span><br><span class="line">            <span class="keyword">return</span> user;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">fun3</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">//配置 dataSource</span></span><br><span class="line">        DriverManagerDataSource dataSource=<span class="keyword">new</span> DriverManagerDataSource();</span><br><span class="line">        dataSource.setDriverClassName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        dataSource.setUrl(<span class="string">"jdbc:mysql:///JAVA"</span>);</span><br><span class="line">        dataSource.setUsername(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">"12345678"</span>);</span><br><span class="line">        <span class="comment">//设置模板</span></span><br><span class="line">        JdbcTemplate jdbcTemplate=<span class="keyword">new</span> JdbcTemplate(dataSource);</span><br><span class="line">        <span class="comment">//操作</span></span><br><span class="line">        String sql=<span class="string">"select * from user where name=?"</span>;</span><br><span class="line">        Object object=jdbcTemplate.queryForObject(sql, <span class="keyword">new</span> MyRowMapper(),<span class="string">"lwen"</span>);</span><br><span class="line">        System.out.println(object);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h5 id="2-多值查询"><a href="#2-多值查询" class="headerlink" title="2.多值查询"></a>2.多值查询</h5><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyRowMapper1</span> <span class="keyword">implements</span> <span class="title">RowMapper</span></span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="function"><span class="keyword">public</span> Object <span class="title">mapRow</span><span class="params">(ResultSet resultSet, <span class="keyword">int</span> i)</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">            User user=<span class="keyword">new</span> User();</span><br><span class="line">            user.setId(resultSet.getInt(<span class="string">"id"</span>));</span><br><span class="line">            user.setName(resultSet.getString(<span class="string">"name"</span>));</span><br><span class="line">            <span class="keyword">return</span> user;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">fun4</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">//配置 dataSource</span></span><br><span class="line">        DriverManagerDataSource dataSource=<span class="keyword">new</span> DriverManagerDataSource();</span><br><span class="line">        dataSource.setDriverClassName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        dataSource.setUrl(<span class="string">"jdbc:mysql:///JAVA"</span>);</span><br><span class="line">        dataSource.setUsername(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">"12345678"</span>);</span><br><span class="line">        <span class="comment">//设置模板</span></span><br><span class="line">        JdbcTemplate jdbcTemplate=<span class="keyword">new</span> JdbcTemplate(dataSource);</span><br><span class="line">        <span class="comment">//操作</span></span><br><span class="line">        String sql=<span class="string">"select * from user"</span>;</span><br><span class="line">        List&lt;User&gt; users=jdbcTemplate.query(sql,<span class="keyword">new</span> MyRowMapper1());</span><br><span class="line">        System.out.println(users);</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-事务管理"><a href="#2-事务管理" class="headerlink" title="2.事务管理"></a>2.事务管理</h3><h4 id="1-xml方式配置"><a href="#1-xml方式配置" class="headerlink" title="1.xml方式配置"></a>1.xml方式配置</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:tx</span>=<span class="string">"http://www.springframework.org/schema/tx"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:aop</span>=<span class="string">"http://www.springframework.org/schema/aop"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">                            http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">                            http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd "</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--jdbcTemplate--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"jdbc"</span> <span class="attr">class</span>=<span class="string">"org.springframework.jdbc.core.JdbcTemplate"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dataSource"</span> <span class="attr">ref</span>=<span class="string">"dataSource"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置事务管理器--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"txManager"</span> <span class="attr">class</span>=<span class="string">"org.springframework.jdbc.datasource.DataSourceTransactionManager"</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--注入 dataSource--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"dataSource"</span> <span class="attr">ref</span>=<span class="string">"dataSource"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置事务的增强--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">tx:advice</span> <span class="attr">id</span>=<span class="string">"txAdvice"</span> <span class="attr">transaction-manager</span>=<span class="string">"txManager"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">tx:attributes</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">tx:method</span> <span class="attr">name</span>=<span class="string">"add"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">tx:attributes</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">tx:advice</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--配置切面--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">aop:config</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--切入点--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">aop:pointcut</span> <span class="attr">id</span>=<span class="string">"point1"</span> <span class="attr">expression</span>=<span class="string">"execution(* com.lwen.Service.add(..))"</span>/&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--切面--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">aop:advisor</span> <span class="attr">advice-ref</span>=<span class="string">"txAdvice"</span> <span class="attr">pointcut-ref</span>=<span class="string">"point1"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">aop:config</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="2-注解方式"><a href="#2-注解方式" class="headerlink" title="2.注解方式"></a>2.注解方式</h4><ul>
<li><p>开启注解</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 开启注解 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">tx:annotation-driven</span> <span class="attr">transaction-manager</span>=<span class="string">"transactionManager"</span>/&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>在事务类上面写注解<br>@Transactional</p>
</li>
</ul>
]]></content>
      <categories>
        <category>-JAVA</category>
      </categories>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring笔记(二)</title>
    <url>/2017/08/23/Java%20Web/Spring%E7%AC%94%E8%AE%B0-%E4%BA%8C/</url>
    <content><![CDATA[<h3 id="1-注解ioc"><a href="#1-注解ioc" class="headerlink" title="1.注解ioc"></a>1.注解ioc</h3><h4 id="1-开启注解扫描"><a href="#1-开启注解扫描" class="headerlink" title="1.开启注解扫描"></a>1.开启注解扫描</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:context</span>=<span class="string">"http://www.springframework.org/schema/context"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!--开启注解扫描--&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">context:component-scan</span> <span class="attr">base-package</span>=<span class="string">"domain1"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<a id="more"></a>
<h4 id="2-在类上面做注解："><a href="#2-在类上面做注解：" class="headerlink" title="2.在类上面做注解："></a>2.在类上面做注解：</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Component</span>(value = <span class="string">"user"</span>)</span><br><span class="line"><span class="meta">@Scope</span>(value=<span class="string">"singleton"</span>)</span><br></pre></td></tr></table></figure>

<p>注解有四个，但是却别都不大：</p>
<ul>
<li>Component</li>
<li>Service</li>
<li>Controller</li>
<li>Repository</li>
</ul>
<h4 id="3-注入属性"><a href="#3-注入属性" class="headerlink" title="3.注入属性"></a>3.注入属性</h4><p>注解的时候 set 方法也无需生成，直接注解就能搞定。<br>直接在属性上面写@Autowired或者@Resource(name = “”)<br>第一种方式我们不用去指定对象的 id 值，而第二个需要些 id 值。主要用的就是第二个，因为他更加的严谨。</p>
<h3 id="2-AOP"><a href="#2-AOP" class="headerlink" title="2.AOP"></a>2.AOP</h3><h4 id="1-基本介绍"><a href="#1-基本介绍" class="headerlink" title="1.基本介绍"></a>1.基本介绍</h4><p>aop 也叫做面向切面编程，或者面向方面编程。他的主要方式就是横向抽取，与以前的继承方式的纵向抽取不同。<br>这里就主要说说横向抽取，aop 主要还是用了动态代理的方式来拓展对象的功能。而动态代理分为两种：</p>
<ul>
<li>jdk 的动态代理<br>这种动态代理就是一个接口，然后我们需要使用 aop 来增强这个接口的实现类的对象的功能，那么这个动态代理方式就是新建一个类，然后这个类实现那个接口，也就是创建一个与被增强的类的平级的一个类，所以称之为横向抽取。之后只需要再次类中进行方法的增强即可，但是这个地方就是用了动态代理的方式而不是切切实实的写一个类，我们代理原来的需要被增强的类，然后在他的基础上做一些拓展。</li>
<li>gclib 动态代理方式<br>这种动态代理是针对的没有接口的代理。主要就是通过集成需要被增强的类，然后进行动态代理。</li>
</ul>
<h4 id="2-相关定义"><a href="#2-相关定义" class="headerlink" title="2.相关定义"></a>2.相关定义</h4><ul>
<li>连接点：类中的哪些类可以被增强的方法就称之为连接点。</li>
<li>切入点：我们实际增强了的方法。</li>
<li>增强：我们增强的功能。分为前置增强（方法之前），后置增强（方法之后），异常增强（抛出异常执行），最终增强（后置之后），环绕增强（之前之后都执行）</li>
<li>切面：把增强应用到方法上的过程。</li>
<li>目标对象：代理的目标对象</li>
</ul>
<h4 id="3-xml-配置方式"><a href="#3-xml-配置方式" class="headerlink" title="3.xml 配置方式"></a>3.xml 配置方式</h4><p>增强类：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> aop;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.aop.aspectj.MethodInvocationProceedingJoinPoint;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Improve</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">before_say</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"before"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">after_say</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"after"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">around_say</span><span class="params">(MethodInvocationProceedingJoinPoint joinPoint)</span> <span class="keyword">throws</span> Throwable </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"before"</span>);</span><br><span class="line">        joinPoint.proceed();</span><br><span class="line">        System.out.println(<span class="string">"after"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>被增强类</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> aop;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">say</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"hello"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>配置文件</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:aop</span>=<span class="string">"http://www.springframework.org/schema/aop"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd</span></span></span><br><span class="line"><span class="tag"><span class="string">                            http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd "</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"improve"</span> <span class="attr">class</span>=<span class="string">"aop.Improve"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">aop:config</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--配置切入点  方法--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">aop:pointcut</span> <span class="attr">id</span>=<span class="string">"point1"</span> <span class="attr">expression</span>=<span class="string">"execution(* aop.User.say(..))"</span>/&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--配置切面 应用增强--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">aop:aspect</span> <span class="attr">ref</span>=<span class="string">"improve"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">aop:before</span> <span class="attr">method</span>=<span class="string">"before_say"</span> <span class="attr">pointcut-ref</span>=<span class="string">"point1"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">aop:aspect</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">aop:config</span>&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"user"</span> <span class="attr">class</span>=<span class="string">"aop.User"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-log4j"><a href="#2-log4j" class="headerlink" title="2.log4j"></a>2.log4j</h3><p>导包：</p>
<ul>
<li>log4j</li>
<li>common-loging<br>配置文件：</li>
<li>src/log4j.properties</li>
</ul>
]]></content>
      <categories>
        <category>-JAVA</category>
      </categories>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring笔记(一)</title>
    <url>/2017/08/23/Java%20Web/Spring%E7%AC%94%E8%AE%B0-%E4%B8%80/</url>
    <content><![CDATA[<h3 id="1-基本介绍"><a href="#1-基本介绍" class="headerlink" title="1.基本介绍"></a>1.基本介绍</h3><p>spring 是一个一站式框架，也就是有了它 web 层，service 层还有 dao 层都能直接搞定而不需使用其他的框架。<br>这三层分别就是：</p>
<ul>
<li>SpringMVC</li>
<li>ioc</li>
<li>JdbcTemplate<a id="more"></a>
<h3 id="2-ioc基本原理"><a href="#2-ioc基本原理" class="headerlink" title="2.ioc基本原理"></a>2.ioc基本原理</h3>ioc 原理就是使用配置文件解析到需要创建的类的 class 然后使用工厂类的静态方法获取这个类的对象。  <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">beans</span> <span class="attr">xmlns</span>=<span class="string">"http://www.springframework.org/schema/beans"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">       <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd"</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"user"</span> <span class="attr">class</span>=<span class="string">"domain.User"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;/<span class="name">beans</span>&gt;</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">fun1</span><span class="params">()</span></span>&#123;</span><br><span class="line">        ApplicationContext context= <span class="keyword">new</span> ClassPathXmlApplicationContext(<span class="string">"user.xml"</span>);</span><br><span class="line">        User user= (User) context.getBean(<span class="string">"user"</span>);</span><br><span class="line">        user.say();</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-spring-差UN感觉爱你对象的三种方式"><a href="#3-spring-差UN感觉爱你对象的三种方式" class="headerlink" title="3.spring 差UN感觉爱你对象的三种方式"></a>3.spring 差UN感觉爱你对象的三种方式</h3><h4 id="1-ioc无参构造"><a href="#1-ioc无参构造" class="headerlink" title="1.ioc无参构造"></a>1.ioc无参构造</h4><p>也就是上面的那个方法，首先配置 bean 然后读取配置文件，根据 id 获取类的对象，这个实例化的时候都是使用该类的默认构造函数。默认构造函数是无参的，当我们使用有参的构造函数覆盖了以后就会报错。这个方法也是最常用的一种方法。</p>
<h4 id="2-静态工厂"><a href="#2-静态工厂" class="headerlink" title="2.静态工厂"></a>2.静态工厂</h4><p>首先创建一个静态工厂的类，这个类里面需要有静态方法，静态方法返回类的实例，最后配置 xml 文件，也和上面一样，但是这里的 class 值写得是静态工厂的类，还有一个 factory-method 属性，就写那个静态方法。</p>
<h4 id="3-实例工厂"><a href="#3-实例工厂" class="headerlink" title="3.实例工厂"></a>3.实例工厂</h4><p>实例工厂和上面的静态工厂不同的方式就是这个工厂需要实例化，然后实例化就交给 ioc 来解决其他的和静态工厂一样。</p>
<h3 id="4-bean-标签的常用属性"><a href="#4-bean-标签的常用属性" class="headerlink" title="4.bean 标签的常用属性"></a>4.bean 标签的常用属性</h3><h4 id="1-id"><a href="#1-id" class="headerlink" title="1.id"></a>1.id</h4><p>id 值就类似于对像的名字</p>
<h4 id="2-class"><a href="#2-class" class="headerlink" title="2.class"></a>2.class</h4><p>需要创建的类的全路径</p>
<h4 id="3-name"><a href="#3-name" class="headerlink" title="3.name"></a>3.name</h4><p>和 id 属性一样，获取对象也是 getBean 方法，他和 id 的不同的在于他可以写一些特殊符号。</p>
<h4 id="4-scope"><a href="#4-scope" class="headerlink" title="4.scope"></a>4.scope</h4><ul>
<li>singleton 单例对象，默认值</li>
<li>prototype 多例</li>
<li>request  创建的对象都放在了request 域</li>
<li>session    创建对象放在 session 域</li>
</ul>
<h3 id="5-ioc-属性注入"><a href="#5-ioc-属性注入" class="headerlink" title="5.ioc 属性注入"></a>5.ioc 属性注入</h3><h4 id="1-构造函数注入"><a href="#1-构造函数注入" class="headerlink" title="1.构造函数注入"></a>1.构造函数注入</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"user"</span> <span class="attr">class</span>=<span class="string">"domain.User"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">constructor-arg</span> <span class="attr">name</span>=<span class="string">"id"</span> <span class="attr">value</span>=<span class="string">"1"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">constructor-arg</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">value</span>=<span class="string">"lwen"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="2-set-方法"><a href="#2-set-方法" class="headerlink" title="2.set 方法"></a>2.set 方法</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"user1"</span> <span class="attr">class</span>=<span class="string">"domain.User"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"id"</span> <span class="attr">value</span>=<span class="string">"1"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">value</span>=<span class="string">"lwen"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="3-注入对象"><a href="#3-注入对象" class="headerlink" title="3.注入对象"></a>3.注入对象</h4><p>属于 set 方法的一种 ，但是他和注入普通的字符串还是不一样的。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"user1"</span> <span class="attr">class</span>=<span class="string">"domain.User"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"userDao"</span> <span class="attr">ref</span>=<span class="string">"Dao 的 id 值"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="4-复杂类型的注入"><a href="#4-复杂类型的注入" class="headerlink" title="4.复杂类型的注入"></a>4.复杂类型的注入</h4><p>还是使用 set 方法</p>
<ul>
<li>数组类型</li>
<li>list</li>
<li>map</li>
<li>properties<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">"user2"</span> <span class="attr">class</span>=<span class="string">"domain.User"</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--数组--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"array"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">list</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">value</span>&gt;</span>1<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">value</span>&gt;</span>1<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">value</span>&gt;</span>1<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">list</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--list--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"list"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">list</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">value</span>&gt;</span>hello<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">value</span>&gt;</span>hello<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">value</span>&gt;</span>hello<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">list</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--map--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"map"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">map</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">entry</span> <span class="attr">key</span>=<span class="string">"a"</span> <span class="attr">value</span>=<span class="string">"a"</span>/&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">entry</span> <span class="attr">key</span>=<span class="string">"b"</span> <span class="attr">value</span>=<span class="string">"b"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">map</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--properties--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"properties"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">props</span>&gt;</span></span><br><span class="line">                <span class="tag">&lt;<span class="name">prop</span> <span class="attr">key</span>=<span class="string">"url"</span>&gt;</span>jdbc:mysql:///java<span class="tag">&lt;/<span class="name">prop</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;/<span class="name">props</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">bean</span>&gt;</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="5-配置文件自动加载"><a href="#5-配置文件自动加载" class="headerlink" title="5.配置文件自动加载"></a>5.配置文件自动加载</h3><p>在服务器创建的时候会创建一个 ServletContext 对象，这个对象有一个监听器，监听他什么时候创建。<br>我们可以在这个对象创建的时候把配置文件给加载，再把加载的文件对象放在 ServletContext 域中。<br>这个监听器就叫做 ServletContextListener 。<br>这个东西目前 spring 已经封装好了，我们只需要进行 xml 的配置即可。</p>
<!-- 配置配置文件路径 -->
<context-param>
       <param-name>contextConfigLocation</param-name>
       <param-value>classpath:aop.xml</param-value>
</context-param>
<!-- 配置监听器 -->
<listener>
       <listener-class>org..web.context.ContextLoaderListener</listener-class>
</listener>
]]></content>
      <categories>
        <category>-JAVA</category>
      </categories>
      <tags>
        <tag>Spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Struts2笔记(三)</title>
    <url>/2017/08/23/Java%20Web/Struts2%E7%AC%94%E8%AE%B0-%E4%B8%89/</url>
    <content><![CDATA[<p>拦截器，struts2方庄了很多功能，这些功能都是使用拦截器来实现的。一般的拦截器只是执行默认的拦截器，就是在struts-core这个包里面，然后李 main 有一个struts-default.xml这个文件李爱你就定义了很多默认的拦截器，interceptor-strck这个标签里面就是默认的拦截器。例如里面有ModelDriven。</p>
<h3 id="1-拦截器执行的时机"><a href="#1-拦截器执行的时机" class="headerlink" title="1.拦截器执行的时机"></a>1.拦截器执行的时机</h3><p>拦截器执行的时机就是action创建完成之后在action调用的方法之前调用。</p>
<h3 id="2-底层原理"><a href="#2-底层原理" class="headerlink" title="2.底层原理"></a>2.底层原理</h3><h4 id="1-aop思想"><a href="#1-aop思想" class="headerlink" title="1.aop思想"></a>1.aop思想</h4><p>aop就是面向切面编程。如果我们需要扩展一个类的功能，一般来说我们会写一个父类，<a id="more"></a>让子类继承父类这样子类的功能就能获得拓展。这就是纵向编程，而所谓的横向的切面编程就在这个类的某个方法之前我们执行某些操作，或者在某个方法之后执行某些操作。蕾丝与thinkphp中的<strong>before()或者</strong>after()。</p>
<h4 id="2-责任链模式"><a href="#2-责任链模式" class="headerlink" title="2.责任链模式"></a>2.责任链模式</h4><p>责任链就类似于过滤链，过滤链就需要首先方形才能够继续进行。也就是如果我们需要进行一系列的操作的话，首先操作一然后必须放行才能进行下一个操作。<br>再多拦截器的时候就需要责任链模式。</p>
<h4 id="3-原理概述"><a href="#3-原理概述" class="headerlink" title="3.原理概述"></a>3.原理概述</h4><p>首先需要创建 action 的代理对象，所谓代理对象就不是真正的对象，而是一个具有被代理的对象的全部功能的对象，然后在代理对象执行 action 的方法之前，做了一个 aop 的操作也就是执行各种默认的拦截器，在每一个拦截器执行完毕以后需要在执行放行操作执行下一个拦截器，最后才执行那个被调用的方法。</p>
<h3 id="3-过滤器与拦截器"><a href="#3-过滤器与拦截器" class="headerlink" title="3.过滤器与拦截器"></a>3.过滤器与拦截器</h3><p>过滤器是服务器启动的时候创建，然后能过滤所有的内容，只要有路径就可以拦截。<br>但是拦截器虽然叫做拦截器，他只能拦截action</p>
<h3 id="4-自定义拦截器"><a href="#4-自定义拦截器" class="headerlink" title="4.自定义拦截器"></a>4.自定义拦截器</h3><h4 id="1-写逻辑的类"><a href="#1-写逻辑的类" class="headerlink" title="1.写逻辑的类"></a>1.写逻辑的类</h4><p>首先需要继承 AbstractInterceptor 类，然后里面有三个方法，分别就是 init 初始化的操作，destroy 销毁的操作，interceptor 拦截的操作。<br>实际在开发的时候尽量继承的 MethodFilterInterceptor 类，重写里面的拦截方法。最后注册拦截器，配置拦截器和 action 的关系。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MyInterceptor</span> <span class="keyword">extends</span> <span class="title">MethodFilterInterceptor</span></span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> String <span class="title">doIntercept</span><span class="params">(ActionInvocation actionInvocation)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="comment">//逻辑</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">//if 成功 -&gt;放行操作</span></span><br><span class="line">        actionInvocation.invoke();</span><br><span class="line">        <span class="comment">//if fail -&gt; 返回一个字符串，就回到 result 中找这个结果就和 action 一样</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">"fail"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在 action 中的所在的包配置拦截器的声明。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">interceptors</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">interceptor</span> <span class="attr">name</span>=<span class="string">"MyInterecpot"</span> <span class="attr">class</span>=<span class="string">"demo2.MyInterceptor"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">interceptors</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>再具体的 action 标签里面使用配置的拦截器</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">action</span> <span class="attr">name</span>=<span class="string">"hello"</span> <span class="attr">class</span>=<span class="string">"demo01.Hello"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">interceptor-ref</span> <span class="attr">name</span>=<span class="string">"MyInterecpot"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">result</span> <span class="attr">name</span>=<span class="string">"success"</span>&gt;</span>/index.jsp<span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">action</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>把默认拦截器写上，不然默认拦截器会失效。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">interceptor-ref</span> <span class="attr">name</span>=<span class="string">"defaultStack"</span>/&gt;</span></span><br></pre></td></tr></table></figure>

<p>不拦截某个方法</p>
<pre><code class="xml"><span class="tag">&lt;<span class="name">interceptor-ref</span> <span class="attr">name</span>=<span class="string">"MyInterecpot"</span>&gt;</span>
                <span class="tag">&lt;<span class="name">param</span> <span class="attr">name</span>=<span class="string">"excludeMethods"</span>&gt;</span>login,add<span class="tag">&lt;/<span class="name">param</span>&gt;</span>
<span class="tag">&lt;/<span class="name">interceptor-ref</span>&gt;</span></code></pre>
]]></content>
      <categories>
        <category>-JAVA</category>
      </categories>
      <tags>
        <tag>struts2</tag>
      </tags>
  </entry>
  <entry>
    <title>Struts2笔记(二)</title>
    <url>/2017/08/22/Java%20Web/Struts2%E7%AC%94%E8%AE%B0-%E4%BA%8C/</url>
    <content><![CDATA[<h3 id="1-结果页面"><a href="#1-结果页面" class="headerlink" title="1.结果页面"></a>1.结果页面</h3><h4 id="1-全局结果页面"><a href="#1-全局结果页面" class="headerlink" title="1.全局结果页面"></a>1.全局结果页面</h4><p>当一些方法的返回值一样的时候并且需要跳转的页面也一样的时候就可以使用全局结果页面。  </p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">package</span> <span class="attr">name</span>=<span class="string">"struts"</span> <span class="attr">extends</span>=<span class="string">"struts-default"</span> <span class="attr">namespace</span>=<span class="string">"/"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">global-results</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">name</span>=<span class="string">"done"</span>&gt;</span>index.jsp<span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">global-results</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">action</span> <span class="attr">name</span>=<span class="string">"hello"</span> <span class="attr">class</span>=<span class="string">"demo01.Hello"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">result</span> <span class="attr">name</span>=<span class="string">"success"</span>&gt;</span>/index.jsp<span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">action</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">package</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>注意他只是太某一个package中，而不是整个项目<a id="more"></a></p>
<h4 id="2-局部结果页面"><a href="#2-局部结果页面" class="headerlink" title="2.局部结果页面"></a>2.局部结果页面</h4><p>全局页面和局部页面同事存在并且冲突的时候是以局部的为准。</p>
<h3 id="2-action获取表单数据。"><a href="#2-action获取表单数据。" class="headerlink" title="2.action获取表单数据。"></a>2.action获取表单数据。</h3><h4 id="1-使用ActionContext对象"><a href="#1-使用ActionContext对象" class="headerlink" title="1.使用ActionContext对象"></a>1.使用ActionContext对象</h4><p>在action中没办法调用request对象，所以我们智力只能使用actionContext对象获取。<br>首先需要获取actionContext对象，然后这个对象不能new，而是通过他自己的一个静态方法获取的，名字就叫做<br>getContext(),最后使用getParamters方法就能获取到表单的数据。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">ActionContext actionContext=ActionContext.getContext();</span><br><span class="line">Map&lt;String, Object&gt; map = actionContext.getParameters();</span><br></pre></td></tr></table></figure>

<h4 id="2-ServletActionContext对象"><a href="#2-ServletActionContext对象" class="headerlink" title="2.ServletActionContext对象"></a>2.ServletActionContext对象</h4><p>里面的方法大多数都是静态的，我们直接可以调用方法。我们可以获取HttpServletRequest对象也可以获取ActionContext对象。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//获取request对象</span></span><br><span class="line">HttpServletRequest request= ServletActionContext.getRequest();</span><br><span class="line">request.getParameter(<span class="string">"name"</span>);</span><br></pre></td></tr></table></figure>

<h4 id="3-接口注入"><a href="#3-接口注入" class="headerlink" title="3.接口注入"></a>3.接口注入</h4><p>让action实现ServletRequestAware接口，然后实现接口的方法，这个方法其实会有一个参数，这个参数就是我们需要的request对象，这样tomcat注入以后我们就可以保存起来直接使用。</p>
<h3 id="3-action操作域对象"><a href="#3-action操作域对象" class="headerlink" title="3.action操作域对象"></a>3.action操作域对象</h3><p>其实action更像是一个servlet，这里action中可以操作的域对象和servlet一样也是三个。</p>
<ul>
<li>request  使用ServletActionContext对象的getRequest方法获取</li>
<li>session  使用request对象的getSession获取</li>
<li>ServletContext  使用ServletActionContext的getServletContext方法获取</li>
</ul>
<h3 id="4-表单数据自动注入"><a href="#4-表单数据自动注入" class="headerlink" title="4.表单数据自动注入"></a>4.表单数据自动注入</h3><h4 id="1-属性封装"><a href="#1-属性封装" class="headerlink" title="1.属性封装"></a>1.属性封装</h4><p>直接把要提交的属性的name值放在action的属性中，然后生成属性的set方法这样就能完表单数据属性的自动注入</p>
<h4 id="2-模型驱动封装"><a href="#2-模型驱动封装" class="headerlink" title="2.模型驱动封装"></a>2.模型驱动封装</h4><p>模型驱动封装就可以直接把这个表单的数据直接封装到一个实体类中<br>首先需要实现ModelDriven接口，这个借口需要传入一个对象类型，这个对象就是实体类的类型。然后我们在这个action中必须要有一个实体类的对象，必须要new出来。然后需要实现这个接口的方法，getModel方法，返回值返回的就是我们创建的实体类的对象。<br>属性封装和模型驱动不能同时使用，到时候只有模型驱动起作用，属性封装获取的都是null。</p>
<h4 id="3-表达式封装"><a href="#3-表达式封装" class="headerlink" title="3.表达式封装"></a>3.表达式封装</h4><ul>
<li>声明实体类，不实例化。</li>
<li>生成变量的set和get方法</li>
<li>在表单name中使用表达式 user.username 就是这个属性。通过get方法获取user，设置完属性以后set方法更新值。</li>
</ul>
<h3 id="5-值栈"><a href="#5-值栈" class="headerlink" title="5.值栈"></a>5.值栈</h3><p>值栈就类似于一个域对象</p>
<h4 id="1-存储位置"><a href="#1-存储位置" class="headerlink" title="1.存储位置"></a>1.存储位置</h4><p>每一个action都只有一个值栈区，每一次访问的时候action都会被重新创建，也就是action多例的。</p>
<h4 id="2-获取值栈的对象"><a href="#2-获取值栈的对象" class="headerlink" title="2.获取值栈的对象"></a>2.获取值栈的对象</h4><p>使用ActionContext对象  </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">actionContext.getValueStack();</span><br></pre></td></tr></table></figure>

<h4 id="3-值栈的结构"><a href="#3-值栈的结构" class="headerlink" title="3.值栈的结构"></a>3.值栈的结构</h4><p>值栈主要分为两部分，root和context。root是List集合而context是一个map集合。context中存放了三大域对象。<br>而我们主要使用的空间就是root，在root中默认的栈顶元素就是当前的action对象。</p>
<h4 id="4-向值栈中存数据"><a href="#4-向值栈中存数据" class="headerlink" title="4.向值栈中存数据"></a>4.向值栈中存数据</h4><ul>
<li><p>调用值栈对象的set方法</p>
</li>
<li><p>调用push方法</p>
</li>
<li><p>在action定义属性，然后生成get方法，最后再调用的方法中设置这个属性的值，这样这个值就被放到了值栈中。</p>
<h4 id="5-获取值栈中的数据"><a href="#5-获取值栈中的数据" class="headerlink" title="5.获取值栈中的数据"></a>5.获取值栈中的数据</h4></li>
<li><p>字符串  &lt;s:property value=”username”/&gt;   直接根据名称获取值</p>
</li>
<li><p>对象  &lt;s:property value=”user.username”/&gt;  user中的username属性</p>
</li>
<li><p>list集合  </p>
<ul>
<li><p>方案一 ： &lt;s:property value=”list[0].username”/&gt;</p>
</li>
<li><p>方案二 ：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">s:iterator</span> <span class="attr">value</span>=<span class="string">"list"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">s:property</span> <span class="attr">value</span>=<span class="string">"username"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">s:iterator</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>方案三 ：<br>使用var的时候每次遍历的user对象的引用放在了值栈中的context中了，但是我们要取值栈中的context中的内容需要使用#，所以就有以下的表达式。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">s:iterator</span> <span class="attr">value</span>=<span class="string">"list"</span> <span class="attr">var</span>=<span class="string">"user"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">s:property</span> <span class="attr">value</span>=<span class="string">"#user.username"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">s:iterator</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
</ul>
</li>
<li><p>获取set方法放的数据     &lt;s:property value=”username”/&gt;</p>
</li>
<li><p>获取push方法放的数据    &lt;s:property value=”[0].top”/&gt;  push是放在了一个栈中了，这个就是一个数组，取数组的第一个元素</p>
</li>
<li><p>使用EL表达式也是可以获取值栈的数据。他的原理就是如果能在域对象中得到变量的时候就是域对象中的，获取不到的时候就去值栈查找，然后再放到域对象中。而这些操作能执行的原因就是在filter中给域对象做了增强也就是用了一个装饰设计模式。</p>
<h3 id="6-OGNL表达式以及struts2标签库"><a href="#6-OGNL表达式以及struts2标签库" class="headerlink" title="6.OGNL表达式以及struts2标签库"></a>6.OGNL表达式以及struts2标签库</h3><p>ognl表达式类似于el表达式，他的主要用途就是操作值栈里面的内容。<br>struts2标签库的使用必须要导入他的标签库。</p>
<h4 id="1-调用方法"><a href="#1-调用方法" class="headerlink" title="1.调用方法"></a>1.调用方法</h4><p>获取字符串hello的长度</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">s:property</span> <span class="attr">value</span>=<span class="string">"'hello'.lenth()"</span>/&gt;</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="2-两个特殊符号的使用"><a href="#2-两个特殊符号的使用" class="headerlink" title="2.两个特殊符号的使用"></a>2.两个特殊符号的使用</h4><ul>
<li><h1 id="这个就是获取context中的数据。-lt-s-property-value-”-request-username”-gt-获取域对象中的username"><a href="#这个就是获取context中的数据。-lt-s-property-value-”-request-username”-gt-获取域对象中的username" class="headerlink" title="这个就是获取context中的数据。  &lt;s:property value=”#request.username”/&gt;  获取域对象中的username"></a>这个就是获取context中的数据。  &lt;s:property value=”#request.username”/&gt;  获取域对象中的username</h1></li>
<li>% 表示在struts标签中使用ognl表达式，不识别，我们为了让他识别使用%  例如  &lt;s:textarea value=”%#request.username”/&gt;</li>
</ul>
]]></content>
      <categories>
        <category>-JAVA</category>
      </categories>
      <tags>
        <tag>struts2</tag>
      </tags>
  </entry>
  <entry>
    <title>Struts2笔记(一)</title>
    <url>/2017/08/22/Java%20Web/Struts2%E7%AC%94%E8%AE%B0-%E4%B8%80/</url>
    <content><![CDATA[<h3 id="1-创建Action"><a href="#1-创建Action" class="headerlink" title="1.创建Action"></a>1.创建Action</h3><p>创建action需要继承ActionSupport类，然后就会有一些常量以及一些方法。<br>struts2的action默认执行的方法就是execute()方法<a id="more"></a></p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> demo01;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.opensymphony.xwork2.ActionSupport;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Hello</span> <span class="keyword">extends</span> <span class="title">ActionSupport</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">execute</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> SUCCESS;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="2-配置核心配置文件"><a href="#2-配置核心配置文件" class="headerlink" title="2.配置核心配置文件"></a>2.配置核心配置文件</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">&lt;!DOCTYPE struts PUBLIC</span></span><br><span class="line"><span class="meta">        "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"</span></span><br><span class="line"><span class="meta">        "http://struts.apache.org/dtds/struts-2.3.dtd"&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">struts</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- name随便配  extends就只能是这个  namespace是将来要访问的路径的前缀 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">package</span> <span class="attr">name</span>=<span class="string">"struts"</span> <span class="attr">extends</span>=<span class="string">"struts-default"</span> <span class="attr">namespace</span>=<span class="string">"/"</span>&gt;</span></span><br><span class="line">      <span class="comment">&lt;!-- name以后和namespace合并形成访问的url  class就是具体的action的类 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">action</span> <span class="attr">name</span>=<span class="string">"hello"</span> <span class="attr">class</span>=<span class="string">"demo01.Hello"</span>&gt;</span></span><br><span class="line">          <span class="comment">&lt;!-- 看返回的值是什么然后转发到对应的页面 --&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">result</span> <span class="attr">name</span>=<span class="string">"success"</span>&gt;</span>/index.jsp<span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">action</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">package</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">struts</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="1-package"><a href="#1-package" class="headerlink" title="1.package"></a>1.package</h4><p>就相当于java中的包，为了区分不同的action。</p>
<ul>
<li>name和功能无关，name值不能重复</li>
<li>extends 他的值是固定的，是继承了struts，只有这样类才有action的特性</li>
<li>namespace 和name构成访问路径  不写默认就是斜杠<h4 id="2-action"><a href="#2-action" class="headerlink" title="2.action"></a>2.action</h4></li>
<li>name就是访问路径</li>
<li>class action的class路径</li>
<li>method 这个就是要执行的方法<h4 id="3-result"><a href="#3-result" class="headerlink" title="3.result"></a>3.result</h4></li>
<li>name  回去返回值</li>
<li>type  就是跳转还是转发<h4 id="4-配置常量"><a href="#4-配置常量" class="headerlink" title="4.配置常量"></a>4.配置常量</h4><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">constant</span> <span class="attr">name</span>=<span class="string">""</span> <span class="attr">value</span>=<span class="string">""</span>/&gt;</span></span><br><span class="line"><span class="comment">&lt;!-- 例如 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">constant</span> <span class="attr">name</span>=<span class="string">"struts.i18n.encoding"</span> <span class="attr">value</span>=<span class="string">"UTF-8"</span>/&gt;</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="3-配置web-xml过滤器"><a href="#3-配置web-xml过滤器" class="headerlink" title="3.配置web.xml过滤器"></a>3.配置web.xml过滤器</h3><p>我们的web服务器并不知道我们访问的是一个action而不是一个Servlet，所以我们要使用一个Struts2的过滤器来处率action的请求。</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">web-app</span> <span class="attr">xmlns</span>=<span class="string">"http://xmlns.jcp.org/xml/ns/javaee"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xmlns:xsi</span>=<span class="string">"http://www.w3.org/2001/XMLSchema-instance"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">xsi:schemaLocation</span>=<span class="string">"http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"</span></span></span><br><span class="line"><span class="tag">         <span class="attr">version</span>=<span class="string">"3.1"</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>struts2<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-class</span>&gt;</span>org.apache.struts2.dispatcher.ng.filter.StrutsPrepareAndExecuteFilter<span class="tag">&lt;/<span class="name">filter-class</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">filter</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">filter-name</span>&gt;</span>struts2<span class="tag">&lt;/<span class="name">filter-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/*<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">filter-mapping</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">web-app</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="4-struts2基本原理"><a href="#4-struts2基本原理" class="headerlink" title="4.struts2基本原理"></a>4.struts2基本原理</h3><h4 id="1-过滤器加载配置文件"><a href="#1-过滤器加载配置文件" class="headerlink" title="1.过滤器加载配置文件"></a>1.过滤器加载配置文件</h4><p>在web服务器启动的时候，过滤器就会创建，然后在init方法就会加载struts2的核心配置文件，以及它自带的jar包中的配置文件</p>
<h4 id="2-访问action执行方法"><a href="#2-访问action执行方法" class="headerlink" title="2.访问action执行方法"></a>2.访问action执行方法</h4><p>在访问的时候都被过滤器拦截到，然后转发到对应的类，让这些类的对应方法执行</p>
<h3 id="5-配置文件的拆分"><a href="#5-配置文件的拆分" class="headerlink" title="5.配置文件的拆分"></a>5.配置文件的拆分</h3><p>可以拆分配置文件，但是子配置文件还是需要dtd，struts标签，然后主配置文件也是如此。然后在主配置文件中的struts标签中写include标签。  </p>
<p>主配置文件：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">&lt;!DOCTYPE struts PUBLIC</span></span><br><span class="line"><span class="meta">        "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"</span></span><br><span class="line"><span class="meta">        "http://struts.apache.org/dtds/struts-2.3.dtd"&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">struts</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">constant</span> <span class="attr">name</span>=<span class="string">"struts.i18n.encoding"</span> <span class="attr">value</span>=<span class="string">"UTF-8"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">include</span> <span class="attr">file</span>=<span class="string">"struts.xml"</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">struts</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>子配置文件：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="UTF-8"?&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">&lt;!DOCTYPE struts PUBLIC</span></span><br><span class="line"><span class="meta">        "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"</span></span><br><span class="line"><span class="meta">        "http://struts.apache.org/dtds/struts-2.3.dtd"&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="tag">&lt;<span class="name">struts</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">constant</span> <span class="attr">name</span>=<span class="string">"struts.i18n.encoding"</span> <span class="attr">value</span>=<span class="string">"UTF-8"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">package</span> <span class="attr">name</span>=<span class="string">"struts"</span> <span class="attr">extends</span>=<span class="string">"struts-default"</span> <span class="attr">namespace</span>=<span class="string">"/"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">action</span> <span class="attr">name</span>=<span class="string">"hello"</span> <span class="attr">class</span>=<span class="string">"demo01.Hello"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">result</span> <span class="attr">name</span>=<span class="string">"success"</span>&gt;</span>/index.jsp<span class="tag">&lt;/<span class="name">result</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">action</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">package</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">struts</span>&gt;</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-JAVA</category>
      </categories>
      <tags>
        <tag>Struts2</tag>
      </tags>
  </entry>
  <entry>
    <title>Hibernate笔记(三)</title>
    <url>/2017/08/21/Java%20Web/Hibernate%E7%AC%94%E8%AE%B0-%E4%B8%89/</url>
    <content><![CDATA[<h3 id="1-对象导航查询"><a href="#1-对象导航查询" class="headerlink" title="1.对象导航查询"></a>1.对象导航查询</h3><p>两个相关的对象</p>
<h3 id="2-OID查询"><a href="#2-OID查询" class="headerlink" title="2.OID查询"></a>2.OID查询</h3><p>用id查出对象</p>
<h3 id="3-hql查询"><a href="#3-hql查询" class="headerlink" title="3.hql查询"></a>3.hql查询</h3><p>Query对象<a id="more"></a></p>
<h4 id="1-hql"><a href="#1-hql" class="headerlink" title="1.hql"></a>1.hql</h4><ul>
<li>查询所有   from entity</li>
<li>条件查询   from entity where name=?     setParameter(index,arg)  设置参数值</li>
<li>模糊查询   from entity where name  like ?；  </li>
<li>排序      from entity order by name desc</li>
<li>分页      setFirstResult()开始位置  setMaxResults()每页数</li>
<li>投影      select property from entity  property不能是*  </li>
<li>聚集函数<ul>
<li>count  select count(* ) from entity    query.uniqueResult()  </li>
<li>其他的类似</li>
</ul>
</li>
<li>多表查询  <ul>
<li>内连接 form entity inner join entity.set  最后返回的是数组</li>
<li>迫切内连接 form entity inner join fetch entity.set  最后返回的是list</li>
<li>外连接 form entity left outer join entity.set  最后返回的是数组</li>
<li>迫切左外连接 form entity left outer join fetch entity.set  最后返回的是list</li>
</ul>
</li>
</ul>
<h3 id="4-QBC查询"><a href="#4-QBC查询" class="headerlink" title="4.QBC查询"></a>4.QBC查询</h3><p>Criteria对象<br>createCriteria(entity.class)</p>
<ul>
<li>查询所有   list()</li>
<li>条件查询   add(Restrictions.eq/like/(“property”,”value”)) -&gt; list()</li>
<li>排序      addOrder(Order.asc(“property”))</li>
<li>分页      setFirstResult()开始位置  setMaxResults()每页数</li>
<li>统计查询   setProjection(Projetions.rowCount(10));</li>
<li>离线查询    DetachCriteria deCriteria=DetachCriteria.forClass(entity.class)<pre><code>Criteria criteria=deCriteria.getExectueableCriteria();
与session无关</code></pre><h3 id="5-本地sql查询"><a href="#5-本地sql查询" class="headerlink" title="5.本地sql查询"></a>5.本地sql查询</h3>SQLQuery对象</li>
</ul>
<h3 id="6-Hibernate的查询策略"><a href="#6-Hibernate的查询策略" class="headerlink" title="6.Hibernate的查询策略"></a>6.Hibernate的查询策略</h3><h4 id="1-立即查询"><a href="#1-立即查询" class="headerlink" title="1.立即查询"></a>1.立即查询</h4><p>get方法就是立即查询，方法执行立即发送语句   get(entity.Class,id)</p>
<h4 id="2-延时查询"><a href="#2-延时查询" class="headerlink" title="2.延时查询"></a>2.延时查询</h4><p>load方法是延时查询，方法调用不会立即发送语句，只有当我们获取返回的对象中的非id字段的值得时候才会发语句。</p>
<h5 id="1-类级别的延迟"><a href="#1-类级别的延迟" class="headerlink" title="1.类级别的延迟"></a>1.类级别的延迟</h5><p>例如根据id的查询，最后查的是一个类的某个对象</p>
<h5 id="2-关联级别的延迟"><a href="#2-关联级别的延迟" class="headerlink" title="2.关联级别的延迟"></a>2.关联级别的延迟</h5><p>当表之间是有关系的，然后我们进行延迟查询</p>
]]></content>
      <categories>
        <category>-JAVA</category>
      </categories>
      <tags>
        <tag>Hibernate</tag>
      </tags>
  </entry>
  <entry>
    <title>Hibernate笔记(二)</title>
    <url>/2017/08/21/Java%20Web/Hibernate%E7%AC%94%E8%AE%B0-%E4%BA%8C/</url>
    <content><![CDATA[<h3 id="1-一对多的关系映射"><a href="#1-一对多的关系映射" class="headerlink" title="1.一对多的关系映射"></a>1.一对多的关系映射</h3><p>对于一的一方：</p>
<a id="more"></a>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> domain;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.HashSet;</span><br><span class="line"><span class="keyword">import</span> java.util.Set;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Customer</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> cid;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="comment">//表示多个联系人</span></span><br><span class="line">    <span class="keyword">private</span> Set&lt;Person&gt; people=<span class="keyword">new</span> HashSet&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Set&lt;Person&gt; <span class="title">getPeople</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> people;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setPeople</span><span class="params">(Set&lt;Person&gt; people)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.people = people;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getCid</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> cid;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setCid</span><span class="params">(<span class="keyword">int</span> cid)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.cid = cid;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>对于多的一方</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> domain;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Person</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> uid;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="comment">//表示客户</span></span><br><span class="line">    <span class="keyword">private</span> Customer customer;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Customer <span class="title">getCustomer</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> customer;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setCustomer</span><span class="params">(Customer customer)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.customer = customer;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getUid</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> uid;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setUid</span><span class="params">(<span class="keyword">int</span> uid)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.uid = uid;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>他们的mapping文件分别就是：<br>一的一方</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE hibernate-mapping PUBLIC</span></span><br><span class="line"><span class="meta">        "-//Hibernate/Hibernate Mapping DTD//EN"</span></span><br><span class="line"><span class="meta">        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd"&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">hibernate-mapping</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">class</span> <span class="attr">name</span>=<span class="string">"domain.Customer"</span> <span class="attr">table</span>=<span class="string">"customer"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">id</span> <span class="attr">name</span>=<span class="string">"cid"</span> <span class="attr">column</span>=<span class="string">"id"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">generator</span> <span class="attr">class</span>=<span class="string">"native"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">column</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">          <span class="comment">&lt;!-- 单方面的维护关系 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">set</span> <span class="attr">name</span>=<span class="string">"people"</span> <span class="attr">inverse</span>=<span class="string">"true"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">key</span> <span class="attr">column</span>=<span class="string">"cpid"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">one-to-many</span> <span class="attr">class</span>=<span class="string">"domain.Person"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">set</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">hibernate-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

<p>多的一方</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE hibernate-mapping PUBLIC</span></span><br><span class="line"><span class="meta">        "-//Hibernate/Hibernate Mapping DTD//EN"</span></span><br><span class="line"><span class="meta">        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd"&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">hibernate-mapping</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">class</span> <span class="attr">name</span>=<span class="string">"domain.Person"</span> <span class="attr">table</span>=<span class="string">"people"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">id</span> <span class="attr">name</span>=<span class="string">"uid"</span> <span class="attr">column</span>=<span class="string">"id"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">generator</span> <span class="attr">class</span>=<span class="string">"native"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">column</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">many-to-one</span> <span class="attr">name</span>=<span class="string">"customer"</span> <span class="attr">class</span>=<span class="string">"domain.Customer"</span> <span class="attr">column</span>=<span class="string">"clid"</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">hibernate-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="2-一对多的级联操作"><a href="#2-一对多的级联操作" class="headerlink" title="2.一对多的级联操作"></a>2.一对多的级联操作</h3><p>首先在一的一方的配置文件的set标签中写一个属性 cascade=”save-update,delete” 这个就是级联配置，然后我们在写具体的代码的时候我们只需要把多的一方设置到一的一方的对象set中就可了，此时直接操作了一的一方就能把多的一方顺便就操作了。</p>
<h3 id="3-多对多的配置"><a href="#3-多对多的配置" class="headerlink" title="3.多对多的配置"></a>3.多对多的配置</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> domain1;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.HashSet;</span><br><span class="line"><span class="keyword">import</span> java.util.Set;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">User</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> cid;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="comment">//表示多个联系人</span></span><br><span class="line">    <span class="keyword">private</span> Set&lt;Role&gt; roles=<span class="keyword">new</span> HashSet&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Set&lt;Role&gt; <span class="title">getRoles</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> roles;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setRoles</span><span class="params">(Set&lt;Role&gt; roles)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.roles = roles;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getCid</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> cid;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setCid</span><span class="params">(<span class="keyword">int</span> cid)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.cid = cid;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> domain1;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.HashSet;</span><br><span class="line"><span class="keyword">import</span> java.util.Set;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Role</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> cid;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="comment">//表示多个联系人</span></span><br><span class="line">    <span class="keyword">private</span> Set&lt;User&gt; users=<span class="keyword">new</span> HashSet&lt;&gt;();</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Set&lt;User&gt; <span class="title">getUsers</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> users;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setUsers</span><span class="params">(Set&lt;User&gt; users)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.users = users;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getCid</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> cid;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setCid</span><span class="params">(<span class="keyword">int</span> cid)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.cid = cid;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE hibernate-mapping PUBLIC</span></span><br><span class="line"><span class="meta">        "-//Hibernate/Hibernate Mapping DTD//EN"</span></span><br><span class="line"><span class="meta">        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd"&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">hibernate-mapping</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">class</span> <span class="attr">name</span>=<span class="string">"domain1.Role"</span> <span class="attr">table</span>=<span class="string">"role"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">id</span> <span class="attr">name</span>=<span class="string">"uid"</span> <span class="attr">column</span>=<span class="string">"id"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">generator</span> <span class="attr">class</span>=<span class="string">"native"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">column</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">set</span> <span class="attr">name</span>=<span class="string">"users"</span> <span class="attr">table</span>=<span class="string">"user_role"</span> <span class="attr">cascade</span>=<span class="string">"save-update,delete"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">key</span> <span class="attr">column</span>=<span class="string">"rid"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">many-to-many</span> <span class="attr">class</span>=<span class="string">"domain1.User"</span> <span class="attr">column</span>=<span class="string">"uid"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">set</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">hibernate-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version="1.0" encoding="utf-8"?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE hibernate-mapping PUBLIC</span></span><br><span class="line"><span class="meta">        "-//Hibernate/Hibernate Mapping DTD//EN"</span></span><br><span class="line"><span class="meta">        "http://www.hibernate.org/dtd/hibernate-mapping-3.0.dtd"&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">hibernate-mapping</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">class</span> <span class="attr">name</span>=<span class="string">"domain1.User"</span> <span class="attr">table</span>=<span class="string">"user"</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">id</span> <span class="attr">name</span>=<span class="string">"cid"</span> <span class="attr">column</span>=<span class="string">"id"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">generator</span> <span class="attr">class</span>=<span class="string">"native"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"name"</span> <span class="attr">column</span>=<span class="string">"name"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="tag">&lt;<span class="name">set</span> <span class="attr">name</span>=<span class="string">"roles"</span> <span class="attr">table</span>=<span class="string">"user_role"</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">key</span> <span class="attr">column</span>=<span class="string">"uid"</span>/&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">many-to-many</span> <span class="attr">class</span>=<span class="string">"domain1.Role"</span> <span class="attr">column</span>=<span class="string">"rid"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">set</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">hibernate-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-JAVA</category>
      </categories>
      <tags>
        <tag>Hibernate</tag>
      </tags>
  </entry>
  <entry>
    <title>Hibernate笔记(一)</title>
    <url>/2017/08/21/Java%20Web/Hibernate%E7%AC%94%E8%AE%B0-%E4%B8%80/</url>
    <content><![CDATA[<h3 id="1-导入jar包"><a href="#1-导入jar包" class="headerlink" title="1.导入jar包"></a>1.导入jar包</h3><p>jar包主要需要导入的有两个文件夹下面的jar包</p>
<ul>
<li>required  必须要导入的核心包</li>
<li>jpa 实体规范的包<h3 id="2-写一个实体类"><a href="#2-写一个实体类" class="headerlink" title="2.写一个实体类"></a>2.写一个实体类</h3>实体类必须要有一个唯一的id值，对应于表中的主键。其他的就是字段值，字段值不一定和数据库的字段一样，而是可以不一样，然后在配置文件中进行映射。<br>然后生成对应的get与set方法。<h3 id="3-写映射配置文件"><a href="#3-写映射配置文件" class="headerlink" title="3.写映射配置文件"></a>3.写映射配置文件</h3>一般位置就是放在该实体类的位置，名字后缀最好是hbm.xml,然后导入dtd的约束。这个约束是mapping的约束注意不要导错了。  <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">class</span> <span class="attr">name</span>=<span class="string">"类路径"</span> <span class="attr">table</span>=<span class="string">"表名"</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">id</span> <span class="attr">name</span>=<span class="string">"实体字段"</span> <span class="attr">column</span>=<span class="string">"表字段"</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 主键的生成策略，常用的就是native自增长，还有就是uuid使用128位的uuid算法产生uuid --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">generter</span>&gt;</span>native<span class="tag">&lt;/<span class="name">gengrter</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">id</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">""</span> <span class="attr">column</span>=<span class="string">""</span>/&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">""</span> <span class="attr">column</span>=<span class="string">""</span>/&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">""</span> <span class="attr">column</span>=<span class="string">""</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">class</span>&gt;</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<h3 id="4-写Hibernate的核心配置文件"><a href="#4-写Hibernate的核心配置文件" class="headerlink" title="4. 写Hibernate的核心配置文件"></a>4. 写Hibernate的核心配置文件</h3><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version='1.0' encoding='utf-8'?&gt;</span></span><br><span class="line"><span class="meta">&lt;!DOCTYPE hibernate-configuration PUBLIC</span></span><br><span class="line"><span class="meta">        "-//Hibernate/Hibernate Configuration DTD//EN"</span></span><br><span class="line"><span class="meta">        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd"&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">hibernate-configuration</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">session-factory</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!--DB base config--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"connection.url"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"connection.driver_class"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"connection.username"</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"connection.password"</span>/&gt;</span></span><br><span class="line"></span><br><span class="line">         <span class="comment">&lt;!--DB schema will be updated if needed--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"hbm2ddl.auto"</span>&gt;</span>update<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"hibernate.show_sql"</span>&gt;</span>true<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"hibernate.format_sql"</span>&gt;</span>true<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">property</span> <span class="attr">name</span>=<span class="string">"hibernate.dialect"</span>&gt;</span>org.hibernate.dialect.MySQLDialect<span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">&lt;!--DB Mapping--&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">mapping</span> <span class="attr">resource</span>=<span class="string">""</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">session-factory</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">hibernate-configuration</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h3 id="4-Hibernate代码"><a href="#4-Hibernate代码" class="headerlink" title="4.Hibernate代码"></a>4.Hibernate代码</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> SessionFactory ourSessionFactory;</span><br><span class="line">    <span class="comment">//由于创建sessionFactory的时候他会去创建表，非常耗资源，所以一个项目之创建一个SessionFactory</span></span><br><span class="line">    <span class="comment">//所以我们写一个工具类，在静态代码快中，只被加载一次</span></span><br><span class="line">    <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Configuration configuration = <span class="keyword">new</span> Configuration();</span><br><span class="line">            configuration.configure();</span><br><span class="line">            ourSessionFactory = configuration.buildSessionFactory();</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Throwable ex) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> ExceptionInInitializerError(ex);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Session <span class="title">getSession</span><span class="params">()</span> <span class="keyword">throws</span> HibernateException </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> ourSessionFactory.openSession();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(<span class="keyword">final</span> String[] args)</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">      <span class="comment">//session是单例的，不可多线程公用，也就是一个session只能被一个线程使用。我们可以使用threadLocal来把session和本地线程绑定。</span></span><br><span class="line">        <span class="keyword">final</span> Session session = getSession();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            System.out.println(<span class="string">"querying all the managed entities..."</span>);</span><br><span class="line">            <span class="keyword">final</span> Map metadataMap = session.getSessionFactory().getAllClassMetadata();</span><br><span class="line">            <span class="keyword">for</span> (Object key : metadataMap.keySet()) &#123;</span><br><span class="line">                <span class="keyword">final</span> ClassMetadata classMetadata = (ClassMetadata) metadataMap.get(key);</span><br><span class="line">                <span class="keyword">final</span> String entityName = classMetadata.getEntityName();</span><br><span class="line">                <span class="keyword">final</span> Query query = session.createQuery(<span class="string">"from "</span> + entityName);</span><br><span class="line">                <span class="keyword">for</span> (Object o : query.list()) &#123;</span><br><span class="line">                    System.out.println(<span class="string">"  "</span> + o);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">            session.close();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-实体类编写规范"><a href="#5-实体类编写规范" class="headerlink" title="5.实体类编写规范"></a>5.实体类编写规范</h3><ul>
<li>属性为私有的</li>
<li>必须有一个唯一的值一般都是id或者uid</li>
<li>属性一般都是使用包装类型，不要使用基本数据类型，就是防止出现null类型的。</li>
</ul>
<h3 id="6-CRUD操作"><a href="#6-CRUD操作" class="headerlink" title="6.CRUD操作"></a>6.CRUD操作</h3><h4 id="1-增加"><a href="#1-增加" class="headerlink" title="1.增加"></a>1.增加</h4><p>首先需要一个对象，这个也就是实体类的对象，然后设置字段的值，最后只需要调用session.save(user)即可插入一条数据。</p>
<h4 id="2-查询"><a href="#2-查询" class="headerlink" title="2.查询"></a>2.查询</h4><p>根据id查询，直接使用session.get(User.class,id) 第一个参数就是类，第二个是id值。最后返回的不是数组或者list什么的，而是一个实体类的对象。</p>
<h4 id="3-修改"><a href="#3-修改" class="headerlink" title="3.修改"></a>3.修改</h4><p>首先就需要进行查询，然后修改实体的属性，最后再使用update方法保存即可。save方法也可以，但是如果记录不存在他就会添加。</p>
<h4 id="4-删除"><a href="#4-删除" class="headerlink" title="4.删除"></a>4.删除</h4><p>还是先查到该对象，然后在调用delete方法。</p>
<h3 id="7-一级缓存"><a href="#7-一级缓存" class="headerlink" title="7.一级缓存"></a>7.一级缓存</h3><p>Hibernate的一级缓存默认是开启的，他的范围是在session开启导session关闭之间的，一级缓存的存放的对象只能就是持久太的数据。<br>一级缓存一个简单的例子就是，我们第一次查询一条数据的时候这条数据就被被放在内存中，而第二次再次查询此数据的时候他不会取数据库获取数据，而是直接从内存中取到这条数据。<br>Hibernate也是有耳机缓存的，但是现在基本不再使用，而是通过一些NoSQL数据库来完成，存放类似于SessionFactory对象之类的东西。</p>
<h3 id="8-session和本地线程绑定"><a href="#8-session和本地线程绑定" class="headerlink" title="8.session和本地线程绑定"></a>8.session和本地线程绑定</h3><p>因为session是单例的不可以多个线程同时访问，我们可以使用threadLocal来绑定session和Thread，但是这样比较麻烦，我们Hibernate已经提供了session和thread绑定的方法。<br>首先需要配置核心配置文件<br><property name="hibernate.current_session_context_class">thread</property><br>然后SessionFactory.getCurrentSession()就是获得的与本地线程绑定的线程。线程结束以后session自动被销毁，不需要我们自己关闭session，否则会报错。</p>
<h3 id="9-Hibernate三个查询对象"><a href="#9-Hibernate三个查询对象" class="headerlink" title="9.Hibernate三个查询对象"></a>9.Hibernate三个查询对象</h3><ul>
<li><p>Query  执行hql语句，调用对应的方法获取结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Query query=session.createQuery(<span class="string">"hql"</span>);  <span class="comment">//执行hql</span></span><br><span class="line">query.list();  <span class="comment">//获取一个list集合</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>Criteria  无需执行hql，直接调用方法获取结果</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Criteria criteria=session.createCriteria(User.class);  <span class="comment">//实体类的class</span></span><br><span class="line">criteria.list();</span><br></pre></td></tr></table></figure>
</li>
<li><p>SQLQuery  调用底层sql</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">SQLQuery sql=session.createSQLQuery(<span class="string">"sql"</span>);<span class="comment">//执行sql</span></span><br><span class="line">sql.list() <span class="comment">//每一条记录是一个数组</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
]]></content>
      <categories>
        <category>-Java Web</category>
      </categories>
      <tags>
        <tag>Hibernate</tag>
      </tags>
  </entry>
  <entry>
    <title>jsp指令和EL表达式</title>
    <url>/2017/08/21/Java%20Web/jsp%E6%8C%87%E4%BB%A4%E5%92%8CEL%E8%A1%A8%E8%BE%BE%E5%BC%8F/</url>
    <content><![CDATA[<h3 id="1-page指令"><a href="#1-page指令" class="headerlink" title="1.page指令"></a>1.page指令</h3><h4 id="1-pageEncoding"><a href="#1-pageEncoding" class="headerlink" title="1.pageEncoding"></a>1.pageEncoding</h4><p>指定jsp的编码</p>
<h4 id="2-contextType"><a href="#2-contextType" class="headerlink" title="2.contextType"></a>2.contextType</h4><p>设置响应头<br>这两个东西其实使用任意一个即可。<a id="more"></a></p>
<h4 id="3-errorPage"><a href="#3-errorPage" class="headerlink" title="3.errorPage"></a>3.errorPage</h4><p>如果这个页面抛出异常以后重定向到哪一个页面。</p>
<h3 id="2-静态包含-include"><a href="#2-静态包含-include" class="headerlink" title="2.静态包含 include"></a>2.静态包含 include</h3><p>他是静态包含和RequestDispatcher类似，但是就是包含的时期不一样。<br>静态包含就是在jsp编译成java的时候形成的，也就是最终是两个文件合并成了一个class，最后形成一个class文件<br>RequestDispatcher则是动态包含，他们在显示之前始终是两个java文件，两个class，在最后需要显示的时候才把内容整合发送给客户端。</p>
<h3 id="3-导入标签库-import"><a href="#3-导入标签库-import" class="headerlink" title="3.导入标签库 import"></a>3.导入标签库 import</h3><p>prefix制定标签库的前缀，uri就是标签库的位置。</p>
<h3 id="4-九大内置对象"><a href="#4-九大内置对象" class="headerlink" title="4.九大内置对象"></a>4.九大内置对象</h3><ul>
<li>out  jsp的输出流，向浏览器输出数据</li>
<li>page  当前的jsp对象，也就是在编译成大java中有page=this</li>
<li>config  对应的servletConfig对象</li>
<li>pageContext  包含其他的所有 域对象之一</li>
<li>request   </li>
<li>response</li>
<li>exception  </li>
<li>session HttpSession</li>
<li>application  servletContext</li>
</ul>
<h3 id="5-四大对象作用范围"><a href="#5-四大对象作用范围" class="headerlink" title="5.四大对象作用范围"></a>5.四大对象作用范围</h3><ul>
<li>ServletContext  一个应用</li>
<li>Session 一个会话</li>
<li>Request 一个请求</li>
<li>pageContext 一个jsp页面 ，一般用来jsp标签的数据传输</li>
</ul>
<h3 id="6-pageContext作用"><a href="#6-pageContext作用" class="headerlink" title="6.pageContext作用"></a>6.pageContext作用</h3><ol>
<li>代理其他的三大域对象<br>pageContext.setAttribute(“key”,”value”,pageContext.SESSION_SCOPE); 存放在session中代理了session</li>
<li>全域查找<br>pageContext.findAttribute(“key”) 在这四大域对象中依次查找</li>
<li>获取其他的jsp八大内置对象</li>
</ol>
<h3 id="7-JSP动作标签"><a href="#7-JSP动作标签" class="headerlink" title="7.JSP动作标签"></a>7.JSP动作标签</h3><ul>
<li>&lt;jsp:forward page=””/&gt;  转发</li>
<li>&lt;jsp:include page=””/&gt;  包含</li>
<li>&lt;jsp:param name=”” value=””/&gt;  为其他的标签传递参数</li>
</ul>
<h3 id="8-EL表达式"><a href="#8-EL表达式" class="headerlink" title="8.EL表达式"></a>8.EL表达式</h3><p>EL表达式主要就是用来代替JSP中的 &lt;%= %&gt; 这个标签的，他可以简单的用于输出语句</p>
<ol>
<li>输出四大域对象中的内容<br>${key}  这样就可以全域查找到四大域对象中的key变量  如果key是一个对象的话，我们希望获取这个对象里面的某个get方法的返回值，我们只需要key.key1  key1就是getKey1()这个方法的返回值。</li>
<li>精确的四大域对象查找<br>${pageScope.key}<br>${requestScope.key}<br>${sessionScope.key}<br>${applicationScope.key}</li>
<li>其他7个内置对象</li>
</ol>
<ul>
<li>param</li>
<li>paramValue</li>
<li>header</li>
<li>headerValue</li>
<li>cookie</li>
<li>pageContext  他可以获取其他所有的10个对象，因为它里面有其他十个对象的get方法。</li>
</ul>
<ol start="4">
<li>EL 函数库<br>导入对应的库，然后使用标签调用函数库</li>
</ol>
<h3 id="9-JSTL"><a href="#9-JSTL" class="headerlink" title="9.JSTL"></a>9.JSTL</h3><p>JSTL是EL的扩展，因为EL只是进行输出而已，但是有一些判断，遍历等等，这些操作就是JSTL。他需要引入jstl.jar<br>他有四大库，但是常用的只有两个一个是core另外一个就是formate标签库<br>注意导入的时候uri是jsp/core  或者 jsp/formate</p>
<h4 id="1-core标签库-c标签"><a href="#1-core标签库-c标签" class="headerlink" title="1.core标签库(c标签)"></a>1.core标签库(c标签)</h4><ul>
<li>out 输出标签  value就是要输出的变量</li>
<li>set 设置某个变量的值  var变量名  value变量值</li>
<li>url url格式化的标签  value 自动添加上项目名  里面如果加param标签那么就可以传递参数 name/value</li>
<li>remove 删除域变量  var变量名  scope域范围，不写的话删除全域的对象中的此值</li>
<li>if if语句  test 判断的条件 ${not empty key} 如果key不是空</li>
<li>forEach<ul>
<li>计数方式  var循环变量  begin循环变量从几开始  end到几结束  step设置步长</li>
<li>用来遍历  items需要迭代的变量  var每一次迭代的变量</li>
</ul>
</li>
<li>choose/when  多分支  <figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">c:choose</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">c:when</span> <span class="attr">test</span>&gt;</span><span class="tag">&lt;/<span class="name">c:when</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">c:when</span> <span class="attr">test</span>&gt;</span><span class="tag">&lt;/<span class="name">c:when</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">c:when</span> <span class="attr">test</span>&gt;</span><span class="tag">&lt;/<span class="name">c:when</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">c:when</span> <span class="attr">test</span>&gt;</span><span class="tag">&lt;/<span class="name">c:when</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">c:choose</span>&gt;</span></span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="2-formate标签库"><a href="#2-formate标签库" class="headerlink" title="2.formate标签库"></a>2.formate标签库</h4><ul>
<li>formateDate  value需要格式化的变量   pattern yyyy-MM-dd HH:mm:ss</li>
<li>formateNumber value变量   pattern 0.00 需要小数点两位  四舍五入  </li>
</ul>
]]></content>
      <categories>
        <category>-JAVA</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaWeb基础二:JSP</title>
    <url>/2017/08/21/Java%20Web/JavaW_eb%E5%9F%BA%E7%A1%80%E4%B8%89_%20JSP/</url>
    <content><![CDATA[<h2 id="1-JSP-标签"><a href="#1-JSP-标签" class="headerlink" title="1. JSP 标签"></a>1. JSP 标签</h2><p>我们常用的 jsp 标签有两种，实际上有三种 jsp 标签。</p>
<ol>
<li>&lt;% %&gt; 这种就是可以放 java 代码</li>
<li>&lt;%= %&gt; 这种就是输出语句，类似 PHP 中的简写语法</li>
<li>&lt;%! %&gt; 放各种生命代码，基本不用。</li>
</ol>
<p>其实JSP最终还是一个Servlet，主要他的优点在于，可以在一个Servlet中直接写html代码，防止我们去写很多 out.print(“html代码”) ，或者说我们可以在html中写 java 脚本。但是并不是说我们使用了jsp 就不再使用Servlet，他们之间是有区别的，分工不一样。</p>
<ul>
<li>jsp主要就用来显示表单，显示数据结果</li>
<li>Servlet就是用来处理数据，返回给jsp<h2 id="2-JSP-原理"><a href="#2-JSP-原理" class="headerlink" title="2. JSP 原理"></a>2. JSP 原理</h2>服务器启动后的第一次访问时，服务器会把这个文件转化为java文件，并且这个java文件是实现了servlet接口的一个类然后再把此java文件，转化成一个class文件，实例化对象，然后调用service方法。而在第二次访问的时候就是直接调用service方法。这也就是第一次惩戒。jsp被编译成的java放在了tomcat的work目录下面。<br>具体的看一下jsp编译出来的文件的时候就会发现里面是有六个private对象，还有两个分别就是request和response被tomcat传入。也就是有8个对象 ，还有一个exception没在这。所以这里就知道了jsp的九大内置对象。</li>
</ul>
<h2 id="3-JSP指令"><a href="#3-JSP指令" class="headerlink" title="3. JSP指令"></a>3. JSP指令</h2><h3 id="1-page指令"><a href="#1-page指令" class="headerlink" title="1. page指令"></a>1. page指令</h3><h5 id="1-pageEncoding"><a href="#1-pageEncoding" class="headerlink" title="1. pageEncoding"></a>1. pageEncoding</h5><p>指定jsp的编码</p>
<h5 id="2-contextType"><a href="#2-contextType" class="headerlink" title="2. contextType"></a>2. contextType</h5><p>设置响应头这两个东西其实使用任意一个即可。</p>
<h5 id="3-errorPage"><a href="#3-errorPage" class="headerlink" title="3. errorPage"></a>3. errorPage</h5><p>如果这个页面抛出异常以后重定向到哪一个页面。</p>
<h5 id="4-IsErrorPage-true"><a href="#4-IsErrorPage-true" class="headerlink" title="4. IsErrorPage=true"></a>4. IsErrorPage=true</h5><p>标记这个页面可使用 expection 对象，其他页面不行。</p>
<h5 id="5-session-”true”"><a href="#5-session-”true”" class="headerlink" title="5. session=”true”"></a>5. session=”true”</h5><p>当前页可使用 session 对象</p>
<h3 id="2-静态包含-include"><a href="#2-静态包含-include" class="headerlink" title="2. 静态包含 include"></a>2. 静态包含 include</h3><p>他是静态包含和RequestDispatcher类似，但是就是包含的时期不一样。  静态包含就是在jsp编译成java的时候形成的，也就是最终是两个文件合并成了一个class，最后形成一个class文件。<br>RequestDispatcher则是动态包含，他们在显示之前始终是两个java文件，两个class，在最后需要显示的时候才把内容整合发送给客户端。</p>
<h3 id="3-导入标签库-import"><a href="#3-导入标签库-import" class="headerlink" title="3. 导入标签库 import"></a>3. 导入标签库 import</h3><p>prefix制定标签库的前缀，uri就是标签库的位置。</p>
<h3 id="4-九大内置对象"><a href="#4-九大内置对象" class="headerlink" title="4.九大内置对象"></a>4.九大内置对象</h3><ul>
<li>out  jsp的输出流，向浏览器输出数据</li>
<li>page  当前的jsp对象，也就是在编译成大java中有page=this</li>
<li>config  对应的servletConfig对象</li>
<li>pageContext  代理其他的所有域对象，在标签之间传数据。</li>
<li>request   </li>
<li>response</li>
<li>exception  </li>
<li>session （HttpSession）</li>
<li>application  （servletContext）</li>
</ul>
<h3 id="5-四大对象作用范围"><a href="#5-四大对象作用范围" class="headerlink" title="5.四大对象作用范围"></a>5.四大对象作用范围</h3><ul>
<li>ServletContext  一个应用</li>
<li>Session 一个会话</li>
<li>Request 一个请求</li>
<li>pageContext 一个jsp页面 ，一般用来jsp标签的数据传输</li>
</ul>
<h3 id="6-pageContext作用"><a href="#6-pageContext作用" class="headerlink" title="6.pageContext作用"></a>6.pageContext作用</h3><ol>
<li>代理其他的三大域对象<br>pageContext.setAttribute(“key”,”value”,pageContext.SESSION_SCOPE); 存放在session中代理了session</li>
<li>全域查找<br>pageContext.findAttribute(“key”) 在这四大域对象中依次查找</li>
<li>获取其他的jsp八大内置对象</li>
</ol>
<h3 id="7-JSP动作标签"><a href="#7-JSP动作标签" class="headerlink" title="7.JSP动作标签"></a>7.JSP动作标签</h3><ul>
<li>&lt;jsp:forward page=””/&gt;  转发</li>
<li>&lt;jsp:include page=””/&gt;  包含</li>
<li>&lt;jsp:param name=”” value=””/&gt;  为其他的标签传递参数</li>
</ul>
<h3 id="8-EL表达式"><a href="#8-EL表达式" class="headerlink" title="8.EL表达式"></a>8.EL表达式</h3><p>EL表达式主要就是用来代替JSP中的 &lt;%= %&gt; 这个标签的，他可以简单的用于输出语句</p>
<ol>
<li>输出四大域对象中的内容<br>${key}  这样就可以全域查找到四大域对象中的key变量  如果key是一个对象的话，我们希望获取这个对象里面的某个get方法的返回值，我们只需要key.key1  key1就是getKey1()这个方法的返回值。</li>
<li>精确的四大域对象查找<br>${pageScope.key}<br>${requestScope.key}<br>${sessionScope.key}<br>${applicationScope.key}</li>
<li>其他7个内置对象</li>
</ol>
<ul>
<li>param</li>
<li>paramValue</li>
<li>header</li>
<li>headerValue</li>
<li>cookie</li>
<li>pageContext  他可以获取其他所有的10个对象，因为它里面有其他十个对象的get方法。</li>
</ul>
<ol>
<li>EL 函数库<br>导入对应的库，然后使用标签调用函数库</li>
</ol>
<h3 id="9-JSTL"><a href="#9-JSTL" class="headerlink" title="9.JSTL"></a>9.JSTL</h3><p>JSTL是EL的扩展，因为EL只是进行输出而已，但是有一些判断，遍历等等，这些操作就是JSTL。他需要引入jstl.jar<br>他有四大库，但是常用的只有两个一个是core另外一个就是formate标签库<br>注意导入的时候uri是jsp/core  或者 jsp/formate</p>
<h4 id="1-core标签库-c标签"><a href="#1-core标签库-c标签" class="headerlink" title="1.core标签库(c标签)"></a>1.core标签库(c标签)</h4><ul>
<li>out 输出标签  value就是要输出的变量</li>
<li>set 设置某个变量的值  var变量名  value变量值</li>
<li>url url格式化的标签  value 自动添加上项目名  里面如果加param标签那么就可以传递参数 name/value</li>
<li>remove 删除域变量  var变量名  scope域范围，不写的话删除全域的对象中的此值</li>
<li>if if语句  test 判断的条件 ${not empty key} 如果key不是空</li>
<li>forEach<ul>
<li>计数方式  var循环变量  begin循环变量从几开始  end到几结束  step设置步长</li>
<li>用来遍历  items需要迭代的变量  var每一次迭代的变量</li>
</ul>
</li>
<li>choose/when  多分支  </li>
</ul>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">c:choose</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">c:when</span> <span class="attr">test</span>&gt;</span><span class="tag">&lt;/<span class="name">c:when</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">c:when</span> <span class="attr">test</span>&gt;</span><span class="tag">&lt;/<span class="name">c:when</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">c:when</span> <span class="attr">test</span>&gt;</span><span class="tag">&lt;/<span class="name">c:when</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">c:when</span> <span class="attr">test</span>&gt;</span><span class="tag">&lt;/<span class="name">c:when</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">c:choose</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="2-formate标签库"><a href="#2-formate标签库" class="headerlink" title="2.formate标签库"></a>2.formate标签库</h4><ul>
<li>formateDate  value需要格式化的变量   pattern yyyy-MM-dd HH:mm:ss</li>
<li>formateNumber value变量   pattern 0.00 需要小数点两位  四舍五入  </li>
</ul>
]]></content>
      <categories>
        <category>-Java Web</category>
      </categories>
      <tags>
        <tag>Java Web</tag>
      </tags>
  </entry>
  <entry>
    <title>Java8新特性</title>
    <url>/2017/08/19/Java%20SE/Java8%E6%96%B0%E7%89%B9%E6%80%A7/</url>
    <content><![CDATA[<h3 id="1-HashMap"><a href="#1-HashMap" class="headerlink" title="1.HashMap"></a>1.HashMap</h3><p>首先就是对 java 的 HashMap 进行了修改，以前是通过 hashCode 方法来判断他们的地址值是否一样<br>，如果相同的话再使用 equals 方法比对他们的 equals 返回的结果是否一样，是则不存入否则的话就形成一个链表<br>直接挂在原有元素的后面。这个地方就有一个比较大的问题就是使用 hashCode 方法的时候组字坏的情况时需要和每一个元素<br>比对 hashCode 比较的次数就变多了。 这里后来提出了一种解决方案，就相当于一个数组，然后数组的元素就是一个 entry<a id="more"></a><br>这样的话 hashCode 值只需要成为数组的下标即可，可以直接取出数据，然后再调用 equals 方法，这里还需要注意的是，这个数组<br>一开始是 16 个，但是当容量达到总容量的 75% 的时候就进行扩容，而不可以等到 100% 原因就是防止有一些元素始终在某个位置插入<br>而其他的位置始终为空。这就是新特性对于 hashCode 方法这一块的优化方案。然后对于后面的 equals 方法形成的链表，java8也进行<br>了优化，具体就是当元素的总个数超过 64 链表的长度超过 8 的时候把链表元素形成红黑树，红黑树也是二叉树的一种，可以简单的理解为<br>若元素大于根元素放左边小于放右边。这样以来再链表查询的时候就可以快速定位。</p>
<h3 id="2-CurrentHashMap"><a href="#2-CurrentHashMap" class="headerlink" title="2.CurrentHashMap"></a>2.CurrentHashMap</h3><p>这里使用了 CAS 算法，从而实现了无锁添加元素，因为 CAS 算法是受底层的操作系统的支持的，所以效率很高。</p>
<h3 id="3-永久区-PremGen"><a href="#3-永久区-PremGen" class="headerlink" title="3.永久区 PremGen"></a>3.永久区 PremGen</h3><p>以前堆内存中是有一块内存称之为永久区，这个区主要存放的就是类的加载信息，尤其是核心类库。但是这个区一般很少被垃圾回收机制所回收。在 java8 中彻底的把这个区去掉了，二天了一个元空间区 Matespace 他是用的物理内存，而不是从系统申请来的。</p>
<h3 id="4-Lambda-表达式"><a href="#4-Lambda-表达式" class="headerlink" title="4.Lambda 表达式"></a>4.Lambda 表达式</h3><p>java8 中的 Lambda 表达式就是一种语法糖，简化书写用的。例如我们需要对一个员工集合里面的元素进行筛选，筛选出工资大于 5000 的员工，然后我们还需要筛选出年龄小于 50 的员工，这样的话我们就会发现这两个函数功能虽然不一样其实很多东西就是重复的，这里可以使用一个设计模式就是策略设计模式，这个设计模式主要的实现方式就是定义一个接口，然后只用写一个方法，在这个方法的参数列表中使用这个接口，使用这个接口的方法来处理我们的需求。类似于所说的回调函数。当我们调用方法的时候我们需要传入这个接口的实际对象，这时候这根 函数就会因为我们传入的对象不同从而执行不同的方法。<br>这个需要传入的对象，我们可以使用匿名内部类来实现，这个时候我们发现这个匿名内部类最重要的就是实现的接口的那个函数的方法体。我们这里就是可使用 Lambda 表达式来代替匿名内部类。</p>
<h3 id="5-Lambda-表达式的语法"><a href="#5-Lambda-表达式的语法" class="headerlink" title="5. Lambda 表达式的语法"></a>5. Lambda 表达式的语法</h3><p>Lambda 表达式的语法很简单，左侧就是需要实现接口的函数的参数，然后这个东西当参数只有一个的时候圆括号可以不写，然后这里面的参数都不用带类型，当然带上也没问题，主要是 java8 的新特新中有一个就是类型推断，例如说在写泛型的时候 new 对象的时候我们并没有写泛型的名称。而是 java 自动推断出来的。<br>其右侧就是正宗的方法体，这个方法体如果有多行代码的时候呢我们需要时使用大括号包含起来。<br>否则就可以直接是用一行代码即可。当只有一行代码的时候我们返回值的 return 语句都可以不用写，直接写表达式即可。<br>但是注意这个地方我们的 Lambda 表达式只实现了接口中的一个方法，那如果接口中有多个方法，表达式就不知道需要实现哪一个，所以这里就有一个约定就是接口必须是函数式接口，所谓函数是借口就是接口中只有一个函数，然后我们可以使用 @FunctionalInterface 这个注解来声明他就是一个函数式接口。</p>
<h3 id="6-四大核心接口"><a href="#6-四大核心接口" class="headerlink" title="6. 四大核心接口"></a>6. 四大核心接口</h3><h4 id="1-Consumer-消费接口"><a href="#1-Consumer-消费接口" class="headerlink" title="1. Consumer 消费接口"></a>1. Consumer 消费接口</h4><p>  传一个参数没有返回值<br>  T accept(T t)</p>
<h4 id="2-Supplier-供给接口"><a href="#2-Supplier-供给接口" class="headerlink" title="2. Supplier 供给接口"></a>2. Supplier 供给接口</h4><p>  不传参数，返回一个东西<br>  T get()</p>
<h4 id="3-Funtion-函数接口"><a href="#3-Funtion-函数接口" class="headerlink" title="3. Funtion  函数接口"></a>3. Funtion  函数接口</h4><p>  传进去一个T，返回一个M<br>  M apply(T t)</p>
<h4 id="4-Pridecate-断言接口"><a href="#4-Pridecate-断言接口" class="headerlink" title="4. Pridecate 断言接口"></a>4. Pridecate 断言接口</h4><p>  boolean test(T t)</p>
]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>Java8</tag>
      </tags>
  </entry>
  <entry>
    <title>Java多线程JUC</title>
    <url>/2017/08/19/Java%20SE/Java%E5%A4%9A%E7%BA%BF%E7%A8%8BJUC/</url>
    <content><![CDATA[<h3 id="1-volatile-关键字"><a href="#1-volatile-关键字" class="headerlink" title="1. volatile 关键字"></a>1. volatile 关键字</h3><p>多线程访问的时候，一个比较严重的问题就是内存不可见，其实在内存访问的时候每一个线程都有一个自己的缓冲区，每次在做修改的时候都是从主存取到数据，然后放到自己的缓冲区中，在做完修改之后放回主存。这样每一个线程之间的变量是不可见的。造成读到的数据可能始终就是错误的，因此有一个关键字可以使得这个共享变量称为透明的。就好像所有的操作就直接是在内存中操作一样，因为他一直不停的去同步主存的数据。<a id="more"></a></p>
<h3 id="2-原子性"><a href="#2-原子性" class="headerlink" title="2.原子性"></a>2.原子性</h3><p>i++ 这个运算，其实在底层低用的就是临时变量的方式，这样的话虽然是一个表达式，但是在多线程的时候就会出现安全问题。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> atomic;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span>: lwen</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Date</span>: Created in 2017/8/19 15:26</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Description</span>:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">int</span> i=<span class="number">0</span>;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">200</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(i++);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Test t=<span class="keyword">new</span> Test();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">new</span> Thread(t).start();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>最后运行的结果，显然就是 volatile 这个关键字他只是让变量课件也就是都是在主存中操作，但是他没有互斥的作用简单来说没有锁来控制，他们都及时的从主存里面拿到了数据，但是他们拿到最新的数据了，正在运算的时候有人提交了结果，所以导致重复元素。最根本原因是 i++ 有三步操作，读，运算，写</p>
<blockquote>
<blockquote>
</blockquote>
<p>1<br>3<br>2<br>0<br>4<br>7<br>6<br>5<br>8<br>8<br>Process finished with exit code 0</p>
</blockquote>
<p>java 底层提供了一些原子性的变量，例如 AtomicInteger 这些东西他们既然是源自的首先就是可见的。这些东西的底层主要是使用了 CAS ( CompareAndSet ) 算法，CAS 算法主要是操作系统在硬件上提供的支持。<br>这个算法有三个重要的参数：第一个就是 V 就是运算前从内存读取的值 A 写入前从内存中读取的值 B 最终需要写入的值。在写入前做一次判断当且仅当 V == A  时才会写入 B 否则什么操作也不做。</p>
<h3 id="3-ConcurrentHahsMap-安全的-HashMap"><a href="#3-ConcurrentHahsMap-安全的-HashMap" class="headerlink" title="3.ConcurrentHahsMap 安全的 HashMap"></a>3.ConcurrentHahsMap 安全的 HashMap</h3><p>这是一个线程安全的 HashMap ，说道线程安全的　HahsMap 自然就有 HashTable 但是这个效率非常的低，主要就是因为他的封锁粒度太大，他锁的是整个 HashTable 也就是两个不相干的 HashTable 也是互斥访问的，在 jdk1.5 以后使用的就是 ConcurrentHahsMap 这个东西那个时候主要使用的锁分段机制，也就是在原来的基础上把 HashTable 分16段，每一段对应一个 HashMap 这样的话两个互不相干的 HashMap 是可以同步访问的。</p>
<h3 id="4-CountDownLatch-闭锁"><a href="#4-CountDownLatch-闭锁" class="headerlink" title="4.CountDownLatch 闭锁"></a>4.CountDownLatch 闭锁</h3><p>所谓的闭锁说白了就是该线程会等到洽谈所有线程的代码都运行结束了才开始运行，他的底层实现就是维护一个变量，这个变量就是当前存活的线程的数量，当他减成0了也就是其他的线程都运行完了，此时这个闭锁线程可以开始。例如说我们开十个线程做某一件事情，我们在主线程中统计这十个线程的总的运行时间。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> latch;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> com.sun.javafx.sg.prism.web.NGWebView;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.CountDownLatch;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span>: lwen</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Date</span>: Created in 2017/8/19 16:06</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Description</span>:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Latch</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> CountDownLatch latch;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Latch</span><span class="params">(CountDownLatch latch)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.latch = latch;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(Thread.currentThread());</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            Thread.sleep(<span class="number">10</span>);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">            e.printStackTrace();</span><br><span class="line">        &#125;</span><br><span class="line">        latch.countDown();  <span class="comment">//线程结束以后，需要给维护的变量减一</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> start= (<span class="keyword">int</span>) System.currentTimeMillis();</span><br><span class="line">        CountDownLatch downLatch= <span class="keyword">new</span> CountDownLatch(<span class="number">10</span>);  <span class="comment">//定义十个线程</span></span><br><span class="line">        Latch latch=<span class="keyword">new</span> Latch(downLatch);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">10</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">new</span> Thread(latch).start();</span><br><span class="line">        &#125;</span><br><span class="line">        downLatch.await();  <span class="comment">//当线程没有完全结束的时候，主线程需要等待</span></span><br><span class="line">        <span class="keyword">int</span> end= (<span class="keyword">int</span>) System.currentTimeMillis();</span><br><span class="line">        System.out.println(end-start);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-线程的第三种创建方式"><a href="#4-线程的第三种创建方式" class="headerlink" title="4.线程的第三种创建方式"></a>4.线程的第三种创建方式</h3><p>一般我们创建线程我们使用的都是继承 Thread 类，或者实现 Runnable 接口，但是还是主要使用的是实现 Runnable 接口，但是注意这两个方式他们嗾使没有返回值的东西。也就是我么不能多线程没有办法返回一些结果。这里就出现了创建线程的第三种方式，也就是可以得到返回值的方式，就是使用 Callable 接口，这个接口的使用需要使用 FutureTask 类，而这个类实现了 Runnable 和 Future 接口，他的具体的使用方式和 Runnable 接口还是有一点不一样的。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> future;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.omg.PortableInterceptor.INACTIVE;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.Callable;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.ExecutionException;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.FutureTask;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span>: lwen</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Date</span>: Created in 2017/8/19 16:23</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Description</span>:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Future</span> <span class="keyword">implements</span> <span class="title">Callable</span>&lt;<span class="title">Integer</span>&gt;</span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">call</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">1000</span>; i++) &#123;</span><br><span class="line">            sum+=i;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> sum;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> ExecutionException, InterruptedException </span>&#123;</span><br><span class="line">        Future future=<span class="keyword">new</span> Future();</span><br><span class="line">        FutureTask&lt;Integer&gt; task=<span class="keyword">new</span> FutureTask&lt;Integer&gt;(future);</span><br><span class="line">        <span class="keyword">new</span> Thread(task).start();</span><br><span class="line"></span><br><span class="line">        System.out.println(task.get());;<span class="comment">//获取最终的返回值，但是注意这个地方的这个方法其实就是一个闭锁，前面的那个县城没有执行完，这个地方是不会执行的</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="5-高级同步"><a href="#5-高级同步" class="headerlink" title="5.高级同步"></a>5.高级同步</h3><p>解决同步问题总共就有三种方式，分别就是同步代码块，同步函数，和同步锁。<br>也就是手动的声明 lock 加锁，然后使用 unlock 释放锁</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> lock;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.Lock;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.ReentrantLock;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span>: lwen</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Date</span>: Created in 2017/8/19 16:33</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Description</span>:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Ticket</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> ticket=<span class="number">100</span>;</span><br><span class="line">    Lock lock=<span class="keyword">new</span> ReentrantLock();</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>) &#123;</span><br><span class="line">            lock.lock();</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                <span class="keyword">if</span> (ticket != <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">20</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                    ticket--;</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">":"</span> + ticket);</span><br><span class="line"></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">                lock.unlock();  <span class="comment">//解锁必须放在finally里面保证能够执行到</span></span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Ticket t=<span class="keyword">new</span> Ticket();</span><br><span class="line">        <span class="keyword">new</span> Thread(t,<span class="string">"窗口一"</span>).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(t,<span class="string">"窗口二"</span>).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(t,<span class="string">"窗口三"</span>).start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="6-生产者和消费者的线程同步问题"><a href="#6-生产者和消费者的线程同步问题" class="headerlink" title="6.生产者和消费者的线程同步问题"></a>6.生产者和消费者的线程同步问题</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> product;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.logging.Level;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span>: lwen</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Date</span>: Created in 2017/8/19 16:57</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Description</span>:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Clerk</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> production=<span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">get</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (production&gt;=<span class="number">1</span>)&#123;  <span class="comment">//notifyAll 可能会形成假唤醒问题，所以说必须要使用while一直判断而不能使用if</span></span><br><span class="line">            System.out.println(<span class="string">"已满"</span>);</span><br><span class="line">            <span class="keyword">this</span>.wait();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(Thread.currentThread().getName()+<span class="string">":"</span>+ ++production);</span><br><span class="line">        <span class="keyword">this</span>.notifyAll();</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">sale</span><span class="params">()</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (production&lt;=<span class="number">0</span>)&#123;</span><br><span class="line">            System.out.println(<span class="string">"卖完"</span>);</span><br><span class="line">            <span class="keyword">this</span>.wait();</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(Thread.currentThread().getName()+<span class="string">":"</span>+ --production);</span><br><span class="line">        <span class="keyword">this</span>.notifyAll();</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Product</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Clerk clerk;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Product</span><span class="params">(Clerk clerk)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.clerk = clerk;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">20</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">20</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                clerk.get();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Consumer</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Clerk clerk;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Consumer</span><span class="params">(Clerk clerk)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.clerk = clerk;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">20</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">20</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                clerk.sale();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Clerk clerk=<span class="keyword">new</span> Clerk();</span><br><span class="line">        Product p=<span class="keyword">new</span> Product(clerk);</span><br><span class="line">        Consumer consumer=<span class="keyword">new</span> Consumer(clerk);</span><br><span class="line">        <span class="keyword">new</span> Thread(p,<span class="string">"生产者1"</span>).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(p,<span class="string">"生产者2"</span>).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(consumer,<span class="string">"消费者1"</span>).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(consumer,<span class="string">"消费者2"</span>).start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="7-读写锁"><a href="#7-读写锁" class="headerlink" title="7.读写锁"></a>7.读写锁</h3><p>排斥写写和读写同时进行</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> readandwrite;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.ReadWriteLock;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.locks.ReentrantReadWriteLock;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span>: lwen</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Date</span>: Created in 2017/8/19 17:29</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Description</span>:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ReadWriteLockDemo</span> </span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> number=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">private</span> ReadWriteLock lock=<span class="keyword">new</span> ReentrantReadWriteLock();</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">read</span><span class="params">()</span></span>&#123;</span><br><span class="line">        lock.readLock().lock();</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            System.out.println(Thread.currentThread().getName()+<span class="string">":"</span>+number);</span><br><span class="line">        &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.readLock().unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">write</span><span class="params">(<span class="keyword">int</span> number)</span> <span class="keyword">throws</span> InterruptedException </span>&#123;</span><br><span class="line">        lock.writeLock().lock();</span><br><span class="line">        Thread.sleep(<span class="number">20</span>);</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="keyword">this</span>.number=number;</span><br><span class="line">            System.out.println(<span class="string">"write"</span>);</span><br><span class="line">        &#125;<span class="keyword">finally</span> &#123;</span><br><span class="line">            lock.writeLock().unlock();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ReadWriteLockDemo readWriteLockDemo=<span class="keyword">new</span> ReadWriteLockDemo();</span><br><span class="line">        <span class="keyword">new</span> Thread(<span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">            <span class="meta">@Override</span></span><br><span class="line">            <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    readWriteLockDemo.write(<span class="number">20</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;).start();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">20</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">new</span> Thread(<span class="keyword">new</span> Runnable() &#123;</span><br><span class="line">                <span class="meta">@Override</span></span><br><span class="line">                <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">                    readWriteLockDemo.read();</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).start();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="4-线程池"><a href="#4-线程池" class="headerlink" title="4.线程池"></a>4.线程池</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> pool;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.*;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span>: lwen</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Date</span>: Created in 2017/8/19 17:56</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Description</span>:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(Thread.currentThread().getName());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test1</span> <span class="keyword">implements</span> <span class="title">Callable</span>&lt;<span class="title">Integer</span>&gt;</span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> Integer <span class="title">call</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">        <span class="keyword">int</span> sum=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt;= <span class="number">100</span>; i++) &#123;</span><br><span class="line">            sum+=i;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> sum;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> ExecutionException, InterruptedException </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//注意线程池里面装的都是线程，这些线程到时候再次使用的时候不需要再次创建，也就是代表操作系统不需要再次分配资源，分配pid等等</span></span><br><span class="line">        <span class="comment">//而不是说里面装的是任务，这里仅仅就是线程而已</span></span><br><span class="line">        ExecutorService executor= Executors.newFixedThreadPool(<span class="number">10</span>);  <span class="comment">//固定大小的线程池</span></span><br><span class="line">        ExecutorService executor1=Executors.newSingleThreadExecutor(); <span class="comment">//一个线程的线程池</span></span><br><span class="line">        ExecutorService executor2=Executors.newCachedThreadPool(); <span class="comment">//动态变化的线程池</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">20</span>; i++) &#123;</span><br><span class="line">            executor.submit(<span class="keyword">new</span> Test());</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        Future&lt;Integer&gt; future=executor1.submit(<span class="keyword">new</span> Test1());</span><br><span class="line">        System.out.println(future.get());</span><br><span class="line"></span><br><span class="line">        executor.shutdown(); <span class="comment">//只有当线程池关闭的时候  程序才会停止</span></span><br><span class="line">        executor1.shutdown();</span><br><span class="line">        executor2.shutdown();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="8-线程调度："><a href="#8-线程调度：" class="headerlink" title="8.线程调度："></a>8.线程调度：</h3><p>线程调度可以决定在多久之后执行什么操作。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> schedule;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Random;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.*;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Author</span>: lwen</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Date</span>: Created in 2017/8/19 18:11</span></span><br><span class="line"><span class="comment"> * <span class="doctag">@Description</span>:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Main</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> ExecutionException, InterruptedException </span>&#123;</span><br><span class="line">        ScheduledExecutorService service= Executors.newScheduledThreadPool(<span class="number">5</span>);</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">5</span>; i++) &#123;</span><br><span class="line">            Future&lt;Integer&gt; future=service.schedule(<span class="keyword">new</span> Callable&lt;Integer&gt;() &#123;</span><br><span class="line">                <span class="meta">@Override</span></span><br><span class="line">                <span class="function"><span class="keyword">public</span> Integer <span class="title">call</span><span class="params">()</span> <span class="keyword">throws</span> Exception </span>&#123;</span><br><span class="line">                    <span class="keyword">int</span> random=<span class="keyword">new</span> Random().nextInt(<span class="number">100</span>);</span><br><span class="line">                    System.out.println(Thread.currentThread().getName()+<span class="string">":"</span>+random);</span><br><span class="line">                    <span class="keyword">return</span> random;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;,<span class="number">1</span>, TimeUnit.SECONDS);</span><br><span class="line">            System.out.println(future.get());</span><br><span class="line">        &#125;</span><br><span class="line">        service.shutdown();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>多线程</tag>
      </tags>
  </entry>
  <entry>
    <title>内部类的作用</title>
    <url>/2017/08/19/Java%20SE/%E5%86%85%E9%83%A8%E7%B1%BB%E7%9A%84%E4%BD%9C%E7%94%A8/</url>
    <content><![CDATA[<h3 id="一、-作用"><a href="#一、-作用" class="headerlink" title="一、 作用"></a>一、 作用</h3><ol>
<li><p>内部类可以很好的实现隐藏，一般的非内部类，是不允许有 private 与protected权限的，但内部类可以加上这几个修饰词。</p>
</li>
<li><p>内部类拥有外围类的所有元素的访问权限，可以直接访问外部类的私有属性及方法</p>
</li>
<li><p>可以间接实现多重继承，多个内部类继承不同的类形成了一个类中复用了多个类的方法。</p>
</li>
<li><p>可以避免在继承一个父类和一个接口的时候导致的方法重复定义。</p>
<a id="more"></a>

</li>
</ol>
<h3 id="二、详解"><a href="#二、详解" class="headerlink" title="二、详解"></a>二、详解</h3><h4 id="1-实现隐藏"><a href="#1-实现隐藏" class="headerlink" title="1.实现隐藏"></a>1.实现隐藏</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Example</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//private 修饰词</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="class"><span class="keyword">class</span> <span class="title">InsideClass</span> <span class="keyword">implements</span> <span class="title">InterfaceTest</span></span></span><br><span class="line"><span class="class"></span></span><br><span class="line"><span class="class">    </span>&#123;</span><br><span class="line"></span><br><span class="line">         <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">test</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">         </span>&#123;</span><br><span class="line"></span><br><span class="line">             System.out.println(<span class="string">"这是一个测试"</span>);</span><br><span class="line"></span><br><span class="line">         &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="2-可以无条件地访问外围类的所有元素"><a href="#2-可以无条件地访问外围类的所有元素" class="headerlink" title="2.可以无条件地访问外围类的所有元素"></a>2.可以无条件地访问外围类的所有元素</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TagBean</span> </span>&#123;</span><br><span class="line"> <span class="keyword">private</span> String name=<span class="string">"lwen"</span>;</span><br><span class="line">   <span class="keyword">private</span> <span class="class"><span class="keyword">class</span> <span class="title">InTest</span></span>&#123;</span><br><span class="line">     <span class="function"><span class="keyword">public</span> <span class="title">InTest</span><span class="params">()</span></span>&#123;</span><br><span class="line">         System.out.println(name);</span><br><span class="line">     &#125;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="3-可以间接实现多重继承"><a href="#3-可以间接实现多重继承" class="headerlink" title="3.可以间接实现多重继承"></a>3.可以间接实现多重继承</h4><p>​    个特点非常重要，个人认为它是内部类存在的最大理由之一。正是由于他的存在使得Java的继承机制更加完善。大家都知道Java只能继承一个类，它的多重继承在我们没有学习内部类之前是用接口来实现的。但使用接口有时候有很多不方便的地方。比如我们实现一个接口就必须实现它里面的所有方法。而有了内部类就不一样了。它可以使我们的类继承多个具体类或抽象类。大家看下面的例子。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">A</span> <span class="keyword">extends</span> <span class="title">C</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">D</span></span>&#123;</span><br><span class="line">        </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h4 id="四、-避免同名方法的冲突。"><a href="#四、-避免同名方法的冲突。" class="headerlink" title="四、 避免同名方法的冲突。"></a>四、 避免同名方法的冲突。</h4><p> 如果你的类要继承一个类，还要实现一个接口，可是你发觉你继承的类和接口里面有两个同名的方法怎么办？你怎么区分它们？？这就需要我们的内部类了。看下面的代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">interface</span> <span class="title">Incrementable</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">increment</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Test</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">increment</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"Other increment()"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Callee2</span> <span class="keyword">extends</span> <span class="title">MyIncrement</span> <span class="keyword">implements</span> <span class="title">Incrementable</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">increment</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>increment()这个方法是属于覆盖MyIncrement这里的方法呢？还是Incrementable这里的方法。我怎么能调到MyIncrement这里的方法？显然这是重复定义。</p>
]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>Java8</tag>
      </tags>
  </entry>
  <entry>
    <title>静态内部类</title>
    <url>/2017/08/19/Java%20SE/%E9%9D%99%E6%80%81%E5%86%85%E9%83%A8%E7%B1%BB/</url>
    <content><![CDATA[<h3 id="定义：静态内部类，定义在类中，任何方法外，用static定义；静态内部类只能访问外部类的静态成员。"><a href="#定义：静态内部类，定义在类中，任何方法外，用static定义；静态内部类只能访问外部类的静态成员。" class="headerlink" title="定义：静态内部类，定义在类中，任何方法外，用static定义；静态内部类只能访问外部类的静态成员。"></a>定义：静态内部类，定义在类中，任何方法外，用static定义；静态内部类只能访问外部类的静态成员。</h3><a id="more"></a>

<h3 id="注意点："><a href="#注意点：" class="headerlink" title="注意点："></a>注意点：</h3><ol>
<li>一般情况下，如果一个内部类不是被定义成静态内部类，那么在定义成员变量或者成员方法的时候，是不能够被定义成静态成员变量与静态成员方法的。<strong>也就是说，在非静态内部类中不可以声明静态成员。</strong></li>
<li>一般非静态外部类可以随意访问其外部类的成员变量以及方法（包括声明为private的方法），但是如果一个内部类被声明为static，则其在访问包括自身的外部类会有诸多的限制。静态内部类不能访问其外部类的非静态成员变量和方法。</li>
<li>在一个类中创建非静态成员内部类的时候，有一个强制性的规定，即内部类的实例一定要绑定在外部类的实例中，也就是在编译时期，编译器会给外部类江上一个字段就叫做 OuterClassName.this 而如果是静态内部类，外部类就不存在这个属性。也就是要引用内部类首先得 new 出外部类。如果要在一个类中定义一个静态的内部类，不需要利用关键字new来创建外部类的实例。即在创建静态类内部对象时，不需要其外部类的对象存在。</li>
</ol>
]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>Java8</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaIO</title>
    <url>/2017/08/09/Java%20SE/JavaIO/</url>
    <content><![CDATA[<p>1.在IO有两种数据传输格式一个是字符流还一个是字节流<br>但是字符流就会涉及到编码的问题</p>
<ol>
<li>一开始美国使用的自己的编码表就是ASCII表</li>
<li>中国的字符需要被识别也需要编码表于是就有了GB2312</li>
<li>但是由于中国的子很多，还有少数名族等等后来又进行了优化扩容就出现了GBK</li>
<li>最后国际标准组织制定了一个包含所有国家所有地区的码表就是Unicode</li>
<li>之后对Unicode进行了优化也就是以前是所有的字符都是两个字节表示，但是现在就可以一个字节或者三个字节，具体看情况的UTF_8<br>字符流一般都包含了编码表，也就是在传输的时候你可以指定编码方式然后IO流帮你转换<a id="more"></a></li>
<li>在IO流里面就是有四个抽象的顶层接口<ul>
<li>字节流的：<br>  InputStream  OutputStream</li>
<li>字符流：<br>  Reader  Writer  </li>
</ul>
</li>
</ol>
<p>3.装饰设计模式：<br>    也就是基于原理已经有的对象，在此对象的基础上提供更强大的功能，也就是能达到不修源码的情况下扩展原来的类的功能<br>    用于增强的的类称为装饰类，这个类一般都是用构造函数来接受需要被装饰的类的对象就例如BufferedRead()内部就调用了<br>    FileReader的read方法<br>    一说到功能扩展自然就会想到继承，这里就要说一下有时候为什么还需要装饰模式。<br>    假如说有以下类层次结构：<br>    —ReadText<br>    —ReadMedia<br>    —ReadData<br>    那么我们要用缓冲区读的话我们就会有以下的类层次结构：<br>        —ReadText<br>            |–BufferedReadText<br>        —ReadMedia<br>            |–BufferedReadMedia<br>        —ReadData<br>            |–BufferedReadData<br>    类层级就非常臃肿<br>    所以我们想到就用装饰者模式：<br>    假如有以下装饰类：<br> <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BufferedReader</span></span>&#123;</span><br><span class="line">    BufferedReader(ReadText readText)&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    BufferedReader(ReadMedia readMedia)&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">    BufferedReader(ReadData readData)&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>  这样虽然可以，但是我们要拓展Read的子类这个装饰类必须要修改，拓展性很差<br>    所以我们直接就用多态性质<br>   <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BufferedReader</span> <span class="keyword">extends</span> <span class="title">Read</span></span>&#123;</span><br><span class="line">  <span class="keyword">private</span> Read read;</span><br><span class="line">    BufferedReader(Read read)&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">read</span><span class="params">(<span class="keyword">char</span> cbuf[], <span class="keyword">int</span> off, <span class="keyword">int</span> len)</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> read.read( cbuf[],  off,  len);<span class="comment">//这里我们必须要复写Read里面的抽象方法，但是我们不知道这个read怎么实现，所以我们直接调用传进来的子类的对象的read方法</span></span><br><span class="line"></span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">close</span><span class="params">()</span></span>&#123;</span><br><span class="line">      <span class="keyword">return</span> read.close();  <span class="comment">//同上</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>  这样一来类层级结构就是：<br>  —Read<br>    |—ReadText<br>    |—ReadMedia<br>    |—ReadData<br>    |—BufferedRead</p>
<p>  注意装饰类是Read的子类，并且他和被装饰类是同一个层次，因为他是更强的被装饰类，所以他们是同一个层级</p>
<p>  4.字节流的处理<br>    InputStream<br>        |–FileInputStream<br>    OutputStream<br>        |–FileOutputStream</p>
<p>   这两个类的处理方式和上面的字符流完全一样，唯一一点的不同就是，它里面读取的时候还有一个avaliable方法可以确定文件中字节大小，还有就是他是以字节为单位的<br>​<br>  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">FileInputStreamDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException</span>&#123;</span><br><span class="line">        FileInputStream fileInputStream=<span class="keyword">new</span> FileInputStream(<span class="string">"a.txt"</span>);</span><br><span class="line">        read_3(fileInputStream);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">read_1</span><span class="params">(FileInputStream fileInputStream)</span><span class="keyword">throws</span> IOException</span>&#123;</span><br><span class="line">        <span class="keyword">byte</span> by;</span><br><span class="line">        <span class="keyword">while</span> ((by= (<span class="keyword">byte</span>) fileInputStream.read())!=-<span class="number">1</span>)&#123;</span><br><span class="line">            System.out.print((<span class="keyword">char</span>) by);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">read_2</span><span class="params">(FileInputStream fileInputStream)</span><span class="keyword">throws</span> IOException</span>&#123;</span><br><span class="line">        <span class="keyword">byte</span>[] buffer=<span class="keyword">new</span> <span class="keyword">byte</span>[<span class="number">1024</span>];</span><br><span class="line">        <span class="keyword">char</span>[] buf=<span class="keyword">new</span> <span class="keyword">char</span>[<span class="number">1024</span>];</span><br><span class="line">        <span class="keyword">int</span> num=<span class="number">0</span>;</span><br><span class="line">        StringBuilder builder=<span class="keyword">new</span> StringBuilder();</span><br><span class="line">        <span class="keyword">while</span> ((num=fileInputStream.read(buffer))!=-<span class="number">1</span>)&#123;</span><br><span class="line">            <span class="comment">// <span class="doctag">TODO:</span> 2017/7/16 转数组为字符串的问题   尤其是基本类型转字符串的问题   是不是还有什么好的对象去做这件事</span></span><br><span class="line">            <span class="keyword">for</span> (<span class="keyword">byte</span> b:buffer)&#123;</span><br><span class="line">                builder.append((<span class="keyword">char</span>) b);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(builder);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">read_3</span><span class="params">(FileInputStream fileInputStream)</span> <span class="keyword">throws</span> IOException</span>&#123;</span><br><span class="line">        <span class="keyword">byte</span>[] by=<span class="keyword">new</span> <span class="keyword">byte</span>[fileInputStream.available()];   <span class="comment">//这个函数就可以确定字节个数   不过只能用于较小的文件  大文件开辟太大的缓冲区会出问题</span></span><br><span class="line">        fileInputStream.read(by);</span><br><span class="line">        StringBuilder stringBuilder=<span class="keyword">new</span> StringBuilder();</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">byte</span> bys:by)&#123;</span><br><span class="line">            <span class="comment">// <span class="doctag">TODO:</span> 2017/7/16 转数组为字符串的问题   尤其是基本类型转字符串的问题   是不是还有什么好的对象去做这件事</span></span><br><span class="line">            stringBuilder.append((<span class="keyword">char</span>)bys);</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(stringBuilder);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>5.当然也是有BufferedInputStream和BufferedOutputStream这两个装饰类   注意这个和前面的一模一样</p>
<p>6.流对象的使用，流对象的使用它们的类别太多，这里为了区分使用哪一个流对象我们就具体分析一下：<br>    1.明确源和目的：<br>        源的话我们就是用InputStream和Reader<br>        目的则是OutputStream和Writer<br>    2.之后就是确定是否为纯文本：<br>        是：字符流<br>        否：字节流<br>    3.确定具体的对象：<br>        通过设备来区分：硬盘，控制台，内存等等   分别使用合适的子类<br>        然后根据是否需要更强的功能，更快的速度使用Buffered包装类</p>
]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>IO</tag>
      </tags>
  </entry>
  <entry>
    <title>Java多线程</title>
    <url>/2017/08/09/Java%20SE/Java%E5%A4%9A%E7%BA%BF%E7%A8%8B/</url>
    <content><![CDATA[<p>1.复写run方法的目的在于，把要运行的代码放到run方法里面，也就是新的线程要跑什么内容<br>这也就是第一种多线程的方法，其主要的步骤如下：</p>
<ol>
<li>继承Thread类</li>
<li>复写run方法</li>
<li>创建对象</li>
<li>start  <a id="more"></a></li>
<li>任何一个程序至少有一个线程就是主线程，主线程也是main方法的线程，这个线程是由jvm启动的，当我们自己创建新的线程的时候实际上是在主线程之外另开的新的线程和主线程并行工作<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DemoRun</span> <span class="keyword">extends</span> <span class="title">Thread</span></span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">60</span>; i++) &#123;</span><br><span class="line">            System.out.println(<span class="string">"thread---"</span>+i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ThreadDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        DemoRun demoRun=<span class="keyword">new</span> DemoRun(); <span class="comment">//创建线程</span></span><br><span class="line">        demoRun.start();<span class="comment">//start方法有两个作用，一个是启动线程，第二个就是调用run方法</span></span><br><span class="line">        <span class="comment">//如果仅仅是使用run方法的话，也就仅仅调用了run方法里面的代码，而并没有开启多线程，run方法的线程还是在主线程</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">60</span>; i++) &#123;</span><br><span class="line">            System.out.println(<span class="string">"main---"</span>+i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>3.第一种创建线程的方式其实会有很大的局限性，例如说，我们说java是单继承的语言，那么也就会出现一个class继承了父类，无法在继承Thread类<br>  而java却是多实现的，我们就可以继承runnable接口完成。但是注意，runnable接口并不是一个Thread类的对象，说白了他不是一个线程，那么我们<br>  就不知道我们多线程到底要运行哪的代码，不明确run方法。所以我们就先建立Thread的对象，然后把runnable接口的对象传递给Thread类，这样一来Thread类就明确了<br>  run方法的位置，也就是多线程要运行的代码的位置。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">RunnableTest</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">60</span>; i++) &#123;</span><br><span class="line">            System.out.println(Thread.currentThread().getName()+<span class="string">"-----"</span>+i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">RunnableDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        RunnableTest runnableTest=<span class="keyword">new</span> RunnableTest();</span><br><span class="line">        Thread run=<span class="keyword">new</span> Thread(runnableTest);</span><br><span class="line">        Thread run1=<span class="keyword">new</span> Thread(runnableTest);</span><br><span class="line">        run.start();</span><br><span class="line">        <span class="comment">//注意如果实现的runnable接口的话一个线程可以启动多次，相当于多个线程，里面的属性可以当做共享变量来使用，因为是同一个对象</span></span><br><span class="line">        run1.start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>4.在说完多线程的创建以后最重要的就是线程的安全问题，主要就是共享变量的问题。例如上面的说到的实现runnable接口的时候，我们的属性就可以是共享变量<br>  一般来说就是使用进程同步代码块来搞定，安全问题。当如具体我们要同步哪一个代码块就看我们那个块使用了共享</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TestDemo</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> num;</span><br><span class="line">    Object obj=<span class="keyword">new</span> Object();</span><br><span class="line">    TestDemo()&#123;</span><br><span class="line">        num=<span class="number">100</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>)&#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (obj) &#123;    <span class="comment">//这个地方可以随便放一个对象，但是注意如果是同一个对象就是同一个锁否则就基本没有同步功能</span></span><br><span class="line">                <span class="keyword">if</span> (num &gt; <span class="number">0</span>) &#123;</span><br><span class="line">                    <span class="keyword">try</span> &#123;</span><br><span class="line">                        Thread.sleep(<span class="number">10</span>);</span><br><span class="line">                    &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                        e.printStackTrace();</span><br><span class="line">                    &#125;</span><br><span class="line">                    System.out.println(Thread.currentThread().getName() + <span class="string">"====="</span> + num);</span><br><span class="line">                    num--;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SyncDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        TestDemo testDemo=<span class="keyword">new</span> TestDemo();</span><br><span class="line">        <span class="keyword">new</span> Thread(testDemo).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(testDemo).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(testDemo).start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>5.同步函数：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Bank</span></span>&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">int</span> sum;</span><br><span class="line">    Bank()&#123;</span><br><span class="line">        sum=<span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">add</span><span class="params">(<span class="keyword">int</span> n)</span></span>&#123;</span><br><span class="line">        sum+=n;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">TestDemo_01</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Bank bank=<span class="keyword">new</span> Bank();</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="comment">//1.首先要明确哪些代码是多线程代码，这里显然就是run里面的，然后run里面调用了add函数，所以add方法也是多线程</span></span><br><span class="line">    <span class="comment">//2.之后就是哪些是共享变量，这里就是sum，bank</span></span><br><span class="line">    <span class="comment">//3.这样我们同步的代码就知道是哪些了既可以笼统的把run里面的操作bank的同步起来</span></span><br><span class="line">    <span class="comment">//这样的话我们就同步了一个函数，函数里面的那些局部变量就被同步了，这样和单线程没却别</span></span><br><span class="line">    <span class="comment">//所以我们同步add方法里面的内容</span></span><br><span class="line">    <span class="comment">//或者说我们可以直接同步add方法  就在方法的前面添加synchronized即可</span></span><br><span class="line">    <span class="comment">//也就是说我们的代码同步有两种方法一个就是同步代码块，一个就是同步函数</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">3</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (bank) &#123;</span><br><span class="line">                bank.add(<span class="number">100</span>);</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    Thread.sleep(<span class="number">10</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125;</span><br><span class="line">                System.out.println(bank.sum);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SyncFunction</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        TestDemo_01 testDemo_01=<span class="keyword">new</span> TestDemo_01();</span><br><span class="line">        <span class="keyword">new</span>  Thread(testDemo_01).start();</span><br><span class="line">        <span class="keyword">new</span>  Thread(testDemo_01).start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>6.如果在我们进行了代码同步以后，代码还挂了那就说明我们的代码没有满足两个前提：</p>
<ul>
<li><p>第一个就是必须是两个及两个以上的线程</p>
</li>
<li><p>第二个就是使用的必须是同一个锁</p>
<p>这里顺带说同步函数使用的锁其实就是this<br>然后如果是静态同步函数的话，我们知道静态函数不可能使用this，因为他不属于对象而是属于类<br>这里也就是类的字解码文件  类名.class   就是这个</p>
</li>
</ul>
<p>7.线程通讯同步，所谓线程同步通讯就是，在多个线程同时对同一个资源进行操作的时候我们使用同步代码让他们同步，但是这样可能会造成两个线程各自执行到底<br>    而非交替执行，为了交替执行我们使用同步，一个执行完了以后就睡眠唤醒另外一个，但是还是要注意这两个线程称一定要使用同一把锁，两个线程的代码都要同步<br>    具体方法就是：<br>    wait(),notify(),notifyAll()这几个函数都是使用在同步中的，因为他们需要对有监视器（锁）的对象进行操作<br>    所以只有在同步中他们才有锁、<br>    这些方法在操作线程的时候必须要标识他们所操作的锁，也就是说被某个锁同步的代码只能由此锁的wait，notify操作不可以对不同的锁的对象进行等待唤醒<br>    由于锁是任意对象所以说这些方法都被定义在Object中</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Resource</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> count;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">boolean</span> flag;</span><br><span class="line">    Resource()&#123;</span><br><span class="line">        count=<span class="number">0</span>;</span><br><span class="line">        flag=<span class="keyword">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">in</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (flag) &#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                wait();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"in==="</span> + count++);</span><br><span class="line">        flag=<span class="keyword">true</span>;</span><br><span class="line">        notify();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">synchronized</span> <span class="keyword">void</span> <span class="title">out</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!flag)&#123;</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                wait();</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        System.out.println(<span class="string">"out------------"</span>+count);</span><br><span class="line">        flag=<span class="keyword">false</span>;</span><br><span class="line">        notify();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Producer</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Resource resource;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Producer</span><span class="params">(Resource resource)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.resource = resource;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">while</span> (<span class="keyword">true</span>)&#123;</span><br><span class="line">            resource.in();</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Consumer</span> <span class="keyword">implements</span> <span class="title">Runnable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> Resource resource;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Consumer</span><span class="params">(Resource resource)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.resource = resource;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        resource.out();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ProducerAndConsumer</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Resource resource=<span class="keyword">new</span> Resource();</span><br><span class="line">        Producer producer=<span class="keyword">new</span> Producer(resource);</span><br><span class="line">        Consumer consumer=<span class="keyword">new</span> Consumer(resource);</span><br><span class="line">        <span class="keyword">new</span> Thread(producer).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(producer).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(consumer).start();</span><br><span class="line">        <span class="keyword">new</span> Thread(consumer).start();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>多线程</tag>
      </tags>
  </entry>
  <entry>
    <title>Java泛型</title>
    <url>/2017/08/09/Java%20SE/Java%E8%8C%83%E5%9E%8B/</url>
    <content><![CDATA[<p>1.java泛型及就是在jdk1.5之后出现的一个新的安全机制<br>  我们发现在集合框架中我们可以放入任何的元素，然而这样做并没有任何意义，绝大多时候我们是默认我们<br>  知道这个容器需要存放什么样的内容，但是用户的输入是不安全的如果他们输入了各种类型然后我们只对某些类型<br>  进行了处理显然到时候运行时必然报错<br>  所以为了解决这个问题，类似于数组的解决方式给集合限定了类型使用尖括号来限定，当然包括Iterator<br>  他的好处就是安全</p>
<a id="more"></a>
<p>2.comparable接口和comparator都可以使用泛型，在使用泛型以后里面的内容就无需在强转直接写泛型的类型即可</p>
<p>3.泛型不仅仅是安全这一说，还有就是以前我们在不确定一个函数的具体接受对象的类型的时候我们都是使用object对象<br>  来接受，然后向下转型。但是如果有了泛型我们就可以直接定义泛型类或者泛型方法。<br>  1）.泛型类就是把泛型定义在类上面这样的话类中只要引用了泛型的地方他们的类型都一致例如集合框架，写在类名后类似于接受参数<br>      泛型的确定就是在类实例化的时候<br>  2）.当然有时候类中的函数想操作的独享并不是类上定义的那个类型我们可以把泛型定义在函数上，写在返回值的前面<br>      类似于参数一样，但是静态方法是无法使用定义在类上面的泛型因为类的泛型在确定类型的时候都是在类的实例化<br>      而函数的泛型则是自动识别无需程序员做任何操作<br>  3）.接口也可以定义泛型，然后类在实例化的时候可以明确泛型也可以不指定泛型然后还是一个泛型类<br>  三个知识点的代码分别如下：<br>  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Author: lwen</span></span><br><span class="line"><span class="comment"> * Date: 2017/07/12</span></span><br><span class="line"><span class="comment"> * Description:定义在类上面的泛型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Worker</span></span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span></span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Tool</span>&lt;<span class="title">T</span>&gt;</span>&#123;</span><br><span class="line">    <span class="keyword">private</span> T t;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> T <span class="title">getT</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> t;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setT</span><span class="params">(T t)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.t=t;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GenericDemo01</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Tool&lt;Worker&gt; tool=<span class="keyword">new</span> Tool&lt;&gt;();</span><br><span class="line">        tool.getT();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Author: lwen</span></span><br><span class="line"><span class="comment"> * Date: 2017/07/12</span></span><br><span class="line"><span class="comment"> * Description:定义在函数上的泛型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Out</span>&lt;<span class="title">T</span>&gt;</span>&#123;</span><br><span class="line">    &lt;M&gt; <span class="function"><span class="keyword">void</span> <span class="title">show</span><span class="params">(M m)</span></span>&#123;</span><br><span class="line">        System.out.println(m);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">print</span><span class="params">(T t)</span></span>&#123;</span><br><span class="line">        System.out.println(t);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">static</span> &lt;O&gt; <span class="function"><span class="keyword">void</span> <span class="title">prints</span><span class="params">(O o)</span></span>&#123;</span><br><span class="line">        System.out.println(o);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GenericDemo02</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Out&lt;String&gt; out=<span class="keyword">new</span> Out&lt;&gt;();</span><br><span class="line">        out.print(<span class="string">"string"</span>);  <span class="comment">//只能传入string因为这个是类上面的泛型</span></span><br><span class="line">        out.show(<span class="number">3</span>);  <span class="comment">//任意类型的因为这个事函数上面的泛型</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Author: lwen</span></span><br><span class="line"><span class="comment"> * Date: 2017/07/12</span></span><br><span class="line"><span class="comment"> * Description:  定义在接口上的泛型</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Inter</span> &lt;<span class="title">T</span>&gt;</span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">print</span><span class="params">(T t)</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">InterImp</span> <span class="keyword">implements</span> <span class="title">Inter</span>&lt;<span class="title">String</span>&gt;</span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">(String s)</span> </span>&#123;</span><br><span class="line">        System.out.println(s);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">InterImp1</span>&lt;<span class="title">T</span>&gt; <span class="keyword">implements</span> <span class="title">Inter</span>&lt;<span class="title">T</span>&gt;</span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">(T t)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GenericDemo03</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>4.泛型的高级应用就是泛型限定：<br>  泛型限定：<br>  1）.当我们要传入的某个参数不确定的时候我们可以使用定义在函数上的泛型，当然还有一种更方便的方法就是<br>      使用？占位符<br>  2）.我们会发现？占位符能接受的类型的范围太大，无论是什么都可以接受，但是我们希望我们只能接受<br>      某一类对象的子对象或者某对象的父类对象<br>      有向下类型限定和向上类型限定：<br>      向上：  ? extends className<br>      向下:   ? super className</p>
  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Author: lwen</span></span><br><span class="line"><span class="comment"> * Date: 2017/07/12</span></span><br><span class="line"><span class="comment"> * Description: 向下限定也就是在comparator接口中他的泛型限定其实就是 ？ super T  这个T就是TreeSet的T</span></span><br><span class="line"><span class="comment"> *              那么也就是说comparator中可以放T的父类</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *              显然如果说我们要给TreeSet传入比较器的话我们肯定需要给每一个特定的类型的TreeSet给一个比较器</span></span><br><span class="line"><span class="comment"> *              但是由于在定义比较器接口的时候人家给的就是某个类型的父类这样的话我们构造一个父类的比较器就能搞定</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line"></span><br><span class="line">    Person(<span class="keyword">int</span> age)&#123;</span><br><span class="line">        <span class="keyword">this</span>.age=age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getAge</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAge</span><span class="params">(<span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Students</span> <span class="keyword">extends</span> <span class="title">Person</span></span>&#123;</span><br><span class="line">    Students(<span class="keyword">int</span> age)&#123;</span><br><span class="line">        <span class="keyword">super</span>(age);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Workers</span> <span class="keyword">extends</span> <span class="title">Person</span></span>&#123;</span><br><span class="line">    Workers(<span class="keyword">int</span> age)&#123;</span><br><span class="line">        <span class="keyword">super</span>(age);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GenericDemo04</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        TreeSet&lt;Workers&gt; ts1=<span class="keyword">new</span> TreeSet&lt;&gt;(<span class="keyword">new</span> Comp());</span><br><span class="line">        ts1.add(<span class="keyword">new</span> Workers(<span class="number">12</span>));</span><br><span class="line">        ts1.add(<span class="keyword">new</span> Workers(<span class="number">13</span>));</span><br><span class="line">        ts1.add(<span class="keyword">new</span> Workers(<span class="number">10</span>));</span><br><span class="line">        <span class="keyword">for</span> (Workers ws :</span><br><span class="line">                ts1) &#123;</span><br><span class="line">            System.out.println(<span class="string">"ws--------"</span>+ws.getAge());</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        TreeSet&lt;Students&gt; ts2=<span class="keyword">new</span> TreeSet&lt;&gt;(<span class="keyword">new</span> Comp());</span><br><span class="line">        ts2.add(<span class="keyword">new</span> Students(<span class="number">12</span>));</span><br><span class="line">        ts2.add(<span class="keyword">new</span> Students(<span class="number">9</span>));</span><br><span class="line">        ts2.add(<span class="keyword">new</span> Students(<span class="number">17</span>));</span><br><span class="line">        <span class="keyword">for</span> (Students stu :</span><br><span class="line">                ts2) &#123;</span><br><span class="line">            System.out.println(<span class="string">"student---"</span>+stu.getAge());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Comp</span> <span class="keyword">implements</span> <span class="title">Comparator</span>&lt;<span class="title">Person</span>&gt;</span>&#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(Person o1, Person o2)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> Integer(o1.getAge()).compareTo(o2.getAge());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Author: lwen</span></span><br><span class="line"><span class="comment"> * Date: 2017/07/12</span></span><br><span class="line"><span class="comment"> * Description:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">A</span></span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">B</span> <span class="keyword">extends</span> <span class="title">A</span></span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">C</span> <span class="keyword">extends</span> <span class="title">B</span></span>&#123;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">GenericDemo05</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        A a=<span class="keyword">new</span> A();</span><br><span class="line">        B b=<span class="keyword">new</span> B();</span><br><span class="line">        C c=<span class="keyword">new</span> C();</span><br><span class="line">        ArrayList&lt;A&gt; ala=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        ala.add(a);</span><br><span class="line">        ArrayList&lt;B&gt; alb=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        alb.add(b);</span><br><span class="line">        ArrayList&lt;C&gt; alc=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        alc.add(c);</span><br><span class="line"></span><br><span class="line">        print(ala);</span><br><span class="line"><span class="comment">//        printSub(ala); error</span></span><br><span class="line">        printSup(ala);</span><br><span class="line">        printSup(alb);</span><br><span class="line"><span class="comment">//        printSup(alc);  error</span></span><br><span class="line">        printSub(alc);</span><br><span class="line">        printSub(alb);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">print</span><span class="params">(ArrayList&lt;?&gt; al)</span></span>&#123;</span><br><span class="line">        Iterator&lt;?&gt; it=al.iterator();</span><br><span class="line">        <span class="keyword">while</span> (it.hasNext())&#123;</span><br><span class="line">            System.out.println(it.next());;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">printSub</span><span class="params">(ArrayList&lt;? extends B&gt; al)</span></span>&#123;</span><br><span class="line">        Iterator&lt;?&gt; it=al.iterator();</span><br><span class="line">        <span class="keyword">while</span> (it.hasNext())&#123;</span><br><span class="line">            System.out.println(it.next());;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">printSup</span><span class="params">(ArrayList&lt;? <span class="keyword">super</span> B&gt; al)</span></span>&#123;</span><br><span class="line">        Iterator&lt;?&gt; it=al.iterator();</span><br><span class="line">        <span class="keyword">while</span> (it.hasNext())&#123;</span><br><span class="line">            System.out.println(it.next());;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>范型</tag>
      </tags>
  </entry>
  <entry>
    <title>JDBC</title>
    <url>/2017/08/09/Java%20Web/JDBC/</url>
    <content><![CDATA[<h3 id="1-普通的-JDBC-链接"><a href="#1-普通的-JDBC-链接" class="headerlink" title="1.普通的 JDBC 链接"></a>1.普通的 JDBC 链接</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JdbcConnect</span> </span>&#123;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">   <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">connect</span><span class="params">()</span> <span class="keyword">throws</span> ClassNotFoundException, SQLException </span>&#123;</span><br><span class="line">        <span class="comment">/**</span></span><br><span class="line"><span class="comment">         * 准备四大参数：  driver  url  username  password</span></span><br><span class="line"><span class="comment">         * 加载驱动类</span></span><br><span class="line"><span class="comment">         * 得到连接</span></span><br><span class="line"><span class="comment">         * 创建statement</span></span><br><span class="line"><span class="comment">         * 调用statement的查询或者更新方法</span></span><br><span class="line"><span class="comment">         * 关闭资源（必须关闭）倒关</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">       Class.forName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        java.sql.Connection con= DriverManager.getConnection(<span class="string">"jdbc:mysql://localhost:3306/Java"</span>,<span class="string">"root"</span>,<span class="string">""</span>);</span><br><span class="line">        Statement statement=con.createStatement();</span><br><span class="line">        <span class="keyword">int</span> effectRow=statement.executeUpdate(<span class="string">"INSERT INTO `user` VALUES (1,'lwen',20,'lwen')"</span>);</span><br><span class="line">        System.out.println(effectRow);</span><br><span class="line">        ResultSet resultSet=statement.executeQuery(<span class="string">"SELECT * FROM `user`"</span>);</span><br><span class="line">        <span class="keyword">while</span> (resultSet.next())&#123;</span><br><span class="line">            System.out.println(resultSet.getInt(<span class="number">1</span>)); <span class="comment">//或者使用列名称来获取</span></span><br><span class="line">            System.out.println(resultSet.getString(<span class="number">2</span>));</span><br><span class="line">            System.out.println(resultSet.getInt(<span class="number">3</span>));</span><br><span class="line">            System.out.println(resultSet.getString(<span class="number">4</span>));</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">//倒关</span></span><br><span class="line">        resultSet.close();</span><br><span class="line">        statement.close();</span><br><span class="line">        con.close();</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<a id="more"></a>
<h3 id="2-数据库连接池："><a href="#2-数据库连接池：" class="headerlink" title="2. 数据库连接池："></a>2. 数据库连接池：</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 数据库连接池</span></span><br><span class="line"><span class="comment"> * 就是为了连接课重用，由于创建销毁比较麻烦，所以放在一个map里面也就是一个池，下次要用的时候直接从里面取</span></span><br><span class="line"><span class="comment"> * 而不用平凡的销毁和创建</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * 常用的就是dbcp连接池  这个也说commons里面的东西</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JdbcConnections</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 1.池参数</span></span><br><span class="line"><span class="comment">     * 2.四大连接参数</span></span><br><span class="line"><span class="comment">     * 3.实现javax.sql.DataSource接口</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">dbcp</span><span class="params">()</span> <span class="keyword">throws</span> SQLException </span>&#123;</span><br><span class="line">        BasicDataSource dataSource=<span class="keyword">new</span> BasicDataSource();</span><br><span class="line"></span><br><span class="line">        dataSource.setDriverClassName(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        dataSource.setUrl(<span class="string">"jdbc:mysql://localhost/Java"</span>);</span><br><span class="line">        dataSource.setUsername(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">""</span>);</span><br><span class="line"></span><br><span class="line">        dataSource.setMaxTotal(<span class="number">20</span>);<span class="comment">//设置最大的连接数</span></span><br><span class="line">        dataSource.setMaxWaitMillis(<span class="number">1000</span>);<span class="comment">//设置最大等待时间</span></span><br><span class="line"></span><br><span class="line">        Connection connection=dataSource.getConnection();<span class="comment">//此时获得的connection不是以前的那个了而是在以前的mysql的连接上的增强</span></span><br><span class="line">        <span class="comment">//也就是说他是装饰者模式，装饰的部分就是close方法，因为此时的close方法不是关闭连接而是将连接归还的操作</span></span><br><span class="line"><span class="comment">//        下面的操作就和一般的sql连接一模一样了</span></span><br><span class="line"></span><br><span class="line"><span class="comment">//        然后注意一般来说上面的哪些创建连接池的操作我们只进行一次。然后整个项目就是用这一个连接池</span></span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 现在dbcp用的更少了  主要用的就是c3p0</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">c3p0</span><span class="params">()</span> <span class="keyword">throws</span> PropertyVetoException, SQLException </span>&#123;</span><br><span class="line">        ComboPooledDataSource dataSource=<span class="keyword">new</span> ComboPooledDataSource();</span><br><span class="line"></span><br><span class="line">        dataSource.setDriverClass(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        dataSource.setJdbcUrl(<span class="string">"jdbc:mysql://localhost/Java"</span>);</span><br><span class="line">        dataSource.setUser(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">""</span>);</span><br><span class="line"></span><br><span class="line">        dataSource.setMaxPoolSize(<span class="number">20</span>);</span><br><span class="line">        dataSource.setMinPoolSize(<span class="number">2</span>);</span><br><span class="line">        dataSource.setInitialPoolSize(<span class="number">20</span>);</span><br><span class="line">        dataSource.setAcquireIncrement(<span class="number">5</span>);</span><br><span class="line"></span><br><span class="line">        Connection connection=dataSource.getConnection();</span><br><span class="line">        System.out.println(connection);  <span class="comment">//返回的是代理连接  而不是装饰者模式  他比装饰者模式更加的灵活</span></span><br><span class="line">        connection.close();</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="3-c-组件中的数据库操作类"><a href="#3-c-组件中的数据库操作类" class="headerlink" title="3. c 组件中的数据库操作类"></a>3. c 组件中的数据库操作类</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">JdbcDbUtils</span> </span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 测试beanHandler  对于单行的javaBean</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@throws</span> SQLException</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun1</span><span class="params">()</span> <span class="keyword">throws</span> SQLException, PropertyVetoException </span>&#123;</span><br><span class="line">        ComboPooledDataSource dataSource=<span class="keyword">new</span> ComboPooledDataSource();</span><br><span class="line">        dataSource.setMaxPoolSize(<span class="number">20</span>);</span><br><span class="line">        dataSource.setAcquireIncrement(<span class="number">4</span>);</span><br><span class="line">        dataSource.setMinPoolSize(<span class="number">5</span>);</span><br><span class="line">        dataSource.setInitialPoolSize(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">        dataSource.setDriverClass(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        dataSource.setJdbcUrl(<span class="string">"jdbc:mysql://localhost/java"</span>);</span><br><span class="line">        dataSource.setUser(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">""</span>);</span><br><span class="line"></span><br><span class="line">        QueryRunner qr=<span class="keyword">new</span> QueryRunner(dataSource);</span><br><span class="line">        String sql=<span class="string">"select * from user where id = ?"</span>;</span><br><span class="line">        Object[] params=&#123;<span class="number">1</span>&#125;;</span><br><span class="line">        <span class="keyword">final</span> Object query = qr.query(sql, <span class="keyword">new</span> BeanHandler&lt;UserBean&gt;(UserBean.class), params);</span><br><span class="line">        System.out.println(query);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun2</span><span class="params">()</span> <span class="keyword">throws</span> SQLException, PropertyVetoException </span>&#123;</span><br><span class="line">        ComboPooledDataSource dataSource=<span class="keyword">new</span> ComboPooledDataSource();</span><br><span class="line">        dataSource.setMaxPoolSize(<span class="number">20</span>);</span><br><span class="line">        dataSource.setAcquireIncrement(<span class="number">4</span>);</span><br><span class="line">        dataSource.setMinPoolSize(<span class="number">5</span>);</span><br><span class="line">        dataSource.setInitialPoolSize(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">        dataSource.setDriverClass(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        dataSource.setJdbcUrl(<span class="string">"jdbc:mysql://localhost/java"</span>);</span><br><span class="line">        dataSource.setUser(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">""</span>);</span><br><span class="line"></span><br><span class="line">        QueryRunner qr=<span class="keyword">new</span> QueryRunner(dataSource);</span><br><span class="line">        String sql=<span class="string">"select * from user"</span>;</span><br><span class="line">        <span class="keyword">final</span> Object query = qr.query(sql, <span class="keyword">new</span> BeanListHandler&lt;UserBean&gt;(UserBean.class));</span><br><span class="line">        System.out.println(query);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun3</span><span class="params">()</span> <span class="keyword">throws</span> SQLException, PropertyVetoException </span>&#123;</span><br><span class="line">        ComboPooledDataSource dataSource=<span class="keyword">new</span> ComboPooledDataSource();</span><br><span class="line">        dataSource.setMaxPoolSize(<span class="number">20</span>);</span><br><span class="line">        dataSource.setAcquireIncrement(<span class="number">4</span>);</span><br><span class="line">        dataSource.setMinPoolSize(<span class="number">5</span>);</span><br><span class="line">        dataSource.setInitialPoolSize(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">        dataSource.setDriverClass(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        dataSource.setJdbcUrl(<span class="string">"jdbc:mysql://localhost/java"</span>);</span><br><span class="line">        dataSource.setUser(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">""</span>);</span><br><span class="line"></span><br><span class="line">        QueryRunner qr=<span class="keyword">new</span> QueryRunner(dataSource);</span><br><span class="line">        String sql=<span class="string">"select * from user where id = ?"</span>;</span><br><span class="line">        Object[] params=&#123;<span class="number">1</span>&#125;;</span><br><span class="line">        <span class="keyword">final</span> Object query = qr.query(sql, <span class="keyword">new</span> MapHandler(), params);</span><br><span class="line">        System.out.println(query);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Test</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">fun4</span><span class="params">()</span> <span class="keyword">throws</span> SQLException, PropertyVetoException </span>&#123;</span><br><span class="line">        ComboPooledDataSource dataSource=<span class="keyword">new</span> ComboPooledDataSource();</span><br><span class="line">        dataSource.setMaxPoolSize(<span class="number">20</span>);</span><br><span class="line">        dataSource.setAcquireIncrement(<span class="number">4</span>);</span><br><span class="line">        dataSource.setMinPoolSize(<span class="number">5</span>);</span><br><span class="line">        dataSource.setInitialPoolSize(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">        dataSource.setDriverClass(<span class="string">"com.mysql.jdbc.Driver"</span>);</span><br><span class="line">        dataSource.setJdbcUrl(<span class="string">"jdbc:mysql://localhost/java"</span>);</span><br><span class="line">        dataSource.setUser(<span class="string">"root"</span>);</span><br><span class="line">        dataSource.setPassword(<span class="string">""</span>);</span><br><span class="line"></span><br><span class="line">        QueryRunner qr=<span class="keyword">new</span> QueryRunner(dataSource);</span><br><span class="line">        String sql=<span class="string">"select * from user "</span>;</span><br><span class="line">        Object[] params=&#123;<span class="number">1</span>&#125;;</span><br><span class="line">        <span class="keyword">final</span> Object query = qr.query(sql, <span class="keyword">new</span> MapListHandler());</span><br><span class="line">        System.out.println(query);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-JavaWeb</category>
      </categories>
      <tags>
        <tag>JDBC</tag>
      </tags>
  </entry>
  <entry>
    <title>JavaWeb基础</title>
    <url>/2017/08/09/Java%20Web/JavaWeb%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h3 id="1-XML"><a href="#1-XML" class="headerlink" title="1. XML"></a>1. XML</h3><ol>
<li>xml一般就用来存放少量的数据，或者是作为配置文件。</li>
<li>xml的声明<?xml version="1.0" encoding="utf-8"?>  必须放在首行的首列（也就是顶头写）</li>
<li>有且仅有一个根标签，其他的都是他的子标签</li>
<li>xml中的换行和空格都当做内容来解析，所以对于缩进来说一定要注意。</li>
<li>xml中的内容区分大小写，不能以数字和下划线开始,不能以xml开始，里面不能包含空格和冒号<a id="more"></a></li>
<li>一个元素可以有多个属性，名字自定义，属性不能冲突</li>
<li>对于一些特殊字符需要转义，使用实体来表示，就和html里面的一样</li>
<li>CDATA区就是里面的内容无需转义  <!--[CDATA[ content ]]--></li>
<li>对于xml的内容的约束有两个 dtd和scheme两种<br> –dtd：<pre><code>1.创建一个dtd文件
2.有几个元素就写几个&lt;!ELEMENT&gt;
3.看元素分别是简单元素还是复杂元素，就是嵌套否
    --简单元素&lt;!ELEMENT 元素名 (#PCDATA) &gt;
    --复杂&lt;!ELEMENT 元素名 (子元素，逗号隔开) &gt;
4.在xml中引入dtd
    &lt;!DOCTYPE 根元素名 SYSTEM &quot;dtd文件路径&quot;&gt;</code></pre></li>
<li>xml的解析方法有两个分别就是dom和sax   （dom说白了就是构建dom树，这个方便的就是增删操作，但是文件过大可能造成内存溢出；sax则是从上往下读，一行行的解析，然后返回对象，但是不可以增删改，但是方便查询）</li>
<li>三个常用的xml的解析器  ：dom4j(重要)  jaxp  jdom</li>
</ol>
<h3 id="2-HTTP-基础："><a href="#2-HTTP-基础：" class="headerlink" title="2. HTTP 基础："></a>2. HTTP 基础：</h3><ol>
<li><p>请求头：</p>
<pre>
请求行（GET /hello/index.html HTTP/1.1 ）
请求头 （多个键值对，其中有一个Keep-Alive这个东西是由于http无状态协议，所以说一个网站加载的时候肯定不止一次请求，像图片等等的东西，我们为了一次请求就让他多连接一会）
     （Content-Type:application-x-www-from-urlencoded  ->表示表单的数据会使用url编码）
一个空行
请求体 （只有POST才有请求体，GET没有）
</pre></li>
<li><p>响应头：</p>
<pre>
 响应行 （HTTP/1.1 200 OK）  2开头的都是成功  3开头的重定向  4开头的客户端错误   5开头的服务器错误
 请求头 （Content-Type:text/html ;charset=ISO-8859-1 文本的话必须要有编码，像图片之类的可以不要）
 空行
 响应正文
</pre>    
</li>
<li><p>Reference 显示的从哪个网站过来的一般的作用就是防盗链和统计广告</p>
</li>
<li><p>重定向就是当客户端给服务器发送求以后，服务器返回了一个带有新的地址的返回，然后客户端去请求这个新的地址（302），而转发则是直接接通到新的服务器客户端不须在请求</p>
</li>
<li><p>304就是缓存  首先浏览器发了一个Last-Modify头   然后服务器比对Last-Modify-Since  如果一样那么返回304  没有请求体</p>
</li>
<li><p>Tomcat是支持java web而不支持java ee  因为java ee范围很广泛  主要就是不支持java ee的JEB（企业级的java 应用）  但是我们有ssh可以代替</p>
</li>
</ol>
<h3 id="3-Servlet"><a href="#3-Servlet" class="headerlink" title="3. Servlet"></a>3. Servlet</h3><ol>
<li>Servlet的创建有三种方式，分别就是实现Servlet接口，继承GenericServlet和继承HttpServlet类<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">AHttpServlet</span> <span class="keyword">extends</span> <span class="title">HttpServlet</span></span>&#123;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * Http servlet首先就是继承的是GenericServlet，它里面有更丰富的方法，但是说到底还是要运行service方法，但是这个类里面的service方法其实有两个</span></span><br><span class="line"><span class="comment">     * 第一个是从上面继承下来的，另一个是自己的实际要用的，他们的不同就在于参数，自己的那个参数是与Http协议相关的，也就是说这个东西绑定了Http协议</span></span><br><span class="line"><span class="comment">     * 但是以前的那个参数是与协议无关的，但是最终tomcat要调用的是父类里面的service方法，所以说在继承的service方法中首先把参数都强转成http类型的参数</span></span><br><span class="line"><span class="comment">     * 也就是自己的service方法的参数，然后去调用自己的service方法，自己的service方法里面会判断来自客户端的请求究竟是post还是get然后调用doPost或者doget</span></span><br><span class="line"><span class="comment">     * 方法，所以说最终我们只需要复写doGet或者doPost即可</span></span><br><span class="line"><span class="comment">     * HttpServlet这个类是抽象的，但是里面的方法没有一个是抽象的，doGet和doPost默认都是给客户端返回一个405也就是不支持的请求类型，所以如果不复写</span></span><br><span class="line"><span class="comment">     * 这两个方法就会出问题405</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doGet</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"doGet"</span>);</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">protected</span> <span class="keyword">void</span> <span class="title">doPost</span><span class="params">(HttpServletRequest req, HttpServletResponse resp)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"doPost"</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>仿写的 GenericServelt 的代码</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SrcGenericServlet</span> <span class="keyword">implements</span> <span class="title">Servlet</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> ServletConfig servletConfig;  <span class="comment">//很重要的一个对象，里面有五个方法，前面已经说了两个方法就是关于初始化配置的两个</span></span><br><span class="line">    <span class="comment">//还有就是Context,以及getServletName()获取Servlet的名字</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">(ServletConfig servletConfig)</span> <span class="keyword">throws</span> ServletException </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.servletConfig=servletConfig;</span><br><span class="line">        <span class="comment">//这个就是钩子，如果我们写好的这个类然后有别人去继承，那么如果他想在init方法里面添加一些自己的功能</span></span><br><span class="line">        <span class="comment">//这个init有参数的如果被覆盖的话，子类中的servletConfig就会指向空指针，因为没有初始化。</span></span><br><span class="line">        <span class="comment">//所以为了拓展，子类能够写自己的代码我们写了一个新的函数，子类只需要覆盖这个函数就可以到到目的</span></span><br><span class="line">        <span class="comment">//不过还有一个方式就是在子类中我们先写自己的代码，然后使用super.init()也是能够搞定的</span></span><br><span class="line">        init();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">init</span><span class="params">()</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ServletContext <span class="title">getServletContext</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.servletConfig.getServletContext();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> ServletConfig <span class="title">getServletConfig</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.servletConfig;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">service</span><span class="params">(ServletRequest servletRequest, ServletResponse servletResponse)</span> <span class="keyword">throws</span> ServletException, IOException </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getServletInfo</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="string">"hello"</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">destroy</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<ol start="2">
<li><p>首先说Servlet接口，这个接口里面总共有五个抽象方法需要进行复写，其中有三个声明周期方法init【在第一次访问这个Servlet的时候】 server【其他每一次访问的时候】 destroy【tomcat关闭的时候】<br>这个是单例的，一个类只有一个对象<br>而且这个是线程不安全的，所以效率高</p>
</li>
<li><p>Servlet要被浏览器访问必须要配置一下 web.xml 让 tomcat 能够调用这个 Servlet   具体就有两块分别就是Servlet的配置也就是配置Servlet的名字和类<br>第二个就是配置浏览器访问的路径，和对应的Servlet的名称</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">servlet</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>AServlet<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet-class</span>&gt;</span>Servlet.AServlet<span class="tag">&lt;/<span class="name">servlet-class</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">servlet</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">servlet-mapping</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">servlet-name</span>&gt;</span>AServlet<span class="tag">&lt;/<span class="name">servlet-name</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">url-pattern</span>&gt;</span>/AServlet<span class="tag">&lt;/<span class="name">url-pattern</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">servlet-mapping</span>&gt;</span></span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>或者直接在 Servlet 的类上面写 @WebServlet( name=””, url-pattern=”” ) 而不用写 web.xml 的配置</p>
<ol start="4">
<li><p>由于Servlet是线程不安全的，所以说最好不要创建一些成员，而一般的创建局部变量。或者是无状态成员（一个类里面没有属性，然后这个类对象作为自己类成员），或者只读的成员，只有get没有set</p>
</li>
<li><p>一个Servlet一般的就是第一请求的时候创建，也可以在tomcat启动的时候创建Servlet。就是在Servlet标签里面load-on-startup标签，里面是一个序号，不要重复</p>
</li>
<li><p>web.xml的继承，其实每一个项目都有一个web.xml，但是要注意每一个web.xml都是继承了tomcat的conf下的web.xml这个xml里面主要定义了三个东西。第一个就是默认Servlet<br>这个默认的Servlet是匹配所有的页面，当我们访问一个页面的时候没有Servlet响应就轮到defaultServlet，他处理的方法也很简单就是返回一个404<br>然后就是一个JSPServlet这个Servlet是用来处理所有的jsp页面的请求的，还有定义了很多的MIME类型</p>
</li>
</ol>
<ol start="7">
<li><p>ServletConfig是javaWeb四大域对象之一，他们的功能就是在Servlet中传递数据，这个对象就是存取属性用于Servlet交互。<br>getAttribute()   setAttribute()  removeAttribute()</p>
</li>
<li><p>设置公共的初始化配置在Servlet标签外面写context-param</p>
</li>
<li><p>获取的ServletConfig对象里面的getRealPath(“/index.jsp)这个就是获取他在服务器上得真是位置，然后需要转化为流对象的话我们可以使用getResourceAsStream()直接返回的流对象</p>
</li>
</ol>
<h2 id="4-ServletContext-对象"><a href="#4-ServletContext-对象" class="headerlink" title="4. ServletContext 对象"></a>4. ServletContext 对象</h2><p>&emsp;&emsp;  这个对象其实就是我们整个的项目，所以一般我们把它称之为 application ，他在整个项目中只有一个，并且他的生命周期和 Tomcat 一模一样。一般用它来做多个 Servlet 之间的数据传递。<br>&emsp;&emsp;  可以使用 ServletConfig 获取这个对象。或者使用 <code>getServletContext()</code> 直接获取 。我们还可以使用 HttpSession 获得这个对象。还能使用 ServletContextEvent 获取这个对象。<br>&emsp;&emsp;  可以获取 Web 根目录下的资源路径，也就是 webapps 下面的项目下的 web 目录，<code>getRealPath()</code> 、<code>getResourceAsStream()</code> ，同样的我们也可以使用 class 和 classloader 来获取类路径下的资源。<br><code>className.class.getClassLoader().getResourceAsStream()</code> 或者 <code>className.class.getResourceAsStream()</code> 。</p>
<h2 id="5-Servlet-四大域对象"><a href="#5-Servlet-四大域对象" class="headerlink" title="5. Servlet 四大域对象"></a>5. Servlet 四大域对象</h2><p>首先说一下域对象的功能，简单来说就是一个用来在多个 Servlet 之间传递数据的对象，类似于一个 Map 容器。</p>
<ol>
<li>PageContext</li>
<li>ServletRequest</li>
<li>HttpSession</li>
<li>ServletContext</li>
</ol>
<p>他们都有 set/get/removeAttribute(“”,obj) 方法来存取数据。</p>
<h3 id="4-反射："><a href="#4-反射：" class="headerlink" title="4.反射："></a>4.反射：</h3><ol>
<li>获取class，有三种方法：<ul>
<li>类名.class</li>
<li>对象.getClass()</li>
<li>Class.forName(“具体路径（包名）”)</li>
</ul>
</li>
<li>对于一个类如果我们不使用new我们可以使用class来获取</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Class clazz=Class.forName(<span class="string">"cn.lwen.Person"</span>);</span><br><span class="line">Person p=(Person) clazz.newInstance();  <span class="comment">//p就是person的实例，这个显然也就是无参数的构造方法来实现的然后对于有参数的构造方法则是使用getConstructor来获取这个实例</span></span><br><span class="line">Class clazz=Class.forName(<span class="string">"cn.lwen.Person"</span>);</span><br><span class="line">Constructor cs=clazz.getConstructor(类型的<span class="class"><span class="keyword">class</span>（如：<span class="title">String</span>.<span class="title">class</span>）由于这个地方是可变参数，所以说参数的个说可以有多个)</span></span><br><span class="line"><span class="class"><span class="title">Person</span> <span class="title">p</span></span>=<span class="keyword">new</span> (Person) cs.newInstance(<span class="string">"lwen"</span>);</span><br></pre></td></tr></table></figure>

<ol start="3">
<li><p>操作类的属性：类的属性是由一个叫做getDeclaredField()的方法获得的，得到的类型是Field类型，然后可以使用返回的类型的set方法类设置，第一个参数是；类的实例，第二个就是属性的内容<br>但是这里只允许操作公共的变量，私有变量是不允许的，filed上面的一个setAccessible(true)则表示可以直接操作底油属性</p>
</li>
<li><p>对于方法的操作就是和上面的属性很类似，首先使用getDeclaredMethod(“”,parm)获得方法然后返回的Method类型的，如果是私有的则和上面执行一样的操作，然后使用<br>返回的Method对象的invoke(obj,parmar)执行函数</p>
</li>
</ol>
<h3 id="5-两大重要对象："><a href="#5-两大重要对象：" class="headerlink" title="5. 两大重要对象："></a>5. 两大重要对象：</h3><h4 id="1-在service这个方法中有两个很重要的对象response和request这两个，现在主要就说说这两个东西怎么给客户端请求以及响应"><a href="#1-在service这个方法中有两个很重要的对象response和request这两个，现在主要就说说这两个东西怎么给客户端请求以及响应" class="headerlink" title="1. 在service这个方法中有两个很重要的对象response和request这两个，现在主要就说说这两个东西怎么给客户端请求以及响应"></a>1. 在service这个方法中有两个很重要的对象response和request这两个，现在主要就说说这两个东西怎么给客户端请求以及响应</h4><h3 id="response"><a href="#response" class="headerlink" title="response"></a>response</h3><ol>
<li><p>首先就有一个发送状态信息的方法：response.sendError(404,”访问的页面存在但是不给你看”);//发送一个错误的返回信息</p>
</li>
<li><p>然后就是设置头信息，头信息就是键值对这里有三个常用的设置头信息的方法：  </p>
</li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">setHeader(<span class="string">""</span>,<span class="string">""</span>)</span><br><span class="line">setIntHeader(<span class="string">""</span>,<span class="keyword">int</span>)</span><br><span class="line">setDateHeader(<span class="string">""</span>,second)</span><br></pre></td></tr></table></figure>

<p>然后这个方法把set改成add就是一个键多个值<br>设置重定向setRedirect(“URL”)  快捷的方法  也可使用设置头来做. <code>setHeader(&quot;Location&quot;,&quot;index.jsp&quot;)</code> 、 <code>setStatus(302,&quot;ss&quot;)</code></p>
<ol start="3">
<li><p>response里面的两个流，字符流和字节流，但是这两个流 不能同时存在否则会抛异常</p>
<pre><code>getOutputStream()
getWriter()</code></pre></li>
<li><p>设置浏览器的缓存</p>
</li>
</ol>
<h3 id="request"><a href="#request" class="headerlink" title="request"></a>request</h3><ol>
<li><p>获取客户端的信息，获取客户端的url信息</p>
</li>
<li><p>获取客户端的请求的参数，无论是get还是post对于单值的属性都可以使用getParameter(“name”)   然后对于多值则是getParameterValues()返回数组<br>   getParameterNames()获取所有的键名，是枚举类型</p>
</li>
<li><p>请求转发和请求包含。</p>
<pre><code>请求转发：如果我们访问A，然后A做了请求转发到B，那么最终返回给客户端的就是保留了A的请求头和B的请求体（留头不留体）这次过程中url不变
请求包含：同上，只是这次包含A的头和体还有B的体
他们与重定向是不同的，浏览器不知道A请求了B，这始终就是一个请求</code></pre></li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">request.getRequestDispatcher(<span class="string">"/AServlet"</span>).forward(request,response);  <span class="comment">//请求转发</span></span><br><span class="line">       request.getRequestDispatcher(<span class="string">"/AServlet"</span>).include(request,response);  <span class="comment">//请求包含</span></span><br><span class="line">       <span class="comment">//request域   在Servlet中有三大域对象，在javaweb中有四个</span></span><br><span class="line">       <span class="comment">//分别就是request,session,application,Context</span></span><br><span class="line">       <span class="comment">//他们的生命周期就和这个对象的生命周期相同</span></span><br><span class="line">       <span class="comment">//他们都用相同的方法setAttribute()  getAttribute()  removeAttribute()  然后供其他的Servlet使用这些存在这些对象的信息</span></span><br></pre></td></tr></table></figure>

<h4 id="2-编码问题："><a href="#2-编码问题：" class="headerlink" title="2. 编码问题："></a>2. 编码问题：</h4><pre><code>编码分为响应编码和请求编码
响应编码：就是服务器向客户端发送的数据的编码，我们首先要自己发送的是utf-8并且还要浏览器用utf-8去解析，我们采用的就是http头信息
          setHeader(&quot;Content-Type:text/html;charset:utf-8&quot;)  或者直接一个简单的方法就是setContentType(&quot;text/html;charset:utf-8&quot;)
请求编码：则是服务器接受客户端过来的请求，进行编码，客户端默认使用的是iso，我们需要转成utf-8，而且这个地方是区分post和get请求方式的
          post就直接使用setCharacterEncoding(&quot;utf-8&quot;)而get就需要先转化成byte数组然后使用string转成utf-8
          byte[] by str.getBytes(&quot;ISO-8859-1&quot;);
          string =new String(byte,&quot;utf-8&quot;)</code></pre><h3 id="6-JSP"><a href="#6-JSP" class="headerlink" title="6. JSP"></a>6. JSP</h3><h4 id="1-JSP三大指令-：-page-include-taglib"><a href="#1-JSP三大指令-：-page-include-taglib" class="headerlink" title="1. JSP三大指令 ： page  include  taglib"></a>1. JSP三大指令 ： page  include  taglib</h4><p>一个jsp页面可以使用多个指令，不一定放在第一行   </p>
<h5 id="—page："><a href="#—page：" class="headerlink" title="—page："></a>—page：</h5><pre><code>pageEncoding  设置页面的编码   在服务器把jsp编译成java时使用这个属性
contentType   设置返回响应头  就和setContentType()方法一样
以上两个属性只要设置了一个属性另一个属性可以不设置
如果都不设置就是iso8859-1
import 导包  可以用逗号隔开
errorPage   当前页面如果抛出异常要转发到哪个页面，不是跳转  状态码200
isErrorPage  设置当前页面是否为处理错误的页面   只有这个页面可以使使用9大内置对象的exception（当标签的内容为true）   状态码为500
isELignore   是否忽略el表达式</code></pre><h5 id="—include-静态包含"><a href="#—include-静态包含" class="headerlink" title="—include  静态包含"></a>—include  静态包含</h5><pre><code>他是在编译成java文件的时候完成的  他们共同编译成一个java文件  然后生成一个class
他和ResponseDispatcher的include作用类似  只是他是动态包含  编译成两个不一样的文件class  最后运行的时候才合并  动态合并
静态包含的作用就是页面分解</code></pre><h5 id="—taglib"><a href="#—taglib" class="headerlink" title="—taglib"></a>—taglib</h5><pre><code>引入标签库
prefix引入前缀
uri 标签的地址</code></pre><p> &lt;%@ page …..%&gt;<br>       在web.xml中配置errorPage    </p>
<figure class="highlight xml"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">error-page</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">error-code</span>&gt;</span>404<span class="tag">&lt;/<span class="name">error-code</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">localtion</span>&gt;</span>/error.jsp<span class="tag">&lt;/<span class="name">localtion</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">error-page</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">error-page</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">error-code</span>&gt;</span>500<span class="tag">&lt;/<span class="name">error-code</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">localtion</span>&gt;</span>/error.jsp<span class="tag">&lt;/<span class="name">localtion</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">error-page</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">error-page</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">exception-type</span>&gt;</span>java.lang.RuntimeException<span class="tag">&lt;/<span class="name">exception-type</span>&gt;</span>   //页面抛出任何异常都会显示500   有事为了更加细致的分  我们可以定异常来处理</span><br><span class="line">    <span class="tag">&lt;<span class="name">localtion</span>&gt;</span>/error.jsp<span class="tag">&lt;/<span class="name">localtion</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">error-page</span>&gt;</span></span><br></pre></td></tr></table></figure>

<h4 id="2-JSP-九大内置对象和四大域对象："><a href="#2-JSP-九大内置对象和四大域对象：" class="headerlink" title="2. JSP 九大内置对象和四大域对象："></a>2. JSP 九大内置对象和四大域对象：</h4><ul>
<li>out   就是response.getWriter  用于想浏览器输出</li>
<li>config   ServletConfig   就是xml中的内容</li>
<li>page  当前jsp对象   引用类型就是Object    Object page=this</li>
<li>pageContext  一个顶9个</li>
<li>response   HttpServletResponse</li>
<li>request    HttpServletRequest</li>
<li>exception   异常</li>
<li>session  </li>
<li>application 整个tomcat存活期有效   ServletContext</li>
</ul>
<p>Servlet三大jsp四大域对象  </p>
<ul>
<li>ServletContext   整个应用</li>
<li>session  整个会话</li>
<li>request  一个请求</li>
</ul>
<p>——————-jsp专有</p>
<ul>
<li>pageContext  一个jsp页面   用于jsp标签中的数据传递   代理其他域可以其他的域查找设置pageContext.setAttribute(“”,””,PageContext.SESSION_SCOPE)   就是设置到session中<pre><code>全域查找  findAttribute(&quot;xx&quot;)   从小到大（域范围），依次查找</code></pre></li>
</ul>
<h4 id="3-JSP的动作标签："><a href="#3-JSP的动作标签：" class="headerlink" title="3.JSP的动作标签："></a>3.JSP的动作标签：</h4><p><a href="jsp:forward" target="_blank" rel="noopener">jsp:forward</a>   他和RequestDispatcher中的forward一样  就是一个在Servlet中使用一个在jsp中使用<br><a href="jsp:include" target="_blank" rel="noopener">jsp:include</a>   同上  include方法一样<br><a href="jsp:param" target="_blank" rel="noopener">jsp:param</a>   用来作为前两个的子标签 用来给包含的或者转发的页面传递参数</p>
<h4 id="4-JavaBean："><a href="#4-JavaBean：" class="headerlink" title="4.JavaBean："></a>4.JavaBean：</h4><p> 必须要为成员提供get和set方法<br> 必须要有默认构造器<br> 某个属性有get和set方法但是没有该属性 也是可以的<br> boolean的属性get方法可以直接就是is。。</p>
<h4 id="5-EL表达式："><a href="#5-EL表达式：" class="headerlink" title="5.EL表达式："></a>5.EL表达式：</h4><p> $(pageScope.xxx)   获取并输入该域中的xxx字段<br> $(requestScope.xxx)<br> $(sessionScope.xxx)<br> $(applicationScope.xxx)<br>如果不写那么就是全域查找</p>
<p>EL表达式就是用来输出的  用来代替&lt;%= %&gt;</p>
<h5 id="EL的11个内置对象："><a href="#EL的11个内置对象：" class="headerlink" title="EL的11个内置对象："></a>EL的11个内置对象：</h5><p>其中只有pageContext不是map类型  因为她就是pageContext类型其他的都是map类型   在El表达式中 Map的值访问可以使用点  或者数组方式  map[‘key’]  map.key<br>上面已经有4个就是pageScope….<br>然后就是<br>param   key为参数名value为参数值   和getParam方法一样   适用于单值的<br>paramValues   适用于多值的  和上面的一样  value是一个数组<br>header:<br>headerValues:<br>这两个同上<br>initParam  获取<context-param></context-param>里面的参数<br>cookie  value是cookie对象  所以在获得cookie对象以后必须要使用value才能获得值<br>pageContext  例如${pageContext.request.contextPage}  获取request域中的contextPage内容</p>
<h3 id="4-EL的函数库（由JSTL提供）"><a href="#4-EL的函数库（由JSTL提供）" class="headerlink" title="4.EL的函数库（由JSTL提供）"></a>4.EL的函数库（由JSTL提供）</h3><p>首先需要导入JSTL的函数库<br>${fn:substring(“123123”,1,2)}</p>
<h3 id="5-自定义函数库："><a href="#5-自定义函数库：" class="headerlink" title="5.自定义函数库："></a>5.自定义函数库：</h3><p>  写一个java类  类中的方法可多个  但是必须为 public  static<br>  然后写tld文件,ide可以生成在xml下</p>
<h3 id="8-JSTL-："><a href="#8-JSTL-：" class="headerlink" title="8. JSTL ："></a>8. JSTL ：</h3><h4 id="1-回顾："><a href="#1-回顾：" class="headerlink" title="1. 回顾："></a>1. 回顾：</h4><pre>
jsp：
    三大指令
    include  page   taglib
    九个内置对象
    response cookie out page pageContext request Exception config application
    动作标签
    forward include param
javaBean：
    默认构造器
    set，get方法

EL表达式：
    基本语法
    11个内置对象pageScope...Header，headerValuers,initParam,cookie,pageContext,param,paramValues
    函数库
</pre>    


<h4 id="2-JSTL标签库："><a href="#2-JSTL标签库：" class="headerlink" title="2.JSTL标签库："></a>2.JSTL标签库：</h4><pre><code>四大标签库：core核心库（重要），fmt（格式化），sql，xml（后两个过时）
导入标签，使用taglib指令</code></pre><h5 id="–core：【C标签】"><a href="#–core：【C标签】" class="headerlink" title="–core：【C标签】"></a>–core：【C标签】</h5><ul>
<li>out &lt;c:out value=”aaa”&gt;字符常量  &lt;c:out value=”${aaa}”&gt;全域查找    default表示默认的值  escapeXml是否转义特殊字符  默认true</li>
<li>set  &lt;c:set var=”” value=”” scope=””&gt;   var变量名   value可以是EL表达式  scope域  默认page</li>
<li>remove  &lt;c:remove var=”” scope=””&gt;  给出scope就删指定的   不然就是所有域</li>
<li>url &lt;c:url value=”” &gt;  在路径前面自动加项目名  但是value一斜杠开头  &lt;c:param name=”” value=””&gt; 子标签用于传参，并对参数进行url编码   var参数用来保存生成的url而不进行输出，否则就直接输出</li>
<li>if  &lt;c:if test=${not empty a}/&gt;  判断a不是空就输出  在全域内查找   test中放bool类型</li>
<li>choose &lt;c:choose /&gt;  表示if….elseif()..else  这个标签主要用于容纳when标签  when标签里面有test属性</li>
<li>forEach &lt;c:forEach var=”” begin=”” end=”” step=””&gt; 包含头也包含尾  items 需要遍历的对象(这里一定不要有空格) var 迭代到这个变量上<pre><code>循环状态变量   vsStatus=“vs”  vs.index  vs.count   vs.first  vs.last vs.current</code></pre>  –fmt标签：【格式化标签】<pre><code>&lt;fmt:formatData value=&quot;&quot; pattern=&quot;&quot;&gt;  例如 yyyy-MM-DD
&lt;fmt:formatNumber value=&quot;&quot; pattern=&quot;&quot;&gt;  例如0.00  两位小数  四舍五入  </code></pre></li>
</ul>
<h4 id="3-JSTL自定义标签"><a href="#3-JSTL自定义标签" class="headerlink" title="3.JSTL自定义标签"></a>3.JSTL自定义标签</h4><pre><code>1. 步骤：
    * 标签处理类  实现SimpleTag接口  或者继承simpleTagSupport他做了很多处理 tomcat传 给simpleTag的参数都被保存了
    * tld文件（xml）标签和类相关联  都放在WEB-INF中  防止别人直接访问
    * 在页面中引入
2. 标签处理类
    * 这些方法都是有tomcat调用  最后调用的是doTag方法  和Servlet非常类似
    * doTag  执行标签的时候调用  
    * getParent 获得父标签
    * setParent  设置父标签
    * setJspBody  设置标签体
    * setJSPContext  设置pageContext
3. 跳过某部分
    直接在处理类中抛出一个跳过页面异常，最终编译的代码就是一个标签一个函数，由于这个函数也是直接抛出异常，所以该标签后面的内容就不执行了
    而是跑到调用该函数的try块中的catch中执行响应的方法，这个catch对SkipPageException特别的照顾了一下，然后就结束了
4. 自定义标签属性：
    1. 在处理类中创建属性
    2. 在tld文件中定义    </code></pre><h4 id="4-三层架构"><a href="#4-三层架构" class="headerlink" title="4.三层架构"></a>4.三层架构</h4><p>mvc是bs架构的公共的东西<br>而三层架构则是java web的东西：<br>    web层  与web相关的  Servlet  jsp<br>    业务层 功能【登陆，注册，转账 等等…】(service)<br>    数据层（dao 【data access Object】）(dao)</p>
]]></content>
      <categories>
        <category>-JavaWeb</category>
      </categories>
      <tags>
        <tag>Web</tag>
      </tags>
  </entry>
  <entry>
    <title>Java集合框架Map</title>
    <url>/2017/08/09/Java%20SE/Java%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6Map/</url>
    <content><![CDATA[<ol>
<li><p>map集合是一对一对的存放，而且要保证键名的唯一性。</p>
</li>
<li><p>map的共性方法：<br> 1.添加：</p>
<pre><code>put(K key,V value)
putAll(K key,V value)</code></pre><p> 2.删除：</p>
<pre><code>clear()
remove(Object key)
&lt;!--more--&gt;</code></pre><p> 3.判断：</p>
<pre><code>isEmpty()
constrainKey(object key)
constrainValue(object value)</code></pre><p> 4.获取：</p>
<pre><code>get(Object key)
size()
values()</code></pre></li>
</ol>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MapDemo01</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        HashMap&lt;String,String&gt; hashMap=<span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        <span class="comment">//当第一次存放元素的时候这个键不存在所以返回的是null</span></span><br><span class="line">        <span class="comment">//第二次再存放同一个键的值的时候不会报错而是返回的改建原有的值这个和Set以及collection是不一样的</span></span><br><span class="line">        System.out.println(hashMap.put(<span class="string">"01"</span>,<span class="string">"zhansgan01"</span>));</span><br><span class="line">        System.out.println(hashMap.put(<span class="string">"01"</span>,<span class="string">"zhansgan02"</span>));</span><br><span class="line">        hashMap.put(<span class="string">"03"</span>,<span class="string">"zhansgan03"</span>);</span><br><span class="line">        hashMap.put(<span class="string">"04"</span>,<span class="string">"zhansgan04"</span>);</span><br><span class="line"></span><br><span class="line">        System.out.println(hashMap.get(<span class="string">"01"</span>));</span><br><span class="line">        Collection&lt;String&gt; collection=hashMap.values();</span><br><span class="line">        System.out.println(collection);</span><br><span class="line"></span><br><span class="line">        System.out.println(hashMap.containsKey(<span class="string">"02"</span>));</span><br><span class="line">        System.out.println(hashMap.containsValue(<span class="string">"zhansgan03"</span>));</span><br><span class="line">        <span class="comment">//remove返回的value</span></span><br><span class="line">        System.out.println(hashMap.remove(<span class="string">"03"</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>3.Map的三个常用的小弟：</p>
<ul>
<li>hashTable   底层是hash表不可以使用null作为键或者值，线程同步  jdk1.0</li>
<li>hashMap     底层是hash表可以使用null作为键或者值，线程不同步  jdk1.2  效率 更高</li>
<li>TreeMap     底层是二叉树，可对map的键进行排序<br>其实set的底层都是Map</li>
</ul>
<p>4.两个特别重要的函数keySet和entrySet<br>  首先是keySet，其实在Map集合中里面的结构是双列结构也就是；两个Set构成的，里面分别存放<br>  键值的引用，而keySet就是只把键取出来，类似于values取出所有的值，当吧所有的键都取出来的时候就意味着我们<br>  具备了所有的值，这时候我们就可以遍历Map了，map本身没有遍历的方法<br>  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">KeyMapDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        HashMap&lt;String,String&gt; hashMap=<span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        hashMap.put(<span class="string">"1"</span>,<span class="string">"zs1"</span>);</span><br><span class="line">        hashMap.put(<span class="string">"2"</span>,<span class="string">"zs2"</span>);</span><br><span class="line">        hashMap.put(<span class="string">"3"</span>,<span class="string">"zs3"</span>);</span><br><span class="line">        hashMap.put(<span class="string">"4"</span>,<span class="string">"zs4"</span>);</span><br><span class="line">        Set&lt;String&gt; keySet =hashMap.keySet();</span><br><span class="line">        Iterator&lt;String&gt; it=keySet.iterator();</span><br><span class="line">        <span class="keyword">for</span> (;it.hasNext();)&#123;</span><br><span class="line">            String key=it.next();</span><br><span class="line">            System.out.println(hashMap.get(key));</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>5.entrySet则是直接返回了这个Map的映射关系，仅仅是关系而不是元组。<br>  这个关系的类型是Map.Entry类型的里面有getValue和getKey方法<br>  Entry其实是Map接口的内部接口就像是内部类一样，这是内部接口<br>  并且这个接口是静态的</p>
  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">EntrySet</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Map&lt;String,String&gt; map=<span class="keyword">new</span> HashMap&lt;&gt;();</span><br><span class="line">        map.put(<span class="string">"1"</span>,<span class="string">"s1"</span>);</span><br><span class="line">        map.put(<span class="string">"2"</span>,<span class="string">"s2"</span>);</span><br><span class="line">        map.put(<span class="string">"3"</span>,<span class="string">"s3"</span>);</span><br><span class="line">        <span class="comment">//返回的还是set集合只里面的类型是Map.Entry类型的</span></span><br><span class="line">        Set&lt;Map.Entry&lt;String, String&gt;&gt; mapEntry=map.entrySet();</span><br><span class="line">        <span class="keyword">for</span> (Map.Entry me :</span><br><span class="line">                mapEntry) &#123;</span><br><span class="line">            System.out.println(me.getKey()+<span class="string">"--------"</span>+me.getValue());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//类似于种东西</span></span><br><span class="line"><span class="class"><span class="keyword">interface</span> <span class="title">Map</span></span>&#123;</span><br><span class="line">    <span class="keyword">static</span> <span class="class"><span class="keyword">interface</span> <span class="title">Entry</span></span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">abstract</span> Object <span class="title">getKey</span><span class="params">()</span></span>;</span><br><span class="line">        <span class="function"><span class="keyword">abstract</span> Object <span class="title">getValue</span><span class="params">()</span></span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>5.一个小demo<br> <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * Created by lwen on 2017/7/13.</span></span><br><span class="line"><span class="comment"> * 把一个字符串里面的字符的数目统计一下，，并且按照字符的顺序显示</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">MapDemo03</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        String str=charCount(<span class="string">"aasdfasadcl"</span>);</span><br><span class="line">        System.out.println(str);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">static</span> String <span class="title">charCount</span><span class="params">(String str)</span></span>&#123;</span><br><span class="line">        <span class="keyword">char</span>[] chs=str.toCharArray();</span><br><span class="line">        TreeMap&lt;Character,Integer&gt; treeMap=<span class="keyword">new</span> TreeMap&lt;&gt;();</span><br><span class="line">        <span class="keyword">int</span> count=<span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">char</span> ch:chs)&#123;</span><br><span class="line">            <span class="keyword">if</span> (treeMap.get(ch)!=<span class="keyword">null</span>)&#123;</span><br><span class="line">                count=treeMap.get(ch);</span><br><span class="line">            &#125;</span><br><span class="line">            count++;</span><br><span class="line">            treeMap.put(ch,count);</span><br><span class="line">            count=<span class="number">0</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        StringBuilder sb=<span class="keyword">new</span> StringBuilder();</span><br><span class="line">        Set&lt;Map.Entry&lt;Character,Integer&gt;&gt; set=treeMap.entrySet();</span><br><span class="line">        <span class="keyword">for</span> (Map.Entry&lt;Character,Integer&gt; em:set)&#123;</span><br><span class="line">            sb.append(em.getKey()).append(<span class="string">"("</span>).append(em.getValue()).append(<span class="string">")"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> sb.toString();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p> 7.集合框架总共有两大部分分别就是：<br> <pre><br>    ---Collection<br>        |--List  ArrayList LinkedList<br>        |--Set  HashSet  TreeSet<br>    ---Map<br>        |--HashTable<br>        |--HashMap<br>        |--TreeMap<br></pre><br>另外还有两个常用的类就是两个工具类Collections,Collections里面提供了:</p>
<ul>
<li>1) 一个sort方法他是专门对Collection排序的</li>
<li>2)还有，max方法用法和sort方法一模一样</li>
<li>3）binarySearch就是二分法查找元素可以自己实现一个试试</li>
<li>5) replaceAll(list,oldEle,newEle)</li>
<li>6)reverseOrder()强行翻转一个比较器，当不传入参数的时候强行逆转默认的自然排序，传入一个比较器则是逆转比较器</li>
</ul>
<p>还有一个工具类就是Arrays这个类里面的方法主要就是为了操作数组的：</p>
<ul>
<li>1）sort</li>
<li>2）fill   全部替换</li>
<li>3）shuflle  乱序</li>
<li>4）asList  把数组转化为List集合，但是注意这个list集合不能进行增删操作否则异常，还有就是如果数组是一个对象类型的数组，转化为list最后就是对象的list<br>而数组时基本数据类型则会把数组当做一个list元素</li>
</ul>
<pre><code>集合变数组，集合变数组使用的就是Collection接口里面的共性方法，toArray(type [collection.size()])
一般转过来的数组的长度最好就是集合的长度否则他会再开辟一个数组，而小于等于的情况会直接使用原来的数组

数组变成集合是为了扩展数组的操作让数组作为集合来处理例如contains数组要判断元素是否存在必须遍历而集合就不用
把集合变成数组是为了限制操作，也就是让数组无法操作，例如再返回的时候最好返回数组

可变参数的方法，就是在函数声明或者定义的时候使用一个数组当做参数，但是我们传参的时候传入的并不是一个数组而是一般状况的传值，个数不限
但是这个函数的固定参数必须放在可变参数的前面
void fun(string str,int... arr)   后面就是可变参数个数不限但是类型有限制</code></pre><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">StrLenComparator</span> <span class="keyword">implements</span> <span class="title">Comparator</span>&lt;<span class="title">String</span>&gt;</span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(String s1,String s2)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (s1.length()&gt;s2.length())&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span> <span class="keyword">if</span>(s1.length()&lt;s2.length())&#123;</span><br><span class="line">            <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> s1.compareTo(s2);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">CollectionsDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        List&lt;String&gt; list=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        list.add(<span class="string">"aaa"</span>);</span><br><span class="line">        list.add(<span class="string">"ab"</span>);</span><br><span class="line">        list.add(<span class="string">"adc"</span>);</span><br><span class="line">        list.add(<span class="string">"dd"</span>);</span><br><span class="line">        list.add(<span class="string">"cd"</span>);</span><br><span class="line">        list.add(<span class="string">"efcasdc"</span>);</span><br><span class="line">        Collections.sort(list);</span><br><span class="line">        <span class="keyword">for</span> (String string:list)&#123;</span><br><span class="line">            System.out.println(string);</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        Collections.sort(list,<span class="keyword">new</span> StrLenComparator());</span><br><span class="line">        <span class="keyword">for</span> (String string:list)&#123;</span><br><span class="line">            System.out.println(string);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>10，静态导入：<br>    当一般的我们import一个包的时候我们导入的是包中的类<br>    而我们使用静态导入的时候import static则是导入类中的所有的静态成员，无论是属性还是方法<br>  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> <span class="keyword">static</span> java.util.Arrays.*;</span><br><span class="line"><span class="keyword">import</span> <span class="keyword">static</span> java.util.Collections.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">StaticImport</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">int</span>[] arr=<span class="keyword">new</span> <span class="keyword">int</span>[<span class="number">11</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">10</span>,j=<span class="number">0</span>;i&gt;<span class="number">0</span>;i--,j++)&#123;</span><br><span class="line">            arr[j]=i;</span><br><span class="line">        &#125;</span><br><span class="line">        sort(arr);   <span class="comment">//可以直接使用Arrays中的方法而不用写Arrays.sort但是如果同一个类中名字冲突的话需要加上</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> ele:arr)&#123;</span><br><span class="line">            System.out.println(ele);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>9.HashMap与HashTable的区别：<br>    1）hashmap不是线程同步的，而hashtable则是线程同步的，也就是前者效率较高后者效率较低，安全性相反<br>    2）hashmap他是Map的子类。而hashtable则是dictionary的子类<br>    3）hashMap的键值均可以为null，hashtable的键值均不可为null<br>10.hashTable的使用。<br>    一般来说hashtable使用的很少，这里也有一个常用的子类就是hashTable的常用子类Properties<br>    类，这个类的作用主要就是读，写，加载配置文件，这个容器里面只允许存放字符串，不论是键值均如此<br>    存放的键值可以转化为普通的配置文件也可以转化为xml文件，之后下一次还可以使用load进行加载</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HashTableDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> IOException</span>&#123;</span><br><span class="line">        Properties properties=<span class="keyword">new</span> Properties();</span><br><span class="line">        properties.setProperty(<span class="string">"user"</span>,<span class="string">"lwen"</span>);</span><br><span class="line">        properties.setProperty(<span class="string">"pwd"</span>,<span class="string">"123"</span>);</span><br><span class="line">        properties.store(<span class="keyword">new</span> FileWriter(<span class="string">"info.properties"</span>),<span class="string">"用户配置文件"</span>);</span><br><span class="line">        properties.store(<span class="keyword">new</span> FileOutputStream(<span class="string">"info_1.properties"</span>),<span class="string">"用户配置文件"</span>);  <span class="comment">//可以字符流也可以字节流</span></span><br><span class="line">        properties.storeToXML(<span class="keyword">new</span> FileOutputStream(<span class="string">"info.xml"</span>),<span class="string">"xml"</span>);  <span class="comment">//这个只能使用字节流对象  xml没有编码</span></span><br><span class="line">        System.out.println(properties.getProperty(<span class="string">"user"</span>,<span class="string">"not this "</span>));  <span class="comment">//如果找不到这个属性，则会返回默认值</span></span><br><span class="line">        properties.load(<span class="keyword">new</span> FileReader(<span class="string">"info.properties"</span>));  <span class="comment">//加载配置文件是直接加载到这个对象里面，而不是打印</span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>11.内存的中的变量一般分为4种，这四种对于垃圾回收机制来说是不一样的：<br>    强引用：无论什么时候都不会被gc回收<br>    软引用：可能会被gc回收，只有在jvm内存不足的时候才会被回收<br>    弱引用：当gc运行的时候就被回收<br>    虚引用：类似于无引用，主要跟踪对象被回收的状态，不能单独使用，一般和引用队列一起使用<br>    这里重点说一下强和弱引用，所谓强引用一般来说就是一些字符串常量，他们长期驻留在内存，用于共享<br>    当我们在一个hashMap中存放了很多内容的时候，有时候我们需要清除一些没用的数据，我们就可以运行gc<br>    因此就诞生了WeakHashMap这样的话，这里面的弱类型的就会被干掉</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">WeakHashMapDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        WeakHashMap&lt;String,String&gt; weakHashMap=<span class="keyword">new</span> WeakHashMap&lt;&gt;();</span><br><span class="line">        <span class="comment">//这两个事在常量池中的常量，所以放到weakmap中也不会被gc</span></span><br><span class="line">        weakHashMap.put(<span class="string">"zhangsan0"</span>,<span class="string">"122"</span>);</span><br><span class="line">        weakHashMap.put(<span class="string">"zhangsan1"</span>,<span class="string">"123"</span>);</span><br><span class="line">        <span class="comment">//这两个则是两个对象在堆内存中，放到这里了以后，肯定被gc</span></span><br><span class="line">        weakHashMap.put(<span class="keyword">new</span> String(<span class="string">"zhangsan2"</span>),<span class="string">"102"</span>);</span><br><span class="line">        weakHashMap.put(<span class="keyword">new</span> String(<span class="string">"zhangsan3"</span>),<span class="string">"1245"</span>);</span><br><span class="line">        <span class="comment">//运行垃圾回收机制之前</span></span><br><span class="line">        Set&lt;Map.Entry&lt;String,String&gt;&gt; me=weakHashMap.entrySet();</span><br><span class="line">        <span class="keyword">for</span> (Map.Entry&lt;String, String&gt; stringStringEntry : me) &#123;</span><br><span class="line">            System.out.println(stringStringEntry.getKey());</span><br><span class="line">            System.out.println(stringStringEntry.getValue());</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">//运行垃圾回收机制</span></span><br><span class="line">        System.gc();</span><br><span class="line">        System.runFinalization();</span><br><span class="line"></span><br><span class="line">        <span class="comment">//垃圾回收之后</span></span><br><span class="line">        System.out.println(<span class="string">"========================="</span>);</span><br><span class="line">        Set&lt;Map.Entry&lt;String,String&gt;&gt; me1=weakHashMap.entrySet();</span><br><span class="line">        <span class="keyword">for</span> (Map.Entry&lt;String, String&gt; stringStringEntry : me1) &#123;</span><br><span class="line">            System.out.println(stringStringEntry.getKey());</span><br><span class="line">            System.out.println(stringStringEntry.getValue());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>12.IdentityHashMap这个容器是以键的地址作为比较的对象<br>   注意是地址而不是其他的，例如说如果是一个字符串常量，显然这个只要内容相同就重复了，而如果是字符串对象就是不同的地址值，哪怕他们的内容一样</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">IdentifiedHashMapDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        IdentityHashMap identityHashMap=<span class="keyword">new</span> IdentityHashMap();</span><br><span class="line">        <span class="comment">//前两个是字符串常量，在常量池中，地址是一样的</span></span><br><span class="line">        identityHashMap.put(<span class="string">"a"</span>,<span class="string">"21"</span>);</span><br><span class="line">        identityHashMap.put(<span class="string">"a"</span>,<span class="string">"22"</span>);</span><br><span class="line">        <span class="comment">//后面两个是不同的对象，因此他们都被存入</span></span><br><span class="line">        identityHashMap.put(<span class="keyword">new</span> String(<span class="string">"a"</span>),<span class="string">"23"</span>);</span><br><span class="line">        identityHashMap.put(<span class="keyword">new</span> String(<span class="string">"a"</span>),<span class="string">"24"</span>);</span><br><span class="line">        System.out.println(identityHashMap.size());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>13.对于集合框架中有时候遇到多线程，我们又需要使用那些非同步的集合的时候我们必须手动的去同步这些集合，以及希望集合不可修改的时候也需要加一些限定</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SyncDemo</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        List&lt;String&gt; list=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        List&lt;String&gt; list1=Collections.synchronizedList(list);</span><br><span class="line"><span class="comment">//        这样一来list1就是一个同步的arraylist</span></span><br><span class="line"><span class="comment">//        其他的几个容器类似</span></span><br><span class="line">        List&lt;String&gt; list2=Collections.unmodifiableList(list1);</span><br><span class="line">        <span class="comment">//这样一来list2就是一个不可修改的容器也就是此时的容器不再支持增删操作</span></span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>集合框架</tag>
      </tags>
  </entry>
  <entry>
    <title>Java集合框架Collections</title>
    <url>/2017/08/09/Java%20SE/Java%E9%9B%86%E5%90%88%E6%A1%86%E6%9E%B6Collections/</url>
    <content><![CDATA[<h3 id="1-基本介绍："><a href="#1-基本介绍：" class="headerlink" title="1.基本介绍："></a>1.基本介绍：</h3><ol>
<li><p>集合就是存放对象的，他比数组好的一点就是他一开始不清楚自己长度<br> 容器一般是分为很多种的，很多的容器在一起然后进过断的抽象和抽取就成了一个体系，我们称之为集合框架<br> 我们看体系首先是看顶层的容器，他是底层的容器都有的特性，然后在逐步求精<br> 最顶层的我们称之为collection 在util包中的</p>
<a id="more"></a></li>
<li><p>在collection中分为两个比较常用的子接口分别是list和set。<br>list是类似于数组的那种，也就是集合元素可重复，有序有脚标。<br>set则为无序的，所以集合元素不可重复，不可脚标查找<br>以下是List的通用方法：</p>
<ul>
<li>添加元素：<br>add(index,data)</li>
<li>删除元素：<br>remove(index)</li>
<li>修改元素：<br>set(index,data)</li>
<li>获取元素：<br>get(index)   配合for循环</li>
<li>迭代器<br>indexOf（data）<br>subList（start,end）</li>
</ul>
<p>在list中有一个特殊的迭代器，其他的集合都没有只是list有叫做ListIterater<br>这个迭代器比一般的迭代器会多非常多的功能</p>
<p>另外注意的一点就是在使用iterater进行list迭代的时候，不能够使用集合的方法对集合进行增删改查的操作<br>否则就会出现一个运行时异常，主要原因就是同时操作一个集合导致不合法，类似于同时IO同一块数据块<br>因此在迭代的过程中只能使用迭代器提供的操作集合的方法<br>或者不使用迭代器，直接使用原生的for循环，然后直接就可以使用迭代器的方法进行对集合的操作</p>
</li>
<li><p>add方法接受的参数类型为object以便于接受任意类型的参数<br>集合中存放的是对象的地址而不是对象本身<br>对象可以直接被打印</p>
</li>
<li><p>List可以分为三种，但是常用的只有两种，他们之间的主要区别就是底层的数据及结构不一样。</p>
<ul>
<li><strong>ArrayList</strong>  底层的实现使用的是数组，也就是说类似于数组构成的线性表，查询快增删慢，而且他是不同步的</li>
<li><strong>LinkedList</strong>  底层使用的是链表，那么就会出现查询慢，增删快</li>
<li><strong>Vector</strong>  底层和ArrayList完全一样，只不过vector就是jdk1.0出现的那时候还没有集合框架，后来有了集合框架被分到List里面，目前被ArrayList代替因为他是同步的速度慢，我们都是用Arraylist然后自己写锁来手动同步</li>
</ul>
</li>
<li><p>arraylist和vector之间区别还有就是他们们的迭代方式可以有不同，由于vector是最先出来的，所以说他一开始用的并不是<br>iterator迭代器而是枚举enumeration他和迭代器很相似，目前由于枚举不好记就用iterator。另外arraylist和vector使用的<br>是变长数组，也就是本来都是固定长度10个元素，然后如果查过十个以后ArrayList使用的是50%的增长也就是会变成15个，二vector则是<br>直接100%增长20个<br>枚举的代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Enmu</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        Vector v=<span class="keyword">new</span> Vector();</span><br><span class="line">        v.add(<span class="string">"1"</span>);</span><br><span class="line">        v.add(<span class="string">"2"</span>);</span><br><span class="line">        v.add(<span class="string">"3"</span>);</span><br><span class="line">        Enumeration e=v.elements(); <span class="comment">//定义枚举</span></span><br><span class="line">        <span class="keyword">while</span> (e.hasMoreElements())&#123;  <span class="comment">//循环遍历</span></span><br><span class="line">            System.out.println(e.nextElement());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>LinkedList中除了一般的List的通用方法还有他自己特有的方法，而且比较重要</p>
<ul>
<li>addFirst</li>
<li>addLast</li>
<li>getFirst    获取元素但是不删除元素</li>
<li>getLast</li>
<li>removeFirst   获取元素而且删除元素，但是如果给的是一个空的链表列表使用此方法会产生异常因此有了以下替代方法</li>
<li>removeLast</li>
<li>offerFirst  添加</li>
<li>offerLast</li>
<li>peekFirst 获取</li>
<li>peekLast</li>
<li>pollFirst 删除</li>
<li>pollLast   空链表列表也不会有异常而是直接返回null</li>
</ul>
<p>代码如下：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">LinkedList_5</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        LinkedList list=<span class="keyword">new</span> LinkedList();</span><br><span class="line">        list.addFirst(<span class="string">"1"</span>);</span><br><span class="line">        list.addFirst(<span class="string">"2"</span>);</span><br><span class="line">        list.addFirst(<span class="string">"3"</span>);</span><br><span class="line">        System.out.println(list.getFirst());</span><br><span class="line">        System.out.println(list.removeFirst());</span><br><span class="line"></span><br><span class="line">        LinkedList list1=<span class="keyword">new</span> LinkedList();</span><br><span class="line">        list1.offerFirst(<span class="string">"1"</span>);</span><br><span class="line">        list1.offerFirst(<span class="string">"2"</span>);</span><br><span class="line">        list1.offerFirst(<span class="string">"3"</span>);</span><br><span class="line">        System.out.println(list1.peekFirst());</span><br><span class="line">        System.out.println(list1.pollFirst());</span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>7.使用LinkedList实现堆栈和队列结构也就是他所特有的addFirst   addLast  以及remove方法的使用<br>     只是要注意在LinkedList里面添加和删除的元素都是object而不是一般的对象，应该说<br>     所有的集合框架里面的东西都是接受object对象的  所以在写具体的函数的时候注意一下</p>
<p>8.使用ArrayList写几个小程序，第一个就是使用ArrayList去处重复元素，其中的元素就是字符串，而第二个则是<br>去除的某些自定义对象。第二个程序也揭示了对于List集合中的元素的比较的接口就是contains他底层是调用<br>的对象的equals方法，对于remove也是底层调用了equals方法从而进行比较和删除。所以说重点在于重写<br>equals方法。<br>另外在迭代器中每使用一次next方法必须要进行一次hasNext的判断否则很有可能出现找不到元素的情况<br>例如一个hasNext里面有两个next方法，而元素又为奇数个时候会抛异常</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Person</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    Person(String name,<span class="keyword">int</span> age)&#123;</span><br><span class="line">        <span class="keyword">this</span>.name=name;</span><br><span class="line">        <span class="keyword">this</span>.age=age;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function">String <span class="title">getName</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">getAge</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.age;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object object)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!(object <span class="keyword">instanceof</span> Person))&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        Person person=(Person)object;  <span class="comment">//注意这个地方必须要强转否则会出现下面的person无法调用方法</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.name.equals(person.getName()) &amp;&amp; <span class="keyword">this</span>.age==person.getAge();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">ArrayList_7</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">static</span> ArrayList&lt;Person&gt; <span class="title">singleElement</span><span class="params">(List&lt;Person&gt; al)</span></span>&#123;</span><br><span class="line">        ArrayList&lt;Person&gt; newAl=<span class="keyword">new</span> ArrayList&lt;Person&gt;();</span><br><span class="line">        Iterator&lt;Person&gt; it=al.iterator();</span><br><span class="line">        <span class="keyword">while</span> (it.hasNext())&#123;</span><br><span class="line">            Person tmp= it.next();  <span class="comment">//iterator返回的是object所以必须要强转</span></span><br><span class="line">            <span class="keyword">if</span> (!newAl.contains(tmp))&#123;</span><br><span class="line">                newAl.add(tmp);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> newAl;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ArrayList&lt;Person&gt; al=<span class="keyword">new</span> ArrayList&lt;&gt;();</span><br><span class="line">        al.add(<span class="keyword">new</span> Person(<span class="string">"zhang01"</span>,<span class="number">1</span>));</span><br><span class="line">        al.add(<span class="keyword">new</span> Person(<span class="string">"zhang02"</span>,<span class="number">2</span>));</span><br><span class="line">        al.add(<span class="keyword">new</span> Person(<span class="string">"zhang03"</span>,<span class="number">1</span>));</span><br><span class="line">        al.add(<span class="keyword">new</span> Person(<span class="string">"zhang03"</span>,<span class="number">1</span>));</span><br><span class="line">        al=singleElement(al);</span><br><span class="line">        <span class="keyword">for</span> (Person per : al) &#123;   <span class="comment">//可以使用这种更好的语言结构来实现迭代省去了iterator迭代 也不用考虑向下类型转换</span></span><br><span class="line">            System.out.println(per.getName());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>9.Set中存放的元素都是无序的并且里面的元素都是不可重复的，显然如果只存放字符串的话很容易就知道是否重复<br>  也就无序我们自己去判断是否重复了。<br>  set中的公共方法就是集合框架中共有的方法，重要的还是他的子类，这里有两个就是hashSet和TreeSet</p>
<p>  而存放一般的自定义对象的时候我们发现如果想要某些属性一致的对象作为重复对象<br>  的话hashSet自身是做不到的，所以我们需要了解hashSet的底层层放原理，hashSet底层就是hash表，在存放元素的时候<br>  首先来判断存放的元素的hashCode值是否一样也就是调用他们的hashCode方法，注意hashCode是object对象的方法，所以<br>  所有的对象都有此方法，另外如果他们的hashCode是一样的然后就调用他们的equals方法.不一样则就存进去一样则被踢出去<br>  那么说白了hashSet底层判断是否为重复元素做了两件事第一个就是判断他们的hashCode第二个就是equals方法<br>  如果要自定义对象如何存放就要重写这两个方法，但是重写的时候一定要注意他们的参数列表否则肯定不会生效，hashCode<br>  一般来说也尽量不要让不同的对象的hashCode一致造成多余的比较</p>
<p>  对于元素判断是否存在和删除元素都是hashCode和equals方法</p>
<p>  下面是hashSet的代码示例：<br>  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">People</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line"></span><br><span class="line">    People(String name,<span class="keyword">int</span> age)&#123;</span><br><span class="line">        <span class="keyword">this</span>.name=name;</span><br><span class="line">        <span class="keyword">this</span>.age=age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function">String <span class="title">getName</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="keyword">int</span> <span class="title">getAge</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">hashCode</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name.hashCode()+age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">boolean</span> <span class="title">equals</span><span class="params">(Object object)</span></span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!(object <span class="keyword">instanceof</span> People))&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">false</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        People person=(People)object;  <span class="comment">//注意这个地方必须要强转否则会出现下面的person无法调用方法</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">this</span>.name.equals(person.getName()) &amp;&amp; <span class="keyword">this</span>.age==person.getAge();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">HashSet_8</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        HashSet&lt;People&gt; hs=<span class="keyword">new</span> HashSet&lt;People&gt;();</span><br><span class="line">        hs.add(<span class="keyword">new</span> People(<span class="string">"1"</span>,<span class="number">11</span>));</span><br><span class="line">        hs.add(<span class="keyword">new</span> People(<span class="string">"2"</span>,<span class="number">11</span>));</span><br><span class="line">        hs.add(<span class="keyword">new</span> People(<span class="string">"1"</span>,<span class="number">11</span>));</span><br><span class="line">        <span class="keyword">for</span> (People pe:hs)&#123;</span><br><span class="line">            System.out.println(pe.getName());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></p>
<p>10.treeSet是在集合中的元素会自动排序，如果是字符串什么的他们都可以自动比较，因为字符串是已经实现了Compareable接口<br>   但是如果要存放一般的元素对象的时候注意一定要让改类实现compareable接口，因为此接口会让类强制具有比较性<br>   然后复写此接口中的compareTo方法，大于返回正数等于为零小于则为负，这里要注意如果有多个排序元素的话然后在比较<br>   的时候相等条件判断要注意对其他排序元素的判断，否则会造成某个条件相等但是并不是同一个元素而无法存入</p>
   <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Student</span> <span class="keyword">implements</span> <span class="title">Comparable</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line"></span><br><span class="line">    Student(String name,<span class="keyword">int</span> age)&#123;</span><br><span class="line">        <span class="keyword">this</span>.name=name;</span><br><span class="line">        <span class="keyword">this</span>.age=age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getAge</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compareTo</span><span class="params">(Object o)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!(o <span class="keyword">instanceof</span> Student))&#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> RuntimeException(<span class="string">"not same type"</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (<span class="keyword">this</span>.age&gt;((Student) o).age)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">        &#125;<span class="keyword">else</span> <span class="keyword">if</span> (<span class="keyword">this</span>.age==((Student) o).age)&#123;</span><br><span class="line">            <span class="keyword">return</span> <span class="keyword">this</span>.name.compareTo(((Student) o).name);</span><br><span class="line">            <span class="comment">//注意多重判断，要是age一样的话他们就会被当成相同元素而无法插入  string类已经实现了comparable接口</span></span><br><span class="line">            <span class="comment">//其实java中很多类都实现了comparable接口，让类具有可比性</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> -<span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TreeSet_9</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        TreeSet&lt;Student&gt; ts=<span class="keyword">new</span> TreeSet&lt;&gt;();</span><br><span class="line">        ts.add(<span class="keyword">new</span> Student(<span class="string">"ab"</span>,<span class="number">1</span>));</span><br><span class="line">        ts.add(<span class="keyword">new</span> Student(<span class="string">"kb"</span>,<span class="number">3</span>));</span><br><span class="line">        ts.add(<span class="keyword">new</span> Student(<span class="string">"mb"</span>,<span class="number">3</span>));</span><br><span class="line">        ts.add(<span class="keyword">new</span> Student(<span class="string">"am"</span>,<span class="number">7</span>));</span><br><span class="line">        <span class="keyword">for</span> (Student stu :</span><br><span class="line">                ts) &#123;</span><br><span class="line">            System.out.println(stu.getName()+<span class="string">"--------"</span>+stu.getAge());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>11.TreeSet底层的数据结构就是二叉树，元素的排列方式就是第一个进入的元素作为根节点然后按照compareTo方法<br>   返回的值来判定是左孩子还是右孩子，这里就是如果说compareTo方法返回的正数则右孩子,负数为左孩子相等就说明<br>   是同一个元素不用再比较。<br>   所以说决定了TreeSet中的元素的重复与否就是compareTo函数的返回值<br>   那么这样的话我们也可以规定一个TreeSet按照放进去的顺序取出来 或者倒序取出来就是compareTo全部返回1<br>   或者-1即可，当然如果返回的始终为0那么最后集合中只有一个元素就是第一个元素</p>
<p>12.在TreeSet中除了实现comparable接口复写compareTo方法以外还有一种排序方法就是比较器，前面的comparable<br>   接口是让元素具有了比较性而比较器则是让集合具有了比较性这个优先级跟高，具体的方法就是在集合实例化的时候传入一个<br>   自定义的比较器，也就是构造方法传入一个比较器对象，这个比较器也是一个接口要实例化的话需要实现他的compare方法<br>   比较器也是更加常用的</p>
   <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyComparator</span> <span class="keyword">implements</span> <span class="title">Comparator</span></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">compare</span><span class="params">(Object o1,Object o2)</span></span>&#123;</span><br><span class="line">        Student obj1=(Student)o1;</span><br><span class="line">        Student obj2=(Student)o2;</span><br><span class="line">        <span class="keyword">int</span> num=obj1.getName().compareTo(obj2.getName());</span><br><span class="line">        <span class="keyword">if</span> (num==<span class="number">0</span>)&#123;</span><br><span class="line">           <span class="keyword">return</span> <span class="keyword">new</span> Integer(obj1.getAge()).compareTo(obj2.getAge());</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> num;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">TreeSet_10</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        TreeSet&lt;Student&gt; ts=<span class="keyword">new</span> TreeSet&lt;Student&gt;(<span class="keyword">new</span> MyComparator());</span><br><span class="line">        ts.add(<span class="keyword">new</span> Student(<span class="string">"dsf"</span>,<span class="number">11</span>));</span><br><span class="line">        ts.add(<span class="keyword">new</span> Student(<span class="string">"dmf"</span>,<span class="number">12</span>));</span><br><span class="line">        ts.add(<span class="keyword">new</span> Student(<span class="string">"ddf"</span>,<span class="number">13</span>));</span><br><span class="line">        ts.add(<span class="keyword">new</span> Student(<span class="string">"jf"</span>,<span class="number">14</span>));</span><br><span class="line">        <span class="keyword">for</span> (Student stu :</span><br><span class="line">                ts) &#123;</span><br><span class="line">            System.out.println(stu.getName()+<span class="string">"----------"</span>+stu.getAge());</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>集合框架</tag>
      </tags>
  </entry>
  <entry>
    <title>Java之StringBuffer</title>
    <url>/2017/08/09/Java%20SE/Java%E4%B9%8BStringBuffer/</url>
    <content><![CDATA[<h3 id="1-存储："><a href="#1-存储：" class="headerlink" title="1.存储："></a>1.存储：</h3><ul>
<li>append(data)  添加在最后</li>
<li>insert(index,data)  在制定位置添加<h3 id="2-删除："><a href="#2-删除：" class="headerlink" title="2.删除："></a>2.删除：</h3></li>
<li>delete(start,end)  删除某一段字符串</li>
<li>deleteCharAt(index)  删除置顶字符<a id="more"></a>
<h3 id="3-获取："><a href="#3-获取：" class="headerlink" title="3.获取："></a>3.获取：</h3></li>
<li>charAt(index)</li>
<li>indexOf(data)</li>
<li>lastIndexOf(data)</li>
<li>length()</li>
<li>subString(start,end)<h3 id="4-修改："><a href="#4-修改：" class="headerlink" title="4.修改："></a>4.修改：</h3></li>
<li>replace(start,end,data)</li>
<li>setCharAt(index,data)<h3 id="5-翻转："><a href="#5-翻转：" class="headerlink" title="5.翻转："></a>5.翻转：</h3></li>
<li>reverse()<h3 id="6-获取缓冲区的制定数据放在指定字符串中"><a href="#6-获取缓冲区的制定数据放在指定字符串中" class="headerlink" title="6.获取缓冲区的制定数据放在指定字符串中"></a>6.获取缓冲区的制定数据放在指定字符串中</h3></li>
<li>getChars(start,end,dstChar,dstIndex)</li>
</ul>
<p>还有一个容器叫做 StringBuilder 其实一般来说这个用的更加频繁，因为 StringBuilder 他是线程不安全的，速度会更快。</p>
]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java面向对象基础(二)</title>
    <url>/2017/08/09/Java%20SE/Java%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%E5%9F%BA%E7%A1%80-%E4%BA%8C/</url>
    <content><![CDATA[<h3 id="1-构造器："><a href="#1-构造器：" class="headerlink" title="1.构造器："></a>1.构造器：</h3><ol>
<li>构造函数在一个类没有写任何的构造函数的时候，系统会生成一个默认的空参数构造函数，这个构造函数的修饰符就是类的修饰符，当我们定义了一个构造函数，默认的构造函数就不存在了而不会出现重载</li>
<li>构造函数是没有返回值的，他没有返回值不是指他就是void，因为void指的是函数的返回值为void类型，他是有返回值的。而没有返回值则是说明根本不用写，这两个有很大区别。<a id="more"></a></li>
<li>没有return语句</li>
<li>函数的名字只能和类名完全一样</li>
<li>构造函数是可以用私用的，但是当所有的构造函数的修饰符都是私有的话对象就无法创建了，其实在看 Java 文档的时候我们也会发现有些类的构造器真的就是私有的，这也就代表了我们没办法使用 new 关键字调用这个类的构造函数，因为他的构造函数对我们来说是透明的，而且既然他自己定义了构造函数那么默认构造函数也不会生成，哪怕他是私有的。再仔细我们会发现他必然会提供一个静态方法让我们获取这个类的实例，类似于单例模式的样子，对外提供一个接口。</li>
<li>对象一建立构造函数就会被调用</li>
<li>构造函数的作用主要就是给对象做初始化</li>
<li>在类中有一种特殊的代码块叫做构造代码块，和一般的代码一样，只是在要执行的代码上添加一个花括号就行。<br>&nbsp;&nbsp;&nbsp;&nbsp;这个构造代码块也是在每次构造对象的时候执行，看起来和构造函数非常类似，但是注意他和构造函数是有很大区别的<br>&nbsp;&nbsp;&nbsp;&nbsp;首先按照执行顺序来说，构造代码块要是中先于构造函数执行。<br>&nbsp;&nbsp;&nbsp;&nbsp;另外对于功能来说构造代码块是针对类的所有对象，而构造函数则是针对类的不同对象。因为构造函数是可以重载的而代码块则是无脑执行。<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PersonDemo</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    &#123;</span><br><span class="line">        System.out.println(<span class="string">"this is construct code block !"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    PersonDemo()&#123;</span><br><span class="line">        <span class="keyword">this</span>.name=<span class="string">"zhangsan"</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    PersonDemo(String name)&#123;</span><br><span class="line">        <span class="keyword">this</span>.name=name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">say</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"name :"</span>+<span class="keyword">this</span>.name);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h3 id="2-Final-关键字："><a href="#2-Final-关键字：" class="headerlink" title="2. Final 关键字："></a>2. Final 关键字：</h3><ol>
<li>final可以修饰类，函数，变量</li>
<li>被final修饰的类无法再次进行继承，也就是为了避免功能被覆盖，保护封装性</li>
<li>被final修饰的方法无法被复写</li>
<li>被final修饰的变量只能赋值一次，就是一个常量</li>
</ol>
<h3 id="3-内部类："><a href="#3-内部类：" class="headerlink" title="3. 内部类："></a>3. 内部类：</h3><ol>
<li><p>内部类就是在一个类中定义了另外一个类</p>
</li>
<li><p>内部类可以直接访问外部类中的成员和方法，包括私有的成员与方法   其原因在于内部类存在一个指向外部类的引用<br> ：就叫做    外部类类名.this</p>
</li>
<li><p>内部类是可以私有的 在私有了以后在别处无法直接创建内部类的对象 只能通过外部类来访问</p>
</li>
<li><p>内部类可以是静态static的，也可用public，default，protected和private修饰。（而外部顶级类即类名和文件名相同的只能使用public和default）。</p>
</li>
<li><p>内部类是一个编译时的概念，一旦编译成功，就会成为完全不同的两类。对于一个名为outer的外部类和其内部定义的名为inner的内部类。编译完成后出现outer.class和outer$inner.class两类。所以内部类的成员变量/方法名可以和外部类的相同。</p>
</li>
<li><p>内部类分为以下几种：  </p>
<ol>
<li><p>成员内部类：因为成员内部类需要先创建了外部类，才能创建它自己的  </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">  <span class="class"><span class="keyword">class</span> <span class="title">Outer</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> x;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="title">Outer</span><span class="params">(<span class="keyword">int</span> x)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.x = x;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">Inner</span></span>&#123;</span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">method</span><span class="params">()</span></span>&#123;</span><br><span class="line">            System.out.println(x);</span><br><span class="line">            System.out.println(Outer.<span class="keyword">this</span>.x);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">getInner</span><span class="params">()</span></span>&#123;</span><br><span class="line">        Inner in=<span class="keyword">new</span> Inner();</span><br><span class="line">        in.method();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>局部内部类：就可以在函数作用域类创建内部类</p>
</li>
<li><p>嵌套内部类<br> 嵌套内部类，就是修饰为static的内部类。声明为static的内部类，不需要内部类对象和外部类对象之间的联系，就是说我们可以直接引用outer.inner，即不需要创建外部类，也不需要创建内部类。</p>
</li>
<li><p>匿名内部类<br>有时候我为了免去给内部类命名，便倾向于使用匿名内部类，因为它没有名字。例如：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">  <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">onClick</span><span class="params">(View v)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">new</span> Thread() &#123;</span><br><span class="line"></span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">run</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">    &#125;.start();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
</li>
</ol>
<h3 id="4-抽象类与抽象方法："><a href="#4-抽象类与抽象方法：" class="headerlink" title="4. 抽象类与抽象方法："></a>4. 抽象类与抽象方法：</h3><ol>
<li>抽象方法就是没有方法体</li>
<li>有抽象方法的类必须声明为抽象类</li>
<li>抽象类没有办法实例化</li>
<li>当子类继承了抽象类，必须把抽象类的所有抽象方法都重写了才能实例化，否则他还是抽象类。</li>
</ol>
<h3 id="5-接口："><a href="#5-接口：" class="headerlink" title="5.接口："></a>5.接口：</h3><ol>
<li>当一个类中的方法都是抽象方法的时候可以把抽象类修改成接口</li>
<li>接口中一般只能定义抽象方法和静态常量，当然如果不写这些修饰符的话java会自动给加上</li>
<li>接口可以被类多实现，也就是说一个类可以实现多个接口  也就是前面所说的多实现</li>
<li>java中的接口是可以多继承的，也就是说如果说java支持多继承的话那么一定给加上限定条件，就是他只在接口中可以</li>
</ol>
<h3 id="6-Static-关键字："><a href="#6-Static-关键字：" class="headerlink" title="6. Static 关键字："></a>6. Static 关键字：</h3><ol>
<li>静态static可以修饰方法，修饰属性<br>当成员被static修饰的时候，这个成员就相当于被类的所有对象共享了，所有对象都可以访问修改</li>
<li>静态成员并不是存放在堆栈中的而是存放在一块特殊的内存叫做『方法区』或者『数据区』，『共享区』等，并且这个地方分的很细，对象的方法也是存放在这个地方</li>
<li>静态成员在调用的时候不仅仅是使用对象来调用还可以使用类名调用，因为静态成员就是属于类的而不是单单属于某个对象</li>
<li>静态成员的生命周期是与类的生命周期相同</li>
<li>静态成员是先于对象加载入内存，也就是静态属性先存在然后才有的对象</li>
</ol>
<h4 id="1-实例变量和类变量的区别："><a href="#1-实例变量和类变量的区别：" class="headerlink" title="1.实例变量和类变量的区别："></a>1.实例变量和类变量的区别：</h4><ol>
<li>存储位置：<br> 类变量存放在方法区中，而实例变量存放在堆内存中</li>
<li>生命周期：<br> 类变量的生命周期是和类一样，但是实例变量的生命周期则是与实例相同</li>
</ol>
<h4 id="2-使用static的注意事项："><a href="#2-使用static的注意事项：" class="headerlink" title="2.使用static的注意事项："></a>2.使用static的注意事项：</h4><ol>
<li>静态方法只能访问静态成员和静态方法。（静态的是独立于对象的，而非静态的则是依赖于对象存在的。另外静态的是先于对象载入内存）</li>
<li>非静态方法即可以访问静态也可以访问非静态成员和方法</li>
<li>静态方法中不可以出现this，super因为静态成员先于对象存在</li>
</ol>
<h4 id="3-那什么时候使用static："><a href="#3-那什么时候使用static：" class="headerlink" title="3.那什么时候使用static："></a>3.那什么时候使用static：</h4><ol>
<li>对于静态方法就是当这个方法中不存在关于对象的成员的时候就把该方法修改成静态的</li>
<li>对于变量则当该变量需要共享的时候就要把属性变成static</li>
</ol>
<h4 id="4-主函数为什么是静态的？"><a href="#4-主函数为什么是静态的？" class="headerlink" title="4.主函数为什么是静态的？"></a>4.主函数为什么是静态的？</h4><p>主函数是一个特殊的函数，作为程序的入口，可被jvm调用<br>public：代表访问权限是最大的<br>static：代表主函数是独立于类存在的，并且该函数是在类的加载的时候被加载<br>void：不用呗jvm返回一个参数<br>String[] args :jvm需要的参数    </p>
<p>注意这个main函数除了args是可以改的以外其他都不可改，不然不可以被jvm识别<br>同时main是可以被重载的，但是只有仅仅满足以上格式的才能被jvm执行，<br>string[] 这个参数其实就是命令行传的参数</p>
<h4 id="4-静态代码块"><a href="#4-静态代码块" class="headerlink" title="4.静态代码块"></a>4.静态代码块</h4><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">//静态代码块</span></span><br><span class="line"> <span class="keyword">static</span> &#123;</span><br><span class="line">        <span class="comment">//static code block</span></span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure>

<ol>
<li>他是随着类的加载而加载，并且仅仅执行一次，因为类进入内存以后再次new对象的时候类不会再次加载</li>
<li>他可以先于main函数执行</li>
<li>它的作用是给类初始化</li>
<li>但是注意当用一个类定义一个变量的时候不加载类<h3 id="7-编译小问题"><a href="#7-编译小问题" class="headerlink" title="7.编译小问题"></a>7.编译小问题</h3>在java编译的时候如果在某个类中调用了其他的类，那么jvm则会在当前目录或者指定目录（classpath）寻找该类的class文件（编译过后的），没找到则找java文件<br>都没找到肯定报错<br>在堆内存中的变量都是有初始值的，而在站内存中的变量在不初始化的情况下是无法参与运算的，对内存中可以</li>
</ol>
<h3 id="8-单例设计模式"><a href="#8-单例设计模式" class="headerlink" title="8.单例设计模式"></a>8.单例设计模式</h3><p>单例设计模式一般来说就有两种方式，一个是懒汉式，一个是饿汉式。其中懒汉式使用的类的延时加载，但是会出现安全问题，需要使用同步代码块包裹返回的对象，并且做双重判断才可以。</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> learn;</span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 单例模式的第一种实现方式，也是最常用的一种叫做饿汉式</span></span><br><span class="line"><span class="comment"> * 他是先初始化对象</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SingleHunger</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> SingleHunger s=<span class="keyword">new</span> SingleHunger(<span class="string">"lwen"</span>,<span class="number">19</span>);</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">SingleHunger</span><span class="params">(String name,<span class="keyword">int</span> age)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name=name;</span><br><span class="line">        <span class="keyword">this</span>.age=age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getAge</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAge</span><span class="params">(<span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SingleHunger <span class="title">getSingle</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> s;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 这个又成为懒汉式也叫做对象的延时加载，他则使用的比较少，因为在进行new新对象的时候可能会有两个线程同时进入临界区导致对相爱那个不唯一</span></span><br><span class="line"><span class="comment"> * 多一需要做同步操作，但是同步的时候又会消耗时间，下面的方式要稍微好一点一般只做了一次同步操作，还有一种解决方案就是直接在class后面添加同步关键字</span></span><br><span class="line"><span class="comment"> * 每次都要判断非常耗时</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">SingleLazy</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> SingleLazy s=<span class="keyword">null</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 使用private限定词是为了防止外部的方法直接调用本类中的构造方法生成新的对象</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> name</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> age</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">SingleLazy</span><span class="params">(String name,<span class="keyword">int</span> age)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.age=age;</span><br><span class="line">        <span class="keyword">this</span>.name=name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> String <span class="title">getName</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setName</span><span class="params">(String name)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.name = name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">getAge</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">setAge</span><span class="params">(<span class="keyword">int</span> age)</span> </span>&#123;</span><br><span class="line">        <span class="keyword">this</span>.age = age;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> SingleLazy <span class="title">getSingle</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="comment">/*</span></span><br><span class="line"><span class="comment">        同步机制加双重判断，防止意外情况发生</span></span><br><span class="line"><span class="comment">         */</span></span><br><span class="line">        <span class="keyword">if</span> (s==<span class="keyword">null</span>)&#123;</span><br><span class="line">            <span class="keyword">synchronized</span> (SingleLazy.class)&#123;</span><br><span class="line">                <span class="keyword">if</span> (s==<span class="keyword">null</span>) &#123;</span><br><span class="line">                    s = <span class="keyword">new</span> SingleLazy(<span class="string">"lwen"</span>, <span class="number">19</span>);</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> s;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">SingleModel</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span></span>&#123;</span><br><span class="line">        SingleHunger s1=SingleHunger.getSingle();</span><br><span class="line">        s1.setAge(<span class="number">20</span>);</span><br><span class="line">        s1.setName(<span class="string">"lwenxu"</span>);</span><br><span class="line">        SingleHunger s2=SingleHunger.getSingle();</span><br><span class="line">        System.out.println(s2.getName());</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h3 id="9-模板设计模式"><a href="#9-模板设计模式" class="headerlink" title="9.模板设计模式"></a>9.模板设计模式</h3><p>模板设计模式,就是在一段确定的方法中调用了一些不确定的方法。那么就把这些不确定的方法定义成抽象的方法，并且把这些不确定的方法的接口给暴露出去</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">codeTime</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">final</span> <span class="keyword">public</span> <span class="keyword">long</span> <span class="title">getTime</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">long</span> start=System.currentTimeMillis();</span><br><span class="line">        runCode();</span><br><span class="line">        <span class="keyword">long</span> end=System.currentTimeMillis();</span><br><span class="line">        <span class="keyword">return</span> end-start;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">abstract</span> <span class="keyword">void</span> <span class="title">runCode</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ACodeTime</span> <span class="keyword">extends</span> <span class="title">codeTime</span></span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">runCode</span><span class="params">()</span></span>&#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;<span class="number">1000</span>;i++)&#123;</span><br><span class="line">            System.out.println(i);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="class"><span class="keyword">class</span> <span class="title">Template</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title">main</span><span class="params">(String[] args)</span> </span>&#123;</span><br><span class="line">        ACodeTime time=<span class="keyword">new</span> ACodeTime();</span><br><span class="line">        System.out.println(<span class="string">"time is :"</span>+time.getTime());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>面向对象</tag>
      </tags>
  </entry>
  <entry>
    <title>ConcurrentHashMap</title>
    <url>/2017/08/09/Java%20SE/ConcurrentHashMap/</url>
    <content><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;</p>
<h1 id="ConcurrentHashMap值得注意的点"><a href="#ConcurrentHashMap值得注意的点" class="headerlink" title="ConcurrentHashMap值得注意的点"></a>ConcurrentHashMap值得注意的点</h1><ol>
<li>不能使用 null 做为键或者是值 他和 HashTable 一样的</li>
<li>​</li>
</ol>
]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>JDK 源码</tag>
      </tags>
  </entry>
  <entry>
    <title>Java面向对象基础</title>
    <url>/2017/08/09/Java%20SE/Java%E9%9D%A2%E5%90%91%E5%AF%B9%E8%B1%A1%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;面向对象一直是一种很流行的思想，他的精髓也就在于他的三大特性：封装，继承和多态。本文就在这三个方面简单的谈一谈Java的面向对象基础。</p>
<h3 id="1-封装："><a href="#1-封装：" class="headerlink" title="1.封装："></a>1.封装：</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;封装顾名思义，就是将一些对象的属性和方法隐藏于本类之中，其他的类无法访问本类的这些被封装的属性和方法。也就是这些方法和属性仅仅是为了本类服务的。<br>除了为本类服务之外封装还可以只暴露自己想给别人提供的服务，而对于一些特殊的底层的服务不希望别人能看到，或者调用。  </p>
<a id="more"></a>
<p>总的来说封装有以下作用或者好处：  </p>
<ol>
<li>为本类提供服务  </li>
<li>对外暴露部分接口</li>
<li>屏蔽类底层的实现细节</li>
<li>防止别人破坏类内部的核心结构，例如直接继承，然后覆盖核心方法，或者装饰者模式，造成违背作则意愿的修改。</li>
</ol>
<h3 id="2-继承："><a href="#2-继承：" class="headerlink" title="2.继承："></a>2.继承：</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;继承这个名词也很形象，就是子类去获得父类的共有的或者保护的属性以及方法，然后再次基础上进行扩充进一步完善类代码和功能。继承一把来说在直观上是为我们节省了代码量，父类中已有的方法和属性我们无需再次书写，但是继承并非为了节省代码量而被提出的，而是因为他展示了类与类之间的联系。一般两个类有间接或直接的关系我们才会去继承，而不是单单为了节省代码量。</p>
<h4 id="1-他的主要功能："><a href="#1-他的主要功能：" class="headerlink" title="1.他的主要功能："></a>1.他的主要功能：</h4><ol>
<li>展现类之间的关系</li>
<li>节省代码量</li>
<li>提高了代码的重用性</li>
<li>因为有了继承才有的多态</li>
</ol>
<h4 id="2-另外还有一些需要注意的地方："><a href="#2-另外还有一些需要注意的地方：" class="headerlink" title="2.另外还有一些需要注意的地方："></a>2.另外还有一些需要注意的地方：</h4><p>&nbsp;&nbsp;&nbsp;&nbsp;java只支持单继承，不支持多继承，多继承会导致功能紊乱<br>&nbsp;&nbsp;&nbsp;&nbsp;例如多个父类中有相同的方法，但是同时继承就会产生不知道继承哪一个方法的问题<br>但是java还是保留了C++的这种多继承的机制，叫做多实现，也就是在接口上支持多继承( 实现 )。<br>&nbsp;&nbsp;&nbsp;&nbsp;但是继承的时候要注意他们是is a的关系也就是每一个子类对象都是一个父类对象。  子类对象 is a  一个父类对象。</p>
<h4 id="3-继承中的-super-和-this"><a href="#3-继承中的-super-和-this" class="headerlink" title="3.继承中的 super 和 this"></a>3.继承中的 super 和 this</h4><h5 id="1-super指针指向的就是父类的对象"><a href="#1-super指针指向的就是父类的对象" class="headerlink" title="1.super指针指向的就是父类的对象"></a>1.super指针指向的就是父类的对象</h5><ol>
<li><p>变量：<br>如果子类中出现了父类中的非私有的同名变量<br>要访问子类中变量直接this或者不写，要访问父类中的变量使用super</p>
</li>
<li><p>函数：<br>覆盖：保留父类的方法的功能定义，重写功能内容；<br>子类的覆盖的函数权限必须大于父类的函数的权限，对于没有权限限定的就是默认权限，他是介于公有与私有之间的一种权限<br>子类中只能使用静态方法覆盖静态方法</p>
</li>
</ol>
<p>注意：</p>
<ol>
<li>重写：他们的参数列表与返回值都必须一样</li>
<li>重载：他们的参数列表一定不一样，返回值无所谓</li>
</ol>
<h5 id="2-this-代表的是所在函数所属对象"><a href="#2-this-代表的是所在函数所属对象" class="headerlink" title="2. this 代表的是所在函数所属对象"></a>2. this 代表的是所在函数所属对象</h5><p>简单来说就是this在哪个函数，这个函数是哪个对象的，this指的就是哪个对象<br>但是注意，this他并不是当前对象而是指向当前对象，他类似于一个指针<br>this的应用：</p>
<ol>
<li>就是当前对象的方法需要引用当前对象的时候我们才使用this  </li>
<li>this的第二个应用就是，在构造函数中使用类似于函数的方式来调用该类中的其他构造函数，注意他也只能出现在构造函数的互相调用之中<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">PersonDemo2</span></span>&#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">int</span> age;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">PersonDemo2</span><span class="params">()</span></span>&#123;</span><br><span class="line">        System.out.println(<span class="string">"no params "</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">private</span> <span class="title">PersonDemo2</span><span class="params">(String name)</span></span>&#123;</span><br><span class="line">        <span class="keyword">this</span>();</span><br><span class="line">        <span class="keyword">this</span>.name=name;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    PersonDemo2(String name,<span class="keyword">int</span> age)&#123;</span><br><span class="line">        <span class="keyword">this</span>(name);</span><br><span class="line">        <span class="keyword">this</span>.age=age;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>this在构造函数中的互相调用不允许一直互相反复调用会造成死循环</p>
<h3 id="3-多态："><a href="#3-多态：" class="headerlink" title="3.多态："></a>3.多态：</h3><p>&nbsp;&nbsp;&nbsp;&nbsp;多态算是面向对象里面比较复杂的一个功能也是一个极其好用的功能。一句话来概括多态的话可以说，” 一个接口，多个实现 “。就是同一种事物表现出的多种形态。<br>&nbsp;&nbsp;&nbsp;&nbsp;多态允许将子类的对象当作父类的对象使用，某父类型的引用指向其子类型的对象,调用的方法是该子类型的方法。这里引用和调用方法的代码编译前就已经决定了,而引用所指向的对象可以在运行期间动态绑定。  </p>
<h4 id="1-他的具体体现就是："><a href="#1-他的具体体现就是：" class="headerlink" title="1.他的具体体现就是："></a>1.他的具体体现就是：</h4><ol>
<li>父类的引用指向子类的对象</li>
<li>父类的引用可以接受子类的对象作为参数</li>
</ol>
<p>&nbsp;&nbsp;&nbsp;&nbsp;这里父类的引用指向了子类的实例，那么这里会发生一个自动的向上类型转换，就是自动的把子类的对象提升为父类的对象，但是这样会造成精度的损失，也就是子类的特性被销毁了子类独有的方法和属性无法再调用了。<br>&nbsp;&nbsp;&nbsp;&nbsp;但是既然可以向上类型转换必然就存在向下类型转换，也就是把父类对象转为子类对象。只不过这种转换仅仅可以出现在父类对象是因为向上类型转换的对象，而不能平白无故的把一个父类对象，转为子类对象。</p>
<h4 id="2-多态的前提："><a href="#2-多态的前提：" class="headerlink" title="2.多态的前提："></a>2.多态的前提：</h4><ol>
<li>必须要有继承或者接口的实现</li>
<li>必须存在方法的覆盖  </li>
</ol>
<h4 id="3-参数的动态绑定："><a href="#3-参数的动态绑定：" class="headerlink" title="3.参数的动态绑定："></a>3.参数的动态绑定：</h4><h6 id="1-在多态中（也就是在父类的引用指向子类的时候）对成员函数来说："><a href="#1-在多态中（也就是在父类的引用指向子类的时候）对成员函数来说：" class="headerlink" title="1.在多态中（也就是在父类的引用指向子类的时候）对成员函数来说："></a>1.在多态中（也就是在父类的引用指向子类的时候）对成员函数来说：</h6><ol>
<li>在编译期间：看引用的成员函数，是否存在如果不存在编译不通过。（编译期间就是看的是类型的声明，声明里有就编译通过没有就失败）</li>
<li>在运行期间：看实际对象的函数，运行实际的对象的方法。（运行使用的是对象运行里面的this指的就是new 哪个类方法就被绑定在哪个对象上）  </li>
</ol>
<p>&nbsp;&nbsp;&nbsp;&nbsp;简单来说就是编译看右边，运行看左边（儿子充当父亲，人家编译的时候看的你是一个父亲那么就看你父亲是不是有这个方法，而运行的时候儿子会做什么就做什么而不是按照父亲的做），总之一句话：“ 编译声明检查，运行动态绑定 ”</p>
<h6 id="2-再多态中，成员变量的特点："><a href="#2-再多态中，成员变量的特点：" class="headerlink" title="2.再多态中，成员变量的特点："></a>2.再多态中，成员变量的特点：</h6><p>&nbsp;&nbsp;&nbsp;&nbsp;变量始终与引用类型看齐（向左看齐）无论是静态变量还是非静态  静态就好说与类绑定自然就看引用  说白了成员变量没有方法那种动态绑定</p>
<h6 id="3-静态方法："><a href="#3-静态方法：" class="headerlink" title="3.静态方法："></a>3.静态方法：</h6><p>&nbsp;&nbsp;&nbsp;&nbsp;始终与引用类型看齐  因为是静态绑定  静态方法与类绑定</p>
]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>面向对象</tag>
      </tags>
  </entry>
  <entry>
    <title>Java基本包装类型</title>
    <url>/2017/08/09/Java%20SE/Java%E5%9F%BA%E6%9C%AC%E5%8C%85%E8%A3%85%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[<p>基本类型的对象包装，也就是将常用的基本数据类型包装成对象</p>
<ul>
<li>byte  Byte</li>
<li>short Short</li>
<li>int  Integer</li>
<li>long  Long</li>
<li>boolean Boolean</li>
<li>float Float</li>
<li>double Double</li>
<li>char Character<a id="more"></a>
最常用的作用就是基本数据类型与字符串的转换<h3 id="1-基本数据类型转字符串："><a href="#1-基本数据类型转字符串：" class="headerlink" title="1. 基本数据类型转字符串："></a>1. 基本数据类型转字符串：</h3>基本数据类型+””<br>基本数据类型类.toString(基本类型的数值)<h3 id="2-字符串转成基本数据类型："><a href="#2-字符串转成基本数据类型：" class="headerlink" title="2.字符串转成基本数据类型："></a>2.字符串转成基本数据类型：</h3>Integer.parseInt()<br>Long.parseLog()<br>对character不用转就是string<h3 id="3-进制转换："><a href="#3-进制转换：" class="headerlink" title="3.进制转换："></a>3.进制转换：</h3>向十进制转：toHexString（）<br>向其他进制转换：parseInt(“”,radax)  radax指的是字符串的进制<h3 id="4-自动拆箱和装箱："><a href="#4-自动拆箱和装箱：" class="headerlink" title="4.自动拆箱和装箱："></a>4.自动拆箱和装箱：</h3></li>
</ul>
<p>1.5版本 的新特性，自动装箱与拆箱以前要这么写：  </p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer x=<span class="keyword">new</span> Integer(<span class="number">1</span>)  </span><br><span class="line">Integer x=<span class="keyword">new</span> Integer(<span class="string">"1"</span>)</span><br></pre></td></tr></table></figure>

<p>现在可以自动装箱：</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer x=<span class="number">5</span>;  <span class="comment">//自动装箱</span></span><br><span class="line">x=x+<span class="number">2</span> <span class="comment">//先拆箱后和装箱  拆箱原理就是x.intValue()</span></span><br></pre></td></tr></table></figure>

<p>1.5后对于在byte范围（-128~+127）内的数 如果一个数已经存在  则不会重新开辟新空间,也就是</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line">Integer x=<span class="number">127</span>,y=<span class="number">127</span>; <span class="comment">//x===y</span></span><br><span class="line">Integer m=<span class="number">128</span>,n=<span class="number">128</span>; <span class="comment">//m!==n</span></span><br></pre></td></tr></table></figure>

<p>还有一点需要注意的就是 new String 和普通的 String = “” 这两个差别很大前者属于一个对象放在了堆内存中，而后者则是直接就在常量池中，不仅仅是字符串，其他都如此。</p>
]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>Java位操作</title>
    <url>/2017/08/09/Java%20SE/Java%E4%BD%8D%E6%93%8D%E4%BD%9C/</url>
    <content><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;无论说是在哪一门计算机语言，位操作运算对于计算机来说肯定是最高效的，因为计算机的底层是按就是二进制，而位操作就是为了节省开销，加快程序的执行速度，以及真正的实现对数的二进制操作。<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;使用位操作，很多代码看起来会很简洁，并且执行速度也会随之提高。在大多数编程语言中都会有 &lt;&lt; 和 &gt;&gt;<br>这两个符号向左的就是左移，反之则是右移这个符号的左边就是需要操作的数，而右边就代表了对这个数移动多少位。</p>
<a id="more"></a>
<h4 id="1-具体位操作"><a href="#1-具体位操作" class="headerlink" title="1.具体位操作"></a>1.具体位操作</h4><ol>
<li>左移( &lt;&lt; )：<br>左移几位就是将这个数再乘以2的几次方，例如说 4 &lt;&lt; 2  其结果就是16，也就是将这个数化作为2进制的数然后向左移动两位，最右边的空位就补0.</li>
<li>右移( &gt;&gt; )：<br>右移就刚好相反，但是也不是完全一样，他是向右移动 n 位，如果说这个数本来就是正的，那么和左移刚好相反就直接除以 2 的 n 次方位，但是如果是负数的话在这个数向右移动 n 位后我们在前面的空位补的是 0 。也就是右移的话是与数相关的问题。右移一个很明显的应用就是在二分法的时候我们就可以直接右移一位，显然速度会提高。</li>
<li>超级右移( &gt;&gt;&gt; )：<br>刚刚说了右移其实还是需要按照情况来的，有时候就不一定是正数，我们就可能补 1 ，但是我们期望结果就是这个数除以 2 的 n 次方，我们就可以使用这个无视正负号的右移操作 &gt;&gt;&gt; ，也就是说他是在任何情况下都是给最高位添加 0 。</li>
<li>与操作( &amp; ):<br>与操作就是把两个数转化为二进制的数，然后再把这两个数，从最低位每位对其，同 1 结果为 1 否则全为 0。</li>
<li>或操作( | ):<br>操作同上只是这个是同 0 为 0，其他都是1。</li>
<li>取反操作( ~ ):<br>二进制的0 变 1 ， 1 变 0。</li>
<li>异或( ^ ):<br>异或有一条很重要的性质，用的非常多就是一个数异或同一个数两次结果还是那个数。</li>
</ol>
<p>上面的与或操作会发现他们有单符号的还有双符号的，不要搞混了单符号的不仅仅就是位操作，他们还是逻辑操作，而双符号的仅仅就是逻辑操作。并且他们有区别例如 &amp; 和 &amp;&amp; 当他们都作为逻辑操作的，前者就是对一个表达式一直判断完毕才会出现他的值，而后者则是判断一半如果知道为假或真他就不再判断了，这也就是我们看到的大多数的 if 判断中是用的双与，而非单与。</p>
<h4 id="2-实际应用："><a href="#2-实际应用：" class="headerlink" title="2.实际应用："></a>2.实际应用：</h4><ol>
<li>第一个就是两个数交换，这个一般有三种方式：<br>第一个：临时变量  <figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> i=<span class="number">3</span>,j=<span class="number">8</span>,temp=<span class="number">0</span>;</span><br><span class="line">temp=i;</span><br><span class="line">i=j;</span><br><span class="line">j=temp;</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>第二个：使用加减法</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> i=<span class="number">3</span>,j=<span class="number">8</span>;</span><br><span class="line">i=j+i;</span><br><span class="line">j=i-j;</span><br><span class="line">i=i-j;</span><br></pre></td></tr></table></figure>

<p>第三个：位操作</p>
<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> i=<span class="number">3</span>,j=<span class="number">8</span>;</span><br><span class="line">i=i^j;</span><br><span class="line">j=i^j;</span><br><span class="line">i=i^j;</span><br></pre></td></tr></table></figure>

<p>这个地方就是用了异或的重要性质</p>
<ol start="2">
<li>第二个就是进制转换了：<br>基本思路就是先把数转为二进制的数，然后如果要 16 进制那么就4位取，8进制3位取，但是又怎么取这个4位或者3位呢，这里与操作就能派上用场取四位我们可以直接与上 15 ，三位就是 7 了，例如：<figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> num=<span class="number">60</span>;</span><br><span class="line"><span class="keyword">int</span> n1=num &amp; <span class="number">15</span>;</span><br><span class="line"><span class="keyword">int</span> tmp=num &gt;&gt;&gt; <span class="number">4</span>;</span><br><span class="line"><span class="keyword">int</span> n2=tmp &amp; <span class="number">15</span>;</span><br><span class="line">System.out.println(<span class="string">"n1: "</span>+n1+<span class="string">" n2 "</span>+n2);</span><br></pre></td></tr></table></figure>

</li>
</ol>
]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>Java</tag>
      </tags>
  </entry>
  <entry>
    <title>proxifier配合ss，实现全局代理</title>
    <url>/2017/07/30/Tools/proxifier%E9%85%8D%E5%90%88ss%EF%BC%8C%E5%AE%9E%E7%8E%B0%E5%85%A8%E5%B1%80%E4%BB%A3%E7%90%86/</url>
    <content><![CDATA[<p>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;proxfixer配合ss的话，基本可以实现全局代理，分应用代理，或者玩外服的游戏（一般的游戏默认不走代理，本软件可以强制应用代理）<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;由于ss使用的是sockets5代理，一般情况下只有浏览器支持，可以实现科学上网。但很多用户希望自己的应用软件，像outlook或游戏之类的软件也实现科学上网。这就需要proxifier的配合。<br>软件可以在官网下载，<a href="https://www.proxifier.com/" target="_blank" rel="noopener">https://www.proxifier.com/</a><br>官网无法下载的也可在如下百度网盘下载，<br>链接: <a href="https://pan.baidu.com/s/1c11bs8C" target="_blank" rel="noopener">https://pan.baidu.com/s/1c11bs8C</a> 密码: 44qj<br>目前仅支持windows和mac os，不支持手机。<br>此软件为收费软件，这里提供两个注册码, 软件分为Standard Edition和Portable Edition版本，注册码不通用，注册用户名任意。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">L6Z8A-XY2J4-BTZ3P-ZZ7DF-A2Q9C（Portable Edition）</span><br><span class="line">5EZ8G-C3WL5-B56YG-SCXM9-6QZAP（Standard Edition）</span><br><span class="line">P427L-9Y552-5433E-8DSR3-58Z68（MAC）</span><br></pre></td></tr></table></figure>

<h3 id="1-打开软件，首先配置代理服务器。"><a href="#1-打开软件，首先配置代理服务器。" class="headerlink" title="1.打开软件，首先配置代理服务器。"></a>1.打开软件，首先配置代理服务器。</h3><p>如下图，添加地址127.0.0.1，以及ss里配置的本地端口，默认为1080，选择socks version 5<br><img src="http://ojrw3x8j2.bkt.clouddn.com/17-7-30/93346575.jpg" alt><br>在配置完成之后需要测试一下，看配置的是否有问题。<br><img src="http://ojrw3x8j2.bkt.clouddn.com/17-7-30/33843553.jpg" alt></p>
<h3 id="2-接下来就要添加规则，来确定哪些软件是走代理的，哪些不用"><a href="#2-接下来就要添加规则，来确定哪些软件是走代理的，哪些不用" class="headerlink" title="2.接下来就要添加规则，来确定哪些软件是走代理的，哪些不用"></a>2.接下来就要添加规则，来确定哪些软件是走代理的，哪些不用</h3><p>按如图所示的添加，这里有个default规则，如果default旁边的action里边选择的时proxy socks5…则本机所有软件都会走代理。一般default会选direct，然后把你需要走代理的软件选成proxy socks5…<br><img src="http://ojrw3x8j2.bkt.clouddn.com/17-7-30/18134133.jpg" alt></p>
]]></content>
      <categories>
        <category>-工具类</category>
      </categories>
      <tags>
        <tag>shadowsocks</tag>
      </tags>
  </entry>
  <entry>
    <title>Redis数据类型</title>
    <url>/2017/07/28/DataBase/Redis%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B/</url>
    <content><![CDATA[]]></content>
  </entry>
  <entry>
    <title>Redis基本入门</title>
    <url>/2017/07/28/DataBase/Redis%E5%9F%BA%E6%9C%AC%E5%85%A5%E9%97%A8/</url>
    <content><![CDATA[<h2 id="1-Redis简介"><a href="#1-Redis简介" class="headerlink" title="1.Redis简介"></a>1.Redis简介</h2><p>&nbsp;&nbsp;&nbsp;&nbsp;Redis 是一种基于内存亦可持久化的日志型，Key-Value 数据库。可持久在于他的部分数据是存放在内存上，而当数据库重启以后他的数据不会立刻丢失，而是会存放在磁盘上，通过日志去加载磁盘上的数据文件。所以说它不仅仅是一种内存型键值数据库。<br>&nbsp;&nbsp;&nbsp;&nbsp;另外 Redis 所支持的数据结构也非常丰富，不仅仅就是一种简单的 NoSql ，而是被称作为数据结构服务器。他所支持的数据结构有：字符串（string），哈希（Map），列表（List），集合（Set），有序集合（ZSet），BitMap，HyperLogLog（超小内存唯一值计数），GEO（地理信息位置）。以上所说的均是值所支持的数据结构，键必然为字符串。</p>
<a id="more"></a>
<h2 id="2-Redis安装"><a href="#2-Redis安装" class="headerlink" title="2.Redis安装"></a>2.Redis安装</h2><p>&nbsp;&nbsp;&nbsp;&nbsp; Redis 最初是由标准 C 写的，但是后来社区非常的活跃，所以现在什么版本的 Redis 都是有的，所以说这个数据库是可以跑在任何平台的，这里主要就说 Windows 下的安装， Linux 与 Mac 下安装很简单，只需要一个命令就能搞定。<br>&nbsp;&nbsp;&nbsp;&nbsp;下载好Redis的二进制文件后，使用 cmd 切到 Redis 安装目录下，然后运行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-server.exe redis.conf</span><br></pre></td></tr></table></figure>

<p>就能跑起一个 Redis 的服务端程序，然后不要关闭这个程序。另开一个 cmd 依然切换到安装目录执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">redis-cli.exe -h 127.0.0.1 -p 6379</span><br></pre></td></tr></table></figure>

<p>这样就开启了一个客户端。</p>
<h2 id="3-Redis的配置"><a href="#3-Redis的配置" class="headerlink" title="3.Redis的配置"></a>3.Redis的配置</h2><p>我们有三种启动 redis 的方式：</p>
<ul>
<li>最简启动，无需配置文件 <code>redis-server</code></li>
<li>加载配置文件 <code>redis-server configpath</code></li>
<li>动态参数的配置</li>
</ul>
<p>客户端的连接方式：</p>
<p><code>redis-cli -h ip -p port</code></p>
<p>&nbsp;&nbsp;&nbsp;&nbsp;Redis 可以使用客户端进行配置也可以使用配置文件，Redis 的配置文件位于 Redis 安装目录下，文件名为 redis.conf。<br>使用客户端基本格式如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">CONFIG SET CONFIG_SETTING_NAME NEW_CONFIG_VALUE</span><br></pre></td></tr></table></figure>

<p>这样的好处在于配置起来方便，但是缺点显然就是服务器重启，配置将不再生效，也就是说现在的配置放在了内存中，然后需要重启还生效的东西必须放在永久存储介质上，一般来说就是磁盘。这个规则不仅仅适用于 Redis ，对于 Linux 以及其他数据库也是如此。<br>&nbsp;&nbsp;&nbsp;&nbsp;接下来主要说一下配置文件里面的重要选项：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1. Redis默认不是以守护进程的方式运行，可以通过该配置项修改，使用yes启用守护进程</span><br><span class="line"></span><br><span class="line">  daemonize no</span><br><span class="line"></span><br><span class="line">2. 当Redis以守护进程方式运行时，Redis默认会把pid写入/var/run/redis.pid文件，可以通过pidfile指定</span><br><span class="line"></span><br><span class="line">  pidfile /var/run/redis.pid</span><br><span class="line"></span><br><span class="line">3. 指定Redis监听端口，默认端口为6379，作者在自己的一篇博文中解释了为什么选用6379作为默认端口，因为6379在手机按键上MERZ对应的号码，而MERZ取自意大利歌女Alessia Merz的名字</span><br><span class="line"></span><br><span class="line">  port 6379</span><br><span class="line"></span><br><span class="line">4. 绑定的主机地址</span><br><span class="line"></span><br><span class="line">  <span class="built_in">bind</span> 127.0.0.1</span><br><span class="line"></span><br><span class="line">5.当 客户端闲置多长时间后关闭连接，如果指定为0，表示关闭该功能</span><br><span class="line"></span><br><span class="line">  timeout 300</span><br><span class="line"></span><br><span class="line">6. 指定日志记录级别，Redis总共支持四个级别：debug、verbose、notice、warning，默认为verbose</span><br><span class="line"></span><br><span class="line">  loglevel verbose</span><br><span class="line"></span><br><span class="line">7. 日志记录方式，默认为标准输出，如果配置Redis为守护进程方式运行，而这里又配置为日志记录方式为标准输出，则日志将会发送给/dev/null</span><br><span class="line"></span><br><span class="line">  logfile stdout/filename</span><br><span class="line"></span><br><span class="line">8. 设置数据库的数量，默认数据库为0，可以使用SELECT &lt;dbid&gt;命令在连接上指定数据库id</span><br><span class="line"></span><br><span class="line">  databases 16</span><br><span class="line"></span><br><span class="line">9. 指定在多长时间内，有多少次更新操作，就将数据同步到数据文件，可以多个条件配合</span><br><span class="line"></span><br><span class="line">  save &lt;seconds&gt; &lt;changes&gt;</span><br><span class="line"></span><br><span class="line">  Redis默认配置文件中提供了三个条件：</span><br><span class="line"></span><br><span class="line">  save 900 1</span><br><span class="line"></span><br><span class="line">  save 300 10</span><br><span class="line"></span><br><span class="line">  save 60 10000</span><br><span class="line"></span><br><span class="line">  分别表示900秒（15分钟）内有1个更改，300秒（5分钟）内有10个更改以及60秒内有10000个更改。</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">10. 指定存储至本地数据库时是否压缩数据，默认为yes，Redis采用LZF压缩，如果为了节省CPU时间，可以关闭该选项，但会导致数据库文件变的巨大</span><br><span class="line"></span><br><span class="line">  rdbcompression yes</span><br><span class="line"></span><br><span class="line">11. 指定本地数据库文件名，默认值为dump.rdb</span><br><span class="line"></span><br><span class="line">  dbfilename dump.rdb</span><br><span class="line"></span><br><span class="line">12. 指定本地数据库,以及存放目录</span><br><span class="line"></span><br><span class="line">  dir ./</span><br><span class="line"></span><br><span class="line">13. 设置当本机为slav服务时，设置master服务的IP地址及端口，在Redis启动时，它会自动从master进行数据同步</span><br><span class="line"></span><br><span class="line">  slaveof &lt;masterip&gt; &lt;masterport&gt;</span><br><span class="line"></span><br><span class="line">14. 当master服务设置了密码保护时，slav服务连接master的密码</span><br><span class="line"></span><br><span class="line">  masterauth &lt;master-password&gt;</span><br><span class="line"></span><br><span class="line">15. 设置Redis连接密码，如果配置了连接密码，客户端在连接Redis时需要通过AUTH &lt;password&gt;命令提供密码，默认关闭</span><br><span class="line"></span><br><span class="line">  requirepass foobared</span><br><span class="line"></span><br><span class="line">16. 设置同一时间最大客户端连接数，默认无限制，Redis可以同时打开的客户端连接数为Redis进程可以打开的最大文件描述符数，如果设置 maxclients 0，表示不作限制。当客户端连接数到达限制时，Redis会关闭新的连接并向客户端返回max number of clients reached错误信息</span><br><span class="line"></span><br><span class="line">  maxclients 128</span><br><span class="line"></span><br><span class="line">17. 指定Redis最大内存限制，Redis在启动时会把数据加载到内存中，达到最大内存后，Redis会先尝试清除已到期或即将到期的Key，当此方法处理 后，仍然到达最大内存设置，将无法再进行写入操作，但仍然可以进行读取操作。Redis新的vm机制，会把Key存放内存，Value会存放在swap区</span><br><span class="line"></span><br><span class="line">  maxmemory &lt;bytes&gt;</span><br><span class="line"></span><br><span class="line">18. 指定是否在每次更新操作后进行日志记录，Redis在默认情况下是异步的把数据写入磁盘，如果不开启，可能会在断电时导致一段时间内的数据丢失。因为 redis本身同步数据文件是按上面save条件来同步的，所以有的数据会在一段时间内只存在于内存中。默认为no</span><br><span class="line"></span><br><span class="line">  appendonly no</span><br><span class="line"></span><br><span class="line">19. 指定更新日志文件名，默认为appendonly.aof</span><br><span class="line"></span><br><span class="line">   appendfilename appendonly.aof</span><br><span class="line"></span><br><span class="line">20. 指定更新日志条件，共有3个可选值：     no：表示等操作系统进行数据缓存同步到磁盘（快）     always：表示每次更新操作后手动调用fsync()将数据写到磁盘（慢，安全）     everysec：表示每秒同步一次（折衷，默认值）</span><br><span class="line"></span><br><span class="line">  appendfsync everysec</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line">21. 指定是否启用虚拟内存机制，默认值为no，简单的介绍一下，VM机制将数据分页存放，由Redis将访问量较少的页即冷数据swap到磁盘上，访问多的页面由磁盘自动换出到内存中（在后面的文章我会仔细分析Redis的VM机制）</span><br><span class="line"></span><br><span class="line">   vm-enabled no</span><br><span class="line"></span><br><span class="line">22. 虚拟内存文件路径，默认值为/tmp/redis.swap，不可多个Redis实例共享</span><br><span class="line"></span><br><span class="line">   vm-swap-file /tmp/redis.swap</span><br><span class="line"></span><br><span class="line">23. 将所有大于vm-max-memory的数据存入虚拟内存,无论vm-max-memory设置多小,所有索引数据都是内存存储的(Redis的索引数据 就是keys),也就是说,当vm-max-memory设置为0的时候,其实是所有value都存在于磁盘。默认值为0</span><br><span class="line"></span><br><span class="line">   vm-max-memory 0</span><br><span class="line"></span><br><span class="line">24. Redis swap文件分成了很多的page，一个对象可以保存在多个page上面，但一个page上不能被多个对象共享，vm-page-size是要根据存储的 数据大小来设定的，作者建议如果存储很多小对象，page大小最好设置为32或者64bytes；如果存储很大大对象，则可以使用更大的page，如果不 确定，就使用默认值</span><br><span class="line"></span><br><span class="line">   vm-page-size 32</span><br><span class="line"></span><br><span class="line">25. 设置swap文件中的page数量，由于页表（一种表示页面空闲或使用的bitmap）是在放在内存中的，，在磁盘上每8个pages将消耗1byte的内存。</span><br><span class="line"></span><br><span class="line">   vm-pages 134217728</span><br><span class="line"></span><br><span class="line">26. 设置访问swap文件的线程数,最好不要超过机器的核数,如果设置为0,那么所有对swap文件的操作都是串行的，可能会造成比较长时间的延迟。默认值为4</span><br><span class="line"></span><br><span class="line">   vm-max-threads 4</span><br><span class="line"></span><br><span class="line">27. 设置在向客户端应答时，是否把较小的包合并为一个包发送，默认为开启</span><br><span class="line"></span><br><span class="line">  glueoutputbuf yes</span><br><span class="line"></span><br><span class="line">28. 指定在超过一定的数量或者最大的元素超过某一临界值时，采用一种特殊的哈希算法</span><br><span class="line"></span><br><span class="line">  <span class="built_in">hash</span>-max-zipmap-entries 64</span><br><span class="line"></span><br><span class="line">  <span class="built_in">hash</span>-max-zipmap-value 512</span><br><span class="line"></span><br><span class="line">29. 指定是否激活重置哈希，默认为开启（后面在介绍Redis的哈希算法时具体介绍）</span><br><span class="line"></span><br><span class="line">  activerehashing yes</span><br><span class="line"></span><br><span class="line">30. 指定包含其它的配置文件，可以在同一主机上多个Redis实例之间使用同一份配置文件，而同时各个实例又拥有自己的特定配置文件</span><br><span class="line"></span><br><span class="line">  include /path/to/local.conf</span><br></pre></td></tr></table></figure>

<h2 id="4-通用命令"><a href="#4-通用命令" class="headerlink" title="4. 通用命令"></a>4. 通用命令</h2><ol>
<li>keys 所有的键 O(n)</li>
<li>dbsize 复杂度为O(1)</li>
<li>exists key</li>
<li>del key</li>
<li>expire key second</li>
<li>type key</li>
<li>ttl/persist key 存活时间，让他一直存活</li>
</ol>
<h3 id="时间复杂度"><a href="#时间复杂度" class="headerlink" title="时间复杂度"></a>时间复杂度</h3><p><img src="https://user-images.githubusercontent.com/22151420/39404881-00ca9fa0-4bce-11e8-90e1-85596195fa3a.png" alt="image"></p>
<h2 id="5-字符串"><a href="#5-字符串" class="headerlink" title="5. 字符串"></a>5. 字符串</h2><p>最大是512M，主要用于缓存、计数器、分布式锁。</p>
<h3 id="1-API"><a href="#1-API" class="headerlink" title="1.API"></a>1.API</h3><ul>
<li>get/set/del</li>
<li>Incr/decr/incrby/decrby</li>
<li>Set/setnx/set .. xx </li>
<li>Mget/mset … 批量操作原子的</li>
<li>getset key newvalue</li>
<li>Append key value</li>
<li>Strlen key</li>
</ul>
<h2 id="6-Hash"><a href="#6-Hash" class="headerlink" title="6. Hash"></a>6. Hash</h2><h3 id="1-API-1"><a href="#1-API-1" class="headerlink" title="1.API"></a>1.API</h3><ul>
<li>Hget/hset/hdel</li>
<li>Hexits/hlen field 数</li>
<li>hmget/hmset</li>
<li>同上</li>
</ul>
<h2 id="7-List"><a href="#7-List" class="headerlink" title="7. List"></a>7. List</h2><ul>
<li><p>lpush/lpop/rpush/rpop</p>
</li>
<li><p>Linsert key before/after </p>
</li>
<li><p>lrem key count value  根据count值，从列表中删除所有value相等的项</p>
<p>(1) count &gt;0，从左到右，删除最多count个value相等的项<br>(2) count&lt;0,从右到左，删除最多Math.abs(count)个value相等的项<br>(3) count=0,删除所有value相等的项<br>lrem listkey 0 a</p>
</li>
<li><p>Itrim key start end   按照索引范围修剪列表,删除不在范围之内的</p>
</li>
<li><p>lrange key start end (包含end)  获取列表指定索引|范围所有item</p>
</li>
<li><p>lindex key index 获取列表指定位置的值</p>
</li>
<li><p>len key 长度</p>
</li>
<li><p>lset key index val</p>
</li>
<li><p>blpop/brpop key timeout 阻塞直到部位空返回</p>
</li>
</ul>
<h2 id="8-set"><a href="#8-set" class="headerlink" title="8. set"></a>8. set</h2><ul>
<li>sadd/sdel/srem</li>
<li>scrad/sismember/srandmember/spop/smembers</li>
</ul>
<ul>
<li>sdiff user:1:follow user:2:follow = music his #差集</li>
<li>sinter user:1:follow user:2:follow = it sports #交集</li>
<li>sunion user:1:follow user:2:follow = it music his sports news ent #并集</li>
<li>sdifflsinterlsuion + store destkey..#将差集、交集、并集结果保存在destkey中</li>
</ul>
<ul>
<li>SADD= Tagging</li>
<li>SPOP/SRANDMEMBER = Random item</li>
<li>SADD + SINTER = Social Graph</li>
</ul>
<h2 id="9-zset"><a href="#9-zset" class="headerlink" title="9. zset"></a>9. zset</h2><ul>
<li>zadd key score element(可以是多对)添加score和element</li>
<li>zrem key element</li>
<li>zsrore key ele</li>
<li>zincrby key increscore element增加或减少元素的分数</li>
<li>Zrank 排名</li>
<li>zrange key start end</li>
<li>zrangebyscore </li>
<li>zrembyscore</li>
<li>zrevarank 反向的排序</li>
</ul>
<h2 id="10-发布订阅"><a href="#10-发布订阅" class="headerlink" title="10. 发布订阅"></a>10. 发布订阅</h2><h2 id="11-RDB"><a href="#11-RDB" class="headerlink" title="11.RDB"></a>11.RDB</h2><ol>
<li>RDB是Redis内存到硬盘的快照，用于持久化。</li>
</ol>
<ol start="2">
<li>save通常会阻塞Redis。</li>
</ol>
<ol start="3">
<li>bgsave不会阻塞Redis,但是会fork新进程。</li>
</ol>
<ol start="4">
<li>save自动配置满足任一就会被执行。</li>
<li>有些触发机制不容忽视</li>
</ol>
]]></content>
      <categories>
        <category>-数据库</category>
      </categories>
      <tags>
        <tag>Redis NoSql 数据库</tag>
      </tags>
  </entry>
  <entry>
    <title>Java异常</title>
    <url>/2017/07/10/Java%20SE/Java%E5%BC%82%E5%B8%B8/</url>
    <content><![CDATA[<p> 从今天开始就要好好的写日志了，每天一篇，把工作中学习中遇到的问题，学习的新知识都放在这个博客里面，有空了就过来回顾一下，记录一下成长。</p>
<a id="more"></a>
]]></content>
      <categories>
        <category>-Java</category>
      </categories>
      <tags>
        <tag>Java学习</tag>
      </tags>
  </entry>
  <entry>
    <title>杂记</title>
    <url>/2017/06/05/Life/%E6%9D%82%E8%AE%B0/</url>
    <content><![CDATA[<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-6-6/21776345.jpg" alt><br>好久好久没有写过东西了，最近说事很多其实也不是很多，反正还是一堆杂事，然后加上贪玩，恶补了几部动漫。</p>
<a id="more"></a>
<p>今天脑子一热把手机上的娱乐性app全给删了，感觉最近还是太过浮躁。学习没有学习，玩没有认真玩，技术也没有认真做。<br>总之感觉状态一直很差吧。或者说自己突然变得很怠惰。<br>目前的计划算是基本确定了，最近把大数据平台的需求分析搞定，暂时不做这方面的技术吧，毕竟马上又要期末考试了。虽然感觉不像去年学的那么烂，但是也没感觉自己学的有多好，所以这段时间的复习还是至关重要的！尽量多把心思放在考试复习上面，嘉木还有大数据平台先搁置一下。<br>之后学长他们旅游回来了，我想大家再聚一次，毕竟以后的机会真的很少很少了啊，还有从那以后技术必须要以自己能看到的速度成长才行，不然自己说的什么都是空话而已！<br>暑假，还是和去年差不多。只是换了一个地方，我想这段时间一定一定要把java和php以及算法和数据结构中的至少两个搞得通透，时间真的不多了，而且这样的整体的时间更是越来越少了。<br>这段时间还是尽量尽量不要受任何人的影响，这样只会让自己想的更多更浮躁。目前的目标只有两个搞定就好了，不多想！</p>
]]></content>
      <categories>
        <category>-杂记</category>
      </categories>
      <tags>
        <tag>杂记</tag>
      </tags>
  </entry>
  <entry>
    <title>Mac配置sshd</title>
    <url>/2017/05/13/Mac/Mac%E9%85%8D%E7%BD%AEsshd/</url>
    <content><![CDATA[<p>修改sshd_config文件把password验证方式打开<br>修改这个文件要用sudo</p>
<blockquote>
<p>PasswordAuthentication yes</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo ssh-keygen -t dsa -f /etc/ssh/ssh_host_dsa_key</span><br><span class="line">sudo ssh-keygen -t rsa -f /etc/ssh/ssh_host_rsa_key</span><br><span class="line">sudo  ssh-keygen -t ecdsa -f /etc/ssh/ssh_host_ecdsa_key</span><br><span class="line">sudo  ssh-keygen -t ed25519 -f /etc/ssh/ssh_host_ed25519_key</span><br><span class="line">sudo /usr/sbin/sshd</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Mac</category>
      </categories>
  </entry>
  <entry>
    <title>数据库Exists关键字举例</title>
    <url>/2017/05/12/DataBase/%E6%95%B0%E6%8D%AE%E5%BA%93Exists%E5%85%B3%E9%94%AE%E5%AD%97%E4%B8%BE%E4%BE%8B/</url>
    <content><![CDATA[<h2 id="一-问题描述："><a href="#一-问题描述：" class="headerlink" title="一.问题描述："></a>一.问题描述：</h2><blockquote>
<p>查询所有未选择03号课程的学生的姓名<br>规定使用存在量词</p>
</blockquote>
<a id="more"></a>
<h4 id="student表："><a href="#student表：" class="headerlink" title="student表："></a>student表：</h4><p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-5-11/11695243-file_1494553101102_14292.png" alt></p>
<h4 id="grade表："><a href="#grade表：" class="headerlink" title="grade表："></a>grade表：</h4><p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-5-11/67277042-file_1494553101231_3769.png" alt></p>
<h2 id="二-思路："><a href="#二-思路：" class="headerlink" title="二.思路："></a>二.思路：</h2><p>既然是存在量词那么也就是Exists和Not Exists两个存在两次来做判断条件。<br>那么就可以想：</p>
<h4 id="1-选出所有学生然后除去那些选了03号课程的同学"><a href="#1-选出所有学生然后除去那些选了03号课程的同学" class="headerlink" title="1.选出所有学生然后除去那些选了03号课程的同学"></a>1.选出所有学生然后除去那些选了03号课程的同学</h4><h4 id="2-直接选出那些没有选择03号课程的同学"><a href="#2-直接选出那些没有选择03号课程的同学" class="headerlink" title="2.直接选出那些没有选择03号课程的同学"></a>2.直接选出那些没有选择03号课程的同学</h4><h2 id="三-开始写Sql："><a href="#三-开始写Sql：" class="headerlink" title="三.开始写Sql："></a>三.开始写Sql：</h2><h3 id="1-尝试"><a href="#1-尝试" class="headerlink" title="1.尝试"></a>1.尝试</h3><p>首先按照第一种思路就是先连接student和grade（或者不连接）然后除掉grade中有03课程记录的学生，那么我们会写出以下Sql:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SELECT DISTINCT sname from Student,Grade WHERE NOT exists(</span><br><span class="line">  SELECT * FROM Grade WHERE cno=<span class="string">'03'</span></span><br><span class="line">);</span><br><span class="line">或者</span><br><span class="line">SELECT DISTINCT sname from Student WHERE NOT exists(</span><br><span class="line">  SELECT * FROM Grade WHERE cno=<span class="string">'03'</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>执行以后我们发现我们得到的结果是空</p>
<p>接下来我们看看student表中到底有多少人：<br>执行以下语句：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SELECT DISTINCT sno,sname FROM Student GROUP BY sno;</span><br></pre></td></tr></table></figure>

<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-5-11/62530586-file_1494547912119_5cb4.png" alt></p>
<p>这里我们发现这条语句根本没有进行筛选，这是因为Exists不知道使用什么条件去筛选数据，前面是一个结果集后面为另一个结果集数据库不清楚按照哪个字段来判断前面的某条记录是否存在与后面的集合中。</p>
<h3 id="2-修正错误："><a href="#2-修正错误：" class="headerlink" title="2.修正错误："></a>2.修正错误：</h3><p>这里在两个表之间显然只有sno字段是相关的，所以在最后添加判断条件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">Grade.sno=Student.sno</span><br><span class="line">也就是</span><br><span class="line">SELECT DISTINCT sname from Student WHERE NOT exists(</span><br><span class="line">  SELECT * FROM Grade,Student WHERE cno=<span class="string">'03'</span> AND Grade.sno=Student.sno</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>此时出现了10条记录经过比对发现是正确的。</p>
<h3 id="3-接下来用sql语句验证结论："><a href="#3-接下来用sql语句验证结论：" class="headerlink" title="3.接下来用sql语句验证结论："></a>3.接下来用sql语句验证结论：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SELECT DISTINCT sno,sname FROM Student GROUP BY sno;</span><br><span class="line">SELECT sno FROM Grade WHERE cno=<span class="string">'03'</span>;</span><br></pre></td></tr></table></figure>

<p>第一条语句就是找到student表中的所有人，第二条语句就是看看哪些人选择了03号课程，结果如下。<br><img src="http://ojrw3x8j2.bkt.clouddn.com/17-5-11/83704707-file_1494548125454_171c0.png" alt></p>
<p>发现结论正确，但是这两个语句貌似可以拼接成一条新的sql来完成这道题：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SELECT DISTINCT sname</span><br><span class="line">FROM Student  <span class="built_in">where</span> sno NOT IN (</span><br><span class="line">  SELECT sno FROM Grade WHERE cno=<span class="string">'03'</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>结果同上。</p>
<h3 id="4-反向思维："><a href="#4-反向思维：" class="headerlink" title="4.反向思维："></a>4.反向思维：</h3><p>既然not Exists是可以的那么是不是可以把那条语句修改成exists语句然后把where里面的条件再非一下，执行以下语句：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SELECT sname FROM Student WHERE  exists(</span><br><span class="line">    SELECT DISTINCT Student.sno FROM Grade WHERE Grade.sno=Student.sno</span><br><span class="line">    AND cno!=<span class="string">'03'</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-5-11/7425028-file_1494548893077_17bc2.png" alt></p>
<p>但是结论是只有七条数据，显然差距比较大。<br>也就是子查询是有问题的，查表以后发现在student表中是有十二个同学，但是grade表中选课的同学总数并不是十二个，也就是说有很多人根本没有选课。并且另外一个问题就是有的人选了不止一门课可能某个同学既选了03又选了别的课程，而在使用exits判断的时候只要有满足条件的就返回，自然是没等判断到选了03就能返回真。所以这个方法行不通。</p>
<h3 id="5-修改："><a href="#5-修改：" class="headerlink" title="5.修改："></a>5.修改：</h3><p>上面出现问题，说明使用exists思路没问题只是子查询错误,试试运用course表看能不能写出其他语句：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SELECT sname FROM Student WHERE  exists(</span><br><span class="line">  SELECT cno FROM Course WHERE sno NOT IN (</span><br><span class="line">    SELECT sno FROM Grade WHERE cno=<span class="string">'03'</span></span><br><span class="line">  )</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>把exits换成IN</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SELECT DISTINCT sno</span><br><span class="line">FROM Student  <span class="built_in">where</span> sno  IN (</span><br><span class="line">  SELECT sno FROM Course WHERE sno NOT IN (</span><br><span class="line">    SELECT sno FROM Grade WHERE cno=<span class="string">'03'</span></span><br><span class="line">  )</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>看以下的句子：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SELECT sname FROM Student WHERE  exists(</span><br><span class="line">  SELECT dno FROM Department WHERE sno NOT IN (</span><br><span class="line">    SELECT sno FROM Grade WHERE cno=<span class="string">'03'</span></span><br><span class="line">  )</span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>仔细看就会发现其实上面的句子其实就是：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">SELECT DISTINCT sname</span><br><span class="line">FROM Student  <span class="built_in">where</span> sno NOT IN (</span><br><span class="line">  SELECT sno FROM Grade WHERE cno=<span class="string">'03'</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure>

<p>只是中间添加了一些毫无逻辑的语句，但是对结果没有任何影响。</p>
]]></content>
      <categories>
        <category>-数据库</category>
      </categories>
  </entry>
  <entry>
    <title>Mac配置PHP7环境</title>
    <url>/2017/05/07/PHP/Mac%E9%85%8D%E7%BD%AEPHP7%E7%8E%AF%E5%A2%83/</url>
    <content><![CDATA[<blockquote>
<p>在Mac下面本来就安装有Apache2和PHP5.5.3<br>这里为了配置更新的环境就稍微折腾了一下，主要配置的就是MySQL和PHP，Apache就使用自带的就行。</p>
</blockquote>
<h2 id="配置MySQL："><a href="#配置MySQL：" class="headerlink" title="配置MySQL："></a>配置MySQL：</h2><p>MySQL在MySQL的官网上直接下载dmg包之后安转就行了。<br>这里Mac版本的MySQL下载地址是</p>
<blockquote>
<p><a href="https://dev.mysql.com/downloads/file/?id=461365" target="_blank" rel="noopener">https://dev.mysql.com/downloads/file/?id=461365</a></p>
</blockquote>
<p>下载之后在安装完成以后注意他会提示有一个默认的密码，一定要给记下来，一会改密码用的上。<br>再安装完成了，打开偏好设置，最下面有一个MySQL的控制面板，在里面开启MySQL<br>然后在终端中修改MySQL的密码</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ln -s /usr/<span class="built_in">local</span>/mysql/bin/mysql /usr/<span class="built_in">local</span>/bin</span><br><span class="line">mysql -uroot -p     <span class="comment">#输入刚才提示的密码，登录以后开始修改密码</span></span><br><span class="line"><span class="built_in">set</span> password <span class="keyword">for</span> root@localhost=password(<span class="string">'password'</span>);</span><br><span class="line"><span class="built_in">exit</span></span><br></pre></td></tr></table></figure>

<h2 id="配置PHP7环境"><a href="#配置PHP7环境" class="headerlink" title="配置PHP7环境"></a>配置PHP7环境</h2><p>添加PHP7的源</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew tap homebrew/dupes  </span><br><span class="line">brew tap homebrew/versions  </span><br><span class="line">brew tap homebrew/homebrew-php  </span><br><span class="line">brew install php70  </span><br><span class="line">brew unlink php55</span><br><span class="line">brew link php70 --with-httpd24</span><br></pre></td></tr></table></figure>

<p>需要等待比较久的时间，因为是直接去官方下载。</p>
<h2 id="配置Apache"><a href="#配置Apache" class="headerlink" title="配置Apache"></a>配置Apache</h2><p>直接vim Apache的配置文件，记得使用sudo，否则没权限写入。<br>当然不用sudo也可以，在修改完成以后执行 :w !sudo tee %<br>然后就可以使用管理员权限写入了。<br>然后打开Apache的配置文件主要得目的就是修改DocumentRoot和PHP模块文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo vim /etc/apache2/httpd.conf</span><br></pre></td></tr></table></figure>

<p>找到DocumentRoot改成你想要的目录，但是这个目录必须要有权限，一般给个777绝对ok<br>并且找到下面的地方，把PHP5的模块改成下面的内容：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment">#Enable PHP 7 module  </span></span><br><span class="line">LoadModule php7_module /usr/<span class="built_in">local</span>/opt/php70/libexec/apache2/libphp7.so</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Mac</category>
      </categories>
  </entry>
  <entry>
    <title>关于PHP的回调函数</title>
    <url>/2017/05/07/PHP/%E5%85%B3%E4%BA%8EPHP%E7%9A%84%E5%9B%9E%E8%B0%83%E5%87%BD%E6%95%B0/</url>
    <content><![CDATA[<p><img src="http://upload-images.jianshu.io/upload_images/1568656-0ad89e4d6ebe34e3.jpg?imageMogr2/auto-orient/strip%7CimageView2/2/w/1240" alt></p>
<ol>
<li>普通函数的定义及调用与js相似,这个定义方式无需返回值,哪怕是有返回值在声明的时候也无需添加</li>
<li>再后来的PHP版本中是添加了一向很有用的功能就是可在函数定义之前进行调用</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">echo add(1,2);</span><br><span class="line">echo &quot;&lt;/br&gt;&quot;;</span><br><span class="line">function add($a,$b)&#123;</span><br><span class="line">    return $a+$b;</span><br><span class="line">&#125;</span><br><span class="line">function sub($a,$b)&#123;</span><br><span class="line">    return $a-$b;</span><br><span class="line">&#125;</span><br><span class="line">echo add(23,12);</span><br><span class="line">echo &quot;&lt;/br&gt;&quot;;</span><br><span class="line">echo sub(23,22);</span><br><span class="line">echo &quot;&lt;/br&gt;&quot;;</span><br></pre></td></tr></table></figure>

<ol>
<li>下面是一个非常有用的功能就是变量函数,顾名思义就是将函数作为一个变量。</li>
<li>其优点在于用同一个变量可以调用不同的函数,非常类似于函数的多态调用。</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$var=&quot;add&quot;;</span><br><span class="line">echo $var(4,2);</span><br><span class="line">echo &quot;&lt;/br&gt;&quot;;</span><br><span class="line">$var=&quot;sub&quot;;</span><br><span class="line">echo $var(4,2);</span><br><span class="line">echo &quot;&lt;/br&gt;&quot;;</span><br></pre></td></tr></table></figure>

<ul>
<li><p>回调函数就是在给一个函数传入一个简单的参数无法解决问题的时候给他传入一个过程,从而达到目的</p>
</li>
<li><p>在函数调用时给他传入一个函数作为参数就是函数回调。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$arr=array(2,3,5,4,1,6,7,9,8);</span><br><span class="line">var_dump($arr);</span><br><span class="line">echo &quot;&lt;/br&gt;&quot;;</span><br></pre></td></tr></table></figure>

</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">//这里是自定义回调函数,返回-1是指将两个元素交换,0和1是不发生改变。</span><br><span class="line">function myrule($a,$b)&#123;</span><br><span class="line">    if ($a&gt;$b)&#123;</span><br><span class="line">        return 1;</span><br><span class="line">    &#125;</span><br><span class="line">    elseif ($a&lt;$b)&#123;</span><br><span class="line">        return -1;</span><br><span class="line">    &#125;</span><br><span class="line">    else&#123;</span><br><span class="line">        return 0;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">//usort就是系统函数,但是他的第二个参数是回调函数,这个函数参数就是排序规则</span><br><span class="line">usort($arr,&quot;myrule&quot;);</span><br><span class="line">var_dump($arr);</span><br><span class="line">echo &quot;&lt;/br&gt;&quot;;</span><br></pre></td></tr></table></figure>

<ul>
<li>自己写回调函数,使用变量函数以及回调完成一个简单的过滤条件,如果需多个条件同时满足给一个&amp;&amp;关系即可。</li>
<li>其中使用的变量函数可以使用系统中的一个叫做call_user_func_array()的函数进行调用,他有两个参数分别是回调函数名称,以及参数数组</li>
<li>call_user_func_array(“demo”,array(1,3));其优点在于array中的参数的数量可以比原函数的少,既有默认缺省参数的时候。</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">//rule1除去数组中是三的倍数的数</span><br><span class="line">function rule1($a)&#123;</span><br><span class="line">    if ($a%3==0)&#123;</span><br><span class="line">        return true;</span><br><span class="line">    &#125;else&#123;</span><br><span class="line">        return false;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">//rule2是除去数组中的回文数（从左边读与从右边读是一样的）</span><br><span class="line">function rule2($a)&#123;</span><br><span class="line">    if ($a==strrev($a))&#123;</span><br><span class="line">        return true;</span><br><span class="line">    &#125;else&#123;</span><br><span class="line">        return false;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">function demo($n,$var)&#123;</span><br><span class="line">    for ($i=0;$i&lt;$n;$i++)&#123;</span><br><span class="line">        if (call_user_func_array($var,array(23)))</span><br><span class="line">        //if ($var($i))</span><br><span class="line">        &#123;</span><br><span class="line">            continue;</span><br><span class="line">        &#125;else&#123;</span><br><span class="line">            echo $i.&quot;&lt;br&gt;&quot;;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">$var=&quot;rule1&quot;;</span><br><span class="line">demo(100,$var);</span><br><span class="line">echo &quot;&lt;/br&gt;&quot;;</span><br><span class="line">echo &quot;&lt;hr&gt;&quot;;</span><br><span class="line">$var=&quot;rule2&quot;;</span><br><span class="line">demo(200,$var);</span><br><span class="line">echo &quot;&lt;/br&gt;&quot;;</span><br></pre></td></tr></table></figure>

<ol>
<li><p>注意在调用对象里面的方法时我们需要传入一个匿名对象,后面指定函数名</p>
</li>
<li><p>而在调用类中的静态方法时只需指定类名即可。</p>
<blockquote>
<p>以上两种情况都不能完全使用变量函数只能用系统的回调call_user_func_array(),只是变量函数来传参而不调用</p>
</blockquote>
</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">class A&#123;</span><br><span class="line">    function one()&#123;</span><br><span class="line">    &#125;</span><br><span class="line">    static function two()&#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">demo(200,array(new A,&quot;one&quot;));</span><br><span class="line">demo(200,array(&quot;A&quot;,&quot;two&quot;));</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-PHP</category>
      </categories>
  </entry>
  <entry>
    <title>VirtualEnv</title>
    <url>/2017/05/06/Python/pip%20%E7%9A%84%E9%98%BF%E9%87%8C%E6%BA%90/</url>
    <content><![CDATA[<h3 id="修改配置文件"><a href="#修改配置文件" class="headerlink" title="修改配置文件"></a>修改配置文件</h3><p>1）检查pip.conf文件是否存在</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&gt;&gt; cd ~</span><br><span class="line">&gt;&gt; mkdir .pip</span><br><span class="line">&gt;&gt; ls ~/.pip</span><br></pre></td></tr></table></figure>

<p>2）直接编辑pip.conf</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo vi ~/.pip/pip.conf</span><br></pre></td></tr></table></figure>

<p>修改成如下的格式</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">[global]</span><br><span class="line">index-url = https://mirrors.aliyun.com/pypi/simple/</span><br><span class="line">[install]</span><br><span class="line">trusted-host = mirrors.aliyun.com</span><br></pre></td></tr></table></figure>

<h3 id="临时作用的方式"><a href="#临时作用的方式" class="headerlink" title="临时作用的方式"></a>临时作用的方式</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pip3 install 包名 -i https://mirrors.aliyun.com/pypi/simple/</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>-Mac</category>
      </categories>
  </entry>
  <entry>
    <title>Mac安装包版本的Python</title>
    <url>/2017/05/06/Python/Mac%E5%AE%89%E8%A3%85%E5%8C%85%E7%89%88%E6%9C%AC%E7%9A%84Python/</url>
    <content><![CDATA[<ol>
<li>安装配置Python版本管理器pyenv</li>
</ol>
<p>brew install pyenv</p>
<ol start="2">
<li>查看已经安装了哪些版本的Python</li>
</ol>
<p>pyenv versions<br>其中版本号前面有*号的就是当前生效的版本,查看当前生效的版本也可以用<br>pyenv version</p>
<ol start="3">
<li>安装指定版本的Python</li>
</ol>
<p>pyenv install 3.7-dev</p>
<blockquote>
<p>安装完成后必须rehash<br>pyenv rehash</p>
</blockquote>
<ol start="4">
<li>切换和使用指定的版本Python版本有3种方法</li>
</ol>
<p>系统全局用系统默认的Python比较好，不建议直接对其操作<br>pyenv global system<br>用local进行指定版本切换，一般开发环境使用。<br>pyenv local 2.7.10<br>对当前用户的临时设定Python版本，退出后失效<br>pyenv shell 3.7-dev<br>取消某版本切换<br>pyenv local 3.5.0 –unset<br>优先级关系：shell——local——global</p>
<blockquote>
<p>另外最重要的一点，当执行了pyenv shell/local/global 命令后，一定要执行 source ~/.zshrc ，不然切换版本就不会生效。</p>
</blockquote>
<p>最后执行</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">➜  ~ pyenv versions</span><br><span class="line">  system</span><br><span class="line">* 3.7-dev</span><br><span class="line">➜  ~</span><br><span class="line">➜  ~ python -V</span><br><span class="line">Python 3.7-dev</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Mac</category>
      </categories>
  </entry>
  <entry>
    <title>两个 python 的虚拟管理工具</title>
    <url>/2017/05/06/Python/virtualenv%E9%85%8D%E7%BD%AE/</url>
    <content><![CDATA[<h1 id="两个-python-的虚拟管理工具"><a href="#两个-python-的虚拟管理工具" class="headerlink" title="两个 python 的虚拟管理工具"></a>两个 python 的虚拟管理工具</h1><hr>

<h2 id="virtualenv"><a href="#virtualenv" class="headerlink" title="virtualenv"></a>virtualenv</h2><h4 id="安装-virtualenv"><a href="#安装-virtualenv" class="headerlink" title="安装 virtualenv"></a>安装 virtualenv</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get install python-virtualenv</span><br></pre></td></tr></table></figure>

<h4 id="创建-env"><a href="#创建-env" class="headerlink" title="创建 env"></a>创建 env</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">virtualenv ENV</span><br></pre></td></tr></table></figure>

<h4 id="启动-env"><a href="#启动-env" class="headerlink" title="启动 env"></a>启动 env</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cd ENV</span><br><span class="line">source ./bin/activate</span><br></pre></td></tr></table></figure>

<h4 id="退出虚拟环境"><a href="#退出虚拟环境" class="headerlink" title="退出虚拟环境"></a>退出虚拟环境</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">deactivate</span><br></pre></td></tr></table></figure>

<h2 id="virtualenv-的加强版-virtualenvwrapper"><a href="#virtualenv-的加强版-virtualenvwrapper" class="headerlink" title="virtualenv 的加强版 virtualenvwrapper"></a>virtualenv 的加强版 virtualenvwrapper</h2><h4 id="优点："><a href="#优点：" class="headerlink" title="优点："></a>优点：</h4><ol>
<li>将所有虚拟环境整合在一个目录下</li>
<li>管理（新增，删除，复制）虚拟环境</li>
<li>切换虚拟环境</li>
</ol>
<h4 id="安装："><a href="#安装：" class="headerlink" title="安装："></a>安装：</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo easy_install virtualenvwrapper</span><br></pre></td></tr></table></figure>

<p>此时还不能使用virtualenvwrapper，默认virtualenvwrapper安装在/usr/local/bin下面，实际上你需要运行virtualenvwrapper.sh文件才行，先别急，打开这个文件看看,里面有安装步骤，我们照着操作把环境设置好。</p>
<h4 id="创建目录用来存放虚拟环境"><a href="#创建目录用来存放虚拟环境" class="headerlink" title="创建目录用来存放虚拟环境"></a>创建目录用来存放虚拟环境</h4><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkdir $HOME/.virtualenvs</span><br></pre></td></tr></table></figure>

<h4 id="配置脚本文件"><a href="#配置脚本文件" class="headerlink" title="配置脚本文件"></a>配置脚本文件</h4><p>在 bashrc 文件中进行如下操作</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">    export WORKON_HOME=$HOME/.virtualenvs</span><br><span class="line">    source /usr/local/bin/virtualenvwrapper.sh</span><br><span class="line">    source ~/.bashrc</span><br><span class="line">​`</span><br></pre></td></tr></table></figure>

<p>此时virtualenvwrapper就可以使用了。</p>
<h4 id="基本操作"><a href="#基本操作" class="headerlink" title="基本操作"></a>基本操作</h4><ol>
<li><p>列出基本环境</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">workon/lsvirtualenv</span><br></pre></td></tr></table></figure>
</li>
<li><p>新建虚拟环境</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">mkvirtualenv [虚拟环境名称]</span><br></pre></td></tr></table></figure>
</li>
<li><p>启动/切换虚拟环</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">workon [虚拟环境名称]</span><br></pre></td></tr></table></figure>
</li>
<li><p>删除虚拟环境</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">rmvirtualenv [虚拟环境名称]</span><br></pre></td></tr></table></figure>
</li>
<li><p>离开虚拟环境</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">deactivate</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>```</p>
]]></content>
      <categories>
        <category>-Mac</category>
      </categories>
  </entry>
  <entry>
    <title>打造酷炫终端</title>
    <url>/2017/05/06/Mac/%E6%89%93%E9%80%A0%E9%85%B7%E7%82%AB%E7%BB%88%E7%AB%AF/</url>
    <content><![CDATA[<p><img src="http://ojrw3x8j2.bkt.clouddn.com/17-5-5/57489163-file_1494081194816_ed64.png" alt></p>
<h2 id="1-安装iTerm2"><a href="#1-安装iTerm2" class="headerlink" title="1.安装iTerm2"></a>1.安装iTerm2</h2><p>iTerm2官方下载地址 <a href="http://www.iterm2.com/downloads.html" target="_blank" rel="noopener">http://www.iterm2.com/downloads.html</a></p>
<h2 id="2-安装Oh-My-Bash"><a href="#2-安装Oh-My-Bash" class="headerlink" title="2.安装Oh My Bash"></a>2.安装Oh My Bash</h2><h4 id="1-使用brew安装zsh："><a href="#1-使用brew安装zsh：" class="headerlink" title="1.使用brew安装zsh："></a>1.使用brew安装zsh：</h4><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">brew install zsh</span><br></pre></td></tr></table></figure>

<a id="more"></a>
<h4 id="2-通过echo-SHELL命令可以查看我们当前正在使用的shell；"><a href="#2-通过echo-SHELL命令可以查看我们当前正在使用的shell；" class="headerlink" title="2.通过echo $SHELL命令可以查看我们当前正在使用的shell；"></a>2.通过echo $SHELL命令可以查看我们当前正在使用的shell；</h4><blockquote>
<p>Mac系统中默认的shell为bash shell<br>/bin/bash</p>
</blockquote>
<h4 id="3-如果当前的shell不是zsh，我们可以通过chsh-s-bin-zsh命令可以将shell切换为shell之zsh，终端重启之后即可生效。"><a href="#3-如果当前的shell不是zsh，我们可以通过chsh-s-bin-zsh命令可以将shell切换为shell之zsh，终端重启之后即可生效。" class="headerlink" title="3.如果当前的shell不是zsh，我们可以通过chsh -s /bin/zsh命令可以将shell切换为shell之zsh，终端重启之后即可生效。"></a>3.如果当前的shell不是zsh，我们可以通过chsh -s /bin/zsh命令可以将shell切换为shell之zsh，终端重启之后即可生效。</h4><h4 id="4-将shell切换为zsh之后，我们就可以安装Oh-My-ZSH了"><a href="#4-将shell切换为zsh之后，我们就可以安装Oh-My-ZSH了" class="headerlink" title="4.将shell切换为zsh之后，我们就可以安装Oh My ZSH了"></a>4.将shell切换为zsh之后，我们就可以安装Oh My ZSH了</h4><p>官方推荐的安装方法为：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sh -c <span class="string">"<span class="variable">$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)</span>"</span></span><br></pre></td></tr></table></figure>

<h2 id="3-配置agnoster主题"><a href="#3-配置agnoster主题" class="headerlink" title="3.配置agnoster主题"></a>3.配置agnoster主题</h2><h4 id="1-Oh-My-Zsh提供的所有主题在线预览："><a href="#1-Oh-My-Zsh提供的所有主题在线预览：" class="headerlink" title="1.Oh My Zsh提供的所有主题在线预览："></a>1.Oh My Zsh提供的所有主题在线预览：</h4><p><a href="https://github.com/robbyrussell/oh-my-zsh/wiki/Themes" target="_blank" rel="noopener">https://github.com/robbyrussell/oh-my-zsh/wiki/Themes</a></p>
<p>安装成功之后，我们可以通过vi ~/.zshrc，设置ZSH_THEME=”agnoster”对主题进行修改。</p>
<p>注意，agnoster主题能否设置成功，还依赖于以下东西：</p>
<h4 id="2-Solarized-Dark配色方案"><a href="#2-Solarized-Dark配色方案" class="headerlink" title="2.Solarized Dark配色方案"></a>2.Solarized Dark配色方案</h4><p>下载完成之后解压，在iTerm2的Preferences——Profiles——colors——Load Presets——Solarized Dark即可设置终端配色</p>
<h4 id="3-特殊字体安装"><a href="#3-特殊字体安装" class="headerlink" title="3.特殊字体安装"></a>3.特殊字体安装</h4><p>将字体克隆到本地，并使用install.sh安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/powerline/fonts</span><br><span class="line">install.sh</span><br></pre></td></tr></table></figure>

<p>安装完成以后，在iTerm2的Preferences——Profiles——Text中同时将Regular Font和Non—ASCII Font设置为Meslo LG M DZ Regular for Powerline即可</p>
]]></content>
      <categories>
        <category>-Mac</category>
      </categories>
  </entry>
  <entry>
    <title>DSDT GUIDE</title>
    <url>/2017/05/05/Mac/dsdt%20guide/</url>
    <content><![CDATA[<p>Overview</p>
<p>In order to make many OS X features work well on a laptop, you will always need a properly patched DSDT (and SSDTs). The purpose of this guide is to provide a foundation for proper patching of your OEM DSDT/SSDTs.</p>
<p>Advanced users may wish to implement hotpatching via Clover. See guide here: <a href="http://www.tonymacx86.com/threads/guide-using-clover-to-hotpatch-acpi.200137/" target="_blank" rel="noopener">http://www.tonymacx86.com/threads/guide-using-clover-to-hotpatch-acpi.200137/</a></p>
<p>Although you may be tempted to use a DSDT from another computer, it will almost always end in failure. You simply cannot be certain it is valid to use ACPI files from another computer. Even minor differences in hardware configuration (BIOS version, amount of memory installed, BIOS options selected, and other hardware differences such as which WiFi card is installed) can make for differences that cause instability and weird bugs if you use foreign ACPI files. Differences such as BIOS version, amount of memory installed, BIOS options selected, and other hardware differences such as which WiFi card is installed, can make various OperationRegion addresses different, which makes a patched DSDT for one system incompatible with another. It is also not uncommon for the same laptop model to be produced in different runs with different motherboards, and potentially incompatible ACPI files.</p>
<a id="more"></a>
<p>The process of patching involves several steps:</p>
<ul>
<li>extracting native files</li>
<li>disassembling the native files</li>
<li>analyzing and filtering the native files</li>
<li>patching</li>
<li>saving (compiling) and installing</li>
</ul>
<p>Extracting native ACPI files</p>
<p>All BIOS implementations provide ACPI files to the OS. So, on any OS, you can extract them for patching later. Extraction can therefore be done on Linux, OS X, Windows, or even in the Clover bootloader. Native files extracted are generally identical, although because of the software used to extract, they may be named differently.</p>
<p>This guide will focus on three methods of extraction: Using patchmatic in OS X, using F4 with Clover, or using Linux.</p>
<p>Extracting with ‘patchmatic’:</p>
<p>If you’ve already installed OS X, provided you’re not currently booting with any patched ACPI files, you can extract your native DSDT/SSDT with patchmatic. Download the patchmatic binary here: <a href="https://github.com/RehabMan/OS-X-MaciASL-patchmatic" target="_blank" rel="noopener">https://github.com/RehabMan/OS-X-MaciASL-patchmatic</a> (be sure to read the README as the download location is linked from it). For ease of use in Terminal, you should copy the binary (inside the ZIP) to /usr/bin.</p>
<p>After installing patchmatic, you can invoke it in Terminal, as such:</p>
<p>Code (Text):</p>
<p>cd ~/Desktop<br>mkdir extract<br>cd extract<br>patchmatic -extract</p>
<p>The patchmatic binary will extract all loaded ACPI files and place them in the current directory. If you’re using any options in the bootloader that affect the injected DSDT/SSDT, you will not get native ACPI files, so be sure you’re not using those options. For example, if you’re using (Chameleon) DropSSDT=Yes, or (Clover) DropOem=true, the native SSDTs are being dropped before OS X can load them, so you’ll be missing them in the patchmatic output. Same goes for any Clover DSDT “Fixes” – those fixes are patching the native DSDT, and to do your own DSDT patching, you don’t want that. Options such as GeneratePStates/GenerateCStates=Yes, or with Clover /ACPI/SSDT/Generate/CStates /ACPI/SSDT/Generate/PStates will inject extra SSDTs which can cause complications with disassembly.</p>
<p>It is for all those reasons, it is often easier to extract via Linux or using Clover F4.</p>
<p>Note: Using ‘patchmatic -extract’ to confirm you’re patching DSDT/SSDTs as you expect is a useful diagnostic tool.</p>
<p>Extracting with Clover F4 (recommended):</p>
<p>Extracting with Clover F4 is recommended, due to ease of extraction, and due to ease of comparison between ACPI/origin and ACPI/patched (for troubleshooting).</p>
<p>At the main Clover bootloader screen, you can press F4 and Clover will dump the native ACPI files to EFI/Clover/ACPI/origin. You can then access them after you boot OS X to disassemble them and patch. Note that some BIOS implementations reverse the function of Fn+F4 with F4, so when in doubt, press both Fn+F4 and F4. There is no feedback during or after the dump, just a slight delay as the files are written. The delay is more noticeable if they are being written to USB, as would be the case when booting from a Clover USB.</p>
<p>Sometimes, Clover F4 will write duplicate SSDTs. These duplicates will cause problems during disassembly. If you run into issues (duplicate definitions) during disassembly, you will need to analyse all SSDTs to eliminate the files which are duplicate. It is easy to see which are duplicates by looking at the file sizes. Files with equal size are likely duplicates.</p>
<p>You can see file sizes in bytes of all SSDTs in Terminal:<br>Code (Text):</p>
<p>ls -l SSDT*.aml</p>
<p>Extracting with Linux:</p>
<p>In Linux, the native ACPI files are available directly from the file system. You can find them at /sys/firmware/acpi/tables and /sys/firmware/acpi/tables/dynamic. It is possible to copy the entire set with a single command in Terminal.</p>
<p>It is not necessary to install Linux. Simply run it from USB: <a href="http://www.ubuntu.com/download/desktop/create-a-usb-stick-on-windows" target="_blank" rel="noopener">http://www.ubuntu.com/download/desktop/create-a-usb-stick-on-windows</a>.</p>
<p>In Linux Terminal:<br>Code (Text):</p>
<h1 id="substitute-DEST-with-the-mountpoint-of-a-FAT32-formatted-USB-stick"><a href="#substitute-DEST-with-the-mountpoint-of-a-FAT32-formatted-USB-stick" class="headerlink" title="substitute DEST with the mountpoint of a FAT32 formatted USB stick"></a>substitute DEST with the mountpoint of a FAT32 formatted USB stick</h1><p>sudo cp -R /sys/firmware/acpi/tables DEST</p>
<p>You should copy the files to a FAT32 formatted USB. Using FAT32 avoids permissions issues as FAT32 has no file permissions. The value of DEST for an auto-mounted USB will depend on the version of Linux you’re using and how you booted it. You can see the mount-point by typing ‘mount’ in Terminal, or hover your mouse over the volume name in the Linux file explorer.</p>
<p>Preparing tools for disassembly</p>
<p>To properly disassemble your extracted files, you need the iasl compiler, which is run from Terminal.</p>
<p>You will need a recent build of iasl to disassemble them properly. There is an appropriate version available here: <a href="https://bitbucket.org/RehabMan/acpica/downloads/" target="_blank" rel="noopener">https://bitbucket.org/RehabMan/acpica/downloads/</a>. It is a good idea to copy the iasl binary to your path (eg. /usr/bin), so it is easily accessed from Terminal.</p>
<p>For example, if you downloaded it to ~/Downloads/iasl.zip, you can extract and copy it in Terminal:<br>Code (Text):</p>
<p>cd ~/Downloads<br>unzip iasl.zip<br>sudo cp iasl /usr/bin</p>
<p>Building the latest iasl from github</p>
<p>You can also build the latest version of my iasl from my github. Assuming you have Xcode installed:<br>Code (Text):</p>
<p>mkdir ~/Projects &amp;&amp; cd ~/Projects<br>git clone <a href="https://github.com/RehabMan/Intel-iasl.git" target="_blank" rel="noopener">https://github.com/RehabMan/Intel-iasl.git</a> iasl.git<br>cd iasl.git</p>
<p>The latest version always tends to have experimental and not well tested code. For example, the new code Intel added to disassemble If/Else blocks as Switch/Case is full of bugs (I have not had time to report them to Intel).</p>
<p>I recommend using the version prior to that change:<br>Code (Text):</p>
<p>git checkout b9c6c2b</p>
<p>Note: The b9c6c2b version is the one currently available from bitbucket.</p>
<p>Then build it:<br>Code (Text):</p>
<p>make</p>
<p>At that point, you can install it with:<br>Code (Text):</p>
<p>sudo make install</p>
<p>And assuming you have MaciASL.app installed to /Applications, you can use the new version (that you just built and installed to /usr/bin) in MaciASL as well:<br>Code (Text):</p>
<p>sudo cp /usr/bin/iasl /Applications/MaciASL.app/Contents/MacOS/iasl61</p>
<p>The newer version of iasl will eventually be available at the bitbucket link, but for those who want to be on the “bleeding edge”…</p>
<p>Disassembling ACPI files</p>
<p>Although the extracted native files can be opened directly in MaciASL, it is not recommended. Opening an AML file directly in MaciASL will cause MaciASL to disassemble the file (with iasl) standalone, and if the AML has complex references to other AMLs, it will not disassemble it correctly. You’ll be left with many hard to fix errors.</p>
<p>As a result, it is better to disassemble all files as a group using iasl in Terminal. To prepare, place all DSDT and SSDT files in a single directory (DO NOT copy ACPI files that don’t begin with DSDT or SSDT), and change the names such that they have an .aml extension.</p>
<p>Then disassemble in OS X Terminal:<br>Code (Text):</p>
<p>cd “to directory where you placed all SSDT/DSDT”<br>iasl -da -dl DSDT.aml SSDT*.aml</p>
<p>Note: Do NOT attempt to disassemble other ACPI files with the -da option. It will not work.</p>
<p>Note: Also read the section below regarding refs.txt. Using refs.txt takes little effort, but can eliminate many common errors.</p>
<p>From this point onward, you will work exclusively with the resulting *.dsl files using MaciASL. Of course, to use them you must save as “ACPI Machine Language Binary” with an extension .aml and place them where they will be loaded by the bootloader. But keep your patched .dsl files in case you need to apply more patches in the future.</p>
<p>Let me state it quite simply (because this comes up a lot): If you are opening an AML file directly in MaciASL and clicking Compile, you are doing it WRONG. Let that soak into the gray matter between your ears for a minute.</p>
<p>Note: The new tools with ACPI 6.1 are much more robust when dealing with AML files that have been compiled with the new version of iasl. ACPI 6.1 adds a feature to the compiler where opcodes for External references are added to the AML binary. ACPI interpreters ignore this data, but the data is useful to the disassembler (also only ACPI 6.1 version of iasl) to create a better disassembly from a standalone AML. As a result, you might find that AML files that have been recompiled with the latest tools may open directly more reliably. Of course, existing OEM ACPI DSDT and SSDTs are not using the new tools at this point, so you still must disassemble initially with all DSDT/SSDT with option -da, as described in this guide.</p>
<p>Note regarding Snow Leopard ACPI implementation: Unfortunately, the 10.6.8 ACPI is old enough that it chokes on AMLs with the external opcode. If you are planning to use your ACPI files with Snow Leopard use the undocumented “-oe” switch to iasl when you compile your AML files. This option is not set when you compile (Save As) from MaciASL, so you will need to compile your files in Terminal. The “-oe” option disables generation of the external opcode in the output AML files.</p>
<p>Disassembly with refs.txt</p>
<p>Sometimes there are additional unresolved externals (symbols not defined in any file). The iasl disassembler will attempt to guess the number of arguments, but often it guesses poorly. You can correct it, by providing the External declarations in a text file. Some common unresolved symbols are SGPO, ECRD, ECWT, and MMTB.</p>
<p>The following refs.txt content has some common (and not so common) missing symbols (as reported by users in this thread) that the disassembler tends to be confused by.</p>
<p>First create refs.txt in the directory where your DSDT/SSDT files are:<br>Code (Text):</p>
<p>External(MDBG, MethodObj, 1)<br>External(_GPE.MMTB, MethodObj, 0)<br>External(_SB.PCI0.LPCB.H_EC.ECWT, MethodObj, 2)<br>External(_SB.PCI0.LPCB.H_EC.ECRD, MethodObj, 1)<br>External(_SB.PCI0.LPCB.H_EC.ECMD, MethodObj, 1)<br>External(_SB.PCI0.PEG0.PEGP.SGPO, MethodObj, 2)<br>External(_SB.PCI0.GFX0.DD02._BCM, MethodObj, 1)<br>External(_SB.PCI0.SAT0.SDSM, MethodObj, 4)<br>External(_GPE.VHOV, MethodObj, 3)<br>External(_SB.PCI0.XHC.RHUB.TPLD, MethodObj, 2)</p>
<p>Note: A handy way to create refs.txt is to use pbpaste in Terminal. Copy the text above to the clipboard (I’m assuming you know how to do that), then:<br>Code (Text):</p>
<p>pbpaste&gt;refs.txt</p>
<p>That will create refs.txt in your current working directory.</p>
<p>Then use it during disassembly:<br>Code (Text):</p>
<p>iasl -da -dl -fe refs.txt DSDT.aml SSDT*.aml</p>
<p>Older versions of the iasl disassembler will place these External declarations before all the other External declarations. This, also, is a poor choice. Most of the time, you’ll need to move them so they follow the other External declarations instead of preceding them. It will be obvious as you’ll get errors from the External declarations that follow those inserted from refs.txt. In the current iasl supporting ACPI 6.1, this bug has been fixed.</p>
<p>Filtering ACPI files</p>
<p>For older computers (Sandy Bridge and prior), the SSDTs related to CPU can cause problems. If this is the case (you already had to use the alternate DropTables, or DropOem=true, or DropSSDT=Yes), then you should not include such SSDTs in ACPI/patched.</p>
<p>I like to include all SSDTs, in their original order, patched appropriately, unless they are known to cause a problem. Keep in mind that an SSDT that doesn’t need any patches, does not need to be recompiled. You can simply use the original AML file unmodified.</p>
<p>Note: The ‘x’ SSDTs from a Clover dump and likewise the SSDTs in the dynamic subdirectory from a Linux dump are loaded dynamically and are never included in ACPI/patched (they are SSDTs loaded on-demand with ACPI Load from SystemMemory).</p>
<p>After you successfully disassemble your files, look at each one in an attempt to determine the SSDT’s purpose. If it is CPU related and is known to cause a problem, set it aside, and don’t include it in your final set for injection via the bootloader. For the most part, SSDTs with objects declared in Scope _PR.CPUx are likely CPU related.</p>
<p>Here are some typical SSDTs you’ll find:</p>
<ul>
<li>CPU related: already discussed above. Include unless known to cause a problem.</li>
<li>SATA: Can be excluded or included… your choice.</li>
<li>PTID: Usually this file is useless for OS X and contains many errors. In rare cases, it may provide clues for how to read fan speed, temperature, or other system status.</li>
<li>IAOE: If this SSDT is present, it is usually accessed from DSDT in _PTS and _WAK. Sleep may not work properly without it.</li>
<li>GFX0: Usually the SSDT with ‘Device GFX0’ will be for integrated graphics. This is the SSDT you patch for backlight control. With older laptops, GFX0 is usually defined in DSDT. With newer Haswell laptops, it is usually defined in SSDT (although it can also be in DSDT).</li>
<li>PEGP: PEGP usually corresponds to the discrete card in a switched dual-GPU configuration. There can be more than one, and usually you will need to include all of them as a group in order to accomplish any meaningful patching. These SSDTs should be patched if you wish to disable the discrete card when running OS X.</li>
</ul>
<p>It is a good idea to keep notes on what the purpose of each SSDT is and which ones you plan to “drop”, which ones you plan to keep unmodified, and those that you plan to patch.</p>
<p>Fixing Errors</p>
<p>Even by disassembling all at once (iasl with -da option), the native files can still have errors. The disassembled files have errors due to changes in iasl over time, imperfections in iasl itself, and differences in the compilation environment between our laptops and the OEM. A common cause of errors (my theory), for example, is that some of the methods referenced are actually inside Windows (MMTB and MDBG for example). There is also clearly cases where bugs are in the code or code was written uninitentionally (sometimes hard to tell the difference).</p>
<p>So.. after determining which files you need, you must patch them so they compile without errors. There are many common patches for such errors in my laptop patch repository for MaciASL.</p>
<p>MaciASL: <a href="https://github.com/RehabMan/OS-X-MaciASL-patchmatic" target="_blank" rel="noopener">https://github.com/RehabMan/OS-X-MaciASL-patchmatic</a><br>Laptop Patches: <a href="https://github.com/RehabMan/Laptop-DSDT-Patch" target="_blank" rel="noopener">https://github.com/RehabMan/Laptop-DSDT-Patch</a></p>
<p>Note: I do not test my patches with DSDT Editor. It has too many bugs and a very old version of iasl. Please do not ask me about it.</p>
<p>Be certain to always read the README, in order to download from the correct location and in order to setup MaciASL correctly. The patches for syntax/error problems begin with “[syn]” in the name. Common examples for older DSDTs are “Fix _PLD Buffer/Package Error”, “Fix TNOT Error”, and “Fix FPED Parse Error”. In order to determine which patch you need, you can look at the error message coming from the iasl compiler and the code at the line the error was detected. You can also attempt to apply a patch just to see if it makes changes as shown in the Preview window in MaciASL. If you’re not familiar with each type of error, it can take some experimentation and trial/error.</p>
<p>For some errors, you can simply remove the line of code causing the error. But, it depends on whether the line is necessary for proper operation of the code or not. For example, errors caused by ‘External’ declarations can generally be removed to fix the error. If you wish, you can create automated patches of your own to remove these lines.</p>
<p>It helps to have some experience with the ACPI spec and some programming experience.</p>
<p>Your goal is to get each .dsl file to compile without errors (warnings/remarks/optimizations are ok). Once you have files that compile without errors, you can move on to patching them to fix issues you may have with your OS X installation.</p>
<p>Common Patches</p>
<p>Generally, a DSDT patch should only be applied after finding a need for that specific fix. But there are several patches that are commonly needed and that have only a small chance of causing a problem. They are in my laptop repo and are listed here:<br>“Fix _WAK Arg0 v2”<br>“HPET Fix”<br>“SMBUS Fix”<br>“IRQ Fix”<br>“RTC Fix”<br>“OS Check Fix”<br>“Fix Mutex with non-zero SyncLevel”<br>“Fix PNOT/PPNT” (use only if you’re dropping CPU related SSDTs)<br>“Add IMEI” (do not use if your DSDT or SSDTs already have IMEI/HECI/MEI device)</p>
<p>Note: The OS Check Fix patch you use has nothing to do with the version of Windows the laptop came with, nor with the version of Windows you’re currently using.</p>
<p>Note: Do not use the “Fix PNOT/PPNT” patch if you’re including all OEM SSDTs. It is intended only for the case you omit the OEM CPU related SSDTs.</p>
<p>The USB patches can be used to fix “instant wake” where the laptop will not sleep without waking up seconds after sleep begins.</p>
<p>Apply the patch appropriate for your hardware:<br>“6-series USB”<br>“7-series/8-series USB”</p>
<p>The USB3 Mutliplex patch can assist in using AppleUSBXHCI.kext instead of GenericUSBXCHI.kext. It is based on information published by Mieze. Most DSDTs will need modifications to use it. The ProBook, for example, uses a modified version of this patch. And the Lenovo u310/u410 can use it as-is:<br>“7-series USB3 Multiplex”</p>
<p>If you’re using GenericUSBXHCI.kext on Yosemite, make sure you’re using one built for Yosemite. Also, to avoid instant wake you may need kernel flag -gux_defer_usb2.</p>
<p>An alternate solution for “instant wake” using AppleUSBXHCI.kext is to use “USB _PRW 0x6D (instant wake)”. You should examine your DSDT to determine what the relevant _PRW methods return to be certain the patch is appropriate for your DSDT. Also provided in the repo is “USB _PRW 0x0D (instant wake)” (0x0D and 0x6D are both common values for XHC/EHC/HDEF return from _PRW).</p>
<p>If you have a Haswell CPU/8-series chipset, and AppleLPC.kext is not loading, you should use this patch to inject a compatible ID that will allow it to load:<br>“Haswell LPC”</p>
<p>If you have a Skylake CPU/100-series chipset, and AppleLPC.kext is not loading, you should use this patch to inject a compatible ID that will allow it to load:<br>“Skylake LPC”</p>
<p>Note regarding renames: Renames must be “balanced.” It is common to rename objects to better match what OS X expects (example “Rename GFX0 to IGPU” for proper IGPU power management). In such cases, all DSDT/SSDTs with references to that name must also be renamed.</p>
<p>Note regarding duplicate identifiers: You must be sure that your patched files do not contain duplicate identifiers. A common case would be adding a _DSM method to a given path in one SSDT, where the OEM has defined a _DSM at the same path in another SSDT. To avoid this problem, you can use the “Remove _DSM methods” patch as one of the first patches you do to all DSDT/SSDTs. Also, “Rename _DSM methods to XDSM” is an alternative (sometimes “Remove _DSM methods” exposes a bug in MaciASL).</p>
<p>Problem specific patching</p>
<p>Battery status: <a href="http://www.tonymacx86.com/yosemite-...de-how-patch-dsdt-working-battery-status.html" target="_blank" rel="noopener">http://www.tonymacx86.com/yosemite-...de-how-patch-dsdt-working-battery-status.html</a></p>
<p>Backlight control: <a href="http://www.tonymacx86.com/yosemite-...ching-dsdt-ssdt-laptop-backlight-control.html" target="_blank" rel="noopener">http://www.tonymacx86.com/yosemite-...ching-dsdt-ssdt-laptop-backlight-control.html</a></p>
<p>Disabling nVidia/Radeon discrete graphics: <a href="http://www.tonymacx86.com/yosemite-...bling-discrete-graphics-dual-gpu-laptops.html" target="_blank" rel="noopener">http://www.tonymacx86.com/yosemite-...bling-discrete-graphics-dual-gpu-laptops.html</a></p>
<p>When following a guide for a specific laptop, it may instruct you to apply a patch that is provided in the post itself. You will recognize it as the syntax used will look similar to other patches you’ve seen in the repository (eg. ‘into_all method label FOO code_regex xxyy removeall_matched;’). These patches are intended to be pasted directly into the patch window in MaciASL so they can be applied.</p>
<p>If you’re interested in writing your own patches, read the documentation on MaciASL patch grammar: <a href="http://sourceforge.net/p/maciasl/wiki/Patching" target="_blank" rel="noopener">http://sourceforge.net/p/maciasl/wiki/Patching</a> Syntax Grammar/</p>
<p>Note: In many cases, DSDT patches are used in conjunction with additional kexts, patched kexts, or Clover config.plist patches that patch the system kexts as they are loaded.</p>
<p>Patches for using patched AppleHDA</p>
<p>With patched AppleHDA, there are two patches that are needed in conjunction with the kext:<br>“Audio Layout 12” (change the layout-id from 12 to the one used by your DSDT)<br>“IRQ Fix”</p>
<p>Note that you must have an AppleHDA that matches your codec, and must determine which layout-id was chosen. The layout-id is an arbitrary choice by the creator of the patched AppleHDA.</p>
<p>To determine the layout-id used by a particular patched AppleHDA: First you need to know your codec id in decimal (eg. 0x10ec0269 = 283902569). Then look in the Info.plist for AppleHDAHardwareConfigDriver.kext (at AppleHDA.kext/Contents/PlugIns/AppleHDAHardwareConfigDriver.kext/Contents/Info.plist), find your codec id under HDAConfigDefault (there may be many entries in a sloppy patched AppleHDA or only one). The LayoutID that matches your codec id is the layout id you need. It is possible that a patched AppleHDA contains more than one layout-id for a given codec. In that case, choose the one you want to use.</p>
<p>Saving files for loading by the bootloader</p>
<p>In order to use your patched DSDT/SSDTs, you must save them where the bootloader can load them. Each bootloader location is unique and has different requirements for naming. Files must be saved in “ACPI Machine Language Binary” (MaciASL-&gt;Save As). Saving a text file (dsl) with an AML extension will likely cause panic or very strange behavior in OS X.</p>
<p>Clover: Files should be placed on the Clover bootloader partition (usually the EFI partition), in EFI/Clover/ACPI/patched. DSDT.aml, if present, will automatically replace the OEM DSDT. For versions of Clover prior to v3062, SSDTs must be named SSDT-x or SSDT-xx, where ‘x’ is a digit (up to SSDT-19). Clover allows gaps in the numbering (eg. SSDT-1.aml, SSDT-5.aml, SSDT-6.aml is allowed). Clover version v3062+ will load all *.aml files present in ACPI/patched, with no restrictions on the name. Keep in mind that SSDT loading order is important. The original order of the SSDTs must be maintained in the patched set.</p>
<p>Note regarding Clover v3062+: A change in the way SSDTs are loaded from ACPI/patched makes the order non-deterministic. You should specify the order explicitly with config.plist/ACPI/SortedOrder. The SortedOrder option is implemented in Clover v3088+. The config.plist files linked from my Clover guide have a good default for SortedOrder: <a href="http://www.tonymacx86.com/yosemite-...de-booting-os-x-installer-laptops-clover.html" target="_blank" rel="noopener">http://www.tonymacx86.com/yosemite-...de-booting-os-x-installer-laptops-clover.html</a></p>
<p>Chameleon(or Chimera): Files should be placed in /Extra on the system volume (or wherever your bootloader /Extra configuration is located). DSDT.aml, if present, will automatically replace the OEM DSDT. Chameleon requires there are no gaps in the names of SSDTs. It will load SSDT.aml, SSDT-1.aml, SSDT-2.aml, SSDT-3.aml, and so-on from /Extra until it does not find the file in the sequence. So, if you have SSDT.aml, SSDT-1.aml, SSDT-4.aml, SSDT-5.aml, only SSDT.aml and SSDT-1.aml will be loaded. SSDT-4.aml and SSDT-5.aml will be ignored.</p>
<p>Finally, keep in mind you cannot provide patched SSDTs without dropping the OEM SSDTs that they replace. The easiest way is to use DropSSDT=Yes (Chameleon) or ACPI/SSDT/DropOem=true (Clover) to drop all the SSDTs, then provide the patched (and unpatched) files for loading by the bootloader.</p>
<p>Floating regions</p>
<p>In ACPI, an OperationRegion can define a MMIO region, SystemMemory region, EmbeddedControl region, etc. These regions usually have fixed addresses dependent only on the machine configuration, BIOS version, or BIOS options. Sometimes, these regions can change randomly or unexpectedly. This is referred to as “floating regions”.</p>
<p>Since by patching DSDT and/or SSDTs, we are providing a snapshot of these addresses at a given point in time, they may not match up should the BIOS decide to place such regions at a different address. If this is the case, you may notice that certain features are intermittently working, or other stability issues that appear to be random.</p>
<p>As a result, it is a good idea to use Clover and its FixRegions feature. You can find the details in the Clover Wiki. All of the config.plist files in the Clover laptop guide use this feature. Note: Only floating regions in DSDT can be fixed by FixRegions. Floating regions in SSDTs are problematic and there is no good solution other than to not provide patched SSDTs for SSDTs subject to randomly floating regions. Working around floating regions in patched SSDTs is beyond the scope of this guide.</p>
<p>Resources</p>
<p>MaciASL (RehabMan fork): <a href="https://github.com/RehabMan/OS-X-MaciASL-patchmatic" target="_blank" rel="noopener">https://github.com/RehabMan/OS-X-MaciASL-patchmatic</a><br>patchmatic: <a href="https://github.com/RehabMan/OS-X-MaciASL-patchmatic" target="_blank" rel="noopener">https://github.com/RehabMan/OS-X-MaciASL-patchmatic</a><br>iasl (RehabMan fork): <a href="https://bitbucket.org/RehabMan/acpica/downloads" target="_blank" rel="noopener">https://bitbucket.org/RehabMan/acpica/downloads</a><br>ACPI spec: <a href="http://acpi.info/spec.htm" target="_blank" rel="noopener">http://acpi.info/spec.htm</a></p>
<p>RehabMan github: <a href="https://github.com/RehabMan?tab=repositories" target="_blank" rel="noopener">https://github.com/RehabMan?tab=repositories</a></p>
<p>Clover laptop guide: <a href="http://www.tonymacx86.com/yosemite-...oting-os-x-installer-laptops-clover-uefi.html" target="_blank" rel="noopener">http://www.tonymacx86.com/yosemite-...oting-os-x-installer-laptops-clover-uefi.html</a><br>Clover config.plist files for laptops: <a href="https://github.com/RehabMan/OS-X-Clover-Laptop-Config" target="_blank" rel="noopener">https://github.com/RehabMan/OS-X-Clover-Laptop-Config</a></p>
<p>Clover thread: <a href="http://www.insanelymac.com/forum/topic/284656-clover-general-discussion/" target="_blank" rel="noopener">http://www.insanelymac.com/forum/topic/284656-clover-general-discussion/</a><br>Clover changes: <a href="http://www.insanelymac.com/forum/topic/304530-clover-change-explanations/" target="_blank" rel="noopener">http://www.insanelymac.com/forum/topic/304530-clover-change-explanations/</a></p>
<p>Providing Feedback</p>
<p>Do not treat this thread as your private troubleshooting thread. If you have a specific problem with your specific laptop, open a separate thread. If you see something here that is in error, or wish to make a contribution, please reply to this thread.</p>
<p>Problem Reporting</p>
<p>Please read above “Providing Feedback”. It is best to open a separate thread.</p>
<p>In that separate thread, describe you problem clearly. And provide relevant data…</p>
<p>Download patchmatic: <a href="https://bitbucket.org/RehabMan/os-x-maciasl-patchmatic/downloads/RehabMan-patchmatic-2015-0107.zip" target="_blank" rel="noopener">https://bitbucket.org/RehabMan/os-x-maciasl-patchmatic/downloads/RehabMan-patchmatic-2015-0107.zip</a><br>Extract the ‘patchmatic’ binary from the ZIP. Copy it to /usr/bin, such that you have the binary at /usr/bin/patchmatic.</p>
<p>In terminal,<br>Code (Text):</p>
<p>if [ -d ~/Downloads/RehabMan ]; then rm -R ~/Downloads/RehabMan; fi<br>mkdir ~/Downloads/RehabMan<br>cd ~/Downloads/RehabMan<br>patchmatic -extract</p>
<p>Note: It is easier if you use copy/paste instead of typing the commands manually.</p>
<p>Attach contents of Downloads/RehabMan directory as ZIP.</p>
<p>Attach ioreg as ZIP: <a href="http://www.tonymacx86.com/audio/58368-guide-how-make-copy-ioreg.html" target="_blank" rel="noopener">http://www.tonymacx86.com/audio/58368-guide-how-make-copy-ioreg.html</a>. Please, use the IORegistryExplorer v2.1 attached to the post! DO NOT reply with an ioreg from any other version of IORegistryExplorer.app.</p>
<p>Provide output (in Terminal):<br>Code (Text):</p>
<p>kextstat|grep -y acpiplat<br>kextstat|grep -y appleintelcpu<br>kextstat|grep -y applelpc<br>kextstat|grep -y applehda</p>
<p>Attach EFI/Clover folder as ZIP (press F4 at main Clover screen before collecting). Please eliminate ‘themes’ directory. Provide only EFI/Clover, not the entire EFI folder.</p>
<p>Attach output of (in Terminal):<br>Code (Text):</p>
<p>sudo touch /System/Library/Extensions &amp;&amp; sudo kextcache -u /</p>
<p>Compress all files as ZIP. Do not use external links. Attach all files using site attachments only.</p>
<p>If you cannot boot, attach photo of panic/hang and the EFI/Clover that causes it (as ZIP, without themes).</p>
]]></content>
      <categories>
        <category>-MacOS</category>
      </categories>
      <tags>
        <tag>-MacOS</tag>
      </tags>
  </entry>
  <entry>
    <title>Idea使用技巧</title>
    <url>/2017/05/05/Tools/Idea%E4%BD%BF%E7%94%A8%E6%8A%80%E5%B7%A7/</url>
    <content><![CDATA[<h2 id="【编辑】"><a href="#【编辑】" class="headerlink" title="【编辑】"></a>【编辑】</h2><p>souf:<br>System.out.printf(“”);</p>
<p>sout:<br>System.out.println();</p>
<a id="more"></a>

<p>soutm:<br>System.out.println(“demo.main”);</p>
<p>soutp:<br>System.out.println(“args = [“ + args + “]”);</p>
<p>soutv:<br>System.out.println(“true = “ + true);</p>
<p>Shift+Click，可以关闭文件</p>
<p>Ctrl+[ / ]，可以跑到大括号的开头与结尾</p>
<p>Ctrl+N，可以快速打开类</p>
<p>Ctrl+Shift+N，可以快速打开文件</p>
<p>Ctrl+Q，可以看到当前方法的声明</p>
<p>Ctrl+Shift+Insert，可以选择剪贴板内容并插入</p>
<p>Alt+Insert，可以生成构造器/Getter/Setter等</p>
<p>Ctrl+Enter，导入包，自动修正</p>
<p>Ctrl+Alt+L，格式化代码</p>
<p>Ctrl+Alt+I，将选中的代码进行自动缩进编排，这个功能在编辑 JSP 文件时也可以工作</p>
<p>Ctrl+R，替换文本</p>
<p>Ctrl+F，查找文本</p>
<p>Ctrl+Shift+Space，自动补全代码</p>
<p>Shift+F6，重构 – 重命名</p>
<p>Ctrl+X，删除行</p>
<p>Ctrl+D，复制行</p>
<p>Ctrl+/或Ctrl+Shift+/，注释（//或者）</p>
<p>Ctrl+J，自动代码（例如：serr）</p>
<p>Ctrl+Alt+J，用动态模板环绕</p>
<p>Ctrl+H，显示类结构图（类的继承层次）</p>
<p>Ctrl+Alt+left/right，返回至上次浏览的位置</p>
<p>Alt+Up/Down，在方法间快速移动定位</p>
<p>F2 或 Shift+F2，高亮错误或警告快速定位</p>
<p>Alt+F3，逐个往下查找相同文本，并高亮显示</p>
<p>Ctrl+O，重写方法</p>
<p>Ctrl+Shift+J，整合两行</p>
<p>Alt+F8，计算变量值</p>
<p>Shift+Esc，不仅可以把焦点移到编辑器上，而且还可以隐藏当前（或最后活动的）工具窗口</p>
<p>Ctrl+Y，删除当前行</p>
<p>Ctrl+Shift+F，全局查找</p>
<p>Ctrl+U，转到父类</p>
<p>Ctrl+G，定位行</p>
<p>Ctrl+Backspace，按单词删除</p>
<p>Ctrl+”+/-”，当前方法展开、折叠</p>
<p>Ctrl+Shift+”+/-”，全部展开、折叠</p>
<h2 id="【调试部分、编译】"><a href="#【调试部分、编译】" class="headerlink" title="【调试部分、编译】"></a>【调试部分、编译】</h2><p>Ctrl+F2，停止<br>Alt+Shift+F9，选择 Debug<br>Alt+Shift+F10，选择 Run<br>Ctrl+Shift+F9，编译<br>Ctrl+Shift+F10，运行<br>Ctrl+Shift+F8，查看断点<br>F8，步过<br>F7，步入<br>Shift+F7，智能步入<br>Shift+F8，步出<br>Alt+Shift+F8，强制步过<br>Alt+Shift+F7，强制步入<br>Alt+F9，运行至光标处<br>Ctrl+Alt+F9，强制运行至光标处<br>F9，恢复程序<br>Alt+F10，定位到断点<br>Ctrl+F8，切换行断点<br>Ctrl+F9，生成项目<br>Alt+1，项目<br>Alt+2，收藏<br>Alt+6，TODO<br>Alt+7，结构<br>Ctrl+Shift+C，复制路径<br>Ctrl+Alt+Shift+C，复制引用，必须选择类名<br>Ctrl+Alt+Y，同步<br>Ctrl+~，快速切换方案（界面外观、代码风格、快捷键映射等菜单）<br>Shift+F12，还原默认布局<br>Ctrl+Shift+F12，隐藏/恢复所有窗口<br>Ctrl+F4，关闭<br>Ctrl+Shift+F4，关闭活动选项卡<br>Ctrl+Tab，转到下一个拆分器<br>Ctrl+Shift+Tab，转到上一个拆分器</p>
<p>##【重构】<br>Ctrl+Alt+Shift+T，弹出重构菜单<br>Shift+F6，重命名<br>F6，移动<br>F5，复制<br>Alt+Delete，安全删除<br>Ctrl+Alt+N，内联<br>ctrl+shift+alt+j 列操作</p>
<p>##【查找】<br>Ctrl+F，查找<br>Ctrl+R，替换<br>F3，查找下一个<br>Shift+F3，查找上一个<br>Ctrl+Shift+F，在路径中查找<br>Ctrl+Shift+R，在路径中替换<br>Ctrl+Shift+S，搜索结构<br>Ctrl+Shift+M，替换结构<br>Alt+F7，查找用法<br>Ctrl+Alt+F7，显示用法<br>Ctrl+F7，在文件中查找用法<br>Ctrl+Shift+F7，在文件中高亮显示用法</p>
<h1 id="1-跳转"><a href="#1-跳转" class="headerlink" title="1. 跳转"></a>1. 跳转</h1><h3 id="1-上次编辑的位置"><a href="#1-上次编辑的位置" class="headerlink" title="1.上次编辑的位置"></a>1.上次编辑的位置</h3><p><img src="https://s1.ax1x.com/2018/05/20/CgVzPH.png" alt="CgVzPH.png"></p>
<h3 id="2-上次查看的位置"><a href="#2-上次查看的位置" class="headerlink" title="2.上次查看的位置"></a>2.上次查看的位置</h3><p><img src="https://s1.ax1x.com/2018/05/20/CgZSGd.png" alt="CgZSGd.png"></p>
<h3 id="3-书签跳转"><a href="#3-书签跳转" class="headerlink" title="3.书签跳转"></a>3.书签跳转</h3><p>使用Ctrl+F11可以输入一个带有数字字母标记的书签，比如我们使用的标签名是1则我们使用Ctrl+1/N可以跳转到对应的位置。</p>
<p>另外我们可以在Favorite中找到对应的书签</p>
<p><img src="https://s1.ax1x.com/2018/05/20/CgZ9xI.png" alt="CgZ9xI.png"></p>
<h3 id="4-收藏类或者方法"><a href="#4-收藏类或者方法" class="headerlink" title="4.收藏类或者方法"></a>4.收藏类或者方法</h3><p>把光标移动到类里面或者上面，使用add to favorite 添加到对应的收藏夹，如果放到方法里面则是添加方法。</p>
<p><img src="https://s1.ax1x.com/2018/05/20/CgZAZ8.png" alt="CgZAZ8.png"></p>
<h3 id="5-使用macsIdea插件进行跳转"><a href="#5-使用macsIdea插件进行跳转" class="headerlink" title="5.使用macsIdea插件进行跳转"></a>5.使用macsIdea插件进行跳转</h3><p>在keymap中配置AceJumpWords的快捷键为alt+j</p>
<p>按下alt+j 然后输入我们需要跳转的字母则会生成字母对应位置的一个快捷键，我们就可以跳转过去。</p>
<h3 id="6-vim分屏"><a href="#6-vim分屏" class="headerlink" title="6.vim分屏"></a>6.vim分屏</h3><p>:sp / :vsp</p>
<p>ctrl+w -&gt; h/l 左右</p>
<p>ctrl+w -&gt; j/k上下</p>
<p>ctrl+w -&gt; c 关闭</p>
<p>ctrl+w &gt;-&gt;+/-/= 尺寸</p>
<h1 id="2-列操作"><a href="#2-列操作" class="headerlink" title="2.列操作"></a>2.列操作</h1><p><a href="https://imgchr.com/i/CgZuzn" target="_blank" rel="noopener"><img src="https://s1.ax1x.com/2018/05/20/CgZuzn.md.png" alt="CgZuzn.md.png"></a></p>
<h1 id="3-posfix"><a href="#3-posfix" class="headerlink" title="3.posfix"></a>3.posfix</h1><p><img src="https://s1.ax1x.com/2018/05/20/CgZGoF.png" alt="CgZGoF.png"></p>
<h2 id="1-for"><a href="#1-for" class="headerlink" title="1. for"></a>1. for</h2><p>100.fori 生成100 的for循环</p>
<h2 id="2-sout"><a href="#2-sout" class="headerlink" title="2. sout"></a>2. sout</h2><p>“hello”.sout</p>
<h2 id="3-field"><a href="#3-field" class="headerlink" title="3.field"></a>3.field</h2><p>name.field 直接创建字段并且在构造方法中赋值</p>
<h2 id="4-return"><a href="#4-return" class="headerlink" title="4.return"></a>4.return</h2><p>name.return </p>
<h2 id="5-nn"><a href="#5-nn" class="headerlink" title="5.nn"></a>5.nn</h2><p>不空  name.nn</p>
<h1 id="4-自动修复"><a href="#4-自动修复" class="headerlink" title="4.自动修复"></a>4.自动修复</h1><p>alt+enter</p>
<p>这个是一个多功能的快捷键，根据不同场景有不同效果。</p>
<ul>
<li>自动创建函数，当函数没有创建的时候</li>
<li>代码优化</li>
<li>string的format和builder</li>
<li>接口的实现，subclass</li>
<li>单词拼写</li>
<li>导包</li>
</ul>
<h1 id="5-重构"><a href="#5-重构" class="headerlink" title="5.重构"></a>5.重构</h1><h2 id="1-shift-f6"><a href="#1-shift-f6" class="headerlink" title="1. shift+f6"></a>1. shift+f6</h2><p>当前的方法名或者变量名都会被选中，然后我们只需要修改一个地方就可以完成整个的修改</p>
<h2 id="2-ctrl-f6"><a href="#2-ctrl-f6" class="headerlink" title="2.ctrl+f6"></a>2.ctrl+f6</h2><p>在函数名字上使用这个快捷键就可以进行函数签名的重构。</p>
<h2 id="3-抽取变量"><a href="#3-抽取变量" class="headerlink" title="3.抽取变量"></a>3.抽取变量</h2><p><img src="https://s1.ax1x.com/2018/05/20/CgZjO0.png" alt="CgZjO0.png"></p>
<h1 id="6-调试"><a href="#6-调试" class="headerlink" title="6. 调试"></a>6. 调试</h1><h2 id="1-ctrl-f8-添加断点"><a href="#1-ctrl-f8-添加断点" class="headerlink" title="1. ctrl+f8 添加断点"></a>1. ctrl+f8 添加断点</h2><h2 id="2-shift-f9-调试程序"><a href="#2-shift-f9-调试程序" class="headerlink" title="2.shift+f9 调试程序"></a>2.shift+f9 调试程序</h2><h2 id="3-f8-单步运行"><a href="#3-f8-单步运行" class="headerlink" title="3.f8 单步运行"></a>3.f8 单步运行</h2><h2 id="4-resume-调到下个断点没有断点结束程序"><a href="#4-resume-调到下个断点没有断点结束程序" class="headerlink" title="4.resume 调到下个断点没有断点结束程序"></a>4.resume 调到下个断点没有断点结束程序</h2><h2 id="5-ctrl-shift-f8-按两次哦-，查看所有断点"><a href="#5-ctrl-shift-f8-按两次哦-，查看所有断点" class="headerlink" title="5.ctrl+shift+f8 按两次哦 ，查看所有断点"></a>5.ctrl+shift+f8 按两次哦 ，查看所有断点</h2><h2 id="6-mute-breakpoints-禁止所有断点"><a href="#6-mute-breakpoints-禁止所有断点" class="headerlink" title="6.mute breakpoints 禁止所有断点"></a>6.mute breakpoints 禁止所有断点</h2><h2 id="7-ctrl-shift-f8条件断点"><a href="#7-ctrl-shift-f8条件断点" class="headerlink" title="7.ctrl+shift+f8条件断点"></a>7.ctrl+shift+f8条件断点</h2><h2 id="8-alt-f8表达式求值"><a href="#8-alt-f8表达式求值" class="headerlink" title="8.alt+f8表达式求值"></a>8.alt+f8表达式求值</h2><h2 id="9-alt-f9-运行到执行行"><a href="#9-alt-f9-运行到执行行" class="headerlink" title="9.alt+f9 运行到执行行"></a>9.alt+f9 运行到执行行</h2><h2 id="10-f2修改值"><a href="#10-f2修改值" class="headerlink" title="10.f2修改值"></a>10.f2修改值</h2><p>所有的导包，以及优化 ctrl+shift+o</p>
<p>查看所有的实现 Ctrl+H</p>
]]></content>
      <categories>
        <category>-开发工具</category>
      </categories>
  </entry>
  <entry>
    <title>Atom使用技巧</title>
    <url>/2017/05/05/Tools/Atom%E4%BD%BF%E7%94%A8%E6%8A%80%E5%B7%A7/</url>
    <content><![CDATA[<h3 id="打开项目"><a href="#打开项目" class="headerlink" title="打开项目"></a>打开项目</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ctrl + o</span><br></pre></td></tr></table></figure>

<h3 id="模糊查找文件"><a href="#模糊查找文件" class="headerlink" title="模糊查找文件"></a>模糊查找文件</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ctrl + t  / ctrl + p</span><br><span class="line">ctrl + b 仅仅查找已经打开的文件</span><br></pre></td></tr></table></figure>

<h3 id="命令模式"><a href="#命令模式" class="headerlink" title="命令模式"></a>命令模式</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ctrl + <span class="built_in">shift</span> +p</span><br></pre></td></tr></table></figure>

<h3 id="另存为"><a href="#另存为" class="headerlink" title="另存为"></a>另存为</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ctrl + <span class="built_in">shift</span> + s</span><br></pre></td></tr></table></figure>

<h3 id="gmerge-conflicts"><a href="#gmerge-conflicts" class="headerlink" title="gmerge-conflicts"></a>gmerge-conflicts</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">在使用 Git 进行合并和 rebase 的时候可以用 alt-m d 来激活这个插件，它会列出所有冲突的文件，将每一处冲突高亮，同时有按钮和快捷键供你快速选用某个版本，在你解决所有冲突后会提示你进行 Commit. 有了这个插件再也不同担心出冲突的时候看瞎眼了。</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Atom</category>
      </categories>
  </entry>
  <entry>
    <title>Linux常用技巧</title>
    <url>/2017/05/04/Linux/Linux%E5%B8%B8%E7%94%A8%E6%8A%80%E5%B7%A7/</url>
    <content><![CDATA[<h3 id="使用ls显示一个文件的绝路径："><a href="#使用ls显示一个文件的绝路径：" class="headerlink" title="使用ls显示一个文件的绝路径："></a>使用ls显示一个文件的绝路径：</h3><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls file |sed <span class="string">"s:^:`pwd`/:"</span></span><br></pre></td></tr></table></figure>

<h3 id="创建一个程序的启动器："><a href="#创建一个程序的启动器：" class="headerlink" title="创建一个程序的启动器："></a>创建一个程序的启动器：</h3><p>使用Desktop Entery的格式，首先需要进入相应目录建立文件，在文件中写入程序相关信息：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cd /usr/share/applications/</span><br><span class="line">sudo vim application.desktop</span><br></pre></td></tr></table></figure>

<p>开始写入文件信息,格式如下：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">[Desktop Entery]</span><br><span class="line">Version=1.0</span><br><span class="line">Name=AppName</span><br><span class="line">Exec=/application/path</span><br><span class="line">Icon=/application/icon.png</span><br><span class="line">Terminal=<span class="literal">false</span></span><br><span class="line">Type=Application</span><br></pre></td></tr></table></figure>

<h3 id="vim中选定内容："><a href="#vim中选定内容：" class="headerlink" title="vim中选定内容："></a>vim中选定内容：</h3><p>命令模式下使用vi命令变为可视之后即可选中，使用d就可以删除</p>
<h3 id="快速处理某个被包含的内容："><a href="#快速处理某个被包含的内容：" class="headerlink" title="快速处理某个被包含的内容："></a>快速处理某个被包含的内容：</h3><p>以下命令可以对标点内的内容进行操作。</p>
<blockquote>
<p>ci’、ci”、ci(、ci[、ci{、ci&lt; - 分别更改这些配对标点符号中的文本内容<br>di’、di”、di(或dib、di[、di{或diB、di&lt; - 分别删除这些配对标点符号中的文本内容<br>yi’、yi”、yi(、yi[、yi{、yi&lt; - 分别复制这些配对标点符号中的文本内容<br>vi’、vi”、vi(、vi[、vi{、vi&lt; - 分别选中这些配对标点符号中的文本内容</p>
</blockquote>
<p>另外如果把上面的i改成a可以连配对标点一起操作。<br>举个例子：<br>比如要操作的文本如下：<br>111”222”333<br>将光标移到”222”的任何一个字符处输入命令 di” ,文本会变成： 111””333<br>若输入命令 da” ,文本会变成： 111333</p>
]]></content>
      <categories>
        <category>-Linux</category>
      </categories>
  </entry>
  <entry>
    <title>Linux下安装shadowsocks</title>
    <url>/2017/05/04/Linux/Linux%E4%B8%8B%E5%AE%89%E8%A3%85shadowsocks/</url>
    <content><![CDATA[<p><img src="http://i4.buimg.com/567571/32f51545bce7861e.jpg" alt></p>
<h2 id="Fedora-Red-Hat-Enterprise-Linux"><a href="#Fedora-Red-Hat-Enterprise-Linux" class="headerlink" title="Fedora/Red Hat Enterprise Linux"></a>Fedora/Red Hat Enterprise Linux</h2><p>使用dnf安装:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo dnf copr <span class="built_in">enable</span> librehat/shadowsocks</span><br><span class="line">sudo dnf update</span><br><span class="line">sudo dnf install shadowsocks-qt5</span><br></pre></td></tr></table></figure>

<p>如果你要使用copr repo可能还需要安装下面这个插件：</p>
<a id="more"></a>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo dnf install dnf-plugins-core</span><br></pre></td></tr></table></figure>

<p>当然你的Linux版本不是特别高的话可能用的不是dnf，这时候用yum就行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo yum update</span><br><span class="line">sudo yum install shadowsocks-qt5</span><br></pre></td></tr></table></figure>

<h2 id="Ubuntu"><a href="#Ubuntu" class="headerlink" title="Ubuntu"></a>Ubuntu</h2><blockquote>
<p>PPA is for Ubuntu &gt;= 14.04.</p>
</blockquote>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo add-apt-repository ppa:hzwhuang/ss-qt5</span><br><span class="line">sudo apt-get update</span><br><span class="line">sudo apt-get install shadowsocks-qt5</span><br></pre></td></tr></table></figure>

<h2 id="Debian"><a href="#Debian" class="headerlink" title="Debian"></a>Debian</h2><p>首先需要安装一系列的ss-qt5的依赖：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo apt-get install qt5-qmake qtbase5-dev libqrencode-dev libqtshadowsocks-dev libappindicator-dev libzbar-dev libbotan1.10-dev</span><br></pre></td></tr></table></figure>

<p>然后执行下面的命令，就能得到一个Deb包</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dpkg-buildpackage -uc -us -b</span><br></pre></td></tr></table></figure>

<p>之后直接使用dpkg安装即可：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo dpkg -i shadowsocks-qt5-&lt;VER_ARCH_ETC&gt;.deb.</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Linux</category>
      </categories>
  </entry>
  <entry>
    <title>Linux下安装Node.js</title>
    <url>/2017/05/04/Linux/Linux%E4%B8%8B%E5%AE%89%E8%A3%85Node-js/</url>
    <content><![CDATA[<p><img src="http://i2.muimg.com/567571/e273349501de49b0.jpg" alt></p>
<h2 id="手动二进制包的安装方法"><a href="#手动二进制包的安装方法" class="headerlink" title="手动二进制包的安装方法"></a>手动二进制包的安装方法</h2><p>一般来说我们在Node的官网上是找不到Linux的Deb包，或者是Rpm包的，他针对Windows和mac都有二进制的安装包，但是Linux只有一个tar.xz这个包下载下来虽然也是一个二进制的包，类似于Windows中的绿色软件，也非常好配置，仅仅使用</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">tar -xvf package.tar.xz</span><br></pre></td></tr></table></figure>

<p>即可完成解压，然后使用软连接，把node与npm文件链接到<em>/usr/local/bin</em>下面</p>
<a id="more"></a>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ln -s /opt/nodejs/bin/node /usr/<span class="built_in">local</span>/bin/node</span><br><span class="line">ln -s /opt/nodejs/bin/npm /usr/<span class="built_in">local</span>/bin/npm</span><br></pre></td></tr></table></figure>

<p>这样以来然后可以看看node的版本，从而来判断node是否安装上了，并且看看版本是不是我们需要的版本。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">node -v</span><br></pre></td></tr></table></figure>

<p>假如出现了类似与以下的信息就说明安装完成</p>
<blockquote>
<p>v6.10.3</p>
</blockquote>
<h2 id="使用nvm管理node版本"><a href="#使用nvm管理node版本" class="headerlink" title="使用nvm管理node版本"></a>使用nvm管理node版本</h2><h3 id="安装nvm"><a href="#安装nvm" class="headerlink" title="安装nvm"></a>安装nvm</h3><p>复制以下代码，放到一个shell脚本中。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#!/usr/bin/env bash</span></span><br><span class="line"></span><br><span class="line">&#123; <span class="comment"># this ensures the entire script is downloaded #</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_has</span></span>() &#123;</span><br><span class="line">  <span class="built_in">type</span> <span class="string">"<span class="variable">$1</span>"</span> &gt; /dev/null 2&gt;&amp;1</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_install_dir</span></span>() &#123;</span><br><span class="line">  <span class="built_in">printf</span> %s <span class="string">"<span class="variable">$&#123;NVM_DIR:-"$HOME/.nvm"&#125;</span>"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_latest_version</span></span>() &#123;</span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"v0.33.2"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_profile_is_bash_or_zsh</span></span>() &#123;</span><br><span class="line">  <span class="built_in">local</span> TEST_PROFILE</span><br><span class="line">  TEST_PROFILE=<span class="string">"<span class="variable">$&#123;1-&#125;</span>"</span></span><br><span class="line">  <span class="keyword">case</span> <span class="string">"<span class="variable">$&#123;TEST_PROFILE-&#125;</span>"</span> <span class="keyword">in</span></span><br><span class="line">    *<span class="string">"/.bashrc"</span> | *<span class="string">"/.bash_profile"</span> | *<span class="string">"/.zshrc"</span>)</span><br><span class="line">      <span class="built_in">return</span></span><br><span class="line">    ;;</span><br><span class="line">    *)</span><br><span class="line">      <span class="built_in">return</span> 1</span><br><span class="line">    ;;</span><br><span class="line">  <span class="keyword">esac</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Outputs the location to NVM depending on:</span></span><br><span class="line"><span class="comment"># * The availability of $NVM_SOURCE</span></span><br><span class="line"><span class="comment"># * The method used ("script" or "git" in the script, defaults to "git")</span></span><br><span class="line"><span class="comment"># NVM_SOURCE always takes precedence unless the method is "script-nvm-exec"</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_source</span></span>() &#123;</span><br><span class="line">  <span class="built_in">local</span> NVM_METHOD</span><br><span class="line">  NVM_METHOD=<span class="string">"<span class="variable">$1</span>"</span></span><br><span class="line">  <span class="built_in">local</span> NVM_SOURCE_URL</span><br><span class="line">  NVM_SOURCE_URL=<span class="string">"<span class="variable">$NVM_SOURCE</span>"</span></span><br><span class="line">  <span class="keyword">if</span> [ <span class="string">"_<span class="variable">$NVM_METHOD</span>"</span> = <span class="string">"_script-nvm-exec"</span> ]; <span class="keyword">then</span></span><br><span class="line">    NVM_SOURCE_URL=<span class="string">"https://raw.githubusercontent.com/creationix/nvm/<span class="variable">$(nvm_latest_version)</span>/nvm-exec"</span></span><br><span class="line">  <span class="keyword">elif</span> [ -z <span class="string">"<span class="variable">$NVM_SOURCE_URL</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">if</span> [ <span class="string">"_<span class="variable">$NVM_METHOD</span>"</span> = <span class="string">"_script"</span> ]; <span class="keyword">then</span></span><br><span class="line">      NVM_SOURCE_URL=<span class="string">"https://raw.githubusercontent.com/creationix/nvm/<span class="variable">$(nvm_latest_version)</span>/nvm.sh"</span></span><br><span class="line">    <span class="keyword">elif</span> [ <span class="string">"_<span class="variable">$NVM_METHOD</span>"</span> = <span class="string">"_git"</span> ] || [ -z <span class="string">"<span class="variable">$NVM_METHOD</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">      NVM_SOURCE_URL=<span class="string">"https://github.com/creationix/nvm.git"</span></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Unexpected value \"<span class="variable">$NVM_METHOD</span>\" for \$NVM_METHOD"</span></span><br><span class="line">      <span class="built_in">return</span> 1</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"<span class="variable">$NVM_SOURCE_URL</span>"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Node.js version to install</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_node_version</span></span>() &#123;</span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"<span class="variable">$NODE_VERSION</span>"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_download</span></span>() &#123;</span><br><span class="line">  <span class="keyword">if</span> nvm_has <span class="string">"curl"</span>; <span class="keyword">then</span></span><br><span class="line">    curl --compressed -q <span class="string">"<span class="variable">$@</span>"</span></span><br><span class="line">  <span class="keyword">elif</span> nvm_has <span class="string">"wget"</span>; <span class="keyword">then</span></span><br><span class="line">    <span class="comment"># Emulate curl with wget</span></span><br><span class="line">    ARGS=$(<span class="built_in">echo</span> <span class="string">"$*"</span> | <span class="built_in">command</span> sed -e <span class="string">'s/--progress-bar /--progress=bar /'</span> \</span><br><span class="line">                           -e <span class="string">'s/-L //'</span> \</span><br><span class="line">                           -e <span class="string">'s/--compressed //'</span> \</span><br><span class="line">                           -e <span class="string">'s/-I /--server-response /'</span> \</span><br><span class="line">                           -e <span class="string">'s/-s /-q /'</span> \</span><br><span class="line">                           -e <span class="string">'s/-o /-O /'</span> \</span><br><span class="line">                           -e <span class="string">'s/-C - /-c /'</span>)</span><br><span class="line">    <span class="comment"># shellcheck disable=SC2086</span></span><br><span class="line">    <span class="built_in">eval</span> wget <span class="variable">$ARGS</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">install_nvm_from_git</span></span>() &#123;</span><br><span class="line">  <span class="built_in">local</span> INSTALL_DIR</span><br><span class="line">  INSTALL_DIR=<span class="string">"<span class="variable">$(nvm_install_dir)</span>"</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ -d <span class="string">"<span class="variable">$INSTALL_DIR</span>/.git"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; nvm is already installed in <span class="variable">$INSTALL_DIR</span>, trying to update using git"</span></span><br><span class="line">    <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"\r=&gt; "</span></span><br><span class="line">    <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> fetch 2&gt; /dev/null || &#123;</span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Failed to update nvm, run 'git fetch' in <span class="variable">$INSTALL_DIR</span> yourself."</span></span><br><span class="line">      <span class="built_in">exit</span> 1</span><br><span class="line">    &#125;</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    <span class="comment"># Cloning to $INSTALL_DIR</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Downloading nvm from git to '<span class="variable">$INSTALL_DIR</span>'"</span></span><br><span class="line">    <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"\r=&gt; "</span></span><br><span class="line">    mkdir -p <span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>"</span></span><br><span class="line">    <span class="keyword">if</span> [ <span class="string">"<span class="variable">$(ls -A "$&#123;INSTALL_DIR&#125;")</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">command</span> git init <span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>"</span> || &#123;</span><br><span class="line">        <span class="built_in">echo</span> &gt;&amp;2 <span class="string">'Failed to initialize nvm repo. Please report this!'</span></span><br><span class="line">        <span class="built_in">exit</span> 2</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>/.git"</span> remote add origin <span class="string">"<span class="variable">$(nvm_source)</span>"</span> 2&gt; /dev/null \</span><br><span class="line">        || <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>/.git"</span> remote <span class="built_in">set</span>-url origin <span class="string">"<span class="variable">$(nvm_source)</span>"</span> || &#123;</span><br><span class="line">        <span class="built_in">echo</span> &gt;&amp;2 <span class="string">'Failed to add remote "origin" (or set the URL). Please report this!'</span></span><br><span class="line">        <span class="built_in">exit</span> 2</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>/.git"</span> fetch origin --tags || &#123;</span><br><span class="line">        <span class="built_in">echo</span> &gt;&amp;2 <span class="string">'Failed to fetch origin with tags. Please report this!'</span></span><br><span class="line">        <span class="built_in">exit</span> 2</span><br><span class="line">      &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">command</span> git <span class="built_in">clone</span> <span class="string">"<span class="variable">$(nvm_source)</span>"</span> <span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>"</span> || &#123;</span><br><span class="line">        <span class="built_in">echo</span> &gt;&amp;2 <span class="string">'Failed to clone nvm repo. Please report this!'</span></span><br><span class="line">        <span class="built_in">exit</span> 2</span><br><span class="line">      &#125;</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> checkout -f --quiet <span class="string">"<span class="variable">$(nvm_latest_version)</span>"</span></span><br><span class="line">  <span class="keyword">if</span> [ ! -z <span class="string">"<span class="variable">$(command git --git-dir="$INSTALL_DIR"/.git --work-tree="$INSTALL_DIR" show-ref refs/heads/master)</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> branch --quiet 2&gt;/dev/null; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> branch --quiet -D master &gt;/dev/null 2&gt;&amp;1</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Your version of git is out of date. Please update it!"</span></span><br><span class="line">      <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> branch -D master &gt;/dev/null 2&gt;&amp;1</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"=&gt; Compressing and cleaning up git repository"</span></span><br><span class="line">  <span class="keyword">if</span> ! <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> gc --aggressive --prune=now ; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Your version of git is out of date. Please update it!"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  <span class="built_in">return</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Automatically install Node.js</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_install_node</span></span>() &#123;</span><br><span class="line">  <span class="built_in">local</span> NODE_VERSION</span><br><span class="line">  NODE_VERSION=<span class="string">"<span class="variable">$(nvm_node_version)</span>"</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$NODE_VERSION</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">return</span> 0</span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"=&gt; Installing Node.js version <span class="variable">$NODE_VERSION</span>"</span></span><br><span class="line">  nvm install <span class="string">"<span class="variable">$NODE_VERSION</span>"</span></span><br><span class="line">  <span class="built_in">local</span> CURRENT_NVM_NODE</span><br><span class="line"></span><br><span class="line">  CURRENT_NVM_NODE=<span class="string">"<span class="variable">$(nvm_version current)</span>"</span></span><br><span class="line">  <span class="keyword">if</span> [ <span class="string">"<span class="variable">$(nvm_version "$NODE_VERSION")</span>"</span> == <span class="string">"<span class="variable">$CURRENT_NVM_NODE</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Node.js version <span class="variable">$NODE_VERSION</span> has been successfully installed"</span></span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Failed to install Node.js <span class="variable">$NODE_VERSION</span>"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">install_nvm_as_script</span></span>() &#123;</span><br><span class="line">  <span class="built_in">local</span> INSTALL_DIR</span><br><span class="line">  INSTALL_DIR=<span class="string">"<span class="variable">$(nvm_install_dir)</span>"</span></span><br><span class="line">  <span class="built_in">local</span> NVM_SOURCE_LOCAL</span><br><span class="line">  NVM_SOURCE_LOCAL=$(nvm_source script)</span><br><span class="line">  <span class="built_in">local</span> NVM_EXEC_SOURCE</span><br><span class="line">  NVM_EXEC_SOURCE=$(nvm_source script-nvm-exec)</span><br><span class="line"></span><br><span class="line">  <span class="comment"># Downloading to $INSTALL_DIR</span></span><br><span class="line">  mkdir -p <span class="string">"<span class="variable">$INSTALL_DIR</span>"</span></span><br><span class="line">  <span class="keyword">if</span> [ -f <span class="string">"<span class="variable">$INSTALL_DIR</span>/nvm.sh"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; nvm is already installed in <span class="variable">$INSTALL_DIR</span>, trying to update the script"</span></span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Downloading nvm as script to '<span class="variable">$INSTALL_DIR</span>'"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  nvm_download -s <span class="string">"<span class="variable">$NVM_SOURCE_LOCAL</span>"</span> -o <span class="string">"<span class="variable">$INSTALL_DIR</span>/nvm.sh"</span> || &#123;</span><br><span class="line">    <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Failed to download '<span class="variable">$NVM_SOURCE_LOCAL</span>'"</span></span><br><span class="line">    <span class="built_in">return</span> 1</span><br><span class="line">  &#125; &amp;</span><br><span class="line">  nvm_download -s <span class="string">"<span class="variable">$NVM_EXEC_SOURCE</span>"</span> -o <span class="string">"<span class="variable">$INSTALL_DIR</span>/nvm-exec"</span> || &#123;</span><br><span class="line">    <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Failed to download '<span class="variable">$NVM_EXEC_SOURCE</span>'"</span></span><br><span class="line">    <span class="built_in">return</span> 2</span><br><span class="line">  &#125; &amp;</span><br><span class="line">  <span class="keyword">for</span> job <span class="keyword">in</span> $(<span class="built_in">jobs</span> -p | sort)</span><br><span class="line">  <span class="keyword">do</span></span><br><span class="line">    <span class="built_in">wait</span> <span class="string">"<span class="variable">$job</span>"</span> || <span class="built_in">return</span> $?</span><br><span class="line">  <span class="keyword">done</span></span><br><span class="line">  chmod a+x <span class="string">"<span class="variable">$INSTALL_DIR</span>/nvm-exec"</span> || &#123;</span><br><span class="line">    <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Failed to mark '<span class="variable">$INSTALL_DIR</span>/nvm-exec' as executable"</span></span><br><span class="line">    <span class="built_in">return</span> 3</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_try_profile</span></span>() &#123;</span><br><span class="line">  <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$&#123;1-&#125;</span>"</span> ] || [ ! -f <span class="string">"<span class="variable">$&#123;1&#125;</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">return</span> 1</span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"<span class="variable">$&#123;1&#125;</span>"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Detect profile file if not specified as environment variable</span></span><br><span class="line"><span class="comment"># (eg: PROFILE=~/.myprofile)</span></span><br><span class="line"><span class="comment"># The echo'ed path is guaranteed to be an existing file</span></span><br><span class="line"><span class="comment"># Otherwise, an empty string is returned</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_detect_profile</span></span>() &#123;</span><br><span class="line">  <span class="keyword">if</span> [ -n <span class="string">"<span class="variable">$&#123;PROFILE&#125;</span>"</span> ] &amp;&amp; [ -f <span class="string">"<span class="variable">$&#123;PROFILE&#125;</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$&#123;PROFILE&#125;</span>"</span></span><br><span class="line">    <span class="built_in">return</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">local</span> DETECTED_PROFILE</span><br><span class="line">  DETECTED_PROFILE=<span class="string">''</span></span><br><span class="line">  <span class="built_in">local</span> SHELLTYPE</span><br><span class="line">  SHELLTYPE=<span class="string">"<span class="variable">$(basename "/$SHELL")</span>"</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ <span class="string">"<span class="variable">$SHELLTYPE</span>"</span> = <span class="string">"bash"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">if</span> [ -f <span class="string">"<span class="variable">$HOME</span>/.bashrc"</span> ]; <span class="keyword">then</span></span><br><span class="line">      DETECTED_PROFILE=<span class="string">"<span class="variable">$HOME</span>/.bashrc"</span></span><br><span class="line">    <span class="keyword">elif</span> [ -f <span class="string">"<span class="variable">$HOME</span>/.bash_profile"</span> ]; <span class="keyword">then</span></span><br><span class="line">      DETECTED_PROFILE=<span class="string">"<span class="variable">$HOME</span>/.bash_profile"</span></span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">elif</span> [ <span class="string">"<span class="variable">$SHELLTYPE</span>"</span> = <span class="string">"zsh"</span> ]; <span class="keyword">then</span></span><br><span class="line">    DETECTED_PROFILE=<span class="string">"<span class="variable">$HOME</span>/.zshrc"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$DETECTED_PROFILE</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">for</span> EACH_PROFILE <span class="keyword">in</span> <span class="string">".profile"</span> <span class="string">".bashrc"</span> <span class="string">".bash_profile"</span> <span class="string">".zshrc"</span></span><br><span class="line">    <span class="keyword">do</span></span><br><span class="line">      <span class="keyword">if</span> DETECTED_PROFILE=<span class="string">"<span class="variable">$(nvm_try_profile "$&#123;HOME&#125;/$&#123;EACH_PROFILE&#125;")</span>"</span>; <span class="keyword">then</span></span><br><span class="line">        <span class="built_in">break</span></span><br><span class="line">      <span class="keyword">fi</span></span><br><span class="line">    <span class="keyword">done</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ ! -z <span class="string">"<span class="variable">$DETECTED_PROFILE</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$DETECTED_PROFILE</span>"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Check whether the user has any globally-installed npm modules in their system</span></span><br><span class="line"><span class="comment"># Node, and warn them if so.</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_check_global_modules</span></span>() &#123;</span><br><span class="line">  <span class="built_in">command</span> -v npm &gt;/dev/null 2&gt;&amp;1 || <span class="built_in">return</span> 0</span><br><span class="line"></span><br><span class="line">  <span class="built_in">local</span> NPM_VERSION</span><br><span class="line">  NPM_VERSION=<span class="string">"<span class="variable">$(npm --version)</span>"</span></span><br><span class="line">  NPM_VERSION=<span class="string">"<span class="variable">$&#123;NPM_VERSION:--1&#125;</span>"</span></span><br><span class="line">  [ <span class="string">"<span class="variable">$&#123;NPM_VERSION%%[!-0-9]*&#125;</span>"</span> -gt 0 ] || <span class="built_in">return</span> 0</span><br><span class="line"></span><br><span class="line">  <span class="built_in">local</span> NPM_GLOBAL_MODULES</span><br><span class="line">  NPM_GLOBAL_MODULES=<span class="string">"<span class="variable">$(</span></span></span><br><span class="line"><span class="string"><span class="variable">    npm list -g --depth=0 |</span></span></span><br><span class="line"><span class="string"><span class="variable">    command sed -e '/ npm@/d' -e '/ (empty)</span>$/d'</span></span><br><span class="line"><span class="string">  )"</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">local</span> MODULE_COUNT</span><br><span class="line">  MODULE_COUNT=<span class="string">"<span class="variable">$(</span></span></span><br><span class="line"><span class="string"><span class="variable">    command printf %s\\n "$NPM_GLOBAL_MODULES" |</span></span></span><br><span class="line"><span class="string"><span class="variable">    command sed -ne '1!p' |                     # Remove the first line</span></span></span><br><span class="line"><span class="string"><span class="variable">    wc -l | tr -d ' '                           # Count entries</span></span></span><br><span class="line"><span class="string"><span class="variable">  )</span>"</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ <span class="string">"<span class="variable">$&#123;MODULE_COUNT&#125;</span>"</span> != <span class="string">'0'</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; You currently have modules installed globally with `npm`. These will no'</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; longer be linked to the active version of Node when you install a new node'</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; with `nvm`; and they may (depending on how you construct your `$PATH`)'</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; override the binaries of modules installed with `nvm`:'</span></span><br><span class="line">    <span class="built_in">echo</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">command</span> <span class="built_in">printf</span> %s\\n <span class="string">"<span class="variable">$NPM_GLOBAL_MODULES</span>"</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; If you wish to uninstall them at a later point (or re-install them under your'</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; `nvm` Nodes), you can remove them from the system Node as follows:'</span></span><br><span class="line">    <span class="built_in">echo</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'     $ nvm use system'</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'     $ npm uninstall -g a_module'</span></span><br><span class="line">    <span class="built_in">echo</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_do_install</span></span>() &#123;</span><br><span class="line">  <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$&#123;METHOD&#125;</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="comment"># Autodetect install method</span></span><br><span class="line">    <span class="keyword">if</span> nvm_has git; <span class="keyword">then</span></span><br><span class="line">      install_nvm_from_git</span><br><span class="line">    <span class="keyword">elif</span> nvm_has nvm_download; <span class="keyword">then</span></span><br><span class="line">      install_nvm_as_script</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">'You need git, curl, or wget to install nvm'</span></span><br><span class="line">      <span class="built_in">exit</span> 1</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">elif</span> [ <span class="string">"<span class="variable">$&#123;METHOD&#125;</span>"</span> = <span class="string">'git'</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">if</span> ! nvm_has git; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"You need git to install nvm"</span></span><br><span class="line">      <span class="built_in">exit</span> 1</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">    install_nvm_from_git</span><br><span class="line">  <span class="keyword">elif</span> [ <span class="string">"<span class="variable">$&#123;METHOD&#125;</span>"</span> = <span class="string">'script'</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">if</span> ! nvm_has nvm_download; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"You need curl or wget to install nvm"</span></span><br><span class="line">      <span class="built_in">exit</span> 1</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">    install_nvm_as_script</span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">echo</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">local</span> NVM_PROFILE</span><br><span class="line">  NVM_PROFILE=<span class="string">"<span class="variable">$(nvm_detect_profile)</span>"</span></span><br><span class="line">  <span class="built_in">local</span> PROFILE_INSTALL_DIR</span><br><span class="line">  PROFILE_INSTALL_DIR=<span class="string">"<span class="variable">$(nvm_install_dir| sed "s:^$HOME:\$HOME:")</span>"</span></span><br><span class="line"></span><br><span class="line">  SOURCE_STR=<span class="string">"\nexport NVM_DIR=\"<span class="variable">$&#123;PROFILE_INSTALL_DIR&#125;</span>\"\n[ -s \"\$NVM_DIR/nvm.sh\" ] &amp;&amp; \\. \"\$NVM_DIR/nvm.sh\"  # This loads nvm\n"</span></span><br><span class="line">  COMPLETION_STR=<span class="string">"[ -s \"\$NVM_DIR/bash_completion\" ] &amp;&amp; \\. \"\$NVM_DIR/bash_completion\"  # This loads nvm bash_completion\n"</span></span><br><span class="line">  BASH_OR_ZSH=<span class="literal">false</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$&#123;NVM_PROFILE-&#125;</span>"</span> ] ; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Profile not found. Tried <span class="variable">$&#123;NVM_PROFILE&#125;</span> (as defined in \$PROFILE), ~/.bashrc, ~/.bash_profile, ~/.zshrc, and ~/.profile."</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Create one of them and run this script again"</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Create it (touch <span class="variable">$&#123;NVM_PROFILE&#125;</span>) and run this script again"</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"   OR"</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Append the following lines to the correct file yourself:"</span></span><br><span class="line">    <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"<span class="variable">$&#123;SOURCE_STR&#125;</span>"</span></span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    <span class="keyword">if</span> nvm_profile_is_bash_or_zsh <span class="string">"<span class="variable">$&#123;NVM_PROFILE-&#125;</span>"</span>; <span class="keyword">then</span></span><br><span class="line">      BASH_OR_ZSH=<span class="literal">true</span></span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">    <span class="keyword">if</span> ! <span class="built_in">command</span> grep -qc <span class="string">'/nvm.sh'</span> <span class="string">"<span class="variable">$NVM_PROFILE</span>"</span>; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">echo</span> <span class="string">"=&gt; Appending nvm source string to <span class="variable">$NVM_PROFILE</span>"</span></span><br><span class="line">      <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"<span class="variable">$&#123;SOURCE_STR&#125;</span>"</span> &gt;&gt; <span class="string">"<span class="variable">$NVM_PROFILE</span>"</span></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">echo</span> <span class="string">"=&gt; nvm source string already in <span class="variable">$&#123;NVM_PROFILE&#125;</span>"</span></span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="keyword">if</span> <span class="variable">$&#123;BASH_OR_ZSH&#125;</span> &amp;&amp; ! <span class="built_in">command</span> grep -qc <span class="string">'$NVM_DIR/bash_completion'</span> <span class="string">"<span class="variable">$NVM_PROFILE</span>"</span>; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">echo</span> <span class="string">"=&gt; Appending bash_completion source string to <span class="variable">$NVM_PROFILE</span>"</span></span><br><span class="line">      <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"<span class="variable">$COMPLETION_STR</span>"</span> &gt;&gt; <span class="string">"<span class="variable">$NVM_PROFILE</span>"</span></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">echo</span> <span class="string">"=&gt; bash_completion source string already in <span class="variable">$&#123;NVM_PROFILE&#125;</span>"</span></span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">if</span> <span class="variable">$&#123;BASH_OR_ZSH&#125;</span> &amp;&amp; [ -z <span class="string">"<span class="variable">$&#123;NVM_PROFILE-&#125;</span>"</span> ] ; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Please also append the following lines to the if you are using bash/zsh shell:"</span></span><br><span class="line">    <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"<span class="variable">$&#123;COMPLETION_STR&#125;</span>"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="comment"># Source nvm</span></span><br><span class="line">  <span class="comment"># shellcheck source=/dev/null</span></span><br><span class="line">  \. <span class="string">"<span class="variable">$(nvm_install_dir)</span>/nvm.sh"</span></span><br><span class="line"></span><br><span class="line">  nvm_check_global_modules</span><br><span class="line"></span><br><span class="line">  nvm_install_node</span><br><span class="line"></span><br><span class="line">  nvm_reset</span><br><span class="line"></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"=&gt; Close and reopen your terminal to start using nvm or run the following to use it now:"</span></span><br><span class="line">  <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"<span class="variable">$&#123;SOURCE_STR&#125;</span>"</span></span><br><span class="line">  <span class="keyword">if</span> <span class="variable">$&#123;BASH_OR_ZSH&#125;</span> ; the</span><br><span class="line">Linux下安装Node-js.md Preview</span><br><span class="line">title	date	tags	categories</span><br><span class="line">Linux下安装Node.js</span><br><span class="line">Fri May 05 2017 01:51:37 GMT+0800 (CST)</span><br><span class="line">null</span><br><span class="line">-Linux</span><br><span class="line">手动二进制包的安装方法</span><br><span class="line">一般来说我们在Node的官网上是找不到Linux的Deb包，或者是Rpm包的，他针对Windows和mac都有二进制的安装包，但是Linux只有一个tar.xz这个包下载下来虽然也是一个二进制的包，类似于Windows中的绿色软件，也非常好配置，仅仅使用</span><br><span class="line"></span><br><span class="line">￼</span><br><span class="line">tar -xvf package.tar.xz</span><br><span class="line"></span><br><span class="line">即可完成解压，然后使用软连接，把node与npm文件链接到/usr/<span class="built_in">local</span>/bin下面</span><br><span class="line"></span><br><span class="line">￼</span><br><span class="line">ln -s /opt/nodejs/bin/node /usr/<span class="built_in">local</span>/bin/node</span><br><span class="line">ln -s /opt/nodejs/bin/npm /usr/<span class="built_in">local</span>/bin/npm</span><br><span class="line"></span><br><span class="line">这样以来然后可以看看node的版本，从而来判断node是否安装上了，并且看看版本是不是我们需要的版本。</span><br><span class="line"></span><br><span class="line">￼</span><br><span class="line">node -v</span><br><span class="line"></span><br><span class="line">假如出现了类似与以下的信息就说明安装完成</span><br><span class="line"></span><br><span class="line">v6.10.3</span><br><span class="line">使用nvm管理node版本</span><br><span class="line">安装nvm</span><br><span class="line">复制以下代码，放到一个shell脚本中。</span><br><span class="line"></span><br><span class="line">￼</span><br><span class="line"><span class="meta">#!/usr/bin/env bash</span></span><br><span class="line"></span><br><span class="line">&#123; <span class="comment"># this ensures the entire script is downloaded #</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_has</span></span>() &#123;</span><br><span class="line">  <span class="built_in">type</span> <span class="string">"<span class="variable">$1</span>"</span> &gt; /dev/null 2&gt;&amp;1</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_install_dir</span></span>() &#123;</span><br><span class="line">  <span class="built_in">printf</span> %s <span class="string">"<span class="variable">$&#123;NVM_DIR:-"$HOME/.nvm"&#125;</span>"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_latest_version</span></span>() &#123;</span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"v0.33.2"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_profile_is_bash_or_zsh</span></span>() &#123;</span><br><span class="line">  <span class="built_in">local</span> TEST_PROFILE</span><br><span class="line">  TEST_PROFILE=<span class="string">"<span class="variable">$&#123;1-&#125;</span>"</span></span><br><span class="line">  <span class="keyword">case</span> <span class="string">"<span class="variable">$&#123;TEST_PROFILE-&#125;</span>"</span> <span class="keyword">in</span></span><br><span class="line">    *<span class="string">"/.bashrc"</span> | *<span class="string">"/.bash_profile"</span> | *<span class="string">"/.zshrc"</span>)</span><br><span class="line">      <span class="built_in">return</span></span><br><span class="line">    ;;</span><br><span class="line">    *)</span><br><span class="line">      <span class="built_in">return</span> 1</span><br><span class="line">    ;;</span><br><span class="line">  <span class="keyword">esac</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Outputs the location to NVM depending on:</span></span><br><span class="line"><span class="comment"># * The availability of $NVM_SOURCE</span></span><br><span class="line"><span class="comment"># * The method used ("script" or "git" in the script, defaults to "git")</span></span><br><span class="line"><span class="comment"># NVM_SOURCE always takes precedence unless the method is "script-nvm-exec"</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_source</span></span>() &#123;</span><br><span class="line">  <span class="built_in">local</span> NVM_METHOD</span><br><span class="line">  NVM_METHOD=<span class="string">"<span class="variable">$1</span>"</span></span><br><span class="line">  <span class="built_in">local</span> NVM_SOURCE_URL</span><br><span class="line">  NVM_SOURCE_URL=<span class="string">"<span class="variable">$NVM_SOURCE</span>"</span></span><br><span class="line">  <span class="keyword">if</span> [ <span class="string">"_<span class="variable">$NVM_METHOD</span>"</span> = <span class="string">"_script-nvm-exec"</span> ]; <span class="keyword">then</span></span><br><span class="line">    NVM_SOURCE_URL=<span class="string">"https://raw.githubusercontent.com/creationix/nvm/<span class="variable">$(nvm_latest_version)</span>/nvm-exec"</span></span><br><span class="line">  <span class="keyword">elif</span> [ -z <span class="string">"<span class="variable">$NVM_SOURCE_URL</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">if</span> [ <span class="string">"_<span class="variable">$NVM_METHOD</span>"</span> = <span class="string">"_script"</span> ]; <span class="keyword">then</span></span><br><span class="line">      NVM_SOURCE_URL=<span class="string">"https://raw.githubusercontent.com/creationix/nvm/<span class="variable">$(nvm_latest_version)</span>/nvm.sh"</span></span><br><span class="line">    <span class="keyword">elif</span> [ <span class="string">"_<span class="variable">$NVM_METHOD</span>"</span> = <span class="string">"_git"</span> ] || [ -z <span class="string">"<span class="variable">$NVM_METHOD</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">      NVM_SOURCE_URL=<span class="string">"https://github.com/creationix/nvm.git"</span></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Unexpected value \"<span class="variable">$NVM_METHOD</span>\" for \$NVM_METHOD"</span></span><br><span class="line">      <span class="built_in">return</span> 1</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"<span class="variable">$NVM_SOURCE_URL</span>"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Node.js version to install</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_node_version</span></span>() &#123;</span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"<span class="variable">$NODE_VERSION</span>"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_download</span></span>() &#123;</span><br><span class="line">  <span class="keyword">if</span> nvm_has <span class="string">"curl"</span>; <span class="keyword">then</span></span><br><span class="line">    curl --compressed -q <span class="string">"<span class="variable">$@</span>"</span></span><br><span class="line">  <span class="keyword">elif</span> nvm_has <span class="string">"wget"</span>; <span class="keyword">then</span></span><br><span class="line">    <span class="comment"># Emulate curl with wget</span></span><br><span class="line">    ARGS=$(<span class="built_in">echo</span> <span class="string">"$*"</span> | <span class="built_in">command</span> sed -e <span class="string">'s/--progress-bar /--progress=bar /'</span> \</span><br><span class="line">                           -e <span class="string">'s/-L //'</span> \</span><br><span class="line">                           -e <span class="string">'s/--compressed //'</span> \</span><br><span class="line">                           -e <span class="string">'s/-I /--server-response /'</span> \</span><br><span class="line">                           -e <span class="string">'s/-s /-q /'</span> \</span><br><span class="line">                           -e <span class="string">'s/-o /-O /'</span> \</span><br><span class="line">                           -e <span class="string">'s/-C - /-c /'</span>)</span><br><span class="line">    <span class="comment"># shellcheck disable=SC2086</span></span><br><span class="line">    <span class="built_in">eval</span> wget <span class="variable">$ARGS</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">install_nvm_from_git</span></span>() &#123;</span><br><span class="line">  <span class="built_in">local</span> INSTALL_DIR</span><br><span class="line">  INSTALL_DIR=<span class="string">"<span class="variable">$(nvm_install_dir)</span>"</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ -d <span class="string">"<span class="variable">$INSTALL_DIR</span>/.git"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; nvm is already installed in <span class="variable">$INSTALL_DIR</span>, trying to update using git"</span></span><br><span class="line">    <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"\r=&gt; "</span></span><br><span class="line">    <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> fetch 2&gt; /dev/null || &#123;</span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Failed to update nvm, run 'git fetch' in <span class="variable">$INSTALL_DIR</span> yourself."</span></span><br><span class="line">      <span class="built_in">exit</span> 1</span><br><span class="line">    &#125;</span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    <span class="comment"># Cloning to $INSTALL_DIR</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Downloading nvm from git to '<span class="variable">$INSTALL_DIR</span>'"</span></span><br><span class="line">    <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"\r=&gt; "</span></span><br><span class="line">    mkdir -p <span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>"</span></span><br><span class="line">    <span class="keyword">if</span> [ <span class="string">"<span class="variable">$(ls -A "$&#123;INSTALL_DIR&#125;")</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">command</span> git init <span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>"</span> || &#123;</span><br><span class="line">        <span class="built_in">echo</span> &gt;&amp;2 <span class="string">'Failed to initialize nvm repo. Please report this!'</span></span><br><span class="line">        <span class="built_in">exit</span> 2</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>/.git"</span> remote add origin <span class="string">"<span class="variable">$(nvm_source)</span>"</span> 2&gt; /dev/null \</span><br><span class="line">        || <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>/.git"</span> remote <span class="built_in">set</span>-url origin <span class="string">"<span class="variable">$(nvm_source)</span>"</span> || &#123;</span><br><span class="line">        <span class="built_in">echo</span> &gt;&amp;2 <span class="string">'Failed to add remote "origin" (or set the URL). Please report this!'</span></span><br><span class="line">        <span class="built_in">exit</span> 2</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>/.git"</span> fetch origin --tags || &#123;</span><br><span class="line">        <span class="built_in">echo</span> &gt;&amp;2 <span class="string">'Failed to fetch origin with tags. Please report this!'</span></span><br><span class="line">        <span class="built_in">exit</span> 2</span><br><span class="line">      &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">command</span> git <span class="built_in">clone</span> <span class="string">"<span class="variable">$(nvm_source)</span>"</span> <span class="string">"<span class="variable">$&#123;INSTALL_DIR&#125;</span>"</span> || &#123;</span><br><span class="line">        <span class="built_in">echo</span> &gt;&amp;2 <span class="string">'Failed to clone nvm repo. Please report this!'</span></span><br><span class="line">        <span class="built_in">exit</span> 2</span><br><span class="line">      &#125;</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> checkout -f --quiet <span class="string">"<span class="variable">$(nvm_latest_version)</span>"</span></span><br><span class="line">  <span class="keyword">if</span> [ ! -z <span class="string">"<span class="variable">$(command git --git-dir="$INSTALL_DIR"/.git --work-tree="$INSTALL_DIR" show-ref refs/heads/master)</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">if</span> <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> branch --quiet 2&gt;/dev/null; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> branch --quiet -D master &gt;/dev/null 2&gt;&amp;1</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Your version of git is out of date. Please update it!"</span></span><br><span class="line">      <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> branch -D master &gt;/dev/null 2&gt;&amp;1</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"=&gt; Compressing and cleaning up git repository"</span></span><br><span class="line">  <span class="keyword">if</span> ! <span class="built_in">command</span> git --git-dir=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span>/.git --work-tree=<span class="string">"<span class="variable">$INSTALL_DIR</span>"</span> gc --aggressive --prune=now ; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Your version of git is out of date. Please update it!"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  <span class="built_in">return</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Automatically install Node.js</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_install_node</span></span>() &#123;</span><br><span class="line">  <span class="built_in">local</span> NODE_VERSION</span><br><span class="line">  NODE_VERSION=<span class="string">"<span class="variable">$(nvm_node_version)</span>"</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$NODE_VERSION</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">return</span> 0</span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"=&gt; Installing Node.js version <span class="variable">$NODE_VERSION</span>"</span></span><br><span class="line">  nvm install <span class="string">"<span class="variable">$NODE_VERSION</span>"</span></span><br><span class="line">  <span class="built_in">local</span> CURRENT_NVM_NODE</span><br><span class="line"></span><br><span class="line">  CURRENT_NVM_NODE=<span class="string">"<span class="variable">$(nvm_version current)</span>"</span></span><br><span class="line">  <span class="keyword">if</span> [ <span class="string">"<span class="variable">$(nvm_version "$NODE_VERSION")</span>"</span> == <span class="string">"<span class="variable">$CURRENT_NVM_NODE</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Node.js version <span class="variable">$NODE_VERSION</span> has been successfully installed"</span></span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Failed to install Node.js <span class="variable">$NODE_VERSION</span>"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">install_nvm_as_script</span></span>() &#123;</span><br><span class="line">  <span class="built_in">local</span> INSTALL_DIR</span><br><span class="line">  INSTALL_DIR=<span class="string">"<span class="variable">$(nvm_install_dir)</span>"</span></span><br><span class="line">  <span class="built_in">local</span> NVM_SOURCE_LOCAL</span><br><span class="line">  NVM_SOURCE_LOCAL=$(nvm_source script)</span><br><span class="line">  <span class="built_in">local</span> NVM_EXEC_SOURCE</span><br><span class="line">  NVM_EXEC_SOURCE=$(nvm_source script-nvm-exec)</span><br><span class="line"></span><br><span class="line">  <span class="comment"># Downloading to $INSTALL_DIR</span></span><br><span class="line">  mkdir -p <span class="string">"<span class="variable">$INSTALL_DIR</span>"</span></span><br><span class="line">  <span class="keyword">if</span> [ -f <span class="string">"<span class="variable">$INSTALL_DIR</span>/nvm.sh"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; nvm is already installed in <span class="variable">$INSTALL_DIR</span>, trying to update the script"</span></span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Downloading nvm as script to '<span class="variable">$INSTALL_DIR</span>'"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  nvm_download -s <span class="string">"<span class="variable">$NVM_SOURCE_LOCAL</span>"</span> -o <span class="string">"<span class="variable">$INSTALL_DIR</span>/nvm.sh"</span> || &#123;</span><br><span class="line">    <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Failed to download '<span class="variable">$NVM_SOURCE_LOCAL</span>'"</span></span><br><span class="line">    <span class="built_in">return</span> 1</span><br><span class="line">  &#125; &amp;</span><br><span class="line">  nvm_download -s <span class="string">"<span class="variable">$NVM_EXEC_SOURCE</span>"</span> -o <span class="string">"<span class="variable">$INSTALL_DIR</span>/nvm-exec"</span> || &#123;</span><br><span class="line">    <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Failed to download '<span class="variable">$NVM_EXEC_SOURCE</span>'"</span></span><br><span class="line">    <span class="built_in">return</span> 2</span><br><span class="line">  &#125; &amp;</span><br><span class="line">  <span class="keyword">for</span> job <span class="keyword">in</span> $(<span class="built_in">jobs</span> -p | sort)</span><br><span class="line">  <span class="keyword">do</span></span><br><span class="line">    <span class="built_in">wait</span> <span class="string">"<span class="variable">$job</span>"</span> || <span class="built_in">return</span> $?</span><br><span class="line">  <span class="keyword">done</span></span><br><span class="line">  chmod a+x <span class="string">"<span class="variable">$INSTALL_DIR</span>/nvm-exec"</span> || &#123;</span><br><span class="line">    <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"Failed to mark '<span class="variable">$INSTALL_DIR</span>/nvm-exec' as executable"</span></span><br><span class="line">    <span class="built_in">return</span> 3</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_try_profile</span></span>() &#123;</span><br><span class="line">  <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$&#123;1-&#125;</span>"</span> ] || [ ! -f <span class="string">"<span class="variable">$&#123;1&#125;</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">return</span> 1</span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"<span class="variable">$&#123;1&#125;</span>"</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Detect profile file if not specified as environment variable</span></span><br><span class="line"><span class="comment"># (eg: PROFILE=~/.myprofile)</span></span><br><span class="line"><span class="comment"># The echo'ed path is guaranteed to be an existing file</span></span><br><span class="line"><span class="comment"># Otherwise, an empty string is returned</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_detect_profile</span></span>() &#123;</span><br><span class="line">  <span class="keyword">if</span> [ -n <span class="string">"<span class="variable">$&#123;PROFILE&#125;</span>"</span> ] &amp;&amp; [ -f <span class="string">"<span class="variable">$&#123;PROFILE&#125;</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$&#123;PROFILE&#125;</span>"</span></span><br><span class="line">    <span class="built_in">return</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">local</span> DETECTED_PROFILE</span><br><span class="line">  DETECTED_PROFILE=<span class="string">''</span></span><br><span class="line">  <span class="built_in">local</span> SHELLTYPE</span><br><span class="line">  SHELLTYPE=<span class="string">"<span class="variable">$(basename "/$SHELL")</span>"</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ <span class="string">"<span class="variable">$SHELLTYPE</span>"</span> = <span class="string">"bash"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">if</span> [ -f <span class="string">"<span class="variable">$HOME</span>/.bashrc"</span> ]; <span class="keyword">then</span></span><br><span class="line">      DETECTED_PROFILE=<span class="string">"<span class="variable">$HOME</span>/.bashrc"</span></span><br><span class="line">    <span class="keyword">elif</span> [ -f <span class="string">"<span class="variable">$HOME</span>/.bash_profile"</span> ]; <span class="keyword">then</span></span><br><span class="line">      DETECTED_PROFILE=<span class="string">"<span class="variable">$HOME</span>/.bash_profile"</span></span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">elif</span> [ <span class="string">"<span class="variable">$SHELLTYPE</span>"</span> = <span class="string">"zsh"</span> ]; <span class="keyword">then</span></span><br><span class="line">    DETECTED_PROFILE=<span class="string">"<span class="variable">$HOME</span>/.zshrc"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$DETECTED_PROFILE</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">for</span> EACH_PROFILE <span class="keyword">in</span> <span class="string">".profile"</span> <span class="string">".bashrc"</span> <span class="string">".bash_profile"</span> <span class="string">".zshrc"</span></span><br><span class="line">    <span class="keyword">do</span></span><br><span class="line">      <span class="keyword">if</span> DETECTED_PROFILE=<span class="string">"<span class="variable">$(nvm_try_profile "$&#123;HOME&#125;/$&#123;EACH_PROFILE&#125;")</span>"</span>; <span class="keyword">then</span></span><br><span class="line">        <span class="built_in">break</span></span><br><span class="line">      <span class="keyword">fi</span></span><br><span class="line">    <span class="keyword">done</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ ! -z <span class="string">"<span class="variable">$DETECTED_PROFILE</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$DETECTED_PROFILE</span>"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Check whether the user has any globally-installed npm modules in their system</span></span><br><span class="line"><span class="comment"># Node, and warn them if so.</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_check_global_modules</span></span>() &#123;</span><br><span class="line">  <span class="built_in">command</span> -v npm &gt;/dev/null 2&gt;&amp;1 || <span class="built_in">return</span> 0</span><br><span class="line"></span><br><span class="line">  <span class="built_in">local</span> NPM_VERSION</span><br><span class="line">  NPM_VERSION=<span class="string">"<span class="variable">$(npm --version)</span>"</span></span><br><span class="line">  NPM_VERSION=<span class="string">"<span class="variable">$&#123;NPM_VERSION:--1&#125;</span>"</span></span><br><span class="line">  [ <span class="string">"<span class="variable">$&#123;NPM_VERSION%%[!-0-9]*&#125;</span>"</span> -gt 0 ] || <span class="built_in">return</span> 0</span><br><span class="line"></span><br><span class="line">  <span class="built_in">local</span> NPM_GLOBAL_MODULES</span><br><span class="line">  NPM_GLOBAL_MODULES=<span class="string">"<span class="variable">$(</span></span></span><br><span class="line"><span class="string"><span class="variable">    npm list -g --depth=0 |</span></span></span><br><span class="line"><span class="string"><span class="variable">    command sed -e '/ npm@/d' -e '/ (empty)</span>$/d'</span></span><br><span class="line"><span class="string">  )"</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">local</span> MODULE_COUNT</span><br><span class="line">  MODULE_COUNT=<span class="string">"<span class="variable">$(</span></span></span><br><span class="line"><span class="string"><span class="variable">    command printf %s\\n "$NPM_GLOBAL_MODULES" |</span></span></span><br><span class="line"><span class="string"><span class="variable">    command sed -ne '1!p' |                     # Remove the first line</span></span></span><br><span class="line"><span class="string"><span class="variable">    wc -l | tr -d ' '                           # Count entries</span></span></span><br><span class="line"><span class="string"><span class="variable">  )</span>"</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ <span class="string">"<span class="variable">$&#123;MODULE_COUNT&#125;</span>"</span> != <span class="string">'0'</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; You currently have modules installed globally with `npm`. These will no'</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; longer be linked to the active version of Node when you install a new node'</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; with `nvm`; and they may (depending on how you construct your `$PATH`)'</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; override the binaries of modules installed with `nvm`:'</span></span><br><span class="line">    <span class="built_in">echo</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">command</span> <span class="built_in">printf</span> %s\\n <span class="string">"<span class="variable">$NPM_GLOBAL_MODULES</span>"</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; If you wish to uninstall them at a later point (or re-install them under your'</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'=&gt; `nvm` Nodes), you can remove them from the system Node as follows:'</span></span><br><span class="line">    <span class="built_in">echo</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'     $ nvm use system'</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">'     $ npm uninstall -g a_module'</span></span><br><span class="line">    <span class="built_in">echo</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="title">nvm_do_install</span></span>() &#123;</span><br><span class="line">  <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$&#123;METHOD&#125;</span>"</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="comment"># Autodetect install method</span></span><br><span class="line">    <span class="keyword">if</span> nvm_has git; <span class="keyword">then</span></span><br><span class="line">      install_nvm_from_git</span><br><span class="line">    <span class="keyword">elif</span> nvm_has nvm_download; <span class="keyword">then</span></span><br><span class="line">      install_nvm_as_script</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">'You need git, curl, or wget to install nvm'</span></span><br><span class="line">      <span class="built_in">exit</span> 1</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">elif</span> [ <span class="string">"<span class="variable">$&#123;METHOD&#125;</span>"</span> = <span class="string">'git'</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">if</span> ! nvm_has git; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"You need git to install nvm"</span></span><br><span class="line">      <span class="built_in">exit</span> 1</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">    install_nvm_from_git</span><br><span class="line">  <span class="keyword">elif</span> [ <span class="string">"<span class="variable">$&#123;METHOD&#125;</span>"</span> = <span class="string">'script'</span> ]; <span class="keyword">then</span></span><br><span class="line">    <span class="keyword">if</span> ! nvm_has nvm_download; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">echo</span> &gt;&amp;2 <span class="string">"You need curl or wget to install nvm"</span></span><br><span class="line">      <span class="built_in">exit</span> 1</span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">    install_nvm_as_script</span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">echo</span></span><br><span class="line"></span><br><span class="line">  <span class="built_in">local</span> NVM_PROFILE</span><br><span class="line">  NVM_PROFILE=<span class="string">"<span class="variable">$(nvm_detect_profile)</span>"</span></span><br><span class="line">  <span class="built_in">local</span> PROFILE_INSTALL_DIR</span><br><span class="line">  PROFILE_INSTALL_DIR=<span class="string">"<span class="variable">$(nvm_install_dir| sed "s:^$HOME:\$HOME:")</span>"</span></span><br><span class="line"></span><br><span class="line">  SOURCE_STR=<span class="string">"\nexport NVM_DIR=\"<span class="variable">$&#123;PROFILE_INSTALL_DIR&#125;</span>\"\n[ -s \"\$NVM_DIR/nvm.sh\" ] &amp;&amp; \\. \"\$NVM_DIR/nvm.sh\"  # This loads nvm\n"</span></span><br><span class="line">  COMPLETION_STR=<span class="string">"[ -s \"\$NVM_DIR/bash_completion\" ] &amp;&amp; \\. \"\$NVM_DIR/bash_completion\"  # This loads nvm bash_completion\n"</span></span><br><span class="line">  BASH_OR_ZSH=<span class="literal">false</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$&#123;NVM_PROFILE-&#125;</span>"</span> ] ; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Profile not found. Tried <span class="variable">$&#123;NVM_PROFILE&#125;</span> (as defined in \$PROFILE), ~/.bashrc, ~/.bash_profile, ~/.zshrc, and ~/.profile."</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Create one of them and run this script again"</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Create it (touch <span class="variable">$&#123;NVM_PROFILE&#125;</span>) and run this script again"</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"   OR"</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Append the following lines to the correct file yourself:"</span></span><br><span class="line">    <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"<span class="variable">$&#123;SOURCE_STR&#125;</span>"</span></span><br><span class="line">  <span class="keyword">else</span></span><br><span class="line">    <span class="keyword">if</span> nvm_profile_is_bash_or_zsh <span class="string">"<span class="variable">$&#123;NVM_PROFILE-&#125;</span>"</span>; <span class="keyword">then</span></span><br><span class="line">      BASH_OR_ZSH=<span class="literal">true</span></span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">    <span class="keyword">if</span> ! <span class="built_in">command</span> grep -qc <span class="string">'/nvm.sh'</span> <span class="string">"<span class="variable">$NVM_PROFILE</span>"</span>; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">echo</span> <span class="string">"=&gt; Appending nvm source string to <span class="variable">$NVM_PROFILE</span>"</span></span><br><span class="line">      <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"<span class="variable">$&#123;SOURCE_STR&#125;</span>"</span> &gt;&gt; <span class="string">"<span class="variable">$NVM_PROFILE</span>"</span></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">echo</span> <span class="string">"=&gt; nvm source string already in <span class="variable">$&#123;NVM_PROFILE&#125;</span>"</span></span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">    <span class="comment"># shellcheck disable=SC2016</span></span><br><span class="line">    <span class="keyword">if</span> <span class="variable">$&#123;BASH_OR_ZSH&#125;</span> &amp;&amp; ! <span class="built_in">command</span> grep -qc <span class="string">'$NVM_DIR/bash_completion'</span> <span class="string">"<span class="variable">$NVM_PROFILE</span>"</span>; <span class="keyword">then</span></span><br><span class="line">      <span class="built_in">echo</span> <span class="string">"=&gt; Appending bash_completion source string to <span class="variable">$NVM_PROFILE</span>"</span></span><br><span class="line">      <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"<span class="variable">$COMPLETION_STR</span>"</span> &gt;&gt; <span class="string">"<span class="variable">$NVM_PROFILE</span>"</span></span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">      <span class="built_in">echo</span> <span class="string">"=&gt; bash_completion source string already in <span class="variable">$&#123;NVM_PROFILE&#125;</span>"</span></span><br><span class="line">    <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">  <span class="keyword">if</span> <span class="variable">$&#123;BASH_OR_ZSH&#125;</span> &amp;&amp; [ -z <span class="string">"<span class="variable">$&#123;NVM_PROFILE-&#125;</span>"</span> ] ; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"=&gt; Please also append the following lines to the if you are using bash/zsh shell:"</span></span><br><span class="line">    <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"<span class="variable">$&#123;COMPLETION_STR&#125;</span>"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line">  <span class="comment"># Source nvm</span></span><br><span class="line">  <span class="comment"># shellcheck source=/dev/null</span></span><br><span class="line">  \. <span class="string">"<span class="variable">$(nvm_install_dir)</span>/nvm.sh"</span></span><br><span class="line"></span><br><span class="line">  nvm_check_global_modules</span><br><span class="line"></span><br><span class="line">  nvm_install_node</span><br><span class="line"></span><br><span class="line">  nvm_reset</span><br><span class="line"></span><br><span class="line">  <span class="built_in">echo</span> <span class="string">"=&gt; Close and reopen your terminal to start using nvm or run the following to use it now:"</span></span><br><span class="line">  <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"<span class="variable">$&#123;SOURCE_STR&#125;</span>"</span></span><br><span class="line">  <span class="keyword">if</span> <span class="variable">$&#123;BASH_OR_ZSH&#125;</span> ; <span class="keyword">then</span></span><br><span class="line">    <span class="built_in">command</span> <span class="built_in">printf</span> <span class="string">"<span class="variable">$&#123;COMPLETION_STR&#125;</span>"</span></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Unsets the various functions defined</span></span><br><span class="line"><span class="comment"># during the execution of the install script</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_reset</span></span>() &#123;</span><br><span class="line">  <span class="built_in">unset</span> -f nvm_has nvm_install_dir nvm_latest_version nvm_profile_is_bash_or_zsh \</span><br><span class="line">    nvm_source nvm_node_version nvm_download install_nvm_from_git nvm_install_node \</span><br><span class="line">    install_nvm_as_script nvm_try_profile nvm_detect_profile nvm_check_global_modules \</span><br><span class="line">    nvm_do_install nvm_reset</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">[ <span class="string">"_<span class="variable">$NVM_ENV</span>"</span> = <span class="string">"_testing"</span> ] || nvm_do_install</span><br><span class="line"></span><br><span class="line">&#125; <span class="comment"># this ensures the entire script is downloaded #</span></span><br><span class="line"></span><br><span class="line">安装node</span><br><span class="line"></span><br><span class="line">  <span class="keyword">fi</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="comment"># Unsets the various functions defined</span></span><br><span class="line"><span class="comment"># during the execution of the install script</span></span><br><span class="line"><span class="comment">#</span></span><br><span class="line"><span class="function"><span class="title">nvm_reset</span></span>() &#123;</span><br><span class="line">  <span class="built_in">unset</span> -f nvm_has nvm_install_dir nvm_latest_version nvm_profile_is_bash_or_zsh \</span><br><span class="line">    nvm_source nvm_node_version nvm_download install_nvm_from_git nvm_install_node \</span><br><span class="line">    install_nvm_as_script nvm_try_profile nvm_detect_profile nvm_check_global_modules \</span><br><span class="line">    nvm_do_install nvm_reset</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">[ <span class="string">"_<span class="variable">$NVM_ENV</span>"</span> = <span class="string">"_testing"</span> ] || nvm_do_install</span><br><span class="line"></span><br><span class="line">&#125; <span class="comment"># this ensures the entire script is downloaded #</span></span><br></pre></td></tr></table></figure>

<h3 id="安装node"><a href="#安装node" class="headerlink" title="安装node"></a>安装node</h3><p>如果执行过程没有报错的话就说明nvm安装成功，接下来就开始安装node，并且切换到此版本</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nvm install  v6.10.3</span><br><span class="line">nvm use v6.10.3</span><br></pre></td></tr></table></figure>

]]></content>
      <categories>
        <category>-Linux</category>
      </categories>
  </entry>
  <entry>
    <title>markdown语法</title>
    <url>/2017/04/27/Tools/markdown%E8%AF%AD%E6%B3%95/</url>
    <content><![CDATA[<p>Markdown 语法说明 (简体中文版) / (点击查看快速入门)</p>
<p>概述<br>宗旨<br>兼容 HTML<br>特殊字符自动转换<br>区块元素<br>段落和换行<br>标题<br>区块引用<br>列表<br>代码区块<br>分隔线<br>区段元素<br>链接<br>强调<br>代码<br>图片<br>其它<br>反斜杠<br>自动链接<br>感谢<br>概述</p>
<a id="more"></a>
<p>宗旨</p>
<p>Markdown 的目标是实现「易读易写」。</p>
<p>可读性，无论如何，都是最重要的。一份使用 Markdown 格式撰写的文件应该可以直接以纯文本发布，并且看起来不会像是由许多标签或是格式指令所构成。Markdown 语法受到一些既有 text-to-HTML 格式的影响，包括 Setext、atx、Textile、reStructuredText、Grutatext 和 EtText，而最大灵感来源其实是纯文本电子邮件的格式。</p>
<p>总之， Markdown 的语法全由一些符号所组成，这些符号经过精挑细选，其作用一目了然。比如：在文字两旁加上星号，看起来就像<em>强调</em>。Markdown 的列表看起来，嗯，就是列表。Markdown 的区块引用看起来就真的像是引用一段文字，就像你曾在电子邮件中见过的那样。</p>
<p>兼容 HTML</p>
<p>Markdown 语法的目标是：成为一种适用于网络的书写语言。</p>
<p>Markdown 不是想要取代 HTML，甚至也没有要和它相近，它的语法种类很少，只对应 HTML 标记的一小部分。Markdown 的构想不是要使得 HTML 文档更容易书写。在我看来， HTML 已经很容易写了。Markdown 的理念是，能让文档更容易读、写和随意改。HTML 是一种发布的格式，Markdown 是一种书写的格式。就这样，Markdown 的格式语法只涵盖纯文本可以涵盖的范围。</p>
<p>不在 Markdown 涵盖范围之内的标签，都可以直接在文档里面用 HTML 撰写。不需要额外标注这是 HTML 或是 Markdown；只要直接加标签就可以了。</p>
<p>要制约的只有一些 HTML 区块元素――比如 <div>、<table>、<pre>、<p> 等标签，必须在前后加上空行与其它内容区隔开，还要求它们的开始标签与结尾标签不能用制表符或空格来缩进。Markdown 的生成器有足够智能，不会在 HTML 区块标签外加上不必要的 </p><p> 标签。</p>
<p>例子如下，在 Markdown 文件里加上一段 HTML 表格：</p>
<p>这是一个普通段落。</p>
<table>
    <tr>
        <td>Foo</td>
    </tr>
</table>

<p>这是另一个普通段落。<br>请注意，在 HTML 区块标签间的 Markdown 格式语法将不会被处理。比如，你在 HTML 区块内使用 Markdown 样式的<em>强调</em>会没有效果。</p>
<p>HTML 的区段（行内）标签如 <span>、<cite>、<del> 可以在 Markdown 的段落、列表或是标题里随意使用。依照个人习惯，甚至可以不用 Markdown 格式，而直接采用 HTML 标签来格式化。举例说明：如果比较喜欢 HTML 的 <a> 或 <img> 标签，可以直接使用这些标签，而不用 Markdown 提供的链接或是图像标签语法。</a></del></cite></span></p>
<p>和处在 HTML 区块标签间不同，Markdown 语法在 HTML 区段标签间是有效的。</p>
<p>特殊字符自动转换</p>
<p>在 HTML 文件中，有两个字符需要特殊处理： < 和 & 。 < 符号用于起始标签，& 符号则用于标记 HTML 实体，如果你只是想要显示这些字符的原型，你必须要使用实体的形式，像是 &lt; 和 &amp;。</p>
<p>& 字符尤其让网络文档编写者受折磨，如果你要打「AT&T」 ，你必须要写成「AT&amp;T」。而网址中的 & 字符也要转换。比如你要链接到：</p>
<p><a href="http://images.google.com/images?num=30&amp;q=larry+bird" target="_blank" rel="noopener">http://images.google.com/images?num=30&amp;q=larry+bird</a><br>你必须要把网址转换写为：</p>
<p><a href="http://images.google.com/images?num=30&amp;q=larry+bird" target="_blank" rel="noopener">http://images.google.com/images?num=30&amp;q=larry+bird</a><br>才能放到链接标签的 href 属性里。不用说也知道这很容易忽略，这也可能是 HTML 标准检验所检查到的错误中，数量最多的。</p>
<p>Markdown 让你可以自然地书写字符，需要转换的由它来处理好了。如果你使用的 & 字符是 HTML 字符实体的一部分，它会保留原状，否则它会被转换成 &amp;。</p>
<p>所以你如果要在文档中插入一个版权符号 ©，你可以这样写：</p>
<p>&copy;<br>Markdown 会保留它不动。而若你写：</p>
<p>AT&T<br>Markdown 就会将它转为：</p>
<p>AT&amp;T<br>类似的状况也会发生在 < 符号上，因为 Markdown 允许 兼容 HTML ，如果你是把 < 符号作为 HTML 标签的定界符使用，那 Markdown 也不会对它做任何转换，但是如果你写：</p>
<p>4 < 5<br>Markdown 将会把它转换为：</p>
<p>4 &lt; 5<br>不过需要注意的是，code 范围内，不论是行内还是区块， < 和 & 两个符号都一定会被转换成 HTML 实体，这项特性让你可以很容易地用 Markdown 写 HTML code （和 HTML 相对而言， HTML 语法中，你要把所有的 < 和 & 都转换为 HTML 实体，才能在 HTML 文件里面写出 HTML code。）</p>
<p>区块元素</p>
<p>段落和换行</p>
<p>一个 Markdown 段落是由一个或多个连续的文本行组成，它的前后要有一个以上的空行（空行的定义是显示上看起来像是空的，便会被视为空行。比方说，若某一行只包含空格和制表符，则该行也会被视为空行）。普通段落不该用空格或制表符来缩进。</p>
<p>「由一个或多个连续的文本行组成」这句话其实暗示了 Markdown 允许段落内的强迫换行（插入换行符），这个特性和其他大部分的 text-to-HTML 格式不一样（包括 Movable Type 的「Convert Line Breaks」选项），其它的格式会把每个换行符都转成 <br> 标签。</p>
<p>如果你确实想要依赖 Markdown 来插入 <br> 标签的话，在插入处先按入两个以上的空格然后回车。</p>
<p>的确，需要多费点事（多加空格）来产生 <br> ，但是简单地「每个换行都转换为 <br>」的方法在 Markdown 中并不适合， Markdown 中 email 式的 区块引用 和多段落的 列表 在使用换行来排版的时候，不但更好用，还更方便阅读。</p>
<p>标题</p>
<p>Markdown 支持两种标题的语法，类 Setext 和类 atx 形式。</p>
<p>类 Setext 形式是用底线的形式，利用 = （最高阶标题）和 - （第二阶标题），例如：</p>
<h1 id="This-is-an-H1"><a href="#This-is-an-H1" class="headerlink" title="This is an H1"></a>This is an H1</h1><h2 id="This-is-an-H2"><a href="#This-is-an-H2" class="headerlink" title="This is an H2"></a>This is an H2</h2><p>任何数量的 = 和 - 都可以有效果。</p>
<p>类 Atx 形式则是在行首插入 1 到 6 个 # ，对应到标题 1 到 6 阶，例如：</p>
<h1 id="这是-H1"><a href="#这是-H1" class="headerlink" title="这是 H1"></a>这是 H1</h1><h2 id="这是-H2"><a href="#这是-H2" class="headerlink" title="这是 H2"></a>这是 H2</h2><h6 id="这是-H6"><a href="#这是-H6" class="headerlink" title="这是 H6"></a>这是 H6</h6><p>你可以选择性地「闭合」类 atx 样式的标题，这纯粹只是美观用的，若是觉得这样看起来比较舒适，你就可以在行尾加上 #，而行尾的 # 数量也不用和开头一样（行首的井字符数量决定标题的阶数）：</p>
<h1 id="这是-H1-1"><a href="#这是-H1-1" class="headerlink" title="这是 H1"></a>这是 H1</h1><h2 id="这是-H2-1"><a href="#这是-H2-1" class="headerlink" title="这是 H2"></a>这是 H2</h2><h3 id="这是-H3"><a href="#这是-H3" class="headerlink" title="这是 H3"></a>这是 H3</h3><p>区块引用 Blockquotes</p>
<p>Markdown 标记区块引用是使用类似 email 中用 > 的引用方式。如果你还熟悉在 email 信件中的引言部分，你就知道怎么在 Markdown 文件中建立一个区块引用，那会看起来像是你自己先断好行，然后在每行的最前面加上 > ：</p>
<blockquote>
<p>This is a blockquote with two paragraphs. Lorem ipsum dolor sit amet,<br>consectetuer adipiscing elit. Aliquam hendrerit mi posuere lectus.<br>Vestibulum enim wisi, viverra nec, fringilla in, laoreet vitae, risus.</p>
<p>Donec sit amet nisl. Aliquam semper ipsum sit amet velit. Suspendisse<br>id sem consectetuer libero luctus adipiscing.<br>Markdown 也允许你偷懒只在整个段落的第一行最前面加上 > ：</p>
</blockquote>
<blockquote>
<p>This is a blockquote with two paragraphs. Lorem ipsum dolor sit amet,<br>consectetuer adipiscing elit. Aliquam hendrerit mi posuere lectus.<br>Vestibulum enim wisi, viverra nec, fringilla in, laoreet vitae, risus.</p>
</blockquote>
<blockquote>
<p>Donec sit amet nisl. Aliquam semper ipsum sit amet velit. Suspendisse<br>id sem consectetuer libero luctus adipiscing.<br>区块引用可以嵌套（例如：引用内的引用），只要根据层次加上不同数量的 > ：</p>
</blockquote>
<blockquote>
<p>This is the first level of quoting.</p>
<blockquote>
<p>This is nested blockquote.</p>
</blockquote>
<p>Back to the first level.<br>引用的区块内也可以使用其他的 Markdown 语法，包括标题、列表、代码区块等：</p>
</blockquote>
<blockquote>
<h2 id="这是一个标题。"><a href="#这是一个标题。" class="headerlink" title="这是一个标题。"></a>这是一个标题。</h2><ol>
<li>这是第一行列表项。</li>
<li>这是第二行列表项。</li>
</ol>
<p>给出一些例子代码：</p>
<pre><code>return shell_exec(&quot;echo $input | $markdown_script&quot;);</code></pre><p>任何像样的文本编辑器都能轻松地建立 email 型的引用。例如在 BBEdit 中，你可以选取文字后然后从选单中选择增加引用阶层。</p>
</blockquote>
<p>列表</p>
<p>Markdown 支持有序列表和无序列表。</p>
<p>无序列表使用星号、加号或是减号作为列表标记：</p>
<ul>
<li>Red</li>
<li>Green</li>
<li>Blue<br>等同于：</li>
</ul>
<ul>
<li>Red</li>
<li>Green</li>
<li>Blue<br>也等同于：</li>
</ul>
<ul>
<li>Red</li>
<li>Green</li>
<li>Blue<br>有序列表则使用数字接着一个英文句点：</li>
</ul>
<ol>
<li>Bird</li>
<li>McHale</li>
<li>Parish<br>很重要的一点是，你在列表标记上使用的数字并不会影响输出的 HTML 结果，上面的列表所产生的 HTML 标记为：</li>
</ol>
<ol>
<li>Bird</li>
<li>McHale</li>
<li>Parish</li>
</ol>
如果你的列表标记写成：

<ol>
<li><p>Bird</p>
</li>
<li><p>McHale</p>
</li>
<li><p>Parish<br>或甚至是：</p>
</li>
<li><p>Bird</p>
</li>
<li><p>McHale</p>
</li>
<li><p>Parish<br>你都会得到完全相同的 HTML 输出。重点在于，你可以让 Markdown 文件的列表数字和输出的结果相同，或是你懒一点，你可以完全不用在意数字的正确性。</p>
</li>
</ol>
<p>如果你使用懒惰的写法，建议第一个项目最好还是从 1. 开始，因为 Markdown 未来可能会支持有序列表的 start 属性。</p>
<p>列表项目标记通常是放在最左边，但是其实也可以缩进，最多 3 个空格，项目标记后面则一定要接着至少一个空格或制表符。</p>
<p>要让列表看起来更漂亮，你可以把内容用固定的缩进整理好：</p>
<ul>
<li><p>Lorem ipsum dolor sit amet, consectetuer adipiscing elit.<br>Aliquam hendrerit mi posuere lectus. Vestibulum enim wisi,<br>viverra nec, fringilla in, laoreet vitae, risus.</p>
</li>
<li><p>Donec sit amet nisl. Aliquam semper ipsum sit amet velit.<br>Suspendisse id sem consectetuer libero luctus adipiscing.<br>但是如果你懒，那也行：</p>
</li>
<li><p>Lorem ipsum dolor sit amet, consectetuer adipiscing elit.<br>Aliquam hendrerit mi posuere lectus. Vestibulum enim wisi,<br>viverra nec, fringilla in, laoreet vitae, risus.</p>
</li>
<li><p>Donec sit amet nisl. Aliquam semper ipsum sit amet velit.<br>Suspendisse id sem consectetuer libero luctus adipiscing.<br>如果列表项目间用空行分开，在输出 HTML 时 Markdown 就会将项目内容用 </p><p> 标签包起来，举例来说：</p>
</li>
<li><p>Bird</p>
</li>
<li><p>Magic<br>会被转换为：</p>
</li>
</ul>
<ul>
<li>Bird</li>
<li>Magic</li>
</ul>
但是这个：

<ul>
<li><p>Bird</p>
</li>
<li><p>Magic<br>会被转换为：</p>
</li>
</ul>
<ul>
<li><p>Bird</p></li>
<li><p>Magic</p></li>
</ul>
列表项目可以包含多个段落，每个项目下的段落都必须缩进 4 个空格或是 1 个制表符：

<ol>
<li><p>This is a list item with two paragraphs. Lorem ipsum dolor<br>sit amet, consectetuer adipiscing elit. Aliquam hendrerit<br>mi posuere lectus.</p>
<p>Vestibulum enim wisi, viverra nec, fringilla in, laoreet<br>vitae, risus. Donec sit amet nisl. Aliquam semper ipsum<br>sit amet velit.</p>
</li>
<li><p>Suspendisse id sem consectetuer libero luctus adipiscing.<br>如果你每行都有缩进，看起来会看好很多，当然，再次地，如果你很懒惰，Markdown 也允许：</p>
</li>
</ol>
<ul>
<li><p>This is a list item with two paragraphs.</p>
<p>This is the second paragraph in the list item. You're<br>only required to indent the first line. Lorem ipsum dolor<br>sit amet, consectetuer adipiscing elit.</p>
</li>
<li><p>Another item in the same list.<br>如果要在列表项目内放进引用，那 > 就需要缩进：</p>
</li>
<li><p>A list item with a blockquote:</p>
<blockquote>
<p>This is a blockquote<br>inside a list item.<br>如果要放代码区块的话，该区块就需要缩进两次，也就是 8 个空格或是 2 个制表符：</p>
</blockquote>
</li>
<li><p>一列表项包含一个列表区块：</p>
<pre><code>&lt;代码写在这&gt;</code></pre><p>当然，项目列表很可能会不小心产生，像是下面这样的写法：</p>
</li>
</ul>
<ol start="1986">
<li>What a great season.<br>换句话说，也就是在行首出现数字-句点-空白，要避免这样的状况，你可以在句点前面加上反斜杠。</li>
</ol>
<p>1986. What a great season.<br>代码区块</p>
<p>和程序相关的写作或是标签语言原始码通常会有已经排版好的代码区块，通常这些区块我们并不希望它以一般段落文件的方式去排版，而是照原来的样子显示，Markdown 会用 <pre> 和 <code> 标签来把代码区块包起来。</code></pre></p>
<p>要在 Markdown 中建立代码区块很简单，只要简单地缩进 4 个空格或是 1 个制表符就可以，例如，下面的输入：</p>
<p>这是一个普通段落：</p>
<pre><code>这是一个代码区块。</code></pre><p>Markdown 会转换成：</p>
<p>这是一个普通段落：</p>

<pre><code>这是一个代码区块。
</code></pre>
<p>这个每行一阶的缩进（4 个空格或是 1 个制表符），都会被移除，例如：</p>
<p>Here is an example of AppleScript:</p>
<pre><code>tell application &quot;Foo&quot;
    beep
end tell</code></pre><p>会被转换为：</p>
<p>Here is an example of AppleScript:</p>

<pre><code>tell application "Foo"
    beep
end tell
</code></pre>
<p>一个代码区块会一直持续到没有缩进的那一行（或是文件结尾）。</p>
<p>在代码区块里面， & 、 < 和 > 会自动转成 HTML 实体，这样的方式让你非常容易使用 Markdown 插入范例用的 HTML 原始码，只需要复制贴上，再加上缩进就可以了，剩下的 Markdown 都会帮你处理，例如：</p>
<pre><code>&lt;div class=&quot;footer&quot;&gt;
    &amp;copy; 2004 Foo Corporation
&lt;/div&gt;</code></pre><p>会被转换为：</p>
<pre><code>&lt;div class="footer"&gt;
    &amp;copy; 2004 Foo Corporation
&lt;/div&gt;
</code></pre>
<p>代码区块中，一般的 Markdown 语法不会被转换，像是星号便只是星号，这表示你可以很容易地以 Markdown 语法撰写 Markdown 语法相关的文件。</p>
<p>分隔线</p>
<p>你可以在一行中用三个以上的星号、减号、底线来建立一个分隔线，行内不能有其他东西。你也可以在星号或是减号中间插入空格。下面每种写法都可以建立分隔线：</p>
<hr>
<hr>
<hr>
<hr>
<hr>
<p>区段元素</p>
<p>链接</p>
<p>Markdown 支持两种形式的链接语法： 行内式和参考式两种形式。</p>
<p>不管是哪一种，链接文字都是用 [方括号] 来标记。</p>
<p>要建立一个行内式的链接，只要在方块括号后面紧接着圆括号并插入网址链接即可，如果你还想要加上链接的 title 文字，只要在网址后面，用双引号把 title 文字包起来即可，例如：</p>
<p>This is <a href="http://example.com/" title="Title" target="_blank" rel="noopener">an example</a> inline link.</p>
<p><a href="http://example.net/" target="_blank" rel="noopener">This link</a> has no title attribute.<br>会产生：</p>
<p>This is <a href="http://example.com/" title="Title" target="_blank" rel="noopener">
an example</a> inline link.</p>

<p><a href="http://example.net/" target="_blank" rel="noopener">This link</a> has no
title attribute.</p>
如果你是要链接到同样主机的资源，你可以使用相对路径：

<p>See my <a href="/about/">About</a> page for details.<br>参考式的链接是在链接文字的括号后面再接上另一个方括号，而在第二个方括号里面要填入用以辨识链接的标记：</p>
<p>This is <a href="http://example.com/" title="Optional Title Here" target="_blank" rel="noopener">an example</a> reference-style link.<br>你也可以选择性地在两个方括号中间加上一个空格：</p>
<p>This is [an example] <a href="http://example.com/" title="Optional Title Here" target="_blank" rel="noopener">id</a> reference-style link.<br>接着，在文件的任意处，你可以把这个标记的链接内容定义出来：</p>
<p>链接内容定义的形式为：</p>
<p>方括号（前面可以选择性地加上至多三个空格来缩进），里面输入链接文字<br>接着一个冒号<br>接着一个以上的空格或制表符<br>接着链接的网址<br>选择性地接着 title 内容，可以用单引号、双引号或是括弧包着<br>下面这三种链接的定义都是相同：</p>
<p>请注意：有一个已知的问题是 Markdown.pl 1.0.1 会忽略单引号包起来的链接 title。</p>
<p>链接网址也可以用方括号包起来：</p>
<p>你也可以把 title 属性放到下一行，也可以加一些缩进，若网址太长的话，这样会比较好看：</p>
<p>网址定义只有在产生链接的时候用到，并不会直接出现在文件之中。</p>
<p>链接辨别标签可以有字母、数字、空白和标点符号，但是并不区分大小写，因此下面两个链接是一样的：</p>
<p>[link text][a]<br>[link text][A]<br>隐式链接标记功能让你可以省略指定链接标记，这种情形下，链接标记会视为等同于链接文字，要用隐式链接标记只要在链接文字后面加上一个空的方括号，如果你要让 "Google" 链接到 google.com，你可以简化成：</p>
<p><a href="http://google.com/" target="_blank" rel="noopener">Google</a><br>然后定义链接内容：</p>
<p>由于链接文字可能包含空白，所以这种简化型的标记内也许包含多个单词：</p>
<p>Visit <a href="http://daringfireball.net/" target="_blank" rel="noopener">Daring Fireball</a> for more information.<br>然后接着定义链接：</p>
<p>链接的定义可以放在文件中的任何一个地方，我比较偏好直接放在链接出现段落的后面，你也可以把它放在文件最后面，就像是注解一样。</p>
<p>下面是一个参考式链接的范例：</p>
<p>I get 10 times more traffic from <a href="http://google.com/" target="_blank" rel="noopener">Google</a> <a href="http://google.com/" title="Google" target="_blank" rel="noopener">1</a> than from<br><a href="http://search.yahoo.com/" title="Yahoo Search" target="_blank" rel="noopener">Yahoo</a> <a href="http://search.yahoo.com/" title="Yahoo Search" target="_blank" rel="noopener">2</a> or <a href="http://search.msn.com/" title="MSN Search" target="_blank" rel="noopener">MSN</a> <a href="http://search.msn.com/" title="MSN Search" target="_blank" rel="noopener">3</a>.</p>
<p>如果改成用链接名称的方式写：</p>
<p>I get 10 times more traffic from <a href="http://google.com/" target="_blank" rel="noopener">Google</a> than from<br><a href="http://search.yahoo.com/" title="Yahoo Search" target="_blank" rel="noopener">Yahoo</a> or <a href="http://search.msn.com/" title="MSN Search" target="_blank" rel="noopener">MSN</a>.</p>
<p>上面两种写法都会产生下面的 HTML。</p>
<p>I get 10 times more traffic from <a href="http://google.com/" title="Google" target="_blank" rel="noopener">Google</a> than from
<a href="http://search.yahoo.com/" title="Yahoo Search" target="_blank" rel="noopener">Yahoo</a>
or <a href="http://search.msn.com/" title="MSN Search" target="_blank" rel="noopener">MSN</a>.</p>
下面是用行内式写的同样一段内容的 Markdown 文件，提供作为比较之用：

<p>I get 10 times more traffic from <a href="http://google.com/" title="Google" target="_blank" rel="noopener">Google</a><br>than from <a href="http://search.yahoo.com/" title="Yahoo Search" target="_blank" rel="noopener">Yahoo</a> or<br><a href="http://search.msn.com/" title="MSN Search" target="_blank" rel="noopener">MSN</a>.<br>参考式的链接其实重点不在于它比较好写，而是它比较好读，比较一下上面的范例，使用参考式的文章本身只有 81 个字符，但是用行内形式的却会增加到 176 个字元，如果是用纯 HTML 格式来写，会有 234 个字元，在 HTML 格式中，标签比文本还要多。</p>
<p>使用 Markdown 的参考式链接，可以让文件更像是浏览器最后产生的结果，让你可以把一些标记相关的元数据移到段落文字之外，你就可以增加链接而不让文章的阅读感觉被打断。</p>
<p>强调</p>
<p>Markdown 使用星号（*）和底线（_）作为标记强调字词的符号，被 * 或 _ 包围的字词会被转成用 <em> 标签包围，用两个 * 或 _ 包起来的话，则会被转成 <strong>，例如：</strong></em></p>
<p><em>single asterisks</em></p>
<p><em>single underscores</em></p>
<p><strong>double asterisks</strong></p>
<p><strong>double underscores</strong><br>会转成：</p>
<p><em>single asterisks</em></p>
<p><em>single underscores</em></p>
<p><strong>double asterisks</strong></p>
<p><strong>double underscores</strong><br>你可以随便用你喜欢的样式，唯一的限制是，你用什么符号开启标签，就要用什么符号结束。</p>
<p>强调也可以直接插在文字中间：</p>
<p>un<em>frigging</em>believable<br>但是如果你的 * 和 _ 两边都有空白的话，它们就只会被当成普通的符号。</p>
<p>如果要在文字前后直接插入普通的星号或底线，你可以用反斜线：</p>
<p>*this text is surrounded by literal asterisks*<br>代码</p>
<p>如果要标记一小段行内代码，你可以用反引号把它包起来（`），例如：</p>
<p>Use the <code>printf()</code> function.<br>会产生：</p>
<p>Use the <code>printf()</code> function.</p>
如果要在代码区段内插入反引号，你可以用多个反引号来开启和结束代码区段：

<p><code>There is a literal backtick (`) here.</code><br>这段语法会产生：</p>
<p><code>There is a literal backtick (`) here.</code></p>
代码区段的起始和结束端都可以放入一个空白，起始端后面一个，结束端前面一个，这样你就可以在区段的一开始就插入反引号：

<p>A single backtick in a code span: <code>`</code></p>
<p>A backtick-delimited string in a code span: <code>`foo`</code><br>会产生：</p>
<p>A single backtick in a code span: <code>`</code></p>

<p>A backtick-delimited string in a code span: <code>`foo`</code></p>
在代码区段内，& 和方括号都会被自动地转成 HTML 实体，这使得插入 HTML 原始码变得很容易，Markdown 会把下面这段：

<p>Please don't use any <code>&lt;blink&gt;</code> tags.<br>转为：</p>
<p>Please don't use any <code>&lt;blink&gt;</code> tags.</p>
你也可以这样写：

<p><code>&amp;#8212;</code> is the decimal-encoded equivalent of <code>&amp;mdash;</code>.<br>以产生：</p>
<p><code>&amp;#8212;</code> is the decimal-encoded
equivalent of <code>&amp;mdash;</code>.</p>
图片

<p>很明显地，要在纯文字应用中设计一个「自然」的语法来插入图片是有一定难度的。</p>
<p>Markdown 使用一种和链接很相似的语法来标记图片，同样也允许两种样式： 行内式和参考式。</p>
<p>行内式的图片语法看起来像是：</p>
<p><img src="/path/to/img.jpg" alt="Alt text"></p>
<p><img src="/path/to/img.jpg" alt="Alt text" title="Optional title"><br>详细叙述如下：</p>
<p>一个惊叹号 !<br>接着一个方括号，里面放上图片的替代文字<br>接着一个普通括号，里面放上图片的网址，最后还可以用引号包住并加上 选择性的 'title' 文字。<br>参考式的图片语法则长得像这样：</p>
<p><img src="http://example.com/" alt="Alt text" title="Optional Title Here"><br>「id」是图片参考的名称，图片参考的定义方式则和连结参考一样：</p>
<p>到目前为止， Markdown 还没有办法指定图片的宽高，如果你需要的话，你可以使用普通的 <img> 标签。</p>
<p>其它</p>
<p>自动链接</p>
<p>Markdown 支持以比较简短的自动链接形式来处理网址和电子邮件信箱，只要是用方括号包起来， Markdown 就会自动把它转成链接。一般网址的链接文字就和链接地址一样，例如：</p>
<p><a href="http://example.com/" target="_blank" rel="noopener">http://example.com/</a><br>Markdown 会转为：</p>
<p><a href="http://example.com/" target="_blank" rel="noopener">http://example.com/</a><br>邮址的自动链接也很类似，只是 Markdown 会先做一个编码转换的过程，把文字字符转成 16 进位码的 HTML 实体，这样的格式可以糊弄一些不好的邮址收集机器人，例如：</p>
<p><a href="mailto:&#x61;&#x64;&#x64;&#x72;&#101;&#x73;&#x73;&#64;&#101;&#120;&#97;&#x6d;&#112;&#x6c;&#x65;&#46;&#x63;&#x6f;&#109;" target="_blank" rel="noopener">&#x61;&#x64;&#x64;&#x72;&#101;&#x73;&#x73;&#64;&#101;&#120;&#97;&#x6d;&#112;&#x6c;&#x65;&#46;&#x63;&#x6f;&#109;</a><br>Markdown 会转成：</p>
<p><a href="&#x6D;&#x61;i&#x6C;&#x74;&#x6F;:&#x61;&#x64;&#x64;&#x72;&#x65;
&#115;&#115;&#64;&#101;&#120;&#x61;&#109;&#x70;&#x6C;e&#x2E;&#99;&#111;
&#109;">&#x61;&#x64;&#x64;&#x72;&#x65;&#115;&#115;&#64;&#101;&#120;&#x61;<br>&#109;&#x70;&#x6C;e&#x2E;&#99;&#111;&#109;</a><br>在浏览器里面，这段字串（其实是 <a href="mailto:address@example.com" target="_blank" rel="noopener">address@example.com</a>）会变成一个可以点击的「<a href="mailto:address@example.com" target="_blank" rel="noopener">address@example.com</a>」链接。</p>
<p>（这种作法虽然可以糊弄不少的机器人，但并不能全部挡下来，不过总比什么都不做好些。不管怎样，公开你的信箱终究会引来广告信件的。）</p>
<p>反斜杠</p>
<p>Markdown 可以利用反斜杠来插入一些在语法中有其它意义的符号，例如：如果你想要用星号加在文字旁边的方式来做出强调效果（但不用 <em> 标签），你可以在星号的前面加上反斜杠：</em></p>
<p>*literal asterisks*<br>Markdown 支持以下这些符号前面加上反斜杠来帮助插入普通的符号：</p>
<p>\   反斜线<br>`   反引号</p>
<ul>
<li>星号<br>_   底线<br>{}  花括号<br>[]  方括号<br>()  括弧<h1 id="井字号"><a href="#井字号" class="headerlink" title="井字号"></a>井字号</h1></li>
</ul>
<ul>
<li>加号</li>
</ul>
<ul>
<li>减号<br>.   英文句点<br>!   惊叹号</li>
</ul>
</pre></table></div></p>]]></content>
      <categories>
        <category>-其他</category>
      </categories>
  </entry>
  <entry>
    <title>关于gitlab的deploy无权push的问题</title>
    <url>/2017/04/27/Linux/%E5%85%B3%E4%BA%8Egitlab%E7%9A%84deploy%E6%97%A0%E6%9D%83push%E7%9A%84%E9%97%AE%E9%A2%98/</url>
    <content><![CDATA[<p>今天准备两个人协同开发一个项目，然后建立了一个组。一开始是给了deploye权限，<br>但是他说没有权限上传，我就把权限提高但是我把权限提到master之后还是不行，清缓存<br>什么的都做了还是不行，提交的时候一直出现以下错误：<br>错误提示：</p>
<a id="more"></a>
<blockquote>
<p>git -c diff.mnemonicprefix=false -c core.quotepath=false push -v origin master:master<br>Pushing to <a href="http://xxx/xxx/xxx_HTML.git" target="_blank" rel="noopener">http://xxx/xxx/xxx_HTML.git</a></p>
</blockquote>
<blockquote>
<p>POST git-receive-pack (47642 bytes)</p>
</blockquote>
<blockquote>
<p>remote: GitLab: You don’t have permission[K</p>
</blockquote>
<blockquote>
<p>To <a href="http://xxx/xxx/xxx_HTML.git" target="_blank" rel="noopener">http://xxx/xxx/xxx_HTML.git</a><br>! [remote rejected] master -&gt; master (pre-receive hook declined)<br>error: failed to push some refs to ‘<a href="http://xxx/xxx/xxx_HTML.git&#39;" target="_blank" rel="noopener">http://xxx/xxx/xxx_HTML.git&#39;</a></p>
</blockquote>
<p>最后还是找到了解决方案了，主要就是项目一般对于master分支对组中的成员是受保护的，具体方案如下：<br>找到项目：<br><img src="http://i1.piimg.com/567571/9c5d32d60ef4a803.png" alt><br>在项目的【Setting】中的【Protected branches】可以设置哪些分支是被保护的，默认情况下【master】分支是处于被保护状态下的，develop角色的人是无法提交到master分支的，在下面的【Developers can push】打上钩就可以了。<br><img src="http://i4.buimg.com/567571/f6d4e2adbfb1487a.png" alt></p>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
      <tags>
        <tag>工具</tag>
      </tags>
  </entry>
  <entry>
    <title>Dell黑苹果折腾</title>
    <url>/2017/03/12/Mac/Dell%E9%BB%91%E8%8B%B9%E6%9E%9C%E6%8A%98%E8%85%BE/</url>
    <content><![CDATA[<p><img src="http://p1.bpimg.com/567571/649a7494ac089099.jpg" alt></p>
<p>早在大一的时候就开始折腾黑苹果了，当时第一次看到mac的界面的时候我就深深的爱上了这款操作系统，而且最重要的是他是集Linux与Windows的优点于一身的系统。<a id="more"></a>我喜欢Linux但是有时需要一些Windows程序的时候wine并不能给我很好的支持，各种问题让我很多时候并不是在编码而是一直处在折腾操作系统的处境，虽然也能学到一些东西，但是我用这个操作系统的根本目的其实还是编码，这样一来反而成为拖累我的累赘。Linux下最好的莫过于他的终端了，迅速简洁的完成各项工做。Windows则是什么东西都有但是很多时候需要安装一些东西例如依赖，对css进行编译各种莫名其妙的问题，或许是版本或许是网络的问题，有些在终端下很简单的操作变得非常复杂，但是很多其他的软件在使用上真的是无可挑剔，一些调用机制写的很完善。<br>最后我选择了一个折中的办法就是Mac，它集中了两个我最喜欢的功能，终端和较多满足日常需要的应用软件，只是有些操作感觉有些怪怪的毕竟Windows用的时间太长了。<br>目前在我电脑上的这个黑苹果基本是完美驱动了，就差一个WiFi无解，intel的无线网卡貌似都不行，而且网卡的接口设计比较奇怪，市面上不容易找到又与之相匹配又容易驱动的无线网卡了，倒也无所谓目前主要用的还是有线。</p>
<h2 id="制作启动盘"><a href="#制作启动盘" class="headerlink" title="制作启动盘"></a>制作启动盘</h2><p> 首先需要下载mac镜像可以直接下载带有Colver的镜像，使用transMac烧录。<br> 烧录的时间可能会有点长，一般完成了以后你只能看到一个EFI分区，这个是引导分区，U盘剩下的空间装的都是mac操作系统。放入一些必要的驱动文件一般三个左右，最重要的是config.plist这个文件如果有错误系统无法启动，所以可能有很多时间都是花费在调节这个文件然后系统引导成功。如果运气比较好的话可能一次就直接进入系统了，这个时候仅仅需要安装。</p>
<h2 id="安装操作系统"><a href="#安装操作系统" class="headerlink" title="安装操作系统"></a>安装操作系统</h2><p>安装操作系统这一步还是很简单的，开始使用磁盘工具抹掉你要安装的位置，然后开始安装，安装一半第一次重启以后你会看到一个新的mac引导，这个时候还是选择从U盘启动，继续安装操作系统，在完全第二次自动重启以后就可以选择从硬盘启动了，这个时候需要做一点点的设置，之后就完成安装了。</p>
<h2 id="安装驱动"><a href="#安装驱动" class="headerlink" title="安装驱动"></a>安装驱动</h2><p>进入系统以后你会发现可能你的界面掉帧，没有网络，没有声音。这是因为还没有安装驱动，建议一开始不要放到系统的驱动目录下，那样不仅消耗时间而且如果驱动出错导致系统完全无法启动，建议将驱动先放在U盘的kexts文件夹下面，一次次测试等到完全通过以后在放进去，重建缓存。</p>
<h2 id="DSDT"><a href="#DSDT" class="headerlink" title="DSDT"></a>DSDT</h2><p>DSDT也是为了解决一些很棘手的问题，例如不能休眠，不能显示电量，自动重启啊什么的，首先需要生成本地的ACPI二进制文件可以在Colver中使用F4即可生成，然后将生成的二进制文件反汇编成文本文件即以dsl结尾的文件，这种文件可以直接使用工具打开修改，然后在编译成二进制的文件即aml结尾的文件放到引导目录下。</p>
]]></content>
      <tags>
        <tag>Mac</tag>
      </tags>
  </entry>
  <entry>
    <title>我的2016</title>
    <url>/2017/02/27/Life/%E6%88%91%E7%9A%842016/</url>
    <content><![CDATA[<p>​    每一年过去，我都会在我的博客记录这一年的总结，回顾过去一年的得失，又可以展望一下新的一年。每次看到这些文字就好像看到了我这一年的旅程。</p>
<a id="more"></a>
<h2 id="网络中心"><a href="#网络中心" class="headerlink" title="网络中心"></a>网络中心</h2><p>说到2016，那就不得不提网络中心，也就是在这我才开始真正接触Web开发。在大一上学期时候偶然的接触到了Linux和Github体会到了开源的强大，不知不觉的就被Linux吸引了。当时就尝试Linux的各种发行版Debian，Kali，CentOS，Arch，Gentoo记得当时最多的时候是电脑上有六个操作系统。不过也就是在那个时候学会了内核编译，系统编译，vim，make，各种服务搭建。当时看到网络中心招人的时候，我还是挺想去的，本来是去那想当一名Linux系统管理员，最后没成想上了PHP的贼船。<br>因为网络中心的主力开发语言是PHP，所以首先就得先学一下原生PHP，然后让我们在看几个框架。最开始Web开发给我的印象感觉很简单，估计就是HTML+CSS+JS然后就能开发网站了，和C++、JAVA那种编译型语言差远了。直到那个时候我才发现我错了，单不说系统开发，就一个简单的网站必须要有HTML，CSS，JavaScript，PHP，Mysql，Linux，最少还要懂一些安全，防止XSS攻击和SQL注入等等安全问题。<br>用一个词来形容当时的我再合适不过就是“浮躁”。当时想为了学到更多，了解更多，尽快的接触到开发。大概三四天就把原生PHP学完了，<br>然后就开始接触框架ThinkPHP，CakePHP，Yii框架。反正当时是硬着头皮去看文档，对于里面提到的MVC三层架构，RBAC权限模型，AOP切面编程是感觉一头雾水，但当时还是把手册给翻完了，可能是为了寻求心理安慰。到最后也只用了ThinkPHP框架写了一个登陆留言系统，然后又转向Shell脚本，ruby on rails ，Anglar Js。就这样上半学期就算在各种折腾和浮躁中结束了。</p>
<h2 id="烛照"><a href="#烛照" class="headerlink" title="烛照"></a>烛照</h2><p>暑假的时候也没太闲着，在家只待了六天就赶到学校了。因为当时在这边找了一个Web公司的实习机会。公司的老板是一个西大的学长，是一个出色的Python程序员，幽默，认真。以前是绿盟公司的面试官，也是我的第一个面试官，一开始我还是挺紧张的，大概聊了四五分钟后情绪基本就稳定下来了，然后开始问一些关于Linux和Web前端的问题，虽然问题不是很难，但是我感觉回答的不太满意。后来我想了一些，应该还是自己对技术太浮躁了，太过眼高手低了。<br>接下来的主要工作就是关于服务器和Web前端。搭建mail服务器，开发机，Linux镜像打包，Docker，Ansible，持续集成。其实很多东西以前听说过有的用过，只是在用于生产环境以后就不得不去考虑更多，降低后来人的学习使用成本。然后那些不会的就自行Google而且老板和同事也特别热心，开发机的思路都是他帮我提供的。当时工作一直比较认真，和老板还有同事的关系也很好。<br>平常大家工作都挺努力的，休息的时候气氛特别活跃。其实我挺喜欢那种一群年轻人为了同一个目标聚在一起朝着一个方向努力，然后又不失活跃的那种。在那的一个半月对我的技术和态度有了很大影响，在公司工作就必须对自己的工作负责，认真做好。还有就是这个行业个人永远无法和团队相比的，一个人单枪匹马的是很难做出优秀的产品，尤其是在互联网发展如此迅速的今天，等一个人开发出第一个版本说不定这个技术早就已经过时了。</p>
<h2 id="NwuPT（嘉木）"><a href="#NwuPT（嘉木）" class="headerlink" title="NwuPT（嘉木）"></a>NwuPT（嘉木）</h2><p>解释一下嘉木是一个ipv6资源分享站点，相对于目前的学校计费的ipv4所有的资源都是免流量下载的。其实这个项目早在2016年年初就开始了，发起人是一个大四的学长。当时他也是找了我好几次，让我也参与，可是一直腾不开时间。然后招募的其他程序员也没在写东西。大概是9月中旬的时候我开始接手这个项目，但是确实找不到开发人员。然后我想着先自己写一份代码，等站点运行起来了以后在招募，没想到这个项目竟然写了半年，大概在12月中旬才勉强上线，上线以后还有各种问题又修修补补等到放假才算基本稳定。当时开发和服务器运维都是我一个人在做，真的是精力跟不上。有时候上课都在想有些地方怎么解决，然后期末成绩也感觉一塌糊涂。不过每次看到站点运行起来了心里还是很兴奋的，毕竟投入了那么大的精力。<br>然后在寒假又花了几天重新修改了代码，目前还有点问题没有完全解决。还在测试中，总之这次没问题了以后就准备开放源代码，为开源做点贡献，也让代码更加的丰富起来，毕竟一个人的能力有限。也希望在座的有意向的同学也能参与进来。</p>
<h2 id="寒假"><a href="#寒假" class="headerlink" title="寒假"></a>寒假</h2><p>其实每年寒假我都挺期待过年的，虽然说现在过年没有了以前过年时候的那种浓浓的年味，但我还是很期待。过年的时候就可以见到很多很久很久没见到的亲人，每年大家都挺忙的，很少有见面的机会，能聚在一起会感到挺温暖的。<br>有空的时候经常也和高中同学出去，一起聊聊天，回想以前的高中生活，那时候很苦，当时唯一的想法就是赶紧上大学，可现在上了大学又挺怀念高中的生活。<br>剩下的时间就陪爸妈聊聊天，看了一点框架的源码，用ThinkPHP写了一个Linux镜像站，目前在解决服务器问题，也快要上线了。</p>
<h2 id="2017展望"><a href="#2017展望" class="headerlink" title="2017展望"></a>2017展望</h2><p>对于我来说2017算是一个比较关键的一年，希望我能在2017年做好以下几件事，也算是和大家共勉：<br>坚持早睡，在寒假算是基本养成了，以前总是晚上休息不好，白天一直头疼，从上大学一直持续到现在。睡眠充足后很少出现这样的情况了。<br>坚持阅读，我希望2017一定不能把这件事落下，无论多忙，只有阅读才能使自己提升，无论是技术方面，还是性格修养。阅读是唯一一个短期内可能没什么太大帮助，但是长期积累下来一定会对你有巨大影响的一件事。<br>坚持写博客，写博客不仅仅是对自己的知识的巩固。而且如果你经常在网络上找资料就会发现好多博客是互相抄袭，最可气的还不能解决问题，可以让自己的博客真正帮助开发者解决一些实际问题。</p>
<pre><code>最后，人不是独立的个体，活在世上不管是有意还是无形，总归会受到一些人的帮助，所以要始终心存感恩。</code></pre>]]></content>
      <categories>
        <category>杂记</category>
      </categories>
  </entry>
  <entry>
    <title>PT站开始内测啦</title>
    <url>/2016/10/09/PT/PT%E7%AB%99%E5%BC%80%E5%A7%8B%E5%86%85%E6%B5%8B%E5%95%A6/</url>
    <content><![CDATA[<p><img src="http://i1.piimg.com/567571/fb0c9df95dd0eb7c.png" alt><br>从暑假一直就在筹备这件事情，但是最后由于种种原因一直搁置到了开学。在中秋的时候正式开始着手做这件事情，基本每天都在改代码，前后台夹杂，改起来比较复杂，所以改到现在前台还不是很好看。<a id="more"></a>但是还是想先用起来，目前已经把文件结构基本清楚了，所以先做一次内测，没大问题先用一段时间<br>在准备再将前台重写…</p>
<h2 id="那么什么是PT站"><a href="#那么什么是PT站" class="headerlink" title="那么什么是PT站?"></a>那么什么是PT站?</h2><p>咱们普通上网使用的是Ipv4协议上网，我们说的ip也是说的ipv4的地址，这个一般是我们需要在运营商购买的。然而还有一个类似于ipv4的协议<br>叫做ipv6，虽然它比ipv4更有优势，但是目前ipv6基本属于空闲状态，所以在运营商那里买的话也很便宜。一般学校从运营商买的网都是带有ipv6的，我们就可以<br>使用这部分空闲资源。但是咱学校不是限流量吗，这好像并没有什么用。值得注意的是用ipv4上网使用的是ipv4的流量，而用ipv6上网使用的是ipv6的流量，所以说<br>两者独立，而学校只计算ipv4的流量，那么咱们使用的ipv6就可以算作不计流量了。<br>ok，说了那么多就是ipv4需要计算流量，ipv6不计算流量，而PT站使用的ipv6，所以ipv6是免流的。</p>
<h2 id="怎么使用PT站？"><a href="#怎么使用PT站？" class="headerlink" title="怎么使用PT站？"></a>怎么使用PT站？</h2><p>首先是最最重要的就是如果你使用的是360的浏览器请务必要把浏览器的模式改成极速模式，千万不要使用兼容模式，由于这个站点的框架不支持IE8及以下的浏览器<br>如果直接访问的话，很可能会出现网页排版错乱的情况。之后我会尽快再写一版主题，解决这个问题。<br>修改方法如下图：<br><img src="http://p1.bpimg.com/567571/b14c6eff8d10b108.png" alt></p>
]]></content>
      <categories>
        <category>PT站</category>
      </categories>
  </entry>
  <entry>
    <title>秒速5厘米</title>
    <url>/2016/10/04/Life/%E7%A7%92%E9%80%9F5%E5%8E%98%E7%B1%B3/</url>
    <content><![CDATA[<p><img src="http://p1.bqimg.com/567571/dfc16c8ac799e74f.jpg" alt></p>
<h2 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h2><p>开学的第二周晚上停电，也就在那晚看完了樱花抄，今天又看完了宇航员和秒速五厘米。樱花抄也是我最喜欢的一部分，一个唯美的名字…<br>少年时形影不离的朋友，从互相有好感，<a id="more"></a>到一次次的分别，每次分别让两个人的距离越来越远，让彼此见面的机会越来越少。年少的的互相倾慕，追寻纯真的爱情。<br><img src="http://p1.bqimg.com/567571/ef9f98620d07f094.jpg" alt><br>贵树与明里约好在车站相见，那份紧张与兴奋想必是少年时都会有的心情。从早上开始下着寒冷的雨，中午时雨又变成了雪。<br>放学后，贵树按约定乘坐电车前往岩舟车站。雪越来越大，时间好像被无限的拉长，当路程到一半时，已经到了约定的时间。急切，不安，失望…<br>当到达约定的车站时已经是十一点，车站空无一人。贵树失望的走进车站，看到竟是孤单一人的明里，明里抓着贵树的衣角抽泣着。委屈，心疼，开心….<br>候车室内贵树吃过明里做的茶点，一同前往其信中的樱花树，大雪过后的雪地留下了两人歪歪斜斜的脚印，在树下相拥亲吻…<br>那天晚上，两人在田边休息了一晚。次日，贵树乘坐早上的第一班电车和明里告别了,那时的天空显得特别的高远，清晰，真实。<br><img src="http://p1.bqimg.com/567571/13da2a78312f66c0.jpg" alt><br>秒速5厘米篇中明里和贵树也会经常做梦，梦到那段青涩的爱恋，那时的明里也要结婚了，贵树和三年的女朋友分手了。相遇总是很微妙的事情，在贵树辞去工作，明里结婚后的一个下午<br>他们擦肩而过，贵树下定决心转身时，疾驰而过的列车挡住了他的视线，列车通过后明里早已离去。<br>青春，像一杯清茶，弥漫芳香。每个人都经历过青春，过程或快乐，或悲伤，但终是逃不过时间的流逝。<br>再美好的青春也总有那么多缺憾，无法弥补，无法追逐，只能放在记忆的脑海里，偶尔怀念，怀念曾今那样的自己，怀念曾经不可一世的感情。<br>我们在成长的道路上渐行渐远，回首间突然发现，曾经不顾一切追逐的事情，或许转瞬间就已遥不可及。<br>我们每个人都如同电影里的主人公，不断相遇，不断地错过，难逃命运的着捉弄，于是乎才明白，当初为何不把想做的事做完，<br>想说的话亲口说给想见的人听，时间就是这样无情，你离开了，错过了当时想要表达心意的机会，便是一生的遗憾，却也缺憾成美，流淌在记忆的时光里缅怀那段青涩的爱恋！</p>
]]></content>
      <categories>
        <category>杂记</category>
      </categories>
  </entry>
  <entry>
    <title>关于apache端口被占用</title>
    <url>/2016/04/04/Linux/%E5%85%B3%E4%BA%8Eapache%E7%AB%AF%E5%8F%A3%E8%A2%AB%E5%8D%A0%E7%94%A8/</url>
    <content><![CDATA[<p><img src="http://p1.bpimg.com/567571/d081845d56c7f263.jpg" alt></p>
<h1 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h1><p>无论我们在安装单个的apache还是装集成环境xampp都是有可能遇到apache意外停止。查看错误日志会发现一般都是端口被占用，一般是被虚拟机占用了，这时一般有两种方法，改apache的端口号，或者把占用apache的端口号的那个服务干掉。</p>
<a id="more"></a>

<h2 id="方案一：改端口号"><a href="#方案一：改端口号" class="headerlink" title="方案一：改端口号"></a>方案一：改端口号</h2><p>在apache的配置文件里找到httpd-ssl.conf文件然后ctrl+f查找port然后改成444或者其他的，只要没被占用的就可以，然后重启apache。MySQL出问题了也是这样的解决方案。</p>
<h2 id="方案二：杀占用的服务"><a href="#方案二：杀占用的服务" class="headerlink" title="方案二：杀占用的服务"></a>方案二：杀占用的服务</h2><p>win+r打开运行，输入cmd打开命令提示符，输入</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">netstat -ano</span><br></pre></td></tr></table></figure>


<p>找到占用443端口的服务，找到他的pid ，然后输入</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">kill</span> /f /pid 端口号</span><br></pre></td></tr></table></figure>


<p>就能结束此服务，然后重启apache即可。</p>
<p>也可以使用任务管理器，停掉对应的服务。 </p>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
  </entry>
  <entry>
    <title>Linux小记</title>
    <url>/2016/03/20/Linux/Linux%E5%B0%8F%E8%AE%B0/</url>
    <content><![CDATA[<h1 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h1><h2 id="定义别名："><a href="#定义别名：" class="headerlink" title="定义别名："></a>定义别名：</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">alias</span> vi=<span class="string">'vim'</span></span><br></pre></td></tr></table></figure>

<h2 id="查看别名："><a href="#查看别名：" class="headerlink" title="查看别名："></a>查看别名：</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">alias</span></span><br></pre></td></tr></table></figure>

<h2 id="让某个用户永久生效"><a href="#让某个用户永久生效" class="headerlink" title="让某个用户永久生效"></a>让某个用户永久生效</h2><a id="more"></a>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim ~/.bashrc</span><br></pre></td></tr></table></figure>

<h2 id="删除别名"><a href="#删除别名" class="headerlink" title="删除别名"></a>删除别名</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ualias vi</span><br></pre></td></tr></table></figure>

<h2 id="快捷键："><a href="#快捷键：" class="headerlink" title="快捷键："></a>快捷键：</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ctrl+u  删除或剪切光标左侧的所有字符</span><br><span class="line">ctrl+y  粘贴</span><br><span class="line">ctrl+r 	搜索历史命令</span><br><span class="line">ctrl+d	推出登陆</span><br><span class="line">ctrl+z  暂停放入后台</span><br><span class="line">ctrl+l 	清屏</span><br><span class="line">ctrl+a 	光标移到开头</span><br><span class="line">ctrl+e  光标移到结尾</span><br></pre></td></tr></table></figure>


<h2 id="是任意多个任意字符-任意可以是0"><a href="#是任意多个任意字符-任意可以是0" class="headerlink" title="*是任意多个任意字符(任意可以是0)"></a>*是任意多个任意字符(任意可以是0)</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">	ls *abc</span><br><span class="line">```	</span><br><span class="line">会显示所有以abc结尾的或者就是abc的文件</span><br><span class="line"></span><br><span class="line"><span class="comment">## []匹配括号中的任意一个，必须是一个。</span></span><br><span class="line"></span><br><span class="line">``` bash</span><br><span class="line">	ls [abc]df</span><br></pre></td></tr></table></figure>


<p>匹配以abc其中一个开始，以df结尾的，文件名是三个字符</p>
<h2 id="？这个是匹配任意一个字符"><a href="#？这个是匹配任意一个字符" class="headerlink" title="？这个是匹配任意一个字符"></a>？这个是匹配任意一个字符</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls ?asc</span><br></pre></td></tr></table></figure>

<pre><code>四个字符，以asc结尾</code></pre><h2 id="与2同只是取反"><a href="#与2同只是取反" class="headerlink" title="[^]与2同只是取反"></a>[^]与2同只是取反</h2><h2 id="‘’单引号中所有的特殊符号都没有特殊的含义"><a href="#‘’单引号中所有的特殊符号都没有特殊的含义" class="headerlink" title="‘’单引号中所有的特殊符号都没有特殊的含义"></a>‘’单引号中所有的特殊符号都没有特殊的含义</h2><h2 id="“”双引号特殊符号都有特殊意义"><a href="#“”双引号特殊符号都有特殊意义" class="headerlink" title="“”双引号特殊符号都有特殊意义"></a>“”双引号特殊符号都有特殊意义</h2><h2 id="反引号等价于-里面的系统命令会先执行反引号和括号里面的命令"><a href="#反引号等价于-里面的系统命令会先执行反引号和括号里面的命令" class="headerlink" title="``反引号等价于$()里面的系统命令会先执行反引号和括号里面的命令"></a>``反引号等价于$()里面的系统命令会先执行反引号和括号里面的命令</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> `ls`</span><br></pre></td></tr></table></figure>

<p>显示本目录下的文件名</p>
<h2 id="去掉特殊符号的意义"><a href="#去掉特殊符号的意义" class="headerlink" title="\去掉特殊符号的意义"></a>\去掉特殊符号的意义</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">echo</span> \<span class="variable">$date</span></span><br></pre></td></tr></table></figure>

<p>显示的是$date去掉了$的特殊意义 </p>
<h2 id="清空历史命令："><a href="#清空历史命令：" class="headerlink" title="清空历史命令："></a>清空历史命令：</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">history</span> -c</span><br></pre></td></tr></table></figure>

<h2 id="强制保存到历史命令文件-bash-history-中："><a href="#强制保存到历史命令文件-bash-history-中：" class="headerlink" title="强制保存到历史命令文件(~/.bash_history)中："></a>强制保存到历史命令文件(~/.bash_history)中：</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">history</span>  -w</span><br></pre></td></tr></table></figure>

<h2 id="更改历史命令保存文件存的条数"><a href="#更改历史命令保存文件存的条数" class="headerlink" title="更改历史命令保存文件存的条数"></a>更改历史命令保存文件存的条数</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">vim /etc/profile   （profile是环境变量的配置文件）</span><br><span class="line">HISTZISE=1000</span><br></pre></td></tr></table></figure>

<h2 id="历史命令的调用："><a href="#历史命令的调用：" class="headerlink" title="历史命令的调用："></a>历史命令的调用：</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">!n  //重复执行第n条命令</span><br><span class="line">!!  //重复执行上一条命令</span><br><span class="line">!字符串  //重复执行最后一条的含有字符串的命令</span><br></pre></td></tr></table></figure>

<h2 id="将错误与正确信息都存在文件里"><a href="#将错误与正确信息都存在文件里" class="headerlink" title="将错误与正确信息都存在文件里"></a>将错误与正确信息都存在文件里</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls &amp;&gt;&gt; abc   (以追加的形式)</span><br><span class="line">ls &amp;&gt;  abc  （以覆盖的方式）</span><br></pre></td></tr></table></figure>

<h2 id="普通的重定向"><a href="#普通的重定向" class="headerlink" title="普通的重定向"></a>普通的重定向</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls &gt;&gt; abc</span><br><span class="line">ls &gt;  abc</span><br></pre></td></tr></table></figure>

<h2 id="只执行命令不进行结果的输出，在脚本中经常使用"><a href="#只执行命令不进行结果的输出，在脚本中经常使用" class="headerlink" title="只执行命令不进行结果的输出，在脚本中经常使用"></a>只执行命令不进行结果的输出，在脚本中经常使用</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls &amp;&gt;/dev/null</span><br></pre></td></tr></table></figure>

<h2 id="分开重定向："><a href="#分开重定向：" class="headerlink" title="分开重定向："></a>分开重定向：</h2><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls &gt;&gt; abc 2&gt;&gt;def (正确的是放在abc，错误的放在def)</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Linux</category>
      </categories>
  </entry>
  <entry>
    <title>虚拟机的三种网络连接方式</title>
    <url>/2016/03/20/Linux/%E8%99%9A%E6%8B%9F%E6%9C%BA%E7%9A%84%E4%B8%89%E7%A7%8D%E7%BD%91%E7%BB%9C%E8%BF%9E%E6%8E%A5%E6%96%B9%E5%BC%8F/</url>
    <content><![CDATA[<p><img src="http://p1.bpimg.com/567571/cd90e3a565524afb.jpg" alt></p>
<h1 id="正文"><a href="#正文" class="headerlink" title="正文"></a>正文</h1><p> VMWare提供了三种工作模式，它们是bridged(桥接模式)、NAT(网络地址转换模式)和host-only(主机模式)。要想在网络管理和维护中合理应用它们，你就应该先了解一下这三种工作模式。</p>
<a id="more"></a>
<h2 id="1-bridged-桥接模式"><a href="#1-bridged-桥接模式" class="headerlink" title="1.bridged(桥接模式)"></a>1.bridged(桥接模式)</h2><p>　　在这种模式下，VMWare虚拟出来的操作系统就像是局域网中的一台独立的主机，它可以访问网内任何一台机器。在桥接模式下，你需要手工为虚拟 系统配置IP地址、子网掩码，而且还要和宿主机器处于同一网段，这样虚拟系统才能和宿主机器进行通信。同时，由于这个虚拟系统是局域网中的一个独立的主机 系统，那么就可以手工配置它的TCP/IP配置信息，以实现通过局域网的网关或路由器访问互联网。</p>
<p>　　使用桥接模式的虚拟系统和宿主机器的关系，就像连接在同一个Hub上的两台电脑。想让它们相互通讯，你就需要为虚拟系统配置IP地址和子网掩码，否则就无法通信。</p>
<p>　　如果你想利用VMWare在局域网内新建一个虚拟服务器，为局域网用户提供网络服务，就应该选择桥接模式。</p>
<h2 id="2-host-only-主机模式"><a href="#2-host-only-主机模式" class="headerlink" title="2.host-only(主机模式)"></a>2.host-only(主机模式)</h2><p>　　在某些特殊的网络调试环境中，要求将真实环境和虚拟环境隔离开，这时你就可采用host-only模式。在host-only模式中，所有的虚拟系统是可以相互通信的，但虚拟系统和真实的网络是被隔离开的。</p>
<p>　　提示:在host-only模式下，虚拟系统和宿主机器系统是可以相互通信的，相当于这两台机器通过双绞线互连。</p>
<p>　　在host-only模式下，虚拟系统的TCP/IP配置信息(如IP地址、网关地址、DNS服务器等)，都是由VMnet1(host-only)虚拟网络的DHCP服务器来动态分配的。</p>
<p>　　如果你想利用VMWare创建一个与网内其他机器相隔离的虚拟系统，进行某些特殊的网络调试工作，可以选择host-only模式。</p>
<h2 id="3-NAT-网络地址转换模式"><a href="#3-NAT-网络地址转换模式" class="headerlink" title="3.NAT(网络地址转换模式)"></a>3.NAT(网络地址转换模式)</h2><p>　　使用NAT模式，就是让虚拟系统借助NAT(网络地址转换)功能，通过宿主机器所在的网络来访问公网。也就是说，使用NAT模式可以实现在虚拟 系统里访问互联网。NAT模式下的虚拟系统的TCP/IP配置信息是由VMnet8(NAT)虚拟网络的DHCP服务器提供的，无法进行手工修改，因此虚 拟系统也就无法和本局域网中的其他真实主机进行通讯。采用NAT模式最大的优势是虚拟系统接入互联网非常简单，你不需要进行任何其他的配置，只需要宿主机 器能访问互联网即可。</p>
<p>　　如果你想利用VMWare安装一个新的虚拟系统，在虚拟系统中不用进行任何手工配置就能直接访问互联网，建议你采用NAT模式。</p>
<p>　　提示:以上所提到的NAT模式下的VMnet8虚拟网络，host-only模式下的VMnet1虚拟网络，以及bridged模式下的 VMnet0虚拟网络，都是由VMWare虚拟机自动配置而生成的，不需要用户自行设置。VMnet8和VMnet1提供DHCP服务，VMnet0虚拟 网络则不提供。</p>
]]></content>
      <categories>
        <category>Web</category>
      </categories>
  </entry>
  <entry>
    <title>Hexo</title>
    <url>/2016/03/09/Tools/Hexo/</url>
    <content><![CDATA[<p><img src="http://p1.bpimg.com/567571/76cdc0b676e4f4aa.png" alt></p>
<h2>动因</h2>
新学期开始了，这个学期准备养成开始写博客的好习惯。以前什么东西都是在QQ空间里，
后来觉得技术性稍微强一点的就是CSDN和博客园。但是啊我觉得他们的广告和版式确实
有点让人不忍直视，毕竟是搞技术的不如自己搭一个博客吧。于是就有了下面的折腾了。
能踩的坑我都帮你们踩过了，大家按照我的步骤一步步往下搭就好，那么现在开始吧！
# 正文
<a id="more"></a>
## 首先声明一下：
平台：windows
hexo版本：3.X
## 所需文件:
1. [Node.js] [http://nodejs.cn/]
2. [Git] (https://git-scm.com/downloads/)
（下载后直接安装就好:next，next，finsh）

<h2 id="配置hexo："><a href="#配置hexo：" class="headerlink" title="配置hexo："></a>配置hexo：</h2><p>找到刚才安装的git bash<br>然后你需要在本地硬盘里面创建一个文件夹用来存放hexo的配置文件，例如我在这里创建的地方是F：/hexo ，<br>那么我们现在打开git bash然后输入</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> F：</span><br><span class="line"><span class="built_in">cd</span> hexo</span><br></pre></td></tr></table></figure>

<p>现在你就会进入到你刚才创建的hexo文件夹了。然后就开始安装hexo了。<br>依次在git bash中输入以下内容，时间会因为网速而不同：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1.npm install hexo-cli -g</span><br><span class="line">2.hexo init</span><br><span class="line">2.npm install hexo-deployer-git --save</span><br><span class="line">3.hexo g</span><br><span class="line">4.hexo s</span><br></pre></td></tr></table></figure>

<p>这样hexo就安装完成了，下面我们要看看hexo给我们的初始界面是什么样的就执行以下命令：</p>
<p>(以//开始的是注释，不要复制，给你们说明用的。)</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">1.hexo g   //生成静态文件</span><br><span class="line">2.hexo s   //开启本地服务器，用于页面预览</span><br></pre></td></tr></table></figure>

<p>之后我们就到浏览器中看看我们的hexo长什么样子了，就在浏览器地址栏输入localhost:4000回车是不是看到了一篇hello world而且界面很漂亮。</p>
<h2 id="配置Github："><a href="#配置Github：" class="headerlink" title="配置Github："></a>配置Github：</h2><p>安装完hexo只是我们在能看到啊，要让所有人都看到我们要把它同步到github上面，就相当于吧本地文件上传到一个免费服务器，别人都看得到。<br>首先你需要注册一个github账号，搜索github然后点击注册就好，按照他的提示一步一步来，注意要把用户名记住啊，那个倒时很有用的。<br>这里我的就是xpf199741，密码是自然的。注册完了就去添加一个仓库就是叫repository的东西<br>然后取一个名字，<strong>注意仓库名字必须是“用户名.github.io”</strong>，例如我的就是<strong>xpf199741.github.io</strong>前面一定要是用户名后面的也要遵循，否则后面步骤肯定通不过。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -C <span class="string">"你的注册github的电子邮箱地址"</span></span><br></pre></td></tr></table></figure>

<p>大家在复制的时候把电子邮箱更改一下啊，有停顿时候回车就是了，然后在本地的c盘用户目录下面产生一个.ssh文件夹然后进去，打开id_rsa.pub，复制里面的全部内容。然后回到网页版的github上进入Settings，左边选择SSH Keys，Add SSH Key,title随便填，粘贴key（就是刚才复制的内容）。</p>
<p>然后回到git bash再输入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssh -T git@github.com</span><br></pre></td></tr></table></figure>

<p>如果如果是第一次的会提示是否continue，输入yes就会看到：You’ve successfully authenticated, but GitHub does not provide shell access 。这就表示已成功链接。</p>
<h2 id="链接Github与hexo："><a href="#链接Github与hexo：" class="headerlink" title="链接Github与hexo："></a>链接Github与hexo：</h2><p>编辑F：\hexo下的_config.yml文件<br>在行末添加一下内容：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">deploy: </span><br><span class="line"> 		<span class="built_in">type</span>: git</span><br><span class="line">		repository: git@github.com:xpf199741/xpf199741.github.io.git</span><br><span class="line">	 	branch: master</span><br></pre></td></tr></table></figure>

<p>把xpf199741改成你的就好了，但是注意一下：<strong>hexo的配置文件中任何’:’后面都是带一个空格的</strong><br>在git bash里输入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">hexo g</span><br><span class="line">hexo d</span><br></pre></td></tr></table></figure>

<p>执行上面的第二个命令，可能会要你输入用户名和密码，输入密码是不显示任何东西的，输完回车就可以了。<br>然后在浏览器中输入</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">xpf199741.github.io.git</span><br></pre></td></tr></table></figure>

<p>是不是在浏览器中看到了hexo的内容，不同于第一次看到的是，这次是在线的，所有人都可以看到。<br>至此你应该已经学会了搭建hexo咯。</p>
<h2 id="尾声"><a href="#尾声" class="headerlink" title="尾声"></a>尾声</h2><p>再遇到什么问题可在下面评论,之后我会继续更新hexo的美化，配置，使用。</p>
]]></content>
  </entry>
  <entry>
    <title>PhpStrom 快捷键</title>
    <url>/2016/03/09/Tools/PHPStrom%20%E5%BF%AB%E6%8D%B7%E9%94%AE/</url>
    <content><![CDATA[<p><img src="http://i1.piimg.com/567571/3d61685b42b92d47.png" alt><br>插入编辑位置    alt+鼠标左<br>跳到代码块的 开始/结束   ctrl+[ / ctrl+]<br>选中代码块的开始/结束的地方到当前位置  ctrl+shift+[ / ctrl+shift+]<br>删除行  ctrl+d<br>删除到单词结束  ctrl+delete<br>删除至单词开始  ctrl+backspace</p>
<a id="more"></a>
<p>疯狂退格  ctrl+shift+backspace<br>显示模版          ctrl+alt+shift+j<br>块注释         ctrl+shift+/<br>格式化代码     ctrl+shift+alt+l<br>alt + `   vcs<br>postfix风格补齐代码<br>$a.echo    echo $a;<br>$a.else    if(!$a){}else{}<br>$foo.fe    feach($foo as $it)<br>$a.if      if($a){}<br>$a.isset   if(isset($a)){}<br>$a.null    if($a==null)<br>$a.notnull    if($a!==null)<br>$a.var_dump   var_dump($a)<br>prof      protect function<br>prosf     protect static function<br>prif<br>prisf<br>pubf<br>pubsf<br>eco     echo “”;<br>fore      foreach<br>forek     foreach ($a $key=&gt;$value)</p>
]]></content>
  </entry>
</search>
